QB.push(
{i:691,d:1,t:"1.1",s:"AAT",diff:2,
q:"Your loop receives <code>stop_reason: \"pause_turn\"</code> while using a server tool such as web search.",
ask:"What should it do?",
a:["Send the assistant content back to continue the conversation, since the server-tool loop reached its iteration limit rather than failing",
"Treat it as an error and abort",
"Treat it as <code>end_turn</code> and return the partial answer",
"Retry the original request from scratch"],
c:0,
w:["Correct. <code>pause_turn</code> means a server-side tool loop hit its limit and the turn can be continued.",
"Nothing failed; aborting discards recoverable progress.",
"Treating it as completion returns a partial answer as if it were final.",
"Restarting discards the work already done and may hit the same limit again."],
e:"<code>pause_turn</code> appears when Anthropic-executed server tools (web search, web fetch, code execution) run their own loop and reach its iteration limit &mdash; ten by default. It is a continuation signal, not a failure: append the assistant content and send the conversation back. This is a good illustration of why branching on every <code>stop_reason</code> matters, since a loop that only knows <code>end_turn</code> and <code>tool_use</code> will mishandle it.",
k:"<code>pause_turn</code> = server-tool loop hit its iteration limit. Send the assistant content back to continue; it is not a failure."},

{i:692,d:1,t:"1.1",s:"AAT",diff:3,
q:"A response returns <code>stop_reason: \"refusal\"</code>.",
ask:"What is the appropriate handling?",
a:["Read <code>stop_details</code> for the refusal category and decide whether to rephrase, route differently, or surface it &mdash; do not blindly retry the identical request",
"Retry the identical request until it succeeds",
"Treat it as a transient failure with exponential backoff",
"Abort the entire workflow"],
c:0,
w:["Correct. <code>refusal</code> is deterministic for that input, and <code>stop_details</code> carries the only additional information available.",
"An identical retry of a refused request is the deterministic-retry mistake in a new costume.",
"Refusal is not transient; backoff changes nothing.",
"Aborting the whole workflow over one refused request is the abort anti-pattern."],
e:"<code>refusal</code> is the only <code>stop_reason</code> where <code>stop_details</code> is populated, and it behaves like a non-retryable error: the same input will produce the same outcome. Treat it with the same taxonomy you apply to tool errors &mdash; deterministic, not retryable as-is, potentially recoverable with a changed input, and otherwise a case to surface rather than to silently swallow.",
k:"<code>refusal</code> is deterministic. Read <code>stop_details</code>; never retry unchanged, never treat it as transient."},

{i:693,d:1,t:"1.2",s:"MAR",diff:2,
q:"You are asked whether a coordinator should be able to see which named agents exist in the session.",
ask:"Why does this matter for orchestration quality?",
a:["Selection is comparative, so the coordinator needs the full roster with clear descriptions to route correctly",
"It matters only for display purposes in the task list",
"It matters only when agents need to call each other",
"It does not matter; routing is determined by the user"],
c:0,
w:["Correct. A coordinator routes by comparing descriptions, so the roster and its quality directly determine routing accuracy.",
"Display is incidental to the routing decision.",
"Spokes calling each other is the mesh anti-pattern; routing is the hub's job.",
"Routing is the coordinator's job, not the user's."],
e:"Agent selection works like tool selection: the model compares available options against the request, so both the roster and the description quality drive accuracy. That is why unused agents add noise, why overlapping descriptions cause unpredictable misrouting, and why the fix for poor routing is almost always sharper descriptions stating trigger conditions and boundaries &mdash; not a separate classifier.",
k:"Agent routing is comparative, like tool selection. Roster size and description sharpness determine accuracy."},

{i:694,d:1,t:"1.3",s:"AAT",diff:2,
q:"A standard subagent starts. Which of the following does it receive?",
ask:"Which is included in a standard subagent's starting context?",
a:["Its own system prompt, the delegation prompt, and CLAUDE.md files from the project hierarchy",
"The parent's full conversation history",
"The parent's auto memory",
"The parent's output style settings"],
c:0,
w:["Correct. Project instruction files load for subagents even though conversation history does not.",
"Conversation history is explicitly not inherited by a standard subagent.",
"The main conversation's auto memory is not loaded into subagents.",
"Output style is not inherited."],
e:"A useful nuance: subagent isolation is about the <em>conversation</em>, not about project configuration. A standard subagent receives its own system prompt, the delegation prompt, CLAUDE.md files from the hierarchy, and a git-status snapshot &mdash; but not conversation history, auto memory or output style.<br><br>That is why a subagent still follows team conventions without being told, and simultaneously why it knows nothing about what you and the parent agent just discussed.",
k:"Subagents inherit project CLAUDE.md but not conversation history, auto memory or output style."},

{i:695,d:1,t:"1.4",s:"CSA",diff:3,
q:"A control must apply even when the agent is running under a permissive permission mode.",
ask:"What should you rely on?",
a:["Deny rules and a blocking hook, since deny and ask rules are evaluated regardless of what a hook returns and permission modes do not override an explicit deny",
"The permission mode alone, set conservatively",
"A prompt instruction repeated in the system prompt",
"Post-hoc audit logging"],
c:0,
w:["Correct. Deny rules are evaluated independently, which is what makes them reliable under permissive modes.",
"Permission modes are configurable per session and a permissive one is exactly the scenario described.",
"A prompt cannot enforce anything.",
"Auditing detects after the fact; it does not prevent."],
e:"Layering matters here. A <code>PreToolUse</code> hook returning <code>permissionDecision: \"deny\"</code> prevents the call, and deny and ask rules are still evaluated regardless of what a hook returns &mdash; so the two reinforce rather than substitute for each other. Permission modes change how much gets auto-approved, which is precisely why a control that must always hold should not depend on the mode being set conservatively.",
k:"Deny rules and blocking hooks reinforce each other and survive permissive permission modes. Never rely on the mode alone."},

{i:696,d:2,t:"2.2",s:"AAT",diff:2,
q:"Your MCP tool returns an error and you want the agent to be able to attempt the same call again after a permission problem is resolved.",
ask:"What does the agent need?",
a:["A clear statement that the block is permission-based and what would have to change, so a retry is attempted only once the condition is met",
"A retryable flag set to true so it keeps trying",
"An exponential backoff schedule",
"No information; the agent will retry automatically"],
c:0,
w:["Correct. Permission errors are not retryable as-is, but they become actionable once the agent knows what would unblock them.",
"Marking a permission error retryable produces a retry storm that cannot succeed.",
"Backoff addresses transient failures, not permission conditions.",
"Automatic retry without a change is the failure mode to avoid."],
e:"Permission errors sit in an interesting middle ground. They are not retryable in the transient sense, so the flag must say so &mdash; but they are not permanently hopeless either. Stating the condition (&ldquo;this account is flagged for manual review; a supervisor must clear the flag&rdquo;) lets the agent explain the situation accurately and escalate to the party who can change it, rather than either looping or giving up opaquely.",
k:"Permission errors: non-retryable as-is, but state what would unblock them so the agent can route to whoever can change it."},

{i:697,d:2,t:"2.4",s:"DPT",diff:2,
q:"You want to withhold every tool from one MCP server from a particular subagent.",
ask:"What is the concise way to express this?",
a:["A tool pattern such as <code>mcp__&lt;server&gt;</code> in that subagent's <code>disallowedTools</code>",
"Listing each of the server's tools individually in <code>disallowedTools</code>",
"Removing the server from <code>.mcp.json</code>",
"Setting the server to user scope"],
c:0,
w:["Correct. Server-level tool patterns exist precisely so you do not have to enumerate tools.",
"Works but is verbose and breaks whenever the server adds a tool.",
"Removes it for everyone, not just this subagent.",
"Scope determines who has the server, not which agent within a session sees it."],
e:"Tool patterns keep per-agent scoping maintainable: <code>mcp__&lt;server&gt;</code> matches all tools from a named server, and <code>mcp__*</code> matches all MCP tools from any server (available in <code>disallowedTools</code>). Enumerating individual tools is fragile because a server upgrade silently reintroduces access. This is how you keep an agent's toolset small when a large community server is connected for another agent's benefit.",
k:"Use <code>mcp__&lt;server&gt;</code> patterns in <code>disallowedTools</code> to withhold a whole server. Enumerating tools breaks on upgrade."},

{i:698,d:3,t:"3.1",s:"CGC",diff:2,
q:"You want to leave notes for human maintainers in CLAUDE.md without spending context tokens on them.",
ask:"What is the documented mechanism?",
a:["Block-level HTML comments, which are stripped before the content is injected into Claude's context",
"Lines beginning with a hash, which are treated as comments",
"A separate <code>CLAUDE.notes.md</code> file",
"There is no mechanism; all content costs tokens"],
c:0,
w:["Correct. Block-level HTML comments are stripped before injection, so they cost no context.",
"Hash-prefixed lines are markdown headings, not comments.",
"No such convention exists.",
"The mechanism does exist."],
e:"A small but genuinely useful detail: block-level HTML comments in CLAUDE.md are stripped before the content reaches Claude's context, so maintainer notes, rationale for a rule, or a reminder about why an instruction exists cost nothing. Comments inside code blocks are preserved, and the comments remain visible if you open the file with the Read tool. It is a neat way to keep the human-facing and model-facing content in one file without paying twice.",
k:"Block-level HTML comments in CLAUDE.md are stripped before injection &mdash; free maintainer notes."},

{i:699,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want a shared set of rules maintained once and used across several repositories.",
ask:"What does <code>.claude/rules/</code> support for this?",
a:["Symlinks, both to a shared directory and to individual rule files, which are resolved and loaded normally",
"An HTTP URL in the frontmatter pointing at a remote rules file",
"A <code>extends</code> key referencing another repository",
"Nothing &mdash; rules must be copied into each repository"],
c:0,
w:["Correct. The rules directory supports symlinks, with circular links detected and handled.",
"No remote-URL mechanism exists for rules.",
"No such key exists.",
"Copying is the fragile alternative symlinks avoid."],
e:"Symlinking is the documented way to maintain shared rules once and link them into several projects &mdash; either the whole directory or an individual file. Two related details: circular symlinks are detected and handled gracefully, and when you need to exclude a symlinked rule via <code>claudeMdExcludes</code>, a pattern matching either the path under <code>.claude/rules/</code> or the link target will do it.",
k:"<code>.claude/rules/</code> supports symlinks to shared directories or individual files &mdash; maintain once, link everywhere."},

{i:700,d:3,t:"3.4",s:"CGC",diff:2,
q:"You want an instruction to apply at the system prompt level rather than as loaded context.",
ask:"Which mechanism does this, and what is its trade-off?",
a:["<code>--append-system-prompt</code>, which must be passed on every invocation and so suits scripts and automation more than interactive use",
"Putting the instruction in CLAUDE.md, which is part of the system prompt",
"A <code>systemPrompt</code> key in settings, applied automatically",
"Prefixing the instruction with SYSTEM: in CLAUDE.md"],
c:0,
w:["Correct, including the trade-off: it is per-invocation, which is why it fits automation better than interactive work.",
"CLAUDE.md content is delivered as a user message <em>after</em> the system prompt, not as part of it.",
"No such automatic settings key provides this.",
"Prefix conventions do not change how content is delivered."],
e:"A precise and easily-missed distinction: <strong>CLAUDE.md content is delivered as a user message after the system prompt, not as part of the system prompt itself</strong>. That is part of why adherence is good but not guaranteed. When you genuinely need system-prompt-level placement, <code>--append-system-prompt</code> provides it &mdash; at the cost of having to pass it every invocation, which is why it belongs in scripted and CI usage.",
k:"CLAUDE.md is delivered as a user message after the system prompt. For true system-prompt placement use <code>--append-system-prompt</code>."},

{i:701,d:4,t:"4.5",s:"SDE",diff:3,
q:"You plan to use prompt caching to reduce the cost of a large shared prefix across a batch job.",
ask:"What limitation applies?",
a:["Cache pre-warming is not supported inside a batch, because an ephemeral cache entry written during batch processing would likely expire before the follow-up request runs",
"Prompt caching cannot be used with batches at all",
"Caching doubles the cost inside a batch",
"Caching requires the synchronous API only for the first request"],
c:0,
w:["Correct. The documented limitation concerns pre-warming, given the asynchronous timing of batch processing.",
"Overstates the limitation.",
"There is no such cost penalty.",
"Not the documented constraint."],
e:"Batch processing is asynchronous with no latency guarantee, so timing assumptions that hold for synchronous request sequences do not carry over. Pre-warming a cache in the expectation that a follow-up request will land within the cache lifetime is exactly such an assumption, and it does not hold when a batch may be processed hours later. Plan batch cost around the 50% discount rather than around caching interactions.",
k:"Cache pre-warming is not supported inside a batch &mdash; asynchronous timing breaks the ephemeral cache lifetime assumption."},

{i:702,d:2,t:"2.3",s:"AAT",diff:3,
q:"You are told that <code>tool_choice</code> has no cost implications.",
ask:"What is the accurate position?",
a:["The tool-use system prompt is slightly larger for <code>any</code> and forced-tool choices than for <code>auto</code> or <code>none</code>, so there is a small per-request difference",
"All four values cost identically",
"<code>none</code> costs the most because it still sends the tools",
"Cost depends only on the number of tools, not on <code>tool_choice</code>"],
c:0,
w:["Correct. The documented token counts differ between <code>auto</code>/<code>none</code> and <code>any</code>/<code>tool</code>.",
"There is a documented difference.",
"<code>none</code> is in the cheaper group; and with no tools supplied at all, <code>none</code> adds zero system-prompt tokens.",
"Tool count matters, but <code>tool_choice</code> also affects the tool-use system prompt size."],
e:"When you use <code>tools</code>, the API automatically includes a tool-use system prompt whose size varies by model and by <code>tool_choice</code> &mdash; <code>any</code> and a forced tool are modestly more expensive than <code>auto</code> and <code>none</code>. The difference is small and should never drive a correctness decision, but it is a reminder that constraining tool choice is not free, and that forcing a tool on every turn of a long loop compounds a cost you did not need to pay.",
k:"<code>any</code> and forced-tool choices carry a slightly larger tool-use system prompt than <code>auto</code> and <code>none</code>."},

{i:703,d:5,t:"5.4",s:"CGC",diff:2,
q:"You want to reduce a long session's context but are worried about losing project instructions.",
ask:"What actually survives compaction?",
a:["Project-root CLAUDE.md is re-read from disk and re-injected; nested CLAUDE.md files and path-scoped rules reload when Claude next reads a matching file",
"Nothing survives; everything must be restated",
"Only the last ten messages survive",
"All instructions survive, including those given in conversation"],
c:0,
w:["Correct. File-backed instructions reload; conversation-only instructions do not.",
"File-backed instructions do survive.",
"Compaction summarises rather than keeping a fixed window.",
"Conversation-only instructions are exactly what is lost."],
e:"This clarifies which context-management worries are real. Instructions that live in files reload, so compaction does not cost you your conventions. What compaction genuinely degrades is the conversational content it summarises &mdash; numbers, dates, exact paths and findings established in dialogue. That is precisely why the discipline is to externalise those specifics to a scratchpad file <em>before</em> compacting, and why conversation-only instructions should be promoted into CLAUDE.md if they must persist.",
k:"File-backed instructions survive compaction; conversation-derived specifics do not. Externalise findings before compacting."},

{i:704,d:1,t:"1.7",s:"CGC",diff:3,
q:"A forked skill makes file edits and a developer expects <code>/rewind</code> to undo them.",
ask:"What is the documented behaviour?",
a:["Edits made by a forked skill are applied outside session checkpoints, so <code>/rewind</code> will not undo them",
"They are undone like any other edit",
"Forked skills cannot make edits",
"They are undone only if <code>background: false</code> was set"],
c:0,
w:["Correct, and it is a meaningful safety consideration when granting a forked skill write capability.",
"Checkpointing does not cover them.",
"They can make edits, which is why this matters.",
"The background setting does not change checkpoint behaviour."],
e:"Context isolation has a consequence beyond token savings: the forked subagent's work sits outside the main session's checkpoint history, so the usual undo path does not cover its edits. That argues for giving forked skills the narrowest capability they need &mdash; and for preferring read-only forked skills where the task is analysis rather than modification, which is the common case for the verbose skills that most benefit from forking.",
k:"Forked-skill edits fall outside session checkpoints and survive <code>/rewind</code>. Prefer read-only forked skills."},

{i:705,d:3,t:"3.6",s:"CCI",diff:2,
q:"A CI run needs to surface what its hooks did, for debugging a configuration problem.",
ask:"How is this available in print mode?",
a:["Launch with <code>--output-format stream-json --verbose</code>, which surfaces hook responses as events in the run's output",
"Hooks never produce output in print mode",
"Set a <code>hookDebug</code> flag in settings",
"Read the hook output from stderr only"],
c:0,
w:["Correct. Streaming JSON with verbose output includes hook response events.",
"Hook activity is observable in print mode with the right flags.",
"No such settings flag is the documented mechanism.",
"Some hook events discard stdout and stderr entirely, so this is unreliable."],
e:"Debugging hooks in CI is otherwise difficult because several events deliberately discard their JSON output fields. Streaming JSON with verbose output surfaces hook responses as events you can inspect, which is the practical way to answer &ldquo;did my enforcement hook actually run, and what did it decide?&rdquo; Pair it with logging the decision inside the hook itself, which is what makes the control auditable in production rather than only debuggable.",
k:"<code>--output-format stream-json --verbose</code> surfaces hook responses in print mode &mdash; the way to debug CI hook configuration."},

{i:706,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent is given <code>--add-dir</code> access to a shared configuration directory and expects that directory's CLAUDE.md to apply.",
ask:"What actually happens by default?",
a:["File access is granted, but CLAUDE.md and most <code>.claude/</code> configuration from that directory are not loaded unless explicitly enabled",
"The directory's CLAUDE.md loads automatically",
"The directory is read-only regardless of settings",
"<code>--add-dir</code> loads configuration but not files"],
c:0,
w:["Correct. <code>--add-dir</code> grants file access; configuration discovery from those directories is separate and off by default.",
"It does not load by default.",
"<code>--add-dir</code> grants read and edit access.",
"Backwards &mdash; it grants file access, not configuration."],
e:"A distinction worth holding: file access and configuration discovery are separate concerns. <code>--add-dir</code> lets Claude read and edit files in another directory but does not pull that directory's instructions into your session &mdash; loading its CLAUDE.md, <code>.claude/CLAUDE.md</code>, <code>.claude/rules/*.md</code> and <code>CLAUDE.local.md</code> requires opting in explicitly. This prevents an added directory silently changing the conventions your session is following.",
k:"<code>--add-dir</code> grants file access, not configuration. Loading that directory's CLAUDE.md and rules is opt-in."},

{i:707,d:1,t:"1.4",s:"AAT",diff:3,
q:"A <code>PreToolUse</code> hook returns <code>permissionDecision: \"ask\"</code>.",
ask:"What does this achieve?",
a:["It escalates the decision to the user rather than deciding automatically &mdash; a middle ground between allow and deny",
"It denies the call and logs the reason",
"It allows the call but records an audit entry",
"It defers the call for later resumption"],
c:0,
w:["Correct. <code>ask</code> prompts the user to confirm, which is the human-in-the-loop option.",
"That is <code>deny</code>.",
"That is <code>allow</code> plus separate logging.",
"That is <code>defer</code>, which exits gracefully so the tool can be resumed later."],
e:"<code>PreToolUse</code> offers four outcomes rather than a binary: <code>allow</code> skips the permission prompt, <code>deny</code> prevents the call, <code>ask</code> prompts the user to confirm, and <code>defer</code> exits gracefully so the tool can be resumed later. <code>ask</code> is the mechanism behind targeted human-in-the-loop gating &mdash; use it for the small set of irreversible, high-blast-radius operations, and let everything reversible flow, to avoid approval fatigue.",
k:"<code>PreToolUse</code> has four outcomes: allow, deny, <strong>ask</strong> (human confirms), defer. <code>ask</code> is targeted human-in-the-loop."},

{i:708,d:5,t:"5.5",s:"SDE",diff:2,
q:"Which statement best captures the relationship between CLAUDE.md and enforcement?",
ask:"Which is accurate?",
a:["CLAUDE.md is context that shapes behaviour, not enforced configuration; to block an action regardless of what Claude decides, use a <code>PreToolUse</code> hook",
"CLAUDE.md is enforced configuration and its rules cannot be violated",
"CLAUDE.md and hooks are interchangeable",
"CLAUDE.md is enforced only when it is under 200 lines"],
c:0,
w:["Correct &mdash; this is close to the documentation's own wording and is the exam's central principle.",
"CLAUDE.md instructions are followed probabilistically, not enforced.",
"They differ fundamentally in what they guarantee.",
"Length affects adherence, not enforcement status."],
e:"The official documentation states this almost verbatim: Claude treats CLAUDE.md and auto memory as context, not enforced configuration, and to block an action regardless of what Claude decides you should use a <code>PreToolUse</code> hook. It adds a second, practically important point &mdash; the more specific and concise your instructions, the more consistently they are followed.<br><br>Together these give the whole Domain 3 strategy: write specific, concise, scoped instructions for behaviour, and use hooks and permission rules for anything that must be guaranteed.",
k:"CLAUDE.md = context, not enforcement. Specific and concise improves adherence; only hooks and permission rules guarantee."},

{i:709,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want a checkup that suggests what to trim from a large committed CLAUDE.md.",
ask:"Which command does this?",
a:["<code>/doctor</code>, which proposes cutting content Claude can derive from the codebase while keeping pitfalls, rationale and non-default conventions",
"<code>/compact</code>",
"<code>/init</code>",
"<code>/memory</code>"],
c:0,
w:["Correct. The <code>/doctor</code> checkup includes a trim proposal for a checked-in CLAUDE.md.",
"Compaction reduces the active conversation, not a file.",
"<code>/init</code> generates or suggests improvements to a starting CLAUDE.md, which is related but not the trim checkup.",
"<code>/memory</code> lists and opens memory file locations."],
e:"The trim heuristic is worth internalising even without the command: cut what Claude can derive from the codebase &mdash; directory layouts, dependency lists, architecture overviews &mdash; and keep pitfalls, rationale, and conventions that <em>differ from tool defaults</em>. That last clause is the sharpest test for whether a CLAUDE.md line earns its context: if the default behaviour already matches your rule, the rule is not doing any work.",
k:"Trim CLAUDE.md of anything derivable from the code. Keep pitfalls, rationale, and conventions that differ from tool defaults."},

{i:710,d:2,t:"2.1",s:"AAT",diff:2,
q:"Claude is not calling tools when you expect it to, under <code>tool_choice: \"auto\"</code>.",
ask:"What is the documented first lever, before changing <code>tool_choice</code>?",
a:["A light system prompt instruction such as &ldquo;use the tools to investigate before responding&rdquo;, escalating to a stronger form if needed",
"Setting <code>any</code> immediately",
"Removing tools until only one remains",
"Raising the temperature"],
c:0,
w:["Correct. The tool-use boundary is steerable through the system prompt, and a light instruction is the documented first step.",
"<code>any</code> is the structural guarantee, appropriate when you truly need it &mdash; but it prevents a text turn, so it is not the first lever.",
"Reducing capability to force selection is a poor trade.",
"Temperature does not govern tool-calling propensity."],
e:"Under <code>auto</code>, Claude calls a tool when the request maps to a described capability and the answer is not already in context. That boundary is steerable: &ldquo;use the tools to investigate before responding&rdquo; increases tool use, &ldquo;always call a tool first before responding&rdquo; pushes further, and &ldquo;use your judgment&rdquo; keeps it conservative. Escalate to <code>any</code> or a forced tool when you need a structural guarantee rather than a tendency &mdash; remembering that <code>any</code> prevents the turn that ends the loop.",
k:"Tool-calling propensity under <code>auto</code> is steerable by prompt. Escalate to <code>any</code> only when you need a guarantee."},

{i:711,d:4,t:"4.3",s:"SDE",diff:2,
q:"A required parameter is missing from the user's request and the model supplies a plausible value it was never given.",
ask:"What is the most reliable mitigation?",
a:["Validate required parameters in code before executing, and design the tool to reject a call whose inputs were not actually supplied",
"Rely on the model to recognise the gap and ask",
"Raise the temperature so the model is less decisive",
"Mark more fields as required in the schema"],
c:0,
w:["Correct. Inferred parameters are untrusted input, so the check belongs in code.",
"More capable models are likelier to ask, but the documentation is explicit that this behaviour is not guaranteed.",
"Temperature does not govern whether a value is inferred.",
"Marking more fields required increases the pressure to invent values rather than reducing it."],
e:"The documentation notes directly that when a prompt lacks enough information to fill a required parameter, the model may infer a reasonable value rather than asking &mdash; and that this behaviour is not guaranteed. Treat any model-supplied parameter as untrusted input, exactly as you would a value from a web form. Validate in code, and where the correct value is determined by your system rather than the request, inject it in a <code>PreToolUse</code> hook rather than asking for it.",
k:"Models may invent missing required parameters. Validate in code; inject system-determined values rather than requesting them."},

{i:712,d:1,t:"1.6",s:"CGC",diff:2,
q:"You want to know exactly which instruction files loaded, when and why, to debug a path-scoped rule that is not firing.",
ask:"Which mechanism is designed for this?",
a:["The <code>InstructionsLoaded</code> hook, which logs which instruction files load, when and why",
"<code>/compact</code> output",
"The <code>PostToolUse</code> hook",
"Raising the log level in settings"],
c:0,
w:["Correct. It exists for audit logging, compliance tracking and observability of instruction loading.",
"Compaction is unrelated to instruction loading.",
"<code>PostToolUse</code> concerns tool results, not instruction files.",
"Not the documented mechanism for this."],
e:"Two complementary tools for the same problem. <code>/context</code> answers &ldquo;what is loaded right now?&rdquo; and is the quick check when a CLAUDE.md seems ignored. The <code>InstructionsLoaded</code> hook answers &ldquo;which files loaded, when and why?&rdquo; over time, which is what you need for lazily-loaded subdirectory files and path-scoped rules that only fire when a matching file is read.",
k:"<code>/context</code> = what is loaded now. <code>InstructionsLoaded</code> hook = which files loaded, when and why, over time."},

{i:713,d:5,t:"5.1",s:"CAP",diff:2,
q:"You want durable knowledge to carry across sessions without writing it yourself.",
ask:"Which mechanism does this, and what is its scope?",
a:["Auto memory, which Claude writes itself per repository, with an index loaded into every session",
"CLAUDE.md, which Claude maintains automatically",
"The session transcript, which is reloaded automatically",
"Path-scoped rules, which accumulate learnings"],
c:0,
w:["Correct. Auto memory is written by Claude, scoped per repository and shared across worktrees.",
"CLAUDE.md is written by you; auto memory is written by Claude.",
"Transcripts are not automatically reloaded across sessions.",
"Rules are instructions you write, not accumulated learnings."],
e:"Two complementary memory systems, both loaded at the start of every session. <strong>CLAUDE.md</strong>: written by you, containing instructions and rules, scoped project or user. <strong>Auto memory</strong>: written by Claude from your corrections and preferences, scoped per repository, with an index loaded each session and topic files read on demand.<br><br>Crucially, both are <em>context</em> rather than enforced configuration &mdash; neither guarantees behaviour, which is why hooks remain the enforcement layer.",
k:"CLAUDE.md is written by you; auto memory is written by Claude per repository. Both are context, neither is enforcement."},

{i:714,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a skill that Claude may invoke automatically but that you should not see in the <code>/</code> menu.",
ask:"Which frontmatter field does this?",
a:["<code>user-invocable: false</code>",
"<code>disable-model-invocation: true</code>",
"<code>context: fork</code>",
"<code>background: true</code>"],
c:0,
w:["Correct. It hides the skill from the <code>/</code> menu while leaving automatic invocation available.",
"That is the opposite &mdash; it prevents Claude from invoking automatically, leaving only manual invocation.",
"That controls context isolation.",
"That controls whether a forked skill runs asynchronously."],
e:"The two invocation-control fields are mirror images and are frequently confused. <code>disable-model-invocation: true</code> means only <em>you</em> can invoke it, manually. <code>user-invocable: false</code> means only <em>Claude</em> can invoke it, automatically. Both default to the permissive setting. Use the first for skills with side effects you want to trigger deliberately, and the second for background behaviours that should not clutter the menu.",
k:"<code>disable-model-invocation: true</code> = manual only. <code>user-invocable: false</code> = automatic only. Mirror images."},

{i:715,d:2,t:"2.4",s:"DPT",diff:3,
q:"An agent needs to work with a very large number of tools &mdash; far beyond what fits reliably in one toolset.",
ask:"Which capability is designed for this?",
a:["The tool search tool, which discovers and loads tools on demand rather than presenting all of them at once",
"Increasing the context window",
"Connecting more MCP servers",
"Setting <code>tool_choice</code> to <code>any</code>"],
c:0,
w:["Correct. Tool search exists precisely to work with thousands of tools by loading them on demand.",
"A larger window holds more definitions but does not fix the selection-reliability problem.",
"More servers makes the toolset larger, which is the problem.",
"Forcing a tool call does not help the model choose among thousands."],
e:"This addresses the limit behind many Domain 2 answers. Selection reliability degrades as the toolset grows, which is why the usual remedies are scoping per role and splitting into specialists. Where the domain genuinely requires a very large tool surface, the tool search tool changes the shape of the problem &mdash; tools are discovered and loaded on demand rather than all being present in every request, so the effective choice space stays small.",
k:"For very large tool surfaces, tool search loads tools on demand instead of presenting all of them &mdash; keeps the choice space small."}
);
