QB.push(
{i:951,d:3,t:"3.1",s:"CGC",diff:2,
q:"Your CLAUDE.md says &ldquo;run the tests before committing&rdquo; and developers report Claude sometimes commits without running them.",
ask:"What is the appropriate escalation if this must always happen?",
a:["A hook on the commit action, since an instruction that must run at a specific point regardless of what Claude decides is a hook, not a memory instruction",
"Repeat the instruction in a rules file as well",
"Make the instruction more emphatic",
"Move the instruction to the top of CLAUDE.md"],
c:0,
w:["Correct. The documentation says explicitly that instructions which must run at a specific point belong in hooks.",
"Duplication costs context and does not add enforcement.",
"Emphasis raises compliance probability without guaranteeing it.",
"Position helps salience marginally and does not guarantee execution."],
e:"The documented rule is precise: if an instruction must run at a specific point &mdash; before every commit, after each file edit &mdash; write it as a hook, because hooks execute as shell commands at fixed lifecycle events and apply regardless of what Claude decides. CLAUDE.md is context that shapes behaviour, and for a rule with a fixed trigger point that is not sufficient.",
k:"Instructions with a fixed trigger point (before commit, after edit) belong in hooks, not CLAUDE.md."},

{i:952,d:3,t:"3.1",s:"CGC",diff:2,
q:"A developer asks whether they should write project context into auto memory or CLAUDE.md.",
ask:"What distinguishes them?",
a:["You write CLAUDE.md deliberately as instructions; auto memory is written by Claude from your corrections and preferences, and it skips anything CLAUDE.md already says",
"They are the same mechanism",
"Auto memory replaces CLAUDE.md",
"CLAUDE.md is loaded per turn and auto memory once per session"],
c:0,
w:["Correct, including the detail that Claude skips recording what CLAUDE.md already covers.",
"They differ in who writes them and what they contain.",
"They are complementary; CLAUDE.md holds deliberate instructions.",
"Both are loaded at the start of every session."],
e:"Two complementary systems, both loaded at session start, both treated as context rather than enforced configuration. <strong>CLAUDE.md</strong>: written by you, containing instructions and rules, scoped project or user. <strong>Auto memory</strong>: written by Claude, containing learnings from your corrections and preferences, scoped per repository. Claude deliberately skips saving anything derivable from the codebase or already stated in CLAUDE.md.",
k:"CLAUDE.md = your deliberate instructions. Auto memory = Claude's learnings from corrections. Both context, not enforcement."},

{i:953,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a skill that runs in a forked context and returns before you continue working.",
ask:"Which field combination expresses this?",
a:["<code>context: fork</code> with <code>background: false</code>",
"<code>context: fork</code> alone",
"<code>background: false</code> alone",
"<code>context: fork</code> with <code>agent</code> set"],
c:0,
w:["Correct. Forking isolates; <code>background: false</code> makes it synchronous within the turn.",
"Forked skills run in the background by default, so it would not block.",
"<code>background</code> only applies alongside <code>context: fork</code>.",
"<code>agent</code> selects the subagent type, not the synchronicity."],
e:"Three fields work together. <code>context: fork</code> isolates the execution. <code>agent</code> selects which subagent type runs it &mdash; <code>Explore</code>, <code>Plan</code>, or a general-purpose one. <code>background</code> defaults to <code>true</code>, so a forked skill runs asynchronously unless you set it to <code>false</code>. Note that setting <code>background: false</code> forces the narrower background tool set regardless.",
k:"<code>context: fork</code> isolates, <code>agent</code> picks the type, <code>background: false</code> waits for the result in this turn."},

{i:954,d:3,t:"3.3",s:"CGC",diff:2,
q:"A rule file lives in a nested directory such as <code>.claude/rules/frontend/components.md</code>.",
ask:"Is it discovered?",
a:["Yes &mdash; all <code>.md</code> files under <code>.claude/rules/</code> are discovered recursively, so subdirectories can be used for organisation",
"No, only files directly in <code>.claude/rules/</code> are found",
"Only if listed in a manifest",
"Only if it carries <code>paths</code> frontmatter"],
c:0,
w:["Correct. Discovery is recursive, which is what makes subdirectory organisation viable.",
"Nested rule files are discovered.",
"There is no manifest.",
"Frontmatter affects when it loads, not whether it is discovered."],
e:"Recursive discovery means you can organise rules into subdirectories &mdash; <code>frontend/</code>, <code>backend/</code>, <code>infra/</code> &mdash; which matters once you have dozens of them. Combine that with <code>paths</code> frontmatter so each loads only when relevant, and a large rule set stays both maintainable and cheap. Note the versioning caveat: before v2.1.211, rules that load on demand loaded even when <code>project</code> was excluded from <code>--setting-sources</code>.",
k:"<code>.claude/rules/</code> is discovered recursively &mdash; organise in subdirectories, scope each with <code>paths</code>."},

{i:955,d:3,t:"3.4",s:"CGC",diff:2,
q:"A task is small and well-specified but touches a module nobody on the team understands.",
ask:"What is the appropriate process?",
a:["Delegate exploration of that module to a subagent, then execute directly from its summary",
"Full planning mode with a written plan",
"Direct execution immediately",
"Escalate to whoever wrote the module"],
c:0,
w:["Correct. Unfamiliarity calls for exploration; a small well-specified change does not need a plan.",
"Planning overhead is disproportionate for a small change with one obvious approach.",
"Executing in code nobody understands risks violating conventions you cannot see.",
"Reasonable if available, but the agent can establish the understanding directly."],
e:"Keep the two variables separate. <em>Uncertainty about the approach</em> drives planning; <em>unfamiliarity with the code</em> drives exploration, which is delegable so its verbose output never enters your main context. A small, well-specified change in unfamiliar code needs the second and not the first: explore, then execute from the summary.",
k:"Unfamiliar code &rarr; delegate exploration. Uncertain approach &rarr; plan. Small and well-specified needs only the first."},

{i:956,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want feedback to improve the implementation without triggering a wholesale rewrite.",
ask:"How should it be framed?",
a:["As a specific defect with the expected behaviour, scoped to what should change &mdash; so the model corrects rather than redesigns",
"As a general statement that the approach is wrong",
"As a request to try a different approach",
"As a list of everything that could be better"],
c:0,
w:["Correct. Specific and scoped feedback produces a correction; open-ended feedback invites a redesign.",
"&ldquo;The approach is wrong&rdquo; without specifics invites starting over.",
"Requesting a different approach explicitly asks for a rewrite.",
"An open list of improvements produces broad changes you did not ask for."],
e:"Unwanted rewrites usually trace to unscoped feedback. Naming the defect, stating the expected behaviour, and indicating the scope of the intended change keeps the correction proportionate. If you do want a different approach, say so explicitly &mdash; the failure mode is getting one when you meant to fix a detail, which then discards work that was fine.",
k:"Scope feedback to the defect and the expected behaviour. Unscoped feedback invites unwanted redesign."},

{i:957,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review needs to run on a repository where <code>project</code> setting sources are excluded.",
ask:"What is the consequence for rules?",
a:["Project rules are skipped, so the review runs without them &mdash; the standards must be supplied another way if they are required",
"Rules load regardless of setting sources",
"Only path-scoped rules are skipped",
"Only unscoped rules are skipped"],
c:0,
w:["Correct. Excluding <code>project</code> skips project rules, so the review loses those standards.",
"Setting sources genuinely control this.",
"Both kinds are skipped when project sources are excluded &mdash; though before v2.1.211 on-demand rules loaded anyway.",
"Both kinds are affected."],
e:"Setting sources control which configuration layers load, and excluding <code>project</code> skips project rules along with other project settings. If your CI review depends on those standards, either include project sources or supply the standards another way. This is worth checking when CI findings look generic compared with local review &mdash; it usually means the standards did not load, which <code>/context</code> or an <code>InstructionsLoaded</code> hook would confirm.",
k:"Excluding <code>project</code> from setting sources skips project rules. Generic CI findings often mean standards did not load."},

{i:958,d:2,t:"2.1",s:"AAT",diff:2,
q:"A tool's description is accurate but written for a human integrator rather than for selection.",
ask:"What typically differs?",
a:["Integrator documentation explains how to use the tool correctly; a selection description must additionally say when to choose it over its neighbours",
"Integrator documentation is longer",
"Integrator documentation uses more jargon",
"There is no meaningful difference"],
c:0,
w:["Correct. The boundary against alternatives is the element integrator docs typically omit.",
"Length is incidental.",
"Jargon may differ but is not the substantive gap.",
"The audiences need different content."],
e:"Human integrator documentation answers &ldquo;how do I call this correctly?&rdquo; A selection description must answer that <em>and</em> &ldquo;when should this be chosen rather than the similar tool next to it?&rdquo; Since selection failures are near-misses between neighbours, the boundary statement is the highest-value element &mdash; and it is exactly what a document written in isolation about one tool never contains.",
k:"Integrator docs explain correct use; selection descriptions must also state the boundary against neighbours."},

{i:959,d:2,t:"2.2",s:"AAT",diff:2,
q:"You are deciding whether a tool should return an error or an empty success for a condition that is unusual but valid.",
ask:"What decides it?",
a:["Whether the operation was performed successfully &mdash; a valid outcome, however unusual, is a success",
"Whether the caller will be disappointed",
"Whether the condition is rare",
"Whether the result is useful"],
c:0,
w:["Correct. Success describes whether the operation completed, not whether the result pleases the caller.",
"Disappointment is not a failure condition.",
"Rarity is irrelevant to correctness.",
"An unhelpful but correct result is still a success."],
e:"The distinction is whether the operation could be performed. A search that ran and found nothing succeeded; a search that could not run failed. Conflating them causes retry storms on one side and invisible gaps on the other. Where an unusual valid outcome deserves attention, convey that with a status field or a note &mdash; not by misclassifying it as an error.",
k:"Success means the operation completed, not that the result is pleasing. Unusual valid outcomes are successes."},

{i:960,d:2,t:"2.3",s:"AAT",diff:2,
q:"You want an agent to be unable to reach production systems while still being able to reach staging.",
ask:"How should this be arranged?",
a:["Separate tools or servers per environment, with only the staging one available to that agent &mdash; not one tool taking an environment parameter",
"One tool with an environment parameter, defaulting to staging",
"One tool with an environment parameter and a prompt instruction",
"One tool with a hook blocking the production value"],
c:0,
w:["Correct. Separate capabilities mean production is structurally unreachable rather than filtered.",
"A default is not a restriction; the model can pass the other value.",
"A prompt instruction guarding an environment boundary is insufficient.",
"A hook works as a layer, but separating the capability is stronger and simpler."],
e:"An environment parameter turns a capability boundary into a value check, which is weaker and easier to get wrong. Separate tools or separate MCP servers per environment mean production is simply not in the agent's choice space &mdash; the same reasoning as omitting a tool rather than granting and blocking it. It also keeps each tool's description simpler, since it has one target.",
k:"Environment boundaries belong in separate tools or servers, not in a parameter. Structural absence beats value filtering."},

{i:961,d:2,t:"2.4",s:"DPT",diff:2,
q:"A colleague proposes putting an experimental MCP server in project scope so the team can try it.",
ask:"What is the objection?",
a:["Project scope grants it to everyone immediately; keep experiments in user scope and promote to project once proven",
"Experimental servers cannot be added to project scope",
"Project scope requires a manifest entry",
"There is no objection"],
c:0,
w:["Correct. User scope for experiments, project scope once proven, is the documented progression.",
"They can be, which is why the discipline matters.",
"No manifest is involved.",
"Granting an untested capability to every developer's agent is a real risk."],
e:"The scope decision follows blast radius. An experimental server in project scope enlarges every developer's toolset immediately &mdash; degrading selection for everyone &mdash; and grants an untested capability team-wide. Keep it in user scope while it is being evaluated, and promote it to <code>.mcp.json</code> once it has proven useful and its surface has been reviewed.",
k:"Experiments in user scope, promote to project once proven. Project scope grants capability team-wide immediately."},

{i:962,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to understand how two modules interact, and each is small.",
ask:"What is reasonable?",
a:["Read both, since the question is about their interaction and the cost is small",
"Read only the interface between them",
"Grep for cross-references and read nothing",
"Read one and infer the other"],
c:0,
w:["Correct. When the question is about interaction and the cost is small, read both.",
"The interface shows the contract, not how each side actually uses it.",
"Cross-references locate the interaction without explaining it.",
"Inference about unread code is guessing."],
e:"Context discipline is about proportion, not minimalism. When the subject of the question is small enough to read fully, read it &mdash; partial reading to save a trivial amount of context risks missing the branch that answers the question. The discipline applies when the material is large: then narrow with search, read targeted ranges, and delegate the verbose sweep.",
k:"Read the subject of the question fully when it is small. Context discipline is proportion, not minimalism."},

{i:963,d:1,t:"1.1",s:"AAT",diff:2,
q:"Your agent loop handles <code>tool_use</code> and <code>end_turn</code> and treats everything else as an error to be surfaced.",
ask:"Is this acceptable?",
a:["It is safer than treating them as &ldquo;continue&rdquo;, but it mishandles <code>pause_turn</code> &mdash; a continuation signal &mdash; and turns recoverable truncation into a hard failure",
"Yes, it is fully correct",
"No, everything else should be treated as continue",
"Yes, provided the error message is clear"],
c:0,
w:["Correct. Failing loudly is better than executing partial calls, but two of the remaining values have better handling.",
"<code>pause_turn</code> and <code>max_tokens</code> both have specific correct handling.",
"Treating unknown reasons as continue is what causes partial tool calls to be executed.",
"A clear message does not recover a continuable turn."],
e:"Failing loudly on unknown stop reasons is a defensible default and much safer than the alternative. But two values reward specific handling: <code>pause_turn</code> means a server-tool loop hit its limit and the turn should be continued by sending the assistant content back, and <code>max_tokens</code> means retry with more headroom rather than fail. Handle those two, and keep loud failure as the fallback for anything genuinely unrecognised.",
k:"Fail loudly on unknown stop reasons, but handle <code>pause_turn</code> (continue) and <code>max_tokens</code> (retry) specifically."},

{i:964,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator delegates and the subagent returns something well-formed but answering a different question.",
ask:"What is the first thing to examine?",
a:["The delegation prompt's goal statement, since a well-formed answer to the wrong question usually means the goal was ambiguous",
"The subagent's tool list",
"The subagent's model",
"The coordinator's context size"],
c:0,
w:["Correct. Well-formed but off-target output points at the goal statement rather than capability.",
"Tool problems produce ungrounded or failed output, not a well-formed wrong answer.",
"Model capability does not cause a coherent answer to a different question.",
"Coordinator context does not shape the subagent's understanding."],
e:"Read the failure mode for the diagnosis. Ungrounded confident output suggests missing tools. Shape mismatches suggest a missing output contract. Shallow-but-valid output suggests missing quality criteria. A well-formed answer to a different question suggests the goal statement was ambiguous &mdash; which is the element you can least afford to leave implicit, since the failure looks like success.",
k:"Diagnose by failure shape: ungrounded &rarr; tools, wrong shape &rarr; contract, shallow &rarr; criteria, off-target &rarr; goal."},

{i:965,d:1,t:"1.3",s:"AAT",diff:2,
q:"You want a subagent to produce output another system will consume programmatically.",
ask:"What is the most reliable way to guarantee the shape?",
a:["Have it produce the output through a <code>tool_use</code> call against a JSON Schema, rather than asking for a format in the prompt",
"State the format very precisely in the prompt",
"Provide several examples of the format",
"Validate and retry until the shape is right"],
c:0,
w:["Correct. Schema-enforced tool output is a structural guarantee rather than a formatting request.",
"Precise prose is still a request the model may not satisfy.",
"Examples improve consistency without guaranteeing it.",
"Validate-and-retry is a useful fallback but costs round trips for something the schema prevents."],
e:"The structured-output pattern applies at agent boundaries exactly as it does for extraction: define the target shape as a tool's <code>input_schema</code>, force it, and read the object from <code>tool_use.input</code>. That eliminates syntax and shape errors structurally. Keep validate-and-retry for the semantic layer &mdash; the schema cannot check that the values are right.",
k:"Guarantee agent output shape with <code>tool_use</code> + JSON Schema. Retry loops are for semantics, not shape."},

{i:966,d:1,t:"1.4",s:"CSA",diff:2,
q:"A control is implemented in a hook and works, but the team wants to be able to change its threshold without a deployment.",
ask:"What should change?",
a:["Move the threshold into configuration or a policy service the hook reads, keeping the enforcement code stable",
"Move the whole control into the prompt",
"Remove the control",
"Hard-code several thresholds and switch between them"],
c:0,
w:["Correct. Separate the stable mechanism from the changeable policy parameters.",
"Moving an enforced control into the prompt loses the guarantee.",
"Removing it is not a response to a deployment-cadence concern.",
"Hard-coding alternatives still requires a deployment to add a new one."],
e:"Mechanism and policy change at different rates, so separate them. The hook &mdash; intercept, evaluate, decide, log &mdash; is stable code that rarely changes. The threshold, the permitted categories, the effective dates are policy that changes at business speed and belongs in configuration or a policy service. Log the policy version with each decision so an audit can reconstruct which rule applied when.",
k:"Separate stable enforcement mechanism from changeable policy parameters. Log the policy version with each decision."},

{i:967,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want to know whether a hook is firing at all in production.",
ask:"What should be in place?",
a:["Logging on every evaluation, including allows, so absence of log entries is itself a signal",
"Logging on blocks only",
"A test in CI",
"An alert when a block occurs"],
c:0,
w:["Correct. Logging allows too gives you the denominator and makes a silent hook detectable.",
"Blocks-only logging cannot distinguish a hook that never fires from one that never needs to block.",
"A CI test proves it works there, not that it is running in production.",
"Alerting on blocks has the same blind spot as blocks-only logging."],
e:"A control that silently stops running is the failure mode you least want and are least likely to notice. Logging every evaluation &mdash; allows and blocks &mdash; means the absence of entries is detectable and alertable. It also gives you the denominator needed to tell whether a control fires rarely and correctly or often and wrongly, which is what tells you the rule is drawn too broadly.",
k:"Log every hook evaluation, allows included. Blocks-only logging cannot detect a hook that stopped running."},

{i:968,d:1,t:"1.6",s:"CGC",diff:2,
q:"A plan's phases are ordered by dependency, but two adjacent phases would be far easier to verify together.",
ask:"What should you do?",
a:["Merge them, since the value of a phase boundary is verifiability and a boundary you cannot verify at provides none",
"Keep them separate for granularity",
"Reorder them",
"Add a third phase between them"],
c:0,
w:["Correct. A boundary that cannot be verified is not earning its overhead.",
"Granularity without verifiability is overhead with no checkpoint benefit.",
"Reordering does not change verifiability.",
"Adding a phase compounds the problem."],
e:"Phase boundaries exist to create checkpoints, so a boundary you cannot verify at provides nothing while still costing coordination. Merge adjacent phases whose combined result is the first checkable state &mdash; a compiling module, a passing test, a working endpoint. Verifiability, not granularity, is what sizes phases.",
k:"Merge phases whose boundary cannot be verified. A checkpoint you cannot check at is pure overhead."},

{i:969,d:1,t:"1.7",s:"CGC",diff:2,
q:"You want to hand a long-running task to a colleague mid-way.",
ask:"What should you hand over?",
a:["The plan file with progress marked, the findings file, and any decisions with their rationale &mdash; all artefacts that exist outside your session",
"Your session id",
"A verbal briefing",
"The full transcript"],
c:0,
w:["Correct. Handover works through durable artefacts, not through session state.",
"A session is a personal working context and is not a handover mechanism.",
"Verbal briefings lose exact paths, values and decisions.",
"A transcript is long, includes dead ends, and buries the conclusions."],
e:"Handover is the third payoff of externalisation, alongside context management and crash recovery. The plan file shows what was intended and how far it got; the findings file holds the exact paths, values and conclusions; the decision record explains why the approach is what it is. Together they let a colleague resume without re-deriving anything, and none of them depends on your session.",
k:"Hand over durable artefacts: plan with progress, findings, decisions with rationale. Sessions do not transfer."},

{i:970,d:5,t:"5.1",s:"CAP",diff:2,
q:"You are deciding what belongs in the conversation versus the facts block for a support agent.",
ask:"Which item belongs in the conversation?",
a:["The customer's explanation of what happened, in their own words",
"The verified customer identifier",
"The disputed amount",
"The decision to escalate"],
c:0,
w:["Correct. Narrative context is exactly what the conversation carries well and what summarisation can compress without harm.",
"An identifier must be exact and belongs in the protected block.",
"An amount must be exact and belongs in the protected block.",
"A decision taken is state that must persist exactly."],
e:"The dividing line is whether imprecision is harmful. Identifiers, amounts, dates and decisions must be exact and are destroyed first by summarisation &mdash; so they go in the code-maintained block. Narrative context tolerates compression: a summarised version of the customer's account of events is still useful, whereas a summarised version of the disputed amount is not.",
k:"Exact-value items go in the facts block; narrative that tolerates compression stays in the conversation."},

{i:971,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent has resolved everything it can and one item remains that requires a human.",
ask:"What should the closing message do?",
a:["State what was resolved, what remains, who will handle it and roughly when &mdash; so the customer knows the current position",
"Apologise for the escalation",
"List only the escalated item",
"End without summarising"],
c:0,
w:["Correct. A complete closing states the position on every tracked item.",
"An apology conveys no information about the position.",
"Omitting what was resolved leaves the customer unsure whether it was handled.",
"Ending without a summary leaves the customer to reconstruct it."],
e:"This closes the multi-aspect loop. Every item that was tracked gets a stated outcome &mdash; resolved, or escalated with an owner and a timeframe. It confirms coverage to the customer, sets accurate expectations on the outstanding item, and prevents the repeat contact that comes from uncertainty about whether the other items were handled.",
k:"Close by stating the outcome of every tracked item: resolved, or escalated with owner and timeframe."},

{i:972,d:5,t:"5.3",s:"MAR",diff:2,
q:"A coordinator's recovery decision turns out to have been wrong in hindsight.",
ask:"What is worth examining?",
a:["Whether the error it received carried enough context to make a better decision possible",
"Whether the coordinator's model was capable enough",
"Whether more retries would have helped",
"Whether the subagent should have decided instead"],
c:0,
w:["Correct. Poor recovery decisions usually trace to errors arriving without enough context.",
"Capability rarely compensates for missing information.",
"Retries are one option among several; the question is whether the choice was informed.",
"The subagent lacks the workflow context the decision requires."],
e:"Judge a recovery decision by the information available when it was made. If the error carried only a category and a message, and a better choice depended on knowing what partial results existed or what alternatives were viable, the fix is upstream in the error contract. Tracking recovery-choice correctness alongside local recovery and escalation rates is what surfaces this pattern.",
k:"Trace poor recovery decisions to the error contract. The fix is usually upstream, in what the error carried."},

{i:973,d:5,t:"5.4",s:"DPT",diff:2,
q:"You want to reduce how often a long session needs compaction.",
ask:"What has the largest effect?",
a:["Reducing what enters the context in the first place &mdash; trimmed tool output, scoped subagent returns, delegated exploration",
"Compacting earlier",
"Using a larger context window",
"Reducing the system prompt"],
c:0,
w:["Correct. Compaction frequency is driven by inflow, so reducing inflow is the lever.",
"Earlier compaction increases how often it happens.",
"A larger window delays it without reducing accumulation.",
"The system prompt is fixed overhead, not the growth driver."],
e:"Distinguish fixed overhead from accumulation. The system prompt and tool definitions are bounded, and their cost is best addressed by prompt caching rather than by cutting content. Accumulation is what forces compaction: verbose tool results, unscoped subagent returns, and completed phase output. Attack those three and compaction becomes rare rather than routine.",
k:"Compaction frequency is driven by inflow. Trim tool output, scope returns, delegate exploration &mdash; not a bigger window."},

{i:974,d:5,t:"5.5",s:"SDE",diff:2,
q:"You want to justify keeping human review on a field that has measured 99.2% accuracy.",
ask:"What would make that justifiable?",
a:["The cost of the 0.8% &mdash; if an error is expensive or hard to detect downstream, a high accuracy rate does not by itself justify removing the check",
"A preference for caution",
"The field's importance to the business",
"The volume of documents"],
c:0,
w:["Correct. Automation decisions combine measured error rate with the consequence of an error.",
"Caution without a cost argument is not a justification.",
"Importance is vague; the cost of an undetected error is the operative fact.",
"Volume affects total errors but not whether the rate is acceptable."],
e:"Threshold decisions need both inputs. 99.2% means roughly one error in 125 documents; whether that is acceptable depends entirely on what an error costs and whether it would be caught downstream. A misread reference number caught by a later lookup is cheap; a misread payment amount that reaches a ledger is not. State the error rate and the consequence together &mdash; that is what a risk decision needs.",
k:"Automation decisions combine measured error rate with the cost of an undetected error. State both."},

{i:975,d:5,t:"5.6",s:"MAR",diff:2,
q:"A finding is well-supported but the sources all come from parties with an interest in the conclusion.",
ask:"What should the report do?",
a:["State the sources' interest alongside the finding, so the reader can weigh it appropriately",
"Exclude the finding",
"Present it as well-supported without qualification",
"Seek only independent sources and report nothing otherwise"],
c:0,
w:["Correct. Source characteristics are part of the finding and belong with it.",
"Interested sources are often the only ones with the data.",
"Multiple interested sources are not independent corroboration in the way the count suggests.",
"Refusing to report anything without independent sourcing discards most available evidence."],
e:"This extends the independence principle beyond citation chains. Three vendor-funded surveys agreeing is weaker evidence than the count implies, because they share an interest rather than merely a source. State it &mdash; &ldquo;based on three industry surveys, all vendor-funded; independent data was not available&rdquo; &mdash; which lets the reader calibrate and simultaneously identifies what would strengthen the finding.",
k:"Shared interest weakens corroboration like shared sourcing does. State source characteristics with the finding."},

{i:976,d:2,t:"2.1",s:"SDE",diff:2,
q:"A tool's description mentions every edge case it handles, running to several hundred words of exceptions.",
ask:"What should you consider?",
a:["Whether the exceptions belong in the description at all, since selection needs the boundary and the input contract rather than an exhaustive behaviour catalogue",
"Whether to make it longer still",
"Whether to remove the description entirely",
"Whether the tool is too complex"],
c:0,
w:["Correct. Exhaustive behaviour documentation serves integrators; selection needs less and different content.",
"Adding more compounds the recurring token cost.",
"Minimal descriptions cause unreliable selection.",
"Possibly true, but the description question is separable."],
e:"Descriptions are billed on every request, so include what changes the calling decision: what it does, when to choose it, input format with an example, what it returns, and the boundary against neighbours. Edge-case behaviour that does not affect whether or how to call belongs in integrator documentation. Where an edge case <em>does</em> affect the decision, state it briefly rather than exhaustively.",
k:"Descriptions carry what changes the calling decision. Exhaustive behaviour catalogues belong in integrator docs."},

{i:977,d:2,t:"2.2",s:"CSA",diff:2,
q:"An error message is accurate and specific but written in a tone that would alarm a customer if relayed.",
ask:"What should change?",
a:["Separate the model-facing detail from a user-safe message, so the agent knows which part is appropriate to relay",
"Soften the message for both audiences",
"Remove the detail",
"Instruct the agent to rephrase before relaying"],
c:0,
w:["Correct. Structural separation removes the judgement rather than relying on it.",
"Softening the model-facing detail loses precision the agent needs for recovery.",
"Removing detail degrades recovery.",
"Asking the agent to judge what is safe to relay is a probabilistic guard on an information-disclosure question."],
e:"Error responses have distinct audiences and should mark them structurally. A <code>userMessage</code> field carries plain, appropriately-toned text the agent may relay; the model-facing fields carry the precise detail and recovery signals; internal diagnostics go to your logs behind a correlation id. Making the separation explicit means the agent does not have to judge what is safe to repeat.",
k:"Mark audiences structurally: user-safe message, model-facing detail, internal diagnostics. Do not rely on judgement."},

{i:978,d:3,t:"3.1",s:"CGC",diff:2,
q:"You want to check whether a large CLAUDE.md is actually helping.",
ask:"What is a practical test?",
a:["Remove a section and see whether behaviour changes; content whose absence changes nothing is not earning its context",
"Count how many lines it has",
"Ask developers whether they find it useful",
"Check it loads via <code>/context</code>"],
c:0,
w:["Correct. Ablation is the direct test of whether content is doing work.",
"Length indicates risk without identifying which content is useless.",
"CLAUDE.md is read by the agent, so developer opinion is not the measure.",
"Confirming it loads tells you it is present, not that it helps."],
e:"Ablation is the honest test. Remove a section, run a representative set of tasks, and see whether behaviour changes. Content whose absence changes nothing is costing context on every request for no benefit &mdash; and it is diluting the instructions that do work. This pairs with the documented trim heuristic: cut what is derivable, keep pitfalls, rationale, and conventions differing from defaults.",
k:"Test CLAUDE.md content by ablation. If removing it changes nothing, it is diluting the content that works."},

{i:979,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator's trace shows it dispatched the correct agents in a sensible order, but the result is still poor.",
ask:"Where should you look next?",
a:["At what was actually sent in each delegation, since correct routing with poor prompts produces poor results",
"At the coordinator's model",
"At the number of subagents",
"At the order of dispatch"],
c:0,
w:["Correct. Routing and delegation quality are separate; the trace confirmed the first, not the second.",
"Model capability does not fix an under-specified delegation.",
"Count is not indicated by this symptom.",
"The trace already showed the order was sensible."],
e:"A trace showing correct routing rules out one class of failure and leaves the delegation content unexamined. Check each prompt for the four elements: goal, full inputs, output contract, quality criteria. Missing goal produces off-target work; missing inputs produces generic work; missing contract produces shape mismatches; missing criteria produces shallow-but-valid results. All four look like &ldquo;correct routing, poor result&rdquo;.",
k:"Correct routing does not imply good delegation. Check goal, inputs, output contract and quality criteria in each prompt."},

{i:980,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want a field that holds a date to be unambiguous about which calendar convention applies.",
ask:"What should the schema and description do?",
a:["Specify a single canonical format such as ISO 8601 in the description and constrain it in the schema, so <code>03/04/2026</code> can never be ambiguous",
"Accept any date format and parse leniently",
"Add a separate field for the format used",
"Instruct the model to use the document's format"],
c:0,
w:["Correct. A single canonical format removes the ambiguity entirely.",
"Lenient parsing silently picks one reading of a genuinely ambiguous value.",
"A format field pushes the ambiguity downstream rather than resolving it.",
"Preserving the source format preserves the ambiguity."],
e:"Date ambiguity is silent and expensive: <code>03/04/2026</code> has two valid readings and no error is raised either way. Mandate ISO 8601 in the description, constrain it with a <code>format</code> in the schema, and demonstrate it in an example. Where the source is genuinely ambiguous about which convention it used, that is an ambiguous-source case for human review, not something to resolve by guessing.",
k:"Mandate ISO 8601 in description and schema. Ambiguous source conventions route to review, not to a guess."},

{i:981,d:5,t:"5.1",s:"AAT",diff:2,
q:"You are told the facts block should contain everything important.",
ask:"What is the accurate framing?",
a:["It should contain what must be exact and would be damaged by summarisation &mdash; importance alone would grow it until it loses salience",
"Yes, everything important belongs there",
"Only numbers belong there",
"Only identifiers belong there"],
c:0,
w:["Correct. The criterion is exactness under summarisation, not importance, because the block must stay small to work.",
"An important-things block grows until it reintroduces the middle-position problem.",
"Decisions and constraints belong there too.",
"Amounts, dates and decisions belong there as well."],
e:"The block works because it is small enough to stay salient in a privileged position. &ldquo;Important&rdquo; is an unbounded criterion that will fill it. The bounded criterion is: must be exact, and would be degraded by summarisation &mdash; identifiers, amounts, dates, decisions taken, hard constraints. Important narrative context is genuinely important and belongs in the conversation, where compression costs little.",
k:"The facts block criterion is exactness under summarisation, not importance. Importance is unbounded and fills it."},

{i:982,d:3,t:"3.6",s:"CCI",diff:2,
q:"A CI review posts findings but developers cannot tell which are blocking and which are advisory.",
ask:"What should the output convey?",
a:["An explicit severity per finding, with the pipeline gating only on the blocking levels",
"A longer explanation per finding",
"Fewer findings",
"Findings sorted by file"],
c:0,
w:["Correct. Severity separates blocking from advisory, and the pipeline enforces the distinction.",
"Length does not convey whether a finding blocks.",
"Reducing volume does not classify what remains.",
"File ordering is organisation, not classification."],
e:"Severity has two jobs: it tells the developer what must be addressed now, and it gives the pipeline a field to gate on. Both depend on severity being assigned consistently, which needs explicit criteria with a code example per level. Without that, severity is assigned inconsistently and the gate becomes arbitrary &mdash; which is its own source of trust erosion.",
k:"Severity separates blocking from advisory and gives the pipeline its gate. Anchor each level with a code example."},

{i:983,d:2,t:"2.3",s:"AAT",diff:2,
q:"An agent's tool list is right but it still occasionally picks a tool that produces a wrong outcome rather than merely an inefficient one.",
ask:"How should you treat this?",
a:["As higher priority than an inefficient choice &mdash; and consider whether the wrong-outcome tool should be reachable by this agent at all",
"The same as any other selection error",
"As acceptable if it is rare",
"As a model capability problem"],
c:0,
w:["Correct. Grade selection errors by consequence, and consider removing the capability where the outcome is genuinely wrong.",
"Wrong-outcome errors deserve more urgency than inefficient-but-correct ones.",
"Rarity does not make a wrong outcome acceptable if the consequence matters.",
"Capability is rarely the cause when the toolset is otherwise appropriate."],
e:"Selection errors are not equivalent. Choosing a broad tool that gets the right answer less efficiently is a description fix with a modest cost. Choosing a tool that produces a wrong outcome or an unintended side effect warrants both a description fix and a capability question: should this agent be able to reach that tool at all, or does the wrong outcome indicate the tool belongs to another role?",
k:"Grade selection errors by consequence. Wrong-outcome errors warrant a capability question, not just a description fix."},

{i:984,d:1,t:"1.4",s:"CSA",diff:2,
q:"You want to introduce an enforcement control without disrupting current operations.",
ask:"What is the safe sequence?",
a:["Shadow-mode it first, review what it would have blocked, adjust the rule, then enable enforcement",
"Enable it and adjust based on complaints",
"Enable it only for new customers",
"Enable it with a very high threshold and lower it gradually"],
c:0,
w:["Correct. Shadow mode reveals false blocks before they affect anyone.",
"Learning from complaints means legitimate operations were already blocked.",
"Partial rollout by customer segment leaves the control absent where it is needed.",
"Gradual tightening is slow and still blocks legitimately at each step."],
e:"Shadow mode is the standard rollout for any control: evaluate the rule, log the decision, take no action. Reviewing what it <em>would</em> have blocked reveals an over-broad condition before it rejects legitimate work. Once the decisions look right, enable enforcement &mdash; and keep logging allows as well as blocks, which is what tells you afterwards whether the rule is firing appropriately.",
k:"Roll out controls in shadow mode: evaluate and log without acting, review decisions, then enforce."},

{i:985,d:4,t:"4.2",s:"SDE",diff:2,
q:"You have measured that removing your few-shot examples does not change accuracy.",
ask:"What should you do?",
a:["Remove them, since they are costing tokens on every request without changing behaviour",
"Keep them in case they help later",
"Add more examples",
"Make them longer"],
c:0,
w:["Correct. Measured no-effect content should be removed; it can be reinstated if a need appears.",
"Keeping measured-useless content costs on every request indefinitely.",
"Adding to something with no measured effect is unlikely to help.",
"Length compounds the cost."],
e:"Examples must earn their recurring cost, and you have measured that these do not &mdash; likely because the schema already guarantees the shape and the fields are unambiguous. Remove them and re-measure. If accuracy degrades on a segment you did not sample, that tells you where examples <em>are</em> needed, which is more useful than keeping a blanket set.",
k:"Remove measured no-effect examples. If accuracy then degrades on a segment, that is where examples are actually needed."},

{i:986,d:5,t:"5.4",s:"DPT",diff:2,
q:"You want to know whether delegating exploration to a subagent actually helped.",
ask:"What would you compare?",
a:["Main-session context consumption and result quality with and without delegation, on comparable tasks",
"The subagent's runtime",
"The number of tool calls the subagent made",
"The length of the subagent's summary"],
c:0,
w:["Correct. The two things delegation is meant to buy are context protection and quality.",
"Runtime is a cost input, not a measure of the benefit.",
"Call count measures activity.",
"Summary length is not a quality measure."],
e:"Delegation buys context isolation and, where the subagent is specialised, better results on its slice. Measure both: how much main-session context was consumed, and whether the result quality held. If the summary is too thin to work from, the return contract needs tightening rather than the delegation abandoning &mdash; that is the common failure mode when delegation appears not to help.",
k:"Measure delegation by main-session context saved and result quality retained. Thin summaries mean a weak return contract."},

{i:987,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a skill to be able to run a specific git command without prompting the developer for permission each time.",
ask:"Which field does this?",
a:["<code>allowed-tools</code> with a scoped pattern such as <code>Bash(git status *)</code>, which pre-approves that command for the turn",
"<code>disallowed-tools</code>",
"<code>context: fork</code>",
"<code>permissionMode</code>"],
c:0,
w:["Correct. This is what <code>allowed-tools</code> genuinely does &mdash; pre-approval, scoped to a pattern, for that turn.",
"That removes tools rather than pre-approving them.",
"Forking isolates context.",
"<code>permissionMode</code> is a subagent field, not skill frontmatter."],
e:"This is <code>allowed-tools</code> used correctly, which is worth practising because the field is so often misdescribed. It <em>pre-approves</em> listed tools so they run without a permission prompt during that turn, supports scoped patterns like <code>Bash(git add *)</code>, and clears when you send your next message. It does not restrict &mdash; unlisted tools remain callable, and <code>disallowed-tools</code> is the field that removes them.",
k:"<code>allowed-tools</code> pre-approves scoped patterns like <code>Bash(git add *)</code> for the turn. It never restricts."},

{i:988,d:1,t:"1.6",s:"MAR",diff:2,
q:"A coordinator must decide whether to decompose further or dispatch as-is.",
ask:"What is the practical test?",
a:["Whether a single subagent could cover the scope well within a reasonable number of steps &mdash; if not, split; if easily, do not",
"Whether there are more than three sub-topics",
"Whether the topic name contains a conjunction",
"Whether more agents are available"],
c:0,
w:["Correct. Size the slice to what one agent can cover well, and stop there.",
"A count threshold is arbitrary.",
"Surface grammar is not a decomposition criterion.",
"Availability is not a design criterion."],
e:"Decomposition has an optimum: too coarse gives shallow coverage, too fine gives coordination overhead and fragmented synthesis. The practical test is whether one agent can cover the slice well &mdash; and pair it with the coherence test, that each slice should produce something meaningful on its own. A slice failing either test needs resizing.",
k:"Size slices to what one agent covers well, and check each produces something meaningful alone."},

{i:989,d:2,t:"2.4",s:"DPT",diff:2,
q:"You want to reduce the tool surface an MCP server contributes without losing the operations you use.",
ask:"What are your options?",
a:["Withhold the unused tools per agent with <code>disallowedTools</code>, or wrap the server to expose only what you use",
"Reduce the number of agents",
"Increase the context window",
"Use a more capable model"],
c:0,
w:["Correct. Both narrow the effective surface without losing needed operations.",
"Fewer agents does not reduce any one agent's toolset.",
"Window size does not address selection reliability.",
"Capability does not compensate for an oversized choice space."],
e:"Two levers, chosen by scope. Per-agent withholding &mdash; <code>disallowedTools</code> with specific names or a <code>mcp__&lt;server&gt;</code> pattern &mdash; is quick and keeps the server available to agents that need it. A wrapper server is more work but narrows the surface for everyone, and gives you a place to normalise output and enforce policy. Use the first for a per-role concern, the second for an organisation-wide one.",
k:"Narrow MCP surfaces per-agent with <code>disallowedTools</code>, or organisation-wide with a wrapper server."},

{i:990,d:5,t:"5.5",s:"SDE",diff:2,
q:"You want the human review queue to stay a manageable size as volume grows.",
ask:"What is the sustainable approach?",
a:["Improve accuracy on the segments that dominate the queue, so a smaller proportion qualifies as volume grows",
"Raise the threshold as volume grows",
"Sample the queue rather than reviewing all of it",
"Add reviewers proportionally"],
c:0,
w:["Correct. Reducing the proportion needing review is the only approach that scales without raising error rates.",
"Raising the threshold reduces the queue by letting more errors through unreviewed.",
"Sampling the queue means genuinely problematic items are auto-approved unreviewed.",
"Linear reviewer growth is not sustainable and does not improve the system."],
e:"There are only two ways to shrink a review queue: send fewer items, or fix more items before they qualify. Only the second preserves the error rate. Use the per-field and per-type breakdown of what reaches review to find the dominant contributors, improve their descriptions, examples or schema, re-measure, and the qualifying proportion falls as volume grows.",
k:"Shrink review queues by improving the dominant segments' accuracy, not by raising thresholds or sampling the queue."}
);
