QB.push(
{i:631,d:1,t:"1.1",s:"CAP",diff:2,
q:"A conversational agent needs to handle a user who changes topic entirely mid-conversation.",
ask:"What should happen to the prior topic's context?",
a:["Keep the established facts available but stop treating the prior topic's constraints as governing the new one",
"Clear all context and start fresh",
"Continue applying the prior topic's constraints",
"Ask the user to start a new conversation"],
c:0,
w:["Correct. Facts remain useful; constraints that were scoped to the old topic should not govern the new one.",
"Clearing loses verified identity and other durable facts that still apply.",
"Applying stale topic-specific constraints produces confusing behaviour.",
"Unnecessary friction; topic changes are normal in conversation."],
e:"Distinguish durable facts from scoped constraints. Verified identity, account details and confirmed preferences carry across topics and belong in the persistent facts block. Constraints established for a specific request &mdash; &ldquo;only show me refunds from March&rdquo; &mdash; were scoped to that topic and should not silently govern the next one. Marking scope in the facts block is what makes this manageable.",
k:"Durable facts persist across topic changes; topic-scoped constraints do not. Mark scope in the facts block."},

{i:632,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator receives results but cannot tell which subagent produced which, because the results are unlabelled.",
ask:"What is the fix?",
a:["Require each subagent's output to identify itself and its assigned scope, so results can be attributed and coverage checked",
"Rely on the order results arrive in",
"Use a different model per subagent so the styles differ",
"Have the coordinator infer the source from the content"],
c:0,
w:["Correct. Self-identifying results are what make attribution and coverage checking possible.",
"Arrival order is unreliable when subagents run concurrently.",
"Style differences are an unreliable and accidental signal.",
"Inference from content is guesswork and fails when scopes overlap."],
e:"Attribution at the agent boundary matters for the same reason it matters at the source boundary. If each result identifies the subagent and the scope it was assigned, the coordinator can check coverage against the partition, attribute findings correctly, and diagnose which subagent underdelivered. Without it, a coverage gap is invisible and a re-dispatch has nothing to target.",
k:"Subagent results must identify the agent and its assigned scope. That is what makes coverage checkable."},

{i:633,d:1,t:"1.3",s:"AAT",diff:2,
q:"A delegation prompt is 200 words of instructions and 12,000 words of inherited data.",
ask:"How should it be structured?",
a:["Data in a clearly delimited block, instructions stated after it, with a short orientation line before the data",
"Instructions buried in the middle of the data",
"Data first with no delimiters and instructions at the end",
"Instructions first, then the data, with no delimiters"],
c:0,
w:["Correct. Bulk data early and delimited, instruction late where attention is strong, plus an orientation line so the model knows what it is reading.",
"Instructions inside the data block risk being read as content.",
"Undelimited bulk data blurs into the instruction, and instructions can be read as part of the data.",
"Instructions first means they compete with 12,000 words that follow; and undelimited data is still a confusion risk."],
e:"Two principles combine. <strong>Delimit and label</strong> inherited context &mdash; XML-style tags such as <code>&lt;prior_findings&gt;</code> and <code>&lt;task&gt;</code> work well with Claude and prevent data being read as instruction. <strong>Position deliberately</strong> &mdash; models attend most reliably to the start and end, so bulk data goes early with a brief orientation line, and the instruction goes last where it is most salient.",
k:"Delimit and label bulk data, orient briefly before it, and put the instruction last where attention is strongest."},

{i:634,d:1,t:"1.4",s:"CSA",diff:2,
q:"A control must apply to actions taken by both your agent and a legacy batch process.",
ask:"Where should it live?",
a:["In the shared resource or service both call, so it holds regardless of which caller invokes it",
"In an Agent SDK hook",
"In the agent's system prompt",
"In both the agent's hook and the batch process's code, maintained separately"],
c:0,
w:["Correct. A control shared by multiple callers belongs at the resource, where all paths converge.",
"A hook covers only the agent path, leaving the batch process unconstrained.",
"A prompt covers neither reliably and only one path at all.",
"Two separate implementations of one rule will drift apart."],
e:"Place controls where all paths converge. A hook is the right layer for agent-specific enforcement and gives an early, explainable block, but when several systems can produce the restricted outcome, the authoritative check belongs in the resource they share. That is the same reasoning as putting the identity precondition inside <code>process_refund</code> rather than only in a hook.",
k:"Shared controls live at the shared resource, where all callers converge. Hooks add an agent-side layer on top."},

{i:635,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want to add a hook without risking existing behaviour.",
ask:"What is the safest rollout?",
a:["Run it in observe-only mode first, logging what it would have done, then enable enforcement once the decisions look correct",
"Enable it in production immediately and monitor",
"Enable it only in development permanently",
"Enable it for a random subset of requests"],
c:0,
w:["Correct. Shadow mode reveals false blocks before they affect anyone.",
"An over-broad hook blocking legitimate calls is a live incident.",
"Development-only enforcement means the control never actually applies where it matters.",
"Random enforcement produces inconsistent behaviour and is hard to reason about."],
e:"Shadow mode is the standard rollout for any enforcement change: evaluate the rule, log the decision, take no action. Reviewing what it <em>would</em> have blocked reveals over-broad conditions before they reject legitimate calls. Once the decisions look right, enable enforcement &mdash; and keep logging both allows and blocks, which is what makes the control auditable.",
k:"Roll out enforcement in shadow mode first: evaluate and log without acting, review the decisions, then enable."},

{i:636,d:1,t:"1.6",s:"CGC",diff:2,
q:"A task's steps are known, but one step's duration varies from seconds to minutes depending on input.",
ask:"What does this suggest about the pipeline design?",
a:["The variable step should have its own timeout and recovery policy, and may warrant isolating so it does not block the rest",
"The whole pipeline should be made adaptive",
"The variable step should be removed",
"The pipeline should always assume the worst-case duration"],
c:0,
w:["Correct. Bound the variable step and consider decoupling it from the critical path.",
"The steps are known, so the pipeline can stay fixed; only the variable step needs special handling.",
"Removing a needed step is not the answer.",
"Designing everything around the worst case makes every run as slow as the worst one."],
e:"Variable-duration steps deserve targeted handling rather than a redesign of the whole pipeline. Give the step its own timeout sized to its realistic upper bound, define what happens when it fires, and if its output is not on the critical path, let downstream work proceed and fold the result in when it arrives &mdash; the same decoupling as the slow optional research subagent.",
k:"Bound variable steps individually and decouple them from the critical path if their output is not required immediately."},

{i:637,d:1,t:"1.7",s:"AAT",diff:2,
q:"You need to know whether a resumed workflow's stored state is still valid.",
ask:"What should the state include?",
a:["Enough context about the world it assumed &mdash; file versions, source timestamps, input identifiers &mdash; to detect that things have changed",
"Only the phase number and completion flags",
"A timestamp of when it was written",
"The model version used"],
c:0,
w:["Correct. Validity checking requires recording the assumptions, so divergence is detectable.",
"Completion flags say what happened, not whether it is still valid.",
"A timestamp tells you age, not whether anything relevant changed.",
"Version helps reproducibility but not staleness detection."],
e:"Stale state is the resumption hazard, and detecting it requires having recorded what the state assumed. File hashes or modification times, source document identifiers and versions, and input record ids let a resumed run compare against the current world and discover that phase four's inputs have changed. Without that, resumption silently proceeds on false premises.",
k:"Record the assumptions alongside the state &mdash; versions, hashes, identifiers &mdash; so staleness is detectable on resume."},

{i:638,d:2,t:"2.1",s:"AAT",diff:2,
q:"You have two tools whose descriptions are clear individually, but a request arrives that legitimately fits both.",
ask:"What should the descriptions add?",
a:["A stated preference rule for the overlapping case &mdash; which to prefer when both apply, and why",
"Nothing; overlap is unavoidable",
"A note that the model should choose randomly",
"Longer general descriptions of each"],
c:0,
w:["Correct. Where genuine overlap exists, state the tie-breaking rule explicitly.",
"Overlap is common but a preference rule makes the behaviour deterministic rather than arbitrary.",
"Random choice makes behaviour irreproducible, which is corrosive to debugging.",
"More general prose does not resolve a specific overlapping case."],
e:"Some overlap is legitimate &mdash; a narrow <code>verify_fact</code> and a broad <code>web_search</code> can both handle a simple check. State the preference explicitly: prefer the narrow tool when the input is a single specific claim; use the broad one when the question is open-ended. That makes behaviour deterministic and reproducible, which is what unpredictable routing costs you.",
k:"For legitimate overlap, state an explicit preference rule. Deterministic routing is what makes behaviour reproducible."},

{i:639,d:2,t:"2.2",s:"SDE",diff:2,
q:"A tool returns an error, and the agent's next action is to call a completely unrelated tool.",
ask:"What might the error response be missing?",
a:["Clear signalling that the failure is terminal for this path and what the appropriate next step is, leaving the agent to improvise",
"A timestamp",
"The tool's version number",
"A longer message"],
c:0,
w:["Correct. Without a clear terminal signal and a suggested path, the agent improvises &mdash; sometimes wildly.",
"Timestamps do not guide recovery.",
"Version information does not either.",
"Length is not the issue; direction is."],
e:"An error that says what went wrong but not what to do next leaves the agent to invent a recovery. Include the category, the retryability flag and, where there is a sensible next step, name it &mdash; escalate, request a different parameter, try the alternative source. That is the same content a hook's block message needs, for the same reason.",
k:"Errors should name the next step where one exists. Without direction, agents improvise recoveries."},

{i:640,d:2,t:"2.3",s:"AAT",diff:2,
q:"An agent's tools are correctly scoped, but it occasionally uses a read tool where a more specific tool exists.",
ask:"Is this a problem?",
a:["Mildly &mdash; it usually indicates the specific tool's description does not make its advantage clear, and it costs efficiency rather than correctness",
"Yes &mdash; it indicates a serious selection failure requiring redesign",
"No &mdash; any tool that produces the right answer is fine",
"Yes &mdash; the read tool should be removed"],
c:0,
w:["Correct. A correct-but-inefficient choice is a description problem with a modest cost.",
"Overstates it; the outcome is right, just less efficient.",
"Understates it; the specific tool exists because it is better here.",
"Removing a generally useful read tool to force one routing decision is disproportionate."],
e:"Grade selection errors by consequence. Choosing a broad tool that gets the right answer less efficiently is a description problem worth fixing cheaply &mdash; state the specific tool's advantage and when to prefer it. Choosing a tool that produces a wrong answer or an unintended side effect is a serious problem warranting redesign. Do not treat them alike.",
k:"Grade selection errors by consequence. Inefficient-but-correct is a description fix; wrong-outcome warrants redesign."},

{i:641,d:2,t:"2.4",s:"DPT",diff:2,
q:"A new MCP server would give agents access to customer personal data.",
ask:"What should be considered before adding it to project scope?",
a:["Whether every agent needs that access, what enters the context, and whether a narrower server exposing only necessary fields is better",
"Only whether the server is well-maintained",
"Only whether the credentials can be supplied via environment variables",
"Only whether it is a community or custom server"],
c:0,
w:["Correct. Data sensitivity raises questions about scope, context exposure and minimal surface.",
"Maintenance quality matters but is not the primary concern for sensitive data.",
"Credential handling is necessary but does not address what data enters the context.",
"Provenance of the server does not determine whether the access is appropriate."],
e:"Personal data entering the context means it enters transcripts, summaries and potentially logs. Ask three questions: does every agent need this, or should it be scoped to one specialist; can a narrower server expose only the fields actually needed; and should a <code>PostToolUse</code> hook redact sensitive fields before they reach the model. Minimising what enters the context is the durable control.",
k:"For sensitive data: scope per agent, expose minimal fields, and redact in a <code>PostToolUse</code> hook before context entry."},

{i:642,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to check whether a test suite passes.",
ask:"What should the tool return to be most useful?",
a:["Structured results &mdash; pass and fail counts, failing test names, and the relevant failure output &mdash; rather than raw console text",
"The full raw output of the test runner",
"A single boolean indicating pass or fail",
"The exit code only"],
c:0,
w:["Correct. Structured results are directly actionable and far smaller than raw output.",
"Raw console output is verbose, noisy and burns context on material the agent must parse.",
"A boolean says something failed without saying what, so the agent cannot act.",
"An exit code has the same problem as a boolean."],
e:"Tool output design determines how usefully an agent can act. Structured test results let it identify the specific failures and go straight to the relevant code; raw output forces it to parse noise and costs far more context. This is the same principle as trimming a 40-field response to 5: return what the consumer reasons over, not everything the underlying system emitted.",
k:"Return structured, actionable tool results &mdash; not raw output, and not a bare pass/fail."},

{i:643,d:3,t:"3.1",s:"CGC",diff:2,
q:"Your CLAUDE.md describes conventions in general terms and developers still see inconsistent output.",
ask:"What is likely missing?",
a:["Concrete examples showing the convention applied, since examples specify what general descriptions leave open",
"A stronger tone",
"More conventions",
"A different file location"],
c:0,
w:["Correct. Examples close the interpretation gap that general descriptions leave.",
"Emphasis does not add specificity.",
"More general conventions compound the problem.",
"Location is correct; content specificity is the issue."],
e:"The example principle applies to configuration as much as to prompts. &ldquo;Use descriptive variable names&rdquo; leaves everything open; a short before-and-after example specifies it. Keep examples compact &mdash; CLAUDE.md is a per-request context budget &mdash; but one concrete example is usually worth several sentences of description.",
k:"Add a compact concrete example to conventions in CLAUDE.md. Examples specify what descriptions leave open."},

{i:644,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill needs to behave differently in CI than locally.",
ask:"What is the cleanest approach?",
a:["Have the skill read an environment signal and branch, or maintain a separate CI-specific invocation, rather than encoding two behaviours ambiguously",
"Create two skills with similar names",
"Have the skill guess based on available tools",
"Have the skill always use the CI behaviour"],
c:0,
w:["Correct. Make the environment distinction explicit rather than implicit or guessed.",
"Similar names reintroduce the ambiguity and shadowing problems.",
"Guessing from available tools is fragile and produces surprising behaviour.",
"Applying CI behaviour locally would be wrong in the common case."],
e:"Make environmental differences explicit. An environment signal the skill reads, or a distinct CI invocation, both make the branch visible and testable. Inference &mdash; guessing from what tools happen to be available &mdash; produces behaviour that is hard to predict and harder to debug when the inference is wrong.",
k:"Make environment branches explicit via a signal or a distinct invocation. Never infer the environment."},

{i:645,d:3,t:"3.3",s:"CGC",diff:2,
q:"A path-scoped rule fires correctly but its content is too long, crowding out other loaded rules.",
ask:"What should you do?",
a:["Tighten the rule to the decisions it must encode, and split genuinely separate concerns into their own scoped rules",
"Broaden the glob so it loads less often",
"Move the content to CLAUDE.md",
"Accept it, since path scoping already limits when it loads"],
c:0,
w:["Correct. Scoping limits when a rule loads; length still matters when it does.",
"Broadening a glob makes it load <em>more</em> often, not less.",
"CLAUDE.md loads always, which is worse.",
"Scoping and concision are complementary, not alternatives."],
e:"Path scoping and concision solve different problems. Scoping ensures the rule loads only when relevant; concision ensures that when it does load, it does not crowd out everything else. Apply the same test as for CLAUDE.md lines &mdash; does this encode a decision the model would otherwise get wrong &mdash; and split separate concerns into separate scoped rules.",
k:"Scoping controls when a rule loads; concision controls its cost when it does. Both matter."},

{i:646,d:3,t:"3.4",s:"CGC",diff:2,
q:"You used planning mode, executed the plan, and the result is correct but took twice as long as expected.",
ask:"What is a reasonable retrospective conclusion?",
a:["Check whether the plan's phases were sized realistically; the planning itself is not automatically at fault for an underestimate",
"Planning mode should not have been used",
"The plan should have been more detailed",
"The task should have been split among more people"],
c:0,
w:["Correct. Estimation accuracy is a separate question from whether planning was the right process.",
"Planning likely prevented worse outcomes; the estimate was wrong, not the process.",
"More detail does not automatically improve estimation and costs planning time.",
"A resourcing conclusion drawn from a single estimate miss."],
e:"Separate the process decision from the estimate. Planning mode was appropriate if the task had scope, multiple approaches or architectural consequence &mdash; and it likely prevented rework. The overrun is an estimation question: were the phases sized realistically, did discovery reveal more than expected, was a phase larger than the plan implied? Feed that back into how you size future phases.",
k:"Judge the planning decision by the task's uncertainty, and the estimate separately. An overrun does not invalidate planning."},

{i:647,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want Claude to match an existing pattern in the codebase rather than inventing a new one.",
ask:"What is most effective?",
a:["Point to a specific existing file that exemplifies the pattern and ask for consistency with it",
"Describe the pattern in prose",
"Instruct it to follow existing conventions generally",
"Add a note to CLAUDE.md about consistency"],
c:0,
w:["Correct. A concrete exemplar is the most precise possible specification &mdash; the pattern as actually implemented.",
"Prose descriptions of a code pattern leave the details open.",
"A general consistency instruction does not identify which pattern applies here.",
"A general note has the same problem, at the cost of permanent context."],
e:"An existing file is a concrete example in the strongest possible form &mdash; it shows the pattern with all its real details, including the ones you would forget to describe. This is the example principle applied to code structure. Where the pattern is stable and recurring, also document it as a convention so the pointer is not needed every time.",
k:"Point at an exemplar file rather than describing a pattern. It is the most precise specification available."},

{i:648,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review is accurate but developers say its comments are hard to act on.",
ask:"What should the output format include?",
a:["The specific location, the concrete problem, and a suggested fix &mdash; so the developer can act without further investigation",
"A longer explanation of the general principle",
"A severity rating only",
"A link to documentation about the issue class"],
c:0,
w:["Correct. Location, problem and suggested fix is the actionable triple.",
"General principle without a specific fix leaves the developer to work it out.",
"A severity without a described problem is not actionable.",
"A link is a useful supplement but not a substitute for a specific fix."],
e:"The blueprint's example of a good few-shot review output format is exactly this: location, issue, severity, suggested fix. Actionability is what determines whether findings get addressed &mdash; a developer who has to investigate what a comment means is likely to skip it, which is the same trust erosion that false positives cause.",
k:"Review findings: location + concrete problem + severity + suggested fix. Actionability determines whether they get fixed."},

{i:649,d:4,t:"4.1",s:"SDE",diff:2,
q:"You want consistent behaviour on documents that contain contradictory information internally.",
ask:"What should the prompt specify?",
a:["Explicit handling &mdash; extract both values with their locations and flag the contradiction, rather than silently choosing one",
"An instruction to pick the most likely value",
"An instruction to skip contradictory documents",
"Nothing; the model will handle it sensibly"],
c:0,
w:["Correct. Preserve and flag rather than resolve silently &mdash; the same principle as conflicting sources.",
"Silent resolution hides a consequential judgement and produces inconsistent behaviour run to run.",
"Skipping loses documents that are mostly extractable.",
"Without specification, behaviour varies unpredictably across runs."],
e:"Internal contradictions in a document mirror conflicting sources in research: do not pick, do not hide. Extract both values with their locations, flag the contradiction in the output, and route to human review. That makes behaviour deterministic, gives the reviewer exactly what they need, and often reveals a systematic source problem when the same contradiction recurs.",
k:"Internal contradictions: extract both with locations, flag, route to review. Never resolve silently."},

{i:650,d:4,t:"4.2",s:"CSA",diff:2,
q:"You have few-shot examples for escalation but the agent still handles novel situations inconsistently.",
ask:"What should complement the examples?",
a:["Explicit criteria stating the general rule, since examples cover cases and criteria cover the space between them",
"More examples of the same kind",
"A confidence threshold",
"A larger model"],
c:0,
w:["Correct. Criteria generalise to cases the examples do not cover; the two techniques are complementary.",
"Examples cover cases; a novel situation between them still needs a rule.",
"Uncalibrated confidence is not a reliable trigger.",
"Capability helps at the margin without supplying the missing rule."],
e:"Examples and criteria do different work. Criteria state the general rule &mdash; escalate on an explicit request, a policy gap, or inability to progress &mdash; which extends to situations you never enumerated. Examples demonstrate the rule at the boundary, where it is hardest to apply. Strong prompts use both, and the exam's escalation questions consistently reward that combination.",
k:"Criteria generalise; examples illustrate the boundary. Use both, especially for decision rules."},

{i:651,d:4,t:"4.3",s:"SDE",diff:2,
q:"Your schema allows a field to be either a string or a number.",
ask:"What is the drawback?",
a:["Downstream code must handle both types on every read, and the ambiguity often indicates the field is doing two jobs",
"Union types are not supported in JSON Schema",
"The model cannot produce union-typed values",
"Union types always increase token usage"],
c:0,
w:["Correct. Union types push complexity downstream and usually signal a modelling problem.",
"Unions are supported; the objection is design, not capability.",
"The model handles them fine.",
"Token impact is negligible compared with the design cost."],
e:"A union-typed field usually means either the field should be split into two fields with distinct meanings, or one representation should be chosen and normalised to. Both are better than pushing type-checking into every consumer. The exception is a genuine nullable, which is a union with <code>null</code> and is the correct way to express absence.",
k:"Union-typed fields usually indicate a modelling problem. Split or normalise; nullable is the legitimate exception."},

{i:652,d:4,t:"4.4",s:"SDE",diff:2,
q:"A document fails validation on the same rule across three retry attempts, each producing a different value.",
ask:"What does this indicate?",
a:["The source is likely ambiguous for that field, so this should route to human review rather than consuming further retries",
"The retry budget should be increased",
"The validation rule is wrong",
"A larger model should be used"],
c:0,
w:["Correct. Different values each attempt is the signature of genuine source ambiguity.",
"More attempts of an ambiguous source produce more different values.",
"Possible, but instability across attempts points at the source rather than the rule &mdash; a wrong rule would usually reject consistent values.",
"Capability rarely resolves genuine ambiguity in the source."],
e:"Read the pattern across retries. Consistent wrong values suggest a systematic extraction problem &mdash; a prompt, schema or rule issue. Different values each attempt suggests the source genuinely does not determine the answer, which is exactly the ambiguous-source criterion for routing to human review. The instability itself is the diagnostic signal.",
k:"Consistent wrong values = systematic problem. Different values each retry = ambiguous source, route to review."},

{i:653,d:4,t:"4.5",s:"SDE",diff:2,
q:"You want to compare two prompts' quality on 40,000 documents cheaply.",
ask:"What is the sensible approach?",
a:["Compare on a labelled sample first, and run the full batch only with the winning prompt",
"Run both prompts over all 40,000 documents and compare",
"Run the new prompt on all documents and compare against last month's results",
"Run both and average the outputs"],
c:0,
w:["Correct. Sample comparison is cheap and controlled; full runs are for the prompt you have chosen.",
"Doubles the cost of the largest run to answer a question a sample answers.",
"Last month's documents are a different input set, so the comparison is uncontrolled.",
"Averaging two extractions produces incoherent records."],
e:"Compare on a stratified labelled sample &mdash; controlled, cheap and measurable at field and document-type level. Then run the full batch once with the winner. The blueprint's guidance to iterate on a sample before large-scale processing is exactly this, and it applies to every prompt change rather than only the first build.",
k:"Compare prompts on a stratified labelled sample; run the full batch once with the winner."},

{i:654,d:4,t:"4.6",s:"CCI",diff:2,
q:"Your generation and review instances use the same CLAUDE.md.",
ask:"Is this a problem for independence?",
a:["No &mdash; shared standards are desirable; independence concerns the generation <em>reasoning</em>, not the shared conventions",
"Yes &mdash; the reviewer must have entirely different context",
"Yes &mdash; CLAUDE.md should be excluded from review runs",
"No &mdash; but only if the reviewer uses a different model"],
c:0,
w:["Correct. Independence means not sharing the conversation that produced the code, not not sharing the standards.",
"Different standards would make the review measure the wrong thing.",
"Excluding CLAUDE.md would produce generic findings unaligned with team criteria.",
"A different model is not required; a different context is."],
e:"Be precise about what independence means. The problem with self-review is that the reviewer retains the reasoning it used to justify its choices. Shared standards are the opposite &mdash; they ensure both instances are working to the same bar, which is what makes findings relevant. The reviewer should see the diff and the standards, and nothing of the generating conversation.",
k:"Independence = no shared generation reasoning. Shared standards are desirable, not a contamination."},

{i:655,d:5,t:"5.1",s:"CAP",diff:2,
q:"Your facts block contains a value the user later corrects.",
ask:"What should happen?",
a:["Update the block to the corrected value and, where the earlier value influenced prior decisions, note that explicitly",
"Add the new value alongside the old one",
"Leave the block unchanged and rely on the conversation",
"Clear the whole block and rebuild it"],
c:0,
w:["Correct. Update it, and flag any decisions that were made on the superseded value.",
"Two values for one fact is exactly the ambiguity the block exists to prevent.",
"The block is what the model relies on; leaving it stale guarantees the wrong value is used.",
"Clearing loses every other established fact unnecessarily."],
e:"The facts block is authoritative, so it must be maintained. Update the corrected value and check the consequences &mdash; if a refund amount was calculated on the old figure, that decision needs revisiting. This is the same accuracy discipline as correcting a scratchpad: an authoritative record that is wrong is worse than no record.",
k:"Update the facts block on correction, and revisit decisions made on the superseded value."},

{i:656,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent escalates correctly but the human queue is currently very long.",
ask:"Should the agent change its escalation behaviour?",
a:["No &mdash; queue length is an operational capacity problem, not a reason to handle cases beyond the agent's authority",
"Yes &mdash; it should attempt more cases autonomously to reduce the queue",
"Yes &mdash; it should tell customers to try again later",
"No &mdash; but it should escalate faster to clear its own backlog"],
c:0,
w:["Correct. Authority boundaries do not move with queue depth.",
"Handling policy exceptions autonomously because humans are busy is exactly the risk escalation exists to manage.",
"Deflecting customers because of internal capacity is poor service and does not resolve their issue.",
"Speed of escalation does not address queue depth and is not the agent's concern."],
e:"Escalation criteria encode authority, not capacity. If the queue is long, the answers are operational &mdash; more staffing, better prioritisation, setting customer expectations about wait time, or a deliberate policy change that expands what the agent may decide. Silently widening the agent's authority under load is how the exceptions that most need human judgement get handled without it.",
k:"Escalation criteria encode authority, not capacity. Queue pressure is an operational problem, not a reason to widen authority."},

{i:657,d:5,t:"5.3",s:"MAR",diff:2,
q:"You want the coordinator to distinguish a subagent that returned quickly because the task was easy from one that returned quickly because it gave up.",
ask:"What should the output contract include?",
a:["An explicit statement of what was attempted and what coverage was achieved, so effort is visible rather than inferred from latency",
"The elapsed time of the subagent's run",
"A confidence score",
"The number of tool calls made"],
c:0,
w:["Correct. Stated coverage and attempts make effort inspectable; latency and call counts are weak proxies.",
"Latency conflates task difficulty with effort.",
"Self-rated confidence is uncalibrated.",
"Call count is a crude proxy that a thorough single call would fail."],
e:"Make effort and coverage explicit in the contract rather than inferring them from operational metrics. &ldquo;Searched these five sources, found relevant material in three, could not access one, coverage of the assigned scope is complete&rdquo; is inspectable. Latency and call counts vary for reasons unrelated to thoroughness and will mislead you in both directions.",
k:"Require stated attempts and coverage in the output contract. Latency and call counts are weak proxies for effort."},

{i:658,d:5,t:"5.4",s:"DPT",diff:2,
q:"An investigation spans two days. On the second day the agent needs a detail established on the first.",
ask:"What makes this reliable?",
a:["The detail was written to the findings file on day one, so day two reads it rather than depending on session continuity",
"Resuming the day-one session",
"Asking the agent to recall it",
"Re-deriving the detail from scratch"],
c:0,
w:["Correct. Externalised findings are reliable across days regardless of session state or filesystem changes.",
"Resuming works if nothing changed, but it is more fragile than a file.",
"There is no cross-session recall.",
"Re-deriving is expensive and may produce a different answer if the code has changed."],
e:"Multi-day work is exactly what externalised findings are for. The file survives session loss, machine changes and compaction, and it can be read selectively. Resumption is a reasonable complement when the context is still valid, but the file is what makes day two reliable regardless &mdash; and it is also what a colleague would need.",
k:"Multi-day work depends on externalised findings, not session continuity. Write as you go."},

{i:659,d:5,t:"5.5",s:"SDE",diff:2,
q:"You are asked how confident you are that automating a field is safe.",
ask:"What is the honest form of that answer?",
a:["The measured error rate for that field on that document type at the chosen threshold, with the sample size behind it",
"A qualitative statement that it seems reliable",
"The model's average self-reported confidence for that field",
"The proportion of extractions that pass schema validation"],
c:0,
w:["Correct. Measured error rate with a stated sample size is what a safety claim rests on.",
"Qualitative impressions are not a basis for an automation decision.",
"Self-reported confidence is uncalibrated until measured against ground truth.",
"Schema pass rate measures syntax, not correctness."],
e:"Safety claims need numbers with provenance: the measured error rate, the segment it applies to, the threshold it was measured at, and the sample size that supports it. Sample size matters because a rare document type validated on twenty examples has a wide confidence interval &mdash; which is why validation sets should over-sample rare and difficult segments.",
k:"Express automation safety as a measured error rate for the segment, at the threshold, with the sample size."},

{i:660,d:5,t:"5.6",s:"MAR",diff:2,
q:"A report needs to convey that a conclusion rests on limited evidence without discouraging its use entirely.",
ask:"How should this be expressed?",
a:["State the evidence base explicitly &mdash; how many sources, of what kind, and what would strengthen it &mdash; so the reader can weigh it",
"Add a hedge word to the conclusion",
"Omit the conclusion",
"State the conclusion with full confidence and note limitations in an appendix"],
c:0,
w:["Correct. Describing the evidence base lets the reader calibrate, and naming what would strengthen it is actionable.",
"Hedge words convey vague unease without saying what the limitation is.",
"Omission loses a conclusion that may be the best available.",
"Front-loading confidence and burying limitations in an appendix misleads the reader who stops at the conclusion."],
e:"Calibrated reporting describes the evidence rather than hedging the language. &ldquo;Based on two industry surveys from 2024, both funded by vendors in the sector; independent data was not available&rdquo; tells the reader exactly how much weight to place on it and what would improve it. This is the stable-versus-disputed structuring principle applied to evidence strength rather than to disagreement.",
k:"Describe the evidence base and what would strengthen it. Hedge words convey unease without information."},

{i:661,d:1,t:"1.2",s:"AAT",diff:2,
q:"A coordinator must choose between two subagents whose descriptions are both plausible for a request.",
ask:"What should happen, ideally?",
a:["The descriptions should have made the choice unambiguous; if they genuinely cannot, the boundary between the agents needs redesigning",
"The coordinator should dispatch to both and compare",
"The coordinator should pick the one used less recently",
"The coordinator should ask the user"],
c:0,
w:["Correct. Ambiguity in agent selection is a design problem, exactly as it is for tools.",
"Dispatching to both doubles cost and produces a reconciliation problem.",
"Recency is an arbitrary tie-break that makes routing irreproducible.",
"Users should not be asked to choose internal agents."],
e:"Agent selection follows tool selection rules. Sharpen the descriptions to state trigger conditions and boundaries; if a request genuinely fits both, add an explicit preference rule; and if the boundary cannot be articulated at all, the two agents' responsibilities overlap and should be merged or re-split. Unpredictable routing is corrosive because it makes behaviour irreproducible.",
k:"Ambiguous agent selection is a design problem. Sharpen descriptions, state a preference rule, or redesign the boundary."},

{i:662,d:2,t:"2.1",s:"DPT",diff:2,
q:"A tool's description mentions it is &ldquo;expensive to call&rdquo;.",
ask:"Is this useful?",
a:["Yes, if paired with guidance on when the cost is justified &mdash; cost alone is not actionable, but a preference rule is",
"No &mdash; cost information never belongs in a description",
"Yes &mdash; the model should always avoid expensive tools",
"No &mdash; the model cannot reason about cost"],
c:0,
w:["Correct. Cost becomes useful when it comes with a decision rule about when it is worth paying.",
"Cost can legitimately inform selection when framed as a preference.",
"Always avoiding an expensive tool means never using it, including when it is the right one.",
"Models reason about stated trade-offs perfectly well when given a rule."],
e:"Cost is a selection input only when it is expressed as a decision rule: &ldquo;this performs a full-corpus scan; prefer <code>search_index</code> for keyword lookups and use this only when the index is known not to cover the query&rdquo;. That is actionable. A bare &ldquo;expensive&rdquo; label leaves the model to invent a threshold, which produces inconsistent avoidance.",
k:"Express cost as a preference rule with conditions, not as a bare label. Labels invite inconsistent avoidance."},

{i:663,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill works well but developers rarely invoke it because they forget it exists.",
ask:"What would help most?",
a:["A clearer name and a short description of when to reach for it, plus a mention in CLAUDE.md pointing at the available skills",
"Forking its context",
"Restricting its tools",
"Adding an argument hint"],
c:0,
w:["Correct. This is a discoverability problem, addressed by naming, description and a pointer in the always-loaded configuration.",
"Context isolation does not affect whether anyone invokes it.",
"Tool restriction is a safety concern, not a discoverability one.",
"Argument hints help at invocation, after someone has decided to invoke it."],
e:"Discoverability is a distinct problem from capability. A clear name conveying the trigger, a short when-to-use description, and a pointer from CLAUDE.md to the available skills all help &mdash; and pruning skills nobody uses helps too, since a long list is as unmemorable as no list. Note that none of the frontmatter fields address this.",
k:"Skill discoverability comes from naming, when-to-use descriptions and a pointer in CLAUDE.md &mdash; not from frontmatter."},

{i:664,d:4,t:"4.2",s:"SDE",diff:2,
q:"You add a fifth few-shot example and accuracy does not improve.",
ask:"What should you conclude?",
a:["You may have reached the point of diminishing returns for examples; check whether the remaining errors have a different cause",
"Examples do not work for this task",
"Ten examples are needed",
"The examples should be longer"],
c:0,
w:["Correct. Flat returns suggest the remaining errors are not caused by unclear format or boundaries.",
"They evidently worked for the first four; the technique is not the issue.",
"Adding more of something that stopped helping is unlikely to help.",
"Length is not the lever."],
e:"Two to four targeted examples is the recommended range partly because returns flatten. When they do, diagnose the remaining errors rather than adding more: is the source ambiguous, is a schema field forcing fabrication, is the requirement itself under-specified, is a validation rule wrong? Each of those has a different remedy, and none of them is another example.",
k:"When example returns flatten, diagnose the remaining errors rather than adding examples. Different causes, different remedies."},

{i:665,d:5,t:"5.1",s:"AAT",diff:2,
q:"You are choosing what to include in a persistent block re-injected on every request.",
ask:"What is the test for inclusion?",
a:["Whether the item must be exactly right and would be damaged by summarisation &mdash; identifiers, amounts, dates, decisions, hard constraints",
"Whether the item is recent",
"Whether the item was mentioned more than once",
"Whether the item is long"],
c:0,
w:["Correct. The block protects what must be exact and is degraded by summarisation.",
"Recency is handled by the conversation itself.",
"Repetition does not indicate that a value must be exact.",
"Long content is the worst candidate; the block must stay small to stay salient."],
e:"The block exists to protect a specific class of content: values that must be exactly right and that summarisation degrades first &mdash; identifiers, amounts, dates, decisions taken and hard constraints. Keep it small enough to stay salient, prune resolved entries, and let everything else live in the summarisable conversation where a little imprecision costs nothing.",
k:"Include in the facts block what must be exact and what summarisation would damage. Keep it small; prune resolved entries."}
);
