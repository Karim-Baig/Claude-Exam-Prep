QB.push(
{i:271,d:4,t:"4.1",s:"CCI",diff:2,
q:"Your review prompt says: <em>Check that code comments are accurate.</em> The bot flags comments that are merely incomplete or stylistically dated, producing a 40% false-positive rate in that category.",
ask:"How should the criterion be rewritten?",
a:["Flag a comment only when it directly contradicts what the code does; do not flag comments that are incomplete, terse or stylistically dated",
"Check comment accuracy carefully and be more conservative in borderline cases",
"Flag comments only when you are highly confident they are wrong",
"Stop checking comments and focus on logic errors instead"],
c:0,
w:["Correct. A categorical criterion &mdash; contradiction, explicitly not incompleteness &mdash; is checkable and eliminates the false-positive class.",
"The blueprint specifically notes that generic guidance such as &ldquo;be more conservative&rdquo; works worse than concrete categorical criteria.",
"Model self-assessed confidence is uncalibrated, so this replaces one vague standard with another.",
"Eliminates false positives by eliminating the check, losing the genuine contradictions too."],
e:"This is the blueprint's own worked example: &ldquo;flag comments only when they contradict code&rdquo; versus &ldquo;check comment accuracy&rdquo;. The difference is that the first names a decidable property. Note the trap in option B &mdash; asking for conservatism sounds like tightening the criterion, but it gives the model no new information about which cases qualify, so it shifts the threshold arbitrarily rather than sharpening the boundary.",
k:"Explicit criteria beat vague ones. State what qualifies and what explicitly does not. &ldquo;Be conservative&rdquo; is not a criterion."},

{i:272,d:4,t:"4.1",s:"CCI",diff:3,
q:"Your review bot's security findings are highly accurate, but its &ldquo;maintainability&rdquo; findings are 45% false positives. Developers have started ignoring the bot entirely, including its security comments.",
ask:"What does this illustrate, and what should you do?",
a:["False positives in one category undermine trust in accurate categories; disable maintainability while you build explicit criteria for it",
"Developers are being unreasonable and should be told to read all comments",
"Add a confidence score to each comment so developers can filter",
"Reduce the total comment volume by sampling findings randomly"],
c:0,
w:["Correct. The blueprint states exactly this effect, and temporarily disabling the noisy category is the named remedy.",
"Trust is a rational response to observed precision; instructing people to ignore their experience does not work.",
"Model-generated confidence is uncalibrated, so the filter would be unreliable &mdash; and it pushes triage onto developers.",
"Random sampling drops accurate security findings at the same rate as noisy maintainability ones."],
e:"The blueprint highlights a specific and important effect: a high false-positive rate in some categories undermines trust in the accurate categories. Trust is not category-scoped in practice &mdash; developers form one impression of the bot. The remedy is to protect the accurate categories by removing the noisy one from production, develop explicit categorical criteria with code examples for it, measure, and only then reintroduce it.",
k:"Trust is global, not per-category. Disable noisy categories to protect accurate ones; reintroduce only after explicit criteria and measurement."},

{i:273,d:4,t:"4.1",s:"CCI",diff:2,
q:"You want your review bot to assign severity levels consistently across runs.",
ask:"What most improves consistency?",
a:["Define each severity level with explicit criteria and a code example of what qualifies at that level",
"Ask the model to be consistent in its severity assignments",
"Use only two severity levels to reduce the decision space",
"Let the model assign a numeric score and bucket it afterwards"],
c:0,
w:["Correct. The blueprint prescribes explicit severity criteria with code examples for each level.",
"An instruction to be consistent supplies no standard to be consistent against.",
"Fewer levels reduces expressiveness without making the remaining boundary any clearer.",
"An uncalibrated numeric score with arbitrary bucket boundaries is no more consistent than direct assignment."],
e:"Severity is a judgement, and judgements become consistent when they are anchored to examples. For each level, state what qualifies and show a short code snippet that exemplifies it &mdash; critical means exploitable security defect or data loss, with an example; major means incorrect behaviour under realistic inputs, with an example; minor means style and readability, with an example. Examples do the work that adjectives cannot.",
k:"Anchor severity levels to explicit criteria plus a code example each. Adjectives do not produce consistent classification."},

{i:274,d:4,t:"4.1",s:"SDE",diff:2,
q:"An extraction prompt says: <em>Extract the important fields from the invoice.</em> Output varies wildly between runs in which fields are included.",
ask:"What is the core problem?",
a:["&ldquo;Important&rdquo; is undefined, so the model applies a different interpretation each run; the required fields must be enumerated",
"The temperature is too high",
"The model needs a larger context window to see the whole invoice",
"Invoices are inherently too variable to extract reliably"],
c:0,
w:["Correct. An undefined selection criterion produces variable selection. Enumerate the fields.",
"Temperature affects phrasing variance, not which fields the model judges important.",
"Window size is unrelated to an undefined requirement.",
"Invoices vary, but that is what a defined schema handles &mdash; the prompt, not the domain, is the problem."],
e:"Vague selection criteria produce variable output by construction. The fix has two layers: enumerate the required fields explicitly in the prompt, and enforce the shape structurally with <code>tool_use</code> and a JSON Schema so the fields are guaranteed present. Add explicit handling for absence &mdash; nullable fields or an <code>unclear</code> enum value &mdash; so a missing field is reported rather than invented.",
k:"Enumerate required fields; never say &ldquo;important&rdquo;. Enforce with a JSON Schema and define how absence is represented."},

{i:275,d:4,t:"4.1",s:"CSA",diff:2,
q:"Your support agent's system prompt says: <em>Be helpful and resolve issues where possible.</em> Escalation behaviour is inconsistent.",
ask:"What should replace this?",
a:["Explicit criteria naming which situations to escalate and which to resolve autonomously, with examples of each",
"A stronger instruction to try harder before escalating",
"A target resolution percentage stated in the prompt",
"A rule to escalate whenever the agent is uncertain"],
c:0,
w:["Correct. The decision boundary must be defined explicitly, with examples on both sides.",
"&ldquo;Try harder&rdquo; shifts behaviour arbitrarily without defining where the line is.",
"Stating a metric target encourages gaming it &mdash; the agent avoids escalating cases it should escalate.",
"Model uncertainty is uncalibrated, so it is not a reliable trigger, and it will fire on cases the agent should handle."],
e:"Vague quality goals produce inconsistent behaviour, and the remedy is always the same: name the categories. Escalate on an explicit request for a human, on a policy gap or exception, and on demonstrated inability to progress; resolve autonomously when the case falls within documented policy. Then give few-shot examples from both sides of the line, because a boundary is only learnable from cases on each side of it.",
k:"Define decision boundaries by naming categories, then show examples from both sides. Metrics in prompts get gamed."},

{i:276,d:4,t:"4.2",s:"SDE",diff:2,
q:"Your extraction output is correct but formatted inconsistently &mdash; sometimes dates are written out, sometimes numeric; sometimes currency symbols are included, sometimes not.",
ask:"Which technique most directly addresses this?",
a:["Few-shot examples demonstrating the exact output format you want",
"A longer prose description of the formatting rules",
"A lower temperature setting",
"A larger model"],
c:0,
w:["Correct. Few-shot examples are the blueprint's stated most effective method for consistently formatted output.",
"Prose descriptions of format leave interpretation room that examples close.",
"Temperature reduces variance in phrasing but does not establish which format is intended.",
"Capability is not the constraint; the specification is."],
e:"The blueprint states that few-shot examples are the most effective method for producing consistently formatted, actionable output. Examples communicate what prose struggles to &mdash; exact date format, whether the currency symbol is included, how nulls appear, ordering of fields. For a hard guarantee on structure, combine few-shot with <code>tool_use</code> and a JSON Schema: the schema fixes the shape, the examples fix the conventions within it.",
k:"Few-shot examples are the most effective tool for output consistency. Combine with a JSON Schema when the shape must be guaranteed."},

{i:277,d:4,t:"4.2",s:"CCI",diff:3,
q:"Your review bot flags legitimate defensive coding patterns (early returns, defensive null checks) as issues, while missing some real defects.",
ask:"Which few-shot strategy helps most?",
a:["Provide contrastive examples showing acceptable patterns alongside genuine issues, with the rationale for each classification",
"Provide more examples of genuine issues only",
"Provide examples of acceptable patterns only",
"Provide a single very detailed example of a perfect review"],
c:0,
w:["Correct. A boundary is learned from cases on both sides, and the rationale is what makes it generalise.",
"One-sided examples teach the model what to flag but not what to leave alone &mdash; false positives persist.",
"The mirror problem: it suppresses false positives at the cost of missing genuine defects.",
"One example, however detailed, cannot delineate a boundary."],
e:"The blueprint names this: provide examples that distinguish acceptable code patterns from real issues. Contrastive pairs are especially powerful when they are near-misses &mdash; a defensive null check that is fine, alongside a null check that masks a genuine upstream bug &mdash; because the difference between them is exactly the distinction the model needs to make. Including the rationale helps it generalise rather than memorise.",
k:"Use contrastive few-shot pairs with rationale, especially near-misses. One-sided examples shift the error to the other side."},

{i:278,d:4,t:"4.2",s:"SDE",diff:2,
q:"How many few-shot examples does the blueprint suggest for clarifying ambiguous scenarios?",
ask:"Which range is recommended?",
a:["Two to four targeted examples, each with the rationale for its classification",
"At least twenty, to cover the full distribution",
"Exactly one, to avoid biasing the model",
"As many as the context window allows"],
c:0,
w:["Correct. The blueprint recommends two to four targeted examples for ambiguous scenarios, with rationale.",
"Twenty examples costs substantial context on every request for diminishing returns.",
"A single example cannot delineate a boundary and is more likely to be over-fitted.",
"Filling the window with examples crowds out the actual task and dilutes attention."],
e:"Few-shot is about targeting, not volume. Two to four well-chosen examples that span the interesting variation &mdash; especially the ambiguous and boundary cases &mdash; outperform a long list of similar happy paths, and cost far less context on every request. Include the rationale so the model learns the principle rather than pattern-matching the surface form.",
k:"Two to four targeted few-shot examples with rationale. Choose boundary cases; volume is not the lever."},

{i:279,d:4,t:"4.2",s:"MAR",diff:2,
q:"Your research agent occasionally fabricates plausible-sounding statistics when a source does not contain the figure it was asked for.",
ask:"Which combination best reduces this?",
a:["Few-shot examples demonstrating correct handling of missing data, plus a schema where the field is nullable with an explicit not-found representation",
"A stern instruction never to hallucinate",
"A lower temperature setting",
"A post-hoc fact-checking pass over the output"],
c:0,
w:["Correct. Show the model what &ldquo;not present&rdquo; looks like and give it a legitimate way to express it in the output.",
"Instructions against hallucination are weakly effective, because the model is not choosing to fabricate &mdash; it is completing a pattern.",
"Temperature reduces variance but a low-temperature model will confidently produce the same fabrication every time.",
"Useful defence in depth, but it detects fabrication after the fact rather than preventing it, and adds cost per item."],
e:"Fabrication often happens because the output format has no way to say &ldquo;absent&rdquo;. If every field must be filled with a plausible value, the model fills it. The blueprint prescribes two remedies together: few-shot examples that demonstrate correct extraction including missing data, and schema design that makes fields optional or nullable so the model is not forced to invent. Explicit enum values like <code>unclear</code> serve the same purpose for categorical fields.",
k:"Fabrication is often a schema problem. Make absence expressible (nullable, <code>not_found</code>, <code>unclear</code>) and demonstrate it in few-shot examples."},

{i:280,d:4,t:"4.2",s:"CSA",diff:3,
q:"Your agent handles common cases well but behaves unpredictably on genuinely ambiguous ones &mdash; a customer whose request is partly a return and partly a warranty claim, for instance.",
ask:"Which few-shot approach is most effective?",
a:["Examples specifically covering ambiguous cases, showing the reasoning that resolves them",
"More examples of the common cases the agent already handles well",
"A single example showing the agent asking a clarifying question",
"Examples drawn randomly from production transcripts"],
c:0,
w:["Correct. The blueprint recommends targeted examples for ambiguous scenarios, with rationale &mdash; examples should target the failure region.",
"Reinforcing behaviour that already works spends context without addressing the failures.",
"One example teaches that clarifying questions exist, not when they are the right move versus deciding.",
"Random production cases are dominated by the common cases, so the ambiguous region stays under-represented."],
e:"Spend your few-shot budget where the model is failing. For ambiguity that means examples showing the reasoning, not just the answer &mdash; why this request needs a clarifying question, why that one can be decided from the information already available. Recall the related principles: multiple customer matches call for asking for another identifier, and a policy gap calls for escalation. Examples should illustrate those distinctions concretely.",
k:"Target few-shot examples at the failure region, and show the reasoning. Examples of what already works are wasted context."},

{i:281,d:4,t:"4.3",s:"SDE",diff:2,
q:"Your pipeline asks Claude to &ldquo;respond in JSON&rdquo; and parses the response. Roughly 3% of responses fail to parse &mdash; trailing commentary, markdown fences, or a truncated object.",
ask:"What is the most reliable fix?",
a:["Define the output as a tool with a JSON Schema and read the structured object from the <code>tool_use</code> block",
"Add a stronger instruction to output only raw JSON with no commentary",
"Strip markdown fences and retry parsing failures",
"Ask for YAML instead, which is more forgiving to parse"],
c:0,
w:["Correct. <code>tool_use</code> with a JSON Schema is the blueprint's stated most reliable way to guarantee schema-conformant output and eliminate syntax errors.",
"A stronger instruction reduces the rate without eliminating it &mdash; it remains a formatting request.",
"Post-processing patches the symptom and cannot recover a genuinely truncated object.",
"YAML is significantly harder to parse unambiguously, and this does not address the underlying issue."],
e:"The blueprint is explicit: <code>tool_use</code> with JSON Schemas is the most reliable way to guarantee schema-conformant output and eliminate JSON syntax errors. Define a tool whose <code>input_schema</code> is your target schema, force it with <code>tool_choice</code>, and read the object from the <code>tool_use</code> block's <code>input</code> field. Remember the boundary of this guarantee &mdash; it eliminates <em>syntax</em> errors, not semantic ones.",
k:"Guaranteed structure = <code>tool_use</code> + JSON Schema + forced <code>tool_choice</code>. Asking for JSON in prose is a request, not a guarantee."},

{i:282,d:4,t:"4.3",s:"SDE",diff:3,
q:"After switching to <code>tool_use</code> with a strict JSON Schema, your extractions always parse correctly. But auditors find invoices where the line items do not sum to the stated total, and occasional cases where the vendor name appears in the customer field.",
ask:"What does this demonstrate?",
a:["Schemas eliminate syntax errors but not semantic errors; these need domain validation in code",
"The schema is not strict enough and needs more type constraints",
"The model needs a larger context window to see the whole invoice",
"Few-shot examples would have prevented these errors"],
c:0,
w:["Correct. The blueprint states this distinction explicitly &mdash; totals that do not reconcile and values in wrong fields are semantic errors.",
"No type constraint can express &ldquo;line items must sum to the total&rdquo; or &ldquo;this string is the vendor, not the customer&rdquo;.",
"Window size is not the issue; the extraction is well-formed but wrong.",
"Examples help accuracy but cannot guarantee arithmetic consistency &mdash; you need a check."],
e:"This is one of the most important distinctions in Domain 4. <strong>Syntax errors</strong> &mdash; malformed JSON, missing required fields, wrong types &mdash; are eliminated by <code>tool_use</code> with a schema. <strong>Semantic errors</strong> &mdash; totals that do not reconcile, values in the wrong field, dates outside a plausible range &mdash; are not, because the output is perfectly well-formed and merely wrong. Catch them with domain validation in code, and design the schema to make checking possible by extracting both <code>calculated_total</code> and <code>stated_total</code>.",
k:"Schemas kill syntax errors, never semantic ones. Add domain validation, and extract both computed and stated values so code can reconcile."},

{i:283,d:4,t:"4.3",s:"SDE",diff:2,
q:"Some source documents genuinely do not contain a purchase-order number. Your schema marks <code>po_number</code> as required.",
ask:"What is the likely consequence and the correct fix?",
a:["The model will invent a value to satisfy the schema; make the field optional or nullable so absence can be reported honestly",
"Extraction will fail with a validation error, which is the desired behaviour",
"The model will leave the field as an empty string, which is acceptable",
"Nothing &mdash; required fields are always present in practice"],
c:0,
w:["Correct. A required field with no source value pressures the model into fabrication; making absence expressible removes the pressure.",
"A hard failure is better than fabrication but still wrong &mdash; a document legitimately without a PO number is not an error case.",
"An empty string is ambiguous: it cannot be distinguished from a genuinely blank field, and downstream code treats it as data.",
"Optional data is common; assuming presence is exactly the flawed premise."],
e:"The blueprint prescribes making fields optional or nullable when the source may not contain the information, specifically to avoid fabricating values. Schema design shapes behaviour: a required field is an instruction to produce something. Give the model a truthful option &mdash; <code>null</code>, or an explicit <code>\"not_present\"</code> marker &mdash; and it will use it. Reserve <code>required</code> for fields that genuinely must exist in every valid document.",
k:"Required fields with absent source data invite fabrication. Make optional data nullable and give absence an explicit representation."},

{i:284,d:4,t:"4.3",s:"SDE",diff:3,
q:"Your document classifier uses an enum of five categories. Documents that fit none of them are being forced into the closest match, corrupting downstream analytics.",
ask:"What is the correct schema design?",
a:["Add <code>other</code> and <code>unclear</code> enum values, each paired with a free-text detail field capturing what the document actually is",
"Add fifteen more enum values to cover every case",
"Remove the enum and accept free-text classification",
"Route unclassifiable documents to a separate pipeline before classification"],
c:0,
w:["Correct. The blueprint prescribes enums with <code>other</code> plus a detail string for extensibility.",
"Enumerating every possibility is impossible and makes selection among twenty categories far less reliable.",
"Free text destroys the analytics value of a controlled vocabulary.",
"Circular &mdash; you need classification to know a document is unclassifiable."],
e:"The blueprint prescribes enum values such as <code>unclear</code> and <code>other</code> plus detail fields for extensible categorisation. This gives you three benefits at once: the model has a truthful option instead of forcing a bad fit; downstream analytics stay clean because the controlled vocabulary is preserved; and the detail field becomes a discovery channel &mdash; reviewing what lands in <code>other</code> tells you which sixth category you actually need.",
k:"Enums need <code>other</code> and <code>unclear</code> escape hatches with a detail field. They prevent forced fits and reveal missing categories."},

{i:285,d:4,t:"4.3",s:"SDE",diff:2,
q:"You have three different extraction schemas for three document types, and the model must always call one of them.",
ask:"Which <code>tool_choice</code> setting is correct?",
a:["<code>{\"type\": \"any\"}</code>",
"<code>{\"type\": \"auto\"}</code>",
"<code>{\"type\": \"tool\", \"name\": \"extract_invoice\"}</code>",
"<code>{\"type\": \"none\"}</code>"],
c:0,
w:["Correct. <code>any</code> guarantees a tool call while leaving the model free to pick the right schema for the document.",
"<code>auto</code> permits a text response, which breaks the guarantee.",
"Forcing one specific schema is wrong when the document type varies.",
"<code>none</code> forbids tools entirely."],
e:"The blueprint gives this precise guidance: use <code>tool_choice: \"any\"</code> to guarantee structured output when multiple schemas exist, and force a specific tool (<code>{\"type\": \"tool\", \"name\": \"extract_metadata\"}</code>) when there is exactly one target schema. The distinction is whether the model still needs to make a choice: <code>any</code> constrains it to structure while preserving the choice; a forced tool removes the choice as well.",
k:"Multiple valid schemas &rarr; <code>any</code>. One known schema &rarr; force that tool by name. Both guarantee structure."},

{i:286,d:4,t:"4.3",s:"AAT",diff:2,
q:"Which statement about JSON Schema use with <code>tool_use</code> is correct?",
ask:"Which is true?",
a:["It guarantees the output conforms to the declared structure and types, but says nothing about whether the values are semantically correct",
"It guarantees both structural and semantic correctness of the extracted values",
"It only works when exactly one tool is defined",
"It requires <code>tool_choice: \"none\"</code>"],
c:0,
w:["Correct. Structural guarantee, no semantic guarantee &mdash; the central distinction in this task area.",
"Nothing about a schema can verify that the extracted vendor name is the actual vendor.",
"Any number of tools may be defined; the schema constrains each tool's input.",
"<code>none</code> forbids tool use, which would prevent structured output entirely."],
e:"Say it precisely because the exam probes it repeatedly: a JSON Schema constrains structure &mdash; required fields, types, enum membership, formats &mdash; and the <code>tool_use</code> mechanism enforces that constraint. What it cannot do is check that the values are right. Semantic validation is domain logic in your code: totals reconcile, dates fall in plausible ranges, cross-field relationships hold, referenced entities exist.",
k:"Schema = structural guarantee only. Semantic correctness requires domain validation in your own code."},

{i:287,d:4,t:"4.4",s:"SDE",diff:2,
q:"An extraction fails validation because the total does not match the sum of line items. You want to retry effectively.",
ask:"What should the retry prompt contain?",
a:["The original document, the incorrect extraction, and the specific validation errors that were detected",
"Just the original document and an instruction to try again more carefully",
"Just the incorrect extraction with a note that it is wrong",
"The original document with the temperature raised for variety"],
c:0,
w:["Correct. The blueprint prescribes exactly this three-part retry payload: source, failed attempt, specific errors.",
"A blind retry from the same input reproduces the same error with high probability.",
"Without the source document the model cannot recompute the correct values.",
"Higher temperature produces a different wrong answer, not a corrected one."],
e:"Retry-with-error-feedback works because the second attempt has strictly more information than the first: it can see what it produced, what specifically was wrong, and the source to re-derive from. The blueprint names all three components. Contrast this with retrying a validation error <em>unchanged</em>, which is guaranteed to fail &mdash; the point is that the retry is materially different.",
k:"Retry payload = original source + the failed extraction + the specific validation errors. Blind retries repeat the failure."},

{i:288,d:4,t:"4.4",s:"SDE",diff:3,
q:"An extraction consistently omits the shipping address because the source invoice genuinely does not contain it &mdash; it lives in a separate shipping manifest.",
ask:"What should the pipeline do?",
a:["Recognise that retrying is futile since the information is not in the source, and either mark the field absent or fetch the manifest",
"Retry with stronger instructions to look harder for the address",
"Retry with a larger model",
"Lower the confidence threshold so the field passes validation"],
c:0,
w:["Correct. The blueprint explicitly notes that retries are ineffective when the information is simply absent from the source.",
"No amount of instruction extracts data that is not present.",
"Model capability cannot conjure absent information.",
"Weakening validation to accept a missing field hides the gap instead of resolving it."],
e:"A key judgement in Domain 4: recognising when retry <em>cannot</em> help. Retry-with-feedback works when the model had the information and produced it wrongly. It cannot work when the source does not contain the data. The correct responses are to mark the field genuinely absent, or to change the pipeline so the required source is available &mdash; fetching the manifest and extracting across both documents.",
k:"Retry only fixes wrong processing of present data. Absent data needs a different source or an honest null &mdash; never more retries."},

{i:289,d:4,t:"4.4",s:"CCI",diff:3,
q:"You want to analyse why your review bot produces certain false positives, but the findings only contain a description and a severity.",
ask:"What should be added to each finding?",
a:["A <code>detected_pattern</code> field recording what specifically triggered the finding, so false positives can be grouped and analysed",
"A confidence score from 1 to 10",
"The full file contents that were analysed",
"A timestamp and the model version"],
c:0,
w:["Correct. The blueprint prescribes tracking the pattern that triggered a finding to enable false-positive analysis.",
"Uncalibrated self-assessed confidence tells you nothing about which pattern misfired.",
"Storing full file contents is expensive and still does not identify the triggering pattern.",
"Useful operational metadata, but it does not explain why a finding fired."],
e:"To improve precision you must be able to group failures by cause. A <code>detected_pattern</code> field &mdash; naming the rule or heuristic that produced the finding &mdash; lets you see that, say, 80% of your false positives come from one pattern, which you can then rewrite with explicit criteria or disable. Without it, every false positive is an anecdote rather than a data point.",
k:"Record <code>detected_pattern</code> on every finding. You cannot fix false positives you cannot group by cause."},

{i:290,d:4,t:"4.4",s:"SDE",diff:3,
q:"You want the extraction itself to expose arithmetic inconsistencies rather than relying on a separate reconciliation step.",
ask:"How should the schema be designed?",
a:["Extract both <code>stated_total</code> and <code>calculated_total</code> as separate fields so code can compare them directly",
"Add a boolean field asking the model whether the arithmetic is correct",
"Add an instruction telling the model to verify its own arithmetic",
"Extract only the line items and always compute the total in code"],
c:0,
w:["Correct. The blueprint prescribes exactly this self-correction design.",
"A self-assessed boolean is an uncalibrated judgement, not a check.",
"Self-verification in the same pass is unreliable and produces no artefact you can validate.",
"Reasonable in isolation, but it discards the stated total &mdash; and a mismatch between stated and computed is itself the valuable signal, often indicating an OCR error or a missed line item."],
e:"The blueprint names this: extract both <code>calculated_total</code> and <code>stated_total</code> to detect discrepancies. Extracting both turns an internal consistency property into two comparable data points your code can check deterministically. The mismatch is diagnostic in itself &mdash; it usually means a line item was missed or misread, which is exactly the case that should be routed to human review.",
k:"Extract both stated and computed values so code can reconcile them. The discrepancy is a signal, not just an error."},

{i:291,d:4,t:"4.4",s:"SDE",diff:2,
q:"Your validation-and-retry loop retries up to five times. Some documents cycle through all five attempts and still fail.",
ask:"What should happen after the retry budget is exhausted?",
a:["Route the document to human review with the validation errors and the best attempt attached",
"Persist the best attempt anyway and flag it in the database",
"Retry indefinitely until it succeeds",
"Discard the document and log a failure"],
c:0,
w:["Correct. Exhausted automation is a human-review case, and attaching the errors and best attempt makes the review fast.",
"Persisting known-invalid data corrupts the warehouse and downstream consumers cannot tell.",
"Unbounded retries burn cost on documents that will never validate.",
"Silently discarding loses the document entirely, and the failure log alone gives the reviewer nothing to work from."],
e:"Every automated loop needs a defined terminal state. When retries are exhausted, the document has not become unimportant &mdash; it has become a human's problem. Route it with everything the reviewer needs: the source, the best attempt, and the specific validation failures. This also creates the feedback channel: patterns in what reaches human review tell you which prompt or schema change would raise automation rates.",
k:"Exhausted retries &rarr; human review with source, best attempt and specific errors attached. Never persist invalid data or discard silently."},

{i:292,d:4,t:"4.5",s:"SDE",diff:2,
q:"You must process 40,000 documents for a quarterly audit report due in four days. Latency per document is irrelevant.",
ask:"Which API approach is appropriate?",
a:["The Message Batches API, which offers around 50% cost savings with processing typically within 24 hours",
"The synchronous Messages API with high concurrency",
"The synchronous Messages API processed serially overnight",
"Streaming responses to reduce perceived latency"],
c:0,
w:["Correct. Non-latency-sensitive bulk work with a generous deadline is the canonical Batch API case.",
"Works but forfeits roughly half the cost saving and requires managing rate limits and concurrency yourself.",
"Serial processing of 40,000 documents is needlessly slow and equally expensive.",
"Streaming affects perceived latency for interactive use and is irrelevant to a batch job."],
e:"The Message Batches API offers approximately 50% cost savings with a processing window of up to 24 hours and no latency SLA. That makes it right for overnight reports, audits and backfills, and wrong for anything blocking &mdash; a pre-merge CI check cannot wait an indeterminate number of hours. Note the other constraint the blueprint highlights: the Batch API does not support multi-turn tool calling within a single request.",
k:"Batch API: ~50% cheaper, up to 24h window, no latency SLA. For non-blocking bulk work only."},

{i:293,d:4,t:"4.5",s:"CCI",diff:3,
q:"An engineer proposes moving your pre-merge CI security check to the Message Batches API to halve costs.",
ask:"What is the problem?",
a:["A pre-merge check is blocking, and the Batch API offers no latency guarantee &mdash; results may take hours",
"The Batch API does not support security analysis",
"The Batch API is more expensive for small volumes",
"CI systems cannot make API calls to batch endpoints"],
c:0,
w:["Correct. Blocking checks require bounded latency, which the Batch API explicitly does not provide.",
"There is no content restriction; the constraint is timing.",
"Batch is cheaper regardless of volume; the trade is latency, not price.",
"CI can call any endpoint; the issue is how long it must wait."],
e:"The blueprint draws this line explicitly: Batch suits non-blocking tasks such as overnight reports and audits, and is unsuitable for blocking tasks such as pre-merge checks. The decision rule is simply whether someone or something is waiting on the result. A developer waiting to merge is waiting; a report generated before Monday morning is not.",
k:"Is anything blocked on the result? Yes &rarr; synchronous. No, and the deadline is generous &rarr; Batch."},

{i:294,d:4,t:"4.5",s:"SDE",diff:2,
q:"How do you match responses back to the correct input document in a Message Batches job?",
ask:"Which mechanism is used?",
a:["The <code>custom_id</code> you assign to each request in the batch",
"The order of results, which matches the order of submission",
"A hash of the request body",
"The <code>request_id</code> returned in the response headers"],
c:0,
w:["Correct. <code>custom_id</code> is the caller-supplied correlation key for batch requests and responses.",
"Batch results are not guaranteed to return in submission order, so positional matching is unsafe.",
"Not a supported mechanism, and it would break if two documents produced identical requests.",
"That identifies an HTTP request for support purposes, not a batch item."],
e:"<code>custom_id</code> is how you correlate batch requests with their responses, and it does more than identify results: when part of a batch fails, you re-submit only the failed items identified by their <code>custom_id</code> rather than reprocessing everything. Choose an id that is meaningful in your own system &mdash; the document id &mdash; so correlation is trivial on the way back.",
k:"<code>custom_id</code> correlates batch requests and responses, and identifies which items to re-submit after partial failure."},

{i:295,d:4,t:"4.5",s:"SDE",diff:3,
q:"You have a 30-hour SLA for processing submitted documents, and the Batch API's window is up to 24 hours.",
ask:"What submission cadence gives you adequate headroom?",
a:["Submit batches roughly every four hours, so any document waits at most about four hours before its batch starts and still completes well inside 30",
"Submit once every 24 hours",
"Submit once every 30 hours to match the SLA exactly",
"Submit each document individually as it arrives"],
c:0,
w:["Correct. Frequent submission bounds queue-wait time, leaving the 24-hour processing window comfortably inside the 30-hour SLA.",
"A document arriving just after a submission waits nearly 24 hours before processing even begins, then up to 24 more &mdash; a potential 48-hour total.",
"Guarantees SLA breaches, since processing has not started when the deadline arrives.",
"Single-document batches forfeit batching efficiency and complicate management."],
e:"The blueprint gives this exact planning example &mdash; four-hour windows for a 30-hour guarantee with 24-hour processing. The key insight is that total time is <em>queue wait plus processing</em>, not processing alone. Submission cadence controls the queue-wait component, so shortening it is how you buy headroom without changing the processing characteristics.",
k:"Total = queue wait + processing window. Shorten the submission interval to bound queue wait and protect the SLA."},

{i:296,d:4,t:"4.5",s:"SDE",diff:2,
q:"Part of a 5,000-document batch fails. What is the correct handling?",
ask:"Which approach is right?",
a:["Re-submit only the failed items, identified by their <code>custom_id</code>",
"Re-submit the entire batch to keep processing consistent",
"Process the failed items synchronously to avoid another batch cycle",
"Discard the failed items and report on the successful ones"],
c:0,
w:["Correct. The blueprint prescribes re-submitting only failed documents identified by <code>custom_id</code>.",
"Reprocessing 5,000 documents to fix a handful wastes cost and time.",
"Defensible if the failures are few and urgent, but it forfeits the batch discount and is not the prescribed approach.",
"Silently dropping documents produces an incomplete audit with no indication that anything is missing."],
e:"Batch failures are per-item, so recovery should be per-item. Use <code>custom_id</code> to identify exactly which documents failed, inspect why (validation, content, transient), and re-submit that subset. If the same items fail repeatedly, they are a different problem &mdash; typically documents needing human review rather than another automated attempt.",
k:"Batch failures are per-item. Re-submit only the failed <code>custom_id</code>s; repeated failures mean human review, not more retries."},

{i:297,d:4,t:"4.5",s:"AAT",diff:3,
q:"You want to run an agentic workflow with multi-turn tool calling for 10,000 documents using the Batch API.",
ask:"What is the constraint?",
a:["The Batch API does not support multi-turn tool calling within a single batch request",
"The Batch API limits batches to 1,000 requests",
"The Batch API does not support JSON Schemas",
"The Batch API cannot use system prompts"],
c:0,
w:["Correct. The blueprint states that Batch does not support multi-turn tool calling within a single request.",
"Not the constraint being tested here.",
"Structured output via tool schemas works fine in batch requests.",
"System prompts are supported."],
e:"A batch request is a single model call, not an agent loop. That makes Batch excellent for single-shot work &mdash; classification, extraction with a forced tool call, summarisation &mdash; and unsuitable for workflows that need several tool-calling round trips per item. If your per-item workflow is genuinely agentic, either restructure it into single-shot steps that can each be batched, or run it synchronously.",
k:"Batch = single-shot calls. No multi-turn tool loops per request. Restructure into single-shot steps or run synchronously."},

{i:298,d:4,t:"4.5",s:"SDE",diff:2,
q:"Before running a 40,000-document batch, what should you do first?",
ask:"Which step is most important?",
a:["Iterate on the prompt against a representative sample until quality is acceptable",
"Submit the full batch and inspect the results afterwards",
"Split into forty batches of 1,000 to limit exposure",
"Raise the temperature to increase output variety"],
c:0,
w:["Correct. The blueprint prescribes iterating on prompts using a sample before large-scale processing.",
"Discovering a prompt defect after 40,000 documents wastes the entire run's cost and up to 24 hours.",
"Splitting limits blast radius but still runs a flawed prompt over the first 1,000 &mdash; and does not tell you the prompt is flawed.",
"Variety is the opposite of what extraction consistency requires."],
e:"Batch runs are slow to fail: you learn about a prompt defect up to 24 hours and 40,000 documents later. Sample first &mdash; choose documents spanning the layouts and edge cases you expect, measure field-level accuracy against labelled expectations, and iterate until it holds. Note the related warning that aggregate accuracy can mask poor performance on specific document types, so stratify the sample rather than drawing it uniformly.",
k:"Always sample-test before a large batch. Stratify the sample by document type; aggregate accuracy hides type-specific failures."},

{i:299,d:4,t:"4.6",s:"CCI",diff:3,
q:"Your agent generates code and then reviews it within the same session, reporting it clean. Independent review finds real defects.",
ask:"Why is self-review weak here?",
a:["The instance retains the reasoning that produced the code and is unlikely to challenge decisions it just argued for",
"The model cannot read code it has written in the same session",
"Review requires a different model architecture from generation",
"The context window is exhausted by the generation, leaving no room for review"],
c:0,
w:["Correct. The blueprint states this directly: the model retains its reasoning context and is less likely to challenge its own decisions.",
"It can read it perfectly well &mdash; the problem is disposition, not access.",
"The same model reviews effectively when given a clean context.",
"The failure occurs even with ample context remaining."],
e:"Self-review fails for the same reason a developer struggles to proofread their own writing: the reasoning that produced the artefact is still active and makes its choices feel justified. The remedy is context isolation &mdash; a separate instance that sees only the diff and the review standards, with no access to the conversation that produced it. Pair this with multi-pass review for the strongest coverage.",
k:"Self-review is weak because the generation reasoning is still in context. Review from an independent instance seeing only the diff."},

{i:300,d:4,t:"4.6",s:"CCI",diff:2,
q:"You are reviewing a change spanning 18 files. A single pass produces shallow feedback concentrated on the first few files.",
ask:"What is the correct architecture?",
a:["Per-file passes for local defects, plus a separate cross-file integration pass for interactions between the changed files",
"A single pass with a much longer and more detailed review prompt",
"Eighteen independent passes with no integration step",
"One pass per file, then average the severity ratings"],
c:0,
w:["Correct. The blueprint prescribes per-file local analysis plus a cross-file integration pass to avoid attention dilution.",
"A longer prompt does not counteract dilution across a large input.",
"Per-file passes alone are structurally blind to cross-file defects such as a changed signature with unupdated callers.",
"Averaging severities is meaningless and still lacks the integration pass."],
e:"Two defect classes need two mechanisms. Per-file passes recover full attention for local issues &mdash; missing validation, unsafe handling, logic errors. The integration pass catches what no per-file pass can see: signature changes with stale callers, broken contracts across module boundaries, migrations inconsistent with models. Give the integration pass the changed symbols and their call sites rather than all the diffs concatenated, or you reproduce the dilution.",
k:"Multi-pass review = per-file passes (local defects) + a cross-file integration pass (contracts, dataflow). Both are required."},

{i:301,d:4,t:"4.6",s:"MAR",diff:3,
q:"You want to route reviews so that uncertain cases get more scrutiny, using a verification pass.",
ask:"Which design is most defensible?",
a:["Use a verification pass with self-rated confidence to route, but calibrate those ratings against labelled data before trusting the thresholds",
"Trust the model's self-rated confidence directly as a routing signal",
"Route randomly, since confidence signals are unreliable",
"Route everything to full scrutiny regardless of confidence"],
c:0,
w:["Correct. The blueprint mentions verification passes with self-rated confidence for calibrated routing &mdash; with calibration being the operative word.",
"Uncalibrated self-rated confidence is repeatedly identified as unreliable; using it raw is the error the exam tests.",
"Random routing discards a signal that is genuinely informative once calibrated.",
"Uniform full scrutiny forfeits the efficiency that routing exists to provide."],
e:"Note the careful nuance here. The exam is consistently sceptical of self-rated confidence as a <em>direct</em> decision signal &mdash; for escalation, for skipping review, for authorising a write. But confidence is not useless: once calibrated against a labelled validation set, at field level rather than document level, it becomes a usable routing input with known error rates at each threshold. Calibrate first; never trust raw scores.",
k:"Self-rated confidence is unreliable raw and usable once calibrated against labelled data. Calibrate at field level, then set thresholds."},

{i:302,d:4,t:"4.6",s:"CCI",diff:2,
q:"Your review agent's per-file pass is excellent but it never notices when a change to a shared utility breaks assumptions in three consuming modules.",
ask:"What must be added?",
a:["A cross-file pass whose input is the changed symbols and the files that consume them",
"A larger context window for the per-file pass",
"A second per-file pass with stricter criteria",
"A rule that shared utilities must never be changed"],
c:0,
w:["Correct. The defect exists in the relationship between files, so the pass must be given that relationship.",
"Window size does not help when the consuming files were never included in the input.",
"Repeating a structurally blind pass produces the same blind spot.",
"A policy against changing shared code is unworkable and does not detect anything."],
e:"Constructing the integration pass well matters as much as having one. Concatenating every diff simply recreates the attention-dilution problem at a larger scale. Instead, identify the symbols that changed, find their consumers, and give the pass exactly that graph &mdash; the changed definitions plus the call sites that depend on them. That keeps the input focused on the relationships where cross-file defects actually live.",
k:"Build the integration pass from changed symbols + their consumers, not from all diffs concatenated."},

{i:303,d:4,t:"4.1",s:"GEN",diff:2,
q:"Which prompt instruction is most likely to improve accuracy on a classification task?",
ask:"Which is best?",
a:["A statement of what qualifies for each class and what explicitly does not, with a borderline example",
"An instruction to classify carefully and accurately",
"An instruction to think step by step before classifying",
"An instruction to only classify when confident"],
c:0,
w:["Correct. Explicit inclusion and exclusion criteria plus a borderline example define the decision boundary.",
"An exhortation with no content about where the boundary lies.",
"Reasoning guidance can help on genuinely multi-step problems, but it does not supply the missing criteria.",
"Uncalibrated confidence gating suppresses classifications without improving the ones that remain."],
e:"The consistent Domain 4 message: explicit criteria beat vague instructions. For classification that means stating for each class what qualifies, what does not, and showing a borderline case with the rationale for its assignment. Borderline examples are disproportionately valuable, because that is where the errors concentrate &mdash; the easy cases were never at risk.",
k:"Classification prompts: inclusion criteria + exclusion criteria + a borderline example with rationale."},

{i:304,d:4,t:"4.2",s:"GEN",diff:2,
q:"Which statement about few-shot prompting is most accurate?",
ask:"Which is true?",
a:["It is the most effective method for producing consistently formatted output, and helps the model generalise when examples include rationale",
"It works only for classification tasks",
"It requires at least ten examples to be effective",
"It is less effective than a detailed prose description of the desired format"],
c:0,
w:["Correct. The blueprint identifies few-shot as the most effective method for consistent, actionable output.",
"It applies broadly &mdash; extraction, formatting, tool selection, review classification, ambiguity handling.",
"Two to four targeted examples is the recommended range.",
"Examples consistently outperform prose descriptions of format."],
e:"Few-shot is the highest-leverage prompting technique in this exam's framing, with several documented uses: demonstrating output format, handling ambiguous cases, distinguishing acceptable patterns from real issues, demonstrating correct extraction across document structures, and reducing hallucination by showing how to represent absent data. Include rationale so the model generalises rather than pattern-matching the surface.",
k:"Few-shot: most effective for consistency. Two to four targeted examples with rationale, aimed at the failure region."},

{i:305,d:4,t:"4.3",s:"GEN",diff:2,
q:"Where do you read the structured object from when Claude responds with a forced tool call?",
ask:"Which is correct?",
a:["From the <code>input</code> field of the <code>tool_use</code> content block",
"From the assistant message's text content, parsed as JSON",
"From the <code>tool_result</code> block in the next user message",
"From the <code>output</code> field of the response"],
c:0,
w:["Correct. The model's structured output is the arguments it constructed, carried in <code>tool_use.input</code>.",
"There is no JSON in the text content &mdash; that is the pattern you moved away from.",
"<code>tool_result</code> is what <em>you</em> send back after executing a tool; it is not where the model's output lives.",
"No such field exists on the response."],
e:"The structured-output pattern works by repurposing the tool-calling mechanism: you define a tool whose <code>input_schema</code> is your target schema, force it with <code>tool_choice</code>, and the model's schema-conformant object arrives as the <code>input</code> of the <code>tool_use</code> block. You do not need to execute anything &mdash; the arguments <em>are</em> the output. That is why the guarantee is structural rather than a formatting request.",
k:"Structured output arrives as <code>tool_use.input</code>. The tool need not exist or execute &mdash; the arguments are the result."},

{i:306,d:4,t:"4.4",s:"GEN",diff:2,
q:"Which error class is <em>not</em> eliminated by using <code>tool_use</code> with a strict JSON Schema?",
ask:"Which remains?",
a:["Semantic errors, such as totals that do not reconcile or values placed in the wrong field",
"Malformed JSON syntax",
"Missing required fields",
"Values of the wrong type"],
c:0,
w:["Correct. Semantic correctness is outside what any structural schema can express.",
"Eliminated &mdash; the mechanism guarantees well-formed output.",
"Eliminated &mdash; <code>required</code> is enforced.",
"Eliminated &mdash; declared types are enforced."],
e:"Learn the boundary precisely, because the exam probes it from several directions. Schemas guarantee structure: well-formed JSON, required fields present, declared types respected, enum membership. They cannot express &ldquo;these numbers must sum&rdquo;, &ldquo;this string is the vendor rather than the customer&rdquo;, or &ldquo;this date must precede that one&rdquo;. Those need domain validation in code, plus schema design that makes checking possible.",
k:"Schemas guarantee structure, not meaning. Semantic errors need domain validation in code."},

{i:307,d:4,t:"4.5",s:"GEN",diff:1,
q:"What cost saving does the Message Batches API offer?",
ask:"Which is correct?",
a:["Approximately 50%",
"Approximately 10%",
"Approximately 90%",
"There is no cost difference; the benefit is throughput"],
c:0,
w:["Correct. Roughly 50% savings is the documented figure.",
"Understates it substantially.",
"Overstates it substantially.",
"There is a real and significant cost difference."],
e:"Memorise the Batch API's three defining characteristics: about 50% cost savings, a processing window of up to 24 hours, and no latency SLA. Those three together determine when it applies &mdash; bulk, non-blocking work with a generous deadline. Remember also that it does not support multi-turn tool calling within a single request.",
k:"Batch API: ~50% cheaper &middot; up to 24h window &middot; no latency SLA &middot; no multi-turn tool calling per request."},

{i:308,d:4,t:"4.6",s:"GEN",diff:2,
q:"Which review architecture is most likely to find subtle defects in generated code?",
ask:"Which is best?",
a:["An independent instance reviewing the diff without the generation context",
"The generating instance reviewing its own output immediately",
"The generating instance reviewing after a delay",
"The generating instance reviewing with a stricter prompt"],
c:0,
w:["Correct. Context isolation is what removes the anchoring that weakens self-review.",
"The generation reasoning is fully active, which is the weakest configuration.",
"Delay does not remove context &mdash; the session still carries the same reasoning.",
"A stricter prompt cannot dislodge the disposition created by having just made these decisions."],
e:"Independence is the operative variable, not effort or strictness. The reviewing instance should see the diff and the review standards from CLAUDE.md, and nothing of the conversation that produced the code. In CI this is naturally achieved by making review a separate invocation. Combine it with multi-pass review &mdash; per-file plus cross-file &mdash; for the strongest coverage.",
k:"Independence is what makes review effective, not strictness. Separate invocation, diff plus standards only."},

{i:309,d:4,t:"4.3",s:"SDE",diff:3,
q:"Your schema requires <code>confidence</code> as a number between 0 and 1 for each extracted field, and you plan to auto-approve anything above 0.9.",
ask:"What must you do before trusting that threshold?",
a:["Calibrate the confidence values against a labelled validation set, at field level, to learn the actual error rate above 0.9",
"Nothing &mdash; a 0.9 threshold is conservative enough by construction",
"Raise the threshold to 0.99 to be safe",
"Average confidence across fields and threshold on the document-level mean"],
c:0,
w:["Correct. Self-reported confidence is uncalibrated until measured against ground truth, and the blueprint prescribes field-level calibration.",
"0.9 on an uncalibrated scale carries no known error rate &mdash; it might be 2% wrong or 25%.",
"Moving an uncalibrated threshold changes the volume auto-approved, not the reliability of the signal.",
"Averaging masks a single badly-extracted critical field behind several easy ones &mdash; exactly the aggregation problem the blueprint warns about."],
e:"The blueprint prescribes field-level confidence calibration using labelled validation sets, and warns that aggregate metrics mask poor performance on specific fields and document types. Calibration means measuring, for each field and each confidence band, what fraction of extractions were actually correct. Only then does a threshold carry a known error rate you can accept or reject on business grounds.",
k:"Calibrate confidence at field level against labelled data before thresholding. Aggregates hide field-specific and type-specific failures."},

{i:310,d:4,t:"4.1",s:"SDE",diff:2,
q:"Two engineers disagree about whether to add more detail to an extraction prompt or add more few-shot examples.",
ask:"What is the most useful framing?",
a:["Use explicit criteria for decisions the model must make, and few-shot examples for formats and ambiguous cases &mdash; they solve different problems",
"Always prefer more prose detail, as it is easier to maintain",
"Always prefer more examples, as they are more effective in every case",
"It makes no difference; both consume the same context"],
c:0,
w:["Correct. They are complementary techniques addressing different failure modes.",
"Prose is easier to edit but weaker for formats and boundaries.",
"Examples are powerful but cannot express a general rule such as an escalation policy as compactly as criteria can.",
"They cost similar context but do very different work."],
e:"Match the technique to the failure. Inconsistent <em>decisions</em> &mdash; what counts as a defect, when to escalate, which severity applies &mdash; call for explicit criteria naming what qualifies and what does not. Inconsistent <em>format</em>, or poor handling of ambiguous and boundary cases, calls for few-shot examples. In practice strong prompts use both: criteria state the rule, examples demonstrate it at the edges.",
k:"Explicit criteria for decision rules; few-shot examples for formats and boundary cases. Strong prompts use both."}
);
