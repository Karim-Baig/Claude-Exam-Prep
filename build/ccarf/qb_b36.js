QB.push(
{i:1451,d:1,t:"1.1",s:"GEN",diff:2,
q:"Which statement about the Messages API agent loop is false?",
ask:"Which is FALSE?",
a:["Tool definitions are cached server-side after the first request of a conversation",
"The full conversation history must be sent on every request",
"<code>stop_reason</code> is the authoritative signal for whether to continue the loop",
"Every <code>tool_use</code> block must receive a matching <code>tool_result</code> in the next user message"],
c:0,
w:["Correct &mdash; this is the false statement. There is no server-side caching of tool definitions; prompt caching exists but is explicit and opt-in.",
"True. The API is stateless.",
"True. Branch on it rather than inspecting content.",
"True, or the API rejects the request."],
e:"Statelessness applies to the whole request &mdash; system prompt, tools and history all go on every call. The related mechanism people confuse this with is <strong>prompt caching</strong>, which is explicit: mark the stable prefix with <code>cache_control</code> and repeat sends are billed at a large discount. It reduces the cost of re-sending, not the need to re-send.",
k:"Nothing is cached implicitly. Prompt caching is explicit via <code>cache_control</code> and reduces cost, not the need to send."},

{i:1452,d:1,t:"1.2",s:"GEN",diff:2,
q:"Which is NOT a coordinator responsibility in hub-and-spoke orchestration?",
ask:"Which does the coordinator NOT do?",
a:["Performing the domain analysis within an assigned subtopic",
"Decomposing the task into subtasks",
"Aggregating subagent results",
"Handling errors and re-routing work"],
c:0,
w:["Correct &mdash; domain analysis is specialist work. The coordinator routes to the specialist rather than doing it.",
"A core coordinator responsibility.",
"A core coordinator responsibility.",
"A core coordinator responsibility."],
e:"Coordinator responsibilities: decomposition, delegation, aggregation, error handling, routing and dynamic selection of subagents. Everything domain-specific belongs to a specialist. The recurring exam distractor is a coordinator doing specialist work &mdash; concatenating raw outputs instead of dispatching synthesis, or analysing instead of delegating.",
k:"Coordinator: decompose, delegate, aggregate, handle errors, route. Specialists do the domain work."},

{i:1453,d:1,t:"1.4",s:"GEN",diff:2,
q:"Which requires programmatic enforcement rather than prompt guidance?",
ask:"Which one?",
a:["Identity verification before a financial transaction",
"Preferring concise responses over verbose ones",
"Acknowledging the customer's issue before proposing a solution",
"Preferring snake_case for variable names"],
c:0,
w:["Correct. A security control gating money &mdash; any residual failure rate is an audit finding.",
"A style preference; occasional verbosity costs nothing that matters.",
"A conversational quality guideline where deviation is tolerable.",
"A mechanical convention, better enforced by a linter than by either prompt or hook."],
e:"Apply the standard test: what does one violation in a hundred cost? Negligible &rarr; prompt, keeping it soft to preserve adaptability. Audit finding, breach or wrong refund &rarr; code: a tool precondition, a permission rule, or a blocking hook. Mechanically checkable &rarr; a linter, which is stronger than either and free at inference time.",
k:"Ask what one violation in a hundred costs. Negligible &rarr; prompt. Audit finding &rarr; code. Mechanical &rarr; linter."},

{i:1454,d:2,t:"2.2",s:"GEN",diff:2,
q:"Which error should be marked <code>isRetryable: false</code>?",
ask:"Which is NOT retryable?",
a:["A refund rejected because it exceeds the policy limit",
"A 503 from a downstream service under momentary load",
"A rate limit with a retry-after hint",
"A connection reset during a transient network blip"],
c:0,
w:["Correct. A business-rule violation is deterministic &mdash; the identical call fails every time.",
"Transient infrastructure failure; retry with backoff.",
"Rate limits resolve with time; retry after the indicated delay.",
"Transient; retry."],
e:"The classification test is whether the identical call would plausibly succeed shortly. Only transient infrastructure conditions qualify. Business rules, permission denials and validation errors are deterministic. Note the nuance for validation: the identical call is not retryable, but a <em>corrected</em> call is &mdash; which is why the message must state the expected format.",
k:"Retryable test: would the identical call succeed shortly? Only transient infrastructure. Business, permission, validation: no."},

{i:1455,d:2,t:"2.5",s:"GEN",diff:2,
q:"Which built-in tool should an agent reach for last?",
ask:"Which is the broadest, most dangerous grant?",
a:["<code>Bash</code>",
"<code>Grep</code>",
"<code>Glob</code>",
"<code>Read</code>"],
c:0,
w:["Correct. <code>Bash</code> confers arbitrary command execution &mdash; anything the shell can do.",
"Content search, narrow and safe.",
"Filename matching, narrow and safe.",
"File reading, narrow and safe."],
e:"<code>Bash</code> is the broadest possible tool grant, which is why the preference is for narrow purpose-built tools that both limit capability and return structured, actionable results. Where shell access is genuinely necessary &mdash; version-control state, for instance &mdash; constrain it with permission rules that allow-list specific commands, or intercept with a <code>PreToolUse</code> hook.",
k:"<code>Bash</code> = arbitrary execution. Prefer narrow tools; constrain it with allow-listed commands or a hook when unavoidable."},

{i:1456,d:3,t:"3.1",s:"GEN",diff:2,
q:"Which CLAUDE.md location is shared with the team through version control?",
ask:"Which one?",
a:["<code>./CLAUDE.md</code> or <code>./.claude/CLAUDE.md</code> in the project",
"<code>~/.claude/CLAUDE.md</code>",
"<code>./CLAUDE.local.md</code>",
"<code>/etc/claude-code/CLAUDE.md</code>"],
c:0,
w:["Correct. Project instructions, version-controlled and active on clone with no setup step.",
"User instructions &mdash; personal, all your projects, never shared.",
"Local instructions &mdash; personal, this project, intended to be gitignored.",
"Managed policy &mdash; machine-wide, deployed by IT, not via the repository."],
e:"Four scopes in load order: managed policy (machine-wide, non-excludable), user (<code>~/.claude/CLAUDE.md</code>), project (repo, shared), and local (<code>CLAUDE.local.md</code>, gitignored). They compose rather than override, ordered root-down so more specific content is read last. When only some developers see a behaviour, suspect user or local scope.",
k:"Four scopes: managed policy, user, project (shared), local (gitignored). They compose; specific content is read last."},

{i:1457,d:3,t:"3.2",s:"GEN",diff:2,
q:"Which <code>SKILL.md</code> frontmatter field runs the skill in an isolated subagent context?",
ask:"Which field?",
a:["<code>context: fork</code>",
"<code>allowed-tools</code>",
"<code>argument-hint</code>",
"<code>disable-model-invocation</code>"],
c:0,
w:["Correct. It runs the skill in a subagent context that does not see the conversation history.",
"That pre-approves tools for the turn; it does not isolate context.",
"That prompts for required parameters at invocation.",
"That prevents automatic invocation, leaving manual only."],
e:"<code>context: fork</code> is the isolation field, and it has companions worth knowing: <code>agent</code> selects the subagent type, and <code>background</code> defaults to <code>true</code> so a forked skill runs asynchronously unless set to <code>false</code>. Note also that a forked skill's edits fall outside session checkpoints, so <code>/rewind</code> will not undo them.",
k:"<code>context: fork</code> isolates; <code>agent</code> picks the type; <code>background: false</code> waits. Forked edits escape <code>/rewind</code>."},

{i:1458,d:3,t:"3.6",s:"GEN",diff:2,
q:"Which flag combination gives a CI pipeline machine-readable output in a known shape?",
ask:"Which is correct?",
a:["<code>-p --output-format json --json-schema '&lt;schema&gt;'</code>",
"<code>--verbose --markdown</code>",
"<code>--ci --structured</code>",
"<code>--headless --parse</code>"],
c:0,
w:["Correct. Non-interactive, JSON output, constrained to your schema.",
"Verbose markdown still requires parsing prose.",
"Neither flag exists.",
"Neither flag exists."],
e:"Three flags doing three jobs. <code>-p</code> / <code>--print</code> makes the run non-interactive so the job does not hang. <code>--output-format json</code> gives structured output; the other values are <code>text</code> and <code>stream-json</code>. <code>--json-schema</code> constrains that output to <em>your</em> shape so the pipeline can branch on specific fields and post inline comments.",
k:"CI trio: <code>-p</code>, <code>--output-format json</code>, <code>--json-schema</code>. Never gate a build on text matching."},

{i:1459,d:4,t:"4.3",s:"GEN",diff:2,
q:"Using <code>tool_use</code> with a JSON Schema eliminates which error class?",
ask:"Which does it eliminate?",
a:["Syntax errors &mdash; malformed JSON, missing required fields, wrong types",
"Semantic errors such as totals that do not reconcile",
"Values placed in the wrong field",
"Implausible dates"],
c:0,
w:["Correct. Structural conformance is exactly what the mechanism guarantees.",
"Semantic errors produce well-formed output that is merely wrong.",
"A vendor name in the customer field is structurally valid.",
"An implausible date is a well-formed date."],
e:"The central Domain 4 distinction. Schemas guarantee structure &mdash; well-formed JSON, required fields present, declared types, enum membership. They cannot express &ldquo;these must sum&rdquo;, &ldquo;this string is the vendor&rdquo; or &ldquo;this date must be plausible&rdquo;. Those need domain validation in code, plus schema design that makes checking possible &mdash; extract both computed and stated totals.",
k:"Schemas kill syntax errors only. Semantics need domain validation and schemas designed to make checking possible."},

{i:1460,d:4,t:"4.5",s:"GEN",diff:2,
q:"Which statement about the Message Batches API is false?",
ask:"Which is FALSE?",
a:["It guarantees completion within a fixed latency SLA",
"It offers approximately 50% cost savings",
"Unprocessed requests expire after 24 hours",
"It does not support multi-turn tool calling within a single request"],
c:0,
w:["Correct &mdash; this is the false statement. There is explicitly no latency SLA, and this is the deciding constraint.",
"True.",
"True &mdash; expired requests must be re-submitted by <code>custom_id</code>.",
"True &mdash; each batch request is a single-shot call."],
e:"Hold all the facts together: roughly 50% cheaper, up to a 24-hour window with expiry, no latency SLA, single-shot only, and limits of 100,000 requests or 256 MB. The missing SLA is what rules Batch out for anything blocking a person or a pipeline, however generous the typical completion time.",
k:"Batch: ~50% cheaper, 24h expiry, <strong>no SLA</strong>, single-shot, 100k requests or 256 MB. The missing SLA decides."},

{i:1461,d:5,t:"5.1",s:"GEN",diff:2,
q:"Which content does progressive summarisation degrade first?",
ask:"Which is most at risk?",
a:["Numeric values, percentages and dates",
"The system prompt",
"Tool definitions",
"The most recent turn"],
c:0,
w:["Correct. Precise values condense into vague qualitative statements.",
"The system prompt is a separate parameter re-sent every request.",
"Tool definitions are re-sent every request.",
"Recent turns are typically preserved verbatim."],
e:"Summarisation degrades in a predictable direction, which makes it both testable and defensible against. Keep identifiers, amounts, dates and decisions in a code-maintained facts block that never passes through the summariser, and externalise findings to a scratchpad file <em>before</em> compacting so exact paths and measured values survive.",
k:"Summarisation destroys numbers, dates and specifics first. Protect them in a facts block and a scratchpad file."},

{i:1462,d:5,t:"5.2",s:"GEN",diff:2,
q:"Which is NOT a valid escalation trigger?",
ask:"Which one?",
a:["The model rating its own confidence below a threshold",
"The customer explicitly asking for a human",
"Policy being silent or ambiguous for this case",
"Demonstrated inability to make progress"],
c:0,
w:["Correct. Self-rated confidence is explicitly unreliable &mdash; models are readily confidently wrong.",
"A valid trigger, and one warranting immediate escalation without diagnostic questions.",
"A valid trigger &mdash; the agent has no authority to invent policy.",
"A valid trigger."],
e:"Three valid triggers: explicit request for a human, policy gap or exception, and inability to progress. The recurring invalid ones are self-rated confidence, customer sentiment, turn count and message length. Note the one nuance: confidence <em>calibrated</em> against labelled data at field level is legitimate for routing &mdash; it is the raw, uncalibrated form that is unreliable.",
k:"Valid: explicit request, policy gap, inability to progress. Invalid: raw confidence, sentiment, turn count."},

{i:1463,d:5,t:"5.6",s:"GEN",diff:2,
q:"What must a finding record carry for a claim to remain checkable a year later?",
ask:"Which set is correct?",
a:["The claim, the source identity, a supporting quote, and the date",
"The claim and a source URL",
"The claim and a confidence score",
"The claim and the subagent that produced it"],
c:0,
w:["Correct. The quote survives link rot; the date establishes currency and prevents temporal differences reading as conflicts.",
"URLs degrade &mdash; pages move, change and disappear.",
"Confidence is an assertion rather than evidence.",
"Pipeline provenance serves debugging, not reader verification."],
e:"Four elements, each doing work. The <strong>claim</strong> and <strong>source</strong> establish attribution. The <strong>quote</strong> records what the source actually said, surviving the page changing and making over-reach detectable when a claim is stronger than its quote supports. The <strong>date</strong> establishes currency and prevents a 2021 and a 2025 figure reading as a contradiction rather than a trend.",
k:"Claim &rarr; source &rarr; quote &rarr; date. The quote survives link rot; the date prevents false conflicts."},

{i:1464,d:1,t:"1.6",s:"GEN",diff:2,
q:"When is dynamic adaptive decomposition preferable to a fixed pipeline?",
ask:"Which case?",
a:["When the next step depends on what earlier steps discover",
"When the same steps run for every input",
"When the steps must be auditable",
"When the work must be reproducible"],
c:0,
w:["Correct. Adaptivity is for open-ended investigation where the useful next step is unknowable in advance.",
"Uniform work is the fixed-pipeline case &mdash; determinism, auditability and cheap per-item retry.",
"Auditability favours fixed pipelines.",
"Reproducibility favours fixed pipelines."],
e:"Fixed pipelines suit predictable multi-aspect work where the steps are known up front &mdash; per-file review then integration, or a uniform extraction flow. Adaptive decomposition suits open-ended investigation: map the structure, build a prioritised plan, investigate, and revise the plan as evidence arrives. Fixed pipelines may still carry named conditional exits without becoming adaptive.",
k:"Steps knowable up front &rarr; fixed pipeline. Next step depends on findings &rarr; adaptive: map, prioritise, investigate, revise."},

{i:1465,d:1,t:"1.7",s:"GEN",diff:2,
q:"When is starting a fresh session preferable to resuming?",
ask:"Which case?",
a:["When files have changed on disk since the session last ran, making its cached results stale",
"When the session is more than a day old",
"When the session used many turns",
"Never; resuming always preserves more"],
c:0,
w:["Correct. The blueprint notes a fresh session with a structured summary can be more reliable than resuming with stale results.",
"Age is a weak proxy &mdash; a file can change in ten minutes.",
"Turn count does not determine validity.",
"Resuming preserves stale context as faithfully as valid context."],
e:"Three mechanisms, three cases. <code>--resume</code> continues a named session &mdash; correct when the accumulated context is still valid. <code>fork_session</code> branches from shared context &mdash; for comparing alternatives without cross-contamination. A fresh session seeded with a structured summary &mdash; when the context has gone stale, which is judged by validity rather than by elapsed time.",
k:"Resume when context is valid, fork to compare alternatives, restart with a summary when it has gone stale."},

{i:1466,d:2,t:"2.3",s:"GEN",diff:2,
q:"Which <code>tool_choice</code> value would prevent an agent loop from ever terminating naturally?",
ask:"Which one?",
a:["<code>{\"type\": \"any\"}</code> left set across every turn",
"<code>{\"type\": \"auto\"}</code>",
"<code>{\"type\": \"none\"}</code>",
"<code>{\"type\": \"tool\", \"name\": \"...\"}</code> on the first turn only"],
c:0,
w:["Correct. <code>any</code> forces a tool call every turn, so the model can never emit the text turn that yields <code>end_turn</code>.",
"<code>auto</code> permits a text answer, which is what allows termination.",
"<code>none</code> forbids tools, so the loop terminates immediately &mdash; the opposite problem.",
"Forcing a tool on turn one then switching to <code>auto</code> is the standard structured-start pattern."],
e:"<code>tool_choice</code> is per-request, so vary it across the loop. The common correct pattern is a constrained first turn &mdash; a forced tool for one known schema, or <code>any</code> when several are valid &mdash; followed by <code>auto</code> for the conversational remainder. Leaving <code>any</code> set throughout is the classic mistake: the loop runs until the iteration cap.",
k:"<code>any</code> across a whole loop prevents termination. Constrain the turn that needs a guarantee, then <code>auto</code>."},

{i:1467,d:3,t:"3.3",s:"GEN",diff:2,
q:"Which glob matches test files at any depth in the repository?",
ask:"Which pattern?",
a:["<code>**/*.test.ts</code>",
"<code>*.test.ts</code>",
"<code>tests/*.test.ts</code>",
"<code>src/*.test.ts</code>"],
c:0,
w:["Correct. <code>**/</code> crosses directory levels, which is what co-located test files require.",
"A single <code>*</code> matches only the repository root.",
"Restricted to one directory, missing co-located tests.",
"Restricted to one level under <code>src/</code>."],
e:"The depth wildcard is the most consequential glob detail and the most common error. A single <code>*</code> stays within one level; <code>**</code> crosses levels. It matters most for exactly the case path-scoped rules exist to serve &mdash; conventions following a file type across the tree. Brace expansion handles multiple extensions: <code>**/*.{test,spec}.ts</code>.",
k:"<code>**/</code> crosses directory levels; a single <code>*</code> does not. Missing it is the most common glob error."},

{i:1468,d:4,t:"4.2",s:"GEN",diff:2,
q:"How many few-shot examples does the blueprint recommend for ambiguous scenarios?",
ask:"Which range?",
a:["Two to four targeted examples, each with its rationale",
"One, to avoid biasing the model",
"Ten to fifteen for good coverage",
"As many as the context window allows"],
c:0,
w:["Correct. Targeted rather than voluminous, with rationale so the principle generalises.",
"One example cannot delineate a boundary and is easily over-fitted.",
"Ten to fifteen costs substantial context on every request for diminishing returns.",
"Filling the window crowds out the task and dilutes attention."],
e:"Few-shot is about targeting rather than volume. Aim the examples at the failure region, use contrastive near-miss pairs where a boundary is involved, keep the format consistent and the content varied, and include the rationale so the model learns the discriminating principle rather than a surface pattern. Balance both sides of any decision boundary.",
k:"Two to four targeted examples with rationale. Consistent format, varied content, balanced across the boundary."},

{i:1469,d:5,t:"5.4",s:"GEN",diff:2,
q:"What is the correct order when a long session's context is filling?",
ask:"Which sequence?",
a:["Externalise findings to a file, then compact or start fresh seeded with that file",
"Compact first, then write down whatever survived",
"Delete the earliest messages, then continue",
"Increase the context window and continue"],
c:0,
w:["Correct. Externalise before reducing, or the specifics are lost in the summarisation.",
"Compacting first is exactly where exact paths and measured values get condensed away.",
"Position-based deletion removes information by age rather than relevance &mdash; the sliding-window mistake.",
"A larger window delays the same failure without addressing it."],
e:"Externalise, then reduce. Writing findings, decisions, exact file paths and measured values to a scratchpad first makes the compaction safe, and gives you an artefact that also survives a crash, transfers to a colleague, and seeds a fresh session. Act on context pressure before the degradation symptom appears, since by then quality has already suffered.",
k:"Externalise first, then compact or restart. The degradation symptom appears after quality has already suffered."},

{i:1470,d:1,t:"1.3",s:"GEN",diff:2,
q:"What does a standard subagent inherit from its parent?",
ask:"Which does it receive?",
a:["Project CLAUDE.md files, but not the conversation history",
"The full conversation history",
"The parent's auto memory",
"The parent's output style"],
c:0,
w:["Correct. Project instruction files load; conversation history, auto memory and output style do not.",
"Conversation history is explicitly not inherited &mdash; a fork is the exception.",
"The main conversation's auto memory is not loaded into subagents.",
"Output style is not inherited."],
e:"Subagent isolation is about the <em>conversation</em>, not project configuration. A standard subagent starts with its own system prompt, the delegation prompt, CLAUDE.md files from the hierarchy, and a git-status snapshot. That is why it follows team conventions without being told, and simultaneously knows nothing about what you and the parent just discussed. A <strong>fork</strong> is the exception &mdash; it inherits the entire parent conversation.",
k:"Subagents inherit project CLAUDE.md but not conversation, memory or style. Forks inherit the whole conversation."},

{i:1471,d:2,t:"2.4",s:"GEN",diff:2,
q:"What happens when several MCP servers are connected?",
ask:"Which is correct?",
a:["All their tools are discovered on connection and available simultaneously",
"Only one server is active at a time",
"Tools must be individually enabled before use",
"Servers must be activated with a slash command each session"],
c:0,
w:["Correct. Connection is discovery, with no activation step and no exclusivity.",
"There is no exclusivity between servers.",
"Configuring the server makes its tools available; there is no per-tool enablement.",
"No such activation command exists."],
e:"A configuration fact with a design consequence the exam cares about: because every connected server's tools join the choice space at once, connecting many servers inflates the toolset and degrades selection for every agent that can see them &mdash; and their definitions are billed as input tokens on every request. Scope servers per agent role rather than connecting everything everywhere.",
k:"All connected servers' tools are available at once. Every connection taxes selection and tokens for every agent."},

{i:1472,d:3,t:"3.4",s:"GEN",diff:2,
q:"Which task most clearly warrants direct execution rather than planning mode?",
ask:"Which one?",
a:["A bug with a clear stack trace pointing to one function with an obvious off-by-one error",
"A migration touching 45 files across three services with several viable approaches",
"A three-line change to an authorisation check",
"Choosing between two libraries whose selection affects downstream design"],
c:0,
w:["Correct. Precise diagnosis, single file, obvious fix &mdash; execute and add a regression test.",
"Large scope plus multiple approaches is the planning trigger.",
"Small, but consequence outranks size on a security-critical path.",
"Multiple defensible options with downstream consequences warrants planning."],
e:"Two signals drive planning: approach uncertainty and consequence. Neither is present in a precisely-diagnosed one-file fix, so direct execution with a regression test is right. Note that option C is the trap &mdash; small diff, high consequence, and therefore a planning case despite its size. Keep exploration separate: unfamiliarity is a third, delegable need.",
k:"Direct execution for precise diagnosis + obvious fix. Planning for approach uncertainty <em>or</em> high consequence."},

{i:1473,d:4,t:"4.4",s:"GEN",diff:2,
q:"What must a retry-with-feedback prompt contain?",
ask:"Which set?",
a:["The original source, the incorrect extraction, and the specific validation errors",
"The specific validation errors only",
"The incorrect extraction and the schema",
"The original source and an instruction to try harder"],
c:0,
w:["Correct. All three, and dropping the source is the worst omission &mdash; it invites a fabricated correction.",
"Errors alone leave the model unable to re-derive the correct value.",
"Without the source, the model cannot check what the document actually says.",
"&ldquo;Try harder&rdquo; supplies no new information."],
e:"Retry works because the second attempt has strictly more information. Each element does distinct work: the source to re-derive from, the previous attempt so it is not re-proposed, and the specific errors so the correction is targeted. Recognise too when retry cannot help &mdash; information absent from the source, or a source that yields a different value each attempt, both route to review.",
k:"Retry payload: source + previous attempt + specific errors. Absent or ambiguous source data routes to review instead."},

{i:1474,d:5,t:"5.5",s:"GEN",diff:2,
q:"Why is a 97% aggregate accuracy figure insufficient for an automation decision?",
ask:"Which is the best reason?",
a:["It can mask substantially worse performance on specific document types or fields",
"Accuracy is always overstated by evaluation harnesses",
"Aggregate figures cannot be computed reliably at scale",
"Automation should be decided on cost rather than accuracy"],
c:0,
w:["Correct. The blueprint states this directly, and the 61%-on-handwritten-forms case is the canonical illustration.",
"Not a general property; segmentation is the issue.",
"They are easy to compute &mdash; just insufficient.",
"Cost matters alongside accuracy, but masking is the stated reason."],
e:"Validate by document type <em>and</em> by field before automating, since both dimensions hide problems. Automate only the segments meeting the bar, route the rest to human review &mdash; whose corrections become labelled data for improving them &mdash; and keep sampling the automated stream, since routing alone never inspects what it approved.",
k:"Segment before automating: by document type and by field. Aggregates hide the segments that will hurt you."},

{i:1475,d:1,t:"1.5",s:"GEN",diff:2,
q:"Which hook position can block a tool call before it executes?",
ask:"Which one?",
a:["<code>PreToolUse</code>",
"<code>PostToolUse</code>",
"<code>SessionStart</code>",
"<code>InstructionsLoaded</code>"],
c:0,
w:["Correct. It returns <code>permissionDecision</code> of allow, deny, ask or defer, and can rewrite arguments via <code>updatedInput</code>.",
"It fires after the tool has already run, so it cannot prevent the effect.",
"It fires at session start and adds context only.",
"It is observational &mdash; audit logging, with no decision control."],
e:"Position determines capability. <code>PreToolUse</code> is the powerful one: four outcomes (allow, deny, ask, defer) plus <code>updatedInput</code> to rewrite arguments before execution, which is where enforcement and system-value injection live. The <code>ask</code> outcome is the targeted human-in-the-loop mechanism &mdash; gate the irreversible minority rather than everything, to avoid approval fatigue.",
k:"<code>PreToolUse</code>: allow / deny / <strong>ask</strong> / defer, plus <code>updatedInput</code>. Position determines what a hook can do."},

{i:1476,d:2,t:"2.1",s:"GEN",diff:2,
q:"Which element of a tool description does the most work in a system with several similar tools?",
ask:"Which one?",
a:["The explicit boundary against the nearest alternatives",
"The list of every edge case it handles",
"The name of the team that maintains it",
"Its average response latency"],
c:0,
w:["Correct. Selection failures are near-misses between neighbours, so the boundary is where the value is.",
"Exhaustive behaviour catalogues belong in integrator documentation.",
"Ownership belongs in a service catalogue.",
"Latency rarely drives correct selection."],
e:"A good description answers five things: what it does, when to choose it, input format with an example, what it returns, and what it is <em>not</em> for. The last does the most work once there is more than a handful of tools, because errors concentrate at the boundaries. And since selection is comparative, that boundary needs revisiting whenever a neighbour changes.",
k:"The boundary against neighbours is the highest-value description element. Revisit it whenever the toolset changes."},

{i:1477,d:3,t:"3.5",s:"GEN",diff:2,
q:"What does the blueprint identify as the most effective way to communicate expectations?",
ask:"Which one?",
a:["Two to three concrete input/output examples",
"A detailed prose specification",
"A formal grammar for the output",
"An emphatic instruction"],
c:0,
w:["Correct. Examples resolve ambiguities a prose description does not know it has.",
"Prose leaves interpretation room that examples close.",
"A grammar constrains syntax without conveying the semantic transformation.",
"Emphasis adds no information."],
e:"Concrete input/output pairs are the named most effective technique, and they compose with the related patterns: test-driven iteration builds the expected behaviour, edge cases and performance bounds into a test set first; the interview pattern has Claude ask the questions that surface considerations you did not know to specify. Choose examples spanning the interesting variation, not three near-identical happy paths.",
k:"Two to three concrete input/output examples. Pair with test-driven iteration and the interview pattern."},

{i:1478,d:4,t:"4.6",s:"GEN",diff:2,
q:"Why is an independent instance better at reviewing generated code than the instance that generated it?",
ask:"Which is the reason?",
a:["The generating instance retains its reasoning and is unlikely to challenge decisions it just argued for",
"A different model is required for review",
"The generating instance's context window is exhausted",
"Review requires a higher effort setting"],
c:0,
w:["Correct. Anchoring from the generation reasoning is the mechanism, and context isolation is the remedy.",
"Independence comes from a separate context, not a different model.",
"The failure occurs with ample context remaining.",
"Effort does not dislodge the anchoring."],
e:"The reviewing instance should see the diff and the standards, and nothing of the conversation that produced the code. Note the nuance: sharing CLAUDE.md between generator and reviewer is <em>desirable</em>, since independence concerns the generation reasoning rather than the conventions. Combine with multi-pass review &mdash; per-file plus a cross-file integration pass &mdash; for the strongest coverage.",
k:"Independence = separate context, not a different model. Share standards; never share the generation reasoning."},

{i:1479,d:5,t:"5.3",s:"GEN",diff:2,
q:"Which pair are both named anti-patterns in multi-agent error handling?",
ask:"Which pair?",
a:["Silently suppressing a failure, and aborting the whole workflow because one component failed",
"Retrying transient failures locally, and returning partial results",
"Annotating coverage gaps, and escalating unresolvable errors",
"Distinguishing empty results from access failures, and including partial results"],
c:0,
w:["Correct. These are the two named anti-patterns, bracketing the correct behaviour from opposite sides.",
"Both are recommended practices.",
"Both are recommended practices.",
"Both are recommended practices."],
e:"The two anti-patterns fail in opposite directions. Silent suppression under-reacts &mdash; the failure becomes invisible and surfaces as a silent gap. Whole-workflow abort over-reacts &mdash; one recoverable failure destroys everything that succeeded. The correct middle is local recovery where possible, structured propagation of what cannot be recovered, and coverage annotation so gaps are visible.",
k:"Anti-patterns: silent suppression (under-reacts) and whole-workflow abort (over-reacts). Middle: recover, propagate, annotate."},

{i:1480,d:1,t:"1.1",s:"GEN",diff:3,
q:"Your loop treats every response that is not <code>end_turn</code> as &ldquo;continue&rdquo;.",
ask:"Which value makes this dangerous?",
a:["<code>max_tokens</code>, since a truncated <code>tool_use</code> block may parse into plausible partial arguments that then execute",
"<code>stop_sequence</code>",
"<code>pause_turn</code>",
"<code>end_turn</code>"],
c:0,
w:["Correct. Executing partially-generated arguments against a state-changing tool is the real hazard.",
"A configured stop sequence firing is handled according to why you configured it; it does not produce partial tool calls.",
"<code>pause_turn</code> genuinely is a continuation signal, so treating it as continue is correct.",
"<code>end_turn</code> is the case already handled."],
e:"Branch on every <code>stop_reason</code>. The seven values are <code>end_turn</code>, <code>tool_use</code>, <code>max_tokens</code>, <code>stop_sequence</code>, <code>pause_turn</code>, <code>refusal</code> and <code>model_context_window_exceeded</code>. Treating everything non-<code>end_turn</code> as continue is dangerous specifically because of truncation: partial JSON can parse into a valid-looking object, and for <code>process_refund</code> the consequence is real.",
k:"On <code>max_tokens</code>, discard the turn &mdash; never execute partial tool arguments. Branch on all seven values."},

{i:1481,d:2,t:"2.2",s:"GEN",diff:3,
q:"A search finds no matching records. What should the tool return?",
ask:"Which is correct?",
a:["Success with an empty result set, ideally echoing the query",
"<code>isError: true</code> with the message &ldquo;No results&rdquo;",
"<code>isError: true</code> with <code>errorCategory: \"transient\"</code>",
"Nothing, so the agent infers absence"],
c:0,
w:["Correct. An empty result is a valid finding, and echoing the query lets the caller judge whether it was well-formed.",
"Classifying a valid outcome as an error causes retry storms and false outage reports.",
"Worse still &mdash; marking it transient invites retries that can never change the outcome.",
"Returning nothing leaves the <code>tool_use</code> block unanswered, which the API rejects."],
e:"The blueprint explicitly requires distinguishing access failures from valid empty results. &ldquo;I searched and found nothing&rdquo; is a finding to report and to reflect in coverage annotation; &ldquo;I could not search&rdquo; is a failure needing recovery. Conflating them produces retry storms on one side and invisible gaps on the other.",
k:"Empty result = success with an empty set, query echoed. Access failure = <code>isError</code> with context. Never conflate."},

{i:1482,d:3,t:"3.1",s:"GEN",diff:3,
q:"Which statement about CLAUDE.md is accurate?",
ask:"Which is true?",
a:["It is context that shapes behaviour, not enforced configuration &mdash; to block an action regardless of what Claude decides, use a <code>PreToolUse</code> hook",
"It is enforced configuration whose rules cannot be violated",
"It is part of the system prompt",
"It is loaded only when explicitly imported"],
c:0,
w:["Correct &mdash; close to the documentation's own wording, and the exam's central principle.",
"Instructions are followed probabilistically, not enforced.",
"It is delivered as a user message <em>after</em> the system prompt, which is part of why adherence is not guaranteed.",
"Project and user CLAUDE.md load automatically."],
e:"This is the whole Domain 3 strategy in one sentence. CLAUDE.md is context &mdash; write it specific and concise for better adherence, target under 200 lines, scope it with path rules. For anything that must hold regardless of what Claude decides, use hooks and permission rules. For anything mechanically checkable, use a linter.",
k:"CLAUDE.md is context, not enforcement, and arrives after the system prompt. Only hooks and permissions guarantee."},

{i:1483,d:4,t:"4.1",s:"GEN",diff:3,
q:"Which is the blueprint's contrast for explicit versus vague criteria?",
ask:"Which pairing is it?",
a:["&ldquo;Flag comments only when they contradict code&rdquo; versus &ldquo;check comment accuracy&rdquo;",
"&ldquo;Be thorough&rdquo; versus &ldquo;be comprehensive&rdquo;",
"&ldquo;Report all issues&rdquo; versus &ldquo;report critical issues&rdquo;",
"&ldquo;Use judgement&rdquo; versus &ldquo;be careful&rdquo;"],
c:0,
w:["Correct. This is the blueprint's worked example, contrasting a decidable property with an undefined one.",
"Both are vague.",
"Both are volume instructions rather than criteria.",
"Both are vague."],
e:"The difference is decidability: &ldquo;contradicts what the code does&rdquo; names a property you can evaluate, while &ldquo;accurate&rdquo; does not. The blueprint adds a related warning &mdash; generic guidance such as &ldquo;be more conservative&rdquo; works <em>worse</em> than concrete categorical criteria, because it shifts the threshold without sharpening the boundary.",
k:"Decidable properties beat adjectives. &ldquo;Be more conservative&rdquo; works worse than concrete categorical criteria."},

{i:1484,d:5,t:"5.1",s:"GEN",diff:3,
q:"What is the lost-in-the-middle effect, and what are its defences?",
ask:"Which describes it correctly?",
a:["Models process the start and end of long inputs reliably and may miss the middle; defend by positioning key material early with headings, splitting into passes, and keeping durable instructions in the system prompt",
"The API drops messages from the middle of long conversations",
"Compaction always removes the middle portion",
"Tool results in the middle of a content array are ignored"],
c:0,
w:["Correct &mdash; an attention characteristic, with three documented defences.",
"The API drops nothing; you send what you send.",
"Compaction summarises and is a separate mechanism.",
"Content array position does not cause results to be ignored."],
e:"It is an attention characteristic, not a mechanical deletion, which is why the defences are about positioning and structure. Place key findings at the beginning of aggregated data with explicit section headings; split very long inputs into passes; keep durable instructions in the system prompt rather than buried at turn two. It also explains why review of a huge diff concentrates on the first files.",
k:"Start and end are reliable; the middle is not. Position early with headings, split into passes, durable instructions up top."},

{i:1485,d:1,t:"1.2",s:"GEN",diff:3,
q:"Why does hub-and-spoke require that spokes never call each other directly?",
ask:"Which is the reason?",
a:["Routing all traffic through the coordinator preserves a single observable trace, consistent error handling and control over coverage",
"Subagents are technically incapable of calling each other",
"Direct calls always cost more tokens",
"Only the coordinator may hold MCP tools"],
c:0,
w:["Correct. Observability and control are what the topology buys.",
"It is usually technically possible, which is why the discipline matters.",
"Routing through the hub often costs slightly more; that is not the argument.",
"Subagents routinely hold their own scoped tools."],
e:"The moment spokes talk to each other you get a mesh: emergent behaviour nobody designed, no single trace to debug, no consistent place to apply error handling or policy, and no reliable way to detect duplicated or missing coverage. Where a spoke genuinely needs a capability constantly, give it a narrow scoped tool &mdash; and withhold the spawning tool from spokes so the topology stays flat.",
k:"Hub-and-spoke buys one trace, one error-handling point and coverage control. Give spokes narrow tools, not each other."},

{i:1486,d:2,t:"2.3",s:"GEN",diff:3,
q:"The blueprint contrasts an agent with 18 tools against one with how many?",
ask:"Which figure?",
a:["Four to five",
"Ten to twelve",
"One to two",
"Eight to nine"],
c:0,
w:["Correct. The illustrative contrast is 18 versus a focused 4&ndash;5.",
"Too high to illustrate the point.",
"Too restrictive for most real roles.",
"Not the figure used."],
e:"The figure illustrates an effect rather than setting a rule: selection reliability falls as the toolset grows, and agents holding tools outside their specialisation tend to misuse them. The remedy is scoped access &mdash; role-relevant tools plus a limited set of narrow cross-role utilities. Measure selection accuracy on representative requests; the count is a prompt to check rather than a hard limit.",
k:"The blueprint contrasts 18 tools with a focused 4&ndash;5. It illustrates the effect; measured accuracy is the real criterion."},

{i:1487,d:3,t:"3.2",s:"GEN",diff:3,
q:"Where do project slash commands live so the whole team gets them on clone?",
ask:"Which location?",
a:["<code>.claude/commands/</code> in the project repository",
"<code>~/.claude/commands/</code>",
"The root CLAUDE.md",
"<code>.claude/config.json</code>"],
c:0,
w:["Correct. Project commands are version-controlled and available to everyone who clones.",
"That is the user-level location for personal commands.",
"CLAUDE.md holds instructions and conventions, not command definitions.",
"That file does not exist in Claude Code's configuration model."],
e:"This mirrors a published sample question, and the reasoning is that <strong>location determines sharing</strong>: project <code>.claude/</code> is version-controlled and active on clone; <code>~/.claude/</code> is personal. Note the distractor pattern &mdash; option D invents a plausible-sounding config file, which the exam does more than once.",
k:"Project <code>.claude/commands/</code> = shared on clone. <code>~/.claude/commands/</code> = personal. Location determines sharing."},

{i:1488,d:4,t:"4.5",s:"GEN",diff:3,
q:"Which workload is unsuitable for the Message Batches API?",
ask:"Which one?",
a:["A pre-merge CI security check blocking a developer's merge",
"Re-classifying two years of archived support tickets",
"An overnight report due the following morning",
"A quarterly audit of 40,000 invoices"],
c:0,
w:["Correct. A blocking check requires bounded latency, which Batch explicitly does not provide.",
"Bulk, non-blocking, generous deadline &mdash; ideal.",
"Non-blocking with a generous deadline.",
"Non-blocking with a generous deadline."],
e:"The decision rule is whether anything is blocked on the result. A developer waiting to merge is blocked; a report generated before Monday morning is not. Batch's absence of a latency SLA is the deciding constraint &mdash; most batches finish in under an hour, but &ldquo;usually fast&rdquo; is not something you can present to a blocked pipeline.",
k:"Is anything blocked on the result? Yes &rarr; synchronous. No, with a generous deadline &rarr; Batch."},

{i:1489,d:5,t:"5.2",s:"GEN",diff:3,
q:"A customer says &ldquo;just let me talk to a person&rdquo;. What should the agent do?",
ask:"Which is correct?",
a:["Escalate immediately with a structured handoff, without diagnostic questions first",
"Ask two diagnostic questions first to try to resolve it",
"Run sentiment analysis to confirm frustration",
"Attempt resolution once to protect first-contact resolution"],
c:0,
w:["Correct. An explicit request for a human is an immediate-escalation trigger, not an invitation to negotiate.",
"Any delay after an explicit request is the same error, however small.",
"Sentiment is unreliable and beside the point &mdash; the customer stated it plainly.",
"Optimising a metric against an explicit instruction damages trust."],
e:"The blueprint distinguishes immediate escalation from attempt-to-resolve. Immediate: the customer explicitly asks for a human. Attempt first: the issue is within documented scope and no such request has been made. Note that the agent's own correctness does not override this &mdash; being right does not entitle it to keep the case.",
k:"Explicit request for a human &rarr; escalate immediately. No diagnostic questions, and correctness does not override it."},

{i:1490,d:1,t:"1.4",s:"GEN",diff:3,
q:"An audit finds refunds processed without a preceding successful identity verification in 12% of cases, despite a clear system prompt instruction.",
ask:"Which change is most effective?",
a:["A programmatic precondition: <code>process_refund</code> rejects any call when the session has no verified customer id",
"Rewrite the system prompt with stronger wording about verification",
"Add few-shot examples showing the correct call sequence",
"Introduce an intent classifier routing refunds to a dedicated agent"],
c:0,
w:["Correct. A hard business invariant enforced at the tool boundary cannot be talked around.",
"Better wording reduces the 12% without reaching zero &mdash; and any residual rate is an audit finding.",
"Few-shot shapes typical behaviour but unusual phrasing routes around it.",
"Routing changes which agent handles it, not whether the ordering constraint holds."],
e:"This mirrors a published sample question and is the archetype for the whole exam. When a tool ordering carries business or regulatory weight, implement a precondition: the downstream tool inspects session state your code owns and refuses until the upstream step succeeded. Equivalent implementations are a blocking <code>PreToolUse</code> hook or a permission rule. Prompts remain valuable as a second layer &mdash; never as the control.",
k:"Ordering with business risk &rarr; precondition in the tool or a blocking hook. Prompts are defence in depth, not the control."},

{i:1491,d:2,t:"2.1",s:"GEN",diff:3,
q:"A tool named <code>analyze_content</code> actually fetches and parses web search results, and the agent keeps calling it on PDFs.",
ask:"Which change is most effective?",
a:["Rename it to <code>extract_web_results</code> and update the description, since the name itself was misleading",
"Add validation rejecting PDFs",
"Add a system prompt rule about which tool handles PDFs",
"Merge it with the PDF tool into a single <code>analyze</code> tool"],
c:0,
w:["Correct. The blueprint gives this exact rename as the remedy for functional overlap.",
"Catching the mistake wastes a turn each time and never fixes the misrouting.",
"A prompt rule patches over a name that will keep misleading in every new context.",
"A general-purpose tool is harder to describe, validate and reason about."],
e:"Names carry selection signal, often more than the description body, because a vague name creates an association the description then has to fight. &ldquo;Content&rdquo; sounds more general than &ldquo;document&rdquo;, which is exactly the pull. Prefer verb-plus-object names that reveal the boundary, and fix naming and descriptions before reaching for routing layers or merges.",
k:"Tool names carry selection signal. Rename to eliminate overlap before adding rules, validation or merges."},

{i:1492,d:3,t:"3.6",s:"GEN",diff:3,
q:"Why is the session that generated code a poor choice to review it in CI?",
ask:"Which is the reason?",
a:["It retains its generation reasoning and is less likely to challenge its own decisions than an independent instance",
"CI sessions cannot review code",
"The generation session lacks the review criteria",
"Review requires a different model"],
c:0,
w:["Correct. The blueprint states this directly, and session context isolation is the remedy.",
"They can; the question is whether they should.",
"It has the same CLAUDE.md &mdash; sharing standards is desirable.",
"Independence comes from a separate context, not a different model."],
e:"Run review as its own invocation seeing the diff and the standards, but not the conversation that produced the code. Two related CI practices: include prior review results when re-running after new commits so the bot reports only new or unfixed issues, and pair independence with multi-pass review &mdash; per-file passes plus a cross-file integration pass.",
k:"Never review in the generating session. Separate invocation, diff plus standards, prior findings passed explicitly."},

{i:1493,d:4,t:"4.3",s:"GEN",diff:3,
q:"Where do you read the extracted object from when the model responds with a forced tool call?",
ask:"Which location?",
a:["The <code>input</code> field of the <code>tool_use</code> content block",
"The assistant message's text content, parsed as JSON",
"The <code>tool_result</code> block in the next user message",
"The <code>output</code> field of the response"],
c:0,
w:["Correct. The arguments the model constructed <em>are</em> the structured output.",
"There is no JSON in the text content &mdash; that is the pattern you moved away from.",
"<code>tool_result</code> is what you send back after executing; it is not where the model's output lives.",
"No such field exists."],
e:"The structured-output pattern repurposes tool calling: define a tool whose <code>input_schema</code> is your target schema, force it with <code>tool_choice</code>, and read the object from <code>tool_use.input</code>. The tool need not exist or execute. The one caveat is that if you continue the conversation, every <code>tool_use</code> block must still receive a matching <code>tool_result</code>.",
k:"Structured output arrives as <code>tool_use.input</code>. The tool need not execute &mdash; the arguments are the result."},

{i:1494,d:5,t:"5.6",s:"GEN",diff:3,
q:"Two credible sources give conflicting figures for the same metric. What should the analysis agent do?",
ask:"Which is correct?",
a:["Complete its analysis carrying both values, annotated as conflicting with attribution and dates, and let the coordinator reconcile",
"Choose the more authoritative source and footnote the discrepancy",
"Halt and escalate before continuing any analysis",
"Report both without marking them as conflicting"],
c:0,
w:["Correct. Complete your scope, preserve both with attribution, route reconciliation upward.",
"Silently resolving a material conflict hides a consequential judgement at the wrong level.",
"Blocking is unnecessary and over-involves the coordinator in routine work.",
"Unmarked conflicting figures are the worst outcome &mdash; downstream consumers cannot see the disagreement."],
e:"This mirrors a published sample question. Three principles combine: complete your scope rather than blocking, preserve rather than resolve conflicts at the leaf level, and make the conflict machine-readable. Check first whether they genuinely conflict &mdash; different dates, definitions or populations explain many apparent contradictions, which is why findings carry all three.",
k:"Conflicts: don't pick, don't hide, don't block. Annotate with attribution and dates; check for date and definition differences."},

{i:1495,d:1,t:"1.6",s:"GEN",diff:3,
q:"A pull request touches 25 files and a single-prompt review produces shallow feedback.",
ask:"What is the correct architecture?",
a:["Per-file passes for local defects, plus a separate cross-file integration pass over the changed symbols and their consumers",
"Resend all 25 diffs with an instruction to be more thorough",
"Review only the five largest files",
"Split into 25 separate pull requests"],
c:0,
w:["Correct. Two defect classes need two mechanisms, and neither substitutes for the other.",
"Attention dilution is not fixed by asking for more effort.",
"Size is uncorrelated with risk &mdash; a one-line authorisation change outranks a large generated file.",
"Restructures the team's workflow to work around a tool limitation."],
e:"Per-file passes recover full attention for local defects &mdash; missing validation, unsafe handling, logic errors. The cross-file pass catches what no per-file pass can see: a changed signature with stale callers, a broken contract, a migration inconsistent with the model. Build its input from the changed symbols and their consumers, not all diffs concatenated, or you reproduce the dilution.",
k:"Multi-pass review: per-file for local defects + cross-file over changed symbols and their consumers. Both required."},

{i:1496,d:2,t:"2.4",s:"GEN",diff:3,
q:"Which file defines MCP servers shared with the team through version control?",
ask:"Which one?",
a:["<code>.mcp.json</code> in the project root",
"<code>~/.claude.json</code>",
"<code>.claude/settings.json</code>",
"<code>CLAUDE.md</code>"],
c:0,
w:["Correct. Project-scoped MCP definitions, version-controlled, supporting <code>${VAR}</code> substitution.",
"That is the user and local scope location &mdash; personal, not shared.",
"Settings configure Claude Code behaviour and permissions, not MCP servers.",
"CLAUDE.md carries instructions and conventions."],
e:"Keep the configuration files distinct, since the exam uses them as distractors for each other. <code>.mcp.json</code> &mdash; project MCP servers, shared. <code>~/.claude.json</code> &mdash; user and local scope MCP servers, personal. <code>.claude/settings.json</code> &mdash; behaviour and permissions. <code>CLAUDE.md</code> &mdash; instructions. <code>.claude/rules/</code> &mdash; topic-focused, optionally path-scoped rules.",
k:"<code>.mcp.json</code> = shared MCP servers &middot; <code>~/.claude.json</code> = personal &middot; <code>settings.json</code> = behaviour &middot; CLAUDE.md = instructions."},

{i:1497,d:3,t:"3.3",s:"GEN",diff:3,
q:"What does <code>paths</code> frontmatter in a <code>.claude/rules/</code> file do?",
ask:"Which is correct?",
a:["Restricts the rule so it loads only when files matching those globs are being worked on",
"Specifies where the rule file itself should be stored",
"Lists the files the rule permits modification of",
"Defines the search path for imported standards"],
c:0,
w:["Correct. Conditional loading, giving both a context saving and protection against misapplication.",
"Rules live in <code>.claude/rules/</code>; <code>paths</code> governs activation.",
"Rules are instructions, not permissions &mdash; they do not gate modification.",
"That describes <code>@path</code> imports in CLAUDE.md."],
e:"Two benefits from one mechanism. Efficiency: a Terraform convention costs nothing on the hundreds of requests touching no Terraform. Correctness: it cannot be misapplied to unrelated work. Note that activation happens when Claude <em>reads</em> a matching file, so a scoped rule legitimately absent early in a session is correct behaviour rather than a fault.",
k:"<code>paths</code> makes rule loading conditional on matching files being read &mdash; saving context and preventing misapplication."},

{i:1498,d:4,t:"4.4",s:"GEN",diff:3,
q:"When is retry-with-feedback ineffective?",
ask:"Which case?",
a:["When the required information is simply absent from the source document",
"When the validation error is specific",
"When the source document is long",
"When the schema is strict"],
c:0,
w:["Correct. The blueprint states this explicitly &mdash; no retry can produce information that is not there.",
"Specific errors are what make retries effective.",
"Length does not prevent correction.",
"Strictness catches errors; it does not make correction impossible."],
e:"Retry fixes wrong <em>processing</em> of present information. It cannot fix information that is absent, and it cannot resolve a source that does not determine the answer &mdash; the signature there is a different plausible value on each attempt. Both are routing cases: mark the field genuinely absent, fetch the other source, or send it to human review.",
k:"Retry only fixes wrong processing of present data. Absent or ambiguous source data routes to review."},

{i:1499,d:5,t:"5.5",s:"GEN",diff:3,
q:"Before trusting a confidence threshold for auto-approval, what must you do?",
ask:"Which is required?",
a:["Calibrate the confidence values against a labelled validation set at field level, so a threshold carries a known error rate",
"Set the threshold conservatively at 0.95",
"Average confidence across fields for a document-level score",
"Confirm the model reports confidence for every field"],
c:0,
w:["Correct. Self-reported confidence is uncalibrated until measured against ground truth.",
"An uncalibrated 0.95 carries no known error rate &mdash; it might be 2% wrong or 25%.",
"Averaging masks a badly-extracted critical field behind several easy ones.",
"Presence of a value is not the same as it being informative."],
e:"Calibration binds a confidence value to an observed error rate for a given model on a given input distribution. Measure at field level and segment by document type, since both dimensions vary. Then re-calibrate periodically &mdash; the binding drifts as the input mix and the model change &mdash; and keep sampling the auto-approved stream to catch drift between calibrations.",
k:"Calibrate confidence at field level against labelled data. Recalibrate as the input mix and model change."},

{i:1500,d:1,t:"1.1",s:"GEN",diff:3,
q:"You are asked to state the single principle that most often decides CCAR-F scenario questions.",
ask:"Which is it?",
a:["Code gives deterministic guarantees; prompts give probabilistic compliance &mdash; so anything with money, security, compliance or irreversibility attached belongs in an enforcement layer, with prompts as a complementary second layer",
"Larger models produce more reliable agents",
"More context always improves results",
"Multi-agent architectures are preferable to single agents"],
c:0,
w:["Correct. This decides more exam items than any other single idea, and the complementarity clause matters as much as the contrast.",
"Model capability rarely resolves a design problem the exam is testing.",
"Irrelevant context dilutes attention and costs tokens &mdash; more is not better.",
"Multi-agent buys isolation and parallelism at the cost of tokens and complexity; it is not universally preferable."],
e:"When a scenario mentions a refund threshold, identity verification, deletion, data egress or any irreversible action, the answer lives in a tool precondition, a permission rule, or a <code>PreToolUse</code> hook &mdash; not in prompt wording. But note the second half: an agent that also knows the rule explains blocks coherently and routes to escalation proactively, so the strongest designs use both. And be alert to over-enforcement, which the exam also tests &mdash; style and preference belong in prompts, and mechanically checkable rules belong in linters.",
k:"Guarantee &rarr; code. Tendency &rarr; prompt. Mechanical &rarr; linter. Use both layers together; over-enforcement is its own failure."}
);
