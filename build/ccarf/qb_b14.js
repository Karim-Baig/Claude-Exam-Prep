QB.push(
{i:551,d:1,t:"1.1",s:"AAT",diff:2,
q:"Your agent's first turn returns text asking a clarifying question instead of calling the tool you expected.",
ask:"Is this necessarily a bug?",
a:["No &mdash; with <code>tool_choice: \"auto\"</code> a text response is valid, and asking for a missing required detail may be correct behaviour",
"Yes &mdash; the model should always call a tool on the first turn",
"Yes &mdash; text responses are only valid on the final turn",
"No &mdash; but only if <code>tool_choice</code> was set to <code>none</code>"],
c:0,
w:["Correct. <code>auto</code> permits text, and a clarifying question can be the right move when a required parameter is genuinely unavailable.",
"Nothing requires a tool call on turn one unless you constrain <code>tool_choice</code>.",
"Text responses are valid on any turn under <code>auto</code>.",
"<code>none</code> would forbid tools entirely, which is a different configuration."],
e:"Judge behaviour against the configuration and the situation. Under <code>auto</code>, a text turn is structurally valid, and if the request lacks a detail the tool requires, asking is better than guessing. If you need a guaranteed tool call on the first turn, constrain it with <code>any</code> or a forced tool &mdash; but then ensure the required parameters are actually derivable, or you have forced the model to invent one.",
k:"Under <code>auto</code>, a clarifying text turn is valid. Force a tool only when the parameters are genuinely derivable."},

{i:552,d:1,t:"1.2",s:"MAR",diff:2,
q:"Your coordinator has no way to tell whether a subagent's result is thorough or superficial.",
ask:"What should be added?",
a:["Explicit quality criteria in the delegation plus an output contract requiring evidence &mdash; sources consulted, coverage claimed, gaps acknowledged",
"A self-rated quality score from the subagent",
"A second subagent to review the first",
"A longer timeout so subagents have more time"],
c:0,
w:["Correct. Requiring evidence makes thoroughness inspectable rather than asserted.",
"Self-rated scores are uncalibrated and a recurring wrong answer on this exam.",
"A reviewing subagent doubles cost and has no better standard than the criteria you have not yet defined.",
"More time does not produce depth without a stated standard."],
e:"Make quality inspectable rather than asserted. If the contract requires sources consulted, coverage claimed and gaps acknowledged, a superficial result is visibly thin &mdash; two sources where five were asked for, no gaps named. The coordinator can then evaluate against the criteria and re-dispatch with specific feedback rather than guessing.",
k:"Require evidence of thoroughness in the output contract. Self-rated quality scores are not a substitute."},

{i:553,d:1,t:"1.3",s:"AAT",diff:2,
q:"A subagent needs a credential to access a system.",
ask:"How should this be handled?",
a:["The tool the subagent calls resolves the credential server-side; it is never passed through the delegation prompt",
"Include the credential in the delegation prompt",
"Have the subagent ask the coordinator for the credential",
"Store the credential in the subagent's system prompt"],
c:0,
w:["Correct. Credentials belong in the tool implementation's environment, never in model-visible context.",
"A credential in a prompt enters the context, the transcript and any logs.",
"Passing it through the model at any point puts it in context.",
"A system prompt is model-visible context like any other."],
e:"Credentials should never enter model-visible context. The tool resolves them from the environment or a secret manager at execution time &mdash; which is exactly what environment variable substitution in <code>.mcp.json</code> supports. Anything in a prompt, a system prompt or a tool argument is in the context and may be logged, summarised or echoed.",
k:"Credentials resolve server-side in the tool. Never in prompts, system prompts or tool arguments."},

{i:554,d:1,t:"1.4",s:"CSA",diff:3,
q:"A hook blocks an action. The agent then tries to accomplish the same outcome using a different tool that is not blocked.",
ask:"What does this reveal?",
a:["The control was placed on one path rather than on the outcome; it must cover every route to the restricted result",
"The agent is behaving maliciously",
"The hook should terminate the session on any block",
"The alternative tool should be removed entirely"],
c:0,
w:["Correct. Controls must be defined over outcomes, not over a single path to them.",
"The agent is doing what agents do &mdash; finding another way to achieve the goal. The gap is in the control.",
"Session termination is disproportionate and does not close the gap.",
"May be right, but the general lesson is to identify every path to the restricted outcome."],
e:"An agent's job is to find a way to the goal, so a control on one route simply redirects it. Enumerate every tool that can produce the restricted outcome and cover them all &mdash; or better, enforce at the resource level, where any path converges. This is the same reasoning as the per-call refund threshold being circumvented by splitting: define the control over the outcome, not the mechanism.",
k:"Define controls over outcomes, not single paths. Agents route around path-specific restrictions by design."},

{i:555,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want the same normalisation applied to results from three different MCP servers.",
ask:"What is the cleanest implementation?",
a:["One <code>PostToolUse</code> hook with the mapping logic, applied to all three servers' results",
"Three separate hooks, one per server",
"Ask each server's maintainer to standardise",
"Normalise inside the agent's prompt"],
c:0,
w:["Correct. One hook with a mapping keeps the logic in one place and makes adding a fourth server a small change.",
"Three hooks duplicate logic that will drift apart.",
"Worth pursuing upstream but slow, often outside your control, and it leaves you broken meanwhile.",
"Prompt-based normalisation is probabilistic where a deterministic mapping is available."],
e:"Centralise deterministic transformations. A single normalisation hook holding the mapping from each source encoding to your canonical form means one place to test, one place to update, and adding a fourth server is a mapping entry rather than a new hook. This is the boundary-normalisation pattern the blueprint prescribes for timestamps and status codes.",
k:"Centralise normalisation in one hook with a mapping table. New sources become mapping entries, not new hooks."},

{i:556,d:1,t:"1.6",s:"MAR",diff:2,
q:"A coordinator's decomposition produces subtasks whose results cannot be meaningfully combined.",
ask:"What went wrong?",
a:["The partition was drawn along an axis that does not compose &mdash; decomposition must consider how results will be reassembled",
"The synthesis agent is inadequate",
"Too many subtasks were created",
"The subagents used different models"],
c:0,
w:["Correct. A decomposition is only good if its parts recombine into a coherent whole.",
"Synthesis cannot combine parts that were never designed to fit together.",
"Count is not the issue; the axis is.",
"Model choice does not determine whether results compose."],
e:"Decomposition and synthesis are two halves of one design. When you draw the partition, ask how the pieces will be reassembled &mdash; slices along a natural axis (sub-domains, components, phases) compose readily, while arbitrary slices produce fragments that overlap awkwardly and leave seams. This is a distinct failure from over-decomposition, though the symptom of fragmented synthesis looks similar.",
k:"Design the decomposition and the recomposition together. Slice along axes whose parts naturally reassemble."},

{i:557,d:1,t:"1.7",s:"CGC",diff:2,
q:"You want to keep a plan file useful as execution progresses.",
ask:"What should you maintain in it?",
a:["Which phases are complete, decisions taken with their rationale, and any revisions to the remaining plan",
"Only the original plan, unchanged, as a record of intent",
"A log of every command executed",
"The full conversation from the planning session"],
c:0,
w:["Correct. A living plan supports resumption, partial rollback and handover.",
"A frozen plan diverges from reality and stops being useful for resumption.",
"A command log is execution detail, not plan state.",
"The planning conversation is long and buries the decisions in deliberation."],
e:"The plan file earns its keep by staying current. Progress markers tell a resumed session where to pick up; recorded decisions and rationale prevent re-litigating settled questions and enable partial rollback when one proves wrong; revisions keep the remaining plan accurate. A stale plan is as misleading as a stale scratchpad.",
k:"Keep the plan file current: progress, decisions with rationale, and revisions. A frozen plan stops being useful."},

{i:558,d:2,t:"2.1",s:"CSA",diff:2,
q:"Your tools are named <code>get_customer</code>, <code>lookup_order</code>, <code>process_refund</code> and <code>escalate_to_human</code>.",
ask:"What is good about these names?",
a:["Each is a verb plus a specific object, so the name alone conveys what the tool does and roughly when it applies",
"They are all the same length",
"They use snake_case consistently",
"They avoid abbreviations"],
c:0,
w:["Correct. Verb-plus-object naming is what makes the name itself a useful selection signal.",
"Length uniformity is cosmetic.",
"Consistent casing is good practice but not what makes these names informative.",
"Also good practice, but secondary to the verb-object structure."],
e:"Good tool names do real selection work before the description is weighed. <code>lookup_order</code> tells you the operation and the entity; <code>process</code> or <code>handle</code> would tell you neither. When two tools overlap, renaming to sharpen the object is often the highest-leverage fix &mdash; the blueprint's <code>analyze_content</code> &rarr; <code>extract_web_results</code> example makes exactly this point.",
k:"Verb + specific object names carry selection signal. Sharpen the object when tools overlap."},

{i:559,d:2,t:"2.2",s:"SDE",diff:2,
q:"A tool that processes documents fails on one page of a fifty-page file.",
ask:"What should it return?",
a:["Partial results for the 49 pages that processed, an explicit partial status, and the failure detail for the one page",
"An error with no results",
"Success with the 49 pages and no mention of the failure",
"An error, with the 49 pages included but the status marked as failed"],
c:0,
w:["Correct. Partial success is its own status, with both halves documented.",
"Discarding 49 pages of successful work is wasteful and unnecessary.",
"Silent suppression &mdash; the caller cannot tell a page is missing.",
"Closer, but a blanket failed status understates what was achieved and may trigger a full retry."],
e:"Partial success needs its own explicit status so the caller neither retries everything nor assumes completeness. Report which pages succeeded, which failed and why, and mark the aggregate as partial. That feeds directly into coverage annotation downstream &mdash; a reader can be told that page 34 was unreadable rather than silently receiving a 49-page extraction.",
k:"Partial results get an explicit partial status with both halves documented. Not success, not blanket failure."},

{i:560,d:2,t:"2.3",s:"CSA",diff:2,
q:"You are designing tool access for a read-only reporting agent.",
ask:"What is the correct approach?",
a:["Grant only read tools, so write operations are structurally impossible regardless of what the model attempts",
"Grant all tools and instruct it to use only read operations",
"Grant write tools but add a hook that blocks writes",
"Grant read tools plus one write tool for emergencies"],
c:0,
w:["Correct. Omit the capability rather than granting and restricting it.",
"The recurring insufficient pattern &mdash; granting capability then asking the model not to use it.",
"A hook blocking every write is a workaround for granting tools that should not have been granted; it also enlarges the toolset and hurts selection.",
"An emergency write tool in a read-only agent is a contradiction that will eventually be used."],
e:"Least privilege is expressed by what you grant. A read-only role gets read tools; the write operations are structurally unavailable, which is stronger than any block and also improves selection reliability by keeping the toolset small. If an emergency write is genuinely needed, it belongs behind a separate agent with an approval gate, not in the reporting agent's list.",
k:"Omit capability rather than granting and blocking. A read-only agent gets read tools only."},

{i:561,d:2,t:"2.4",s:"DPT",diff:2,
q:"A community MCP server you depend on releases a breaking change.",
ask:"What is the practical mitigation?",
a:["Pin the version in your configuration and upgrade deliberately after testing, rather than tracking latest",
"Fork the server and maintain your own copy permanently",
"Stop using community servers",
"Wrap every call in a retry"],
c:0,
w:["Correct. Version pinning is the standard mitigation for a dependency you do not control.",
"Forking is a heavy permanent commitment for a problem pinning solves.",
"Community servers remain preferable for standard integrations; the trade-off is managed, not avoided.",
"Retries do nothing about a breaking interface change."],
e:"Preferring community servers means accepting a dependency you do not control, and the standard mitigation applies: pin the version, upgrade deliberately, and test before promoting. This is worth stating explicitly because the blueprint's guidance to prefer community servers is sometimes read as having no downside &mdash; it has one, and pinning is how you manage it.",
k:"Pin MCP server versions and upgrade deliberately. Preferring community servers means managing a dependency you don't control."},

{i:562,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent is asked to summarise what a 4,000-line file does.",
ask:"What is a reasonable approach?",
a:["Read the file's structure &mdash; imports, exported symbols, top-level definitions &mdash; then read specific sections in depth as needed",
"Read the entire file in one call",
"Grep for comments and summarise those",
"Read the first 200 lines and extrapolate"],
c:0,
w:["Correct. Establish the shape first, then read selectively where depth is required.",
"Sometimes acceptable, but for a large file it spends a lot of context when a structural pass may suffice.",
"Comments may be stale or absent, and they describe intent rather than behaviour.",
"Extrapolating from the opening is guessing, and the interesting logic is rarely in the first 200 lines."],
e:"Structure-first reading applies within a file as well as across a repository: establish the shape, then deepen selectively. For a summary of what a file does, the exported surface and top-level definitions often answer the question entirely. Where deeper reading is needed, targeted range reads keep the context spend proportional to the question.",
k:"Structure first, then selective depth &mdash; within files as well as across repositories."},

{i:563,d:3,t:"3.1",s:"CGC",diff:2,
q:"Your CLAUDE.md says: <em>we use PostgreSQL</em>, and nothing more about the database.",
ask:"Is this a useful entry?",
a:["Marginally &mdash; it is a fact rather than a convention, so it is useful only if it changes decisions the model would otherwise get wrong",
"Yes &mdash; all project facts belong in CLAUDE.md",
"No &mdash; facts should never appear in CLAUDE.md",
"Yes &mdash; it prevents the model suggesting another database"],
c:0,
w:["Correct. Judge every line by whether it changes a decision; some facts do, most do not.",
"Indiscriminate fact collection is how CLAUDE.md bloats to 1,400 lines.",
"Too absolute &mdash; a fact that changes decisions (which ORM, which migration tool) earns its place.",
"Partly true, but the model would usually infer the database from the code anyway."],
e:"Apply the test consistently: does this line change a decision the model would otherwise get wrong? &ldquo;We use PostgreSQL&rdquo; is often inferable from the code and dependencies. &ldquo;Use the repository pattern for data access; raw SQL only in <code>db/queries/</code>&rdquo; is a convention that changes what gets written. Prefer conventions to facts, and facts only where they are not obvious from the code.",
k:"Judge CLAUDE.md lines by whether they change a decision. Prefer conventions; include facts only when non-obvious."},

{i:564,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a command that runs a specific multi-step workflow the same way every time.",
ask:"What is the appropriate mechanism?",
a:["A project slash command containing the workflow prompt, escalating to a skill if it needs isolation or tool restrictions",
"An entry in CLAUDE.md describing the workflow",
"A shell script the developer runs first",
"A note in the team wiki"],
c:0,
w:["Correct. Choose the lightest mechanism that fits, and escalate only when operational configuration is required.",
"CLAUDE.md is for always-on conventions, not on-demand invocable workflows.",
"A shell script cannot invoke the agent workflow.",
"A wiki is not in the agent's context."],
e:"Match the mechanism to the requirement: CLAUDE.md for conventions that should always apply, a slash command for an invocable reusable prompt, a skill when you need <code>context: fork</code>, <code>allowed-tools</code> or <code>argument-hint</code>. Start at the lightest and escalate only when a requirement demands it.",
k:"CLAUDE.md = always-on conventions. Command = invocable prompt. Skill = when you need operational config."},

{i:565,d:3,t:"3.3",s:"CGC",diff:2,
q:"A path-scoped rule is not loading and you suspect the glob.",
ask:"What is the most common mistake to check for?",
a:["Missing <code>**/</code> so the pattern matches only one directory level rather than any depth",
"Using lowercase in the pattern",
"Placing the rule in <code>.claude/rules/</code>",
"Including a file extension in the pattern"],
c:0,
w:["Correct. <code>*.test.tsx</code> matches only the root; <code>**/*.test.tsx</code> matches at any depth.",
"Case is not the usual culprit, though case sensitivity does vary by platform.",
"That is the correct location.",
"Extensions in patterns are entirely normal."],
e:"The depth wildcard is the most common glob mistake, and it matters most for exactly the case path-scoped rules exist to serve &mdash; conventions that follow a file type across many directories. <code>**/</code> is what makes co-located test files anywhere in the tree match a single rule. Check depth first when a rule is not firing.",
k:"Missing <code>**/</code> is the most common glob error. It is what makes a pattern match at any depth."},

{i:566,d:3,t:"3.4",s:"CGC",diff:2,
q:"A plan produced in planning mode turns out to be much larger than expected once written down.",
ask:"What is the value of discovering this at plan time?",
a:["Scope can be renegotiated or the work phased before any code is written, when changing direction is cheapest",
"It proves planning mode is unnecessary",
"It indicates the model misunderstood the task",
"It means the task should be abandoned"],
c:0,
w:["Correct. Surfacing true scope before implementation is one of planning mode's main benefits.",
"The opposite &mdash; this is planning mode working exactly as intended.",
"Possible, but the simpler and more common explanation is that the task really is that large.",
"Large is not the same as impossible; it means phasing or renegotiation."],
e:"Discovering true scope is a planning output as valuable as the approach itself. A task revealed to be forty files rather than five can be renegotiated, phased, or split across people &mdash; decisions that are cheap before implementation and expensive halfway through. Write the plan to a file so the phasing survives across sessions.",
k:"Planning surfaces true scope cheaply. Renegotiate or phase before writing code, not halfway through."},

{i:567,d:3,t:"3.5",s:"CGC",diff:2,
q:"You give three concrete examples and the implementation handles all three correctly but fails on a fourth case you did not show.",
ask:"What does this suggest?",
a:["The examples did not span the relevant variation; add an example covering the class the fourth case belongs to",
"Examples are ineffective for this task",
"The implementation should be rewritten from scratch",
"Three examples is too few in all cases"],
c:0,
w:["Correct. Examples generalise from what they span, so a gap in coverage becomes a gap in behaviour.",
"They worked for the three classes shown &mdash; the coverage was incomplete, not the technique.",
"An incremental fix with an added example is far cheaper than a rewrite.",
"Two to four is the recommended range; what matters is that they span the variation."],
e:"Choose examples to span the interesting variation rather than to accumulate count. When a failure appears in an uncovered class, add an example representing that class &mdash; and add it as a test case at the same time, so the requirement is both communicated and permanently verified. That is the test-driven iteration loop in practice.",
k:"Examples generalise from what they span. On a failure, add an example for that class &mdash; and make it a test."},

{i:568,d:3,t:"3.6",s:"CCI",diff:2,
q:"A CI run needs to review a change that spans code and infrastructure definitions with different standards.",
ask:"How should the standards be applied?",
a:["Path-scoped rules for each, so both sets load automatically based on which files are in the diff",
"One combined standard covering both",
"Two separate CI jobs with different prompts",
"Review only the code and handle infrastructure manually"],
c:0,
w:["Correct. Both rule sets load automatically for a diff that touches both, with no orchestration required.",
"A combined standard applies infrastructure rules to code and vice versa.",
"Duplicates configuration across jobs and drifts; the rules already handle this.",
"Leaving infrastructure unreviewed is where costly misconfigurations reach production."],
e:"Path-scoped rules compose naturally for mixed diffs: each rule fires for the files it matches, so a change touching both code and infrastructure loads both sets without you orchestrating anything. The same configuration also governs local development, so a developer sees the same standards before pushing.",
k:"Path-scoped rules compose for mixed diffs. Each fires for its own files, locally and in CI alike."},

{i:569,d:4,t:"4.1",s:"CSA",diff:2,
q:"You want the agent to prioritise resolution speed but never at the cost of accuracy on financial figures.",
ask:"How should this be expressed?",
a:["State the priority order explicitly and name the exception &mdash; and enforce the financial accuracy part in code rather than relying on the prompt",
"Instruct the agent to be fast but careful",
"Set a target response time in the prompt",
"Instruct the agent to double-check everything"],
c:0,
w:["Correct. Explicit priorities in the prompt, with the non-negotiable part enforced in code.",
"&ldquo;Fast but careful&rdquo; states a tension without resolving it.",
"A time target encourages cutting the very corners the exception forbids.",
"Blanket double-checking slows everything and still does not guarantee financial accuracy."],
e:"Two techniques for two parts of the requirement. The priority ordering is a judgement, so state it explicitly in the prompt: prefer speed, except where financial figures are involved, where accuracy takes precedence. The financial accuracy itself is a hard requirement, so back it with validation in code &mdash; reconcile computed against stated values rather than trusting the instruction.",
k:"State priority orders explicitly in the prompt; enforce the non-negotiable exception in code."},

{i:570,d:4,t:"4.2",s:"SDE",diff:2,
q:"You need few-shot examples for a document type you have only two real samples of.",
ask:"What is a reasonable approach?",
a:["Use both real samples and construct one or two representative synthetic examples covering variation you expect but have not yet seen",
"Use only the two real samples",
"Wait until more real samples are available",
"Use samples from a different document type"],
c:0,
w:["Correct. Synthetic examples covering anticipated variation are legitimate and often necessary early on.",
"Two samples from a narrow slice risks teaching that slice's incidental characteristics.",
"Delaying indefinitely is not an option when the pipeline is needed.",
"Examples from a different type teach the wrong positional and structural conventions."],
e:"Synthetic examples are a reasonable bridge when real coverage is thin, provided they are realistic and represent variation you genuinely expect. Validate against real documents as they arrive, and replace synthetic examples with real ones where they turn out to be unrepresentative &mdash; an example that teaches a pattern the real world does not contain is worse than none.",
k:"Construct synthetic examples to cover anticipated variation, then validate and replace them with real samples."},

{i:571,d:4,t:"4.3",s:"SDE",diff:2,
q:"Your extraction tool's schema declares 30 required fields. Documents vary in which fields they contain.",
ask:"What is the consequence?",
a:["The model will fabricate values for fields the document lacks, since the schema leaves it no way to report absence",
"Extraction will fail cleanly on any document missing a field",
"The model will return an empty object",
"The schema will be relaxed automatically"],
c:0,
w:["Correct. Required fields with no source data create pressure to invent, which is a documented cause of fabrication.",
"A clean failure would at least be honest, but the model typically produces a schema-satisfying value instead.",
"It produces a complete-looking object, not an empty one.",
"Nothing relaxes automatically."],
e:"Schema design shapes behaviour. Marking a field required is an instruction to produce something for it, so a document genuinely lacking that field yields a fabrication. Reserve <code>required</code> for fields present in every valid document, make the rest nullable, and give absence an explicit representation so it is reported rather than invented.",
k:"Over-using <code>required</code> causes fabrication. Reserve it for universally present fields; make the rest nullable."},

{i:572,d:4,t:"4.4",s:"SDE",diff:2,
q:"A validation rule is firing on 30% of documents, and inspection shows most of those documents are actually correct.",
ask:"What should you do?",
a:["Fix the validation rule, since a rule with a high false-positive rate wastes retries and can push correct extractions into review",
"Increase the retry budget to accommodate the failures",
"Route all 30% to human review",
"Disable validation for that field"],
c:0,
w:["Correct. An over-strict rule is a defect in the validator, not in the extraction.",
"Retrying correct extractions produces different correct extractions that also fail the bad rule.",
"Sending correct extractions to review consumes reviewer capacity and causes review fatigue.",
"Disabling loses genuine detection along with the noise."],
e:"Validators need the same precision discipline as review criteria. A rule firing on 30% of documents where most are correct is over-strict &mdash; perhaps a range that is too narrow, or a cross-field check that does not hold for a legitimate document variant. Fix the rule against the real distribution, and keep tracking which rule fires so precision stays measurable.",
k:"Over-strict validation rules waste retries and flood review. Measure per-rule precision and fix the rule."},

{i:573,d:4,t:"4.5",s:"SDE",diff:2,
q:"You must choose between one batch of 10,000 documents and ten batches of 1,000.",
ask:"What is the main consideration?",
a:["Failure isolation and partial-result availability &mdash; smaller batches let you act on early results and limit the impact of a systematic problem",
"Cost, which is significantly lower for one large batch",
"Ten batches will always complete faster",
"One batch is required above 5,000 documents"],
c:0,
w:["Correct. Smaller batches give earlier partial results and contain the blast radius of a bad prompt.",
"Per-token pricing does not meaningfully change with batch size.",
"Completion time is not reliably improved by splitting.",
"No such requirement is the substantive consideration here."],
e:"Batch granularity is an operational trade-off. Smaller batches surface a systematic problem after 1,000 documents rather than 10,000, and let downstream work begin on early results. Larger batches are simpler to manage. Either way, sample-test the prompt before the first run &mdash; that is what catches the systematic problem before either option pays for it.",
k:"Smaller batches give earlier signal and contain blast radius. Sample-test before any large run regardless."},

{i:574,d:4,t:"4.6",s:"CCI",diff:2,
q:"Your review architecture has one pass. You are considering adding a second, independent pass over the same input.",
ask:"What determines whether this is worthwhile?",
a:["Whether the second pass has a genuinely different remit or perspective &mdash; two identical passes mostly duplicate findings",
"Whether the budget allows a second model call",
"Whether the first pass takes less than five minutes",
"Whether developers have asked for more comments"],
c:0,
w:["Correct. Value comes from difference &mdash; a distinct remit, a distinct input, or genuine independence.",
"Budget is a constraint, not a reason the second pass would help.",
"Runtime is unrelated to whether it adds findings.",
"More comments is not the goal; more <em>distinct</em> findings is."],
e:"Additional passes pay off when they differ. Different remit (security versus correctness), different input (per-file versus cross-file), or genuine independence from the generation context all produce findings the first pass would miss. An identical second pass over identical input mostly repeats the first, at double the cost and with duplicate comments that erode trust.",
k:"Extra review passes must differ in remit, input or independence. Identical passes duplicate rather than add."},

{i:575,d:5,t:"5.1",s:"CAP",diff:2,
q:"An agent must apply a rule stated by the user 30 turns ago, but the user has since given a conflicting instruction.",
ask:"What should govern?",
a:["The later instruction, with the agent confirming the change so the user knows the earlier rule has been superseded",
"The earlier instruction, since it was established first",
"Whichever appears in the system prompt",
"The agent should refuse to act until the user resolves the conflict"],
c:0,
w:["Correct. Later instructions supersede earlier ones, and confirming makes the change visible.",
"Precedence by recency, not by order of establishment.",
"Neither is in the system prompt; both are conversational.",
"Refusing is unnecessary friction when the resolution rule is clear."],
e:"Conversational instructions follow recency, and the confirmation matters because the user may not have realised they were overriding an earlier preference. This connects to durable-instruction placement: if a rule must genuinely persist and not be casually overridden, it belongs in the system prompt or a preferences block rather than as a conversational statement.",
k:"Later conversational instructions supersede earlier ones; confirm the change. Truly durable rules belong in the system prompt."},

{i:576,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent identifies that a customer's problem is caused by a known system-wide outage.",
ask:"What is the right handling?",
a:["Inform the customer of the outage and the expected resolution, without escalating an issue that no individual agent can resolve",
"Escalate to a human agent",
"Attempt the failed operation repeatedly until the outage ends",
"Tell the customer their request could not be processed"],
c:0,
w:["Correct. A known outage with known information is resolvable within the agent's scope by informing.",
"Escalating an outage floods human agents with cases none of them can resolve.",
"Repeated attempts during an outage waste resources and delay the customer.",
"Withholding the known cause is unhelpful and invites a repeat contact."],
e:"Recognise cases where information is the resolution. A known outage has no per-customer remedy, so the agent's job is to inform accurately and set expectations. Escalating would multiply human load with identical unresolvable cases, and retrying would just delay the customer. This depends on the agent having access to outage status &mdash; which is a good argument for exposing it as an MCP resource.",
k:"For known system-wide issues, informing <em>is</em> the resolution. Escalating multiplies unresolvable human load."},

{i:577,d:5,t:"5.3",s:"MAR",diff:2,
q:"A subagent returns an error your coordinator's recovery logic does not recognise.",
ask:"What should the default behaviour be?",
a:["Treat unrecognised errors conservatively &mdash; do not retry blindly, surface the raw error, and annotate the gap",
"Retry, since most errors are transient",
"Ignore it and proceed",
"Terminate the workflow"],
c:0,
w:["Correct. Unknown means unknown; do not assume retryability, and make the situation visible.",
"Assuming transience on an unrecognised error causes retry storms on deterministic failures.",
"Ignoring produces an unannotated gap.",
"Terminating over an unrecognised error is the abort anti-pattern."],
e:"Design the unknown-error path deliberately, because it is the one you have not thought about. Conservative handling means no blind retry, the raw error preserved and surfaced for diagnosis, and the coverage gap annotated in the output. Unrecognised errors are also a signal to extend the error contract &mdash; each one is a case your taxonomy did not cover.",
k:"Unrecognised errors: no blind retry, surface the raw detail, annotate the gap, and extend the taxonomy."},

{i:578,d:5,t:"5.4",s:"DPT",diff:2,
q:"Your findings file and the live session context disagree about a detail.",
ask:"Which should be treated as authoritative?",
a:["Whichever was verified more recently against the actual source &mdash; and the disagreement itself should be investigated and resolved",
"The findings file, always",
"The live context, always",
"Neither &mdash; re-derive the detail from scratch"],
c:0,
w:["Correct. Recency of verification against the source decides, and the divergence is itself worth understanding.",
"The file can be stale if the world changed after it was written.",
"Live context can be degraded or built on a stale read.",
"Sometimes necessary, but the first step is to determine which is right and why they diverged."],
e:"A disagreement between your externalised record and the live context means one of them is wrong, and knowing which tells you something about your process. If the file is stale, your write discipline needs tightening; if the context is degraded, it is time to reset from the file. Resolve against the actual source, then update whichever was wrong.",
k:"Resolve record-versus-context disagreements against the source, and treat the divergence as a process signal."},

{i:579,d:5,t:"5.5",s:"SDE",diff:2,
q:"Your sampling of auto-approved extractions finds a 4% error rate on a field you believed was 99% accurate.",
ask:"What should you conclude?",
a:["The calibration or the validation set no longer reflects production; investigate what changed and recalibrate before continuing to automate that field",
"The sample was unlucky and should be ignored",
"The threshold should be lowered slightly",
"Human reviewers are making mistakes"],
c:0,
w:["Correct. A large gap between believed and observed accuracy means the model of quality is wrong.",
"Dismissing an inconvenient measurement is how drift goes undetected.",
"Adjusting a threshold without understanding the cause is guesswork.",
"Blaming the measurement rather than investigating is the wrong instinct."],
e:"Sampling the auto-approved stream exists precisely to catch this. A 4% observed rate against a believed 1% means either the input distribution has shifted, the model or prompt changed, or the original validation set was unrepresentative. Investigate which, recalibrate against current data, and consider pausing automation for that field while you do.",
k:"A gap between believed and sampled accuracy means calibration is stale. Investigate the cause before adjusting thresholds."},

{i:580,d:5,t:"5.6",s:"MAR",diff:2,
q:"You want readers to be able to verify any claim in a report quickly.",
ask:"What must the report contain?",
a:["Inline attribution per claim &mdash; source, date and the supporting quote &mdash; rather than a source list at the end",
"A longer bibliography",
"A confidence rating per section",
"An appendix containing all consulted sources in full"],
c:0,
w:["Correct. Verification must be local to the claim, or nobody will do it.",
"A bibliography proves consultation, not which source supports which claim.",
"A confidence rating is an assertion, not evidence.",
"A full-text appendix is enormous and still requires the reader to find the relevant passage."],
e:"Verification has to be cheap or it does not happen. Inline attribution &mdash; the source, its date and the specific quote &mdash; puts everything the reader needs next to the claim. This is why the claim-to-source-to-quote-to-date record must survive through aggregation and rendering: flattening at the last step undoes all the earlier discipline.",
k:"Make verification local: source, date and quote inline with each claim, not collected at the end."},

{i:581,d:1,t:"1.1",s:"AAT",diff:2,
q:"An agent loop runs 30 iterations on a task that should take 4.",
ask:"What should you inspect first in the trace?",
a:["Whether the same tool is being called repeatedly with similar arguments, which usually indicates unclear or incomplete tool results",
"Whether the model is too capable for the task",
"Whether the system prompt is too long",
"Whether the iteration cap is set correctly"],
c:0,
w:["Correct. Repeated near-identical calls are the classic signature of results the agent cannot act on confidently.",
"Capability does not cause loop inflation.",
"System prompt length does not drive iteration count.",
"The cap bounds the runaway; it does not explain why it happened."],
e:"Read the trace for repetition. Repeated calls with similar arguments mean the agent cannot tell that it already has what it needs, or the result was ambiguous, or an empty result was indistinguishable from a failure. All three are tool-output problems. Fix the contract &mdash; complete, labelled results with unambiguous empty and error representations &mdash; rather than tightening the cap.",
k:"Loop inflation is usually a tool-output problem. Look for repeated near-identical calls in the trace."},

{i:582,d:2,t:"2.2",s:"AAT",diff:2,
q:"Two tools return errors with different field names for the same concept: one uses <code>retryable</code>, the other <code>canRetry</code>.",
ask:"What should you do?",
a:["Normalise to one contract, either in the tools themselves or in a hook, so recovery logic is written once",
"Have the agent handle both field names",
"Use whichever field name each tool prefers",
"Remove retryability information from both"],
c:0,
w:["Correct. One contract means one recovery implementation and no per-tool special cases.",
"Asking the model to handle inconsistent contracts is exactly the deterministic-mapping work that belongs in code.",
"Inconsistency compounds as more tools are added.",
"Removing the field loses the signal that drives recovery."],
e:"Interface consistency is what makes recovery logic simple. Where you own the tools, standardise the contract directly; where you do not, normalise at the boundary in a <code>PostToolUse</code> hook. Either way the agent and your code see one shape, and adding a fifth tool with yet another field name becomes a mapping entry rather than a new branch everywhere.",
k:"Normalise error contracts to one shape &mdash; in the tools you own, in a hook for the ones you do not."},

{i:583,d:3,t:"3.1",s:"CGC",diff:2,
q:"Your team debates whether to put a convention in CLAUDE.md or enforce it with a linter rule.",
ask:"What should decide?",
a:["Whether the convention is mechanically checkable &mdash; if a linter can enforce it, it should, freeing CLAUDE.md context for judgement-based conventions",
"Whether the team prefers reading markdown or configuration",
"Whether the convention is new or established",
"Whether CLAUDE.md is currently short"],
c:0,
w:["Correct. Push rules down to the strongest applicable tier; linters are deterministic and free at inference time.",
"Preference is not the criterion.",
"Age of the convention is irrelevant.",
"Current file length is a symptom, not the deciding principle."],
e:"Three tiers, strongest first. <strong>Deterministic tooling</strong> for mechanically checkable rules &mdash; formatting, import order, naming patterns. <strong>CLAUDE.md and rules</strong> for judgement-based conventions tooling cannot express. <strong>Hooks and permissions</strong> for hard constraints that must be guaranteed. Pushing each rule to the strongest tier that can hold it keeps CLAUDE.md small and effective.",
k:"Mechanically checkable &rarr; linter. Judgement-based &rarr; CLAUDE.md. Must be guaranteed &rarr; hook."},

{i:584,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want to extract a list that may legitimately be empty.",
ask:"How should the schema handle this?",
a:["Declare the array as required with an empty array as a valid value, so absence of items is expressed unambiguously",
"Make the array optional so it can be omitted",
"Use <code>null</code> to mean no items",
"Use a sentinel entry meaning &ldquo;none&rdquo;"],
c:0,
w:["Correct. A required array with an empty value distinguishes &ldquo;none found&rdquo; from &ldquo;not looked for&rdquo;.",
"Omission is ambiguous &mdash; it cannot be distinguished from the field not being processed.",
"Null and empty carry different meanings; using null for empty conflates them.",
"Sentinel entries pollute the data and require special handling everywhere."],
e:"For collections, a required field with an empty array is the clearest design: the field is always present, and emptiness means genuinely none. That mirrors the tool-result principle of distinguishing an empty result set from a failure &mdash; an empty array says &ldquo;I looked and found none&rdquo;, while an absent field says nothing at all.",
k:"Collections: required field, empty array for none. Distinguishes &ldquo;found none&rdquo; from &ldquo;did not look&rdquo;."},

{i:585,d:5,t:"5.2",s:"CSA",diff:2,
q:"A customer asks a question the agent can answer, then adds &ldquo;and if you can't help, put me through to someone.&rdquo;",
ask:"How should the agent proceed?",
a:["Attempt the resolution, since the request for a human was conditional on being unable to help",
"Escalate immediately, since a human was mentioned",
"Ask the customer to clarify which they want",
"Answer and escalate simultaneously"],
c:0,
w:["Correct. This is a conditional, not an unconditional request for a human.",
"An immediate-escalation trigger requires an explicit unconditional request; this one is conditional on failure.",
"The condition is clearly stated; asking adds friction for nothing.",
"Escalating a case you resolved wastes human capacity."],
e:"Distinguish an unconditional request (&ldquo;let me talk to a person&rdquo;) from a conditional one (&ldquo;if you can't help, put me through&rdquo;). The first is an immediate-escalation trigger with no diagnostic questions first. The second sets a fallback, and the correct behaviour is to attempt resolution and escalate only if the condition is met &mdash; which is exactly what the customer asked for.",
k:"Unconditional request for a human &rarr; escalate immediately. Conditional request &rarr; attempt, then escalate if the condition holds."},

{i:586,d:1,t:"1.4",s:"AAT",diff:2,
q:"You are told a rule &ldquo;must always be followed&rdquo; but the consequence of a violation is a slightly worse user experience.",
ask:"How should you interpret this?",
a:["Push back on &ldquo;always&rdquo; &mdash; the consequence suggests prompt guidance, and over-enforcement removes useful flexibility",
"Implement it as a blocking hook, since &ldquo;always&rdquo; was stated",
"Implement it in both a hook and the prompt",
"Ignore the rule since the consequence is minor"],
c:0,
w:["Correct. Match enforcement to consequence, and say so when the stated requirement overshoots it.",
"Taking &ldquo;always&rdquo; literally where the consequence is minor produces brittle systems.",
"Double enforcement of a low-consequence preference is unnecessary machinery.",
"Ignoring a stated requirement is not the alternative to over-enforcing it."],
e:"&ldquo;Always&rdquo; is often rhetorical emphasis rather than a specification of consequence. The right response is to ask what happens on the one violation in a hundred: if the answer is a slightly worse experience, prompt guidance is appropriate and preserves the agent's ability to adapt to unusual cases. Over-enforcement is a genuine failure mode the exam tests, not just under-enforcement.",
k:"Interrogate &ldquo;always&rdquo;. Enforcement should match consequence &mdash; over-enforcement is its own failure mode."},

{i:587,d:2,t:"2.4",s:"DPT",diff:2,
q:"You need an MCP server available to only one specialised agent, not to every agent in the system.",
ask:"What is the design consideration?",
a:["Scope the connection to that agent, since every connected server's tools join the choice space for any agent that can see them",
"MCP servers are always global and cannot be scoped",
"Put the server in user scope instead of project scope",
"Reduce the server's tool count instead"],
c:0,
w:["Correct. Per-agent scoping keeps the specialist's capability out of everyone else's toolset.",
"Which agents get which tools is a design decision you control.",
"User versus project scope determines <em>who</em> is affected, not which agent within a system.",
"Worth doing as well, but it does not address which agents see the server."],
e:"All connected servers' tools are simultaneously available to an agent that can see them, so connecting a server globally taxes selection for every agent. Where a capability belongs to one specialisation, scope it there &mdash; that is the tool-allocation principle applied at the MCP level, and it keeps each agent's toolset small and role-coherent.",
k:"Scope MCP servers to the agents that need them. Global connections tax every agent's selection reliability."},

{i:588,d:3,t:"3.4",s:"CGC",diff:2,
q:"A task is small but you are unfamiliar with the codebase.",
ask:"What is appropriate?",
a:["A brief exploration &mdash; possibly via a subagent &mdash; then direct execution, without full planning mode",
"Full planning mode with a written plan",
"Direct execution immediately",
"A multi-agent workflow"],
c:0,
w:["Correct. Unfamiliarity warrants exploration; small scope with one obvious approach does not warrant full planning.",
"Planning overhead is disproportionate for a small change with no architectural choice.",
"Executing in unfamiliar code without any exploration risks missing conventions and context.",
"Heavy orchestration for a small task."],
e:"The two variables &mdash; how well specified the change is, and how familiar the codebase is &mdash; can point in different directions. Unfamiliar but small and well-specified calls for exploration without full planning: understand the local conventions and the affected code, then execute. Reserve planning mode for cases with genuine architectural choice or large scope.",
k:"Unfamiliar + small + well-specified &rarr; explore briefly, then execute. Planning is for architectural choice and scale."},

{i:589,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to reduce human review volume without increasing error rates.",
ask:"Which approach is most principled?",
a:["Improve first-attempt accuracy on the fields that most often trigger review, so fewer items qualify in the first place",
"Raise the confidence threshold so fewer items route to review",
"Sample the review queue instead of reviewing all of it",
"Reduce the number of validation rules"],
c:0,
w:["Correct. Reducing the population that needs review is the only approach that cuts volume without raising errors.",
"Raising the threshold routes fewer items to review by letting more errors through unreviewed.",
"Sampling the queue means some genuinely problematic items are auto-approved unreviewed.",
"Fewer rules means fewer detections, not fewer errors."],
e:"There are two ways to reduce review volume: send fewer items, or fix more items before they qualify. Only the second preserves the error rate. Use the per-rule and per-field failure breakdown to find which fields drive review volume, then improve their descriptions, examples or schema. That is why recording the failing rule on every retry matters.",
k:"Cut review volume by improving accuracy on the fields that trigger it &mdash; not by routing fewer items."},

{i:590,d:5,t:"5.3",s:"MAR",diff:2,
q:"You want the coordinator to distinguish a subagent that found nothing from one that could not run.",
ask:"What must the subagent's contract include?",
a:["Distinct representations for a successful empty result and an access or execution failure",
"A single status field with values success and failure",
"A timestamp on every result",
"A confidence score on every result"],
c:0,
w:["Correct. The distinction between an empty finding and an access failure must be explicit in the contract.",
"Two values cannot express three states: found something, found nothing, could not look.",
"Timestamps are useful metadata but do not express the distinction.",
"Confidence scores are uncalibrated and do not express it either."],
e:"Three outcomes need three representations: results found, no results (a valid finding), and could not execute (a failure). Collapsing the last two is the empty-result-as-error anti-pattern, and collapsing them the other way is silent suppression. Include the query attempted in the empty case so the coordinator can judge whether a reformulation is worth trying.",
k:"Three outcomes, three representations: found, found nothing, could not look. Never collapse the last two."},

{i:591,d:1,t:"1.2",s:"AAT",diff:2,
q:"A coordinator's routing decisions are correct but nobody can see why a particular agent was chosen on a given run.",
ask:"What should be captured?",
a:["The routing rationale alongside each delegation &mdash; which agent, why, and what alternatives were considered",
"The total number of delegations per run",
"The latency of each delegation",
"The model version used by the coordinator"],
c:0,
w:["Correct. Rationale is what makes a routing decision reviewable and a misroute diagnosable.",
"Counts describe volume, not reasoning.",
"Latency is operational data.",
"Version helps reproducibility but does not explain a specific choice."],
e:"Observability in a multi-agent system means capturing decisions, not just events. Recording which agent was chosen and why turns a misroute from an unexplained anomaly into a diagnosable case &mdash; you can see whether the description misled, whether two agents overlapped, or whether the request genuinely fell between specialisations. That is what the single hub trace is for.",
k:"Log routing rationale, not just routing events. Decisions are what make misroutes diagnosable."},

{i:592,d:2,t:"2.1",s:"AAT",diff:2,
q:"A tool has an optional parameter that significantly changes its behaviour, and the model rarely sets it.",
ask:"What should you consider?",
a:["Whether the behaviours should be separate tools, since a behaviour-changing parameter hides a decision from the selection machinery",
"Whether the parameter should be required",
"Whether the default should be changed",
"Whether the model needs more examples of setting it"],
c:0,
w:["Correct. This is the multi-mode tool problem &mdash; the significant decision is invisible to the name, description and schema.",
"Making it required forces a decision without making the choice any clearer.",
"Changing the default shifts which behaviour is silently chosen, without surfacing the decision.",
"Examples help at the margin but leave the decision buried in a parameter."],
e:"A parameter that significantly changes behaviour is a mode parameter, and it hides the second decision from the selection signal. Splitting into separate tools gives each behaviour its own name, description and schema, so the model chooses between them the same way it chooses between any two tools &mdash; and you gain per-behaviour permission scoping as a bonus.",
k:"Behaviour-changing parameters are hidden mode switches. Split them into separate tools with their own names and schemas."},

{i:593,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want a rule to apply to two file types in different directories: <code>api/**/*.ts</code> and <code>workers/**/*.ts</code>.",
ask:"How should the frontmatter express this?",
a:["A <code>paths</code> array containing both glob patterns",
"Two separate rule files with identical content",
"A single broader glob covering both plus everything between them",
"A rule with no paths, applying everywhere"],
c:0,
w:["Correct. The <code>paths</code> key takes multiple patterns, which is what makes one rule cover several locations.",
"Duplicate content in two files drifts apart.",
"A broader glob applies the rule to files it should not govern.",
"Applying everywhere loses the scoping benefit entirely."],
e:"Multiple globs in a single <code>paths</code> array are what let one rule follow a concern across several locations without duplication or over-broadening. One definition, one place to update, and it fires precisely on the files it governs. Prefer this over either duplicated rules or a glob widened until it accidentally covers unrelated code.",
k:"<code>paths</code> takes several globs. One rule, several locations, no duplication and no over-broadening."},

{i:594,d:4,t:"4.2",s:"CCI",diff:2,
q:"Your few-shot review examples show findings but never show a clean file with no findings.",
ask:"What is the risk?",
a:["The model may feel obliged to find something in every file, inflating false positives on clean code",
"The examples are too long",
"The model will refuse to review clean files",
"Nothing &mdash; examples of findings are what teach detection"],
c:0,
w:["Correct. Examples establish an implicit expectation, and never showing a null result implies findings are always expected.",
"Length is not the issue.",
"It will review them &mdash; and is likely to manufacture a finding.",
"Detection matters, but so does knowing that reporting nothing is a valid outcome."],
e:"Few-shot examples set expectations about output, including how often output is empty. Including a clean example that reports no findings makes the null result explicitly legitimate. This is the same principle as making absence expressible in an extraction schema: if the format leaves no room for &ldquo;nothing here&rdquo;, the model produces something.",
k:"Include a clean, no-findings example. If the null result is never demonstrated, the model manufactures findings."},

{i:595,d:5,t:"5.5",s:"SDE",diff:2,
q:"Your pipeline processes three document types. You have validated accuracy for two of them.",
ask:"What should happen with the third?",
a:["Route it to human review until its accuracy has been validated, rather than automating on the strength of the other two",
"Automate it, since the pipeline is the same",
"Exclude it from the pipeline",
"Automate it with a higher confidence threshold"],
c:0,
w:["Correct. Automation decisions are per segment, and an unvalidated segment has an unknown error rate.",
"Accuracy varies substantially by document type; the pipeline being identical does not make performance identical.",
"Excluding loses the throughput benefit entirely when review would suffice.",
"A higher threshold on uncalibrated confidence still has an unknown error rate."],
e:"Segment-level validation means segment-level automation decisions. An unvalidated type is not presumed good because its siblings are &mdash; the 97%-overall, 61%-on-handwritten example exists precisely because performance varies by type. Review it, use the review outcomes as your labelled set, and automate once the measured accuracy supports it.",
k:"Automate per validated segment. Unvalidated types go to review, and the review outcomes become your labelled set."}
);
