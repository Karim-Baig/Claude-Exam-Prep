QB.push(
{i:221,d:3,t:"3.1",s:"CGC",diff:2,
q:"A new team member clones the repository and starts using Claude Code. They report that Claude ignores several conventions the rest of the team takes for granted &mdash; commit message format, the preferred test runner, and the error-handling style. Everyone else's Claude Code follows them consistently.",
ask:"What is the most likely cause?",
a:["The conventions live in each existing developer's user-level <code>~/.claude/CLAUDE.md</code>, which is not shared through version control",
"The new developer needs to run an initialisation command to load the project's conventions",
"The project CLAUDE.md is too long and is being truncated for the new developer",
"Claude Code caches conventions per machine and the new machine has an empty cache"],
c:0,
w:["Correct. User-level CLAUDE.md applies to one user on one machine and is never shared through the repository.",
"There is no activation step &mdash; project CLAUDE.md is picked up automatically when it exists.",
"Length would affect everyone equally, not only the new joiner.",
"There is no per-machine convention cache."],
e:"This is the archetypal CLAUDE.md hierarchy question. The levels are: <strong>user</strong> (<code>~/.claude/CLAUDE.md</code>) &mdash; personal, applies to all your projects, never shared; <strong>project</strong> (<code>CLAUDE.md</code> in the repo root, or <code>.claude/CLAUDE.md</code>) &mdash; version-controlled and shared with everyone who clones; <strong>directory</strong> (a CLAUDE.md in a subdirectory) &mdash; applies to work in that part of the tree. When only some developers see a behaviour, suspect user-level configuration.",
k:"CLAUDE.md hierarchy: user (<code>~/.claude/</code>, personal) &rarr; project (repo, shared) &rarr; directory (subtree). Team conventions must be project-level."},

{i:222,d:3,t:"3.1",s:"CGC",diff:2,
q:"Your root CLAUDE.md has grown to 900 lines covering testing standards, API conventions, deployment procedures and database migration rules. Developers report that Claude sometimes applies deployment rules while writing tests.",
ask:"What is the best restructuring?",
a:["Split into topic-focused files under <code>.claude/rules/</code>, each covering one concern",
"Reorder the file so the most important sections appear first",
"Move the whole file to user level so each developer keeps only what they need",
"Shorten each section to a single line"],
c:0,
w:["Correct. The <code>.claude/rules/</code> directory exists precisely to replace a monolithic CLAUDE.md with topic-focused files.",
"Ordering helps marginally with salience but leaves all 900 lines competing for attention on every task.",
"User level destroys sharing, which is the whole point of project conventions.",
"Compressing to one line per section loses the specificity that makes conventions actionable."],
e:"The blueprint names this pattern: split a large CLAUDE.md into multiple <code>.claude/rules/</code> files &mdash; <code>testing.md</code>, <code>api-conventions.md</code>, <code>deployment.md</code>. Each file covers one concern, which keeps them maintainable and reviewable, and sets you up for the next step: adding <code>paths</code> frontmatter so a rule loads only when the relevant files are being edited. That is what stops deployment rules bleeding into test authoring.",
k:"Monolithic CLAUDE.md &rarr; topic-focused files in <code>.claude/rules/</code>. One concern per file, then scope them by path."},

{i:223,d:3,t:"3.1",s:"CGC",diff:3,
q:"A monorepo has five packages, each with different testing conventions. You want each package's CLAUDE.md to include the shared coding-style document plus its own testing standards.",
ask:"Which mechanism supports this?",
a:["The <code>@path</code> import syntax, for example <code>@./standards/coding-style.md</code>, referenced from each package's CLAUDE.md",
"Symbolic links from each package directory to the shared file",
"Copying the shared content into all five CLAUDE.md files",
"A single root CLAUDE.md containing all five packages' conventions"],
c:0,
w:["Correct. <code>@path</code> lets a CLAUDE.md reference external files, which is the documented way to modularise shared standards.",
"Symlinks are a filesystem workaround for something the tool supports natively, and they behave poorly on some platforms.",
"Five copies drift apart the moment one is updated.",
"Puts all five packages' conventions in every context, which is the attention-dilution problem you are trying to avoid."],
e:"The <code>@path</code> syntax lets CLAUDE.md reference external files, so shared standards live in one place and each package composes what it needs: a package CLAUDE.md might contain <code>@./standards/coding-style.md</code> and <code>@./standards/testing-react.md</code>. The blueprint gives exactly this use case &mdash; selectively including standards in each package's CLAUDE.md. Compare with <code>.claude/rules/</code> plus <code>paths</code> frontmatter, which is the better choice when conventions apply by file type across many directories rather than by package.",
k:"<code>@path</code> imports external files into CLAUDE.md, so shared standards live once and each package composes what it needs."},

{i:224,d:3,t:"3.1",s:"CGC",diff:2,
q:"A developer wants Claude to always address them by name and to prefer verbose explanations, across every project they work on.",
ask:"Where should this go?",
a:["Their user-level <code>~/.claude/CLAUDE.md</code>",
"The project CLAUDE.md of each repository they work in",
"A <code>.claude/rules/</code> file in each project",
"The project's <code>.claude/settings.json</code>"],
c:0,
w:["Correct. A personal preference spanning all projects is exactly what user-level CLAUDE.md is for.",
"Would impose one developer's personal preference on the entire team.",
"Same problem &mdash; rules files in a project are shared.",
"Settings configure behaviour and permissions, not conversational preferences."],
e:"The scope question is always &ldquo;who should this affect?&rdquo; Personal preferences that follow you across projects go in user-level CLAUDE.md. Conventions the whole team must follow go in project CLAUDE.md or <code>.claude/rules/</code>. Conventions specific to one part of the tree go in a directory-level CLAUDE.md or a path-scoped rule. Putting a personal preference at project level is the mirror image of the new-joiner problem in the earlier question.",
k:"Scope by audience: personal &rarr; user level. Team &rarr; project level. Subtree or file type &rarr; directory CLAUDE.md or path-scoped rule."},

{i:225,d:3,t:"3.1",s:"CGC",diff:3,
q:"Your repository has a <code>terraform/</code> directory with conventions that apply to nothing else, and a testing convention that applies to <code>**/*.test.tsx</code> files scattered across every package.",
ask:"How should each be configured?",
a:["A directory-level CLAUDE.md in <code>terraform/</code>, and a <code>.claude/rules/</code> file with <code>paths: [\"**/*.test.tsx\"]</code> frontmatter for the testing convention",
"Both as directory-level CLAUDE.md files",
"Both as <code>.claude/rules/</code> files with path frontmatter",
"Both in the root CLAUDE.md"],
c:0,
w:["Correct. Directory-level suits a convention confined to one subtree; path-scoped rules suit a convention that follows a file type across the tree.",
"A directory CLAUDE.md cannot express &ldquo;test files wherever they live&rdquo;, because they live everywhere.",
"Defensible, and a path-scoped rule for terraform would work &mdash; but the directory CLAUDE.md is the more natural fit for a self-contained subtree.",
"Puts both in every context regardless of what is being edited, wasting attention and tokens."],
e:"The blueprint draws this distinction explicitly: glob-based path rules are preferable to directory-level CLAUDE.md when conventions apply across many directories, giving tests as the example. Match the mechanism to the shape of the applicability. Confined to one subtree, everything in it &rarr; directory CLAUDE.md. Follows a file type or pattern across the repository &rarr; <code>.claude/rules/</code> with <code>paths</code> frontmatter.",
k:"Convention confined to a subtree &rarr; directory CLAUDE.md. Convention that follows a file type across the tree &rarr; path-scoped rule."},

{i:226,d:3,t:"3.1",s:"CGC",diff:2,
q:"Which statement about CLAUDE.md precedence is correct when user, project and directory files all contain guidance?",
ask:"Which is true?",
a:["All applicable levels contribute, with more specific levels providing the context most relevant to the current work",
"Only the most specific file is used and the others are ignored entirely",
"Only the root project CLAUDE.md is used",
"User-level CLAUDE.md overrides project-level for everyone on the team"],
c:0,
w:["Correct. The levels compose, with directory-level guidance adding specificity for work in that part of the tree.",
"They are not mutually exclusive &mdash; project conventions still apply while you work in a subdirectory.",
"Directory-level files exist precisely because the root is not the only source.",
"User-level configuration affects only that user; it is not shared and cannot affect the team."],
e:"Think composition, not override. User-level adds personal preferences, project-level adds team conventions, directory-level adds locality. The practical consequences: keep each level to what belongs at that level, avoid contradictions between levels (they are genuinely ambiguous to resolve), and remember that composition is why a bloated root CLAUDE.md hurts &mdash; it is in context even when you are working somewhere it does not apply.",
k:"CLAUDE.md levels compose rather than override. Keep each level to its own scope and avoid contradictions between them."},

{i:227,d:3,t:"3.1",s:"CGC",diff:2,
q:"Your CLAUDE.md contains the line: <em>Write good, clean, maintainable code.</em>",
ask:"What is the problem with this instruction?",
a:["It is not actionable &mdash; it states a goal every developer already shares without specifying any checkable convention",
"It is too short and should be expanded to a paragraph",
"It belongs at user level rather than project level",
"It should be in a <code>.claude/rules/</code> file instead"],
c:0,
w:["Correct. Vague exhortations consume context without changing behaviour, because they encode no decision.",
"Length is not the issue; a longer version of the same vagueness is worse.",
"The scope is right &mdash; the content is the problem.",
"Moving vague content to a different file leaves it equally vague."],
e:"CLAUDE.md competes for context on every request, so every line must earn its place by encoding a decision the model would otherwise get wrong. &ldquo;Write clean code&rdquo; encodes nothing. &ldquo;Use Result types for recoverable errors; reserve exceptions for programmer errors&rdquo; or &ldquo;every new endpoint needs a contract test in <code>tests/contract/</code>&rdquo; encodes a specific, checkable convention. This is the Domain 4 explicit-criteria principle applied to configuration.",
k:"Every CLAUDE.md line must encode a checkable decision. Vague quality exhortations cost context and change nothing."},

{i:228,d:3,t:"3.1",s:"CGC",diff:3,
q:"A team keeps adding to CLAUDE.md whenever Claude makes a mistake. After six months it is 1,400 lines and Claude's adherence to any individual rule has visibly declined.",
ask:"What is happening and what should be done?",
a:["The instructions are diluting each other; prune to the rules that still matter, split by topic into <code>.claude/rules/</code>, and scope them by path",
"Nothing is wrong; longer instructions always produce better adherence",
"Move the file to user level so it does not affect the team",
"Add emphasis markers such as IMPORTANT to the most critical rules"],
c:0,
w:["Correct. Instruction dilution is real: 1,400 lines of guidance competing on every request degrades adherence to all of it.",
"Adherence does not scale with length &mdash; it degrades past a point, which is what the team is observing.",
"Discards the team conventions entirely to fix a size problem.",
"Emphasis helps a little and quickly loses force when everything is marked important. It does not address dilution."],
e:"CLAUDE.md is a context budget, not a documentation dump. Three remedies apply together: <strong>prune</strong> rules that were reactions to one-off incidents or that tooling now enforces; <strong>split</strong> by topic into <code>.claude/rules/</code> so files stay coherent; and <strong>scope</strong> with <code>paths</code> frontmatter so only relevant rules load. Mechanical rules &mdash; formatting, import order, naming &mdash; are better enforced by linters than by instructions.",
k:"Instructions dilute each other. Prune, split by topic, scope by path, and push mechanical rules into linters."},

{i:229,d:3,t:"3.1",s:"DPT",diff:2,
q:"A convention applies only when working inside <code>services/payments/</code>, and that directory is self-contained.",
ask:"What is the simplest correct configuration?",
a:["A CLAUDE.md inside <code>services/payments/</code>",
"A rule in the root CLAUDE.md that begins &ldquo;when working in payments&hellip;&rdquo;",
"A <code>.claude/rules/</code> file with no path frontmatter",
"A user-level CLAUDE.md entry"],
c:0,
w:["Correct. Directory-level CLAUDE.md is exactly the mechanism for conventions confined to a subtree.",
"Puts the payments rules in context for everyone doing anything, relying on the model to notice the conditional.",
"Without path frontmatter, the rule loads regardless of what is being edited.",
"Personal scope, so the team would not get it."],
e:"Prefer the mechanism that makes applicability structural rather than conditional. A directory CLAUDE.md is loaded because of <em>where the work is happening</em>, so the model never has to evaluate a &ldquo;when working in X&rdquo; clause. That saves context for everyone else and removes a source of misapplication. Reach for a path-scoped rule instead when the applicability follows a file pattern across directories rather than a single subtree.",
k:"Make applicability structural: directory CLAUDE.md for a subtree, path frontmatter for a file pattern. Avoid &ldquo;when working in X&rdquo; clauses."},

{i:230,d:3,t:"3.1",s:"GEN",diff:1,
q:"Where does the user-level CLAUDE.md live?",
ask:"Which path is correct?",
a:["<code>~/.claude/CLAUDE.md</code>",
"<code>./CLAUDE.md</code>",
"<code>.claude/rules/CLAUDE.md</code>",
"<code>~/.claude.json</code>"],
c:0,
w:["Correct. User-level instructions live in the home <code>.claude</code> directory.",
"That is the project root CLAUDE.md, shared through version control.",
"<code>.claude/rules/</code> holds topic-focused rule files, not the user CLAUDE.md.",
"<code>~/.claude.json</code> is for user-scoped MCP server configuration."],
e:"Memorise the locations, because the exam uses them as distractors for each other. <code>~/.claude/CLAUDE.md</code> &mdash; user instructions. <code>CLAUDE.md</code> in the repo root or <code>.claude/CLAUDE.md</code> &mdash; project instructions. <code>.claude/rules/*.md</code> &mdash; topic-focused, optionally path-scoped rules. <code>.claude/commands/</code> &mdash; project slash commands. <code>~/.claude/commands/</code> &mdash; user slash commands. <code>.claude/skills/</code> &mdash; skills. <code>.mcp.json</code> &mdash; project MCP servers. <code>~/.claude.json</code> &mdash; user MCP servers.",
k:"Learn the paths cold: <code>~/.claude/CLAUDE.md</code>, <code>.claude/rules/</code>, <code>.claude/commands/</code>, <code>.claude/skills/</code>, <code>.mcp.json</code>, <code>~/.claude.json</code>."},

{i:231,d:3,t:"3.2",s:"CGC",diff:2,
q:"You need a custom <code>/review</code> command that runs your team's standard code-review checklist, and it must be available to every developer as soon as they clone the repository.",
ask:"Where should the command file be created?",
a:["<code>.claude/commands/</code> in the project repository",
"<code>~/.claude/commands/</code>",
"The root CLAUDE.md",
"<code>.claude/config.json</code>"],
c:0,
w:["Correct. Project commands in <code>.claude/commands/</code> are version-controlled and automatically available to everyone who clones.",
"That is the user-level location for personal commands, so the team would not get it.",
"CLAUDE.md holds instructions and conventions, not command definitions.",
"That file does not exist in Claude Code's configuration model."],
e:"This mirrors a published sample question. The reasoning to internalise: <strong>location determines sharing</strong>. <code>.claude/commands/</code> in the repository is version-controlled, so cloning is all a developer needs to do. <code>~/.claude/commands/</code> is personal. Note the distractor pattern &mdash; option D invents a plausible-sounding config file, which the exam does more than once.",
k:"Project slash commands &rarr; <code>.claude/commands/</code> (shared via VCS). Personal &rarr; <code>~/.claude/commands/</code>."},

{i:232,d:3,t:"3.2",s:"CGC",diff:3,
q:"You are creating a skill that runs a large dependency audit. Its output is extremely verbose, and you do not want it consuming the main session's context.",
ask:"Which frontmatter setting achieves this?",
a:["<code>context: fork</code>",
"<code>allowed-tools</code>",
"<code>argument-hint</code>",
"<code>isolated: true</code>"],
c:0,
w:["Correct. <code>context: fork</code> runs the skill in an isolated subagent context so its output does not pollute the main session.",
"<code>allowed-tools</code> restricts which tools the skill may call &mdash; a capability constraint, not context isolation.",
"<code>argument-hint</code> prompts the developer for required parameters.",
"Not a real frontmatter key."],
e:"Know the three <code>SKILL.md</code> frontmatter fields the blueprint highlights, and what each does. <code>context: fork</code> &mdash; isolate the skill's execution in a subagent context, for verbose skills. <code>allowed-tools</code> &mdash; restrict the skill's capability, for safety and reliability. <code>argument-hint</code> &mdash; prompt for required parameters so the skill is invoked correctly. They are frequently offered as distractors for each other.",
k:"<code>SKILL.md</code>: <code>context: fork</code> isolates context &middot; <code>allowed-tools</code> restricts capability &middot; <code>argument-hint</code> prompts for parameters."},

{i:233,d:3,t:"3.2",s:"CGC",diff:3,
q:"A skill must be able to read files and search the codebase, but must never be able to modify files or run shell commands.",
ask:"Which <code>SKILL.md</code> frontmatter field actually removes those capabilities?",
a:["<code>disallowed-tools</code>, listing Write, Edit and Bash",
"<code>allowed-tools</code>, listing only Read, Grep and Glob",
"<code>context: fork</code>",
"A note in the skill's body text saying it is read-only"],
c:0,
w:["Correct. <code>disallowed-tools</code> removes tools from Claude's available pool while the skill is active &mdash; it is the field that actually restricts.",
"The tempting answer, and the one older study material gives. But <code>allowed-tools</code> <em>pre-approves</em> tools so they run without a permission prompt; it does <strong>not</strong> restrict, and every unlisted tool remains callable.",
"Forking isolates context; it does not restrict what the skill can do inside that context.",
"Body text is guidance to the model, not enforcement."],
e:"This is the most commonly misremembered pair in Domain 3, so learn the two directions separately.<br><br>In <strong>skill and command frontmatter</strong>: <code>allowed-tools</code> <em>pre-approves</em> listed tools for that turn so they run without a permission prompt, and explicitly does not restrict anything &mdash; unlisted tools stay callable. <code>disallowed-tools</code> is the one that <em>removes</em> tools from the pool. Both clear when you send your next message.<br><br>In a <strong>subagent definition</strong> the opposite naming applies: the <code>tools</code> field (and the Agent SDK's <code>allowedTools</code>) <em>is</em> an allowlist and does restrict, with <code>disallowedTools</code> as the denylist.",
n:"<strong>Verified against Anthropic docs.</strong> The published exam blueprint and most third-party study guides say &ldquo;use <code>allowed-tools</code> to restrict what tools a skill can use&rdquo;. The official Claude Code skills reference contradicts this: <code>allowed-tools</code> &ldquo;pre-approves tools for this turn only&hellip; Does not restrict which tools are available; all tools remain callable.&rdquo; If the exam offers <code>allowed-tools</code> as the restriction answer and no <code>disallowed-tools</code> option exists, pick it &mdash; but know the real behaviour.",
k:"Skills: <code>allowed-tools</code> pre-approves, <code>disallowed-tools</code> restricts. Subagents: <code>tools</code> is an allowlist and <em>does</em> restrict."},

{i:234,d:3,t:"3.2",s:"CGC",diff:2,
q:"A developer wants a personal variant of a shared team skill, with different defaults, without changing the team's version.",
ask:"How should this be done?",
a:["Create the variant in <code>~/.claude/skills/</code> under a different name",
"Edit the project's <code>.claude/skills/</code> version locally and avoid committing the change",
"Add a conditional branch inside the shared skill for that developer",
"Copy the team skill into a personal branch of the repository"],
c:0,
w:["Correct. User-level skills are personal, and a distinct name avoids ambiguity with the team version.",
"An uncommitted local modification to a tracked file causes constant conflicts and risks being committed by accident.",
"Per-developer branches inside a shared artefact do not scale and pollute team configuration.",
"A personal long-lived branch breaks the moment they need to work on another branch."],
e:"The blueprint notes that personal skill variants can live in <code>~/.claude/skills/</code> under different names. This is the same user-versus-project scope decision that governs CLAUDE.md, commands and MCP servers, and the different name matters &mdash; it makes invocation unambiguous and signals to the developer which version they are running.",
k:"Personal skill variants &rarr; <code>~/.claude/skills/</code> under a distinct name. Never locally modify shared project configuration."},

{i:235,d:3,t:"3.2",s:"CGC",diff:3,
q:"A skill requires a target module name to work, but developers frequently invoke it without one and it fails confusingly.",
ask:"Which frontmatter field helps?",
a:["<code>argument-hint</code>, which prompts the developer for the required parameter",
"<code>allowed-tools</code>",
"<code>context: fork</code>",
"<code>required: true</code>"],
c:0,
w:["Correct. <code>argument-hint</code> exists to prompt for required parameters at invocation time.",
"That restricts capability, not invocation.",
"That isolates the context, not the arguments.",
"Not a real frontmatter key."],
e:"<code>argument-hint</code> improves the invocation experience by telling the developer what the skill needs before it runs, rather than letting it fail partway through. It is a small feature that the exam tests because it distinguishes skills from plain slash commands &mdash; skills carry operational configuration (isolation, capability, arguments) that a simple command file does not.",
k:"<code>argument-hint</code> prompts for required skill parameters. Skills carry operational config; plain commands do not."},

{i:236,d:3,t:"3.2",s:"CGC",diff:2,
q:"What is the key difference between a project slash command and a skill?",
ask:"Which statement is most accurate?",
a:["Skills carry operational configuration in frontmatter &mdash; context isolation, tool restrictions, argument hints &mdash; while a plain command is primarily a reusable prompt",
"Slash commands can only be used by the developer who created them",
"Skills cannot be shared through version control",
"Slash commands run faster because they skip the agent loop"],
c:0,
w:["Correct. The distinguishing feature is the declarative operational configuration in <code>SKILL.md</code> frontmatter.",
"Project commands in <code>.claude/commands/</code> are shared with the whole team.",
"Skills in <code>.claude/skills/</code> are version-controlled and shared like any other project file.",
"Both invoke the model; there is no such execution shortcut."],
e:"When a question mentions context isolation, tool restriction or required parameters, the answer is a skill. When it is simply a reusable prompt the team should share, a project slash command in <code>.claude/commands/</code> is sufficient. Both are shared when they live under the project's <code>.claude/</code> directory and personal when they live under <code>~/.claude/</code>.",
k:"Skill = prompt + operational config (<code>context: fork</code>, <code>allowed-tools</code>, <code>argument-hint</code>). Command = reusable prompt. Both share via <code>.claude/</code>."},

{i:237,d:3,t:"3.3",s:"CGC",diff:3,
q:"A codebase has distinct conventions for React components, API handlers and database migrations. Test files are co-located with source throughout. You want the right conventions applied automatically based on what is being edited.",
ask:"Which approach is correct?",
a:["<code>.claude/rules/</code> files with YAML frontmatter <code>paths</code> globs, so each rule loads only when matching files are edited",
"Put every convention in the root CLAUDE.md and rely on the model to apply the right one",
"Create skills in <code>.claude/skills/</code> for each convention set",
"Place a CLAUDE.md in every directory"],
c:0,
w:["Correct. Glob-based path rules activate on file match, which is exactly automatic convention loading &mdash; and it handles co-located tests.",
"Relies on the model inferring applicability, which is the mechanism failing today, and keeps all conventions in context always.",
"Skills are invoked manually and on demand; they do not activate automatically when a file is edited.",
"Does not work when relevant files are scattered across many directories, which is the case for co-located tests."],
e:"This mirrors a published sample question. <code>.claude/rules/</code> files can carry YAML frontmatter with <code>paths</code> globs such as <code>paths: [\"**/*.test.tsx\"]</code> or <code>paths: [\"terraform/**/*\"]</code>, and load only when matching files are in play. The benefits are twofold: correctness, because conventions cannot be misapplied to unrelated work; and efficiency, because unused rules never consume context or tokens.",
k:"<code>.claude/rules/</code> + <code>paths</code> frontmatter globs = automatic, file-scoped convention loading. Saves context and prevents misapplication."},

{i:238,d:3,t:"3.3",s:"CGC",diff:2,
q:"Which glob would scope a rule to every test file in a TypeScript React project, wherever it lives?",
ask:"Which pattern is correct?",
a:["<code>paths: [\"**/*.test.tsx\"]</code>",
"<code>paths: [\"tests/*\"]</code>",
"<code>paths: [\"*.test.tsx\"]</code>",
"<code>paths: [\"src/tests/**\"]</code>"],
c:0,
w:["Correct. <code>**/</code> matches at any depth, so co-located test files anywhere in the tree are covered.",
"Assumes tests live in a single top-level <code>tests/</code> directory, which contradicts co-location.",
"Without <code>**/</code> this matches only the root directory.",
"Restricts to one specific directory, missing co-located tests elsewhere."],
e:"The <code>**</code> wildcard matches across directory levels and is what makes path-scoped rules work for conventions that follow a file type rather than a location. This is precisely the case where the blueprint prefers path-scoped rules to directory-level CLAUDE.md: the convention applies to test files regardless of where they sit, and no directory boundary captures that.",
k:"Use <code>**/</code> for patterns that must match at any depth. That capability is why path rules beat directory CLAUDE.md for file-type conventions."},

{i:239,d:3,t:"3.3",s:"CGC",diff:2,
q:"What is the main efficiency benefit of path-scoped rules over a single large CLAUDE.md?",
ask:"Which benefit is most directly claimed?",
a:["Rules load only when matching files are being edited, saving context and tokens on every unrelated request",
"Rules are compiled into a binary format that loads faster",
"Rules are cached server-side and do not count against the context window",
"Rules allow more total instruction text than CLAUDE.md permits"],
c:0,
w:["Correct. Conditional loading is the stated benefit: context and tokens are spent only when the rule is relevant.",
"There is no compilation step.",
"No such server-side caching exemption exists.",
"There is no such quota difference; the benefit is about relevance, not capacity."],
e:"Path-scoped rules deliver two wins from one mechanism. Efficiency: a Terraform convention costs nothing on the hundreds of requests that touch no Terraform. Correctness: it cannot be misapplied to unrelated work, which is how deployment rules end up influencing test authoring. Both matter more as a repository grows and the total body of conventions increases.",
k:"Path-scoped rules load conditionally: no context cost when irrelevant, no misapplication when unrelated. Both scale with repository size."},

{i:240,d:3,t:"3.3",s:"CGC",diff:3,
q:"You have a convention that applies to all Python files except those under <code>generated/</code>.",
ask:"What is the cleanest way to express this?",
a:["Scope the rule with a <code>paths</code> glob for Python files and state the exclusion explicitly in the rule body",
"Create a second rule that cancels the first for generated files",
"Put the convention in the root CLAUDE.md with a note about the exception",
"Rename the <code>generated/</code> directory so it does not match the glob"],
c:0,
w:["Correct. Scope structurally where you can, and state the narrow exception in the rule text where the model will read it in context.",
"Contradictory rules loaded together are genuinely ambiguous and hard to reason about.",
"Loads the convention on every request, including the many that touch no Python at all.",
"Renaming source directories to work around configuration is a poor trade and breaks tooling that expects the name."],
e:"Combine mechanisms deliberately: use path globs for the broad structural scoping, and use the rule body for narrow exceptions that the glob cannot express cleanly. The one thing to avoid is two rules that contradict each other &mdash; when both are loaded, the model has no principled basis for choosing, and the behaviour becomes unpredictable. One rule with a stated exception is unambiguous.",
k:"Scope broadly with globs, state narrow exceptions in the rule body. Never write two rules that contradict each other."},

{i:241,d:3,t:"3.4",s:"CGC",diff:2,
q:"You need to restructure a monolith into microservices, which involves dozens of files and several service-boundary decisions with no obviously correct answer.",
ask:"Which approach should you use?",
a:["Planning mode &mdash; explore the codebase, understand the dependencies, and design an approach before making changes",
"Direct execution, working incrementally file by file",
"Direct execution with very detailed up-front instructions",
"Direct execution, switching to planning mode if it becomes difficult"],
c:0,
w:["Correct. Large change, multiple viable approaches, architectural decisions &mdash; the textbook planning-mode trigger.",
"Incremental execution without a plan risks expensive rework once a boundary decision turns out to be wrong at file 30.",
"Detailed instructions presuppose you already understand the dependency structure, which is what exploration establishes.",
"Reactive. By the time it is obviously difficult, decisions have been made that constrain the remaining options."],
e:"This mirrors a published sample question. Planning mode is for tasks with architectural consequence, large scope and several defensible approaches. It lets the agent explore safely before changing anything, so the approach is chosen with knowledge of the actual dependency structure. The mirror-image question &mdash; a clear stack trace in one file &mdash; is equally likely on the exam, and there the answer is direct execution.",
k:"Planning mode: large scope + multiple viable approaches + architectural consequence. Direct execution: small, well-understood, clearly diagnosed."},

{i:242,d:3,t:"3.4",s:"CGC",diff:2,
q:"A bug report includes a stack trace pointing to one function, and the fix is a single well-understood validation check.",
ask:"Which approach is appropriate?",
a:["Direct execution &mdash; make the change and verify with a test",
"Planning mode, to be safe",
"Planning mode followed by a multi-phase execution",
"An Explore subagent to map the codebase first"],
c:0,
w:["Correct. Simple, well-understood changes go straight to execution with verification.",
"Over-planning has a real cost in time and tokens and adds nothing when the diagnosis is already precise.",
"Even heavier overhead for a one-line change.",
"Exploration is unnecessary when the stack trace has already located the defect."],
e:"The exam deliberately tests both directions of this decision. Reflexively answering &ldquo;planning mode&rdquo; is a trap, because unnecessary planning wastes time and context. The rule: match the process to the uncertainty. High uncertainty and high consequence earn planning; a precise diagnosis with an obvious fix earns direct execution plus a regression test to lock the behaviour in.",
k:"Match process to uncertainty. Precise diagnosis + obvious fix &rarr; direct execution. Do not over-plan."},

{i:243,d:3,t:"3.4",s:"CGC",diff:3,
q:"A multi-phase task requires substantial codebase discovery, and previous attempts exhausted the context window before implementation began.",
ask:"Which combination is most effective?",
a:["Use an Explore subagent to isolate the verbose discovery, then plan from its summary, then execute in phases",
"Increase the context window and repeat the previous approach",
"Skip discovery and start implementing, exploring only when blocked",
"Run discovery and implementation in the same session but compact frequently"],
c:0,
w:["Correct. Isolate the verbose phase, plan from a compact summary, then execute &mdash; each phase gets clean headroom.",
"A larger window postpones the same failure and does nothing about the wasteful access pattern.",
"Implementing without understanding produces rework, and the exploration still has to happen eventually.",
"Frequent compaction is lossy about exactly the file paths and specifics that implementation needs."],
e:"The blueprint pairs these ideas: the Explore subagent isolates verbose discovery output, and planning mode enables safe exploration before changes. Combining them gives the strongest workflow for large tasks &mdash; delegate discovery so its bulk never enters the main context, plan from the distilled summary, write the plan to a file so it survives, then execute phase by phase against that plan.",
k:"Large tasks: delegate discovery to a subagent &rarr; plan from the summary &rarr; write the plan to a file &rarr; execute in phases."},

{i:244,d:3,t:"3.4",s:"CGC",diff:2,
q:"What is the primary benefit of planning mode beyond producing a plan document?",
ask:"Which benefit is most significant?",
a:["It allows exploration of the codebase without making changes, so understanding is built before anything is committed",
"It runs the model at a lower temperature for more consistent output",
"It automatically creates a git branch for the work",
"It disables all tool use until the plan is approved"],
c:0,
w:["Correct. Safe, read-only exploration before modification is the core benefit.",
"Planning mode is not a temperature setting.",
"Branch management is a separate concern that planning mode does not handle.",
"It does not disable all tools &mdash; exploration requires reading and searching."],
e:"Planning mode changes the risk profile of investigation: the agent can read widely, follow dependencies and test hypotheses about the structure without any possibility of leaving the codebase in a half-changed state. That matters most precisely when the task is large and the right approach is unclear &mdash; the situation where an exploratory edit is most likely to be wrong and most expensive to unwind.",
k:"Planning mode enables safe, read-only exploration before commitment. The plan document is the artefact; the risk reduction is the point."},

{i:245,d:3,t:"3.4",s:"DPT",diff:2,
q:"You are asked to add a single well-specified field to a form component, in a codebase you know well.",
ask:"Which is appropriate?",
a:["Direct execution",
"Planning mode",
"An Explore subagent followed by planning mode",
"A multi-agent workflow with separate implementation and review agents"],
c:0,
w:["Correct. Small, well-specified change in familiar code &mdash; execute and verify.",
"Unnecessary overhead for a task with no uncertainty.",
"Exploration is pointless in a codebase you already know well.",
"Elaborate machinery for a single field addition."],
e:"Two variables drive this decision: how well the change is specified, and how well the codebase is understood. Both high means direct execution. Poorly specified or unfamiliar codebase pushes toward exploration and planning. Architectural consequence pushes toward planning regardless. The exam includes several deliberately small tasks to check that you do not reflexively reach for the heaviest process.",
k:"Decide from two variables: how well specified the change is, and how well you know the code. Both high &rarr; direct execution."},

{i:246,d:3,t:"3.5",s:"CGC",diff:2,
q:"You need Claude to transform data in a specific way, and your prose description keeps producing subtly wrong output.",
ask:"What is the most effective way to communicate the requirement?",
a:["Provide two or three concrete input/output examples showing exactly the transformation you want",
"Rewrite the description with more precise adjectives",
"Provide a formal grammar for the output format",
"Break the description into numbered steps"],
c:0,
w:["Correct. Concrete input/output pairs are the blueprint's stated most effective way to communicate expectations.",
"More adjectives is more prose, which is what is already failing.",
"A formal grammar constrains syntax but not the semantic transformation, and it is heavy to write and read.",
"Better than a paragraph, but still describes the transformation rather than demonstrating it."],
e:"The blueprint states that concrete input/output examples are the most effective way to communicate expectations, and recommends two to three of them. Examples resolve ambiguities you did not know your description had &mdash; edge cases, whitespace handling, how to treat missing values &mdash; because they show rather than tell. Choose examples that span the interesting variation rather than three near-identical happy paths.",
k:"Two to three concrete input/output examples beat any prose description. Choose examples that span the interesting variation."},

{i:247,d:3,t:"3.5",s:"CGC",diff:3,
q:"You are about to ask Claude to implement a caching layer. You suspect there are design considerations you have not thought through, such as invalidation and failure behaviour.",
ask:"Which technique surfaces those considerations?",
a:["The interview pattern &mdash; ask Claude to question you about the requirements before implementing",
"Write a very long specification covering everything you can think of",
"Implement it and review afterwards",
"Ask for three alternative implementations and pick one"],
c:0,
w:["Correct. The interview pattern is the blueprint's named technique for surfacing non-obvious design considerations.",
"A long specification can only cover what you already thought of, which is the stated problem.",
"Discovering an invalidation flaw after implementation is far more expensive than discovering it before.",
"Three implementations of an under-specified requirement gives you three variants of the same gap."],
e:"The interview pattern inverts the usual direction: instead of you specifying everything, Claude asks questions to surface what is unspecified. The blueprint names cache invalidation and failure modes as exactly the kind of considerations it uncovers. It is most valuable precisely when you suspect gaps but cannot name them &mdash; which, by definition, is when a longer specification cannot help.",
k:"Interview pattern: have Claude question you before implementing. It surfaces the considerations a longer spec cannot, because you did not know to include them."},

{i:248,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to iterate toward a correct implementation of a well-understood function with clear edge cases.",
ask:"Which approach works best?",
a:["Test-driven iteration &mdash; write the test set first, including edge cases, then iterate until all tests pass",
"Implement first, then write tests to match the implementation",
"Describe the edge cases in prose and ask for a careful implementation",
"Implement it several times and pick the version that reads best"],
c:0,
w:["Correct. Tests first give an objective, executable definition of done that iteration can drive against.",
"Tests written to match an implementation validate what was built, not what was required, and bake in any defect.",
"Prose descriptions of edge cases are not checkable, so nothing verifies that they were handled.",
"Readability is a poor proxy for correctness on edge cases."],
e:"The blueprint names test-driven iteration and recommends building the test set &mdash; expected behaviour, edge cases and performance requirements &mdash; <em>before</em> implementation. This is the goal-driven pattern applied to code: convert &ldquo;implement this&rdquo; into &ldquo;make these tests pass&rdquo;. The iteration loop then has a hard signal rather than a subjective judgement, and the edge cases you cared about are permanently guarded.",
k:"Write the test set first &mdash; behaviour, edge cases, performance. Iterate until green. Tests written afterwards validate the bug too."},

{i:249,d:3,t:"3.5",s:"CGC",diff:3,
q:"You have found six issues in generated code. Three are interdependent (they all stem from one wrong assumption about the data model), and three are unrelated one-line fixes.",
ask:"How should you deliver the feedback?",
a:["Give the three interdependent issues together in one message so they can be addressed coherently, and the independent ones separately",
"Give all six in a single message to minimise round trips",
"Give all six one at a time in sequence",
"Give only the most severe issue and re-review afterwards"],
c:0,
w:["Correct. The blueprint's rule: interdependent issues go together, independent issues go separately.",
"Six issues at once dilutes attention, and the interdependence of three of them gets lost in the list.",
"Fixing interdependent issues one at a time causes rework, since fixing one changes the others.",
"Artificially slow and discards five findings you already have."],
e:"The blueprint distinguishes when to provide all issues in one message (interdependent) from when to provide them sequentially (independent). The reasoning is about coherence: three symptoms of one wrong assumption should be fixed by revisiting the assumption once, which requires seeing them together. Unrelated one-liners each get cleaner attention on their own and do not interact.",
k:"Interdependent issues &rarr; one message, fixed together. Independent issues &rarr; separately, for focused attention."},

{i:250,d:3,t:"3.5",s:"CGC",diff:2,
q:"An agent's output is close to what you want but misses several edge cases you care about.",
ask:"Which feedback is most effective?",
a:["Concrete test cases with sample inputs and the expected output for each edge case",
"A note saying the edge cases need more attention",
"A request to be more thorough about edge cases in general",
"A longer description of what an edge case means in this domain"],
c:0,
w:["Correct. The blueprint prescribes concrete test cases with sample inputs and expected outputs for edge cases.",
"Names a deficiency without specifying which cases or what correct behaviour looks like.",
"The canonical vague instruction &mdash; consistently contrasted with explicit criteria throughout this exam.",
"Definitions are abstract; the model needs the specific cases and the specific expected outputs."],
e:"The pattern repeats across Domains 3 and 4: replace vague quality requests with concrete, checkable artefacts. For edge cases that means input/output pairs &mdash; empty list, single element, duplicate keys, null in an optional field &mdash; each with the expected result. These double as regression tests, so the fix is verified automatically and stays fixed.",
k:"Edge-case feedback = concrete input/expected-output pairs. They specify the requirement and become the regression tests."},

{i:251,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI pipeline runs <code>claude \"Analyze this pull request for security issues\"</code> and the job hangs until it times out.",
ask:"What is the correct fix?",
a:["Add the <code>-p</code> (or <code>--print</code>) flag to run non-interactively",
"Increase the CI job timeout",
"Pipe <code>yes</code> into the command to answer prompts automatically",
"Run the command in a background process and poll for output"],
c:0,
w:["Correct. <code>-p</code> / <code>--print</code> is the non-interactive mode intended for automated pipelines.",
"A longer timeout means a longer hang; it never completes.",
"A hack that answers unknown prompts blindly and may approve dangerous operations.",
"Backgrounding a hung process does not un-hang it."],
e:"This mirrors a published sample question and is a direct recall item. Without <code>-p</code>, Claude Code runs interactively and waits for input that never comes in CI. Pair it with <code>--output-format json</code> when the pipeline needs to parse the result, and with <code>--json-schema</code> to constrain that output to a shape your tooling expects &mdash; for example to generate inline PR comments.",
k:"CI: <code>claude -p</code> (or <code>--print</code>) for non-interactive runs. Add <code>--output-format json</code> and <code>--json-schema</code> for machine-readable output."},

{i:252,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI job needs Claude Code's review findings as structured data so it can post them as inline pull-request comments.",
ask:"Which flags should you use?",
a:["<code>--output-format json</code> together with <code>--json-schema</code> to constrain the shape",
"<code>--verbose</code> and parse the log output",
"<code>--markdown</code> and convert the result",
"No flags &mdash; ask for JSON in the prompt"],
c:0,
w:["Correct. These are the documented flags for structured output in automated pipelines.",
"Parsing free-form logs is brittle and breaks whenever the output format changes.",
"Markdown still requires parsing prose to recover the structured fields.",
"A prompt request for JSON is probabilistic; the schema flag makes the shape a guarantee."],
e:"The blueprint names <code>--output-format json</code> and <code>--json-schema</code> together for structured CI output. The pairing matters: <code>--output-format json</code> gets you JSON, and <code>--json-schema</code> constrains it to <em>your</em> shape so downstream tooling can rely on the fields being present. This is the CLI analogue of using <code>tool_use</code> with a JSON Schema in the API &mdash; structural guarantee rather than formatting request.",
k:"<code>--output-format json</code> + <code>--json-schema</code> = guaranteed machine-readable CI output. Never parse prose."},

{i:253,d:3,t:"3.6",s:"CCI",diff:3,
q:"Your CI bot re-reviews a pull request after each new commit and repeats every finding from previous runs, including issues the developer has already fixed or explicitly dismissed.",
ask:"What should change?",
a:["Include prior review results in the run's context and instruct the bot to report only new or still-unfixed issues",
"Only review the most recent commit's diff in isolation",
"Reduce the review criteria so fewer issues are found overall",
"Review only once, on the first push"],
c:0,
w:["Correct. The blueprint prescribes including prior review results so re-runs report only new or unfixed issues.",
"Reviewing only the latest commit misses issues introduced earlier in the branch and still present.",
"Weakening criteria reduces noise by reducing coverage &mdash; the wrong trade.",
"Skipping later reviews means defects introduced after the first push are never caught."],
e:"Repetition is the fastest way to destroy trust in a review bot: developers stop reading it. The fix is stateful review &mdash; pass the previous run's findings as input, and have the bot diff against them so it reports what is new, what remains unfixed, and stays silent about what has been resolved or dismissed. Note that this is context passed <em>explicitly</em> as input, not implicit session carryover, which keeps CI runs reproducible.",
k:"Re-reviews must be stateful: pass prior findings as explicit input, report only new or unfixed issues. Repetition destroys trust."},

{i:254,d:3,t:"3.6",s:"CCI",diff:3,
q:"The same Claude Code session that generated a module is then asked to review it. It reports the code as clean; an independent reviewer later finds several genuine defects.",
ask:"What is the correct architectural change?",
a:["Run review in a separate, independent invocation that sees only the diff and the standards, not the generation context",
"Ask the generating session to review more critically",
"Increase the review prompt's detail while keeping the same session",
"Run the review twice in the same session and compare"],
c:0,
w:["Correct. Session context isolation is the stated remedy: an instance without the generation context finds subtler issues.",
"The generating instance retains the reasoning that produced the code and is unlikely to challenge its own decisions.",
"A better prompt cannot dislodge the anchoring created by having just argued for these choices.",
"Two passes in the same contaminated context reproduce the same blind spots twice."],
e:"The blueprint states it plainly: the session that generated code is less effective at reviewing it than an independent instance. In CI this means review runs as its own invocation with its own context, seeing the diff and the review standards from CLAUDE.md but not the conversation that produced the code. Combine with multi-pass review &mdash; per-file passes plus a cross-file integration pass &mdash; for the strongest coverage.",
k:"Never review in the generating session. Independent invocation, sees only the diff and the standards."},

{i:255,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI test-generation runs produce tests that duplicate existing coverage and use a different style from the rest of the suite.",
ask:"Which two changes address this?",
a:["Include existing test files in the run's context, and document testing standards and available fixtures in CLAUDE.md",
"Increase the number of tests requested per run",
"Run test generation on a larger model",
"Generate tests only for files with no existing tests"],
c:0,
w:["Correct. The blueprint names both: include existing tests to avoid duplication and keep style consistent, and document standards and fixtures in CLAUDE.md.",
"More tests of the same kind produces more duplication.",
"Model capability does not tell it what already exists or which fixtures are available.",
"A crude filter that misses files with partial coverage &mdash; usually where the gaps actually are."],
e:"Both defects stem from missing context. Duplication happens because the generator cannot see what is already covered; style divergence happens because the conventions and available fixtures were never stated. The blueprint prescribes exactly these two remedies, and they are complementary &mdash; CLAUDE.md carries the durable standards, and the existing test files carry the current state.",
k:"Test generation needs both: existing tests in context (avoid duplication, match style) and standards + fixtures documented in CLAUDE.md."},

{i:256,d:3,t:"3.6",s:"CCI",diff:2,
q:"Why does CLAUDE.md matter for a CI-triggered Claude Code run, given that no developer is present to read it?",
ask:"Which is the best reason?",
a:["It supplies the project context &mdash; testing standards, review criteria, conventions &mdash; that the run needs to produce relevant output",
"It is required for the CLI to start in non-interactive mode",
"It configures the CI runner's environment variables",
"It determines which model the CI run uses"],
c:0,
w:["Correct. CLAUDE.md is the durable project context that makes CI output relevant rather than generic.",
"<code>-p</code> controls non-interactive mode; CLAUDE.md is not required to start.",
"Environment configuration belongs to the CI system, not CLAUDE.md.",
"Model selection is a CLI or settings concern."],
e:"CLAUDE.md is read by the agent, not by developers, which is exactly why it matters in CI. A review run guided by documented review criteria produces findings aligned to what the team cares about; a test-generation run guided by documented standards and fixtures produces tests that fit the suite. Without it, CI output is generic and the false-positive rate rises &mdash; which is what erodes developer trust.",
k:"CLAUDE.md is context for the agent, not documentation for humans. In CI it is what makes output project-relevant rather than generic."},

{i:257,d:3,t:"3.6",s:"CCI",diff:3,
q:"Developers are ignoring your review bot because roughly 40% of its comments in one category &mdash; &ldquo;possible performance issue&rdquo; &mdash; are false positives, even though its security findings are highly accurate.",
ask:"What is the most effective immediate action?",
a:["Temporarily disable the performance category while you develop explicit criteria for it, keeping the accurate categories running",
"Reduce the overall number of comments the bot posts per run",
"Add a disclaimer to every comment saying findings may be inaccurate",
"Ask developers to ignore performance comments specifically"],
c:0,
w:["Correct. The blueprint names temporarily disabling categories with high false-positive rates, which protects trust in the accurate ones.",
"Cutting volume indiscriminately removes accurate security findings alongside the noisy ones.",
"A blanket disclaimer teaches developers to distrust everything, including the accurate categories.",
"Relies on every developer remembering a manual exception, and the noise still floods the review."],
e:"The blueprint makes a specific point here: a high false-positive rate in <em>some</em> categories undermines trust in the accurate categories too. Because trust is not category-scoped in practice, the right move is to remove the noisy category from production while you develop explicit criteria for it &mdash; concrete definitions with code examples of what does and does not qualify &mdash; then reintroduce it once measured.",
k:"False positives in one category poison trust in all of them. Disable the noisy category, build explicit criteria, then reintroduce."},

{i:258,d:3,t:"3.6",s:"CCI",diff:2,
q:"Which statement about running Claude Code in CI is correct?",
ask:"Which is true?",
a:["Non-interactive mode is required, structured output should be requested explicitly, and each run should be isolated from previous runs unless context is passed deliberately",
"CI runs automatically inherit context from the developer's local session",
"CI runs cannot use CLAUDE.md or MCP servers",
"CI runs must use a different CLI binary from local development"],
c:0,
w:["Correct. These are the three defining characteristics of a well-configured CI integration.",
"There is no such inheritance, and it would make CI runs irreproducible if there were.",
"Both are available and both are recommended &mdash; CLAUDE.md for standards, MCP for integrations.",
"It is the same CLI, invoked with different flags."],
e:"A summary item. CI integration rests on three properties: <strong>non-interactive</strong> (<code>-p</code>) so the job never blocks; <strong>machine-readable output</strong> (<code>--output-format json</code>, <code>--json-schema</code>) so results feed the pipeline; and <strong>isolation by default</strong> so runs are reproducible, with prior findings passed explicitly when continuity is wanted.",
k:"CI = non-interactive + machine-readable output + isolated by default, with context passed explicitly when needed."},

{i:259,d:3,t:"3.6",s:"CCI",diff:3,
q:"You want your CI review to have low false positives on comment-accuracy findings. The current instruction is: <em>check that comments are accurate.</em>",
ask:"How should it be rewritten?",
a:["Flag a comment only when it directly contradicts what the code does &mdash; not when it is merely incomplete, outdated in tone, or stylistically poor",
"Check comment accuracy carefully and be conservative",
"Check that every function has a comment",
"Ignore comments entirely to eliminate the false positives"],
c:0,
w:["Correct. A specific categorical criterion &mdash; contradiction, not incompleteness &mdash; is exactly what makes the finding checkable.",
"&ldquo;Be conservative&rdquo; is explicitly identified as working worse than concrete categorical criteria.",
"Changes the check to something else entirely and would generate a flood of low-value comments.",
"Eliminates false positives by eliminating the check, losing genuine findings."],
e:"The blueprint uses this exact contrast: &ldquo;flag comments only when they contradict code&rdquo; versus &ldquo;check comment accuracy&rdquo;, and states that generic guidance like &ldquo;be more conservative&rdquo; works worse than concrete categorical criteria. The lesson generalises to every review category &mdash; define what qualifies, define what does not, and give code examples for each severity level.",
k:"Replace vague criteria with categorical ones: state what qualifies, what does not, with examples. &ldquo;Be conservative&rdquo; is not a criterion."},

{i:260,d:3,t:"3.6",s:"GEN",diff:1,
q:"Which flag runs Claude Code non-interactively for use in an automated pipeline?",
ask:"Which is correct?",
a:["<code>-p</code> or <code>--print</code>",
"<code>--ci</code>",
"<code>--headless</code>",
"<code>--batch</code>"],
c:0,
w:["Correct. <code>-p</code> / <code>--print</code> is the non-interactive mode.",
"Not a Claude Code flag.",
"Plausible-sounding but not the flag.",
"Not the flag; batching is an API concept, not a CLI mode here."],
e:"A direct recall item that has appeared in published sample material. Memorise the CI trio: <code>-p</code> / <code>--print</code> for non-interactive execution, <code>--output-format json</code> for machine-readable results, and <code>--json-schema</code> to constrain that output to your shape. Also remember <code>--resume &lt;session&gt;</code> for continuing named sessions, which appears in session-management questions.",
k:"CI flags: <code>-p</code>/<code>--print</code>, <code>--output-format json</code>, <code>--json-schema</code>. Session: <code>--resume &lt;name&gt;</code>."},

{i:261,d:3,t:"3.1",s:"GEN",diff:2,
q:"Which syntax imports an external file into a CLAUDE.md?",
ask:"Which is correct?",
a:["<code>@./standards/coding-style.md</code>",
"<code>!include ./standards/coding-style.md</code>",
"<code>&lt;import src=\"./standards/coding-style.md\"&gt;</code>",
"<code>{{ ./standards/coding-style.md }}</code>"],
c:0,
w:["Correct. The <code>@path</code> syntax references external files from CLAUDE.md.",
"Not the syntax.",
"Not the syntax.",
"Not the syntax."],
e:"<code>@path</code> is what makes CLAUDE.md modular: shared standards live in one file and each package or directory composes the subset it needs. The blueprint gives <code>@./standards/coding-style.md</code> as the example and pairs it with the monorepo use case &mdash; each package's CLAUDE.md importing the shared style plus its own testing standards.",
k:"<code>@path</code> imports a file into CLAUDE.md. Use it to share standards across packages without duplicating them."},

{i:262,d:3,t:"3.2",s:"GEN",diff:2,
q:"Where do project-level skills live?",
ask:"Which path is correct?",
a:["<code>.claude/skills/</code> with a <code>SKILL.md</code> per skill",
"<code>.claude/commands/</code>",
"<code>~/.claude/skills/</code>",
"<code>.claude/rules/</code>"],
c:0,
w:["Correct. Skills live in <code>.claude/skills/</code>, each defined by a <code>SKILL.md</code> with YAML frontmatter.",
"That is where slash commands live.",
"That is the user-level skills location, for personal variants.",
"That directory holds topic-focused rule files."],
e:"Keep the <code>.claude/</code> subdirectories distinct, since the exam swaps them as distractors. <code>commands/</code> &mdash; slash commands. <code>skills/</code> &mdash; skills with <code>SKILL.md</code> frontmatter (<code>context: fork</code>, <code>allowed-tools</code>, <code>argument-hint</code>). <code>rules/</code> &mdash; topic-focused rule files, optionally scoped with <code>paths</code> frontmatter. Anything under the project's <code>.claude/</code> is shared through version control; the <code>~/.claude/</code> equivalents are personal.",
k:"<code>.claude/</code>: <code>commands/</code> = slash commands &middot; <code>skills/</code> = SKILL.md &middot; <code>rules/</code> = scoped rules. Project = shared, <code>~/.claude/</code> = personal."},

{i:263,d:3,t:"3.4",s:"GEN",diff:2,
q:"Which situation most clearly calls for planning mode rather than direct execution?",
ask:"Which is it?",
a:["A refactor spanning many files where several different architectural approaches are defensible",
"Renaming a variable in a single function",
"Adding a log statement to an existing handler",
"Fixing a typo in a documentation file"],
c:0,
w:["Correct. Scope plus genuine architectural choice is the planning-mode trigger.",
"A trivial, well-understood change &mdash; direct execution.",
"Also trivial and localised.",
"Trivial and carries no architectural consequence."],
e:"Planning mode earns its overhead when the cost of choosing the wrong approach is high and the right approach is not obvious. Three signals point to it: large scope, multiple defensible approaches, and architectural consequence. Direct execution is correct for everything else, and the exam includes trivial cases specifically to check that you do not over-apply the heavier process.",
k:"Planning-mode triggers: large scope, multiple defensible approaches, architectural consequence. Anything else &rarr; direct execution."},

{i:264,d:3,t:"3.3",s:"GEN",diff:2,
q:"What does the <code>paths</code> key in a <code>.claude/rules/</code> file's YAML frontmatter do?",
ask:"Which is correct?",
a:["Restricts the rule so it loads only when files matching those glob patterns are being worked on",
"Specifies where the rule file itself should be stored",
"Lists the files the rule is permitted to modify",
"Defines the search path for imported standards"],
c:0,
w:["Correct. <code>paths</code> globs scope the rule's activation to matching files.",
"The rule file's location is <code>.claude/rules/</code>; <code>paths</code> is about activation.",
"Rules are instructions, not permissions; they do not gate file modification.",
"That is what <code>@path</code> imports do, and they live in CLAUDE.md."],
e:"<code>paths</code> makes convention loading conditional, which delivers both efficiency (irrelevant rules cost nothing) and correctness (rules cannot be misapplied to unrelated work). It is the mechanism the blueprint prefers over directory-level CLAUDE.md whenever a convention follows a file type across many directories &mdash; co-located test files being the canonical example.",
k:"<code>paths</code> frontmatter scopes when a rule activates. Efficiency plus correctness, and it handles patterns that cross directory boundaries."},

{i:265,d:3,t:"3.5",s:"CGC",diff:2,
q:"You are iterating on a generated implementation and want each round of feedback to move things forward reliably.",
ask:"Which practice matters most?",
a:["Make each round of feedback specific and verifiable &mdash; concrete cases, expected outputs, or failing tests",
"Keep each round short so the model is not overwhelmed",
"Change only one thing per round regardless of how they relate",
"Restate the entire original requirement each round"],
c:0,
w:["Correct. Specific, verifiable feedback is what makes an iteration loop converge instead of oscillate.",
"Brevity is not the property that matters; specificity is. A short vague note helps nothing.",
"Too rigid &mdash; interdependent issues should be addressed together, as the blueprint states.",
"Restating everything each round wastes tokens and buries the delta that actually needs attention."],
e:"Iterative refinement converges when each round supplies a checkable target. That means concrete input/output pairs, failing test cases, or precisely-stated criteria &mdash; not &ldquo;this still is not quite right&rdquo;. Group interdependent issues so they can be fixed coherently, keep independent ones separate for focused attention, and prefer feedback that becomes a permanent regression test.",
k:"Iteration converges on specific, verifiable feedback. Group interdependent issues; keep independent ones separate."},

{i:266,d:3,t:"3.1",s:"CGC",diff:3,
q:"Two CLAUDE.md files in the hierarchy give contradictory guidance: the root says to use <code>async/await</code>, and a directory-level file in a legacy module says to use promise chains.",
ask:"What is the best resolution?",
a:["Keep the directory-level exception but make it explicit that it is a deliberate legacy-module exception to the root rule",
"Delete the directory-level file so the root rule always applies",
"Delete the root rule so each directory decides for itself",
"Move both rules to user level so each developer chooses"],
c:0,
w:["Correct. Both levels load, so make the relationship explicit rather than leaving two bare contradictory statements.",
"Forces a legacy module to adopt a convention its code does not follow, creating inconsistency within that module.",
"Loses the default that applies correctly to the other 95% of the codebase.",
"Turns a team convention into a personal preference and guarantees inconsistency across the team."],
e:"Because CLAUDE.md levels compose rather than override, a bare contradiction leaves the model with two conflicting instructions and no basis for choosing. Phrasing the specific rule as an explicit exception &mdash; &ldquo;this legacy module predates the async migration; use promise chains here, overriding the project default&rdquo; &mdash; resolves it unambiguously and documents the reason for the next person who reads it.",
k:"Levels compose, so contradictions are ambiguous. Phrase local rules as explicit, reasoned exceptions to the broader rule."},

{i:267,d:3,t:"3.2",s:"CGC",diff:2,
q:"A team wants a reusable prompt for generating conventional-commit messages, shared with everyone, with no special tool restrictions or context isolation.",
ask:"What is the simplest correct implementation?",
a:["A project slash command in <code>.claude/commands/</code>",
"A skill in <code>.claude/skills/</code> with <code>context: fork</code>",
"An entry in the root CLAUDE.md",
"A user-level command in <code>~/.claude/commands/</code>"],
c:0,
w:["Correct. A reusable shared prompt with no operational requirements is exactly a project slash command.",
"A skill would work but adds frontmatter machinery for isolation and capability that this case does not need.",
"CLAUDE.md carries always-on conventions, not on-demand invocable prompts.",
"User level would not share it with the team."],
e:"Pick the lightest mechanism that meets the requirement. A shared reusable prompt with no isolation or capability needs is a project slash command. Escalate to a skill only when you need <code>context: fork</code>, <code>allowed-tools</code> or <code>argument-hint</code>. Use CLAUDE.md for conventions that should always apply, not for things you invoke deliberately.",
k:"Lightest mechanism that fits: CLAUDE.md for always-on conventions, commands for shared prompts, skills when you need operational config."},

{i:268,d:3,t:"3.6",s:"CCI",diff:2,
q:"A CI review run needs access to your internal design-docs system to check that changes match approved designs.",
ask:"How should this be configured?",
a:["Configure an MCP server for the design-docs system in the project's <code>.mcp.json</code>, with the token supplied via an environment variable in CI",
"Copy the relevant design documents into CLAUDE.md",
"Have the CI job download the documents and pass them in the prompt",
"Give the CI job a personal access token in <code>~/.claude.json</code> on the runner"],
c:0,
w:["Correct. Project-scoped MCP configuration with environment-variable secrets works identically for developers and CI.",
"Design documents change constantly and would bloat every request; CLAUDE.md is for durable conventions.",
"Workable but reinvents the integration, and it fetches everything rather than what the review needs.",
"User-scoped configuration on an ephemeral runner is fragile, and a personal token in CI is poor practice."],
e:"The strength of project-scoped MCP configuration is that one definition serves every consumer: a developer resolves <code>${DESIGN_DOCS_TOKEN}</code> from their environment, and the CI runner resolves it from its secret store. Nothing is duplicated and no credential enters the repository. Use a CI-specific service credential rather than a person's token so access is attributable and revocable.",
k:"One <code>.mcp.json</code> serves developers and CI. Resolve secrets from the environment; use a service credential in CI, not a personal token."},

{i:269,d:3,t:"3.4",s:"CGC",diff:3,
q:"You used planning mode and produced a good plan. The task will take several hours across multiple sessions.",
ask:"What should you do with the plan?",
a:["Write it to a file in the repository so each phase can start from the plan rather than from the previous session's full history",
"Keep it in the session context and resume the session for each phase",
"Paste it into every prompt as a reminder",
"Rely on Claude to remember the plan across sessions"],
c:0,
w:["Correct. A plan file survives compaction, crashes and session boundaries, and lets each phase start clean.",
"Resuming carries the accumulated history of previous phases, which is exactly the context pressure you want to avoid.",
"Pasting the whole plan every turn is wasteful and buries the current phase in the full document.",
"There is no cross-session memory to rely on."],
e:"Writing the plan to a file is what makes phased execution work: each phase begins from a compact, accurate plan rather than from the sprawling history of the previous phase. It also enables partial rollback &mdash; if a phase-one decision proves wrong, you can identify which downstream phases depended on it and re-execute only those. And because it is in the repository, it is reviewable and survives a machine change.",
k:"Write the plan to a file. Each phase starts from the plan, not from the last session's history &mdash; and rollback becomes possible."},

{i:270,d:3,t:"3.3",s:"CGC",diff:2,
q:"You add a <code>.claude/rules/terraform.md</code> file with <code>paths: [\"terraform/**/*\"]</code>. A developer editing a Python file reports that the Terraform conventions are not being applied.",
ask:"What is the correct interpretation?",
a:["This is the intended behaviour &mdash; the rule is scoped to Terraform files and correctly does not load for Python work",
"The glob pattern is wrong and should be <code>terraform/*</code>",
"Path frontmatter is not supported and the rule should be moved to CLAUDE.md",
"The rule file needs to be registered in <code>.claude/settings.json</code>"],
c:0,
w:["Correct. Conditional loading is the feature: irrelevant rules do not load, saving context and preventing misapplication.",
"<code>terraform/**/*</code> is correct for matching at any depth; <code>terraform/*</code> would match only the top level.",
"Path frontmatter is supported and is the documented mechanism.",
"Rules are discovered from the directory; no registration step exists."],
e:"A question testing whether you understand the mechanism well enough to recognise correct behaviour as correct. Path-scoped rules load only for matching files, by design. The exam includes items like this because a common misreading is to treat conditional loading as a bug &mdash; when in fact it is the entire benefit over a monolithic always-loaded CLAUDE.md.",
k:"Path-scoped rules not loading for non-matching files is correct behaviour, not a bug. That conditionality is the whole point."}
);
