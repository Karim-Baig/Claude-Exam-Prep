QB.push(
{i:911,d:4,t:"4.1",s:"SDE",diff:2,
q:"Your prompt describes a field as &ldquo;the total&rdquo; and documents contain subtotal, tax total and grand total.",
ask:"What is the fix?",
a:["Name the specific total wanted, describe how it is identified, and add separate fields for the others if they are also needed",
"Instruct the model to pick the largest total",
"Instruct the model to pick the last total on the page",
"Add an example showing one document"],
c:0,
w:["Correct. Name the field precisely and capture the siblings separately if they matter.",
"A positional or magnitude heuristic breaks on documents where it does not hold.",
"Same objection &mdash; a layout heuristic is not a definition.",
"One example does not disambiguate the general rule and may teach a layout habit."],
e:"Ambiguity between sibling values is one of the most common extraction defects, and it is a specification problem. Name the field (<code>grand_total</code>), state how it is identified in these documents, and extract the siblings into their own fields &mdash; which additionally lets your validation reconcile them, catching exactly the case where the model grabbed the wrong one.",
k:"Name sibling values precisely and extract them separately. Separate fields let validation reconcile and catch mix-ups."},

{i:912,d:4,t:"4.1",s:"CCI",diff:2,
q:"Your review criteria are precise but developers say the bot misses issues they care about.",
ask:"What does this indicate?",
a:["The criteria are precise but incomplete &mdash; precision and coverage are different properties",
"The criteria are not precise enough",
"The model is not capable enough",
"The false-positive rate is too low"],
c:0,
w:["Correct. A precise criterion set can still omit whole categories developers value.",
"Precision is already good; the gap is what is covered.",
"Capability does not compensate for a category that was never requested.",
"A low false-positive rate is desirable, not a cause of misses."],
e:"Precision and recall are independent, and criteria affect both. Sharp definitions raise precision within the categories you defined; they say nothing about categories you never defined. Ask developers which findings they wanted and did not get, define those categories with the same rigour, and measure both dimensions &mdash; adding a category will initially cost precision until its criteria are tightened.",
k:"Precision and coverage are independent. Sharp criteria within too few categories still misses what developers want."},

{i:913,d:4,t:"4.2",s:"SDE",diff:2,
q:"You have limited context and must choose which few-shot examples to keep.",
ask:"Which should you keep?",
a:["The ones covering cases the model currently gets wrong, since examples of already-correct behaviour spend context for nothing",
"The shortest ones",
"The most representative common cases",
"The most recently added ones"],
c:0,
w:["Correct. Spend the budget on the failure region.",
"Length is not a value measure.",
"Common cases are usually already handled; that is why they are common and easy.",
"Recency is arbitrary."],
e:"Few-shot budget should go where behaviour is wrong. Examples of cases the model already handles cost tokens on every request and change nothing. Identify the failure region from your validation data &mdash; which fields, which document types, which boundary cases &mdash; and target examples there, keeping enough variation that they teach the rule rather than one region.",
k:"Spend few-shot budget on the failure region. Examples of already-correct behaviour are pure cost."},

{i:914,d:4,t:"4.2",s:"CSA",diff:2,
q:"You want to teach an agent a rule that has many exceptions.",
ask:"What is the effective structure?",
a:["State the general rule, then enumerate the exceptions explicitly, and give examples at the boundary between rule and exception",
"State only the general rule and let the model infer exceptions",
"State only the exceptions",
"Give many examples without stating the rule"],
c:0,
w:["Correct. Rule plus enumerated exceptions plus boundary examples covers the space compactly and generalises.",
"Inferred exceptions are exactly where behaviour becomes inconsistent.",
"Exceptions without the rule leave the default case undefined.",
"Examples alone generalise less reliably than a stated rule with examples."],
e:"Criteria and examples do complementary work, which matters most for rule-plus-exception structures. The stated rule and its enumerated exceptions cover the space compactly and extend to cases you did not list. The examples go at the boundary &mdash; the near-miss pairs where rule and exception are hardest to tell apart &mdash; which is where errors concentrate.",
k:"State the rule, enumerate the exceptions, and put examples at the boundary between them."},

{i:915,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want the extraction to record which document a value came from when several are processed together.",
ask:"How should the schema handle this?",
a:["A source identifier field on each record, populated from the input rather than inferred by the model",
"Rely on the order of results matching the order of inputs",
"Include the document text in each record",
"Process one document at a time only"],
c:0,
w:["Correct. The identifier comes from your pipeline, which knows it authoritatively.",
"Positional correlation is fragile and unnecessary when an identifier is available.",
"Including full text is enormously wasteful for an identity question.",
"Sometimes appropriate, but it does not solve attribution when batching is desirable."],
e:"Attribution should come from your pipeline, not from the model. You know which document you sent; injecting that identifier &mdash; the same way you set <code>custom_id</code> on a batch request &mdash; is authoritative and free. Asking the model to identify the source is unnecessary work and an opportunity for error, particularly when several documents share a template.",
k:"Inject source identifiers from the pipeline, which knows them authoritatively. Never ask the model to attribute."},

{i:916,d:4,t:"4.3",s:"SDE",diff:3,
q:"Your schema uses deeply nested objects five levels down.",
ask:"What should you consider?",
a:["Whether the nesting reflects genuine structure or accidental complexity, since deep nesting is harder to extract reliably and harder to validate",
"Whether the model supports nesting",
"Whether nesting increases token cost",
"Nothing; nesting depth is irrelevant"],
c:0,
w:["Correct. Question whether the depth is essential; flatter schemas extract and validate more reliably.",
"Nested objects are supported.",
"Token cost is a minor consideration next to reliability.",
"Depth affects both extraction reliability and validation clarity."],
e:"Schema shape affects extraction quality. Deep nesting asks the model to maintain more structural state while extracting, and it makes validation errors harder to localise and report specifically &mdash; which matters because retry feedback must name the failing field precisely. Where the depth reflects genuine domain structure, keep it; where it is inherited from an internal representation, flatten for the extraction schema and reshape afterwards in code.",
k:"Prefer flatter extraction schemas. Deep nesting hurts reliability and makes validation errors hard to localise."},

{i:917,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want validation errors to be usable both by the retry loop and by human reviewers.",
ask:"What should they contain?",
a:["The field, the received value, the rule violated and the expectation &mdash; which serves a corrected retry and tells a reviewer exactly what to check",
"A generic message plus an error code",
"The full document",
"A severity rating"],
c:0,
w:["Correct. The same four elements serve both consumers.",
"A code requires a lookup and helps neither directly.",
"The document is context they both already have.",
"Severity orders the queue without saying what is wrong."],
e:"Well-formed validation errors serve two audiences without duplication. The retry loop needs the field, the value and the expectation to construct a corrected attempt. The human reviewer needs exactly the same information to know where to look. Adding severity is useful for ordering the review queue, but it does not replace the substance.",
k:"Validation errors: field + received value + rule + expectation. Serves the retry loop and the reviewer alike."},

{i:918,d:4,t:"4.4",s:"SDE",diff:2,
q:"A field fails validation on 2% of documents and manual inspection shows all 2% are genuinely ambiguous in the source.",
ask:"What is the right handling?",
a:["Route them to human review, since retrying cannot resolve genuine source ambiguity",
"Increase the retry budget",
"Loosen the validation rule",
"Change the extraction prompt"],
c:0,
w:["Correct. Ambiguous sources are a routing case, not a retry case.",
"Retrying an ambiguous source produces different answers, not a correct one.",
"Loosening validation lets ambiguous values through unflagged.",
"The prompt cannot resolve ambiguity that exists in the document."],
e:"Recognising when retry cannot help is one of the judgements Domain 4 tests. Retry fixes wrong <em>processing</em> of present information. It cannot fix information that is genuinely absent, and it cannot resolve a source that does not determine the answer. Both are routing cases &mdash; and a 2% ambiguous rate is a perfectly healthy human-review load rather than a problem to engineer away.",
k:"Retry fixes wrong processing. Absent or ambiguous source data routes to review instead."},

{i:919,d:4,t:"4.5",s:"SDE",diff:2,
q:"You want to process a large corpus but also want to catch a systematic prompt problem early.",
ask:"What is the practical approach?",
a:["Run a small stratified sample first, validate it, and only then submit the full corpus",
"Submit the full corpus and monitor the first results",
"Submit in ten batches and check after each",
"Submit everything and validate at the end"],
c:0,
w:["Correct. A validated sample first is the documented practice and the cheapest protection.",
"Batch results are not available incrementally in a way that supports early abort.",
"Better than one batch, but a sample run costs far less and catches the same problems.",
"Validating at the end means the whole run is wasted if the prompt was wrong."],
e:"The blueprint prescribes iterating on the prompt with a sample before large-scale processing. Stratify the sample across document types and difficulty rather than drawing it uniformly, so a type-specific defect is visible &mdash; a uniform sample from a corpus dominated by easy documents will look fine and hide the problem you are trying to catch.",
k:"Validate a stratified sample before any large run. Uniform samples hide type-specific defects."},

{i:920,d:4,t:"4.5",s:"SDE",diff:2,
q:"Your batch job's cost is dominated by input tokens rather than output.",
ask:"What is worth examining?",
a:["Whether each request carries content it does not need &mdash; oversized tool schemas, unnecessary examples, or full documents where an extract would do",
"Whether to reduce <code>max_tokens</code>",
"Whether to use a smaller model",
"Nothing; input cost is fixed"],
c:0,
w:["Correct. Input-dominated cost means per-request payload is where the savings are.",
"<code>max_tokens</code> caps output, which is not the dominant term here.",
"A smaller model reduces quality to address a payload problem.",
"Input payload is very much under your control."],
e:"Input-dominated cost multiplies across every item, so per-request payload deserves scrutiny. Tool definitions are billed on every request &mdash; trim descriptions to what selection needs. Few-shot examples cost every item &mdash; keep the ones aimed at the failure region. And where a document is large but only one section is relevant, extracting that section first can cut the payload dramatically.",
k:"Input-dominated batch cost: trim tool schemas, prune examples to the failure region, send extracts not whole documents."},

{i:921,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want to know whether your review is finding the issues that actually reach production.",
ask:"What feedback loop gives you this?",
a:["Tracing production incidents back to the pull requests that introduced them, and checking whether the review flagged the cause",
"Counting the review's findings per month",
"Surveying developers about the review's usefulness",
"Measuring the review's runtime"],
c:0,
w:["Correct. Incident traceback is the ground truth for whether review catches what matters.",
"Finding counts measure activity, not effectiveness against real incidents.",
"Perception is useful but not a measure of what reached production.",
"Runtime is a cost metric."],
e:"Labelled test sets measure detection against known defects; incident traceback measures it against the defects that actually cost you something. Each incident traced back to its introducing change tells you whether the review saw it and did not flag it (a criteria gap), flagged it and it was dismissed (a precision or trust problem), or never saw it (a scope problem). Each has a different remedy.",
k:"Trace production incidents back to the review that missed them. Criteria gap, dismissal, or scope gap &mdash; different fixes."},

{i:922,d:4,t:"4.6",s:"CCI",diff:3,
q:"An independent reviewer flags an issue that turns out to be a deliberate, documented design decision.",
ask:"What should change?",
a:["Make the decision visible to the reviewer &mdash; in the code, a rules file or the standards it receives &mdash; so the same finding is not re-raised every run",
"Instruct the reviewer to trust the author",
"Remove the category that produced the finding",
"Accept the recurring finding as unavoidable"],
c:0,
w:["Correct. The reviewer applied the standards it had; give it the missing context.",
"Trusting the author defeats independence.",
"Removing a whole category to suppress one finding loses genuine detections.",
"Recurring known-invalid findings are noise that erodes trust."],
e:"Independent review works from the standards it is given, so a deliberate deviation it does not know about will be flagged every run. Record the decision where the reviewer will see it &mdash; a comment at the site, a path-scoped rule stating the exception, or the review standards. That is the resolution-record step that stops disagreements being re-litigated on every run.",
k:"Recurring valid-but-known findings mean the reviewer lacks the decision record. Put the exception where it will see it."},

{i:923,d:4,t:"4.1",s:"SDE",diff:2,
q:"You are writing criteria for when an extraction should be marked uncertain rather than given a value.",
ask:"What makes this workable?",
a:["Concrete conditions &mdash; the region is illegible, several candidate values are equally supported, the label is missing &mdash; rather than an instruction to flag uncertainty",
"An instruction to mark anything uncertain",
"A confidence threshold the model applies to itself",
"Marking every extraction uncertain and reviewing all"],
c:0,
w:["Correct. Name the observable conditions that constitute uncertainty.",
"An open instruction produces inconsistent application.",
"Self-applied thresholds on uncalibrated confidence are unreliable.",
"Reviewing everything abandons the automation."],
e:"&ldquo;Mark it uncertain if you are unsure&rdquo; delegates the definition of uncertainty to the model, which produces inconsistency. Naming observable conditions &mdash; illegible region, multiple equally-supported candidates, missing label, conflicting values on the page &mdash; makes it decidable. Those same conditions are the ambiguous-source criterion that routes an item to human review.",
k:"Define uncertainty by observable conditions, not by self-assessed doubt. Those conditions drive review routing."},

{i:924,d:4,t:"4.2",s:"SDE",diff:2,
q:"Your examples show correct extraction but never show the model declining to extract.",
ask:"What is the likely consequence?",
a:["The model treats extraction as always possible and produces a value even where the correct answer is absence",
"The model will refuse more often",
"The examples are too short",
"There is no consequence"],
c:0,
w:["Correct. Never demonstrating the null case implies a value is always expected.",
"The bias runs the other way.",
"Length is not the issue.",
"The consequence is fabrication on absent data."],
e:"Examples set expectations about what output is normal, including how often absence occurs. If every example produces a full record, the model infers that a full record is the expected shape &mdash; and fills fields the source does not support. Include an example where a field is genuinely absent and correctly returned as <code>null</code>, alongside a schema that permits it. Both mechanisms are needed.",
k:"Demonstrate the absent case in an example, alongside a nullable schema. Both are needed to prevent fabrication."},

{i:925,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want to prevent the model returning a value with the wrong unit &mdash; pounds where the schema means pence.",
ask:"What helps most?",
a:["Name the unit in the field name and description, and validate the plausible range in code",
"Add an example with a value in the correct unit",
"Instruct the model to use the correct unit",
"Divide by 100 downstream"],
c:0,
w:["Correct. Naming the unit removes the ambiguity, and a range check catches the error when it happens anyway.",
"One example helps but does not make the unit explicit in the contract.",
"An instruction is guidance without a check.",
"A blind conversion downstream assumes the error always occurs in one direction."],
e:"Unit errors are silent and expensive. Encode the unit in the field name (<code>amount_pence</code>) so it is unambiguous at every point the field is referenced, state it in the description, and add a plausibility range in domain validation &mdash; an invoice total of 45 pence or 4.5 million pounds is worth flagging. That is the syntax/semantics split applied to units: the name and description prevent, the range check catches.",
k:"Encode units in field names, state them in descriptions, and range-check in code. Unit errors are silent otherwise."},

{i:926,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to reduce the number of documents needing any retry at all.",
ask:"Where should you look?",
a:["The distribution of first-attempt failures by field and by document type, to find the concentrated causes",
"The total retry count",
"The retry budget",
"The model version"],
c:0,
w:["Correct. Failures concentrate, and the concentration tells you what to fix.",
"A total is a symptom without a cause.",
"Budget governs what happens after a failure, not how many occur.",
"Version matters for change tracking, not for locating the cause."],
e:"First-attempt failures are rarely uniform. Grouping by field and by document type usually reveals that a small number of combinations dominate &mdash; one field on one document type, say &mdash; and that is where a description change, a targeted example, or a schema adjustment pays. Recording the failing rule on every retry is what makes this analysis possible at all.",
k:"Group first-attempt failures by field and document type. A few combinations usually dominate; fix those."},

{i:927,d:4,t:"4.5",s:"SDE",diff:2,
q:"You are told batch processing is unsuitable because your workflow needs two model calls per document.",
ask:"Is that correct?",
a:["Not necessarily &mdash; two independent single-shot calls can each be batched; what batch cannot do is a multi-turn tool loop within one request",
"Correct; batch supports only one call per document",
"Correct; batch cannot be used for multi-step workflows at all",
"Incorrect; batch supports multi-turn tool calling"],
c:0,
w:["Correct. Restructure into single-shot stages and batch each stage.",
"You can submit a document to two separate batches, or two requests in one batch.",
"Multi-step workflows can often be restructured into batched single-shot stages.",
"Batch does not support multi-turn tool calling within a single request."],
e:"The constraint is specific: no multi-turn tool loop <em>within a single batch request</em>. A two-stage pipeline &mdash; extract, then classify the extraction &mdash; is two single-shot calls and can be run as two batch stages, with the second batch built from the first's results. Only genuinely agentic per-item workflows, where the next call depends on a tool result mid-turn, are ruled out.",
k:"Batch forbids a multi-turn tool loop per request, not multi-stage pipelines. Restructure into batched single-shot stages."},

{i:928,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want the review to be useful on a pull request that is entirely test additions.",
ask:"What should apply?",
a:["Test-specific criteria &mdash; coverage of edge cases, assertion quality, use of existing fixtures &mdash; loaded via a path-scoped rule",
"The same criteria as production code",
"No review, since tests are low risk",
"A shorter review"],
c:0,
w:["Correct. Tests have their own quality criteria, and path scoping applies them automatically.",
"Production criteria applied to tests produce irrelevant findings.",
"Poor tests give false confidence, which is a real risk.",
"Length is not the variable; relevance is."],
e:"Tests deserve their own criteria: do they cover the edge cases that matter, do assertions check behaviour rather than implementation, do they reuse existing fixtures rather than duplicating setup, do they duplicate existing coverage. A path-scoped rule on test file patterns loads exactly these when tests are in the diff &mdash; the canonical case for path scoping, since test files are co-located throughout the tree.",
k:"Tests need their own review criteria, loaded by a path-scoped rule on test file patterns."},

{i:929,d:4,t:"4.1",s:"CSA",diff:2,
q:"You want the agent to handle a category of request it has never seen before in a sensible way.",
ask:"What should the prompt provide?",
a:["A general decision framework &mdash; what is in scope, what triggers escalation, what to do when unsure &mdash; that extends beyond the enumerated cases",
"An exhaustive list of every request type",
"An instruction to use common sense",
"More few-shot examples of known cases"],
c:0,
w:["Correct. A stated framework generalises to cases you never enumerated.",
"Exhaustive enumeration is impossible and fails on the first novel case.",
"&ldquo;Common sense&rdquo; is undefined.",
"Examples cover cases; a framework covers the space between them."],
e:"This is the strongest argument for criteria over examples. Examples teach specific cases and the boundaries near them; a stated framework &mdash; in scope means X, escalate on the three triggers, when unsure about authority escalate &mdash; extends to situations nobody anticipated. Use examples to illustrate the framework at its hardest boundaries, not to substitute for it.",
k:"Frameworks generalise to novel cases; examples do not. State the decision framework, illustrate its boundaries."},

{i:930,d:4,t:"4.2",s:"CCI",diff:2,
q:"You want few-shot examples that stay valid as the codebase evolves.",
ask:"What makes an example durable?",
a:["Illustrating a principle rather than depending on specific code that will change",
"Being drawn from the most recent commit",
"Being very short",
"Being drawn from the oldest stable code"],
c:0,
w:["Correct. Principle-based examples survive; examples tied to particular code go stale with it.",
"Recency does not confer durability.",
"Length is unrelated.",
"Old code goes stale too, and may model outdated conventions."],
e:"Examples tied to specific code become misleading when that code changes &mdash; and a stale example is worse than none, because it confidently teaches something that is no longer true. Prefer examples that illustrate the principle with minimal, self-contained code, and review them when the conventions they demonstrate change. This is the same maintenance discipline that applies to CLAUDE.md entries.",
k:"Durable examples illustrate a principle with self-contained code. Examples tied to real code go stale and mislead."},

{i:931,d:4,t:"4.3",s:"SDE",diff:2,
q:"Your schema is correct but the model frequently omits an optional field that is present in the document.",
ask:"What should you check?",
a:["Whether the field's description makes clear when it should be populated, since optional fields need a stated trigger",
"Whether to make it required",
"Whether the model supports optional fields",
"Whether the field name is too long"],
c:0,
w:["Correct. Optional means the model must decide, and that decision needs a stated condition.",
"Making it required forces a value on documents that genuinely lack it &mdash; fabrication pressure.",
"Optional fields are supported.",
"Name length is irrelevant."],
e:"Optionality shifts a decision to the model, so it needs guidance about that decision. State when the field should be populated and how it is identified in the source, and add an example showing it populated. That is the mirror of the earlier problem &mdash; where over-using <code>required</code> causes fabrication, under-specified optionality causes omission.",
k:"Optional fields need a stated population trigger. <code>required</code> causes fabrication; unguided optional causes omission."},

{i:932,d:4,t:"4.4",s:"SDE",diff:3,
q:"You want validation to catch a value that is structurally valid, semantically plausible, and still wrong.",
ask:"What class of check can catch this?",
a:["Cross-referencing against an independent source &mdash; a database record, a prior document, a computed value &mdash; since internal consistency cannot detect it",
"A stricter schema",
"A wider plausibility range",
"A second extraction attempt"],
c:0,
w:["Correct. When a value is internally consistent and plausible, only external cross-reference detects an error.",
"Schemas check structure; this value is structurally fine.",
"Widening a range makes detection less likely.",
"A second attempt from the same source may reproduce the same wrong value."],
e:"There is a hierarchy of checks with increasing power and cost. Schema catches malformed and mistyped. Domain validation catches implausible and internally inconsistent &mdash; totals that do not reconcile, impossible dates. Cross-reference against an independent source catches values that pass both and are still wrong: a plausible customer number that belongs to someone else. Reserve the third for the fields where an error is expensive.",
k:"Schema &rarr; structure. Domain validation &rarr; plausibility and internal consistency. Cross-reference &rarr; everything else."},

{i:933,d:4,t:"4.5",s:"SDE",diff:2,
q:"A batch completes and you want to know its quality before releasing results downstream.",
ask:"What should the pipeline do?",
a:["Sample the output against ground truth or validation rules before release, so a systematic problem is caught before propagation",
"Release immediately and monitor downstream",
"Release only after human review of everything",
"Compare against the previous batch's output"],
c:0,
w:["Correct. A gate before release is where a systematic problem is cheapest to catch.",
"Downstream monitoring catches it after consumers have used the data.",
"Full human review abandons the automation.",
"A different corpus is not a controlled comparison."],
e:"Treat batch output as untrusted until sampled. Run the validation rules over the whole output and inspect a stratified sample against ground truth, then release. Since a batch runs for hours without supervision, this is your only opportunity to catch a systematic problem before it reaches consumers &mdash; and a systematic problem in a 40,000-item batch propagates widely.",
k:"Gate batch output on validation plus a stratified sample before release. Unsupervised runs need a release gate."},

{i:934,d:4,t:"4.6",s:"CCI",diff:2,
q:"Your per-file review passes run concurrently and one fails.",
ask:"What should happen?",
a:["Report findings from the passes that succeeded and state that one file was not reviewed, rather than failing the whole review or silently omitting it",
"Fail the entire review",
"Report the successful findings without mentioning the failure",
"Retry until all passes succeed"],
c:0,
w:["Correct. Partial results with an explicit coverage note &mdash; the same pattern as any fan-out.",
"Failing everything over one file discards useful findings.",
"Silent omission means a developer believes a file was reviewed when it was not.",
"Unbounded retry may block the pipeline indefinitely."],
e:"A review fan-out follows the same partial-success rules as any other. Use what succeeded, annotate the gap explicitly &mdash; &ldquo;<code>payment_service.py</code> was not reviewed: the pass failed after two attempts&rdquo; &mdash; so the developer knows where the review is silent. Both anti-patterns apply here too: aborting everything, and reporting partial coverage as complete.",
k:"Review fan-out follows partial-success rules: use what succeeded, annotate what was not reviewed."},

{i:935,d:4,t:"4.1",s:"SDE",diff:2,
q:"A criterion works well for one document type and produces errors on another.",
ask:"What should you consider?",
a:["Making the criterion type-specific, since a single criterion may not fit genuinely different document structures",
"Loosening the criterion for both",
"Tightening the criterion for both",
"Removing the criterion"],
c:0,
w:["Correct. Segment-specific behaviour often needs segment-specific criteria.",
"Loosening degrades the type that worked.",
"Tightening worsens the type that already fails.",
"Removing loses the value it provides on the type it suits."],
e:"When a criterion's performance varies by segment, a single global version is being asked to fit two different things. Classify the document type first, then route to type-specific criteria and examples &mdash; the classify-then-route pattern. That also gives you per-type accuracy measurement, which is what automation decisions need anyway.",
k:"Criteria that vary in performance by segment should be type-specific. Classify then route, and measure per type."},

{i:936,d:4,t:"4.2",s:"SDE",diff:2,
q:"You are told few-shot examples should always be included.",
ask:"When are they not worth the context?",
a:["When the task is unambiguous and the output shape is enforced by a schema, so the examples add cost without changing behaviour",
"When the task is difficult",
"When the model is capable",
"Never; examples always help"],
c:0,
w:["Correct. Examples earn their cost when they resolve ambiguity or demonstrate a format not otherwise enforced.",
"Difficulty is a reason to include them, not to omit them.",
"Capability does not remove format ambiguity.",
"They cost tokens on every request and must earn it."],
e:"Judge examples by what they resolve. If a JSON Schema already guarantees the shape and the fields are unambiguous, examples may add nothing. They earn their place when they demonstrate conventions the schema cannot express &mdash; date formatting, how absence is represented, which of two similar values to pick &mdash; or when they target a known failure region. Measure with and without.",
k:"Examples earn their cost by resolving ambiguity a schema cannot express. Measure with and without."},

{i:937,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want to extract from documents in several languages with one schema.",
ask:"What should the schema account for?",
a:["Whether extracted values should be normalised to one language or preserved as written, stated explicitly so behaviour is consistent",
"Nothing; language does not affect schema design",
"A separate schema per language",
"A language field only"],
c:0,
w:["Correct. Normalise-or-preserve is a decision that must be stated, or behaviour varies.",
"It materially affects what the extracted values mean.",
"A separate schema per language duplicates a contract that is otherwise identical.",
"A language field records the input language without resolving the value question."],
e:"Multilingual extraction hides a decision: is a vendor name preserved as written, or transliterated? Are categorical values returned in the source language or mapped to your canonical enum? Leave it unstated and behaviour varies by document. State it, and use enums with canonical values for categorical fields so they are language-independent by construction &mdash; recording the source language separately if you need it.",
k:"State normalise-or-preserve for multilingual values. Canonical enums make categorical fields language-independent."},

{i:938,d:4,t:"4.4",s:"SDE",diff:2,
q:"Your retry loop is working but each retry costs as much as the original extraction.",
ask:"What optimisation is available where the schema permits?",
a:["Scope the retry to the failing fields, with their specific errors and targeted examples, rather than re-extracting everything",
"Use a cheaper model for retries",
"Reduce the retry budget",
"Skip validation on retries"],
c:0,
w:["Correct. A field-scoped retry is smaller and more focused than a full re-extraction.",
"A weaker model on the case that already failed is the wrong direction.",
"Fewer retries pushes more work to human review.",
"Skipping validation defeats the purpose of the loop."],
e:"Field-scoped retries reduce cost and often improve accuracy, since the prompt can focus entirely on the failing fields with their errors and targeted examples. Two caveats: validate the merged record afterwards, since a retry regenerates only what you asked for; and retry interdependent fields together, since line items and totals cannot be corrected in isolation.",
k:"Retry failing fields, not whole documents. Validate the merged record; retry interdependent fields together."},

{i:939,d:4,t:"4.5",s:"SDE",diff:2,
q:"You want to run the same extraction over new documents every night with no manual step.",
ask:"What must the pipeline handle automatically?",
a:["Submission, expiry and partial failure &mdash; re-submitting expired or failed items by <code>custom_id</code> without human intervention",
"Only submission",
"Only retrieval",
"Nothing; batches are fully managed"],
c:0,
w:["Correct. Unattended operation requires handling the failure modes automatically.",
"Submission without failure handling leaves items silently unprocessed.",
"Retrieval alone does not address expiry or per-item failures.",
"Batches expire and individual requests fail; neither is handled for you."],
e:"Unattended batch operation needs the full lifecycle automated: submit, poll for completion, reconcile submitted against returned by <code>custom_id</code>, re-submit expired and transiently-failed items, and route persistently-failing ones to review rather than retrying forever. Add the release gate &mdash; validation plus a sampled check &mdash; before results reach consumers.",
k:"Unattended batches need the whole lifecycle: submit, reconcile by <code>custom_id</code>, re-submit failures, gate on validation."},

{i:940,d:4,t:"4.6",s:"CCI",diff:2,
q:"You are asked whether the same model should generate and review, given they are separate invocations.",
ask:"What is the accurate answer?",
a:["Yes &mdash; independence comes from separate context, not from using a different model",
"No &mdash; review requires a different model",
"No &mdash; review requires a more capable model",
"Yes &mdash; but only if the review runs later"],
c:0,
w:["Correct. Context isolation is the operative variable.",
"Model identity is not what makes review effective.",
"Capability helps generally but is not what independence means.",
"Timing does not create independence if the context is shared, and is unnecessary if it is not."],
e:"Be precise about what independence means. The problem with self-review is that the reviewing instance retains the reasoning it used to justify its choices. A separate invocation of the same model, seeing only the diff and the standards, does not have that reasoning &mdash; so it is independent in the way that matters. A different model adds diversity, which can help, but it is not the mechanism.",
k:"Independence comes from separate context, not a different model. Same model, fresh context, diff plus standards only."},

{i:941,d:4,t:"4.1",s:"CCI",diff:2,
q:"You want a criterion that is strict on new code and lenient on legacy code being modified.",
ask:"How should this be expressed?",
a:["As a stated condition in the criterion, with the definition of legacy made concrete &mdash; a path, a marker, or an age &mdash; so it is decidable",
"By running two separate reviews",
"By instructing the reviewer to use judgement about what is legacy",
"By reviewing legacy code less often"],
c:0,
w:["Correct. A conditional criterion works when the condition is decidable.",
"Two reviews duplicates configuration for something one conditional expresses.",
"&ldquo;Judgement about what is legacy&rdquo; is undefined and will be applied inconsistently.",
"Frequency does not express a criterion difference."],
e:"Conditional criteria are fine when the condition is observable. Define legacy concretely &mdash; files under a particular path, files carrying a marker, files untouched since a date &mdash; and state the differing expectation for each side. Where the condition maps cleanly to paths, a path-scoped rule expresses it structurally and the criterion itself stays simple.",
k:"Conditional criteria need a decidable condition. Where it maps to paths, express it as a path-scoped rule instead."},

{i:942,d:4,t:"4.2",s:"SDE",diff:2,
q:"An example in your prompt is longer than the actual documents being processed.",
ask:"What should you consider?",
a:["Trimming it to the parts that demonstrate the point, since example length is paid on every request",
"Making the documents longer",
"Removing all examples",
"Nothing; example length does not matter"],
c:0,
w:["Correct. Examples are billed on every request, so they should be as long as necessary and no longer.",
"Document length is determined by the input.",
"Removing all examples loses their benefit.",
"Length is a recurring cost across the whole run."],
e:"Examples cost input tokens on every request, which for a 40,000-item batch multiplies substantially. Trim each to the parts that demonstrate the point &mdash; you rarely need a complete realistic document to show how a date is formatted or how absence is represented. Keep enough context that the example is realistic, and cut the rest.",
k:"Trim examples to what demonstrates the point. Example length is paid on every single request."},

{i:943,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want downstream code to be able to tell whether a field was extracted, defaulted, or corrected by a retry.",
ask:"What should the schema support?",
a:["Provenance metadata per field, so downstream consumers can treat values differently by how they were obtained",
"A single record-level status",
"Nothing; all values should be treated equally",
"A timestamp per field"],
c:0,
w:["Correct. Per-field provenance lets consumers apply different confidence to differently-obtained values.",
"A record-level status cannot express that one field was corrected and the rest were clean.",
"Values obtained differently warrant different treatment.",
"Timestamps do not convey how the value was obtained."],
e:"This is provenance applied to extraction. A value read cleanly, a value corrected on the second attempt, and a value that came from a default are different in ways downstream consumers may care about &mdash; and per-field metadata lets a reviewer focus on the corrected ones. It also feeds your own analysis: corrected fields are exactly where accuracy measurement should concentrate.",
k:"Per-field provenance (extracted, corrected, defaulted) lets consumers and reviewers weight values appropriately."},

{i:944,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to avoid a retry loop that runs forever on a document that can never validate.",
ask:"What bounds it?",
a:["An explicit retry budget with a defined terminal action &mdash; route to human review with the errors and the best attempt attached",
"A longer timeout",
"A stricter validation rule",
"A more capable model"],
c:0,
w:["Correct. A bounded budget plus a defined terminal state is what makes the loop finite and useful.",
"Timeouts bound a single call, not the loop.",
"Stricter validation causes more retries, not fewer.",
"Capability does not resolve genuinely absent or ambiguous source data."],
e:"Every automated loop needs a bound and a defined terminal state. Exhausted retries do not make a document unimportant &mdash; they make it a human's problem, and the handoff should carry the source, the best attempt and the specific validation failures so the review is quick. Patterns in what reaches review then tell you which prompt or schema change would raise the automation rate.",
k:"Bound retry loops and define the terminal state: route to review with source, best attempt and specific errors."},

{i:945,d:4,t:"4.5",s:"SDE",diff:2,
q:"A stakeholder asks why you are not using batch processing for everything to halve costs.",
ask:"What is the accurate explanation?",
a:["Batch trades bounded latency for cost, so it suits anything not blocking a person or a pipeline &mdash; and rules out everything that is",
"Batch produces lower quality output",
"Batch is limited to small volumes",
"Batch cannot use tools at all"],
c:0,
w:["Correct. The trade is latency for cost, and the blocking question decides.",
"Quality is identical; batch is a delivery mechanism.",
"Limits are 100,000 requests or 256 MB &mdash; large, not small.",
"Single-shot tool calls work; only multi-turn loops per request are excluded."],
e:"Frame it as a trade rather than a limitation. Batch gives roughly 50% savings in exchange for an unbounded window up to 24 hours. That is excellent for backfills, audits, overnight reports and reclassification, and unusable for anything a person or a pipeline is waiting on. Route by the blocking question and share the prompt and schema across both paths so quality is identical.",
k:"Batch trades bounded latency for ~50% cost. Route by the blocking question; share prompt and schema across both paths."},

{i:946,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want a review to catch issues arising from the interaction of this change with a change merged yesterday.",
ask:"What does this require?",
a:["Reviewing against the merged state rather than the change in isolation, so recent interacting changes are visible",
"Reviewing the two changes together",
"Reviewing only the current change",
"Reviewing after both are merged"],
c:0,
w:["Correct. The base state must include yesterday's change for the interaction to be visible.",
"Yesterday's change is already merged and is not part of this review.",
"Isolation is exactly what makes the interaction invisible.",
"Post-merge review means the defect has already reached main."],
e:"Scope means the change plus enough context to evaluate it, and the base state is part of that context. Reviewing against a stale base makes interactions with recently-merged work invisible. Combined with the cross-file integration pass &mdash; changed symbols plus their consumers, evaluated against the current merged state &mdash; this is what catches contract breaks introduced by two changes that are each individually fine.",
k:"Review against the current merged base, not a stale one, or interactions with recent changes stay invisible."},

{i:947,d:4,t:"4.1",s:"SDE",diff:2,
q:"You want a criterion that will still be correct after the schema changes next quarter.",
ask:"What makes it durable?",
a:["Expressing it in terms of the domain meaning rather than the current field names or structure",
"Making it shorter",
"Making it stricter",
"Versioning it"],
c:0,
w:["Correct. Domain-level criteria survive representational changes.",
"Length is unrelated to durability.",
"Strictness does not confer durability.",
"Versioning tracks change rather than reducing it &mdash; useful, but not what makes a criterion durable."],
e:"Criteria written against the representation break when the representation changes; criteria written against the domain meaning survive. &ldquo;The stated total must reconcile with the sum of line items&rdquo; holds regardless of how those fields are named or nested; &ldquo;<code>totals.grand.value</code> must equal the sum of <code>items[].amt</code>&rdquo; breaks on the next refactor. Express intent, and let the implementation map to the current schema.",
k:"Write criteria against domain meaning, not field names. Representational criteria break on the next schema change."},

{i:948,d:4,t:"4.2",s:"CSA",diff:2,
q:"You want examples that teach an agent to handle a rare but high-stakes case correctly.",
ask:"What is the trade-off?",
a:["A rare case costs example budget on every request; include it when the stakes justify the ongoing cost, and consider enforcing the critical part in code instead",
"Rare cases should never get examples",
"Rare cases should get the most examples",
"Frequency should be the only consideration"],
c:0,
w:["Correct. Weigh stakes against ongoing cost, and remember that examples are probabilistic anyway.",
"High stakes can justify the cost.",
"Weighting by rarity alone ignores cost.",
"Stakes matter alongside frequency."],
e:"Two considerations. Cost: an example is paid on every request whether or not the rare case arises. Sufficiency: an example makes correct handling more likely, not certain &mdash; so if the stakes are high enough to justify the example, they may be high enough to warrant enforcement in code. Often the best answer is both: a hook that guarantees the boundary, and an example so the agent handles the situation well around it.",
k:"High-stakes rare cases: weigh example cost, and consider whether code enforcement is warranted instead of or alongside."},

{i:949,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want to know whether a schema change improved or degraded extraction quality.",
ask:"What do you need?",
a:["A fixed labelled set run before and after, compared at field level",
"The number of validation failures before and after",
"The extraction latency before and after",
"Developer opinion"],
c:0,
w:["Correct. Labelled ground truth on fixed inputs is the only controlled comparison.",
"Validation failures measure agreement with your rules, not correctness &mdash; and a schema change may alter what the rules can even check.",
"Latency is a cost metric.",
"Opinion is not measurement."],
e:"Schema changes are quality changes and deserve the same controlled comparison as prompt changes. Note the trap in the validation-failure option: changing the schema changes what validation can express, so a drop in failures may mean the new schema simply checks less. Only ground truth on a fixed set, compared at field level, tells you whether extraction actually improved.",
k:"Compare schema changes on a fixed labelled set at field level. Validation-failure counts are not a quality measure."},

{i:950,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to detect when your extraction starts failing in a new way you have not seen before.",
ask:"What surfaces this?",
a:["Sampling the passing output against ground truth, since new failure modes by definition pass your existing rules",
"Adding more validation rules",
"Monitoring the validation failure rate",
"Increasing the retry budget"],
c:0,
w:["Correct. A novel failure mode passes the rules you already have, so only ground-truth sampling finds it.",
"New rules can only encode failure modes you already know about.",
"The failure rate measures known failures.",
"Retries address known failures."],
e:"Validation catches what you anticipated; sampling against ground truth catches what you did not. This is why stratified sampling of the auto-approved stream is a standing practice rather than a one-off &mdash; a new document template, an upstream OCR change or a model update can introduce a failure mode that passes every existing rule silently. Sampling is the only mechanism that sees it.",
k:"Validation catches known failures; ground-truth sampling of passing output catches novel ones. Keep sampling standing."}
);
