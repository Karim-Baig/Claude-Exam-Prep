QB.push(
{i:1401,d:1,t:"1.1",s:"CSA",diff:3,
q:"Your support agent's loop is correct, but a customer asking about two unrelated orders gets an answer covering only one.",
ask:"What is the root cause?",
a:["The agent ended its turn without verifying every enumerated item was addressed, so coverage was never checked",
"The tool returned only one order",
"The context window was exceeded",
"The iteration cap fired"],
c:0,
w:["Correct. Multi-aspect coverage requires an explicit verification step before <code>end_turn</code>.",
"The premise is two orders were asked about; a single-order result would be a tool problem, but the failure described is coverage.",
"Two orders is not a context problem.",
"A cap firing would truncate visibly rather than silently omit one item."],
e:"The agentic loop's third phase is <em>verify results</em>, and it applies to coverage as much as to quality. Enumerate the distinct items, work each, and check every one is resolved or escalated before ending the turn. This is the same failure as a research report covering one sub-domain &mdash; the work succeeded on what it addressed, and nothing checked what it did not.",
k:"Gather &rarr; act &rarr; <strong>verify</strong>. Enumerate multi-part requests and check coverage before <code>end_turn</code>."},

{i:1402,d:1,t:"1.2",s:"MAR",diff:3,
q:"A coordinator dispatches four subagents. Two return quickly with thin results, one returns rich results, one fails. The coordinator synthesises all of it without comment.",
ask:"How many distinct failures does this represent?",
a:["Three &mdash; no evaluation of the thin results, no recovery for the failure, and no coverage annotation in the output",
"One &mdash; the subagent failure",
"Two &mdash; the failure and the thin results",
"None &mdash; partial results are acceptable"],
c:0,
w:["Correct. Evaluation, recovery and annotation are three separate obligations, and all three were skipped.",
"The failure is only one of three.",
"The missing annotation is a third, separate failure affecting the reader.",
"Partial results are acceptable <em>when annotated</em>; unannotated they mislead."],
e:"Three distinct obligations were missed. <strong>Evaluation</strong>: thin results should be checked against quality criteria and re-dispatched with specific feedback. <strong>Recovery</strong>: the failure should have produced a structured error the coordinator acted on. <strong>Annotation</strong>: the output should state which sections are partial and why. Note that a success status is not a quality judgement &mdash; thin-but-successful results bypass error handling entirely.",
k:"Coordinators owe three things: evaluate results against criteria, recover from failures, annotate coverage gaps."},

{i:1403,d:1,t:"1.3",s:"AAT",diff:3,
q:"A coordinator writes: <em>Analyse the attached findings using our standard criteria and return a summary.</em>",
ask:"How many of the four delegation elements are missing or defective?",
a:["Three &mdash; the inputs are referenced rather than included, the criteria are referenced rather than stated, and the output contract is unspecified",
"One &mdash; only the inputs",
"Two &mdash; the inputs and the criteria",
"None &mdash; the goal is clear"],
c:0,
w:["Correct. Only the goal survives; inputs, criteria and contract are all defective.",
"The criteria and contract are also defective.",
"The output contract is a third defect &mdash; &ldquo;a summary&rdquo; specifies no shape.",
"The goal is clear; the other three are not."],
e:"A complete delegation is goal + full inputs + output contract + quality criteria. Here &ldquo;the attached findings&rdquo; and &ldquo;our standard criteria&rdquo; are dangling references that resolve to nothing in an isolated context, and &ldquo;a summary&rdquo; specifies no fields. Apply the competent-stranger test: a capable person with no access to your conversation could not do this task.",
k:"Goal + full inputs + output contract + quality criteria. References resolve to nothing; &ldquo;a summary&rdquo; is not a contract."},

{i:1404,d:1,t:"1.4",s:"CSA",diff:3,
q:"A refund control is implemented as a hook. Audit shows zero violations, but also shows the agent attempted blocked refunds 200 times last month, each producing a confused customer conversation.",
ask:"What should change?",
a:["State the rule in the prompt so the agent routes large refunds to escalation proactively, and make the block message explanatory and non-retryable",
"Relax the threshold",
"Remove the hook, since the agent clearly needs the capability",
"Accept it &mdash; the control is working"],
c:0,
w:["Correct. Enforcement is working; the agent's competence around it is the gap.",
"Relaxing a working financial control to improve conversations is the wrong trade.",
"Removing enforcement because it fires is exactly backwards.",
"Zero violations is good; 200 confused conversations is not."],
e:"This is the complementarity point with data attached. The hook guarantees the outcome &mdash; zero violations &mdash; but an agent that does not know the rule discovers it only by being blocked, burning turns and confusing customers. State the rule in the prompt so it offers escalation proactively, and make the block message name the rule, mark itself non-retryable, and point at the alternative.",
k:"Enforcement working plus poor conversations means the prompt is missing the rule and the block message is uninformative."},

{i:1405,d:1,t:"1.5",s:"AAT",diff:3,
q:"An enforcement hook consults a policy service. During a service outage, refunds continue to be processed normally.",
ask:"What is wrong?",
a:["The hook is failing open, so an unevaluable control is being treated as an approval",
"The hook is failing closed",
"The hook has too short a timeout",
"The policy service should be more reliable"],
c:0,
w:["Correct. Enforcement hooks must fail closed &mdash; unknown policy state is not approval.",
"Failing closed would deny, which is the correct behaviour.",
"A short timeout is fine; what it does on timeout is the issue.",
"Reliability helps without changing the required behaviour during an outage."],
e:"Enforcement hooks fail closed; observational hooks fail open. Treating an unevaluable financial control as approval means unapproved refunds go out during exactly the incident when oversight is weakest. What makes failing closed acceptable is the escalation path &mdash; deny, return a clear non-retryable explanation, and route the customer to a human who can approve manually.",
k:"Enforcement hooks fail closed with an escalation path. Unknown policy state is not approval."},

{i:1406,d:1,t:"1.6",s:"CGC",diff:3,
q:"A 45-file migration is planned as three phases. Phase one turns out to touch 40 of the files and cannot be verified until it is complete.",
ask:"What is the defect in the plan?",
a:["Phase one is not a phase &mdash; it has no intermediate checkpoint, so a mistake surfaces only after 40 files have been changed",
"Three phases is too few",
"The files should be split alphabetically",
"The migration should be done in one phase"],
c:0,
w:["Correct. A phase that cannot be verified until 40 files are changed provides no checkpoint.",
"Count is not the criterion; verifiability is.",
"Alphabetical splitting is arbitrary and ignores dependencies.",
"One phase removes the checkpoints entirely."],
e:"Phasing exists to create verification checkpoints, so a phase must end where the work is in a coherent, checkable state. Split phase one at the first point something can be verified &mdash; a module compiling, a test suite passing &mdash; so a mistake surfaces after a handful of files rather than forty. Update the plan file, since a phase that outgrew its estimate is evidence to act on.",
k:"A phase must end at a verifiable state. Split phases that cannot be checked until dozens of files have changed."},

{i:1407,d:1,t:"1.7",s:"CGC",diff:3,
q:"You resume a session and it confidently describes a class that a colleague deleted overnight.",
ask:"What happened, and what is the general lesson?",
a:["The session restored cached tool results describing the old state; resumption preserves stale context as faithfully as valid context",
"The session was corrupted",
"The model hallucinated the class",
"The file system is out of sync"],
c:0,
w:["Correct. Session context is a cache of the world as it was when read, with no invalidation.",
"Nothing is corrupted; the cache is simply stale.",
"It is not hallucination &mdash; the class genuinely existed when the session read it.",
"The filesystem is correct; the session's snapshot is not."],
e:"Session context is an uninvalidated cache. Resuming restores everything, including what has since become false, and the failure is silent &mdash; the session reasons confidently from a world that no longer exists. Either name the changed files and have it re-read them, or start fresh with a structured summary, which the blueprint notes can be more reliable than resuming with stale results.",
k:"Session context is an uninvalidated cache. If the world moved, invalidate explicitly or restart with a summary."},

{i:1408,d:2,t:"2.1",s:"CSA",diff:3,
q:"Two tools misroute 30% of the time. You expand both descriptions with input formats, examples and boundaries. Misrouting falls to 22%.",
ask:"What does the residual 22% suggest?",
a:["The boundary between them may be genuinely ambiguous, in which case they should be merged or re-split along a different axis",
"The descriptions need to be longer still",
"Few-shot examples will close the gap",
"22% is an acceptable ceiling"],
c:0,
w:["Correct. A large residual after good description work indicates a structural boundary problem.",
"Longer descriptions of an ambiguous distinction restate the ambiguity at greater length.",
"Few-shot is a reasonable bridge but teaches navigation of a bad boundary rather than removing it.",
"22% misrouting is not acceptable and has an identified structural cause."],
e:"Descriptions have a ceiling. When two tools genuinely overlap &mdash; both can legitimately serve the same request &mdash; the model's uncertainty reflects reality rather than a description defect. Apply the one-sentence test: if you cannot say concisely when to choose each, merge them, or re-split along an axis that is genuinely unambiguous, such as by data source or permission level.",
k:"Descriptions cannot fix a genuinely ambiguous boundary. Apply the one-sentence test, then merge or re-split."},

{i:1409,d:2,t:"2.2",s:"MAR",diff:3,
q:"A subagent returns <code>{\"status\": \"partial\", \"results\": [...], \"note\": \"some sources were slow\"}</code>.",
ask:"What is still missing for the coordinator to act well?",
a:["Which sources failed, why, whether retrying would help, and what coverage was achieved &mdash; the note conveys none of it",
"A confidence score",
"The elapsed time",
"The subagent's identity"],
c:0,
w:["Correct. The status is right; the context needed for a recovery decision is absent.",
"Self-rated confidence is uncalibrated and is not what the coordinator needs.",
"Elapsed time is operational.",
"Identity aids attribution but does not inform recovery."],
e:"The partial status is correct &mdash; better than reporting success or a blanket failure. What is missing is everything the coordinator needs to decide: which sources, what kind of failure, retryable or not, and how much of the assigned scope was covered. &ldquo;Some sources were slow&rdquo; is the generic-status anti-pattern in a friendlier voice.",
k:"A partial status needs the detail: which failed, why, retryability, and coverage achieved. A prose note conveys none of it."},

{i:1410,d:2,t:"2.3",s:"AAT",diff:3,
q:"An agent has 4 well-differentiated tools and still selects incorrectly on 15% of requests.",
ask:"What should you examine next, having ruled out toolset size?",
a:["The descriptions' boundary statements and the system prompt's vocabulary, since either can bias selection independently of tool count",
"Adding more tools for coverage",
"Increasing the context window",
"Reducing to two tools"],
c:0,
w:["Correct. With size ruled out, descriptions and system-prompt vocabulary are the next causes.",
"More tools enlarges the choice space and makes it worse.",
"Window size is not a selection factor.",
"Cutting needed capability to force selection is the wrong trade."],
e:"Work through the causes in order: toolset size and role scope, then names and descriptions, then system prompt vocabulary, then model. The third is easily missed &mdash; a persona described as a &ldquo;data analyst&rdquo; creates an unintended pull toward anything analytical, independently of how good the descriptions are.",
k:"Selection debugging order: size and scope &rarr; names and descriptions &rarr; system prompt vocabulary &rarr; model."},

{i:1411,d:2,t:"2.4",s:"DPT",diff:3,
q:"A developer adds a community MCP server exposing 40 tools to project scope, in a pull request titled &ldquo;add Jira integration&rdquo;.",
ask:"What should the review consider?",
a:["That this grants 40 tools to every developer's agent, degrading selection system-wide &mdash; and whether a wrapper exposing the few operations used would be better",
"Only whether the server is well-maintained",
"Only whether the credentials are handled correctly",
"Nothing; MCP configuration is routine"],
c:0,
w:["Correct. Project scope is a team-wide capability grant with a measurable selection cost.",
"Maintenance matters after deciding to adopt it.",
"Credential handling is necessary but not the main consideration.",
"It is a capability grant, not routine configuration."],
e:"All connected servers' tools are available simultaneously, so 40 new tools enter every agent's choice space and are billed as input tokens on every request. Review it as a capability grant: which operations are actually needed, should it be scoped to one agent with <code>disallowedTools</code> elsewhere, and would a thin wrapper exposing four operations serve better than exposing forty.",
k:"A project MCP server is a team-wide capability grant. Review the surface size, not just credentials and maintenance."},

{i:1412,d:2,t:"2.5",s:"DPT",diff:3,
q:"An agent is asked which modules depend on a utility function. It greps the name, finds 6 call sites, and reports 6 dependents. A human finds 14.",
ask:"What did the search miss, and how should it have been done?",
a:["Indirect usage through re-exports and wrappers &mdash; it should have traced the re-exported names as well, and stated its coverage",
"It should have read every file",
"It should have used Glob instead",
"It should have asked the developer"],
c:0,
w:["Correct. Direct symbol search misses callers reaching the function through an intermediary.",
"Exhaustive reading is unnecessary and enormously expensive.",
"Glob matches filenames, which does not answer a usage question.",
"The agent can determine this by tracing properly."],
e:"Symbol searches under-report through re-exports and wrappers, and over-report through comments and name collisions. The method: grep the original symbol to find direct callers <em>and</em> the modules re-exporting it, then grep those re-exported names, repeating per layer. Report coverage honestly &mdash; &ldquo;6 direct, 8 via the <code>utils</code> barrel&rdquo; &mdash; rather than presenting a partial list as exhaustive.",
k:"Trace re-exports layer by layer and state coverage. Direct symbol search under-reports and over-reports simultaneously."},

{i:1413,d:3,t:"3.1",s:"CGC",diff:3,
q:"A monorepo's root CLAUDE.md is 1,400 lines. Adherence to every rule has visibly declined, and a rule about Terraform is being applied to React components.",
ask:"What is the full remedy?",
a:["Prune what is derivable from the code, split the remainder by topic into <code>.claude/rules/</code>, and add <code>paths</code> frontmatter so each loads only for its own files",
"Reorder so important rules come first",
"Move it to user scope",
"Add emphasis to the most important rules"],
c:0,
w:["Correct. Three remedies address the two distinct symptoms: dilution and misapplication.",
"Ordering helps salience marginally and leaves 1,400 lines competing.",
"User scope destroys team sharing.",
"Emphasis loses force when everything is marked important."],
e:"Two symptoms with a shared cause. Declining adherence is instruction dilution &mdash; too much competing on every request. Terraform rules applied to React is misapplication &mdash; content loaded where it does not belong. Pruning addresses volume, splitting addresses maintainability, and <code>paths</code> scoping addresses both by making loading conditional.",
k:"Bloated CLAUDE.md: prune the derivable, split by topic, scope by path. That fixes dilution and misapplication together."},

{i:1414,d:3,t:"3.2",s:"CGC",diff:3,
q:"A team skill runs a deployment. A developer creates a personal skill with the same name that skips a safety check, and later cannot reproduce a colleague's deployment failure.",
ask:"What went wrong?",
a:["Personal skills override same-named project skills silently, so the developer was running a different deployment than everyone else",
"The project skill was deleted",
"Claude Code raised an error that was ignored",
"Both skills ran in sequence"],
c:0,
w:["Correct. Personal skills override project skills of the same name, with no warning.",
"Nothing is deleted.",
"No error is raised, which is what makes it silent.",
"Only one is invoked."],
e:"Personal skills in <code>~/.claude/skills/</code> override same-named project skills silently. Because there is no warning, the developer believes they are running the team's version. Give personal variants distinct names &mdash; the same discipline as naming personal MCP servers distinctly, and for the same reason: shadowing produces behaviour nobody else can reproduce.",
k:"Personal skills silently override same-named project skills. Name variants distinctly or you get unreproducible behaviour."},

{i:1415,d:3,t:"3.3",s:"CGC",diff:3,
q:"A rule with <code>paths: [\"*.test.ts\"]</code> is not firing for <code>src/api/handlers/user.test.ts</code>.",
ask:"Why, and what is the fix?",
a:["A single <code>*</code> does not cross directory levels; the pattern must be <code>**/*.test.ts</code>",
"The rule must be in the project root",
"Test files cannot be path-scoped",
"The frontmatter key should be <code>path</code>"],
c:0,
w:["Correct. <code>**/</code> is what matches at any depth, and its absence is the most common glob error.",
"Rules live in <code>.claude/rules/</code>; location is not the issue.",
"Test files are the canonical path-scoping use case.",
"The key is <code>paths</code>, which is correct here."],
e:"A single <code>*</code> matches within one directory level; <code>**</code> crosses levels. Missing <code>**/</code> is why a rule does not fire for nested files, and it matters most for exactly the case path scoping exists to serve &mdash; conventions following a file type across a tree, such as co-located test files. Use brace expansion for multiple extensions: <code>**/*.{test,spec}.ts</code>.",
k:"<code>*.test.ts</code> matches only the root. Use <code>**/*.test.ts</code>. Missing <code>**/</code> is the most common glob error."},

{i:1416,d:3,t:"3.4",s:"CGC",diff:3,
q:"A three-line change to an authorisation check, in code you know well, with one obvious implementation.",
ask:"Planning mode or direct execution?",
a:["Planning mode &mdash; consequence outranks size, and a security-critical path warrants reasoning about the approach explicitly",
"Direct execution, since it is three lines in familiar code",
"Direct execution with an extra test",
"Neither; escalate to a security specialist"],
c:0,
w:["Correct. The planning triggers are scope, multiple approaches and <em>consequence</em> &mdash; and consequence dominates here.",
"Size and familiarity are satisfied, but consequence is the deciding signal.",
"A test is necessary and does not replace reasoning about the approach.",
"Specialist review is valuable in addition, not instead."],
e:"The exam tests both directions of this decision, and this is the case where the small-change instinct is wrong. Consequence outranks diff size: a three-line change to an authorisation path deserves the approach reasoned about explicitly, plus independent review of the diff. Compare with a three-line typo fix, where direct execution is clearly right.",
k:"Consequence outranks size in the planning decision. Security-critical paths get planned regardless of diff size."},

{i:1417,d:3,t:"3.5",s:"CGC",diff:3,
q:"You give feedback three times. Each iteration fixes the stated issue and introduces a different one.",
ask:"What should you change about the process?",
a:["Build a test set capturing everything that must hold, so each iteration is checked against all of it rather than only the latest complaint",
"Give more detailed feedback",
"Give all three issues at once",
"Switch to a more capable model"],
c:0,
w:["Correct. Regression coverage is what stops each fix breaking something else.",
"Detail about the current issue does not protect previously-working behaviour.",
"Batching helps for interdependent issues; here the problem is missing regression coverage.",
"Any model can break untested behaviour."],
e:"Iteration without regression coverage trades one defect for another. The blueprint's test-driven iteration prescribes building the test set first &mdash; expected behaviour, edge cases and performance requirements &mdash; so every iteration is checked against everything that previously worked. It also gives the loop a hard termination signal rather than a subjective judgement.",
k:"Fixes introducing new breaks means missing regression coverage. Build the test set first, not after."},

{i:1418,d:3,t:"3.6",s:"CCI",diff:3,
q:"A CI pipeline runs <code>claude \"review this PR for security issues\"</code> and the job times out after 30 minutes with no output.",
ask:"What is wrong, and what else should be added?",
a:["It is running interactively and waiting for input &mdash; add <code>-p</code>, and add <code>--output-format json</code> with <code>--json-schema</code> so the pipeline can act on the result",
"The review is genuinely taking 30 minutes",
"The model is unavailable",
"The prompt is too vague"],
c:0,
w:["Correct. <code>-p</code> fixes the hang; structured output makes the result usable.",
"A hang with no output is the signature of waiting for interactive input.",
"Unavailability produces an error rather than a silent hang.",
"Vagueness affects quality, not whether the job hangs."],
e:"Without <code>-p</code> / <code>--print</code>, Claude Code runs interactively and waits for input that never arrives in CI. That is the immediate fix. The pipeline also needs to act on the result, which means <code>--output-format json</code> constrained by <code>--json-schema</code> so it can branch on severity and post inline comments &mdash; never by grepping prose.",
k:"CI hang with no output = missing <code>-p</code>. Add <code>--output-format json</code> + <code>--json-schema</code> to make results usable."},

{i:1419,d:4,t:"4.1",s:"CCI",diff:3,
q:"Your review bot has 92% precision on security findings and 55% on style findings. Developers have started ignoring it entirely.",
ask:"What should you do?",
a:["Disable the style category while you build explicit criteria for it, since false positives in one category erode trust in the accurate ones",
"Reduce the total number of comments",
"Add a confidence score to each finding",
"Explain the precision figures to developers"],
c:0,
w:["Correct. Trust is global, so the noisy category must be removed to protect the accurate one.",
"Cutting volume indiscriminately drops accurate security findings too.",
"Uncalibrated confidence scores push triage onto developers unreliably.",
"Statistics do not rebuild trust; observed precision does."],
e:"The blueprint makes this point specifically: a high false-positive rate in some categories undermines trust in the accurate categories. Because developers form one impression of the bot rather than per-category ones, the remedy is to protect the good category by removing the noisy one, develop explicit categorical criteria with code examples for it, measure, and reintroduce.",
k:"Trust is global, not per-category. Disable noisy categories to protect accurate ones; reintroduce after measuring."},

{i:1420,d:4,t:"4.2",s:"CSA",diff:3,
q:"You add three few-shot examples of the agent escalating. Escalation rate rises from 12% to 31%, and human reviewers say two thirds of the new escalations were resolvable.",
ask:"What went wrong?",
a:["One-sided examples shifted behaviour toward escalation; balanced examples showing resolution are needed too",
"Three examples is too many",
"The examples were too detailed",
"The escalation criteria are too loose"],
c:0,
w:["Correct. Examples shift the distribution toward what they demonstrate; a boundary needs both sides.",
"Count is not the issue; balance is.",
"Detail helps; the imbalance is the problem.",
"The criteria may be fine &mdash; the examples overwhelmed them."],
e:"Few-shot examples are a strong steer. Demonstrating only escalation teaches that escalation is the expected response, overwhelming the stated criteria. Add balanced examples &mdash; ideally near-miss pairs where a superficially similar case is resolved in-policy &mdash; each with the rationale, so the model learns the discriminating feature rather than a bias.",
k:"Boundary examples must be balanced. One-sided examples shift behaviour toward whichever side you demonstrated."},

{i:1421,d:4,t:"4.3",s:"SDE",diff:3,
q:"Extraction switched to <code>tool_use</code> with a strict JSON Schema. Parse failures dropped to zero. Auditors then find invoices where <code>vendor_name</code> contains the customer and the line items sum to a different figure than <code>total</code>.",
ask:"What is the correct diagnosis and remedy?",
a:["These are semantic errors, outside what any schema can express &mdash; add domain validation, and extract <code>calculated_total</code> alongside <code>stated_total</code> so the discrepancy is checkable in code",
"The schema needs stricter type constraints",
"The model needs a larger context window",
"More few-shot examples would have prevented both"],
c:0,
w:["Correct. Schemas guarantee structure; totals reconciling and values being in the right field are semantics.",
"No type constraint can express &ldquo;line items must sum to the total&rdquo; or &ldquo;this string is the vendor&rdquo;.",
"Window size is not the constraint; the output is well-formed and wrong.",
"Examples improve accuracy without guaranteeing arithmetic consistency."],
e:"This is the central Domain 4 distinction. <code>tool_use</code> with a schema eliminates <em>syntax</em> errors &mdash; malformed JSON, missing fields, wrong types. It does nothing for <em>semantic</em> errors, because the output is perfectly well-formed and merely wrong. The blueprint's self-correction pattern is to extract both the computed and the stated total so a discrepancy becomes visible to code, and to state the vendor/customer discriminator explicitly.",
k:"Schemas kill syntax errors, never semantics. Extract computed <em>and</em> stated values so code can reconcile them."},

{i:1422,d:4,t:"4.4",s:"SDE",diff:3,
q:"A document fails validation on the shipping address. Three retries each produce a different plausible address. The source invoice does not contain a shipping address at all &mdash; it lives on a separate manifest.",
ask:"What is the correct handling?",
a:["Recognise that retrying cannot succeed when the information is absent from the source, and either mark the field absent or fetch the manifest",
"Increase the retry budget",
"Use a more capable model",
"Loosen the validation rule"],
c:0,
w:["Correct. The blueprint notes explicitly that retries are ineffective when the information is simply absent.",
"More attempts at absent data produce more fabrications.",
"Capability cannot conjure information that is not present.",
"Loosening validation admits fabricated addresses."],
e:"Two signals point to the same conclusion. The information is absent from the source, so no retry can succeed. And different values on each attempt is the signature of a source that does not determine the answer. Either change the pipeline so the manifest is available, or make the field nullable so absence is reported honestly rather than fabricated.",
k:"Retry fixes wrong processing of present data. Absent data needs another source or an honest null &mdash; never more retries."},

{i:1423,d:4,t:"4.5",s:"SDE",diff:3,
q:"A pre-merge CI security check is moved to the Message Batches API to halve costs. Developers now wait up to several hours to merge.",
ask:"What was the error in reasoning?",
a:["Batch offers no latency SLA, so it is unsuitable for anything blocking &mdash; the 50% saving buys latency uncertainty",
"The batch was submitted too infrequently",
"The batch size was too large",
"The prompt was too slow"],
c:0,
w:["Correct. The blocking question decides, and a pre-merge check is blocking by definition.",
"Cadence bounds queue wait but not the up-to-24-hour processing window.",
"Size affects the limits, not the SLA.",
"Prompt complexity is not why results took hours."],
e:"The Batch API's defining constraint is the absence of a latency SLA &mdash; up to 24 hours, most finishing sooner, no guarantee. That makes it excellent for overnight reports, audits and backfills, and unusable for anything a person or pipeline waits on. Route by the blocking question, and share the prompt and schema across both paths so quality is identical.",
k:"Batch has no latency SLA. Anything blocking a person or pipeline stays synchronous regardless of cost."},

{i:1424,d:4,t:"4.6",s:"CCI",diff:3,
q:"An agent generates a module and reviews it in the same session, reporting it clean. An independent instance reviewing only the diff finds four genuine defects.",
ask:"What explains the gap?",
a:["The generating instance retains its reasoning and is unlikely to challenge decisions it just argued for; independence comes from a separate context, not a different model",
"The review prompt was insufficiently detailed",
"The generating instance had exhausted its context",
"The independent instance used a better model"],
c:0,
w:["Correct. The blueprint states this directly, and the mechanism is context isolation.",
"A better prompt cannot dislodge the anchoring from having just made these decisions.",
"The failure occurs with ample context remaining.",
"The same model reviews effectively given a clean context."],
e:"Self-review is weak because the reasoning that produced the code is still active and makes its choices feel justified. The remedy is context isolation: a separate invocation seeing the diff and the standards, but not the conversation that produced it. Note that sharing CLAUDE.md between them is desirable &mdash; independence concerns the generation reasoning, not the standards.",
k:"Independence is a separate context, not a different model. Share standards; never share the generation reasoning."},

{i:1425,d:5,t:"5.1",s:"CSA",diff:3,
q:"At turn 4 a customer states a disputed amount of $1,247.83. At turn 30 the agent refers to &ldquo;the disputed charge of around twelve hundred dollars&rdquo;.",
ask:"What failed, and what is the remedy?",
a:["Progressive summarisation condensed a precise figure into a vague one; the amount belongs in a code-maintained facts block outside the summarised history",
"The context window was exceeded",
"The customer restated the amount imprecisely",
"The temperature was too high"],
c:0,
w:["Correct. Summarisation degrades numbers, dates and percentages first &mdash; exactly this symptom.",
"The window may be fine; the summarisation is what lost the precision.",
"The premise is that the customer stated it precisely.",
"Temperature does not round a figure that is still in context."],
e:"Progressive summarisation degrades in a predictable direction: numeric values, percentages and dates become vague qualitative statements. The remedy is a structured case-facts block your code maintains and re-injects on every request &mdash; verified identity, order id, disputed amount, transaction date, decisions taken. It is small, exact, and never passes through the summariser.",
k:"Summarisation destroys numbers and dates first. Keep them in a code-maintained facts block outside the summarised stream."},

{i:1426,d:5,t:"5.2",s:"CSA",diff:3,
q:"A customer says: &ldquo;This is the third time I've called about this. Just give me someone who can actually help.&rdquo;",
ask:"What should the agent do?",
a:["Escalate immediately with a structured handoff, since this is an explicit request for a human &mdash; no diagnostic questions first",
"Attempt resolution once more, since first-contact resolution is the target",
"Run sentiment analysis to gauge severity",
"Ask what the previous two calls covered before deciding"],
c:0,
w:["Correct. An explicit request for a human is an immediate-escalation trigger, and repeat contact reinforces it.",
"Optimising a metric against an explicit customer instruction damages trust and usually produces a worse outcome.",
"Sentiment is explicitly an unreliable proxy, and the customer stated their request in plain words.",
"Gathering context is useful <em>for the handoff</em>, but it must not delay honouring the request."],
e:"Two of the three valid triggers are present: an explicit request for a human, and demonstrated inability to progress across three contacts. Escalate immediately. The context gathering still matters &mdash; but it goes into the structured handoff, drawn from durable case state written after the previous contacts, rather than into further questions to the customer.",
k:"Explicit request for a human = immediate escalation. Gather context for the handoff, not by questioning the customer."},

{i:1427,d:5,t:"5.3",s:"MAR",diff:3,
q:"A web-search subagent times out. Which response best enables intelligent coordinator recovery?",
ask:"Which is best?",
a:["Structured error context: failure type, the query attempted, partial results gathered, and viable alternatives",
"Internal exponential backoff, then a generic &ldquo;search unavailable&rdquo; status",
"Catching the timeout and returning an empty result set marked as success",
"Propagating the exception to a top-level handler that terminates the workflow"],
c:0,
w:["Correct. This lets the coordinator retry with a modified query, route elsewhere, or proceed with annotated partial coverage.",
"Backoff is fine; collapsing the outcome into a generic status discards the context the decision needs.",
"Masking failure as success makes the gap invisible until it appears as a silent omission.",
"Aborting the whole workflow over one timeout discards everything that succeeded."],
e:"This mirrors a published sample question, and the distractors are a tour of the three named anti-patterns: generic status hiding context, silent suppression, and whole-workflow abort. Structured error context turns a dead end into a decision. Note that internal backoff is not itself wrong &mdash; local recovery for transient failures is correct &mdash; what is wrong is discarding the context afterwards.",
k:"Structured failure context: type, attempt, partial results, alternatives. Anti-patterns: generic status, suppression, abort."},

{i:1428,d:5,t:"5.4",s:"DPT",diff:3,
q:"Two hours into a codebase investigation, answers become vague and refer to &ldquo;typical patterns in services like this&rdquo; rather than to the classes read earlier.",
ask:"What is the correct response, in order?",
a:["Write findings and exact paths to a scratchpad file first, then compact or start fresh seeded with that file",
"Compact immediately to free context",
"Continue and accept reduced quality",
"Increase the context window and continue"],
c:0,
w:["Correct. Externalise before reducing, or the specifics are lost in the summarisation.",
"Compacting first is exactly where the exact paths and values get condensed into vagueness.",
"The symptom means quality has already suffered.",
"A larger window delays the same failure without addressing it."],
e:"This is the documented context-degradation symptom: pattern-level answers replacing specific ones. The order matters &mdash; externalise, <em>then</em> reduce. Writing findings, decisions and exact file paths to a scratchpad first makes the compaction safe and gives you an artefact that also survives a crash and seeds a fresh session.",
k:"Vague pattern-level answers = context degradation. Externalise to a file <em>first</em>, then compact or restart."},

{i:1429,d:5,t:"5.5",s:"SDE",diff:3,
q:"A pipeline reports 97% overall accuracy. Handwritten forms &mdash; 4% of volume &mdash; extract correctly 61% of the time. Stakeholders want full automation.",
ask:"What should you propose?",
a:["Automate the validated segments and route handwritten forms to review, since aggregate accuracy masks segment-level performance",
"Automate everything, since 97% exceeds the threshold",
"Improve overall accuracy to 99% then automate everything",
"Exclude handwritten forms from the pipeline"],
c:0,
w:["Correct. Automation decisions are per segment, validated by document type and field.",
"The aggregate is dominated by the easy 96% and says nothing about the hard 4%.",
"Raising the aggregate would still hide the handwritten-form problem.",
"Excluding a legitimate input class is an overreaction when review handles it."],
e:"The blueprint states that aggregate metrics mask poor performance on specific document types or fields. Validate by type <em>and</em> by field, automate only the segments meeting the bar, and route the rest to human review &mdash; whose corrections then become labelled data for improving that segment. Keep sampling the automated stream to catch drift.",
k:"Aggregates mask segments. Validate by type and field, automate what qualifies, route the rest to review."},

{i:1430,d:5,t:"5.6",s:"MAR",diff:3,
q:"A government report cites 40% growth; an industry analysis cites 12%. Both look credible and the difference changes the conclusion.",
ask:"How should the document-analysis subagent handle it?",
a:["Complete its analysis carrying both values, annotated as conflicting with full attribution and dates, and let the coordinator reconcile",
"Apply credibility heuristics to choose one and footnote the discrepancy",
"Stop and escalate to the coordinator before continuing",
"Report both without marking them as conflicting"],
c:0,
w:["Correct. Complete your scope, preserve both with attribution, route reconciliation to the layer with broader context.",
"Silently resolving a material conflict hides a consequential judgement at the wrong level.",
"Blocking is unnecessary &mdash; the analysis can complete &mdash; and over-involves the coordinator.",
"Unmarked conflicting figures are the worst outcome: downstream consumers cannot tell there is a disagreement."],
e:"This mirrors a published sample question. Three principles combine: complete your own scope rather than blocking, preserve rather than resolve material conflicts at the leaf level, and make the conflict machine-readable. Check first whether they genuinely conflict &mdash; different measurement years, definitions or populations explain many apparent contradictions, which is why findings must carry dates and definitions.",
k:"Conflicts: don't pick, don't hide, don't block. Annotate with attribution and dates; escalate the reconciliation."},

{i:1431,d:1,t:"1.1",s:"AAT",diff:3,
q:"Your loop treats any response containing a <code>tool_use</code> block as &ldquo;execute and continue&rdquo;. Occasionally a refund is issued for a partial amount that appears nowhere in the conversation.",
ask:"What is the most likely mechanism?",
a:["A response with <code>stop_reason: \"max_tokens\"</code> truncated the tool call mid-generation, and the partial arguments parsed into a valid-looking object that was executed",
"The model hallucinated the amount",
"The tool schema is malformed",
"The customer supplied the amount earlier"],
c:0,
w:["Correct. Truncated JSON can parse into a plausible partial object, which is why <code>stop_reason</code> must be checked first.",
"Possible in general, but the described symptom &mdash; a partial amount &mdash; matches truncation.",
"A malformed schema would produce validation errors rather than a partial amount.",
"The premise is that the amount appears nowhere."],
e:"Content inspection is not a substitute for <code>stop_reason</code>. <code>max_tokens</code> means generation was cut off, and the final content block &mdash; often a <code>tool_use</code> with half-written arguments &mdash; is incomplete. For a state-changing tool the consequence is real. Branch on <code>stop_reason</code> first: on <code>max_tokens</code>, discard the turn and retry with more headroom.",
k:"Check <code>stop_reason</code> before executing. Truncated tool arguments can parse into a plausible, wrong object."},

{i:1432,d:1,t:"1.4",s:"CSA",diff:3,
q:"Policy limits refunds to $500. A customer has three separate orders, each refunded at $400 by the agent, totalling $1,200. The per-call hook approved all three.",
ask:"What must change?",
a:["The control's scope &mdash; track cumulative refunds per customer or per period in server-side state and evaluate the aggregate, since per-call thresholds are trivially split",
"Lower the per-call threshold to $300",
"Instruct the agent not to split refunds",
"Accept it, since each refund was individually within policy"],
c:0,
w:["Correct. An aggregate limit requires aggregate state; per-call thresholds do not express it.",
"A lower per-call limit just requires more splits.",
"A prompt instruction guarding a financial control is the pattern the exam consistently marks insufficient.",
"The policy intent was clearly an aggregate limit, and it was breached."],
e:"Threshold controls need an explicitly defined scope: per call, per conversation, per customer, or per period. A per-call limit is the easiest to implement and the easiest to circumvent &mdash; and circumvention need not be deliberate, since three genuine orders produce the same effect. Track cumulative state in your store and evaluate the aggregate in the hook.",
k:"Define threshold scope explicitly. Per-call limits are trivially split; aggregate limits need aggregate server-side state."},

{i:1433,d:2,t:"2.2",s:"SDE",diff:3,
q:"A search tool returns <code>{\"isError\": true, \"message\": \"No matching records\"}</code>. The agent retries it four times, then tells the customer the system is unavailable.",
ask:"What are the two defects?",
a:["A valid empty result is misclassified as an error, and the resulting message misrepresents a successful search as an outage",
"The retry budget is too high and the message is too short",
"The tool is too slow and the agent is too persistent",
"The schema is wrong and the model is miscalibrated"],
c:0,
w:["Correct. Misclassification causes the retry storm, and the customer is then told something false.",
"The budget is a symptom; the misclassification is the cause.",
"Neither latency nor persistence is the issue.",
"Neither the schema nor the model is at fault."],
e:"The blueprint is explicit about distinguishing access failures from valid empty results. &ldquo;I searched and found nothing&rdquo; is a finding to report; &ldquo;I could not search&rdquo; is a failure to recover from. Conflating them produces a retry storm on one side and, here, a customer told the system is down when it worked correctly. Return success with an empty result set and the query echoed.",
k:"Empty result = success with an empty set. Misclassifying it causes retry storms and false outage reports."},

{i:1434,d:3,t:"3.2",s:"CGC",diff:3,
q:"You need a shared workflow that runs in an isolated context, cannot modify files, and prompts for a target module.",
ask:"What is the complete configuration?",
a:["A skill in <code>.claude/skills/</code> with <code>context: fork</code>, <code>disallowed-tools</code> removing Write, Edit and Bash, and an <code>argument-hint</code>",
"A skill with <code>context: fork</code>, <code>allowed-tools</code> listing read tools, and an <code>argument-hint</code>",
"A slash command in <code>~/.claude/commands/</code> with the instructions",
"An entry in the root CLAUDE.md describing the procedure"],
c:0,
w:["Correct. Project location for sharing, plus the three fields matching the three requirements.",
"Nearly right, but <code>allowed-tools</code> <em>pre-approves</em> and does not restrict &mdash; <code>disallowed-tools</code> is what removes capability.",
"User location is not shared, and a plain command has no isolation or capability control.",
"CLAUDE.md carries conventions, not invocable configured workflows."],
e:"Four requirements, four decisions. Shared &rarr; <code>.claude/skills/</code> in the project. Isolated context &rarr; <code>context: fork</code>. Cannot modify &rarr; <code>disallowed-tools</code>, since <code>allowed-tools</code> pre-approves rather than restricts. Prompts for a parameter &rarr; <code>argument-hint</code>. Option B is the trap most study guides fall into.",
n:"<strong>Verified against Anthropic docs.</strong> Most third-party guides &mdash; and likely the exam &mdash; describe <code>allowed-tools</code> as restricting capability. The official skills reference says it &ldquo;pre-approves tools for this turn only&hellip; Does not restrict which tools are available.&rdquo; If the exam offers only <code>allowed-tools</code>, pick it; in production use <code>disallowed-tools</code>.",
k:"Shared + isolated + restricted + parameterised = project skill, <code>context: fork</code>, <code>disallowed-tools</code>, <code>argument-hint</code>."},

{i:1435,d:4,t:"4.3",s:"SDE",diff:3,
q:"Your extraction must handle three document types with genuinely different layouts, and currently one prompt tries to serve all three. Accuracy is 78%, 91% and 62% by type.",
ask:"What is the right restructuring?",
a:["Classify the type first, then route to a prompt with layout-specific criteria and examples, adding an <code>unknown</code> class routed to review",
"Add all three layouts' instructions to the single prompt",
"Process every document with all three prompts and reconcile",
"Improve the schema's type constraints"],
c:0,
w:["Correct. Classify-then-route keeps each prompt focused and yields per-type accuracy metrics.",
"That is close to what is failing &mdash; three instruction sets competing for attention.",
"Triples the cost and creates a reconciliation problem with no principled tie-break.",
"Schema constraints govern structure, not layout interpretation."],
e:"When inputs fall into a small number of known classes needing genuinely different handling, classify then route. Each prompt stays focused, its examples stay relevant to that layout, and you gain the per-type accuracy metrics automation decisions require &mdash; which matters because a 62% type is exactly what an aggregate would hide. The <code>unknown</code> class prevents forcing an unfamiliar layout into the nearest fit.",
k:"Known input classes needing different handling &rarr; classify then route, with an <code>unknown</code> class routed to review."},

{i:1436,d:5,t:"5.2",s:"CSA",diff:3,
q:"An agent resolves 55% of contacts against an 80% target. It escalates straightforward returns while attempting complex policy exceptions autonomously.",
ask:"What is the most effective single intervention?",
a:["Define explicit escalation criteria covering both what to escalate and what to resolve, with few-shot examples of each",
"Raise the escalation threshold so fewer cases escalate",
"Lower the escalation threshold so more cases escalate",
"Add a second agent to review each escalation decision"],
c:0,
w:["Correct. Errors in both directions mean the boundary itself is undefined, so defining it fixes both.",
"Moving a one-dimensional threshold would reduce over-escalation while worsening the dangerous under-escalation.",
"The mirror problem &mdash; fewer risky autonomous attempts, more simple cases sent to humans.",
"A review agent doubles cost with no better basis, since the criteria are still undefined."],
e:"The diagnostic insight is that errors in <em>both</em> directions rule out a threshold problem &mdash; a threshold is one-dimensional and trades one error type for the other. Bidirectional miscalibration means the agent has no usable concept of complexity, so supply one: explicit criteria plus contrastive examples from both sides of the line.",
k:"Errors in both directions = undefined boundary, not a mis-set threshold. Define criteria and show both sides."},

{i:1437,d:1,t:"1.2",s:"MAR",diff:3,
q:"Every subagent succeeds. Reports on &ldquo;AI's impact on creative industries&rdquo; cover only visual art. Coordinator logs show subtasks: <em>AI in digital art</em>, <em>AI in graphic design</em>, <em>AI in photography</em>.",
ask:"What is the root cause?",
a:["The coordinator decomposed the topic too narrowly &mdash; music, literature and film were never assigned to anyone",
"The synthesis agent failed to detect coverage gaps",
"The web-search agent did not search thoroughly enough",
"The document-analysis agent filtered out non-visual sources"],
c:0,
w:["Correct. The log is decisive: all three subtasks are visual-domain, so the missing content was never requested.",
"Gap detection is a useful mitigation for a decomposition failure, not its cause.",
"Contradicted by the evidence &mdash; each agent returned relevant results for the subtopic it was given.",
"Also contradicted; the agents performed correctly on their inputs."],
e:"This mirrors a published sample question and teaches the diagnostic habit the exam rewards: when every component reports success but the outcome is incomplete, suspect the decomposition rather than the workers. Defences are enumerating the domain before subdividing, stating exclusions, and a coverage check at synthesis that names what was <em>not</em> examined.",
k:"All subagents succeed but coverage is incomplete &rarr; decomposition failure. Gaps are assignment failures."},

{i:1438,d:2,t:"2.3",s:"MAR",diff:3,
q:"A synthesis agent needs to verify claims while merging. Currently it returns control to the coordinator, which calls the search agent and re-invokes synthesis &mdash; adding 2&ndash;3 round trips and 40% latency. Analysis shows 85% are simple fact checks and 15% need real investigation.",
ask:"How do you reduce overhead while preserving reliability?",
a:["Give synthesis a narrow <code>verify_fact</code> tool for simple checks, keeping complex verification routed through the coordinator",
"Give synthesis full access to the web-search toolset",
"Batch all verification needs and return them to the coordinator at the end",
"Pre-fetch and cache extra context around every source"],
c:0,
w:["Correct. Least privilege sized to the 85% case, with the escalation path preserved for the tail.",
"Breaks separation of concerns and hands synthesis a large toolset outside its specialisation, degrading selection.",
"Introduces blocking dependencies &mdash; later synthesis steps often depend on facts verified earlier.",
"Speculative caching cannot predict which facts need checking and inflates context."],
e:"This mirrors a published sample question. The principle is least privilege sized to the common case: measure the distribution, grant a minimal scoped capability covering the bulk, preserve the coordinator-mediated path for the tail. Option B is the tempting trap &mdash; the blueprint repeatedly states that tools outside an agent's specialisation reduce selection reliability.",
k:"Grant a narrow tool for the common case; route the tail through the coordinator. Broad toolsets degrade selection."},

{i:1439,d:3,t:"3.3",s:"CGC",diff:3,
q:"A codebase has different conventions for React components, API handlers and database migrations. Tests are co-located with source throughout. You want conventions applied automatically.",
ask:"Which approach is correct?",
a:["<code>.claude/rules/</code> files with YAML <code>paths</code> frontmatter using glob patterns",
"Everything in the root CLAUDE.md",
"Skills in <code>.claude/skills/</code> for each convention set",
"A CLAUDE.md in every directory"],
c:0,
w:["Correct. Glob-based path rules activate on file match &mdash; automatic, and they handle co-located tests.",
"Relies on the model inferring applicability, and keeps all conventions in context always.",
"Skills are invoked manually or by auto-activation triggers; conventions should apply automatically to file work.",
"Directory files cannot express &ldquo;test files wherever they live&rdquo;, which is the co-location case."],
e:"This mirrors a published sample question. <code>paths</code> frontmatter with globs such as <code>**/*.test.tsx</code> loads a rule only when matching files are worked on. Two wins: correctness, since conventions cannot be misapplied to unrelated work; and efficiency, since unused rules cost no context. The co-located-tests detail is what rules out directory-level CLAUDE.md.",
k:"<code>.claude/rules/</code> + <code>paths</code> globs = automatic, file-scoped conventions that cross directory boundaries."},

{i:1440,d:4,t:"4.1",s:"CCI",diff:3,
q:"Your review criterion reads: <em>check that code comments are accurate.</em> It produces a 40% false-positive rate by flagging comments that are merely incomplete or dated.",
ask:"What is the correct rewrite?",
a:["Flag a comment only when it directly contradicts what the code does; do not flag incomplete, terse or stylistically dated comments",
"Check comment accuracy carefully and be more conservative in borderline cases",
"Flag comments only when highly confident they are wrong",
"Stop checking comments and focus on logic errors"],
c:0,
w:["Correct. A categorical criterion naming both what qualifies and what does not.",
"The blueprint specifically notes that generic guidance like &ldquo;be more conservative&rdquo; works worse than concrete categorical criteria.",
"Model self-assessed confidence is uncalibrated &mdash; one vague standard replaced by another.",
"Eliminates false positives by eliminating the check, losing genuine contradictions."],
e:"This is the blueprint's own worked example. The difference is that &ldquo;contradicts what the code does&rdquo; names a decidable property, while &ldquo;accurate&rdquo; does not. Note the trap in option B: asking for conservatism sounds like tightening, but it gives no new information about which cases qualify &mdash; it shifts the threshold arbitrarily rather than sharpening the boundary.",
k:"Explicit categorical criteria beat vague ones. State what qualifies <em>and</em> what does not. &ldquo;Be conservative&rdquo; is not a criterion."},

{i:1441,d:5,t:"5.3",s:"MAR",diff:3,
q:"A document-analysis subagent fails on PDFs three ways: corrupted sections, password protection, and the parser hanging on very large files. Any exception terminates it and returns an error, forcing the coordinator into routine error handling.",
ask:"Which architectural improvement is most effective?",
a:["Local recovery in the subagent for what it can handle, escalating only unresolvable errors with attempted steps and partial results",
"Coordinator pre-validation of every document before dispatch",
"Always returning partial results with a success status and error detail in metadata",
"A dedicated error-handling agent monitoring a shared failure queue"],
c:0,
w:["Correct. Handle errors at the lowest level capable of resolving them; escalate the rest with full context.",
"Pre-validation duplicates the parser's work and cannot predict a hang.",
"Reporting failure as success is the silent-suppression anti-pattern.",
"A separate error agent with direct channels to subagents breaks hub-and-spoke and adds no information."],
e:"This mirrors a published sample question. Each failure mode has a local remedy &mdash; skip corrupted sections and report reduced coverage, mark password-protected files as needing credentials, apply a timeout and chunk large files &mdash; and the subagent is best placed to apply them. What reaches the coordinator should be genuinely unresolvable, with attempted steps and partial results attached.",
k:"Recover locally where possible; escalate the unresolvable with attempted steps and partial results."},

{i:1442,d:1,t:"1.3",s:"AAT",diff:3,
q:"A coordinator's <code>allowedTools</code> is <code>[\"Read\", \"Grep\", \"Glob\"]</code>. Three subagent definitions exist and are well described, but the coordinator never delegates &mdash; it does everything itself, slowly.",
ask:"What is missing?",
a:["<code>\"Task\"</code> must be in the coordinator's <code>allowedTools</code>, since spawning a subagent is a tool call",
"The subagents must be registered in <code>.mcp.json</code>",
"<code>tool_choice</code> must be set to <code>any</code> to force delegation",
"Each subagent must list the coordinator in its own tool list"],
c:0,
w:["Correct. Without the spawning tool in its allow-list, delegation is impossible regardless of the prompt.",
"MCP configuration registers servers and their tools; agent definitions are separate.",
"Forcing a tool call cannot conjure a tool absent from the list.",
"Backwards &mdash; subagents do not invoke the coordinator."],
e:"A concrete configuration fact worth checking before rewriting prompts: when a coordinator &ldquo;does everything itself&rdquo;, look at the tool allow-list. The mirror rule also matters &mdash; subagents should normally <em>not</em> have the spawning tool, or they spawn their own children and flatten hub-and-spoke into a mesh with no single observable trace.",
n:"<strong>Naming note.</strong> The blueprint calls this tool <code>Task</code>, which is what the exam will use. Current Claude Code renamed it to the <code>Agent</code> tool after v2.1.62. The mechanics are unchanged: it must be in the spawning agent's tool list, and several calls in one turn run concurrently.",
k:"Coordinators need the spawning tool in their allow-list. Subagents should not have it, or hub-and-spoke becomes a mesh."},

{i:1443,d:2,t:"2.4",s:"DPT",diff:3,
q:"Your team needs a GitHub MCP server available to everyone on clone, using each developer's own credential.",
ask:"What is the correct configuration?",
a:["Define it in the project's <code>.mcp.json</code> with the token referenced as <code>${GITHUB_TOKEN}</code>",
"Define it in each developer's <code>~/.claude.json</code> with their token inline",
"Define it in <code>.mcp.json</code> with a shared team token committed to the repository",
"Document the configuration in CLAUDE.md for each developer to replicate"],
c:0,
w:["Correct. Project scope shares the definition; environment variable substitution keeps credentials out of version control.",
"Works, but every developer configures it independently and the team gets no shared definition.",
"Committing a credential is a security incident and destroys per-developer attribution.",
"CLAUDE.md carries instructions, not MCP server definitions."],
e:"Two facts combine. <strong>Scope</strong>: project <code>.mcp.json</code> is version-controlled and active on clone with no setup step. <strong>Secrets</strong>: <code>.mcp.json</code> supports <code>${VAR}</code> substitution in <code>command</code>, <code>args</code>, <code>env</code>, <code>url</code> and <code>headers</code>, with a <code>${VAR:-default}</code> form available. Note that an unset variable warns but still loads the server with the literal text, so document required variables.",
k:"Team MCP servers: project <code>.mcp.json</code> with <code>${ENV_VAR}</code>. Unset variables warn but load unexpanded."},

{i:1444,d:3,t:"3.4",s:"CGC",diff:3,
q:"You must restructure a monolith into microservices &mdash; dozens of files, several defensible service boundaries.",
ask:"What approach should you use?",
a:["Planning mode: explore the codebase, understand dependencies, and design the approach before making changes",
"Direct execution, working incrementally file by file",
"Direct execution with detailed up-front instructions",
"Direct execution, switching to planning mode if it becomes difficult"],
c:0,
w:["Correct. Large change, multiple viable approaches, architectural decisions &mdash; the textbook planning trigger.",
"Incremental execution without a plan risks expensive rework once a boundary decision proves wrong at file 30.",
"Detailed instructions presuppose you already understand the dependency structure, which exploration establishes.",
"Reactive &mdash; by the time it is obviously difficult, decisions have been made that constrain what remains."],
e:"This mirrors a published sample question. Planning mode suits architectural consequence, large scope and several defensible approaches, and it allows safe exploration before anything is committed. Write the plan to a file so each phase starts from the plan rather than the previous phase's full history &mdash; which also makes partial rollback possible.",
k:"Large scope + multiple approaches + architectural consequence &rarr; planning mode, plan written to a file, phased execution."},

{i:1445,d:4,t:"4.5",s:"SDE",diff:3,
q:"You must process submitted documents within a 30-hour SLA. The Batch API's processing window is up to 24 hours.",
ask:"What submission cadence gives adequate headroom?",
a:["Roughly every four hours, so queue wait plus the full processing window stays inside 30 hours",
"Once every 24 hours",
"Once every 30 hours to match the SLA",
"Whenever convenient, since batches usually complete in under an hour"],
c:0,
w:["Correct. Frequent submission bounds queue wait, leaving the 24-hour window comfortably inside the SLA.",
"A document arriving just after a submission waits nearly 24 hours before processing even starts &mdash; up to 48 in total.",
"Guarantees breaches, since processing has not started when the deadline arrives.",
"There is no latency SLA, so planning against typical completion is exactly what the guarantee does not cover."],
e:"The blueprint gives this planning example directly. The key insight is that total time is <strong>queue wait plus processing window</strong>, not processing alone &mdash; submission cadence controls the first term. Plan against the 24-hour maximum rather than the typical sub-hour completion, and handle expiry: unprocessed requests are marked <code>expired</code> and must be re-submitted by <code>custom_id</code>.",
k:"Total = queue wait + processing window. Shorten submission intervals to bound queue wait; plan against the maximum."},

{i:1446,d:5,t:"5.4",s:"CGC",diff:3,
q:"You resume <code>--resume refactor-auth</code> from yesterday. Overnight a colleague merged a refactor that moved and renamed several files the session had read. Claude begins editing based on the old structure.",
ask:"What is the best course of action?",
a:["Start a fresh session seeded with a structured summary, or explicitly tell the resumed session which files changed and have it re-read them",
"Resume again &mdash; the session re-reads files automatically",
"Ask Claude to re-read every file it touched yesterday",
"Continue and rely on Edit failing when the text no longer matches"],
c:0,
w:["Correct. Stale cached tool results are the hazard; either replace the context or explicitly invalidate what changed.",
"Resuming restores stored context as-is; nothing re-reads the filesystem for you.",
"Workable but incomplete &mdash; renamed and deleted files will not be found, and stale reasoning persists.",
"Edit failing is late and partial: a match can succeed in the wrong file, and by then several turns rest on a false model."],
e:"The blueprint notes that a new session with a structured summary can be <em>more</em> reliable than resuming with stale results. Session context is a cache of the world as it was when read, with no invalidation protocol. For a few known changes, name them and re-read; for structural change &mdash; renames, moves, deletions &mdash; a fresh session with an accurate summary is safer.",
k:"Resumed sessions carry stale tool results. Invalidate explicitly for a few files; restart with a summary for structural change."},

{i:1447,d:1,t:"1.5",s:"CSA",diff:3,
q:"Different MCP tools return timestamps as Unix epochs, ISO 8601 strings and <code>MM/DD/YYYY</code>. The agent frequently miscomputes date differences.",
ask:"Where is the cleanest place to fix this?",
a:["Normalise to a single format at the boundary before results reach the model &mdash; the blueprint's <code>PostToolUse</code> hook pattern",
"A system prompt section explaining all three formats and how to convert",
"A rule that the agent must call a <code>convert_date</code> tool before any comparison",
"Few-shot examples demonstrating correct date arithmetic"],
c:0,
w:["Correct. Deterministic mapping between known encodings belongs in code, applied once and centrally.",
"Asks the model to do reliable format detection and arithmetic on every call &mdash; mostly right, occasionally not.",
"Adds a round trip per comparison and depends on the agent remembering.",
"Examples cannot make format ambiguity go away; <code>03/04/2026</code> is genuinely ambiguous."],
e:"The blueprint gives date normalisation as the canonical boundary-transformation example. The general principle: do deterministic transformation in code and reserve the model's attention for judgement. A single mapping also means a fourth server with yet another encoding is a mapping entry rather than a re-teaching exercise.",
n:"<strong>Verified against Anthropic docs.</strong> The blueprint describes <code>PostToolUse</code> as the place for this, and that is the exam's answer. Anthropic's current hooks reference says <code>PostToolUse</code> fires <em>after</em> execution and cannot rewrite results &mdash; <code>updatedInput</code> is documented only for <code>PreToolUse</code> and <code>PermissionRequest</code>. In production, normalise inside the tool or MCP server.",
k:"Deterministic mapping belongs in code at the boundary, not in the model's per-call reasoning."},

{i:1448,d:2,t:"2.1",s:"CSA",diff:3,
q:"Your agent calls <code>get_customer</code> for order-related questions that <code>lookup_order</code> should handle. Both descriptions are one line and both mention &ldquo;retrieving account information&rdquo;.",
ask:"What is the first step?",
a:["Expand each description with input formats, example queries, edge cases and the boundary against the other",
"Add few-shot examples of correct selection to the system prompt",
"Add a routing classifier in front of the agent",
"Merge the two tools into one <code>get_account_data</code>"],
c:0,
w:["Correct. Descriptions are the primary selection mechanism &mdash; lowest effort, highest impact, and it targets the stated cause.",
"Few-shot adds tokens on every request without addressing why the descriptions overlap.",
"A routing layer is over-engineering for a metadata problem, adding latency and a component to maintain.",
"Merging is more effort than justified and produces a broader tool with its own problems."],
e:"This mirrors a published sample question, and the reasoning is worth memorising: descriptions are the model's primary selection mechanism, so minimal or overlapping descriptions produce unreliable selection. State what each does, its input format, example queries that should route to it, and the explicit boundary &mdash; &ldquo;use <code>lookup_order</code> for anything keyed by order number or status&rdquo;.",
k:"Fix ambiguous descriptions first: inputs, examples, edge cases, boundary. Classifiers are the over-engineering distractor."},

{i:1449,d:4,t:"4.3",s:"SDE",diff:3,
q:"You have three extraction schemas for three document types, and the model must always produce structured output rather than text.",
ask:"Which <code>tool_choice</code> setting is correct?",
a:["<code>{\"type\": \"any\"}</code>",
"<code>{\"type\": \"auto\"}</code>",
"<code>{\"type\": \"tool\", \"name\": \"extract_invoice\"}</code>",
"<code>{\"type\": \"none\"}</code>"],
c:0,
w:["Correct. <code>any</code> guarantees a tool call while leaving the model free to pick the right schema.",
"<code>auto</code> permits a text response, which breaks the guarantee.",
"Forcing one specific schema is wrong when the document type varies.",
"<code>none</code> forbids tools entirely."],
e:"The blueprint gives this precise guidance: use <code>any</code> to guarantee structured output when multiple schemas exist, and force a specific tool when there is one target schema. The distinction is whether the model still needs to choose. Note the caveat &mdash; <code>any</code> in a multi-turn loop prevents the text turn that yields <code>end_turn</code>, so set it per request rather than globally.",
k:"Several valid schemas &rarr; <code>any</code>. One known schema &rarr; force it by name. Never leave <code>any</code> set across a loop."},

{i:1450,d:5,t:"5.5",s:"SDE",diff:3,
q:"You auto-approve extractions above a 0.9 confidence threshold. You want to detect new error patterns appearing inside that auto-approved stream.",
ask:"Which technique applies?",
a:["Stratified random sampling of the high-confidence stream, checked against ground truth",
"Reviewing only the extractions that fell below the threshold",
"Raising the threshold to 0.95",
"Trusting the confidence scores, since they were calibrated"],
c:0,
w:["Correct. The blueprint prescribes stratified sampling to measure error rates in high-confidence extractions.",
"Below-threshold items already go to review and say nothing about the auto-approved stream.",
"Raising the threshold reduces automation volume without measuring what is approved.",
"Calibration drifts as document mixes, upstream formats and models change."],
e:"Confidence-based routing has a blind spot: you never inspect what you auto-approved, so a new failure mode inside the high-confidence band is invisible. Stratified sampling &mdash; across confidence bands and document types &mdash; closes it. Note the complementary signal: a very high approval rate in the review queue means the threshold is set too conservatively.",
k:"Sample the auto-approved stream stratified by band and type. Routing alone never inspects what it approved."}
);
