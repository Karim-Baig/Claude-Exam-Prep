QB.push(
{i:596,d:1,t:"1.1",s:"CSA",diff:2,
q:"You are asked whether an agent loop should have a maximum iteration count at all, given that <code>stop_reason</code> already controls termination.",
ask:"What is the correct position?",
a:["Yes &mdash; it is a circuit breaker against runaway loops caused by flaky tools or pathological states, but never the primary termination mechanism",
"No &mdash; <code>stop_reason</code> makes it redundant",
"Yes &mdash; and exhaustion should be treated as successful completion",
"No &mdash; caps prevent legitimate long tasks from completing"],
c:0,
w:["Correct. Both statements matter: keep the cap as a safety net, and never use it as the definition of done.",
"A flaky tool can produce an unbounded retry cycle that <code>stop_reason</code> never terminates.",
"Treating exhaustion as success reports a truncated task as complete.",
"A generously-sized cap does not obstruct legitimate work; it bounds pathological cases."],
e:"The blueprint names arbitrary iteration limits as an anti-pattern specifically <em>as the primary stopping mechanism</em>. As a circuit breaker they remain necessary &mdash; a tool timing out repeatedly can spin indefinitely. Set it generously, and when it fires, treat that as an incident: log it, return a clear failure, and escalate rather than reporting success.",
k:"Iteration caps are a circuit breaker, not a termination condition. Firing one is an incident, not a completion."},

{i:597,d:1,t:"1.2",s:"MAR",diff:2,
q:"You need to decide how many subagents to dispatch for a research topic with six natural sub-domains.",
ask:"What should drive the number?",
a:["The natural seams in the topic &mdash; six sub-domains suggests six subtasks, adjusted so sizes are comparable",
"A fixed number chosen for consistency across all topics",
"As many as the coordinator's context can accommodate",
"The number of specialised agent types available"],
c:0,
w:["Correct. Slice at natural seams, then balance sizes so no branch dominates latency or gets covered thinly.",
"A fixed count forces either over- or under-decomposition depending on the topic.",
"Context capacity bounds the design but should not determine the partition.",
"Agent types are a capability question, not a partitioning one &mdash; one type can be instantiated several times."],
e:"Decomposition follows the structure of the problem, not an arbitrary target. Six natural sub-domains suggests six subtasks; if one is far larger, sub-split it so branch sizes are comparable. Over-decomposition brings coordination overhead and fragmented synthesis; under-decomposition brings shallow coverage. Both are named risks.",
k:"Slice at natural seams, then balance sizes. Fixed counts force over- or under-decomposition."},

{i:598,d:1,t:"1.3",s:"AAT",diff:2,
q:"You want a subagent to be able to say &ldquo;I could not determine this&rdquo; rather than producing a confident guess.",
ask:"What makes this possible?",
a:["An output contract with an explicit representation for uncertainty, and quality criteria stating that admitting uncertainty is preferred to guessing",
"A lower temperature",
"A more capable model",
"An instruction to be honest"],
c:0,
w:["Correct. Give uncertainty a place in the output and state that using it is the preferred behaviour.",
"Temperature does not create a way to express uncertainty.",
"Capability does not help if the output format has no slot for &ldquo;unknown&rdquo;.",
"A general honesty instruction is vague and provides no mechanism."],
e:"This is the same principle as making extraction fields nullable: if the output format has no way to say &ldquo;I do not know&rdquo;, a confident answer is the only available response. Provide the slot &mdash; a <code>confidence</code> or <code>determination</code> field with an <code>undetermined</code> value plus a reason &mdash; and state in the quality criteria that using it is correct behaviour rather than failure.",
k:"Uncertainty needs a slot in the output contract and explicit permission to use it, or the model produces confidence."},

{i:599,d:1,t:"1.4",s:"CSA",diff:2,
q:"Your escalation volume doubles after a policy change that removed a category of autonomous resolution.",
ask:"What is the appropriate interpretation?",
a:["This is expected behaviour from the policy change; the question is whether the policy change was correct, not whether the agent is miscalibrated",
"The agent has become miscalibrated and needs new escalation criteria",
"The escalation criteria should be tightened to restore the previous volume",
"Sentiment analysis should be added to reduce escalations"],
c:0,
w:["Correct. Correctly implementing a policy that escalates more is not miscalibration.",
"The agent is following the new policy, which is what it should do.",
"Tightening criteria to hit a volume target means resolving cases the policy says must escalate.",
"Sentiment is an unreliable proxy and would not address a policy-driven change anyway."],
e:"Distinguish calibration problems from policy consequences. Miscalibration means the agent misjudges cases against a stable policy. Here the policy changed, and the agent's behaviour changed accordingly &mdash; correctly. The right conversation is whether the policy change was the intended trade, not whether to override it by tuning the agent against a volume metric.",
k:"Separate miscalibration from policy consequence. Do not tune an agent against a metric to undo a deliberate policy change."},

{i:600,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want to test that a <code>PreToolUse</code> enforcement hook works correctly.",
ask:"What should the test cover?",
a:["Both the blocked case and the allowed case, plus the hook's own failure behaviour when its dependencies are unavailable",
"Only the blocked case, since that is what the hook exists for",
"Only that the hook executes without error",
"The agent's behaviour after a block, rather than the hook itself"],
c:0,
w:["Correct. A hook that blocks everything passes a blocked-case-only test; you need all three.",
"A hook that blocks legitimate calls is as broken as one that allows illegitimate ones.",
"Executing without error says nothing about whether it decides correctly.",
"Agent behaviour after a block is worth testing too, but it does not verify the hook."],
e:"Enforcement hooks need three test categories: the violation is blocked, the legitimate call is allowed, and the hook fails closed when it cannot evaluate (policy service down). The third is the one most often missed and the one that matters during incidents &mdash; it is also where the block message and escalation path get exercised.",
k:"Test hooks three ways: blocks the violation, allows the legitimate call, fails closed when it cannot evaluate."},

{i:601,d:1,t:"1.6",s:"CGC",diff:2,
q:"You are deciding whether a multi-step task's steps should be separate prompts or one prompt with numbered steps.",
ask:"What favours separate prompts?",
a:["When each step's output should be validated before the next begins, and when a failed step should be re-run without redoing the others",
"When the steps are described in different documents",
"When there are more than three steps",
"When different people wrote each step's requirements"],
c:0,
w:["Correct. Validation checkpoints and independent re-execution are the substantive benefits of chaining.",
"Documentation structure says nothing about runtime characteristics.",
"Count alone is not the criterion.",
"Authorship is irrelevant."],
e:"Prompt chaining earns its extra round trips through focus, verifiability and cheap recovery: each step gets a dedicated prompt and full attention, its output can be validated before the next consumes it, and a bad step is re-run alone. One prompt with numbered steps is simpler and cheaper when the steps are small and the intermediate outputs need no checking.",
k:"Chain when you want validation between steps and independent re-execution. Otherwise one prompt is simpler."},

{i:602,d:1,t:"1.7",s:"AAT",diff:2,
q:"A long-running workflow's state file grows to include every intermediate artefact.",
ask:"What should it contain instead?",
a:["Phase completion markers plus references to artefacts stored elsewhere, keeping the state file small and fast to read",
"Every artefact inline for completeness",
"Only the current phase number",
"A summary regenerated after each phase"],
c:0,
w:["Correct. State should be small and authoritative; artefacts live in storage with references from the state.",
"Inlining large artefacts makes the state file expensive to read on every checkpoint.",
"A phase number alone leaves the resumed run without its inputs.",
"Regenerated summaries are lossy about exactly the specifics a resumed phase needs."],
e:"Separate state from artefacts. The state file records what has happened &mdash; phases complete, decisions taken, references to outputs &mdash; and stays small enough to read and write cheaply at every checkpoint. Artefacts live in files or object storage, retrieved on demand by whichever phase needs them. This mirrors the scratchpad discipline: structured, selectively readable, not one growing blob.",
k:"State file = completion markers, decisions and artefact references. Artefacts live in storage, fetched on demand."},

{i:603,d:2,t:"2.1",s:"AAT",diff:2,
q:"A tool is used correctly 95% of the time. The 5% of misuses all involve one specific input type.",
ask:"What does this suggest?",
a:["The description covers the common case but not that input type; add an example and an explicit statement about it",
"The tool should be split into two",
"The model needs a general instruction to be careful",
"The failure rate is acceptable and needs no action"],
c:0,
w:["Correct. A concentrated failure mode points at a specific gap in the description, which is cheap to fix.",
"Splitting may eventually be right, but the cheaper first fix is to describe the case properly.",
"General care instructions do not address a specific unhandled input type.",
"A concentrated, diagnosable, cheaply-fixable failure mode should be fixed."],
e:"Concentration is diagnostic. When misuse clusters on one input type, the description almost certainly does not address it &mdash; either the format is unstated or the applicability boundary for that case is unclear. Add an explicit statement and an example. Escalate to splitting the tool only if the clarification fails, which suggests the case genuinely belongs elsewhere.",
k:"Clustered tool misuse points at a specific description gap. Add a statement and an example before splitting."},

{i:604,d:2,t:"2.2",s:"AAT",diff:2,
q:"A tool's error message includes a suggestion: <em>try again with a valid customer ID.</em>",
ask:"What would make this better?",
a:["Stating what a valid customer ID looks like and echoing the invalid value received",
"Making the message shorter",
"Removing the suggestion to avoid leading the model",
"Adding an internal error code"],
c:0,
w:["Correct. Format plus the received value is what enables a corrected retry in one step.",
"Brevity is not the goal; actionability is.",
"Suggestions are valuable &mdash; the problem is that this one is not specific enough.",
"An internal code without a lookup adds nothing for the caller."],
e:"Validation messages should enable a corrected retry immediately. Naming the field, echoing the offending value and stating the expected format (&ldquo;expected an 8-digit customer ID such as 40218833; received ACME-CORP&rdquo;) means the retry has everything it needs. Where several fields failed, list them all so one corrected retry fixes everything.",
k:"Validation messages: field + received value + expected format, for every failing field."},

{i:605,d:2,t:"2.3",s:"CSA",diff:2,
q:"You have five specialist agents and are considering a sixth for a rare case handled today by escalation to humans.",
ask:"What should you weigh?",
a:["Whether the case volume justifies the added routing complexity and selection cost, against the human time it would save",
"Whether five is the maximum number of agents",
"Whether the sixth agent would use the same model",
"Whether the case is technically automatable"],
c:0,
w:["Correct. Every added agent taxes routing for the other five, so volume and saved human time are the deciding factors.",
"There is no fixed maximum; the cost is gradual.",
"Model choice is secondary to whether the agent should exist.",
"Technical feasibility is necessary but not sufficient &mdash; a rare case may not repay the complexity."],
e:"Adding an agent has an ongoing cost: it enlarges the coordinator's choice space, adds a description that must be distinguished from five others, and adds a component to maintain. For a rare case, escalation to a human may remain the better trade. Recall the earlier lesson &mdash; six rarely-chosen agents added selection noise for the two that mattered.",
k:"Each added agent taxes routing. Weigh case volume and saved human time against the ongoing complexity cost."},

{i:606,d:2,t:"2.4",s:"DPT",diff:2,
q:"Your <code>.mcp.json</code> and a developer's <code>~/.claude.json</code> both define a server with the same name.",
ask:"What is the practical concern?",
a:["Ambiguity about which definition is active, and a developer working with different capability than their colleagues without realising it",
"The API will reject duplicate server names",
"The project definition is automatically deleted",
"Both servers will connect and duplicate every tool"],
c:0,
w:["Correct. Silent divergence in capability is the real risk &mdash; behaviour nobody else can reproduce.",
"There is no such validation.",
"Nothing is deleted.",
"Duplicate connection is not the primary concern being tested."],
e:"The same shadowing problem as duplicate skill names. A developer whose user-scoped server shadows the project one has different capability from the team, so their agent behaves in ways colleagues cannot reproduce. Use distinct names for personal variants, and treat unexplained behavioural differences between developers as a configuration-scope question first.",
k:"Name personal MCP servers distinctly. Shadowing produces capability differences nobody else can reproduce."},

{i:607,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent must apply the same small change to twelve files it has already identified.",
ask:"What is the appropriate tool usage?",
a:["Twelve targeted <code>Edit</code> operations, each with a match unique within its file",
"One <code>Bash</code> command performing a global replace",
"Twelve <code>Write</code> operations replacing each file entirely",
"One <code>Edit</code> with a pattern matching all twelve"],
c:0,
w:["Correct. Targeted per-file edits produce a minimal, reviewable diff and cannot touch anything unintended.",
"A global replace can hit occurrences in files that were not identified, including comments and strings.",
"Whole-file rewrites produce large diffs and risk unintended changes to untouched content.",
"<code>Edit</code> operates on one file with a unique match; it is not a cross-file pattern tool."],
e:"Targeted edits keep the change surface exactly as large as intended. The uniqueness requirement forces each change to be specific, and the resulting diff shows only what was meant to change &mdash; which matters for review. Reach for a global replace only when you have inspected every match, and even then, per-file edits remain safer.",
k:"Apply known changes as targeted per-file edits. Global replaces reach files and occurrences you did not inspect."},

{i:608,d:3,t:"3.1",s:"CGC",diff:2,
q:"A convention exists but nobody has written it down, and Claude keeps violating it.",
ask:"What is the correct response?",
a:["Write it into CLAUDE.md or a path-scoped rule, converting a recurring correction into a one-time configuration change",
"Correct it in each conversation as it occurs",
"Accept the violations as unavoidable",
"Add it to the code review checklist"],
c:0,
w:["Correct. Documenting it once is the leverage that CLAUDE.md exists to provide.",
"Repeating a correction every session is the cost documenting removes.",
"Undocumented conventions are exactly what configuration is for.",
"A review checklist catches it after the fact, repeatedly, rather than preventing it."],
e:"Recurring corrections are a configuration signal. Each one is evidence that a convention exists in people's heads but not where the model can see it. Write it as a specific, checkable statement &mdash; and scope it with <code>paths</code> if it applies to particular file types, so it costs nothing on unrelated work.",
k:"A correction you give twice belongs in configuration. Recurring corrections are a documentation signal."},

{i:609,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill's output is short but it performs many tool calls internally.",
ask:"Does it need <code>context: fork</code>?",
a:["Yes &mdash; the intermediate tool results, not the final output, are what would accumulate in the main context",
"No &mdash; short output means low context impact",
"No &mdash; forking is only for skills that modify files",
"Yes &mdash; all skills should be forked by default"],
c:0,
w:["Correct. Context cost comes from everything that enters the window, and tool results dominate here.",
"Output length is not the measure; the tool results in between are.",
"Forking is about context isolation, not about file modification.",
"Forking has a cost too &mdash; apply it where verbose intermediate work justifies it, not universally."],
e:"Judge <code>context: fork</code> by total context consumed, not by output size. A skill making twenty tool calls fills the window with twenty results even if it concludes in one sentence. Forking keeps all of that in the isolated context and returns only the sentence &mdash; which is exactly the shape that benefits most.",
k:"Fork on total context consumed, not output size. Many tool calls with a short conclusion is the ideal fork candidate."},

{i:610,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want a rule to apply when editing any file in a package, including its tests and configuration.",
ask:"Which glob is appropriate?",
a:["<code>packages/checkout/**/*</code>, matching everything under that package at any depth",
"<code>packages/checkout/*.ts</code>",
"<code>**/checkout*</code>",
"<code>packages/*/**</code>"],
c:0,
w:["Correct. Everything under the package directory, at any depth, regardless of file type.",
"Restricts to TypeScript files at one level, missing tests, configuration and nested directories.",
"Matches anything named with a checkout prefix anywhere, which will catch unrelated files.",
"Matches every package, not just this one."],
e:"Globs should be as wide as the convention's scope and no wider. For a package-wide convention that covers all file types, <code>packages/checkout/**/*</code> is exact. Note that a directory-level CLAUDE.md inside <code>packages/checkout/</code> would achieve the same thing; the path rule is preferable when you also want it to cover files belonging to the package that live elsewhere.",
k:"Match the glob to the convention's exact scope. Too narrow misses files; too wide governs code it should not."},

{i:611,d:3,t:"3.4",s:"CGC",diff:2,
q:"Planning mode produces a plan whose first phase is &ldquo;explore the codebase further&rdquo;.",
ask:"What does this indicate?",
a:["Exploration during planning was insufficient, or the task genuinely needs a discovery phase that should be delegated to a subagent",
"Planning mode failed and should be abandoned",
"The plan is well-structured and should proceed as written",
"The model is stalling"],
c:0,
w:["Correct. Either planning was cut short, or discovery is genuinely a phase &mdash; in which case delegate it to protect context.",
"One phase needing exploration does not invalidate the plan.",
"Proceeding without asking why is how a plan built on thin understanding gets executed.",
"Identifying a genuine discovery need is useful output, not stalling."],
e:"A plan that opens with more exploration is telling you something. Either planning did not explore enough &mdash; continue it &mdash; or the task genuinely has a discovery phase, which should be delegated to a subagent so its verbose output never enters the main context. Then re-plan from the summary, which is the delegate-plan-execute sequence for large tasks.",
k:"A plan opening with exploration means planning was cut short, or discovery is a phase &mdash; delegate it and re-plan."},

{i:612,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to verify that an implementation handles concurrency correctly.",
ask:"What is the most effective specification?",
a:["Concrete test cases describing the interleaving and the expected outcome, since concurrency requirements are hard to convey in prose",
"An instruction to make the code thread-safe",
"A note in CLAUDE.md about concurrency mattering",
"A request to use locks everywhere"],
c:0,
w:["Correct. Concurrency is precisely where prose is ambiguous and concrete scenarios are not.",
"&ldquo;Thread-safe&rdquo; means different things for different invariants and is unverifiable as stated.",
"A general note gives no specific requirement for this code.",
"Prescribing a mechanism rather than a requirement invites over-locking and does not verify correctness."],
e:"Concurrency requirements are among the hardest to express in prose because they concern interleavings rather than states. Describe the scenario &mdash; two callers invoking this concurrently with these inputs &mdash; and the expected outcome, then make it a test. That specifies the requirement precisely and verifies it permanently, which is the test-driven iteration pattern applied where it helps most.",
k:"Specify concurrency with concrete interleaving scenarios and expected outcomes, as tests. Prose is ambiguous here."},

{i:613,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review must not comment on files the pull request author explicitly marked as out of scope.",
ask:"How should this be implemented?",
a:["Exclude those files when constructing the diff passed to the review, so they never enter the context",
"Instruct the model to skip files marked out of scope",
"Post all comments and filter them afterwards",
"Ask reviewers to ignore comments on those files"],
c:0,
w:["Correct. Filter at input construction &mdash; deterministic, and it saves the tokens too.",
"An instruction still pays the token cost and may not be honoured reliably.",
"Generating then discarding wastes the full cost of reviewing them.",
"Pushes filtering onto humans and adds noise that erodes trust."],
e:"Control the input rather than instructing about it. Filtering out-of-scope files before the diff is sent makes the exclusion deterministic, saves tokens, and sharpens attention on the files that matter. This is the same reasoning as excluding generated files and lockfiles: what is not in the context cannot be commented on.",
k:"Filter at input construction, not by instruction. Content that never enters the context cannot be commented on."},

{i:614,d:4,t:"4.1",s:"CCI",diff:2,
q:"You want to reduce false positives on a category without losing genuine findings in it.",
ask:"What is the most direct approach?",
a:["Narrow the category's definition with explicit inclusion and exclusion criteria, informed by reviewing the actual false positives",
"Lower the number of findings reported per run",
"Raise the severity threshold for that category",
"Ask the model to be more selective"],
c:0,
w:["Correct. Sharpening the definition removes the false-positive class while keeping the true positives.",
"Volume caps drop true and false positives indiscriminately.",
"Threshold changes trade recall for precision rather than fixing the definition.",
"&ldquo;More selective&rdquo; is the vague instruction the blueprint contrasts with explicit criteria."],
e:"Review the false positives to find their common shape, then write exclusion criteria that name it. If most false positives are defensive null checks flagged as redundant, state that defensive checks at trust boundaries are acceptable. That removes the class without touching genuine findings &mdash; which is what recording <code>detected_pattern</code> on each finding makes possible.",
k:"Review actual false positives, find their shape, write exclusion criteria naming it. Do not cap volume."},

{i:615,d:4,t:"4.2",s:"SDE",diff:2,
q:"Your few-shot examples are drawn from documents that all happen to be well-formatted.",
ask:"What should you add?",
a:["Examples from messy, imperfectly-formatted documents, since that is where extraction actually struggles",
"More well-formatted examples for consistency",
"Longer examples",
"Examples from a different domain"],
c:0,
w:["Correct. Aim examples at the failure region &mdash; clean documents were never the problem.",
"Reinforces behaviour that already works while spending context.",
"Length is not the lever; representativeness is.",
"Cross-domain examples teach the wrong conventions."],
e:"Examples should span the real input distribution, including its difficult tail. Well-formatted documents extract well without help; messy ones &mdash; inconsistent layouts, missing labels, values in unexpected positions &mdash; are where demonstration pays. Include correct handling of ambiguous and absent data in those examples, since that is where fabrication starts.",
k:"Draw examples from the messy tail, not the clean centre. Include correct handling of ambiguous and missing data."},

{i:616,d:4,t:"4.3",s:"SDE",diff:2,
q:"Downstream code needs to distinguish a value the document stated as zero from a value that was absent.",
ask:"How should the schema express this?",
a:["A nullable number, where <code>0</code> means the document stated zero and <code>null</code> means the value was absent",
"A number defaulting to 0 when absent",
"An empty string when absent",
"A separate boolean indicating presence, with the number always populated"],
c:0,
w:["Correct. Null and zero are different values, and a nullable field distinguishes them cleanly.",
"Defaulting absence to zero destroys the distinction entirely &mdash; and zero is often a meaningful business value.",
"An empty string in a numeric field mixes types and requires downstream parsing.",
"Workable but more complex, and it permits invalid combinations such as present-false with a non-zero value."],
e:"Zero and absent are semantically different, and conflating them is a classic data-quality defect &mdash; a stated tax amount of zero is a fact, while an absent tax field is unknown. A nullable typed field expresses both without sentinels. This mirrors the tool-result principle of distinguishing an empty result from a failure: make the states nameable.",
k:"Zero and absent are different. Use a nullable typed field; never default absence to zero."},

{i:617,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want retries to be cheaper than the original extraction where possible.",
ask:"What optimisation is available?",
a:["Retry only the fields that failed validation, rather than re-extracting the whole document, where the schema allows partial extraction",
"Use a smaller model for retries",
"Reduce <code>max_tokens</code> on retries",
"Skip the source document on retries"],
c:0,
w:["Correct. Targeted re-extraction of failing fields is smaller and more focused than a full re-run.",
"A weaker model on the case that already failed is the wrong direction.",
"Truncating the retry output causes a different failure.",
"Without the source the model cannot re-derive values and will fabricate."],
e:"Field-scoped retries are both cheaper and often more accurate, because the prompt can focus entirely on the failing fields with their specific validation errors and targeted examples. The caveat is that you must still validate the merged record afterwards &mdash; and where fields are interdependent (line items and totals), they should be retried together.",
k:"Retry failing fields rather than whole documents where possible &mdash; cheaper and more focused. Retry interdependent fields together."},

{i:618,d:4,t:"4.5",s:"SDE",diff:2,
q:"Your batch job's prompt is updated. The next run's results differ noticeably from the previous run's.",
ask:"What should you have done first?",
a:["Validated the new prompt against a labelled sample and compared results with the old prompt before the full run",
"Run the full batch and compare afterwards",
"Kept both prompts and averaged the results",
"Assumed the new prompt is better since it was an update"],
c:0,
w:["Correct. Prompt changes are quality changes and need controlled comparison on a fixed labelled set.",
"Discovering a regression after 40,000 documents wastes an entire run and up to 24 hours.",
"Averaging two extractions produces incoherent records.",
"Newer is not automatically better; that is what measurement establishes."],
e:"Treat prompt changes as you would code changes: validate against a fixed labelled set, compare with the current version at field and document-type level, and only then promote. The blueprint's guidance to iterate on a sample before large-scale processing applies to every change, not just the initial build.",
k:"Validate prompt changes against a fixed labelled set before a full batch run. Newer is not automatically better."},

{i:619,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want to know whether adding a cross-file review pass is worth its cost.",
ask:"What should you measure?",
a:["The number of genuine defects it finds that the per-file passes missed, against its added cost and false positives",
"The total number of comments it produces",
"How long it takes to run",
"How many files it examines"],
c:0,
w:["Correct. Incremental value is what justifies an added pass: unique true positives, weighed against cost and noise.",
"Total comments includes duplicates of what the per-file passes already found.",
"Runtime is a cost input, not a measure of value.",
"Coverage breadth says nothing about whether it found anything the other passes missed."],
e:"Judge an additional pass by its <em>incremental</em> contribution. Count the genuine defects it alone found, subtract the noise it added, and weigh against its cost. If it mostly duplicates the per-file findings, the remit or the input is wrong &mdash; give it the changed symbols and their consumers plus the prior findings, so its output is genuinely additive.",
k:"Measure additional passes by unique true positives minus added noise, not by total comments."},

{i:620,d:5,t:"5.1",s:"CAP",diff:2,
q:"A user's stated preference conflicts with a system-level policy in your system prompt.",
ask:"How should this be handled?",
a:["The system policy governs, and the agent should explain why the preference cannot be honoured",
"The user's preference governs, since they are the customer",
"The agent should ask the user to resolve the conflict",
"The agent should silently apply the policy without explanation"],
c:0,
w:["Correct. System-level policy outranks a conversational preference, and explaining maintains a good experience.",
"User preferences do not override operator policy.",
"The user cannot resolve a conflict with a policy they did not set.",
"Silent non-compliance leaves the user confused about why their request was not honoured."],
e:"Precedence runs from the system level down: operator policy in the system prompt governs, and conversational preferences apply within it. The explanation matters as much as the precedence &mdash; this is the same pattern as a hook blocking an action, where enforcement holds but the agent should still be able to say what happened and why.",
k:"System policy outranks conversational preference. Enforce it, and explain why rather than silently ignoring the request."},

{i:621,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent must decide whether to escalate a case where policy is clear but the customer disputes the policy itself.",
ask:"What should happen?",
a:["Apply the policy and explain it; escalate only if the customer explicitly requests a human or the dispute reveals a genuine policy gap",
"Escalate immediately because the customer is disputing",
"Make an exception to satisfy the customer",
"Repeat the policy until the customer accepts it"],
c:0,
w:["Correct. Disagreement with a clear policy is not a policy gap, and no valid trigger has fired yet.",
"Disputes are common; escalating every one is a form of over-escalation.",
"Granting exceptions is not within the agent's authority.",
"Repetition is unhelpful and does not address the customer's position."],
e:"Distinguish an ambiguous policy from a clear policy someone dislikes. The first is an escalation trigger; the second is not. Apply the policy, explain the reasoning clearly, and remain alert to two things: an explicit request for a human, which triggers immediate escalation, and a dispute that reveals the policy genuinely does not cover this case, which is a gap.",
k:"Disagreement with clear policy is not a policy gap. Apply and explain; escalate on an explicit request or a genuine gap."},

{i:622,d:5,t:"5.3",s:"MAR",diff:2,
q:"You want to know how often subagents recover locally from transient failures.",
ask:"Where should this be captured?",
a:["In the subagent's result metadata and your logs, as an operational signal that does not require coordinator action",
"By escalating every recovered failure to the coordinator",
"By counting coordinator-level retries only",
"It cannot be measured"],
c:0,
w:["Correct. Observability without escalation &mdash; the coordinator does not need to act, but you need to see the trend.",
"Escalating recovered failures floods the coordinator with non-decisions.",
"Coordinator retries miss everything the subagents handled themselves.",
"It is straightforwardly measurable through result metadata and logs."],
e:"Separate escalation from observability. Local recovery working correctly should not consume coordinator attention, but the rate is a leading indicator &mdash; a dependency increasingly needing two attempts is trending toward an outage. Record retries in result metadata and logs so the trend is visible without changing the control flow.",
k:"Record local recoveries as observability, not escalation. Rising retry rates are a leading indicator of an outage."},

{i:623,d:5,t:"5.4",s:"DPT",diff:2,
q:"You want a subagent's investigation to be reproducible later.",
ask:"What should it return alongside its conclusion?",
a:["The specific evidence &mdash; file paths, line references, commands run &mdash; so someone can retrace the reasoning",
"A confidence score",
"The full transcript of its exploration",
"The model version it used"],
c:0,
w:["Correct. Citable evidence is what makes a conclusion checkable and retraceable.",
"A score is an assertion, not evidence.",
"The full transcript defeats the context isolation the subagent provided.",
"Useful for reproducibility of the run but does not let a person retrace the reasoning."],
e:"Reproducibility comes from citable evidence, not from the transcript. File paths with line references, the specific commands or queries run, and quoted excerpts let a person verify the conclusion in minutes without the exploration noise. It also serves as a check on grounding &mdash; a conclusion that cannot cite anything specific may have come from priors rather than from the code.",
k:"Return citable evidence &mdash; paths, lines, commands, quotes. It makes conclusions checkable and reveals ungrounded answers."},

{i:624,d:5,t:"5.5",s:"SDE",diff:2,
q:"Your labelled validation set was built by one person applying their own interpretation of ambiguous cases.",
ask:"What is the risk?",
a:["The ground truth encodes one interpretation, so measured accuracy reflects agreement with that person rather than objective correctness",
"The set is too small",
"The set will become stale",
"Single-annotator sets cannot be used at all"],
c:0,
w:["Correct. On ambiguous cases, a single annotator's judgement becomes the definition of correct.",
"Size is a separate concern from annotation quality.",
"Staleness is a real but different issue.",
"They are usable &mdash; the limitation just has to be understood."],
e:"Ground truth is only as good as its annotation process. For genuinely ambiguous cases, a single annotator's interpretation becomes the standard, and your measured accuracy is agreement with that interpretation. Use multiple annotators on ambiguous cases, measure inter-annotator agreement, and document the interpretation rules &mdash; which often reveals that the extraction schema needs an <code>unclear</code> value rather than a forced choice.",
k:"Single-annotator ground truth measures agreement with one interpretation. Use multiple annotators on ambiguous cases."},

{i:625,d:5,t:"5.6",s:"MAR",diff:2,
q:"Two subagents cite the same figure but attribute it to different sources.",
ask:"What should the coordinator do?",
a:["Investigate which attribution is correct, since one is wrong and propagating either blindly damages the report's credibility",
"Include both attributions",
"Pick the more reputable-sounding source",
"Drop the attribution and state the figure alone"],
c:0,
w:["Correct. Conflicting attribution for the same figure is a factual error somewhere and needs resolving.",
"Listing two attributions for one figure signals a problem without resolving it.",
"Guessing by reputation is not verification.",
"Dropping attribution is exactly the provenance loss the discipline exists to prevent."],
e:"Note the distinction from conflicting <em>values</em>. Two different figures from two sources is a genuine disagreement to preserve and annotate. The same figure attributed to two different sources is an error &mdash; one subagent mis-attributed, or one source is quoting the other without saying so. That is worth resolving, and the supporting quotes usually settle it immediately.",
k:"Conflicting values &rarr; preserve and annotate. Conflicting attribution for one value &rarr; an error to resolve."},

{i:626,d:1,t:"1.1",s:"AAT",diff:2,
q:"Your agent must call a tool that takes 40 seconds to complete.",
ask:"What should the loop account for?",
a:["A timeout longer than the expected duration, and a defined behaviour if it is exceeded &mdash; retry, alternative, or escalate",
"Nothing special; long tools work like short ones",
"Splitting the tool into shorter calls regardless of whether that is meaningful",
"Reducing <code>max_tokens</code> to compensate"],
c:0,
w:["Correct. A timeout sized to the operation, plus a defined response when it fires.",
"A default timeout shorter than 40 seconds would fail every call.",
"Splitting may not be meaningful for an inherently long operation.",
"Output limits are unrelated to tool execution duration."],
e:"Long-running tools need a timeout matched to their real duration and, more importantly, a defined recovery. A timeout without a paired policy just turns a hang into a tidier hang. Decide in advance whether firing means retry (only if plausibly transient), route elsewhere, or proceed with annotated partial coverage &mdash; and make sure the agent is told which, in a well-formed error.",
k:"Size timeouts to the operation and always pair them with a recovery policy."},

{i:627,d:2,t:"2.2",s:"AAT",diff:2,
q:"A tool succeeds but returns data that fails your own downstream validation.",
ask:"Should the tool have returned an error?",
a:["No if the tool did its job correctly &mdash; but the validation failure should be surfaced to the agent so it can react, rather than being silently swallowed",
"Yes &mdash; any downstream failure means the tool failed",
"No &mdash; and the agent does not need to know",
"Yes &mdash; and the tool should perform your validation itself"],
c:0,
w:["Correct. The tool's contract was satisfied; the validation failure is a separate signal the agent still needs.",
"Downstream expectations are not the tool's contract.",
"Hiding the failure means the agent proceeds on data your system rejected.",
"Pushing consumer-specific validation into a shared tool couples it to one consumer."],
e:"Separate the tool's contract from your consumer-side expectations. The tool returned what it promised, so it did not fail. But your validation rejecting the data is information the agent needs &mdash; surface it as a distinct result so the agent can seek an alternative, request different parameters, or escalate. Silently swallowing it is the suppression anti-pattern in a different place.",
k:"Tool contract and consumer validation are separate. Surface validation failures to the agent rather than swallowing them."},

{i:628,d:3,t:"3.1",s:"CGC",diff:2,
q:"Your CLAUDE.md and a <code>.claude/rules/</code> file both mention testing, without contradicting each other.",
ask:"Is this a problem?",
a:["Not necessarily, but check for redundancy &mdash; duplicated guidance costs context twice and drifts as one copy is updated",
"Yes &mdash; testing guidance may appear in only one place",
"No &mdash; redundancy improves adherence",
"Yes &mdash; rules files may not mention topics covered in CLAUDE.md"],
c:0,
w:["Correct. Not an error, but duplication has a recurring cost and a drift risk.",
"There is no such restriction.",
"Redundancy costs context on every request and creates two things to keep in sync.",
"No such restriction exists."],
e:"A sensible division: CLAUDE.md carries the always-relevant testing philosophy, and a path-scoped rule carries the detailed conventions that apply when test files are being edited. That is complementary rather than redundant. What to avoid is the same guidance stated in both places, which costs context twice and drifts as soon as one copy is updated.",
k:"Split by scope: always-relevant guidance in CLAUDE.md, file-specific detail in a scoped rule. Avoid duplication."},

{i:629,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want the model to extract a value and also indicate where in the document it found it.",
ask:"How should the schema support this?",
a:["A field for the value and a companion field for its location or the surrounding source text",
"A single string combining the value and its location",
"An instruction to mention the location in a note",
"A separate extraction call asking where the value came from"],
c:0,
w:["Correct. Separate typed fields keep the value usable while carrying its provenance.",
"Combining them forces downstream parsing and breaks the value's type.",
"A free-text note is unstructured and cannot be validated or used programmatically.",
"A second call doubles cost and may not reproduce the same reasoning."],
e:"This is provenance applied to extraction: the value and the evidence for it travel together in separate typed fields. A source excerpt or page and line reference makes the value verifiable, supports human review by showing the reviewer exactly where to look, and helps diagnose systematic errors when a field is consistently read from the wrong region.",
k:"Extract value and source location as separate fields. Provenance makes values verifiable and errors diagnosable."},

{i:630,d:5,t:"5.4",s:"CGC",diff:2,
q:"You want to resume work tomorrow on a task you are halfway through today.",
ask:"What is the most reliable preparation?",
a:["Write current state, decisions and next steps to a file now, while the details are still accurate in context",
"Rely on resuming the session tomorrow",
"Write a summary tomorrow before continuing",
"Leave the session open overnight"],
c:0,
w:["Correct. Capture while the details are accurate; the file works whether or not you resume the session.",
"Resuming is reasonable if nothing changes, but it fails if files change overnight or the session is lost.",
"Tomorrow's summary would be written from a context you may not have.",
"An open session does not protect against a crash, a restart, or files changing."],
e:"Externalise while the context is fresh. A state file costs a minute today and works in every scenario: resume the session and it is a useful index; lose the session and it is your recovery; files change overnight and it seeds a fresh session with accurate context. It is also what a colleague would need if the work transfers.",
k:"Write state before stopping, not before resuming. The file covers resumption, recovery and handover alike."}
);
