QB.push(
{i:311,d:5,t:"5.1",s:"CSA",diff:3,
q:"In a long billing dispute, the customer stated a disputed amount of $1,247.83 and a transaction date of 14 March at turn 4. By turn 30, after several rounds of progressive summarisation, the agent refers to &ldquo;the disputed charge from earlier this year&rdquo; and quotes a rounded figure.",
ask:"What is the correct remedy?",
a:["Extract transactional facts into a persistent case-facts block maintained outside the summarised history and re-injected on every request",
"Increase the summarisation frequency so summaries stay shorter and fresher",
"Instruct the summariser to preserve all numbers and dates",
"Ask the customer to restate the amount and date periodically"],
c:0,
w:["Correct. The blueprint's prescribed remedy: pull load-bearing facts out of the summarised stream entirely.",
"More frequent summarisation compounds the loss &mdash; each pass degrades specifics further.",
"An instruction to a lossy process; it reduces the rate without eliminating it, and it fails silently.",
"Pushes the system's memory failure onto the customer."],
e:"Progressive summarisation degrades in a predictable direction: numeric values, percentages and dates get condensed into vague qualitative statements. The blueprint's remedy is to maintain a structured case-facts block &mdash; verified identity, order id, disputed amount, transaction date, decisions made &mdash; that your code owns and re-injects each request. It is small, exact, and never passes through the summariser.",
k:"Summarisation destroys numbers and dates first. Keep them in a persistent facts block outside the summarised history."},

{i:3115,d:5,t:"5.1",s:"MAR",diff:3,
q:"A synthesis agent receives 40 findings in one long input. Reviewers notice that findings from the middle of the list are consistently underweighted in the final report, while the first and last few are handled well.",
ask:"What explains this and what is the fix?",
a:["The lost-in-the-middle effect; place key findings at the beginning with explicit section headings, or split the synthesis into passes",
"The findings in the middle were lower quality",
"The output token limit truncated the middle of the response",
"The temperature was too high during synthesis"],
c:0,
w:["Correct. Models process the beginning and end of long inputs reliably and are less dependable about the middle.",
"There is no reason quality would correlate with position in the list.",
"Truncation removes the end of an output, not the middle of the input's influence.",
"Temperature affects variance, not positional attention."],
e:"The lost-in-the-middle effect is named explicitly in the blueprint: models reliably process the start and end of long inputs but may miss findings from the middle. Two remedies follow. Position deliberately &mdash; place the most important material at the beginning of aggregated data with explicit section headings so it is both early and structurally marked. Or split the work into passes so no single input is long enough for the effect to bite.",
k:"Lost in the middle: start and end are reliable, the middle is not. Position key material early with headings, or split into passes."},

{i:312,d:5,t:"5.1",s:"CSA",diff:2,
q:"A <code>lookup_order</code> tool returns 40 fields per order. The agent uses five. Across a 30-turn conversation this consumes a large share of the context window.",
ask:"What is the appropriate fix?",
a:["Trim the tool output to the relevant fields before it enters the context, typically in a <code>PostToolUse</code> hook",
"Summarise the conversation more aggressively once it grows long",
"Increase the context window",
"Instruct the agent to ignore the fields it does not need"],
c:0,
w:["Correct. The blueprint names trimming verbose tool outputs to relevant fields as the direct remedy.",
"Compaction is lossy and reactive; preventing the bloat is strictly better than compressing it afterwards.",
"Accommodates the waste rather than removing it, and the same conversation on a busier account refills the window.",
"The tokens are consumed the moment the result enters the context; the model cannot un-read them."],
e:"The blueprint identifies this precisely: tool outputs accumulate in context disproportionately to their relevance &mdash; 40 fields returned where 5 are needed. Trim at the boundary, in code, consistently. The ordering of good practice is worth internalising: prevent bloat at the source (trim results, scope return payloads, delegate verbose work), and only then reach for mitigation (compaction, summarisation, larger windows).",
k:"Prevent context bloat at the source. Trim tool output by field in a <code>PostToolUse</code> hook before it ever enters the context."},

{i:313,d:5,t:"5.1",s:"CAP",diff:3,
q:"A user gave a standing instruction in turn 2: <em>always reply in British English and never use bullet points.</em> By turn 35 the agent is writing American spellings in bulleted lists, despite full history being sent.",
ask:"Which fix is architecturally correct?",
a:["Move the standing instruction into the system prompt, or into a persistent preferences block your code re-injects each request",
"Repeat the instruction in a user message after every assistant reply",
"Reduce temperature so the model deviates less",
"Increase the context window so turn 2 remains in scope"],
c:0,
w:["Correct. Durable instructions belong in a stable, privileged position rather than buried in conversational history.",
"It works but bloats the transcript, costs tokens every turn, and clutters the visible conversation.",
"Temperature governs sampling variance, not the salience of an instruction 33 turns back.",
"Turn 2 is already in scope &mdash; the full history is being sent. Salience, not presence, is the problem."],
e:"Instruction persistence is a placement problem, not a capacity problem. The full history being present does not mean an early instruction competes successfully with 33 turns of intervening content &mdash; that is the lost-in-the-middle effect applied to instructions. Anything that must hold for the session belongs in the system prompt or in a persistent preferences block. Situational, turn-specific requests stay in the conversation.",
k:"Durable instruction &rarr; system prompt or persistent block. Presence in history is not the same as salience."},

{i:314,d:5,t:"5.1",s:"MAR",diff:2,
q:"You aggregate findings from four subagents into one input for synthesis. What structural choice most improves how reliably the synthesis uses them?",
ask:"Which choice matters most?",
a:["Place the most important findings first and mark every section with explicit headings",
"Sort the findings alphabetically by source name",
"Concatenate them in the order the subagents happened to finish",
"Interleave findings from different subagents to encourage cross-referencing"],
c:0,
w:["Correct. The blueprint prescribes placing key findings at the beginning of aggregated data with explicit section headings.",
"Alphabetical order is arbitrary with respect to importance.",
"Completion order is effectively random and puts important material wherever it lands.",
"Interleaving fragments each subagent's contribution and makes attribution harder to preserve."],
e:"Position-aware input design is an explicit blueprint skill. Because attention is strongest at the start and end of a long input, ordering is a lever you control: put the material that must not be missed at the front, and use explicit headings so each section is structurally marked rather than relying on the model to infer boundaries. This pairs with preserving claim-to-source structure so attribution survives aggregation.",
k:"Order aggregated input by importance and mark sections with explicit headings. Position is a design decision, not an accident."},

{i:315,d:5,t:"5.1",s:"AAT",diff:2,
q:"Which statement about the Messages API and conversation history is correct?",
ask:"Which is true?",
a:["The API is stateless: the full conversation history must be sent on every request",
"The API retains conversation state server-side for 30 minutes",
"Only the last ten messages need to be sent; earlier ones are cached",
"History is retained automatically when using the same API key"],
c:0,
w:["Correct. Every request carries the complete history, system prompt and tool definitions.",
"There is no server-side conversation state.",
"No such automatic windowing exists; you send what you want the model to see.",
"API keys authenticate; they do not maintain conversations."],
e:"Statelessness underpins several exam answers. It is why the agent loop must append the assistant turn and the <code>tool_result</code> user turn before re-sending; why tool definitions must accompany every request; and why context management is your responsibility rather than the platform's. To reduce the cost of re-sending a large stable prefix, use prompt caching with <code>cache_control</code> rather than omitting content.",
k:"Stateless API: full history + system prompt + tools on every request. Cut the cost with prompt caching, never with omission."},

{i:316,d:5,t:"5.1",s:"MAR",diff:2,
q:"Subagents return findings as prose paragraphs. By the time synthesis runs, dates and source attributions have become detached from the claims they belong to.",
ask:"What should be required of subagent output?",
a:["Structured records where each finding carries its own claim, source, supporting quote and date",
"Longer prose with more contextual detail",
"A summary paragraph at the top of each response",
"A confidence rating for the response as a whole"],
c:0,
w:["Correct. Structure is what keeps a value bound to its provenance through aggregation and synthesis.",
"More prose has the same defect at greater length &mdash; bindings still drift.",
"A summary compresses further, which loses attribution faster.",
"A whole-response rating says nothing about which claim came from which source."],
e:"Attribution is lost during summarisation unless it is carried structurally. Require each finding as a record: the claim, the source URL or document name, a supporting quote, and the date. Dates matter more than people expect &mdash; without them, a 2021 figure and a 2025 figure read as a contradiction rather than as a trend. Preserve those records through aggregation rather than flattening them into prose.",
k:"Findings are records: claim + source + quote + date. Prose lets those bindings drift; structure preserves them."},

{i:317,d:5,t:"5.2",s:"CSA",diff:2,
q:"Which of the following is a valid escalation trigger?",
ask:"Which one qualifies?",
a:["The customer explicitly asks to speak to a human",
"The customer's message contains angry language",
"The model rates its own confidence below 6 out of 10",
"The conversation has exceeded ten turns"],
c:0,
w:["Correct. An explicit request for a human is an immediate-escalation trigger.",
"Sentiment is explicitly named as an unreliable proxy for case complexity.",
"Self-rated confidence is explicitly unreliable &mdash; models are readily confidently wrong.",
"Turn count measures conversation length, not whether the case exceeds the agent's authority."],
e:"Memorise the three valid triggers: an explicit request for a human, a policy gap or exception the agent has no authority to decide, and demonstrated inability to make progress. Memorise the invalid ones too, because they are the recurring distractors: sentiment, self-rated confidence, turn count, and message length. Note the distinction between immediate escalation (explicit request) and attempt-to-resolve-first (issue is within documented scope).",
k:"Valid triggers: explicit human request &middot; policy gap or exception &middot; inability to progress. Not sentiment, not confidence, not turn count."},

{i:318,d:5,t:"5.2",s:"CSA",diff:3,
q:"A customer requests something the policy document simply does not address &mdash; it is neither permitted nor forbidden.",
ask:"What should the agent do?",
a:["Escalate, because a policy gap is outside the agent's authority to decide",
"Deny the request, since it is not explicitly permitted",
"Approve the request, since it is not explicitly forbidden",
"Apply the nearest analogous policy and proceed"],
c:0,
w:["Correct. Policy gaps and exceptions are a named escalation trigger; deciding one would be inventing policy.",
"Reads silence as prohibition &mdash; an invention of policy in the restrictive direction.",
"Reads silence as permission &mdash; the same invention in the expensive direction.",
"Applying an analogy to an uncovered case is precedent-setting, which the agent has no mandate to do."],
e:"The reasoning is about authority, not difficulty. The agent could produce a confident answer in any of these directions; what it lacks is the mandate to set precedent for a case the policy does not cover. Note the shape of the distractors &mdash; B, C and D all resolve the gap unilaterally, in three different directions. Escalation with a structured handoff is the only option that respects the boundary.",
k:"Policy silent &rarr; escalate. Interpreting silence in any direction is inventing policy the agent has no authority to set."},

{i:319,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent has attempted three different approaches to a technical issue and none has worked. The customer is still stuck.",
ask:"What should happen?",
a:["Escalate with a structured handoff describing what was attempted and why each approach failed",
"Continue attempting further approaches until something works",
"Tell the customer the issue cannot be resolved",
"Restart the conversation from the beginning"],
c:0,
w:["Correct. Demonstrated inability to make progress is a valid trigger, and the handoff must carry what was tried.",
"Unbounded attempts waste the customer's time and are unlikely to succeed after three failures.",
"Declaring it unresolvable without human involvement abandons the customer.",
"Restarting discards everything learned and repeats the failed approaches."],
e:"Inability to progress is the third valid trigger, and its handoff content is what makes it useful: listing what was attempted and why each failed saves the human agent from repeating the same three approaches. That is the same structured-handoff discipline as elsewhere &mdash; identifier, issue, verified facts, what was tried and the outcome, reason for escalation, recommended next action.",
k:"Inability to progress is a valid trigger. The handoff must list what was tried and why each attempt failed."},

{i:320,d:5,t:"5.2",s:"CSA",diff:3,
q:"A tool returns three customer records matching the name provided.",
ask:"What should the agent do?",
a:["Ask the customer for an additional identifier &mdash; order number, email or postcode &mdash; and re-query",
"Select the record with the most recent activity",
"Escalate to a human immediately",
"Proceed with all three records and let downstream tools disambiguate"],
c:0,
w:["Correct. The blueprint prescribes asking for additional identifiers rather than heuristic guessing.",
"A heuristic guess on identity risks acting on the wrong person's account.",
"Premature &mdash; the agent has a fast, safe way to resolve this within its own scope.",
"Carrying three candidate identities forward makes a wrong action more likely, not less."],
e:"This distinguishes ambiguity you should resolve from ambiguity you should escalate. Multiple matches are resolvable with one clarifying question, so ask &mdash; it is fast, safe and preserves first-contact resolution. Escalate instead when policy is silent, when the customer asks for a human, or when you cannot progress. Guessing identity heuristically is the one option that risks real harm.",
k:"Multiple matches &rarr; ask for another identifier. Resolve what a question can resolve; escalate what exceeds your authority."},

{i:321,d:5,t:"5.2",s:"CSA",diff:2,
q:"An issue is squarely within the agent's documented scope and policy clearly covers it. The customer has not asked for a human.",
ask:"What should the agent do?",
a:["Resolve it autonomously",
"Escalate anyway, to be safe",
"Ask the customer whether they would prefer a human",
"Resolve it but flag the case for human review afterwards"],
c:0,
w:["Correct. No valid trigger is present, so the attempt-to-resolve path applies.",
"Over-escalation depresses first-contact resolution and hands humans work they do not need to do.",
"Offering escalation unprompted invites it and depresses resolution rates unnecessarily.",
"Blanket post-hoc review of in-policy cases creates review load with no signal to justify it."],
e:"The blueprint distinguishes immediate escalation (explicit request for a human) from attempt-to-resolve (the issue is within scope). With no valid trigger present, resolving is correct. Note the symmetry with the over-escalation calibration questions: escalating too readily is as much a miscalibration as escalating too rarely, and both are fixed by explicit criteria plus examples from both sides.",
k:"No valid trigger &rarr; resolve. Over-escalation is a miscalibration too, not a safe default."},

{i:322,d:5,t:"5.3",s:"MAR",diff:3,
q:"A web-search subagent times out. You are designing what it returns to the coordinator.",
ask:"Which response best enables intelligent recovery?",
a:["Structured error context: the failure type, the query attempted, any partial results gathered, and viable alternatives",
"Retry internally with exponential backoff, then return a generic &ldquo;search unavailable&rdquo; status",
"Catch the timeout and return an empty result set marked as success",
"Propagate the timeout to a top-level handler that terminates the entire workflow"],
c:0,
w:["Correct. The coordinator can then choose between retrying with a modified query, an alternative route, or annotated partial coverage.",
"Backoff is reasonable, but the generic status discards the context needed for the coordinator's decision.",
"Masking failure as success makes the gap invisible and it surfaces later as a silent omission.",
"Aborting the whole workflow because one subagent timed out discards everything that succeeded."],
e:"This mirrors a published sample question, and the distractors are a tour of the three named anti-patterns: generic status hiding context, silent suppression, and whole-workflow abort. Structured error context turns a dead end into a decision. Note that internal backoff is not itself wrong &mdash; local recovery for transient failures is correct &mdash; what is wrong is collapsing the outcome into an uninformative status afterwards.",
k:"Structured error context: type, attempt, partial results, alternatives. Anti-patterns: generic status, silent suppression, whole-workflow abort."},

{i:323,d:5,t:"5.3",s:"MAR",diff:2,
q:"A search subagent completes successfully but finds no matching documents.",
ask:"How should this be reported?",
a:["As a successful result with an empty result set, distinguishable from an access failure",
"As an error, since the coordinator received nothing useful",
"As a success with fabricated placeholder results to keep the pipeline moving",
"As a timeout, so the coordinator retries"],
c:0,
w:["Correct. The blueprint explicitly requires distinguishing access failures from valid empty results.",
"Classifying a valid finding as an error triggers pointless retries and misrepresents what happened.",
"Fabrication corrupts the entire downstream analysis.",
"Misreporting the failure type causes exactly the wrong recovery."],
e:"&ldquo;I searched and found nothing&rdquo; is a finding; &ldquo;I could not search&rdquo; is a failure. They demand opposite responses &mdash; the first should be reported and reflected in coverage annotation, the second should trigger a recovery decision. Include the query in the successful empty response so the coordinator can judge whether the search was well-formed, and consider whether a reformulated query is worth trying.",
k:"Empty result = success with an empty set (echo the query). Access failure = error with context. Never conflate them."},

{i:324,d:5,t:"5.3",s:"MAR",diff:2,
q:"Which pair of behaviours are both named anti-patterns in multi-agent error handling?",
ask:"Which pair is it?",
a:["Silently suppressing a failure, and aborting the whole workflow because one component failed",
"Retrying transient failures locally, and returning partial results",
"Annotating coverage gaps, and escalating unresolvable errors",
"Distinguishing empty results from access failures, and including partial results"],
c:0,
w:["Correct. These are the two anti-patterns named explicitly in the blueprint.",
"Both are recommended practices, not anti-patterns.",
"Both are recommended practices.",
"Both are recommended practices."],
e:"The two anti-patterns bracket the correct behaviour from opposite sides. Silent suppression under-reacts &mdash; the failure becomes invisible and surfaces as a silent gap in the output. Whole-workflow abort over-reacts &mdash; one recoverable failure destroys everything that succeeded. The correct middle is local recovery where possible, structured propagation of what cannot be recovered, and coverage annotation so gaps are visible.",
k:"Two anti-patterns: silent suppression (under-reacting) and whole-workflow abort (over-reacting). Correct: local recovery, structured propagation, annotation."},

{i:325,d:5,t:"5.3",s:"MAR",diff:3,
q:"Your final report reads as complete and authoritative, but one subtopic rests on a single source because the second search agent failed.",
ask:"What should the synthesis include?",
a:["Explicit coverage annotation stating which sections are fully supported and which are partial, with the reason",
"A generic disclaimer that the report may be incomplete",
"Nothing &mdash; the reader should assume any report may have gaps",
"Removal of the under-supported subtopic"],
c:0,
w:["Correct. The blueprint prescribes annotating coverage: what is well supported versus where gaps remain.",
"A blanket disclaimer applies equally to good and bad sections, so it carries no information and gets ignored.",
"Leaves the reader to calibrate trust against confident prose that does not deserve it.",
"Silently dropping the subtopic is the same invisibility problem in a different form."],
e:"Reports that mix complete and incomplete coverage without saying so are worse than reports that admit the gap, because readers calibrate trust on the confident tone. Per-section annotation &mdash; <em>Music: partial coverage, secondary search agent timed out after two attempts</em> &mdash; travels with the content and tells the reader exactly where to be sceptical. This depends on structured error propagation: the coordinator can only annotate what the subagents told it.",
k:"Annotate coverage per section, with the reason. Blanket disclaimers carry no information; silent gaps mislead."},

{i:326,d:5,t:"5.4",s:"DPT",diff:3,
q:"Two hours into a codebase investigation, Claude's answers become vague and it begins referring to &ldquo;typical patterns in services like this&rdquo; rather than to the specific classes it read earlier.",
ask:"What does this indicate?",
a:["Context degradation in a long session &mdash; specifics have been crowded out or summarised away",
"The model is hallucinating and should be replaced with a larger one",
"The temperature drifted upward during the session",
"The system prompt was lost partway through"],
c:0,
w:["Correct. The blueprint describes this symptom precisely: unstable answers referring to typical patterns instead of specific classes.",
"It is not primarily a capability problem; the specifics are no longer available or salient.",
"Temperature does not drift; it is set per request.",
"System prompts are re-sent on every request by construction."],
e:"Learn to recognise this symptom, because the exam describes it in these terms. When a long session starts answering from general knowledge rather than from what it actually read, the specific material has been lost. The remedy is preventive: write key findings, decisions and exact file paths to a scratchpad file <em>as you go</em>, so they survive any context boundary. Then compact, or start fresh seeded with that file.",
k:"Generic, pattern-level answers late in a session = context degradation. Externalise specifics to a scratchpad before it happens."},

{i:327,d:5,t:"5.4",s:"DPT",diff:2,
q:"You need to preserve specific findings &mdash; file paths, function names, measured timings &mdash; across a context boundary.",
ask:"Which mechanism is most reliable?",
a:["A scratchpad file the agent writes findings to and can re-read on demand",
"Repeating the findings in each new message",
"Relying on <code>/compact</code> to preserve important details",
"Increasing the context window so no boundary is reached"],
c:0,
w:["Correct. A file survives compaction, crashes and session boundaries, and can be re-read selectively.",
"Repetition costs tokens on every turn and is itself subject to the next compaction.",
"Compaction is exactly where specific values get condensed into vague statements.",
"Postpones the boundary rather than surviving it, and long investigations outgrow any window."],
e:"The blueprint names scratchpad files as the mechanism for preserving key findings across context boundaries. The discipline is to write as you go rather than when you notice the context filling &mdash; by then the details may already be degraded. A good scratchpad holds findings with exact paths and values, decisions with rationale, and the open questions that remain, which also makes it the seed for a fresh session.",
k:"Scratchpad files preserve specifics across context boundaries. Write as you go, not when the window fills."},

{i:328,d:5,t:"5.4",s:"DPT",diff:2,
q:"An investigation requires answering six specific questions about an unfamiliar codebase, each needing extensive searching.",
ask:"How should this be structured to protect the main context?",
a:["Spawn a subagent per question and keep high-level coordination in the main session, receiving only the answers",
"Answer all six sequentially in the main session",
"Answer the three most important questions and skip the rest",
"Increase the context window and answer all six in the main session"],
c:0,
w:["Correct. The blueprint prescribes spawning subagents for specific questions while keeping coordination in the main agent.",
"Six extensive searches in one context is exactly the accumulation that causes degradation.",
"Discards a third of the required work to work around a solvable constraint.",
"A larger window still fills with six searches' worth of output."],
e:"Delegation for context isolation is the primary defence for investigations. Each subagent runs its own searches in its own window and returns only the answer with supporting evidence; the verbose exploration dies with it. The main session keeps the plan, the accumulated answers and the coordination. Scope each subagent's return payload tightly, or the answers themselves will refill the parent context.",
k:"Delegate each investigation question to a subagent; keep coordination in the main session. Scope the return payloads tightly."},

{i:329,d:5,t:"5.4",s:"CGC",diff:2,
q:"You are midway through a long task and need breathing room in the context window, without losing the thread.",
ask:"What is the correct sequence?",
a:["Write current findings and remaining steps to a file, then run <code>/compact</code> or start a fresh session seeded with that file",
"Run <code>/compact</code> immediately and continue",
"Delete the earliest messages manually",
"Keep going and accept degradation"],
c:0,
w:["Correct. Externalise first so nothing specific is lost, then reduce.",
"Compacting before externalising is where the exact paths and values get summarised into vagueness.",
"Position-based deletion removes information by age rather than relevance &mdash; the sliding-window mistake.",
"Accepts a known degradation pattern on work that still has to be correct."],
e:"The safe order is always <strong>externalise, then reduce</strong>. <code>/compact</code> is a legitimate mid-session tool for reclaiming room, but it is lossy in a specific direction: numbers, dates and paths go first. Writing them to a file beforehand makes the compaction safe, and gives you an artefact that survives a crash and can seed a fresh session with full headroom.",
k:"Externalise, then compact. <code>/compact</code> is lossy about exactly the specifics you will need."},

{i:330,d:5,t:"5.4",s:"AAT",diff:3,
q:"A long-running agent crashes after completing 7 of 12 phases and restarts from phase 1.",
ask:"What was missing?",
a:["Structured state persistence recording which phases completed and their outputs, so the restart resumes from phase 8",
"A longer timeout to prevent the crash",
"A larger context window",
"An instruction telling the agent to remember completed work"],
c:0,
w:["Correct. Durable checkpointing is what enables crash recovery; in-memory context does not survive process death.",
"Reduces crash frequency without changing what happens when one occurs.",
"Same objection &mdash; helps the run finish, provides no recovery.",
"Nothing survives to remember from; the process died."],
e:"The blueprint names structured state persistence for crash recovery. Checkpoint after each phase: which phases are complete, their outputs or artefact paths, and what remains. On restart, read the state and resume at the first incomplete phase. Pair with idempotent phases, so a phase interrupted mid-execution can safely re-run without duplicating side effects.",
k:"Crash recovery = durable checkpoints + idempotent phases. Neither is sufficient alone."},

{i:331,d:5,t:"5.4",s:"DPT",diff:2,
q:"Before spawning the next phase's subagents, what should the coordinator do with the current phase's results?",
ask:"Which practice is prescribed?",
a:["Summarise the key findings, so the next phase's delegations carry a compact accurate brief rather than raw accumulated output",
"Pass the complete raw output of every previous subagent into each new delegation",
"Discard the previous results entirely to keep contexts clean",
"Store the results and pass only a reference to them"],
c:0,
w:["Correct. The blueprint prescribes summarising key findings before spawning next-phase subagents.",
"Raw accumulation inflates every subsequent delegation and buries the relevant parts.",
"Discarding results means the next phase repeats work and loses continuity.",
"A reference resolves to nothing &mdash; subagents inherit no context and cannot follow it."],
e:"There is a real tension here worth holding: summarising loses specifics, but passing raw accumulated output is unaffordable at scale. The resolution is to summarise <em>selectively</em> &mdash; preserve the exact values, paths and source attributions that the next phase needs, and compress only the narrative around them. Where a subagent needs a full document, pass the document; where it needs a conclusion, pass the conclusion with its evidence.",
k:"Summarise between phases, but preserve exact values, paths and attributions. Compress narrative, never the load-bearing specifics."},

{i:332,d:5,t:"5.5",s:"SDE",diff:3,
q:"Your extraction pipeline reports 97% overall accuracy. Stakeholders want to automate approval entirely. Spot checks reveal that handwritten forms are extracted correctly only 61% of the time, but they are a small share of volume.",
ask:"What does this illustrate?",
a:["Aggregate metrics mask poor performance on specific document types; accuracy must be validated by document type and field before automating",
"97% is not high enough for automation and should be improved to 99%",
"Handwritten forms should be excluded from the pipeline permanently",
"The spot check sample was too small to be meaningful"],
c:0,
w:["Correct. The blueprint states that aggregate metrics can mask poor performance on specific document types or fields.",
"Raising the aggregate would still hide the handwritten-form problem behind the volume of easy documents.",
"An overreaction that discards a legitimate input class rather than routing it appropriately.",
"Dismissing the finding rather than investigating it &mdash; a 36-point gap warrants attention, not scepticism about the sample."],
e:"The blueprint is explicit: aggregate accuracy masks segment-level failure. Before automating, break accuracy down by document type <em>and</em> by field, because both dimensions hide problems &mdash; a pipeline can be 97% accurate overall while being 61% on handwritten forms, or while being 99% on vendor names and 70% on tax amounts. Automate the segments that are validated as stable and route the rest to human review.",
k:"Segment before you automate: accuracy by document type and by field. Aggregates hide the segments that will hurt you."},

{i:333,d:5,t:"5.5",s:"SDE",diff:3,
q:"You auto-approve extractions above a confidence threshold and want to detect new error patterns appearing in that auto-approved stream.",
ask:"Which technique applies?",
a:["Stratified random sampling of the high-confidence stream, reviewed against ground truth",
"Reviewing only the extractions that fell below the threshold",
"Increasing the confidence threshold",
"Trusting the confidence scores, since they were calibrated once"],
c:0,
w:["Correct. The blueprint prescribes stratified random sampling to measure error rates in high-confidence extractions.",
"Below-threshold items are already going to review; they tell you nothing about the auto-approved stream.",
"Reduces automation volume without measuring whether the remaining approvals are correct.",
"Calibration drifts as document mixes, upstream formats and models change; it needs ongoing measurement."],
e:"Confidence-based routing has a blind spot: you never look at what you auto-approved, so a new failure mode inside the high-confidence band is invisible. Stratified random sampling closes it &mdash; sample across confidence bands and document types, review against ground truth, and watch for drift. Note that this complements rather than replaces routing low-confidence items to review; you need both.",
k:"Sample the auto-approved stream, stratified by band and document type. Routing alone never inspects what it approved."},

{i:334,d:5,t:"5.5",s:"SDE",diff:2,
q:"You want to know which extracted fields are reliable enough to automate and which need review.",
ask:"What should you measure?",
a:["Field-level accuracy against a labelled validation set, segmented by document type",
"Overall document-level accuracy",
"The model's average self-reported confidence per field",
"The proportion of extractions that pass schema validation"],
c:0,
w:["Correct. The blueprint prescribes field-level confidence calibration using labelled validation sets.",
"Document-level accuracy hides which specific fields are failing.",
"Self-reported confidence is uncalibrated until measured against ground truth &mdash; it is the thing to be validated, not the validation.",
"Schema validation catches syntax errors only; a well-formed wrong value passes."],
e:"Three ideas combine here. Measure at <strong>field level</strong>, because fields differ enormously in difficulty. Measure against <strong>labelled ground truth</strong>, because self-reported confidence is exactly what needs calibrating. And <strong>segment by document type</strong>, because the same field can be reliable on typed invoices and unreliable on scans. Only then can you set per-field, per-type automation thresholds with known error rates.",
k:"Measure field-level accuracy against labelled data, segmented by document type. Schema-pass rate is not accuracy."},

{i:335,d:5,t:"5.5",s:"SDE",diff:2,
q:"Which extractions should be routed to human review?",
ask:"Which routing rule is most defensible?",
a:["Those with low calibrated confidence, ambiguous source material, or fields whose validated accuracy is below the automation bar",
"A fixed random 10% of all extractions",
"Only those that fail schema validation",
"Those from the largest documents"],
c:0,
w:["Correct. The blueprint prescribes routing low-confidence or ambiguous-source extractions to human review.",
"Random sampling measures quality but does not catch the specific items that need attention. It is a complement, not a routing rule.",
"Schema failures are only the syntax errors; well-formed wrong values would sail through.",
"Document size is a poor proxy for extraction difficulty."],
e:"Route on signals correlated with error: calibrated low confidence, source material that is genuinely ambiguous (poor scan quality, conflicting values on the page), and fields whose measured accuracy for that document type falls below the automation bar. Combine with stratified sampling of the auto-approved stream, which is how you catch failure modes the routing signals are missing.",
k:"Route on calibrated confidence, source ambiguity and measured per-field accuracy. Sample the rest to catch what routing misses."},

{i:336,d:5,t:"5.6",s:"MAR",diff:3,
q:"Two credible sources give different figures for the same metric: a government report says 40% growth, an industry analysis says 12%.",
ask:"How should the analysis agent handle it?",
a:["Complete its analysis carrying both values, annotated as conflicting with full source attribution, and pass the reconciliation decision to the coordinator",
"Choose the more authoritative source and footnote the discrepancy",
"Stop and escalate to the coordinator before continuing any analysis",
"Report both without marking them as conflicting and let synthesis decide"],
c:0,
w:["Correct. Complete your scope without blocking, preserve both with attribution, and route the reconciliation to the layer with broader context.",
"Silently resolving a material conflict inside a subagent hides a consequential judgement in the wrong place.",
"Blocking is unnecessary &mdash; the analysis can complete &mdash; and over-involves the coordinator in routine work.",
"Unmarked conflicting numbers are the worst outcome: downstream consumers cannot tell there is a disagreement."],
e:"This mirrors a published sample question. Three principles combine: complete your own scope rather than blocking; preserve rather than resolve material conflicts at the leaf level; and make the conflict machine-readable. Also check whether the figures genuinely conflict &mdash; different measurement years, definitions or populations explain many apparent contradictions, which is precisely why findings must carry dates and definitions.",
k:"Conflicts: don't pick, don't hide, don't block. Annotate with attribution and dates; escalate reconciliation to the coordinator."},

{i:337,d:5,t:"5.6",s:"MAR",diff:2,
q:"A report cites a 40% figure from 2021 and a 12% figure from 2025 for the same metric, presenting them as contradictory.",
ask:"What is most likely happening?",
a:["The figures may reflect a real change over time rather than a contradiction; missing dates caused the misreading",
"One of the sources is fabricated",
"The synthesis agent has a bug in its comparison logic",
"The metric definition is ambiguous and both figures are meaningless"],
c:0,
w:["Correct. The blueprint prescribes including dates precisely so temporal differences are not misread as contradictions.",
"An unwarranted accusation; the far more common explanation is a temporal gap.",
"The synthesis agent worked correctly with the information it was given &mdash; which lacked dates.",
"Possible in principle, but the four-year gap is the obvious first explanation."],
e:"The blueprint calls out including publication and collection dates for correct temporal interpretation. Without dates, a genuine trend looks like a disagreement, and a genuine disagreement looks like a trend. Require dates in every finding record alongside the claim, source and quote &mdash; and where definitions differ between sources, capture those too, since definitional differences are the second most common cause of false conflicts.",
k:"Always carry dates with figures. Temporal differences read as contradictions when dates are missing."},

{i:338,d:5,t:"5.6",s:"MAR",diff:2,
q:"A report merges findings from three subagents into flowing prose. Reviewers cannot determine which source supports which claim.",
ask:"Where did attribution break down?",
a:["Attribution was lost during aggregation because claim-to-source mappings were not carried as structured data",
"The subagents did not consult enough sources",
"The report is too long for reviewers to read carefully",
"The reviewers should ask the coordinator to re-run the searches"],
c:0,
w:["Correct. Attribution is lost during summarisation unless the claim-to-source mapping is preserved structurally.",
"Source quantity is not the issue; traceability is.",
"Length is not why a specific claim cannot be traced to a specific source.",
"Shifts the cost to reviewers and cannot reconstruct which source supported which sentence."],
e:"Provenance must be structural to survive. Require subagents to output claim-to-source mappings &mdash; the claim, the source URL or document name, a supporting quote, the date &mdash; preserve those records through aggregation, and render them with inline attribution rather than flattening into prose. Flattening is where the mapping dies, so the discipline has to hold at every step, not just at collection.",
k:"Provenance is structural: claim &rarr; source &rarr; quote &rarr; date, preserved end to end. Prose flattening destroys it."},

{i:339,d:5,t:"5.6",s:"MAR",diff:3,
q:"A report contains both well-corroborated findings and a few claims resting on a single disputed source.",
ask:"How should the report be structured?",
a:["Separate stable, well-supported findings from disputed ones, and state the nature of the dispute",
"Present everything with equal confidence for a consistent tone",
"Omit the disputed claims entirely",
"Present the disputed claims first, since they are most interesting"],
c:0,
w:["Correct. The blueprint prescribes structuring reports to separate stable findings from disputed ones.",
"Uniform confidence misrepresents the evidence and misleads the reader about where to be sceptical.",
"Omission removes information the reader may need, and hides that a dispute exists.",
"Prominence is not the organising principle; evidential status is."],
e:"Readers make decisions from reports, so the report must convey not only what was found but how firmly it is established. Separating stable from disputed findings &mdash; and stating the nature of each dispute, with both values and their sources and dates &mdash; lets the reader weight them appropriately. This pairs with coverage annotation, which conveys where evidence is thin rather than merely contested.",
k:"Separate stable from disputed findings and state the nature of the dispute. Uniform confidence misrepresents the evidence."},

{i:340,d:5,t:"5.6",s:"MAR",diff:2,
q:"Your report mixes quarterly financial figures, industry news summaries and benchmark results in a single prose narrative.",
ask:"What should change?",
a:["Render by content type &mdash; financial data as tables, news as prose, technical findings as structured lists",
"Convert everything to bullet points for scannability",
"Convert everything to prose for readability",
"Let the model choose a format per section on each run"],
c:0,
w:["Correct. The blueprint prescribes rendering by content type.",
"Bullets flatten tabular relationships and make narrative choppy.",
"Financial figures in prose are hard to compare and easy to misread.",
"Free choice produces inconsistency across runs and sections."],
e:"An explicitly tested point: match presentation to content type. Quantitative and comparative data belong in tables where values align; narrative and contextual material reads better as prose; enumerable technical findings suit structured lists. Consistency matters as much as the choice &mdash; the same content type should render the same way every run, so readers learn where to look.",
k:"Render by content type: tables for quantitative, prose for narrative, lists for technical findings. Be consistent across runs."},

{i:341,d:5,t:"5.1",s:"GEN",diff:2,
q:"What is the &ldquo;lost in the middle&rdquo; effect?",
ask:"Which describes it?",
a:["Models process the start and end of long inputs reliably but may miss material positioned in the middle",
"Messages in the middle of a conversation are dropped by the API",
"Compaction always removes the middle portion of the history",
"Tool results in the middle of a content array are ignored"],
c:0,
w:["Correct. It is an attention characteristic of long inputs, not a mechanical deletion.",
"The API drops nothing; you send what you send.",
"Compaction summarises, and it is a separate mechanism from this attention effect.",
"Content array position does not cause tool results to be ignored."],
e:"The lost-in-the-middle effect drives several design practices: place key findings at the beginning of aggregated data with explicit section headings; keep durable instructions in the system prompt rather than buried in history; split very long inputs into passes; and maintain a compact facts block instead of relying on early conversation turns. It also explains why review of a huge diff concentrates on the first files.",
k:"Lost in the middle = an attention characteristic. Position important material early, use headings, or split into passes."},

{i:342,d:5,t:"5.4",s:"GEN",diff:1,
q:"Which Claude Code command reduces the context used by a long conversation?",
ask:"Which is it?",
a:["<code>/compact</code>",
"<code>/memory</code>",
"<code>/resume</code>",
"<code>/clear</code>"],
c:0,
w:["Correct. <code>/compact</code> summarises the conversation to reclaim context.",
"<code>/memory</code> manages memory files rather than compacting the active conversation.",
"Resuming continues a prior session; it does not reduce context.",
"Clearing discards the conversation entirely rather than compacting it."],
e:"<code>/compact</code> is the mid-session remedy for context pressure, and the discipline around it matters more than the command: externalise specifics to a scratchpad file <em>before</em> compacting, because summarisation degrades numbers, dates and paths first. Know <code>/memory</code> as its neighbour &mdash; it manages memory files &mdash; since the two appear as distractors for each other.",
k:"<code>/compact</code> reduces context by summarising &mdash; lossy about specifics. Externalise to a file first. <code>/memory</code> manages memory files."},

{i:343,d:5,t:"5.2",s:"GEN",diff:2,
q:"Which is explicitly identified as an unreliable proxy for case complexity?",
ask:"Which one?",
a:["Model self-rated confidence and customer sentiment",
"The number of tools the agent called",
"Whether the customer is on a paid plan",
"The length of the policy document"],
c:0,
w:["Correct. Both are named explicitly as unreliable proxies in the blueprint.",
"Not discussed as a proxy, and it would at least correlate weakly with effort.",
"Account tier is a business prioritisation signal, not a complexity measure, and is not the named answer.",
"Not relevant to a specific case's complexity."],
e:"The blueprint states that sentiment analysis and model confidence self-ratings are unreliable proxies for case complexity. This shows up as a distractor across many escalation and routing questions, so treat any option built on self-rated confidence or detected emotion with suspicion. The exception is confidence that has been <em>calibrated</em> against labelled data at field level &mdash; then it carries a known error rate and is usable.",
k:"Sentiment and raw self-rated confidence are unreliable proxies. Calibrated confidence, measured against labelled data, is different."},

{i:344,d:5,t:"5.3",s:"GEN",diff:2,
q:"Where should a transient failure ideally be handled in a coordinator-subagent system?",
ask:"Which is correct?",
a:["Locally in the subagent, with only unresolvable errors propagated to the coordinator alongside partial results",
"Always at the coordinator, for centralised consistency",
"At a dedicated error-handling agent that monitors all subagents",
"At the top-level handler that terminates the workflow"],
c:0,
w:["Correct. Handle errors at the lowest level capable of resolving them.",
"Centralising routine transient handling floods the coordinator with work it has no better information to do.",
"A separate error agent with direct channels to subagents breaks hub-and-spoke and adds no information.",
"Terminating the workflow on a transient failure is the named abort anti-pattern."],
e:"The division of labour is clean: the subagent knows the operation and can retry it cheaply, so transient failures are its problem; the coordinator knows the workflow and can re-route, re-scope or annotate, so unresolvable failures are its problem. What crosses the boundary must carry context &mdash; failure type, what was attempted, partial results, alternatives &mdash; or the coordinator can only retry blindly.",
k:"Transient &rarr; recover locally in the subagent. Unresolvable &rarr; propagate with structured context and partial results."},

{i:345,d:5,t:"5.5",s:"GEN",diff:2,
q:"Why is an aggregate accuracy figure insufficient for deciding whether to automate an extraction pipeline?",
ask:"Which is the best reason?",
a:["It can mask substantially worse performance on specific document types or specific fields",
"Accuracy is always overstated by the evaluation harness",
"Aggregate figures cannot be computed reliably at scale",
"Automation decisions should be based on cost, not accuracy"],
c:0,
w:["Correct. The blueprint states that aggregate metrics mask poor performance on specific document types or fields.",
"Not a general property; the issue is segmentation, not systematic overstatement.",
"They can be computed easily &mdash; they are just not sufficient.",
"Cost matters, but the stated reason concerns hidden accuracy variation."],
e:"A pipeline at 97% overall can be 99% on typed invoices and 61% on handwritten forms, or 99% on vendor names and 70% on tax amounts. Both segmentations hide real risk. Before automating, validate accuracy by document type and by field, then automate only the segments that meet the bar and route the rest to human review. Keep sampling the automated stream afterwards to catch drift.",
k:"Segment accuracy by document type and by field before automating. Aggregates hide the segments that will hurt you."}
);
