QB.push(
{i:716,d:4,t:"4.1",s:"SDE",diff:2,
q:"Your extraction prompt says: <em>ignore irrelevant information.</em> Different runs disagree about what is irrelevant.",
ask:"What is the underlying problem?",
a:["Relevance is defined only by what you want extracted, so the fix is to enumerate the required fields rather than describe what to ignore",
"The model cannot judge relevance at all",
"The instruction should say &ldquo;ignore all irrelevant information&rdquo;",
"The temperature is too high"],
c:0,
w:["Correct. Specifying inclusions defines exclusions automatically; the reverse does not work.",
"It judges relevance fine when given a target.",
"Adding emphasis to an undefined term changes nothing.",
"Temperature does not supply a definition."],
e:"Negative specifications are weak because the space of things to ignore is unbounded. Positive specifications are strong because they are finite and checkable. Enumerate the fields you want and the schema that holds them, and irrelevance is defined by construction. Reserve explicit exclusions for cases where a near-miss is genuinely tempting &mdash; &ldquo;extract the vendor, not the customer&rdquo; &mdash; which is a boundary statement rather than an open-ended ignore instruction.",
k:"Specify what to include, not what to ignore. Exclusions only help at genuine near-miss boundaries."},

{i:717,d:4,t:"4.1",s:"CCI",diff:2,
q:"Your review prompt asks for &ldquo;anything that could cause a production incident&rdquo;.",
ask:"Why does this produce inconsistent output?",
a:["Almost any code change could conceivably cause an incident, so the criterion has no boundary and each run draws it differently",
"Production incidents are rare so there is little to report",
"The model has no knowledge of production systems",
"The criterion is too narrow"],
c:0,
w:["Correct. Unbounded hypothetical framing gives the model no threshold to apply.",
"Rarity of incidents is not why the criterion is unusable.",
"The model reasons about failure modes perfectly well when given a threshold.",
"It is far too broad, not too narrow."],
e:"&ldquo;Could cause&rdquo; framing invites unbounded speculation, in the same way &ldquo;potential security issue&rdquo; does. Replace it with categorical definitions: unhandled error paths on external calls, missing input validation before a query or shell command, resource leaks on the failure path, breaking changes to a public contract &mdash; each with a code example. That gives the model something decidable and gives you something you can measure precision against.",
k:"&ldquo;Could cause&rdquo; and &ldquo;potential&rdquo; framing has no boundary. Replace with decidable categories plus code examples."},

{i:718,d:4,t:"4.1",s:"CSA",diff:2,
q:"You want the agent to prioritise between competing goals: resolution speed, accuracy, and tone.",
ask:"How should this be expressed?",
a:["State the priority order explicitly, and name the conditions under which the order changes",
"List all three as equally important",
"State only the highest priority and omit the others",
"Let the model infer the priorities from examples alone"],
c:0,
w:["Correct. An explicit ordering plus its exceptions is what makes trade-offs resolvable.",
"Equal weighting gives no way to resolve a conflict, which is the whole point of stating priorities.",
"Omitting the others means they are silently deprioritised to zero.",
"Examples illustrate an ordering but a general rule states it more compactly and generalises further."],
e:"Priorities are only useful when they are ordered and conditional. &ldquo;Prefer speed; where financial figures are involved, accuracy takes precedence over speed; maintain a professional tone throughout, but never at the cost of clarity&rdquo; resolves the conflicts that actually arise. Then back the non-negotiable element with code &mdash; the accuracy of financial figures should be validated, not merely prioritised.",
k:"State priorities as an explicit order with named exceptions. Back any non-negotiable element with code."},

{i:719,d:4,t:"4.2",s:"SDE",diff:2,
q:"Your few-shot examples include one that is subtly wrong.",
ask:"What is the likely effect?",
a:["The model reproduces the error pattern, and it may generalise beyond the specific case shown",
"The model ignores incorrect examples",
"The error affects only that exact input",
"The other examples cancel it out"],
c:0,
w:["Correct. Examples are strong steers, so a wrong one teaches the wrong rule.",
"Nothing detects that an example is wrong.",
"Generalisation is exactly what makes a wrong example dangerous.",
"A minority wrong example still shifts behaviour, particularly on similar inputs."],
e:"Few-shot examples are among the strongest levers available, which cuts both ways. Review them as carefully as you would review test fixtures &mdash; ideally against ground truth &mdash; because a subtly wrong example is worse than no example at all: it produces confident, consistent, wrong output. This is a good reason to draw examples from a labelled validation set rather than from memory.",
k:"A wrong few-shot example teaches a wrong rule confidently. Review examples against ground truth."},

{i:720,d:4,t:"4.2",s:"CCI",diff:3,
q:"You want the model to generalise a review criterion to code patterns you have not shown it.",
ask:"What should the examples include?",
a:["The rationale for each classification, so the model learns the underlying principle rather than the surface pattern",
"More examples of the same surface pattern",
"Longer code snippets",
"The severity rating only"],
c:0,
w:["Correct. Rationale is what turns examples from pattern-matching into rule-learning.",
"Repetition of one surface form teaches that form, not the principle.",
"Length does not convey why a case qualifies.",
"A rating without reasoning conveys the outcome but not the criterion."],
e:"The blueprint recommends few-shot examples with rationale, and the reason is generalisation. Showing that a particular null check is acceptable teaches one case; explaining that it is acceptable <em>because it guards a trust boundary rather than masking an upstream defect</em> teaches the discriminating feature. That is what lets the model classify a pattern it has never seen.",
k:"Include rationale in few-shot examples. It converts pattern-matching into rule-learning that generalises."},

{i:721,d:4,t:"4.2",s:"SDE",diff:2,
q:"You are choosing between showing examples of correct output and showing examples of incorrect output with corrections.",
ask:"When is the second form more useful?",
a:["When there is a specific, recurring error you need to eliminate &mdash; a corrected pair names the failure directly",
"Always; corrections are strictly more informative",
"Never; showing errors teaches the model to produce them",
"Only when the correct output is very long"],
c:0,
w:["Correct. A before-and-after pair targets a known failure mode precisely.",
"For establishing a baseline format, clean correct examples are cleaner and cheaper.",
"Clearly-labelled corrections do not teach the error; they mark it as wrong.",
"Output length is not the relevant factor."],
e:"Match the example form to the job. Clean correct examples establish the target format and are the default. Corrected pairs &mdash; here is a plausible-looking output, here is why it is wrong, here is the correct version &mdash; target a specific recurring failure and are what the retry-with-feedback pattern uses. Contrastive pairs of two <em>correct-looking</em> cases with different classifications are the third form, for teaching a boundary.",
k:"Clean examples set the format; corrected pairs kill a known error; contrastive pairs teach a boundary."},

{i:722,d:4,t:"4.3",s:"SDE",diff:2,
q:"Your schema declares a field as a string with no further constraint, and downstream code expects one of four values.",
ask:"What should change?",
a:["Declare it as an enum with those four values plus explicit escape values, moving the constraint from downstream code into the schema",
"Add a description listing the four values",
"Validate it downstream and reject anything else",
"Add a regular expression pattern matching the four values"],
c:0,
w:["Correct. Express the constraint structurally where it can be enforced rather than checked after the fact.",
"A description is guidance the model may not follow.",
"Downstream rejection catches the problem but wastes the extraction that produced it.",
"A pattern works but is less readable and less semantically clear than an enum for a fixed value set."],
e:"Every constraint you express in the schema is one your downstream code no longer has to defend against, and one the model cannot violate. Use <code>enum</code> for a fixed value set, <code>type</code> for the data type, <code>format</code> for structured strings, ranges for numeric bounds. Remember the companion rule: include <code>other</code> and <code>unclear</code> escape values so the enum does not force a bad fit on an unanticipated case.",
k:"Move constraints from downstream checks into the schema. Enums for fixed value sets, always with escape values."},

{i:723,d:4,t:"4.3",s:"SDE",diff:3,
q:"You need extracted data where one field's validity depends on another &mdash; a tax amount is required only when a tax rate is present.",
ask:"Where should this rule live?",
a:["In domain validation code, since cross-field conditional requirements are semantic and beyond what a structural schema expresses well",
"In the <code>required</code> array, listing both fields",
"In the field description",
"In a regular expression on the tax amount"],
c:0,
w:["Correct. Conditional cross-field rules are semantics, which schemas do not capture.",
"Marking both required forces a tax amount on documents with no tax at all.",
"A description is guidance, not validation.",
"A pattern constrains one field's format, not its relationship to another."],
e:"This is the syntax/semantics boundary again, in its subtlest form. A schema can say a field is a number, is required, and falls in a range. It cannot say &ldquo;required <em>if</em> another field is present&rdquo;, &ldquo;must sum to&rdquo;, or &ldquo;must precede&rdquo;. Those are domain rules for your validation layer &mdash; and the failure should feed the retry loop with the specific relationship stated, so the retry is guided rather than blind.",
k:"Cross-field and conditional rules are semantics, not schema. Validate in code and feed the specific rule into the retry."},

{i:724,d:4,t:"4.4",s:"SDE",diff:2,
q:"Your retry prompt includes the validation errors but phrases them as &ldquo;the extraction was invalid&rdquo;.",
ask:"What is wrong?",
a:["It names the outcome rather than the specific defect, so the retry has no more information than the first attempt",
"It is too short",
"It should not mention the failure at all",
"It should include the schema"],
c:0,
w:["Correct. A retry only improves on the first attempt if it carries specific new information.",
"Length is irrelevant; specificity is the issue.",
"The failure must be mentioned &mdash; that is the point of feedback.",
"The schema is useful but the missing element is the specific defect."],
e:"Retry-with-feedback works because the second attempt has strictly more information. &ldquo;Invalid&rdquo; adds nothing. &ldquo;The line items sum to 1,240.00 but <code>stated_total</code> is 1,420.00; re-check whether a line item was missed or a digit transposed&rdquo; names the defect, points at the likely cause, and is actionable. Include the source document and the previous extraction alongside it, or the model cannot re-derive the correct value.",
k:"Retry feedback must name the specific defect, not the outcome. &ldquo;Invalid&rdquo; adds no information."},

{i:725,d:4,t:"4.4",s:"SDE",diff:2,
q:"A validation rule rejects an extraction, but manual inspection shows the extraction is correct and the rule's assumption is outdated.",
ask:"What should happen?",
a:["Fix the rule, and check whether other rules encode the same outdated assumption",
"Add an exception for this document",
"Lower the strictness of all validation rules",
"Route all documents of this type to human review"],
c:0,
w:["Correct. The defect is in the validator, and outdated assumptions often cluster.",
"Per-document exceptions accumulate into an unmaintainable special-case list.",
"Blanket loosening removes genuine detection along with the bad rule.",
"Sending correct extractions to review consumes reviewer capacity for no benefit."],
e:"Validators encode assumptions about the domain, and domains change &mdash; a new invoice template, a regulatory change, a new supplier convention. When a rule rejects correct data, fix the rule and look for siblings built on the same assumption. Tracking which rule fired on each rejection is what makes this discoverable; without it, a stale rule looks like a persistent extraction problem.",
k:"A rule rejecting correct data is a validator defect. Fix it and check for sibling rules with the same stale assumption."},

{i:726,d:4,t:"4.4",s:"SDE",diff:3,
q:"You want to distinguish extraction errors caused by the model from those caused by upstream OCR.",
ask:"What should you capture?",
a:["The source text the model was given alongside the extraction, so you can tell whether the value was misread before the model ever saw it",
"The model version only",
"The extraction latency",
"The number of retries"],
c:0,
w:["Correct. Comparing the extraction against the text actually presented separates the two causes.",
"Version helps with change tracking but does not localise the cause.",
"Latency is unrelated.",
"Retry count measures difficulty without identifying the cause."],
e:"Pipelines have several stages and errors can originate at any of them. If the OCR text already reads <code>1,42O.00</code> with a letter O, the model faithfully extracted what it was given and the remedy is upstream. Capturing the input alongside the output localises the fault, and it feeds the routing decision too &mdash; documents with poor OCR quality are exactly the ambiguous-source cases that belong in human review rather than in a retry loop.",
k:"Capture the input the model actually saw alongside its output. It separates model errors from upstream errors."},

{i:727,d:4,t:"4.5",s:"SDE",diff:2,
q:"You are estimating the cost of a batch job and want to know what is billed.",
ask:"Which is correct?",
a:["Input tokens including the <code>tools</code> parameter, plus output tokens, at roughly half the synchronous rate",
"Output tokens only",
"A flat fee per batch",
"Input tokens only, since output is free in batch mode"],
c:0,
w:["Correct. Batch pricing is the standard token model at approximately a 50% discount.",
"Input tokens are billed too, and for large tool schemas they can dominate.",
"There is no flat fee.",
"Output tokens are billed."],
e:"Batch billing follows the ordinary token model with roughly a 50% discount. The detail that catches people out is that input tokens include the <code>tools</code> parameter &mdash; names, descriptions and schemas &mdash; on every request. For a batch of 40,000 documents each carrying a large tool schema, that overhead is substantial, which is another reason to keep tool definitions complete but concise.",
k:"Batch bills input (including <code>tools</code>) + output at ~50% off. Large tool schemas multiply across every batch item."},

{i:728,d:4,t:"4.5",s:"SDE",diff:2,
q:"A stakeholder asks whether batch processing improves accuracy.",
ask:"What is the accurate answer?",
a:["No &mdash; batch changes cost and latency characteristics, not model quality; accuracy comes from the prompt, schema and validation",
"Yes, because the model has more time to think",
"Yes, because batch requests use a more capable model",
"No, because batch requests use a less capable model"],
c:0,
w:["Correct. Batch is a delivery mechanism, not a quality mechanism.",
"There is no extra deliberation in batch processing.",
"The same models are available.",
"Batch does not downgrade the model either."],
e:"Batch is an operational choice: roughly 50% cheaper, up to 24 hours, no latency SLA, single-shot only. It does not change what the model produces for a given request. Accuracy comes from the prompt's explicit criteria, few-shot examples spanning the input variation, schema design that makes absence expressible, and validation with guided retries. Choosing batch is orthogonal to all of that.",
k:"Batch changes cost and latency, never quality. Accuracy comes from prompt, examples, schema and validation."},

{i:729,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want a review pass focused solely on security, separate from the general correctness pass.",
ask:"What makes this worthwhile?",
a:["A focused remit with its own criteria and examples produces deeper coverage of that class than a general pass dividing attention across everything",
"It doubles the number of comments",
"Security requires a different model",
"General passes cannot detect security issues at all"],
c:0,
w:["Correct. Specialised remit plus specialised criteria is what makes the extra pass additive.",
"Comment volume is not the goal; distinct findings are.",
"Model choice is not the differentiator.",
"General passes find some security issues; a focused pass finds more of them."],
e:"Additional passes pay off when they differ in remit, input or independence. A security pass with its own criteria &mdash; unvalidated input reaching a query or shell, credentials in source, bypassable authorisation checks &mdash; and its own examples goes deeper than a general pass that must also weigh style, structure and test coverage. Deduplicate on file and line at merge so the passes do not produce overlapping comments.",
k:"Specialised passes are additive when they carry their own criteria and examples. Deduplicate findings at merge."},

{i:730,d:4,t:"4.6",s:"CCI",diff:3,
q:"Your multi-pass architecture has grown to six passes and CI now takes 20 minutes.",
ask:"What should you evaluate?",
a:["Each pass's incremental true positives against its cost, retiring passes that mostly duplicate others",
"Whether all six can run on a faster model",
"Whether the passes can be merged into one prompt",
"Whether to move the whole review to batch processing"],
c:0,
w:["Correct. Judge each pass by what it uniquely contributes, and retire the ones that do not.",
"A faster model reduces time at the cost of quality without addressing redundancy.",
"Merging six remits into one prompt reproduces the attention dilution the split solved.",
"Batch has no latency SLA and is unsuitable for a blocking pre-merge check."],
e:"Architectures accrete. Measure each pass's <em>unique</em> true positives &mdash; findings no other pass produced &mdash; against its runtime and token cost, and retire the ones that mostly duplicate. Where latency rather than value is the problem, run independent passes concurrently instead of serially, which is the correct lever for a blocking check that must stay synchronous.",
k:"Measure each pass's unique true positives. Retire duplicates; parallelise the rest rather than moving to batch."},

{i:731,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want extraction output to include a field only when relevant, without the model inventing it otherwise.",
ask:"What is the correct schema design?",
a:["Make the field optional or nullable, and demonstrate the absent case in a few-shot example",
"Mark it required and instruct the model to leave it blank when absent",
"Mark it required with a default value",
"Omit the field from the schema entirely"],
c:0,
w:["Correct. Structural permission to omit, plus a demonstration of doing so, is what prevents fabrication.",
"A required field with a blank-when-absent instruction pits the schema against the prompt.",
"Defaults silently substitute a value for absence, destroying the distinction.",
"Omitting the field means genuinely present data is never captured."],
e:"Two mechanisms working together. The schema must permit absence &mdash; optional or nullable &mdash; or the model is structurally pressured to produce something. The few-shot examples must <em>demonstrate</em> absence, or the model may not realise omission is acceptable in practice. The blueprint names both: make fields optional when the source may not contain the information, and show correct handling of missing data in examples.",
k:"Permit absence in the schema <em>and</em> demonstrate it in an example. Either alone leaves fabrication pressure."},

{i:732,d:4,t:"4.1",s:"SDE",diff:2,
q:"Two engineers write extraction prompts for the same field and get different results from each.",
ask:"What does this indicate?",
a:["The requirement itself is under-specified, so each prompt encodes a different interpretation",
"One engineer is better at prompting",
"The model is unreliable for this field",
"The prompts should be averaged"],
c:0,
w:["Correct. Divergent prompts for the same field usually mean the field's definition was never pinned down.",
"Possible, but the systematic explanation is an ambiguous requirement.",
"Reliability follows specification here.",
"Prompts cannot be averaged."],
e:"When two competent people produce different behaviour from the same requirement, the requirement is the problem. Pin the field down: what exactly is it, how is it identified in the source, what does it look like when absent, what are the near-miss cases it must be distinguished from. Write that as concrete input/output examples and it becomes both the specification and the labelled test set.",
k:"Divergent implementations of one requirement mean the requirement is ambiguous. Pin it down with input/output examples."},

{i:733,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to know whether your validation layer is catching the errors that matter.",
ask:"How would you measure this?",
a:["Compare validation outcomes against a labelled set: how many genuine errors were caught, and how many correct extractions were wrongly rejected",
"Count how often validation fires",
"Measure the time validation takes",
"Count the number of rules"],
c:0,
w:["Correct. A validator has both recall and precision, and both need measuring against ground truth.",
"Firing frequency says nothing about whether it fired correctly.",
"Runtime is an operational metric.",
"Rule count measures effort, not effectiveness."],
e:"Validators deserve the same two-dimensional measurement as review criteria. Recall: what fraction of genuine errors does it catch? Precision: what fraction of its rejections are actually wrong? A validator with high recall and poor precision floods human review with correct extractions; one with high precision and poor recall lets bad data through quietly. Measure both against labelled data, per rule.",
k:"Measure validators on recall and precision against labelled data, per rule. Both failure directions have real costs."},

{i:734,d:4,t:"4.2",s:"CSA",diff:2,
q:"Your escalation few-shot examples are all drawn from billing cases, but the agent also handles technical and shipping issues.",
ask:"What is the risk?",
a:["The examples teach billing-specific cues rather than the general escalation criteria, so behaviour on other domains is less well calibrated",
"The examples are too few",
"The examples are too long",
"There is no risk if the criteria are also stated"],
c:0,
w:["Correct. Examples generalise from what they span, so a single-domain set teaches that domain's surface cues.",
"Count is not the issue; coverage is.",
"Length is not the issue.",
"Stated criteria help, but the examples still pull behaviour toward the demonstrated domain."],
e:"Examples should span the space the agent actually operates in. When the criteria are general &mdash; explicit request for a human, policy gap, inability to progress &mdash; but every example is a billing case, the model learns the criteria partly through billing-specific cues. Include an example from each major domain, and keep them balanced across escalate and resolve so the boundary is visible from both sides.",
k:"Examples must span the operating domains, and be balanced across both sides of the decision boundary."},

{i:735,d:4,t:"4.5",s:"SDE",diff:3,
q:"You have a daily batch and an occasional urgent single document.",
ask:"What is the sensible architecture?",
a:["Route by urgency &mdash; the daily volume through Batch, urgent single documents through the synchronous API &mdash; sharing the same prompt and schema",
"Put everything through Batch and accept the delay on urgent items",
"Put everything through the synchronous API for consistency",
"Maintain two separate prompts, one tuned for each path"],
c:0,
w:["Correct. Route on the blocking question, and share the prompt and schema so quality is identical on both paths.",
"Urgent items cannot tolerate an unbounded window.",
"Forfeits roughly half the cost on the bulk volume for no benefit.",
"Two prompts diverge and you end up validating both."],
e:"The blocking question decides the path, not the pipeline. Share the prompt, schema and validation across both routes so behaviour is identical and you only maintain one set &mdash; the difference is purely how the request is delivered. This also keeps your measurement valid: accuracy validated on the batch path applies to the synchronous path because nothing about the request differs.",
k:"Route by whether anything is blocked. Share prompt, schema and validation across both paths so quality is identical."},

{i:736,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want a reviewer to catch issues that only appear when a change is considered against the project's conventions.",
ask:"What must the reviewer receive?",
a:["The project conventions &mdash; from CLAUDE.md or path-scoped rules &mdash; alongside the diff",
"A larger context window",
"The full repository",
"The commit history"],
c:0,
w:["Correct. Convention violations are invisible to a reviewer that does not know the conventions.",
"Window size does not supply content that was never sent.",
"The whole repository dilutes attention and mostly does not bear on this change.",
"History shows what changed before, not what the conventions are."],
e:"Findings are only as relevant as the standards the reviewer holds. Supplying CLAUDE.md and path-scoped rules is what makes CI review project-specific rather than generic, and it is why the docs' emphasis on documenting standards matters for automation as much as for interactive work. Note that sharing standards between the generator and the reviewer is desirable &mdash; independence concerns the generation <em>reasoning</em>, not the conventions.",
k:"Reviewers need the conventions, not more code. Shared standards are desirable; shared generation reasoning is not."},

{i:737,d:4,t:"4.3",s:"SDE",diff:2,
q:"Your extraction returns a well-formed object but a downstream consumer rejects it for a missing field the schema marked optional.",
ask:"Where is the mismatch?",
a:["Between the schema's optionality and the consumer's expectation &mdash; one of them is wrong and the contract needs settling",
"In the model's output, which should have included the field",
"In the validation layer, which should have caught it",
"Nowhere; optional fields may always be absent"],
c:0,
w:["Correct. A contract disagreement between producer and consumer, which must be resolved rather than patched at either end.",
"The model complied with the schema it was given.",
"Validation enforced the schema correctly; the schema is what is contested.",
"True in isolation, but the consumer's expectation shows the contract is not agreed."],
e:"Optionality is a contract statement, and both sides must agree on it. Either the field is genuinely optional and the consumer must handle absence, or it is required in practice and the schema should say so &mdash; in which case you also need a plan for documents that genuinely lack it, since marking it required creates fabrication pressure. Settle it explicitly rather than patching at whichever end complained.",
k:"Optionality is a contract. Settle producer and consumer expectations explicitly; do not patch at one end."},

{i:738,d:4,t:"4.1",s:"CCI",diff:2,
q:"You want fewer but higher-value review comments.",
ask:"Which change achieves this without losing genuine findings?",
a:["Narrow the criteria to the categories that matter and state exclusions explicitly, rather than capping the number of comments",
"Cap comments at five per file",
"Report only the highest severity finding",
"Ask the model to be more selective"],
c:0,
w:["Correct. Sharpening what qualifies removes low-value findings while keeping the ones you care about.",
"A cap drops genuine findings arbitrarily once the limit is hit.",
"Severity ranking is not a substitute for defining what qualifies at all.",
"&ldquo;More selective&rdquo; is the vague instruction the blueprint contrasts with explicit criteria."],
e:"Volume controls and criteria are different levers. A cap reduces output indiscriminately &mdash; the sixth finding may be the important one. Narrowing the criteria reduces output <em>selectively</em>, by removing whole categories of low-value findings. State exclusions as explicitly as inclusions, and measure precision per category so you know which categories to narrow.",
k:"Reduce comment volume by narrowing criteria, not by capping counts. Caps drop findings arbitrarily."},

{i:739,d:4,t:"4.4",s:"SDE",diff:2,
q:"You add a new validation rule and the retry rate jumps from 4% to 22%.",
ask:"What should you check first?",
a:["Whether the new rule is over-strict &mdash; a large jump usually means it rejects correct extractions rather than that quality collapsed",
"Whether the model changed",
"Whether the retry budget is too low",
"Whether the documents changed"],
c:0,
w:["Correct. The rule is the variable that changed, so it is the first hypothesis.",
"Possible in general, but here you changed one thing and the rate moved.",
"Budget affects what happens after rejection, not the rejection rate.",
"Also possible, but the timing points at the rule you just added."],
e:"Change one thing, attribute the effect to it first. Sample the newly-rejected extractions and inspect them: if most are correct, the rule is over-strict and needs its assumption revisited against the real distribution. This is why per-rule failure tracking matters &mdash; without it, a bad new rule looks like a general quality regression and you go hunting in the wrong place.",
k:"Attribute a metric jump to the thing you just changed. Sample the newly-rejected items before concluding quality fell."},

{i:740,d:4,t:"4.2",s:"SDE",diff:2,
q:"You want examples to teach output format without also teaching content the model should derive from the input.",
ask:"What should the examples look like?",
a:["Realistic but varied in content, so the format is the consistent element and the values clearly derive from each input",
"Identical content with only the format varying",
"Placeholder values such as XXX in every field",
"A single example repeated three times"],
c:0,
w:["Correct. Consistent format with varied content isolates format as the thing being taught.",
"Identical content teaches the content as much as the format.",
"Placeholders fail to show how real values are derived or formatted.",
"Repetition teaches that specific case, not the general format."],
e:"The design rule is consistent format, varied content. Consistency in the structure teaches the shape; variation in the values makes clear that the values come from the input rather than from the examples. Placeholder values are a common mistake &mdash; they show the field exists but not how a real value should be normalised, which is often the actual question.",
k:"Consistent format, varied realistic content. Placeholders show structure but not how real values are formatted."},

{i:741,d:4,t:"4.6",s:"CCI",diff:3,
q:"You want to be sure your review architecture has not regressed after a prompt change.",
ask:"What do you need in place?",
a:["A fixed set of pull requests with known defects, run before and after, compared on detection rate and false positives",
"A count of comments before and after",
"Developer feedback after the change",
"A larger sample of recent pull requests"],
c:0,
w:["Correct. A fixed labelled set is what makes a before-and-after comparison controlled.",
"Comment counts conflate finding more with producing more noise.",
"Feedback is valuable but slow and subjective for a regression check.",
"A different sample is an uncontrolled comparison."],
e:"Regression detection needs the inputs held constant and the labels known, or you cannot separate a quality change from an input change. Build the labelled set once from historical pull requests with known defects, run it on every meaningful prompt or architecture change, and compare detection rate and false positives together &mdash; a change that raises both may well be a net loss.",
k:"Regression-test review changes on a fixed labelled PR set. Compare detection rate <em>and</em> false positives together."},

{i:742,d:4,t:"4.5",s:"SDE",diff:2,
q:"A colleague suggests splitting one batch into ten so results arrive sooner.",
ask:"What is the accurate expectation?",
a:["Earlier partial results and better failure isolation, but no guarantee the total completes sooner",
"Guaranteed faster total completion",
"Lower cost per document",
"Higher accuracy per document"],
c:0,
w:["Correct. The benefits are partial availability and blast-radius containment, not total throughput.",
"There is no such guarantee; batches have no latency SLA.",
"Per-token pricing does not change with batch count.",
"Accuracy is unaffected by batching granularity."],
e:"Be precise about what splitting buys. You get earlier partial results, so downstream work can begin, and you get failure isolation, so a systematic problem is caught after 1,000 documents rather than 10,000. What you do not get is a completion-time guarantee &mdash; the API offers no latency SLA regardless of how you slice the work. Sample-testing the prompt first remains the real protection against a systematic problem.",
k:"Splitting batches buys earlier partial results and failure isolation, not faster completion. There is still no SLA."},

{i:743,d:4,t:"4.1",s:"CSA",diff:2,
q:"You want the agent to be concise, but not at the cost of omitting information the customer needs.",
ask:"How should this be expressed?",
a:["Name what must always be included &mdash; the action taken, the reason, the next step &mdash; and ask for brevity in everything else",
"Instruct the agent to be concise",
"Set a maximum response length",
"Instruct the agent to be thorough"],
c:0,
w:["Correct. Specify the required content, then constrain everything outside it.",
"Unqualified brevity drops content the customer needs.",
"A length cap truncates arbitrarily and does not protect the required elements.",
"Unqualified thoroughness produces the verbosity you are trying to avoid."],
e:"Conflicting quality goals are resolved by specifying the floor rather than the ceiling. Name the elements that must always appear &mdash; what was done, why, and what happens next &mdash; and ask for brevity everywhere else. That converts a vague tension into a checkable requirement, and it is the same technique as defining review criteria by what must be reported and what must be ignored.",
k:"Resolve conciseness-versus-completeness by naming the required elements, then constraining everything else."},

{i:744,d:4,t:"4.3",s:"SDE",diff:2,
q:"An extracted numeric field must never be negative.",
ask:"Where does this constraint belong?",
a:["In the schema as a minimum bound, since it is a structural constraint a schema can express",
"In domain validation code only",
"In the field description",
"In a few-shot example showing positive values"],
c:0,
w:["Correct. A numeric range is exactly what schema constraints express, so enforce it there.",
"Code validation is a reasonable second layer, but the schema can enforce this directly.",
"A description is guidance, not enforcement.",
"Examples shape behaviour without constraining it."],
e:"Use the schema for everything it can express, and reserve code for what it cannot. A minimum bound on a single field is structural &mdash; put it in the schema. A rule spanning fields (&ldquo;line items must sum to the total&rdquo;), a conditional requirement, or a plausibility judgement (&ldquo;this date should be within two years&rdquo;) is semantic and belongs in your validation layer.",
k:"Single-field ranges are schema constraints. Cross-field, conditional and plausibility rules are code."},

{i:745,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want retries to converge rather than oscillate between two wrong answers.",
ask:"What most helps?",
a:["Include the previous attempt in the retry prompt, so the model can see what it already tried and avoid repeating it",
"Raise the temperature so it explores different answers",
"Reduce the retry budget to one",
"Use a different model on each retry"],
c:0,
w:["Correct. Showing the previous attempt is what prevents the model re-proposing it.",
"Higher temperature increases oscillation rather than convergence.",
"A smaller budget hides the oscillation rather than fixing it.",
"Model switching adds variance without adding information."],
e:"The three-part retry payload &mdash; source, previous attempt, specific errors &mdash; does distinct work, and the previous attempt is what prevents cycling. Without it the model re-derives from the same input and often lands in the same place. If it still oscillates between two candidates, that is diagnostic: the source is likely ambiguous for that field, which makes it a human-review case rather than a retry case.",
k:"Include the previous attempt so the retry does not re-propose it. Persistent oscillation means an ambiguous source."},

{i:746,d:4,t:"4.6",s:"CCI",diff:2,
q:"An independent reviewer and the original author disagree about whether a finding is valid.",
ask:"What is the healthiest resolution mechanism?",
a:["A human decides, and the reasoning is recorded so the same finding can be dismissed quickly if it recurs",
"The reviewer's judgement always wins",
"The author's judgement always wins",
"Run a third review as a tie-breaker"],
c:0,
w:["Correct. A human decides, and the decision record prevents re-litigating it every run.",
"An independent reviewer is not automatically right.",
"Deferring to the author defeats the purpose of independent review.",
"A third instance has no better basis and may simply add a third opinion."],
e:"Independent review exists to produce useful friction, and the friction needs a resolution path. A human decides on merit, and the decision is recorded &mdash; in the code, in a rules file, or in the review configuration &mdash; so the same finding is dismissed cheaply next time rather than re-argued on every run. Recurring disagreements are also a signal that the criteria need sharpening.",
k:"Humans resolve reviewer-author disagreements, and the decision is recorded so it is not re-argued each run."},

{i:747,d:4,t:"4.2",s:"SDE",diff:2,
q:"Your prompt has both explicit criteria and few-shot examples, and they subtly disagree.",
ask:"What is the likely effect?",
a:["Inconsistent behaviour, since the model receives two conflicting specifications and may follow either",
"The criteria always win",
"The examples always win",
"The model resolves the conflict correctly by inference"],
c:0,
w:["Correct. Conflicting instructions produce arbitrary resolution, exactly as conflicting CLAUDE.md rules do.",
"There is no guaranteed precedence.",
"Examples are a strong steer but do not reliably override stated criteria.",
"Nothing guarantees the model resolves it the way you would."],
e:"Criteria and examples must agree, because together they form one specification. When they diverge the model has no principled basis for choosing and behaviour becomes irreproducible. Review them as a pair whenever either changes &mdash; this is the same hazard as two contradictory CLAUDE.md rules, where the documentation notes the model may pick one arbitrarily.",
k:"Criteria and examples are one specification. If they disagree, behaviour becomes arbitrary and irreproducible."},

{i:748,d:4,t:"4.5",s:"SDE",diff:2,
q:"You need results from a 30,000-document job before a meeting in three hours.",
ask:"What should you do?",
a:["Use the synchronous API with controlled concurrency, since three hours is inside no batch guarantee",
"Submit as a batch and hope it completes",
"Submit as a batch split into ten parts",
"Submit as a batch and prepare a fallback"],
c:0,
w:["Correct. A hard three-hour deadline requires bounded latency, which only the synchronous path provides.",
"Hoping is not a plan when there is no SLA.",
"Splitting gives earlier partial results but still no completion guarantee.",
"A fallback is prudent, but if you need the synchronous path as a fallback you should use it as the primary."],
e:"The absence of a latency SLA is the decisive Batch constraint, and a hard deadline inside the possible window rules it out. Most batches do finish in under an hour, but &ldquo;most&rdquo; is not a guarantee you can present to a meeting. Use the synchronous API with managed concurrency and pay the full rate &mdash; the 50% saving is not worth a missed deadline.",
k:"A hard deadline inside the batch window rules Batch out. &ldquo;Usually fast&rdquo; is not an SLA."},

{i:749,d:4,t:"4.3",s:"SDE",diff:3,
q:"You want the model to extract a value <em>and</em> flag how confident it is, so routing can use it.",
ask:"What must you do before trusting the confidence field?",
a:["Calibrate it against labelled data at field level, so a given confidence value corresponds to a known error rate",
"Nothing &mdash; the model's confidence is inherently meaningful",
"Normalise it to a 0&ndash;100 scale",
"Average it across fields to get a document-level score"],
c:0,
w:["Correct. Self-reported confidence is uncalibrated until measured against ground truth.",
"Uncalibrated self-assessment is a recurring wrong answer throughout this exam.",
"Rescaling an uncalibrated number leaves it uncalibrated.",
"Averaging masks a single badly-extracted critical field behind several easy ones."],
e:"Confidence is legitimate as a routing input <em>after</em> calibration and useless before it. Calibrate at field level against a labelled set, because fields differ enormously in difficulty, and segment by document type as well. Then a threshold carries a known error rate you can accept on business grounds. Re-calibrate periodically, since the binding drifts as the input mix and the model change.",
k:"Calibrate confidence at field level against labelled data before using it to route. Recalibrate as inputs drift."},

{i:750,d:4,t:"4.1",s:"SDE",diff:2,
q:"You are writing criteria for a field that is genuinely subjective, such as classifying a complaint's tone.",
ask:"What is the most effective approach?",
a:["Define each category with concrete examples of borderline cases, accepting that measured inter-annotator agreement sets a realistic accuracy ceiling",
"Insist on objective criteria and remove the field",
"Let the model use its own judgement without guidance",
"Use a numeric scale rather than categories"],
c:0,
w:["Correct. Anchor subjective categories with borderline examples, and set expectations from human agreement levels.",
"Removing genuinely useful subjective fields is an overreaction.",
"Unguided judgement produces the inconsistency you are trying to avoid.",
"A numeric scale is not more objective and is harder to anchor."],
e:"Subjective fields can be made consistent, but not perfectly accurate, because there is no single ground truth. Anchor each category with borderline examples and rationale, measure inter-annotator agreement among humans on the same cases, and treat that as the realistic ceiling &mdash; expecting a model to exceed the agreement rate of your own annotators is expecting it to resolve a genuine ambiguity. This is also why a single-annotator labelled set is a weak standard for subjective fields.",
k:"Anchor subjective categories with borderline examples. Human inter-annotator agreement sets the realistic ceiling."}
);
