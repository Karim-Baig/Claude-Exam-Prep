QB.push(
{i:751,d:3,t:"3.1",s:"CGC",diff:2,
q:"A team writes CLAUDE.md entries describing the directory layout and the dependency list.",
ask:"What is the problem with this content?",
a:["Claude can derive it from the codebase, so it consumes context on every request without changing any decision",
"It is not detailed enough to be useful",
"It belongs in a path-scoped rule instead",
"It should be in the README as well"],
c:0,
w:["Correct. Derivable content is the canonical thing to trim from CLAUDE.md.",
"More detail would make the problem worse, not better.",
"Scoping derivable content still costs context when it loads, for no benefit.",
"Duplicating it elsewhere does not address the context cost."],
e:"The documented trim heuristic is precise: cut content Claude can derive from the codebase &mdash; directory layouts, dependency lists, architecture overviews &mdash; and keep pitfalls, rationale, and conventions that differ from tool defaults. That last clause is the sharpest test. If the default behaviour already matches your rule, the rule is doing no work; if Claude can read it from the code, writing it down is paying twice.",
k:"Trim what Claude can derive from the code. Keep pitfalls, rationale, and conventions that differ from tool defaults."},

{i:752,d:3,t:"3.1",s:"CGC",diff:2,
q:"Your project CLAUDE.md and a nested CLAUDE.md in <code>src/api/</code> both apply while you work on an API file.",
ask:"How is the content ordered in context?",
a:["From the filesystem root down to the working directory, so the more specific file is read last",
"The most specific file first, then broader ones",
"Alphabetically by path",
"Only the most specific file is included"],
c:0,
w:["Correct. Content is ordered root-down, so instructions closest to where you launched are read last.",
"The ordering runs the other way.",
"Ordering follows directory depth, not alphabetical order.",
"All applicable files are concatenated rather than overriding."],
e:"Files are concatenated rather than overriding, ordered from the filesystem root down to your working directory &mdash; so more specific instructions appear later. Within a directory, <code>CLAUDE.local.md</code> is appended after <code>CLAUDE.md</code>, making personal notes the last thing read at that level. This ordering is why a directory-level exception phrased explicitly as an override of the project default reads naturally in context.",
k:"CLAUDE.md files concatenate root-down; specific content is read last. <code>CLAUDE.local.md</code> comes after <code>CLAUDE.md</code>."},

{i:753,d:3,t:"3.1",s:"CGC",diff:2,
q:"A developer wants personal project-specific preferences that are not committed to version control.",
ask:"What is the documented mechanism?",
a:["A <code>CLAUDE.local.md</code> at the project root, added to <code>.gitignore</code>",
"A <code>CLAUDE.md</code> in a personal branch",
"Comments in the project CLAUDE.md marked personal",
"A second project CLAUDE.md with a different name"],
c:0,
w:["Correct. <code>CLAUDE.local.md</code> loads alongside <code>CLAUDE.md</code> and is intended to be gitignored.",
"A personal branch breaks the moment they work on another branch.",
"Comments in a shared file are still shared, and block-level HTML comments are stripped before Claude sees them.",
"Arbitrary filenames are not discovered."],
e:"Four scopes now, in load order: managed policy, user (<code>~/.claude/CLAUDE.md</code>), project (<code>./CLAUDE.md</code> or <code>./.claude/CLAUDE.md</code>), and local (<code>./CLAUDE.local.md</code>, gitignored, personal to this project). One practical caveat: a gitignored local file exists only in the worktree where you created it, so to share personal instructions across worktrees, import a file from your home directory instead.",
k:"<code>CLAUDE.local.md</code> = personal, project-specific, gitignored. It does not follow you across worktrees &mdash; import from home for that."},

{i:754,d:3,t:"3.1",s:"CGC",diff:3,
q:"You want a monorepo package's CLAUDE.md to include shared standards without duplicating them, but you are told imports do not reduce context.",
ask:"What is the accurate trade-off?",
a:["<code>@path</code> imports aid organisation and prevent drift, but imported files still load at launch, so they do not reduce context &mdash; use path-scoped rules for that",
"Imports both organise and reduce context",
"Imports neither organise nor help in any way",
"Imports reduce context but cause drift"],
c:0,
w:["Correct, and it is exactly why path-scoped rules exist as the separate remedy for context size.",
"Imported files are expanded and loaded at launch alongside the file referencing them.",
"They genuinely prevent duplication and drift; that is their value.",
"They prevent drift rather than causing it."],
e:"Two mechanisms, two purposes, easily conflated. <code>@path</code> imports solve <em>duplication</em> &mdash; shared standards live once and each package composes what it needs &mdash; but the imported content still loads at launch, so context is unchanged. <code>.claude/rules/</code> with <code>paths</code> frontmatter solves <em>context size</em>, because a non-matching rule never loads at all. Use both: imports for organisation, path scoping for cost.",
k:"<code>@path</code> imports prevent duplication but not context cost. Path-scoped rules are the mechanism that reduces context."},

{i:755,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill lives at <code>apps/web/.claude/skills/deploy/SKILL.md</code> in a monorepo.",
ask:"How is it invoked?",
a:["Under a directory-qualified name such as <code>/apps/web:deploy</code>, available when working in that subdirectory",
"As <code>/deploy</code> from anywhere in the repository",
"It is not discovered; skills must be at the repository root",
"Only by the full filesystem path"],
c:0,
w:["Correct. Nested project skills get a directory-qualified name and become available in that subtree.",
"An unqualified name would collide with other packages' skills of the same name.",
"Nested skills are discovered.",
"Invocation is by name, not path."],
e:"Directory qualification is what makes a monorepo workable: several packages can each have a <code>deploy</code> skill without collision, and the qualified name makes it obvious which one you are running. This mirrors the wider monorepo pattern &mdash; per-directory CLAUDE.md for subtree conventions, path-scoped rules for conventions that cross packages, and nested skills for package-specific workflows.",
k:"Nested project skills get a directory-qualified name (<code>/apps/web:deploy</code>), avoiding collisions across packages."},

{i:756,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a skill to load automatically when Claude works with particular files, rather than being invoked.",
ask:"Which frontmatter field does this?",
a:["<code>paths</code>, using the same glob format as path-specific rules",
"<code>context: fork</code>",
"<code>when_to_use</code>",
"<code>disable-model-invocation: true</code>"],
c:0,
w:["Correct. Skills support a <code>paths</code> field limiting when they are auto-activated.",
"Forking controls context isolation, not activation.",
"<code>when_to_use</code> adds trigger context to the description but does not scope by file path.",
"That prevents automatic invocation entirely &mdash; the opposite."],
e:"<code>paths</code> appears on both rules and skills, with the same glob format, but it means slightly different things. On a rule it controls when the instructions <em>load into context</em>. On a skill it limits when the skill is <em>auto-activated</em>. Note that commands in <code>.claude/commands/</code> ignore <code>paths</code>, which is one of the two fields that distinguishes commands from skills.",
k:"<code>paths</code> scopes rule loading and skill auto-activation. Commands ignore it &mdash; one of two fields they drop."},

{i:757,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill's <code>description</code> and <code>when_to_use</code> together are very long.",
ask:"What limit applies?",
a:["They are combined and capped at 1,536 characters",
"There is no limit",
"Each is capped at 500 characters separately",
"The cap applies to <code>description</code> only"],
c:0,
w:["Correct. <code>when_to_use</code> is appended to <code>description</code> and the combination is capped.",
"A cap does exist.",
"The cap applies to the combination, not to each separately.",
"<code>when_to_use</code> counts toward the same cap."],
e:"The cap matters because <code>description</code> plus <code>when_to_use</code> is what Claude uses to decide when to invoke a skill automatically &mdash; it is the selection signal, exactly as a tool description is. Spend the budget on trigger conditions, example requests and the boundary against similar skills, not on prose about what the skill does internally.",
k:"<code>description</code> + <code>when_to_use</code> is the skill's selection signal, capped at 1,536 characters combined."},

{i:758,d:3,t:"3.3",s:"CGC",diff:2,
q:"A path-scoped rule and a nested CLAUDE.md would both work for a convention.",
ask:"What tips the decision?",
a:["Whether the convention follows a file pattern that crosses directories, in which case only the path rule can express it",
"Which file is shorter",
"Which loads faster",
"Whether the team prefers rules or CLAUDE.md"],
c:0,
w:["Correct. A directory CLAUDE.md cannot express &ldquo;test files wherever they live&rdquo;.",
"File length is not the criterion.",
"Load performance is not a meaningful difference.",
"Preference is a weak basis for a structural decision."],
e:"Both are legitimate and they overlap. A nested CLAUDE.md is natural when the convention covers everything in one self-contained subtree. A path-scoped rule is required when the convention follows a file type or pattern across many directories &mdash; co-located test files being the canonical example, since no directory boundary captures them. When both would work, either is fine; when only one can express the scope, that decides it.",
k:"Directory CLAUDE.md for a self-contained subtree; path rule when the convention crosses directories by file pattern."},

{i:759,d:3,t:"3.3",s:"CGC",diff:3,
q:"A path-scoped rule is expected to apply but only fires sometimes.",
ask:"What behaviour explains this?",
a:["Path-scoped rules trigger when Claude reads a matching file, not on every tool use &mdash; so they load once a relevant file is touched",
"Rules load randomly to save context",
"Rules load only in the first turn of a session",
"Rules load only when the file is edited, not read"],
c:0,
w:["Correct. Activation is tied to reading matching files, which is why a rule may not be active early in a session.",
"There is nothing random about it.",
"They load whenever a matching file is read, at any point.",
"Reading is sufficient; editing is not required."],
e:"This explains a common confusion. A path-scoped rule is not active from session start &mdash; it activates when Claude reads a file matching its patterns. So a question asked before any matching file has been touched gets an answer without that rule's guidance. It also explains why such rules reload after compaction only once a matching file is read again, which the compaction documentation calls out explicitly.",
k:"Path-scoped rules activate when Claude reads a matching file, not at session start. Same for reloading after compaction."},

{i:760,d:3,t:"3.4",s:"CGC",diff:2,
q:"You are unsure whether a task warrants planning mode.",
ask:"Which question best resolves it?",
a:["If the first approach chosen turns out to be wrong, how expensive is the rework?",
"How many files will change?",
"How long will the task take?",
"How familiar is the codebase?"],
c:0,
w:["Correct. Cost of a wrong approach is what planning buys down.",
"File count is a weak proxy that misleads in both directions.",
"Duration correlates loosely and is not the deciding factor.",
"Familiarity affects whether you need <em>exploration</em>, which is a different question from planning."],
e:"Planning mode buys down the cost of choosing wrongly, so the deciding question is what a wrong choice costs. A three-line change to an authorisation path warrants planning despite its size; a forty-file mechanical rename with one obvious approach does not. Keep the second variable separate &mdash; unfamiliarity calls for exploration, which can be delegated to a subagent, and is not the same as needing a plan.",
k:"Plan when a wrong approach is expensive to unwind. Explore when the codebase is unfamiliar. Different questions."},

{i:761,d:3,t:"3.4",s:"CGC",diff:2,
q:"A plan is produced and you notice it makes an assumption you cannot verify.",
ask:"What should happen before execution?",
a:["Verify the assumption, since a plan built on an unverified premise fails at whichever phase depends on it",
"Execute and see whether the assumption holds",
"Remove the phase that depends on the assumption",
"Add a fallback phase in case it is wrong"],
c:0,
w:["Correct. Verifying at plan time is far cheaper than discovering the error mid-execution.",
"Discovering a false premise at phase four costs everything built on it.",
"Removing the phase may remove required work.",
"A fallback is reasonable in addition, but verifying first is cheaper than planning for failure."],
e:"Assumptions are the main failure mode of plans, and plan time is the cheapest moment to test them. Where verification is genuinely expensive, make the assumption explicit in the plan file and sequence the dependent phase early, so a false premise surfaces at phase two rather than phase eight &mdash; that also makes partial rollback cheaper, since fewer phases depend on it.",
k:"Verify plan assumptions before executing. Where verification is costly, sequence the dependent phase early."},

{i:762,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want Claude to ask you clarifying questions before implementing, but it starts implementing immediately.",
ask:"What should you do?",
a:["Ask explicitly for the interview &mdash; request the questions it would need answered before designing, rather than expecting it to ask unprompted",
"Provide a longer specification",
"Reduce the temperature",
"Use planning mode instead"],
c:0,
w:["Correct. The interview pattern is something you invoke, not a default behaviour.",
"A longer spec covers only what you already thought of, which is the gap the interview fills.",
"Temperature does not govern whether it asks questions.",
"Planning mode addresses the approach; the interview addresses unclear requirements."],
e:"The default behaviour is to attempt the task with what it has, so the interview pattern has to be requested. It is most valuable when you suspect gaps you cannot name &mdash; the blueprint cites cache invalidation and failure modes as typical of what it surfaces. Sequence matters: interview to settle the requirements, then plan to settle the approach, then execute.",
k:"Ask for the interview explicitly; it is not a default. Interview settles requirements, planning settles approach."},

{i:763,d:3,t:"3.5",s:"CGC",diff:2,
q:"You provide an example of the desired output but the implementation matches the example too literally, including incidental details.",
ask:"What should you change?",
a:["Provide two or three examples that vary in the incidental details, so the invariant structure is what they share",
"Provide one more detailed example",
"Describe the example in prose instead",
"Instruct the model not to copy the example literally"],
c:0,
w:["Correct. Variation across examples is what isolates the invariant from the incidental.",
"One example, however detailed, cannot distinguish what is essential from what is incidental.",
"Prose loses the precision that made the example useful.",
"An instruction not to over-fit is vague and unreliable."],
e:"A single example is ambiguous about which of its features matter. Two or three that share the structure but differ in the incidentals &mdash; different values, different lengths, different edge conditions &mdash; make the invariant visible. This is the same principle as varying content while holding format constant in few-shot prompting, and it is why the recommended range is two to four rather than one.",
k:"Vary the incidentals across two to three examples so the invariant structure is what they share."},

{i:764,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI job must not be able to modify the repository, only comment on it.",
ask:"How should this be enforced?",
a:["Restrict the tools available to the CI run and back it with repository permissions on the CI credential, rather than relying on the prompt",
"Instruct the model in the prompt not to modify files",
"Review the diff after each CI run",
"Run the job on a copy of the repository"],
c:0,
w:["Correct. Capability restriction plus a scoped credential &mdash; two layers, both in code.",
"A prompt instruction is guidance and cannot guarantee it.",
"Post-hoc review detects rather than prevents.",
"A copy helps, but a scoped credential is the durable control and the copy still needs one."],
e:"Two layers, as with any irreversible outward-facing action. Locally, restrict what the run can call &mdash; withhold Write, Edit and Bash, or use <code>disallowedTools</code>. Remotely, give the CI credential only the permissions it needs, so the constraint holds even if the local configuration is wrong. Never rely on the prompt for a capability boundary.",
k:"CI capability limits: restrict tools locally <em>and</em> scope the CI credential remotely. Never rely on the prompt."},

{i:765,d:3,t:"3.6",s:"CCI",diff:3,
q:"Your CI review needs different behaviour for draft pull requests than for ready-to-review ones.",
ask:"What is the cleanest implementation?",
a:["Branch in the pipeline, passing different prompts or criteria based on the PR state, rather than asking the model to infer the state",
"Include the PR state in the prompt and let the model decide how to behave",
"Run both behaviours and discard one",
"Review drafts identically and let authors ignore comments"],
c:0,
w:["Correct. The pipeline knows the state deterministically; branch there.",
"Workable, but it converts a known fact into a model judgement for no reason.",
"Wasteful and produces duplicate output.",
"Noise on drafts is a fast route to developers ignoring the bot entirely."],
e:"When your pipeline already knows a fact deterministically, branch on it in code rather than delegating it to the model. Drafts typically warrant a lighter touch &mdash; blocking issues only, no style commentary &mdash; which protects the bot's signal-to-noise ratio at the stage where the author is still iterating. The same reasoning applies to filtering generated files out of the diff.",
k:"Branch in the pipeline on facts the pipeline already knows. Only delegate genuine judgement to the model."},

{i:766,d:3,t:"3.6",s:"CCI",diff:2,
q:"You want CI review findings to appear as inline comments on the exact lines they concern.",
ask:"What does the output schema need?",
a:["A file path and line number per finding, so the pipeline can anchor each comment precisely",
"A severity rating per finding",
"A summary paragraph",
"A confidence score per finding"],
c:0,
w:["Correct. Anchoring requires the location as structured fields the pipeline can consume.",
"Severity is useful for filtering but does not locate the comment.",
"A summary cannot be anchored to lines.",
"Confidence does not locate anything."],
e:"Design the output schema around what the consumer needs to do with it. For inline comments that means file path and line number as structured fields, plus the issue, the severity and a suggested fix. Requesting that shape with <code>--json-schema</code> makes it a guarantee rather than a hope, and it is what turns a wall of prose into comments developers can act on where they are working.",
k:"Design the CI output schema around the consumer: path + line + issue + severity + suggested fix, enforced by <code>--json-schema</code>."},

{i:767,d:3,t:"3.1",s:"CGC",diff:2,
q:"Two CLAUDE.md files in the hierarchy give contradictory guidance and nobody noticed for months.",
ask:"What is the documented consequence and the remedy?",
a:["Claude may pick one arbitrarily; review CLAUDE.md files, nested files and <code>.claude/rules/</code> periodically to remove outdated or conflicting instructions",
"The more specific file always wins, so there is no consequence",
"Claude reports the conflict as an error",
"The conflict is resolved by load order deterministically"],
c:0,
w:["Correct. The documentation states the arbitrary-pick consequence and prescribes periodic review.",
"Files concatenate rather than override, so specificity does not resolve a contradiction.",
"No error is raised, which is why it went unnoticed.",
"Load order affects position, not which instruction is followed."],
e:"Because all applicable files are concatenated into context rather than overriding one another, two contradictory instructions leave the model with no principled basis for choosing &mdash; and the documentation says plainly that it may pick one arbitrarily. The remedies are periodic review across all three sources, phrasing narrower rules as explicit exceptions to broader ones, and <code>claudeMdExcludes</code> where other teams' files are the source of the conflict.",
k:"Contradictory instructions are picked arbitrarily. Review all three sources periodically; phrase local rules as explicit exceptions."},

{i:768,d:3,t:"3.2",s:"CGC",diff:2,
q:"A team wants a skill available to Claude automatically but is worried it will trigger too often.",
ask:"Which two fields together give the most control?",
a:["<code>when_to_use</code> to sharpen the trigger conditions, and <code>paths</code> to limit auto-activation to relevant files",
"<code>allowed-tools</code> and <code>disallowed-tools</code>",
"<code>context: fork</code> and <code>background</code>",
"<code>model</code> and <code>effort</code>"],
c:0,
w:["Correct. One sharpens the semantic trigger, the other scopes it structurally by file.",
"Those control capability, not activation.",
"Those control isolation and synchronicity.",
"Those control the model and reasoning budget."],
e:"Over-triggering is a selection problem, addressed the same way as tool over-selection. <code>when_to_use</code> adds trigger phrases and example requests to the description, sharpening the semantic boundary. <code>paths</code> adds a structural constraint so the skill is only considered when relevant files are in play. Together they narrow both dimensions. <code>disable-model-invocation: true</code> is the blunt fallback if it should never auto-trigger at all.",
k:"Control skill over-triggering with <code>when_to_use</code> (semantic) and <code>paths</code> (structural). <code>disable-model-invocation</code> is the blunt fallback."},

{i:769,d:3,t:"3.4",s:"CGC",diff:2,
q:"You are told planning mode is only for large tasks.",
ask:"What is the more accurate framing?",
a:["It is for tasks where the approach is genuinely uncertain or the consequence of choosing wrongly is high &mdash; which often but not always correlates with size",
"It is only for tasks over twenty files",
"It is for any task in an unfamiliar codebase",
"It is for any task involving more than one person"],
c:0,
w:["Correct. Uncertainty and consequence are the drivers; size is a correlate.",
"No file-count threshold defines it.",
"Unfamiliarity calls for exploration, which is a distinct need.",
"Team size is unrelated."],
e:"Size correlates with the real drivers without being one of them. A large mechanical refactor with one obvious approach needs no plan; a three-line change to an authorisation path does. The two genuine signals are <em>uncertainty about the approach</em> &mdash; several defensible options with different consequences &mdash; and <em>cost of getting it wrong</em>. Keep exploration separate: that addresses unfamiliarity and is delegable.",
k:"Planning is driven by approach uncertainty and consequence, not size. Exploration addresses unfamiliarity separately."},

{i:770,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to lock in a behaviour so it is not lost when the code is later refactored.",
ask:"What is the most durable mechanism?",
a:["A test asserting the behaviour, since it is checked automatically on every future change",
"A note in CLAUDE.md describing the behaviour",
"A code comment at the implementation site",
"A line in the pull request description"],
c:0,
w:["Correct. Tests are the only one of these that is verified automatically on every future change.",
"CLAUDE.md guides future work but does not verify that the behaviour still holds.",
"A comment can be deleted with the code it describes.",
"PR descriptions are historical records that nothing consults."],
e:"Distinguish guidance from verification. CLAUDE.md and rules shape how future work is done; tests verify that a specific behaviour still holds regardless. For an edge case you care about, the test is the durable artefact &mdash; which is why the recommended pattern converts a requirement into a test rather than a description, and why edge-case feedback should be given as input/output pairs that become regression tests.",
k:"Guidance shapes future work; tests verify it still holds. Lock behaviours in with tests, not with prose."},

{i:771,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review is running against the whole branch history rather than the change under review.",
ask:"What is the consequence?",
a:["Findings on code that was not changed by this pull request, which authors reasonably ignore as out of scope",
"The review will be more thorough and therefore better",
"The review will fail with an error",
"The review will be faster"],
c:0,
w:["Correct. Out-of-scope findings are noise that erodes trust in the whole bot.",
"Breadth without relevance is noise, not thoroughness.",
"No error occurs; it just reviews the wrong thing.",
"It will be slower, reviewing far more content."],
e:"Scope the input to the change under review. Findings about untouched code are correct in isolation but unactionable in this pull request, and authors learn to skim past the bot &mdash; the same trust erosion that false positives cause. Where pre-existing issues genuinely matter, surface them through a separate scheduled scan rather than attaching them to unrelated changes.",
k:"Review the change, not the codebase. Out-of-scope findings are noise; run a separate scan for pre-existing issues."},

{i:772,d:3,t:"3.1",s:"CGC",diff:3,
q:"An organisation wants both a machine-wide behavioural instruction and a machine-wide technical restriction.",
ask:"Where does each belong?",
a:["The behavioural instruction in a managed CLAUDE.md (or the <code>claudeMd</code> settings key); the technical restriction in managed settings such as <code>permissions.deny</code>",
"Both in managed CLAUDE.md",
"Both in managed settings",
"Both in each project's CLAUDE.md"],
c:0,
w:["Correct. The documentation draws this division explicitly: settings for technical enforcement, CLAUDE.md for behavioural guidance.",
"CLAUDE.md cannot enforce a technical restriction.",
"Settings are not the place for behavioural guidance.",
"Project files reach only repositories that have them and can be excluded."],
e:"The split mirrors the exam's central principle at organisational scale. Managed settings enforce: <code>permissions.deny</code> blocks tools, commands and paths; <code>sandbox.enabled</code> enforces isolation; <code>env</code> and login settings control routing and access. Managed CLAUDE.md guides: code style, data-handling reminders, behavioural instructions. Settings are enforced by the client regardless of what Claude decides; CLAUDE.md shapes behaviour without guaranteeing it.",
k:"Organisation-wide: managed settings for technical enforcement, managed CLAUDE.md for behavioural guidance."},

{i:773,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill needs to run with more reasoning budget than the session default.",
ask:"Which frontmatter field applies?",
a:["<code>effort</code>, which overrides the session effort while the skill is active",
"<code>model</code>",
"<code>context: fork</code>",
"<code>background</code>"],
c:0,
w:["Correct. <code>effort</code> sets the reasoning level for that skill's turn.",
"<code>model</code> changes which model runs, which is a related but distinct lever.",
"Forking isolates context.",
"That controls whether a forked skill runs asynchronously."],
e:"Skills can override several session-level settings for their own turn: <code>model</code> selects a different model (or <code>inherit</code>), and <code>effort</code> selects a reasoning level from <code>low</code> through <code>max</code>. Both revert afterwards. With <code>context: fork</code>, <code>model</code> sets the subagent's model rather than the session's &mdash; a useful combination for an expensive analysis skill that should run on a stronger model in isolation.",
k:"Skills override <code>model</code> and <code>effort</code> for their turn. With <code>context: fork</code>, <code>model</code> applies to the subagent."},

{i:774,d:3,t:"3.3",s:"CGC",diff:2,
q:"A rules file has no <code>paths</code> frontmatter.",
ask:"When does it load?",
a:["At launch, unconditionally, with the same priority as <code>.claude/CLAUDE.md</code>",
"Never, since rules require paths",
"Only when explicitly invoked",
"Only when a file in the rules directory is read"],
c:0,
w:["Correct. Rules without <code>paths</code> load unconditionally at launch.",
"<code>paths</code> is optional.",
"Rules are not invoked; skills are.",
"Rules are not tied to reading files unless they carry <code>paths</code>."],
e:"This is why splitting a large CLAUDE.md into unscoped rules files improves <em>organisation</em> but not context cost &mdash; unscoped rules load at launch just as CLAUDE.md does. The context saving comes specifically from adding <code>paths</code>, which makes loading conditional on matching files being read. Split for maintainability, scope for cost.",
k:"Unscoped rules load at launch like CLAUDE.md. Splitting aids maintainability; only <code>paths</code> saves context."},

{i:775,d:3,t:"3.4",s:"CGC",diff:2,
q:"A plan's phases each look small, but the plan has thirty of them.",
ask:"What should you consider?",
a:["Whether the decomposition is too fine, since thirty phases carries coordination overhead and may fragment work that belongs together",
"Whether each phase needs its own subagent",
"Whether the plan should be executed in reverse order",
"Whether more phases would be better still"],
c:0,
w:["Correct. Over-decomposition is a named risk in planning as much as in task delegation.",
"Thirty subagents for thirty small phases is heavy over-engineering.",
"Order follows dependencies, not preference.",
"More phases compounds the overhead."],
e:"Decomposition has an optimum in planning exactly as it does in multi-agent delegation. Too coarse and a phase is unverifiable; too fine and you pay coordination overhead, fragment naturally-cohesive work, and produce a plan nobody reads. Size phases so each ends at a point where the result can be meaningfully verified &mdash; that is what the checkpoints are for.",
k:"Size plan phases so each ends at a verifiable checkpoint. Thirty micro-phases is over-decomposition."},

{i:776,d:3,t:"3.5",s:"CGC",diff:2,
q:"You give feedback and the next iteration fixes the issue but breaks something that previously worked.",
ask:"What is missing from the workflow?",
a:["Regression tests covering the previously-working behaviour, so a fix cannot silently break it",
"More detailed feedback",
"A larger context window",
"A different model"],
c:0,
w:["Correct. Tests are what make previously-correct behaviour resistant to later changes.",
"Detail about the current issue does not protect unrelated behaviour.",
"Window size is not the cause.",
"Any model can break untested behaviour."],
e:"Iteration without regression coverage trades one defect for another. Building the test set first &mdash; expected behaviour, edge cases and performance requirements &mdash; means each iteration is checked against everything that previously worked, not just against the latest complaint. That is precisely the value the blueprint attributes to test-driven iteration.",
k:"Iteration without regression tests trades one defect for another. Build the test set before iterating."},

{i:777,d:3,t:"3.6",s:"CCI",diff:2,
q:"You want CI runs to be reproducible so a failure can be investigated later.",
ask:"What matters most?",
a:["Isolation by default with all context passed explicitly, so a run's inputs are fully determined and recreatable",
"Running on the same machine each time",
"Using the same model version",
"Logging the output"],
c:0,
w:["Correct. Explicit, fully-determined inputs are what make a run recreatable.",
"Machine identity rarely affects the result and is not the substantive factor.",
"Version pinning helps reproducibility but implicit context still makes runs non-deterministic.",
"Logging preserves the output without letting you recreate the run."],
e:"Reproducibility follows from determined inputs. Implicit session carryover makes a run depend on invisible state, so the same commit can produce different reviews. Isolation by default plus explicit context passing &mdash; the diff, the standards, prior findings if wanted &mdash; means the inputs are visible and recreatable. Model version pinning is a useful addition once the inputs are determined.",
k:"Reproducible CI = isolated by default, all context passed explicitly. Implicit carryover makes runs non-deterministic."},

{i:778,d:3,t:"3.1",s:"CGC",diff:2,
q:"A CLAUDE.md instruction is being followed inconsistently and you have confirmed the file loads.",
ask:"What is the documented next step?",
a:["Make the instruction more specific &mdash; concrete enough to verify &mdash; and look for conflicting instructions across files",
"Move the file to a different scope",
"Repeat the instruction three times in the file",
"Convert the file to a skill"],
c:0,
w:["Correct. Specificity and consistency are the two documented levers once loading is confirmed.",
"Scope determines who is affected, not adherence.",
"Repetition adds tokens without adding specificity.",
"Skills are invoked on demand; they do not replace always-on conventions."],
e:"The documented debugging sequence is worth following in order: confirm the file loaded via <code>/context</code>; check it is in a location that loads for your session; make the instruction concrete enough to verify (&ldquo;use 2-space indentation&rdquo; rather than &ldquo;format code properly&rdquo;); and look for conflicting instructions across CLAUDE.md files, nested files and rules. If it must run at a specific point regardless of what Claude decides, it belongs in a hook instead.",
k:"Adherence debugging: confirm it loaded &rarr; make it verifiable &rarr; remove conflicts &rarr; if it must always run, use a hook."},

{i:779,d:3,t:"3.2",s:"CGC",diff:3,
q:"A skill registers hooks in its frontmatter.",
ask:"What is the scope of those hooks?",
a:["They are registered when the skill is invoked and keep running for the rest of the session",
"They apply only during the skill's own turn",
"They apply only to tools the skill itself calls",
"They are discarded immediately after the skill completes"],
c:0,
w:["Correct. Skill-registered hooks persist for the remainder of the session.",
"Unlike <code>allowed-tools</code> and <code>disallowed-tools</code>, hooks are not turn-scoped.",
"They are session-scoped, not skill-call-scoped.",
"They persist rather than being discarded."],
e:"This is a meaningful asymmetry worth remembering. <code>allowed-tools</code> and <code>disallowed-tools</code> clear when you send your next message &mdash; they are turn-scoped. Hooks registered by a skill keep running for the rest of the session. That makes a skill a way to install durable behaviour, and it means a skill that registers an enforcement hook has effects well beyond its own invocation.",
k:"Skill <code>hooks</code> persist for the session; <code>allowed-tools</code> and <code>disallowed-tools</code> clear after the turn."},

{i:780,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want personal rules that apply to every project on your machine.",
ask:"Where do they go, and what is their priority?",
a:["<code>~/.claude/rules/</code>, loaded before project rules so project rules take higher priority",
"<code>~/.claude/rules/</code>, loaded after project rules so personal rules win",
"<code>.claude/rules/</code> with a personal naming convention",
"There is no user-level rules directory"],
c:0,
w:["Correct. User-level rules load first, giving project rules higher priority.",
"The ordering runs the other way.",
"That is the project directory and would be shared.",
"The directory exists."],
e:"User-level rules in <code>~/.claude/rules/</code> mirror user-level CLAUDE.md: personal, machine-wide, not shared. Loading before project rules means project conventions are read later and take precedence, which is the sensible default &mdash; a team convention should generally win over a personal preference. Note also that user-scope memory files' imports are trusted without the approval dialog that project-level external imports trigger.",
k:"<code>~/.claude/rules/</code> = personal, all projects, loaded before project rules so project rules take priority."},

{i:781,d:3,t:"3.4",s:"CGC",diff:2,
q:"You want the agent to explore a codebase without any possibility of modifying it during the exploration.",
ask:"What combination achieves this?",
a:["Planning mode for the workflow, and a read-only tool set for hard assurance",
"Planning mode alone, which forbids all tools",
"A prompt instruction not to modify anything",
"Direct execution with careful review afterwards"],
c:0,
w:["Correct. Planning mode structures the workflow; the tool set is what makes modification impossible.",
"Planning mode does not disable all tools &mdash; exploration requires reading and searching.",
"An instruction is guidance without enforcement.",
"Reviewing afterwards does not prevent modification."],
e:"Planning mode changes the workflow so that exploration precedes commitment, which is valuable. If you need a hard guarantee that nothing can be modified during exploration &mdash; say, on a production configuration repository &mdash; that comes from capability: withhold Write, Edit and Bash, or use <code>disallowedTools</code>. Workflow and enforcement are separate layers, as always.",
k:"Planning mode structures the workflow; a read-only tool set guarantees it. Workflow and enforcement are separate."},

{i:782,d:3,t:"3.5",s:"CGC",diff:2,
q:"You have three unrelated issues to raise and you provide them in one message.",
ask:"What is the risk?",
a:["Attention divides across three independent problems, so each gets less focus than it would alone",
"The model will address only the first",
"The model will refuse a multi-part request",
"There is no risk; batching is always more efficient"],
c:0,
w:["Correct. Independent issues get better treatment separately, which is the documented guidance.",
"It will typically attempt all three, just less thoroughly.",
"Multi-part requests are handled normally.",
"Batching helps for interdependent issues, not independent ones."],
e:"The rule is about interdependence, not count. Issues that share a root cause must be given together, or fixing one changes the others and produces rework. Independent issues get better attention one at a time. The failure mode being described here is attention dilution across unrelated problems &mdash; the same effect that makes a 3,000-line diff review shallow.",
k:"Interdependent issues together; independent issues separately. The rule is interdependence, not count."},

{i:783,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI job needs to know whether Claude Code exited because it completed or because it hit an error.",
ask:"What should the pipeline use?",
a:["The process exit code, plus the structured JSON output for detail about what happened",
"The presence of output text",
"The runtime of the job",
"A string match on the output"],
c:0,
w:["Correct. Exit code for the coarse signal, structured output for detail.",
"Output can be present in both success and failure.",
"Runtime says nothing about outcome.",
"String matching is brittle and the reason structured output exists."],
e:"Use the coarse and fine signals together. The exit code gives the pipeline a reliable pass or fail. The structured output &mdash; <code>--output-format json</code> constrained by <code>--json-schema</code> &mdash; carries the detail your pipeline branches on, such as findings and their severities. Reserve <code>stream-json --verbose</code> for when you need to debug what the run actually did, including hook responses.",
k:"Exit code for the coarse outcome, <code>--output-format json</code> for detail, <code>stream-json --verbose</code> for debugging."},

{i:784,d:3,t:"3.1",s:"CGC",diff:2,
q:"A CLAUDE.md file has grown past 4 MiB.",
ask:"What happens?",
a:["Claude Code skips the file entirely",
"It is truncated to the first 4 MiB",
"It loads but adherence degrades",
"It causes a startup error"],
c:0,
w:["Correct. A CLAUDE.md over 4 MiB is skipped rather than partially loaded.",
"It is skipped, not truncated.",
"It does not load at all at that size.",
"No error is raised; it is silently skipped."],
e:"Two thresholds worth distinguishing. The <em>target</em> is under about 200 lines, because longer files reduce adherence &mdash; a quality guideline. The <em>hard limit</em> is 4 MiB, beyond which the file is skipped entirely. Silent skipping is why <code>/context</code> is the right first check when a CLAUDE.md appears to be ignored: if it is not listed under Memory files, Claude never saw it.",
k:"CLAUDE.md: target under ~200 lines for adherence; over 4 MiB it is silently skipped. Check <code>/context</code>."},

{i:785,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a command to exist but never be auto-invoked by Claude, only run by a developer.",
ask:"Which field does this?",
a:["<code>disable-model-invocation: true</code>",
"<code>user-invocable: false</code>",
"<code>paths</code>",
"<code>context: fork</code>"],
c:0,
w:["Correct. It prevents Claude from loading the skill automatically, leaving manual invocation only.",
"That is the mirror image &mdash; it hides it from you and leaves automatic invocation.",
"<code>paths</code> scopes auto-activation, and commands ignore it anyway.",
"Forking controls context isolation."],
e:"Use <code>disable-model-invocation: true</code> for anything with side effects you want triggered deliberately &mdash; a deploy, a commit, a destructive cleanup. The mirror field, <code>user-invocable: false</code>, is for background behaviours that should not clutter the <code>/</code> menu. They are commonly offered as distractors for each other, so anchor on which party is being excluded.",
k:"<code>disable-model-invocation: true</code> = manual only. <code>user-invocable: false</code> = automatic only."},

{i:786,d:3,t:"3.3",s:"CGC",diff:3,
q:"You want to understand why a path-scoped rule fired for a file you did not expect to match.",
ask:"What is a likely cause worth checking?",
a:["A brace-expansion or bracket pattern matching more broadly than intended, or a symlinked path resolving into the project directory",
"Rules always fire regardless of paths",
"The rule was cached from a previous session",
"Path matching is case-insensitive on all platforms"],
c:0,
w:["Correct. Both over-broad expansion and symlink resolution are documented causes of unexpected matching.",
"<code>paths</code> genuinely constrains matching.",
"There is no cross-session rule cache.",
"Case sensitivity varies by platform and is not the primary cause here."],
e:"Two documented subtleties. Brace expansion multiplies patterns quickly &mdash; <code>{a,b}/{c,d}/*.{ts,tsx}</code> expands to eight &mdash; and a broad expansion can cover more than intended. And path matching also works when Claude reaches a file through a symlinked path into the project directory, which can pull in files you thought were outside scope. The <code>InstructionsLoaded</code> hook is the tool for confirming exactly which rules loaded and why.",
k:"Unexpected rule matches: check brace expansion breadth and symlinked paths. Debug with the <code>InstructionsLoaded</code> hook."},

{i:787,d:3,t:"3.4",s:"CGC",diff:2,
q:"You want to use plan mode specifically for changes under one directory.",
ask:"Where would you express that preference?",
a:["In CLAUDE.md as a stated convention, since it is behavioural guidance rather than an enforced constraint",
"In <code>.mcp.json</code>",
"In a hook",
"It cannot be expressed"],
c:0,
w:["Correct. A workflow preference is behavioural guidance, which is what CLAUDE.md is for.",
"MCP configuration defines servers, not workflow preferences.",
"Hooks enforce at tool boundaries; this is a workflow preference, not a boundary.",
"It is straightforwardly expressible as a convention."],
e:"The documentation itself shows this pattern &mdash; a CLAUDE.md line saying to use plan mode for changes under a particular path. It is guidance rather than enforcement, which is appropriate: using direct execution once on a file in that directory is a minor inefficiency, not an audit finding. Apply the standard test &mdash; what does one violation cost? &mdash; and a workflow preference lands squarely in prompt territory.",
k:"Workflow preferences like &ldquo;plan mode for this directory&rdquo; belong in CLAUDE.md as guidance, not in a hook."},

{i:788,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to communicate a requirement that involves timing &mdash; an operation must complete before another begins.",
ask:"What is the most reliable specification?",
a:["A test that fails if the ordering is violated, since ordering is behaviour and tests verify behaviour",
"A prose description of the required order",
"A numbered list in the prompt",
"A comment in the code"],
c:0,
w:["Correct. A test verifies the ordering permanently rather than describing it once.",
"Prose is understood at implementation time and unverified thereafter.",
"A numbered list is clearer prose, still unverified.",
"A comment documents intent without checking it."],
e:"Ordering constraints are behaviour, so they belong in tests. Note the parallel with Domain 1's enforcement question: if the ordering carries business or security consequence at <em>runtime</em>, a test is not sufficient either &mdash; that needs a precondition in code, so the sequence cannot be violated regardless of what any caller does. Tests verify your implementation; preconditions constrain every caller.",
k:"Ordering requirements go in tests. If violation carries runtime consequence, add a precondition too."},

{i:789,d:3,t:"3.6",s:"CCI",diff:2,
q:"You want to reduce CI token cost without reducing review quality.",
ask:"Which change is most effective?",
a:["Scope the input precisely &mdash; the diff plus the consuming context and the relevant standards &mdash; rather than sending the whole repository",
"Use a smaller model",
"Reduce the review criteria",
"Run the review less often"],
c:0,
w:["Correct. Most CI token cost is irrelevant input, and removing it improves quality as well as cost.",
"A weaker model trades quality for cost directly.",
"Fewer criteria means less coverage.",
"Less frequent review lets defects reach main."],
e:"Input scoping is the rare optimisation that improves cost and quality together: irrelevant content both costs tokens and dilutes attention. Send the diff, the files that consume the changed symbols, the relevant path-scoped standards, and prior findings if this is a re-run. Filter out generated files, vendored dependencies and lockfiles at input construction rather than instructing the model to ignore them.",
k:"Scope CI input precisely. Removing irrelevant content cuts cost and improves attention at the same time."},

{i:790,d:3,t:"3.1",s:"CGC",diff:2,
q:"You are asked when something should be added to CLAUDE.md.",
ask:"Which trigger is documented?",
a:["When Claude makes the same mistake a second time, or you type the same correction you typed last session",
"When a new file is added to the project",
"When a dependency is upgraded",
"At the start of every sprint"],
c:0,
w:["Correct. Repetition of a correction is the documented signal.",
"File additions are derivable from the codebase.",
"Dependency versions are derivable from the manifest.",
"Calendar-driven additions accumulate content without a trigger."],
e:"The documented triggers are all forms of repetition: Claude makes the same mistake twice; a code review catches something Claude should have known; you type the same correction you typed last session; a new teammate would need the same context. The unifying test is whether you are re-explaining something. And if the entry turns out to be a multi-step procedure or only relevant to one part of the codebase, it belongs in a skill or a path-scoped rule instead.",
k:"Add to CLAUDE.md when you re-explain something. Multi-step procedures go to skills; area-specific rules go to path scoping."}
);
