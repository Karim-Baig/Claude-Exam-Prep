QB.push(
{i:991,d:1,t:"1.1",s:"CSA",diff:2,
q:"An agent's conversation shows it calling a tool, receiving a clear result, then asking the customer for information the result already contained.",
ask:"What is the most likely cause?",
a:["The result is present but hard to interpret &mdash; unlabelled fields or an unfamiliar shape &mdash; so the agent did not recognise it had the answer",
"The customer's original message was unclear",
"The tool returned the wrong data",
"The model needs a longer context window"],
c:0,
w:["Correct. Legibility failures produce exactly this &mdash; the data is there and the agent does not use it.",
"The tool result contained the answer, so the ambiguity is downstream of the customer.",
"The premise states the result was clear in content.",
"The result is recent, so window size is not the issue."],
e:"Asking for information you already have is a tool-output legibility problem, and it damages the customer experience directly. Return clearly labelled fields, echo the query the result answers, and keep the shape consistent across calls. This is the same root cause as redundant re-querying, showing up as a conversational failure rather than a latency one.",
k:"Asking for data the tool already returned is a legibility failure. Label fields, echo the query, keep shapes consistent."},

{i:992,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator's subagents each produce good work, but the final report reads as several disconnected sections.",
ask:"What is missing?",
a:["A synthesis step with a mandate to integrate rather than concatenate, and criteria describing what an integrated report looks like",
"More subagents",
"A larger context window for synthesis",
"Better subagent output"],
c:0,
w:["Correct. Disconnected sections is the signature of concatenation rather than synthesis.",
"More agents produces more sections.",
"Window size does not create an integration mandate.",
"The premise states the subagent work is good."],
e:"Concatenation looks like synthesis until you read it. Give the synthesis agent an explicit mandate &mdash; identify themes across sections, reconcile conflicts with attribution, note where findings reinforce or contradict each other, produce a unified narrative &mdash; and quality criteria describing an integrated report. Without that, it defaults to presenting the inputs in order, which is what the coordinator could have done itself.",
k:"Disconnected sections mean concatenation, not synthesis. Give the synthesis agent an integration mandate and criteria."},

{i:993,d:1,t:"1.3",s:"AAT",diff:2,
q:"You are writing a delegation and are unsure how much background to include.",
ask:"What is the test?",
a:["Whether a competent stranger could do the task from the prompt alone, since that is exactly the subagent's position",
"Whether it fits in a paragraph",
"Whether the coordinator already knows it",
"Whether it duplicates the system prompt"],
c:0,
w:["Correct. The competent-stranger test captures the subagent's actual situation.",
"Length is not the criterion.",
"What the coordinator knows is precisely what the subagent does not.",
"Duplicating the subagent's own system prompt is wasteful, but that is a narrower check."],
e:"The competent-stranger test is the practical form of &ldquo;subagents inherit nothing&rdquo;. Assume a capable person who knows the domain but has not seen your conversation: could they do the task from this prompt? That immediately surfaces the dangling references &mdash; &ldquo;the document&rdquo;, &ldquo;as discussed&rdquo;, &ldquo;the usual criteria&rdquo; &mdash; and the missing output contract and quality bar.",
k:"Test delegations with the competent-stranger question. It surfaces dangling references and missing contracts."},

{i:994,d:1,t:"1.4",s:"CSA",diff:2,
q:"You have enforced a rule in code and the agent now produces a poor conversation when it hits the block.",
ask:"What is missing?",
a:["The rule stated in the prompt so the agent understands it, and a block message explaining it &mdash; enforcement without understanding produces a confused agent",
"Removal of the enforcement",
"A softer enforcement threshold",
"An apology template"],
c:0,
w:["Correct. Enforcement guarantees the outcome; the prompt and block message make the agent competent around it.",
"Removing the control defeats its purpose.",
"Threshold changes alter which cases block, not the quality of the conversation.",
"A template does not give the agent the understanding to explain the situation."],
e:"This is the complementarity point in its most practical form. An agent that knows the $500 threshold proactively routes larger refunds to escalation, explains the reason, and sets expectations &mdash; without ever hitting the block. An agent that discovers the rule only by being blocked burns turns and produces a confused conversation. Layer both: hook for the guarantee, prompt for the competence, explanatory block message for the case that still occurs.",
k:"Enforcement guarantees the outcome; the prompt and an explanatory block message make the agent competent around it."},

{i:995,d:1,t:"1.5",s:"AAT",diff:2,
q:"You are told hooks make a system harder to reason about.",
ask:"What is the balanced view?",
a:["They do add indirection, which is why they need single responsibilities, documented order and tested composition &mdash; but they are the only mechanism that guarantees a rule",
"They add no complexity",
"They should be avoided for that reason",
"Complexity is irrelevant if the rule matters"],
c:0,
w:["Correct. Acknowledge the cost and manage it, rather than denying it or abandoning the mechanism.",
"Fourteen overlapping hooks is a real and documented failure mode.",
"Avoiding hooks means abandoning enforcement, which is worse.",
"Complexity that makes behaviour unpredictable undermines the rule it was meant to protect."],
e:"Hooks are indirection: behaviour that is not visible in the prompt or the tool definition. That is a genuine cost, and it becomes severe when hooks accumulate without discipline. Manage it &mdash; one responsibility each, documented execution order, tested as a composed pipeline, and logged decisions &mdash; and keep them for what only they can do, which is guarantee an outcome.",
k:"Hooks add real indirection. Manage it with single responsibilities, documented order and tested composition."},

{i:996,d:1,t:"1.6",s:"CGC",diff:2,
q:"A task could be decomposed by layer (data, logic, interface) or by feature.",
ask:"What should decide?",
a:["Which axis produces subtasks that can be verified independently and recombine cleanly &mdash; usually feature slices, since a layer alone is rarely verifiable",
"Layer decomposition, since it matches the architecture",
"Feature decomposition, always",
"Whichever produces more subtasks"],
c:0,
w:["Correct. Independent verifiability and clean recombination are the criteria, and vertical slices usually satisfy them better.",
"Matching the architecture is not the same as producing verifiable slices.",
"Feature slicing is usually better here but not universally &mdash; a shared contract may warrant a design phase first.",
"Count is not a goal."],
e:"Apply the two decomposition tests. Does each slice produce something meaningful alone? A data layer with no logic or interface usually does not; a thin vertical slice through all three usually does. Does it recombine cleanly? Vertical slices generally do. The exception is when the layers share a contract that must be designed once &mdash; then a shared design phase followed by parallel feature slices is the hybrid.",
k:"Slice for independent verifiability and clean recombination &mdash; usually vertical. Shared contracts warrant a design phase first."},

{i:997,d:1,t:"1.7",s:"CGC",diff:2,
q:"You want to know whether to resume a session or start fresh, and you are not sure what has changed.",
ask:"What is the safe default?",
a:["Start fresh with a structured summary, since resuming with stale context fails silently while restarting only costs re-establishing what you summarised",
"Resume, since it preserves more",
"Resume and ask the agent whether anything looks stale",
"Neither; wait until you know"],
c:0,
w:["Correct. The failure modes are asymmetric &mdash; stale context fails silently, a fresh start merely costs a little.",
"Resuming preserves stale content as faithfully as valid content.",
"The agent cannot detect staleness in its own cached results.",
"Waiting does not resolve the uncertainty."],
e:"Compare the failure modes. Resuming with stale context produces confident work built on a false picture of the world, and it fails silently. Starting fresh with an accurate summary costs re-establishing some understanding, and it fails visibly if the summary is thin. Under uncertainty, prefer the failure mode you can see &mdash; which is also why the blueprint notes a fresh session can be <em>more</em> reliable than resuming.",
k:"Under uncertainty, restart fresh with a summary. Stale context fails silently; a thin summary fails visibly."},

{i:998,d:2,t:"2.1",s:"AAT",diff:2,
q:"You are reviewing a tool description that states what the tool does but not what it returns.",
ask:"Why does the return shape matter for selection?",
a:["Because choosing a tool involves anticipating whether its output will answer the question, so an unknown return shape makes that judgement guesswork",
"It does not; selection depends only on the input",
"Because the API validates return shapes",
"Because it affects token cost"],
c:0,
w:["Correct. The model chooses a tool partly on whether its output will serve the goal.",
"Selection weighs whether the tool will produce what is needed.",
"The API does not validate tool result shapes.",
"Cost is incidental to the selection decision."],
e:"Selection is goal-directed: the model is choosing an action expected to advance the task, which requires anticipating the output. A description that says what a tool does but not what it returns leaves that anticipation to guesswork &mdash; and produces both wrong selections and redundant follow-up calls when the output turns out not to answer the question. State the return shape briefly alongside the input contract.",
k:"State what a tool returns, not just what it does. Selection depends on anticipating whether the output serves the goal."},

{i:999,d:2,t:"2.2",s:"AAT",diff:2,
q:"A tool returns an error and the agent's recovery makes the situation worse.",
ask:"What should you examine?",
a:["Whether the error's category and suggested next step were accurate, since a mislabelled error directs the agent toward the wrong recovery",
"Whether the agent's model is capable enough",
"Whether the retry budget is too high",
"Whether the tool should be removed"],
c:0,
w:["Correct. Agents act on what errors tell them, so a mislabelled error causes a confidently wrong recovery.",
"Capability does not compensate for false information.",
"Budget affects how many attempts, not whether the direction was right.",
"Removing the tool is disproportionate."],
e:"Errors are instructions to the caller, so a wrong classification produces a confidently wrong recovery &mdash; a permission failure labelled transient produces a retry storm, a business-rule violation labelled retryable produces the same. Audit your error mapping against real failures periodically, particularly at the boundary where you translate an upstream service's errors into your own contract.",
k:"Mislabelled errors cause confidently wrong recoveries. Audit the mapping from upstream failures to your error contract."},

{i:1000,d:2,t:"2.3",s:"AAT",diff:2,
q:"You want to allow an agent to read production data but only through an aggregate that cannot expose individual records.",
ask:"How should this be expressed?",
a:["As a tool that returns only the aggregate, rather than a general query tool with an instruction to aggregate",
"A general query tool plus a prompt instruction",
"A general query tool plus a hook checking the result",
"A general query tool with a row limit"],
c:0,
w:["Correct. Build the constraint into the capability so individual records are never reachable.",
"An instruction guarding a data-exposure boundary is insufficient.",
"Checking results after the query means the records were already retrieved.",
"A row limit still exposes individual records, just fewer of them."],
e:"Where the constraint is on what data may be reached at all, build it into the tool's contract rather than filtering afterwards. An aggregate-only tool cannot return an individual record no matter how it is called &mdash; the same structural reasoning as omitting a write tool rather than blocking writes. Post-hoc filtering also has a subtler problem: the records have already entered your system and possibly your logs.",
k:"Build data-exposure constraints into the tool's contract. Post-hoc filtering means the data was already retrieved."},

{i:1001,d:2,t:"2.4",s:"DPT",diff:2,
q:"Your team relies on a community MCP server that has become unmaintained.",
ask:"What are the practical options?",
a:["Pin the last working version while evaluating whether to fork, replace it, or wrap a narrower internal equivalent",
"Continue using latest and hope",
"Immediately remove it",
"Fork it immediately"],
c:0,
w:["Correct. Pin for stability, then evaluate the options deliberately.",
"Tracking latest on an unmaintained project offers no benefit and some risk.",
"Removing a working integration abruptly disrupts the team.",
"Forking takes on permanent maintenance before you have decided it is warranted."],
e:"Preferring community servers means accepting a dependency you do not control, and abandonment is one of the risks that comes with it. Pin the working version to stabilise, then evaluate: is there a maintained alternative, is the surface small enough to reimplement narrowly, or is forking justified? A narrow internal replacement is often attractive, since you probably use a fraction of the surface anyway.",
k:"Unmaintained dependency: pin first, then evaluate replace, wrap narrowly, or fork. A narrow reimplementation is often smallest."},

{i:1002,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to determine whether a pattern appears anywhere in a large repository, and the answer is likely no.",
ask:"What is the efficient approach?",
a:["A content search returning paths only, since a negative result costs one cheap operation and no reads",
"Reading the most likely files to confirm absence",
"Globbing for candidate files then reading each",
"Asking the developer"],
c:0,
w:["Correct. Search answers existence cheaply, and a negative result is definitive without any reads.",
"Reading a few files cannot establish absence across a repository.",
"Reading candidates to establish absence is the expensive way round.",
"The agent can determine this directly."],
e:"Negative results are where content search is most valuable: one cheap operation establishes absence across the whole tree, which no amount of reading can do. Request paths only rather than matching lines when the question is existence &mdash; matching the output mode to the question is the same discipline as trimming tool output to the fields you need.",
k:"Search establishes absence cheaply and definitively; reading cannot. Request paths only for existence questions."},

{i:1003,d:3,t:"3.1",s:"CGC",diff:2,
q:"A team maintains CLAUDE.md carefully but never removes anything.",
ask:"What is the predictable outcome?",
a:["Accumulated stale and low-value content dilutes the instructions that still matter, reducing adherence to all of them",
"The file becomes more useful over time",
"Nothing, since Claude ignores irrelevant instructions",
"Only the file size grows"],
c:0,
w:["Correct. Adherence degrades with length, and the degradation affects every instruction.",
"Accretion without pruning degrades rather than improves it.",
"Nothing detects irrelevance; every line competes for attention.",
"The consequence is behavioural, not merely a size metric."],
e:"CLAUDE.md needs pruning as much as addition. Stale entries cost context on every request, dilute the instructions that still matter, and can actively mislead when they reference things that no longer exist. Prune as part of the change that makes an entry obsolete, and periodically apply the trim heuristic: cut what is derivable from the code, keep pitfalls, rationale and non-default conventions.",
k:"Prune CLAUDE.md as you add. Accumulated stale content dilutes adherence to everything else."},

{i:1004,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a skill's behaviour to be testable before the team relies on it.",
ask:"What makes a skill testable?",
a:["A narrow, stated purpose with a defined output, so you can run it on known inputs and check the result",
"A short prompt",
"Running it in a forked context",
"Restricting its tools"],
c:0,
w:["Correct. Testability comes from a defined contract: known inputs, expected outputs.",
"Brevity does not confer a checkable contract.",
"Forking isolates context without defining the contract.",
"Tool restriction constrains capability without defining expected output."],
e:"Skills deserve the same contract discipline as tools and subagents. A skill with a stated purpose and a defined output shape can be run on representative inputs and checked; one that does &ldquo;whatever seems appropriate&rdquo; cannot be. Where the output is consumed programmatically, enforce it with a schema rather than describing it &mdash; that makes the shape a guarantee and the test meaningful.",
k:"Testable skills have a narrow stated purpose and a defined output. Enforce the shape with a schema where it is consumed."},

{i:1005,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want a convention to apply to a file type only within one part of the repository.",
ask:"How is this expressed?",
a:["A path glob combining both constraints, such as <code>services/payments/**/*.test.ts</code>",
"Two separate rules",
"A directory CLAUDE.md plus a file-type rule",
"It cannot be expressed"],
c:0,
w:["Correct. A single glob can constrain both location and file type.",
"Two rules would each be broader than intended.",
"A directory CLAUDE.md would apply to all file types in that subtree.",
"Globs combine both constraints naturally."],
e:"Globs express location and file type together, which is what makes path-scoped rules precise. <code>services/payments/**/*.test.ts</code> covers test files in one service and nothing else &mdash; narrower than a directory CLAUDE.md, which would apply to everything in that subtree, and narrower than a file-type rule, which would apply across every service. Match the glob to the convention's exact scope.",
k:"Globs constrain location and file type together. Match the glob to the convention's exact scope &mdash; no wider."},

{i:1006,d:3,t:"3.4",s:"CGC",diff:2,
q:"A plan is produced but you disagree with one phase while the rest is sound.",
ask:"What is the efficient response?",
a:["Revise that phase specifically, keeping the rest &mdash; a plan is a document to amend, not accept or reject wholesale",
"Reject the plan and request a new one",
"Accept it and fix the phase during execution",
"Execute the phases you agree with only"],
c:0,
w:["Correct. Targeted revision preserves the sound work and is far cheaper than regeneration.",
"Regenerating discards sound phases and may lose reasoning you agreed with.",
"Fixing during execution is where phase-level rework starts.",
"Skipping a phase may leave a dependency unmet."],
e:"Plans are working documents. Revise the phase you disagree with, state why, and check whether downstream phases depended on the version you replaced &mdash; the dependency structure is exactly what the plan file records. That targeted revision is also what makes planning cheap: being wrong at plan time costs one amendment, being wrong at execution time costs rework.",
k:"Amend plans phase by phase and check downstream dependencies. Wholesale regeneration discards sound reasoning."},

{i:1007,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to communicate a preference that has no objectively correct answer, such as naming style.",
ask:"What is the appropriate mechanism?",
a:["State it as a convention in CLAUDE.md &mdash; and where a linter can enforce it mechanically, prefer the linter",
"A hook blocking non-conforming names",
"A test asserting naming conventions",
"Leave it to the model's judgement"],
c:0,
w:["Correct. Conventions belong in CLAUDE.md, and mechanically-checkable ones belong in tooling that is stronger and free at inference time.",
"A hook for a naming preference is disproportionate enforcement.",
"A naming test is unusual and brittle compared with a linter rule.",
"Unstated preferences produce inconsistency."],
e:"Push each rule to the strongest applicable tier. Naming style is mechanically checkable, so a linter enforces it deterministically at no inference-time cost &mdash; and that also frees the CLAUDE.md line for something only CLAUDE.md can express. Where no linter rule fits, state it as a convention with a concrete example rather than a description.",
k:"Mechanically checkable preferences go to linters, freeing CLAUDE.md for judgement conventions only it can express."},

{i:1008,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review works well locally but produces different findings in CI.",
ask:"What should you check first?",
a:["Whether the same configuration loaded &mdash; CLAUDE.md, path-scoped rules and setting sources &mdash; since generic CI findings usually mean standards did not load",
"Whether CI uses a different model",
"Whether CI has less memory",
"Whether CI runs at a different time"],
c:0,
w:["Correct. Configuration divergence is the most common cause of CI findings differing from local ones.",
"Worth checking, but configuration loading is the more common cause.",
"Memory does not change which standards apply.",
"Timing is irrelevant."],
e:"Local and CI runs should share configuration, and when findings diverge the usual cause is that they did not. Check whether setting sources exclude <code>project</code>, whether the working directory means the expected CLAUDE.md files are in the load path, and whether path-scoped rules matched given the files in the diff. <code>/context</code> locally and an <code>InstructionsLoaded</code> hook in CI answer this directly.",
k:"Divergent CI findings usually mean configuration did not load. Check setting sources, load paths and rule matching."},

{i:1009,d:4,t:"4.1",s:"SDE",diff:2,
q:"You are writing criteria and find yourself unable to state precisely what qualifies.",
ask:"What does this indicate?",
a:["The requirement is not yet understood well enough to automate &mdash; resolve it with examples and labelled cases before writing the criterion",
"The criterion should be left vague and refined later",
"A more capable model would resolve it",
"The criterion should be dropped"],
c:0,
w:["Correct. Inability to state a criterion is a specification gap, not a prompting problem.",
"A vague criterion produces inconsistent behaviour that is hard to diagnose later.",
"Capability cannot resolve a requirement you have not defined.",
"The requirement may be genuinely valuable once defined."],
e:"Difficulty writing a criterion is diagnostic. Work from cases instead: gather examples that clearly qualify and clearly do not, look at what distinguishes them, and write the criterion from that. If competent people disagree on the boundary cases, the field is genuinely subjective &mdash; which is workable with borderline examples and an <code>unclear</code> option, but it also sets a realistic accuracy ceiling.",
k:"If you cannot state the criterion, work from labelled cases. Persistent disagreement means it is genuinely subjective."},

{i:1010,d:4,t:"4.2",s:"SDE",diff:2,
q:"You want to know which of your few-shot examples is doing the work.",
ask:"How would you find out?",
a:["Remove them one at a time and measure the effect on a fixed labelled set",
"Ask the model which example it used",
"Count how often each example's pattern appears in output",
"Assume the longest one"],
c:0,
w:["Correct. Ablation on a fixed labelled set isolates each example's contribution.",
"The model cannot reliably report which example influenced it.",
"Pattern frequency conflates the example's influence with the input distribution.",
"Length is unrelated to contribution."],
e:"Ablation applies to examples as it does to CLAUDE.md content. Remove one, run the fixed labelled set, and measure. Examples that change nothing are costing tokens on every request for no benefit; examples whose removal degrades a specific segment tell you exactly what they are teaching. This is also how you find that a set has become redundant as the prompt or schema improved.",
k:"Ablate examples one at a time against a fixed labelled set. No-effect examples are pure recurring cost."},

{i:1011,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want extracted records to be safely mergeable when the same document is processed twice.",
ask:"What should the schema include?",
a:["A stable identifier derived from the source, so a repeat extraction updates rather than duplicates",
"A timestamp of extraction",
"A random record id",
"A sequence number"],
c:0,
w:["Correct. A source-derived stable key is what makes repeat processing idempotent.",
"A timestamp distinguishes runs without identifying the record.",
"A random id guarantees duplication on reprocessing.",
"Sequence numbers depend on processing order and are not stable."],
e:"This is idempotency applied to extraction output. A key derived from the source &mdash; document id, or a hash of identifying fields &mdash; means reprocessing updates the existing record rather than creating a second one. It matters because reprocessing happens routinely: batch re-submissions after expiry, retries, and pipeline reruns after a prompt change.",
k:"Use a source-derived stable key so reprocessing updates rather than duplicates. Reprocessing is routine."},

{i:1012,d:4,t:"4.4",s:"SDE",diff:2,
q:"A validation rule and a schema constraint both check the same thing.",
ask:"Is this redundancy a problem?",
a:["Not necessarily &mdash; but check the schema constraint is the tighter one, since anything the schema can express is caught earlier and more cheaply",
"Yes, remove one",
"Yes, remove the schema constraint",
"No, and both should always be maintained separately"],
c:0,
w:["Correct. Prefer the schema where it can express the constraint; the validation layer covers what it cannot.",
"Some overlap is fine; the question is which layer is doing the work.",
"The schema constraint is the cheaper and earlier check &mdash; keep it.",
"Maintaining duplicate logic in two places invites drift."],
e:"Layer the checks by what each can express. Schema constraints &mdash; types, enums, formats, ranges, required fields &mdash; are enforced structurally and catch problems before any output is produced. Domain validation handles what schemas cannot: cross-field consistency, plausibility, conditional requirements. Duplicating a schema-expressible check in code is harmless but adds a place to drift.",
k:"Push checks to the schema where it can express them; reserve validation code for cross-field and plausibility rules."},

{i:1013,d:4,t:"4.5",s:"SDE",diff:2,
q:"You want a batch pipeline to be safe to re-run after a partial failure.",
ask:"What does that require?",
a:["Idempotent output writes keyed on a stable identifier, so re-processing an item updates rather than duplicates",
"A longer batch window",
"A larger batch size",
"More retries"],
c:0,
w:["Correct. Idempotent writes make re-submission safe, which is what per-item recovery depends on.",
"Window length does not make re-runs safe.",
"Size is unrelated to idempotency.",
"Retries without idempotency create duplicates."],
e:"Per-item recovery &mdash; re-submitting failed or expired items by <code>custom_id</code> &mdash; is only safe if writing the result twice is harmless. Key writes on a stable source-derived identifier so a repeat updates the existing record. Without that, every recovery run risks duplicating the records that succeeded the first time.",
k:"Batch recovery requires idempotent writes keyed on a stable identifier, or re-submission duplicates records."},

{i:1014,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want to reduce review latency without reducing coverage.",
ask:"What is the appropriate lever?",
a:["Run independent passes concurrently, since per-file passes have no dependency on each other",
"Reduce the number of passes",
"Use a faster model",
"Move to batch processing"],
c:0,
w:["Correct. Independent passes parallelise, converting a serial sum into a maximum.",
"Fewer passes reduces coverage, which is the constraint.",
"A faster model trades quality for latency.",
"Batch has no latency SLA and is unsuitable for a blocking check."],
e:"Per-file passes are independent by construction, so running them concurrently turns total latency from the sum into roughly the maximum, plus the cross-file pass that depends on them. That preserves the multi-pass architecture's coverage benefit entirely. Note the parallel with agent fan-out: independence permits concurrency, and the critical path is the longest dependency chain.",
k:"Parallelise independent review passes. Latency becomes the longest chain, not the sum, with coverage unchanged."},

{i:1015,d:5,t:"5.1",s:"CAP",diff:2,
q:"An agent's persistent block includes a constraint that has since been lifted.",
ask:"What is the consequence?",
a:["The agent continues applying a constraint that no longer holds, and does so confidently because the block is authoritative",
"Nothing, since the constraint is harmless",
"The agent will notice the contradiction",
"The constraint will expire automatically"],
c:0,
w:["Correct. An authoritative record that is wrong produces confidently wrong behaviour.",
"An applied constraint that should not apply changes outcomes.",
"Nothing signals that the block is stale.",
"Nothing expires; your code maintains it."],
e:"The facts block's authority is exactly why it must be maintained. Remove entries when they stop applying, just as you update them on correction. A stale entry is worse than an absent one, because the agent treats the block as current state and applies it without hesitation &mdash; the same hazard as a stale scratchpad or a stale plan file.",
k:"Remove facts-block entries when they stop applying. An authoritative record that is stale produces confident errors."},

{i:1016,d:5,t:"5.2",s:"CSA",diff:2,
q:"You want to reduce escalations without reducing appropriate ones.",
ask:"Where should you look?",
a:["At escalations that a human resolved without needing judgement the agent lacked &mdash; those indicate a capability or criteria gap the agent could close",
"At the total escalation count",
"At the longest conversations",
"At customer satisfaction scores"],
c:0,
w:["Correct. Escalations a human resolved routinely are the ones that should not have escalated.",
"A total does not distinguish appropriate from inappropriate.",
"Length is a weak proxy for whether escalation was warranted.",
"Satisfaction is confounded by many factors."],
e:"Escalation quality is measured by what the human had to do. If the human applied documented policy the agent could have applied, that is a criteria gap &mdash; sharpen the boundary and add contrastive examples. If the human used a capability the agent lacks, that is a tool gap &mdash; consider building it. Escalations requiring genuine judgement or authority are the ones that should be there.",
k:"Judge escalations by what the human had to do. Routine resolutions indicate a criteria or capability gap."},

{i:1017,d:5,t:"5.3",s:"MAR",diff:2,
q:"A coordinator receives errors from several subagents and handles each independently.",
ask:"What might it miss?",
a:["A pattern across them &mdash; a shared dependency failing, or a systematic cause that individual handling cannot see",
"Nothing; independent handling is complete",
"The order the errors arrived in",
"The subagents' identities"],
c:0,
w:["Correct. Correlated failures carry information no single error does.",
"Independent handling misses cross-error patterns.",
"Arrival order is rarely meaningful.",
"Identity is captured per error."],
e:"The coordinator is the only component that sees all the errors, which makes correlation detection its unique contribution. Three simultaneous transient failures point at a shared dependency, and the right response &mdash; back off, or trip a circuit breaker &mdash; differs from retrying each independently, which would add load to a struggling service. Look at the set, not just each member.",
k:"Only the coordinator sees all errors. Correlated failures indicate a shared cause and warrant backing off, not retrying each."},

{i:1018,d:5,t:"5.4",s:"DPT",diff:2,
q:"You want to make a long investigation's context usage predictable.",
ask:"What contributes most to unpredictability?",
a:["Unbounded tool output, since a single large result can consume a large share of the window without warning",
"The number of turns",
"The system prompt length",
"The model choice"],
c:0,
w:["Correct. An unbounded result &mdash; a huge file, a large query response &mdash; is the main source of sudden context consumption.",
"Turn count contributes gradually and predictably.",
"The system prompt is fixed overhead.",
"Model choice affects window size, not variance in consumption."],
e:"Unbounded tool output is what makes context consumption spiky rather than gradual. One <code>Read</code> of a very large file or one broad query can consume a large fraction of the window in a single step. Bound it: read targeted ranges, paginate large results, trim to needed fields, and delegate genuinely large sweeps to a subagent whose isolated window absorbs the bulk.",
k:"Unbounded tool output makes context spiky. Bound reads, paginate results, trim fields, delegate large sweeps."},

{i:1019,d:5,t:"5.5",s:"SDE",diff:2,
q:"You want confidence in an automation decision for a document type you see only occasionally.",
ask:"What does this require?",
a:["Deliberately over-sampling that type in the validation set, since its natural frequency gives too few examples for a reliable estimate",
"Waiting until enough examples accumulate naturally",
"Using the aggregate accuracy as a proxy",
"Automating it and monitoring"],
c:0,
w:["Correct. Over-sample rare segments so the estimate for each is reliable.",
"Waiting may take a very long time and blocks the decision.",
"The aggregate is dominated by common types and tells you nothing about this one.",
"Automating an unmeasured segment is the decision without the evidence."],
e:"Segment-level decisions need adequate samples per segment, which means the validation set's composition should be driven by the decisions you need to make rather than by natural frequency. Deliberately over-sample rare and difficult types, weight appropriately when reporting an aggregate, and until a segment is measured, route it to review rather than assuming its siblings' performance carries over.",
k:"Over-sample rare types in the validation set. Natural frequency gives too few examples for a segment-level decision."},

{i:1020,d:5,t:"5.6",s:"MAR",diff:2,
q:"A synthesis produces a claim that no single source supports, but which follows from combining two.",
ask:"How should it be attributed?",
a:["As an inference, citing both sources and making clear the claim is derived rather than directly stated by either",
"To whichever source is closer",
"To both, as though each stated it",
"Without attribution, since it is the synthesis agent's own conclusion"],
c:0,
w:["Correct. Derived claims should be marked as such, with the inputs cited.",
"Attributing a derived claim to one source misrepresents that source.",
"Neither source stated it, so citing them as though they did is inaccurate.",
"Unattributed claims are exactly the provenance loss to avoid."],
e:"Synthesis legitimately produces claims neither source made, and that is much of its value. The requirement is honesty about the epistemic status: mark it as an inference, cite the inputs it rests on, and state the reasoning briefly. That lets a reader evaluate the inference itself rather than mistaking it for a directly-sourced fact &mdash; which they would otherwise have no way to detect.",
k:"Mark derived claims as inferences, cite the inputs, and state the reasoning. Do not present them as sourced facts."},

{i:1021,d:1,t:"1.1",s:"AAT",diff:2,
q:"You want to bound how much a single agent conversation can cost.",
ask:"What combination gives you this?",
a:["An iteration cap, bounded tool output, and a token budget checked between turns &mdash; since cost accrues through turns, output volume and context growth together",
"An iteration cap alone",
"A <code>max_tokens</code> limit alone",
"A wall-clock timeout"],
c:0,
w:["Correct. Cost has three drivers and bounding one leaves the others unbounded.",
"An iteration cap bounds turns while each turn's context can still grow without limit.",
"<code>max_tokens</code> bounds output per turn, not the growing input.",
"Time bounds duration, which correlates loosely with cost."],
e:"Cost accrues along three axes. Turn count &mdash; bounded by the iteration cap. Output per turn &mdash; bounded by <code>max_tokens</code>. And context size, which grows as history and tool results accumulate and is billed in full on every request. That third one usually dominates in long conversations, which is why trimming tool output and prompt-caching the stable prefix matter as much as the caps.",
k:"Bound cost on three axes: turns, output per turn, and context growth. Context growth usually dominates."},

{i:1022,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator must choose between waiting for a slow subagent and proceeding without it.",
ask:"What should have been decided in advance?",
a:["Whether that subagent's output is on the critical path, and what the output looks like without it",
"Which model the subagent uses",
"How many retries it gets",
"Its position in the dispatch order"],
c:0,
w:["Correct. Criticality and degraded-mode output are the decisions that make this a policy rather than an improvisation.",
"Model choice does not determine whether you can proceed without it.",
"Retry policy is downstream of the criticality decision.",
"Dispatch order does not determine whether its output is required."],
e:"Deciding under time pressure produces inconsistent behaviour. Classify each delegation in advance as critical-path or optional enrichment, and define what the output looks like in degraded mode &mdash; omitted section, placeholder, or annotated partial coverage. Then a slow subagent triggers a defined policy rather than an improvised judgement, and the coverage annotation follows automatically.",
k:"Classify delegations as critical-path or optional in advance, and define the degraded-mode output."},

{i:1023,d:1,t:"1.3",s:"AAT",diff:2,
q:"A subagent's output contract specifies fields but not what to do when a field cannot be determined.",
ask:"What is the likely result?",
a:["The subagent fills the field with a plausible value rather than reporting that it could not determine it",
"The subagent omits the field",
"The subagent errors",
"The subagent asks for clarification"],
c:0,
w:["Correct. A required-looking field with no stated absence representation creates fabrication pressure.",
"Omission is possible but less likely than filling it.",
"Nothing raises an error.",
"It is more likely to produce a value than to ask."],
e:"This is the extraction-schema fabrication problem at the agent boundary. A contract that names fields without saying how absence is represented implies every field should be filled. State the representation &mdash; <code>null</code>, <code>\"undetermined\"</code> with a reason &mdash; and state in the quality criteria that using it is correct behaviour rather than failure. Both parts are needed.",
k:"Output contracts must state how absence is represented, and that using it is correct. Otherwise fields get fabricated."},

{i:1024,d:1,t:"1.4",s:"CSA",diff:2,
q:"An enforced rule blocks an action and the customer asks why.",
ask:"What determines whether the agent can answer well?",
a:["Whether the rule is stated in the prompt and the block message explains it &mdash; enforcement alone gives the agent nothing to say",
"Whether the agent has a larger context window",
"Whether the block was logged",
"Whether the agent can retry"],
c:0,
w:["Correct. The agent can only explain a rule it knows about, in terms the block message gave it.",
"Window size does not supply the reason.",
"Logging serves operators, not the customer conversation.",
"Retry is irrelevant to explaining a deterministic block."],
e:"This is why prompts and enforcement are complementary rather than alternatives. The hook guarantees the outcome; the prompt gives the agent the understanding to explain it in its own words; the block message supplies the specific reason and the alternative path. Missing either of the latter two produces a technically correct block wrapped in a confusing customer experience.",
k:"To explain a block, the agent needs the rule in the prompt and the reason in the block message. Enforcement alone is silent."},

{i:1025,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want to add a hook whose logic depends on which subagent is calling.",
ask:"What should you consider first?",
a:["Whether the rule should instead be expressed per-subagent through their tool lists and their own hooks, since caller-dependent logic in a shared hook is harder to reason about",
"Whether the hook can access the caller identity",
"Whether to duplicate the hook",
"Whether to make it asynchronous"],
c:0,
w:["Correct. Per-agent scoping usually expresses the intent more clearly than a shared hook branching on identity.",
"Access may be available, but the design question comes first.",
"Duplication creates drift.",
"Synchronicity is unrelated."],
e:"A shared hook branching on caller identity concentrates several agents' rules in one place, which makes each harder to see and the composition harder to reason about. Subagent definitions support their own tool lists and their own scoped hooks &mdash; expressing the rule where it applies keeps each agent's constraints visible in its own definition, which is also what makes a capability review tractable.",
k:"Prefer per-subagent scoping over a shared hook branching on caller identity. Keep each agent's constraints visible in its definition."},

{i:1026,d:1,t:"1.6",s:"CGC",diff:2,
q:"You are deciding whether a step belongs in the plan or is an implementation detail.",
ask:"What is the test?",
a:["Whether it involves a decision that could be made differently with different consequences &mdash; decisions belong in the plan, mechanics do not",
"Whether it takes more than an hour",
"Whether it touches more than one file",
"Whether it is difficult"],
c:0,
w:["Correct. Plans capture decisions and sequencing; mechanics belong in execution.",
"Duration is a weak proxy for whether a decision is involved.",
"File count does not indicate a decision point.",
"Difficulty and decision-content are different things."],
e:"A plan records what was decided, in what order, and why &mdash; so its granularity should follow the decisions. &ldquo;Migrate the payments module to the new interface&rdquo; is a phase; &ldquo;update the import statement&rdquo; is mechanics. Recording decisions with rationale is also what makes partial rollback possible, since you can see which phases rested on a decision that later proved wrong.",
k:"Plans capture decisions, sequencing and rationale. Mechanics belong in execution, not in the plan."},

{i:1027,d:1,t:"1.7",s:"AAT",diff:2,
q:"You want a workflow's state to be inspectable by an operator during a long run.",
ask:"What does this require?",
a:["State written to a durable, readable location as the run progresses, rather than held only in the agent's context",
"Verbose logging",
"A larger context window",
"A progress bar"],
c:0,
w:["Correct. Inspectable state must exist outside the agent's context in a form an operator can read.",
"Logs record events; they do not present current state.",
"Context is not operator-visible.",
"A progress indicator shows position without showing state."],
e:"The same durable state that enables crash recovery and handover also enables operational inspection. Write completed phases, their outputs and the current position to a location an operator can read, in a structured form &mdash; then &ldquo;what is it doing and how far has it got?&rdquo; is answerable without interrupting the run. That is a fourth payoff of externalisation, alongside context management, recovery and handover.",
k:"Durable structured state serves four purposes: context management, crash recovery, handover, and operational inspection."},

{i:1028,d:2,t:"2.2",s:"SDE",diff:2,
q:"You want a tool's errors to be usable for alerting as well as for agent recovery.",
ask:"What supports both?",
a:["A stable machine-readable category alongside the human-readable message, so alerting can aggregate by category while the agent reads the message",
"A longer message",
"A stack trace",
"A severity word in the message text"],
c:0,
w:["Correct. A stable categorical field is what alerting aggregates on reliably.",
"Message length does not support aggregation.",
"Stack traces are internal detail and vary too much to aggregate on.",
"Parsing severity out of message text is brittle."],
e:"The same structured error contract serves three consumers. The agent reads the category, the retryability flag and the message to choose a recovery. Your alerting aggregates on the stable category field and the tool name. An operator follows the correlation id into the logs for full internal detail. One contract, three consumers, no parsing of prose anywhere.",
k:"One error contract serves agent recovery, alerting aggregation and operator diagnosis. Never parse prose for any of them."},

{i:1029,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want to prevent a skill from being auto-invoked on files it should not touch.",
ask:"Which field constrains this structurally?",
a:["<code>paths</code>, which limits when the skill is auto-activated using the same glob format as path-specific rules",
"<code>allowed-tools</code>",
"<code>when_to_use</code>",
"<code>disable-model-invocation</code>"],
c:0,
w:["Correct. <code>paths</code> is the structural constraint on auto-activation.",
"That pre-approves tools and does not constrain activation.",
"That sharpens the semantic trigger without a structural constraint.",
"That disables automatic invocation entirely, which is broader than scoping it."],
e:"Two levers for over-triggering, and they combine. <code>when_to_use</code> sharpens the <em>semantic</em> trigger by adding trigger phrases and example requests to the description. <code>paths</code> adds a <em>structural</em> constraint so the skill is only considered when matching files are in play. <code>disable-model-invocation: true</code> is the blunt fallback when it should never auto-trigger at all.",
k:"<code>paths</code> constrains skill auto-activation structurally; <code>when_to_use</code> sharpens it semantically. Combine them."},

{i:1030,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to know whether your retry loop is worth its cost.",
ask:"What should you compare?",
a:["The proportion of retries that succeed against the cost of the retry versus the cost of routing straight to human review",
"The total number of retries",
"The average retry latency",
"The retry budget"],
c:0,
w:["Correct. A retry earns its cost when it is cheaper than the human review it avoids and succeeds often enough.",
"Volume measures activity rather than value.",
"Latency is one cost input among several.",
"The budget is a parameter, not a measure of value."],
e:"Retries compete economically with human review. If a retry costs a fraction of a reviewer's time and succeeds 60% of the time, it is clearly worth it &mdash; and worth extending the budget. If it succeeds 5% of the time, most retries are burning cost before routing to review anyway, and the budget should shrink. Measure the success rate per retry attempt, since it typically falls sharply after the first.",
k:"Retries compete with human review economically. Measure success rate per attempt against reviewer cost."}
);
