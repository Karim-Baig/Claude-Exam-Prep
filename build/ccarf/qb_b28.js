QB.push(
{i:1071,d:1,t:"1.1",s:"CSA",diff:2,
q:"Your agent's average conversation uses 6 tool-calling turns. After adding a new tool it uses 9, with no improvement in outcomes.",
ask:"What is the likely explanation?",
a:["The new tool is being selected in situations where an existing one already sufficed, adding a step without adding information",
"The model has become more thorough",
"The new tool is slower",
"The iteration cap was raised"],
c:0,
w:["Correct. More turns with unchanged outcomes points at redundant selection.",
"Thoroughness that does not improve outcomes is waste, not a benefit.",
"Tool latency does not change turn count.",
"A cap bounds turns; it does not cause more of them."],
e:"Turn count is a useful regression signal after any tool-surface change. Rising turns with unchanged outcomes usually means the new tool is being selected where an existing one already answered the question &mdash; a boundary problem. Sharpen both descriptions to state when to prefer each, and check whether the new tool is genuinely earning its place in the choice space.",
k:"Rising turn count with unchanged outcomes after adding a tool means redundant selection. Sharpen the boundary."},

{i:1072,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator dispatches to a specialist that returns a result outside its stated specialisation.",
ask:"What should you check?",
a:["Whether the specialist's system prompt actually constrains its scope, since a specialisation stated only in the description governs selection rather than behaviour",
"Whether the coordinator misrouted",
"Whether the specialist needs more tools",
"Whether the result is wrong"],
c:0,
w:["Correct. The description drives selection; the system prompt drives what the agent actually does.",
"Possible, but the described symptom is about behaviour after correct selection.",
"More tools would widen rather than constrain its scope.",
"The result may be fine and still out of scope, which is the issue."],
e:"Keep the fields' jobs distinct. The <code>description</code> tells the coordinator when to select this agent. The system prompt tells the agent how to work, what standards apply and what is out of scope. An agent whose specialisation exists only in its description will be selected correctly and then do whatever seems useful &mdash; including work that belongs to a sibling.",
k:"Description governs selection; system prompt governs behaviour. State scope and exclusions in the system prompt."},

{i:1073,d:1,t:"1.3",s:"AAT",diff:2,
q:"You want a subagent's output to be safe to insert directly into a customer-facing document.",
ask:"What should the contract require?",
a:["That output contains no internal identifiers, system detail or speculation &mdash; stated as an explicit constraint, since the subagent cannot infer the eventual audience",
"That it be shorter",
"That it be well-formatted",
"That it be reviewed afterwards"],
c:0,
w:["Correct. Audience constraints must be stated because the subagent has no visibility of downstream use.",
"Length does not make content safe to publish.",
"Formatting is presentation, not safety.",
"Review is a useful backstop but not a substitute for the constraint."],
e:"A subagent sees its task, not the pipeline. If its output will reach a customer, say so and state the constraints &mdash; no internal identifiers, no system detail, no speculation presented as fact. Otherwise it will produce output appropriate for an internal consumer, which is a reasonable default given what it knows. This is the same class of omission as failing to state the output contract.",
k:"State audience constraints explicitly. A subagent cannot infer that its output will be customer-facing."},

{i:1074,d:1,t:"1.4",s:"CSA",diff:2,
q:"You want to test that an enforcement control cannot be circumvented.",
ask:"What should the test attempt?",
a:["The alternative routes to the same outcome &mdash; other tools, split operations, different parameter framings &mdash; not just the direct blocked call",
"The blocked call repeatedly",
"The blocked call with a different model",
"The blocked call from a different session"],
c:0,
w:["Correct. Controls fail at the routes you did not enumerate, not at the one you blocked.",
"Repeating the direct call tests the same path.",
"Model choice does not open a new route.",
"Session identity does not change the available paths."],
e:"Testing enforcement means testing the outcome, not the path. Enumerate every tool that could produce the restricted result, try splitting the operation to stay under a per-call threshold, and try framing the arguments differently. The two documented failure modes are exactly these: a control placed on one path while another exists, and a per-call threshold circumvented by splitting.",
k:"Test controls against every route to the outcome: alternative tools, split operations, reframed arguments."},

{i:1075,d:1,t:"1.5",s:"AAT",diff:2,
q:"A hook's evaluation depends on data that is itself produced by a tool the agent calls.",
ask:"What ordering hazard exists?",
a:["The hook may run before that data exists, so it must handle the not-yet-established case explicitly rather than assuming presence",
"Hooks always run after all tools",
"Hooks cannot read tool output",
"There is no hazard"],
c:0,
w:["Correct. Stateful enforcement must handle the state not yet being established.",
"Hooks run per tool call, in whatever order the agent calls them.",
"Hooks can read session state that tools wrote.",
"The ordering hazard is real and is how precondition checks fail open."],
e:"This is precisely the identity-verification precondition. The check on <code>process_refund</code> must handle the case where <code>get_customer</code> has not run &mdash; and it must handle it by <em>denying</em>, since an unestablished precondition is not a satisfied one. Failing open on absent state is the subtle bug that makes a precondition look present while permitting exactly the case it was written to prevent.",
k:"Stateful enforcement must deny when the precondition state is absent. Failing open on missing state defeats the check."},

{i:1076,d:1,t:"1.6",s:"CGC",diff:2,
q:"A plan's phases are each verifiable, but verifying phase four requires work from phase six.",
ask:"What does this indicate?",
a:["The phase ordering does not match the dependency structure &mdash; reorder so each phase is verifiable when it completes",
"Phase four should skip verification",
"The phases should be merged",
"Verification should happen at the end"],
c:0,
w:["Correct. If verification needs later work, the ordering is wrong.",
"Skipping verification removes the checkpoint's value.",
"Merging may be right if they are genuinely one unit, but reordering is the direct fix.",
"End-only verification abandons phased checkpoints entirely."],
e:"A phase boundary that cannot be verified when it completes is not a checkpoint. Either the ordering is wrong &mdash; move the phase that supplies the verification earlier &mdash; or the two phases are really one unit and should be merged. Both restore the property that makes phasing valuable: a mistake surfaces at the boundary rather than several phases later.",
k:"Each phase must be verifiable on completion. If verification needs later work, reorder or merge."},

{i:1077,d:1,t:"1.7",s:"AAT",diff:2,
q:"You want a workflow to survive being moved to a different machine mid-run.",
ask:"What does this require?",
a:["State and artefacts in shared durable storage rather than local files, since machine-local state does not move with the workflow",
"A longer timeout",
"Idempotent steps only",
"A larger context window"],
c:0,
w:["Correct. Portability requires the state to live somewhere both machines can reach.",
"Timeouts do not make state portable.",
"Idempotency makes re-execution safe but does not move the state.",
"Context does not survive a machine change."],
e:"Recovery requirements determine where state lives. Surviving a process crash needs durable local state. Surviving a machine change needs shared storage both machines can reach. Idempotent steps are required in both cases, so an interrupted step can safely re-run &mdash; but idempotency alone does not tell the new machine where to resume.",
k:"Crash recovery needs durable state; machine portability needs shared storage. Idempotency is required for both."},

{i:1078,d:2,t:"2.1",s:"SDE",diff:2,
q:"A tool's description is precise but written in terms of the internal system's vocabulary rather than the domain's.",
ask:"What is the risk?",
a:["The model may not connect a user request phrased in domain terms to a tool described in internal terms, so it goes unselected",
"The description is too long",
"The API will reject unfamiliar vocabulary",
"The tool will be selected too often"],
c:0,
w:["Correct. Selection depends on connecting the request to the description, which internal jargon obstructs.",
"Length is not the issue.",
"No vocabulary validation occurs.",
"The failure is under-selection, not over-selection."],
e:"Descriptions must bridge from how requests are phrased to what the tool does. A tool described as querying the <code>CUSTMSTR</code> table will not obviously answer &ldquo;what plan is this customer on?&rdquo; Use domain vocabulary, and include example queries that show the connection &mdash; example triggers do more work here than any amount of precise internal terminology.",
k:"Describe tools in domain vocabulary with example triggers. Internal jargon breaks the link from request to tool."},

{i:1079,d:2,t:"2.2",s:"AAT",diff:2,
q:"You want an error to help an agent decide between retrying and escalating.",
ask:"What single field does most of that work?",
a:["<code>isRetryable</code>, since it directly answers the immediate question the agent faces",
"The message",
"The category",
"The timestamp"],
c:0,
w:["Correct. It answers the branch the agent must take, without requiring inference.",
"The message informs the explanation and the corrected retry, but the branch is a separate signal.",
"The category is valuable and still requires the agent to infer retryability from it.",
"Timestamps are operational metadata."],
e:"The three fields do complementary work. <code>isRetryable</code> answers the immediate branch explicitly rather than leaving it to be inferred. <code>errorCategory</code> tells the agent what kind of problem this is, which shapes the alternative when retrying is ruled out. The message enables an explanation or a corrected retry. Include all three &mdash; but if the agent must branch, the explicit flag is what removes the guesswork.",
k:"<code>isRetryable</code> answers the branch explicitly; category shapes the alternative; the message enables explanation."},

{i:1080,d:2,t:"2.3",s:"AAT",diff:2,
q:"An agent needs a capability only when handling a particular request type.",
ask:"What is the cleanest arrangement?",
a:["Route those requests to a specialist that holds the capability, keeping it out of the general agent's toolset",
"Give the general agent the capability",
"Give it conditionally at runtime",
"Give it and block it with a hook otherwise"],
c:0,
w:["Correct. Routing keeps each toolset small and role-coherent.",
"Granting a request-type-specific capability to a general agent enlarges its choice space permanently.",
"Runtime conditional grants add complexity for something routing handles cleanly.",
"Granting and blocking enlarges the toolset while adding a control to maintain."],
e:"Request-type-specific capabilities are what specialists are for. Routing keeps the general agent's toolset small &mdash; which is what preserves its selection reliability &mdash; and puts the capability with a focused system prompt that knows how to use it well. Compare this with the narrow read-only cross-role utility, which is appropriate when the need is frequent and the capability trivial.",
k:"Route request-type-specific capabilities to specialists. Reserve cross-role utilities for frequent, trivial, read-only needs."},

{i:1081,d:2,t:"2.4",s:"DPT",diff:2,
q:"You want to reduce the risk of a project MCP server change breaking everyone at once.",
ask:"What practice helps?",
a:["Pinning versions and promoting changes deliberately, with the change reviewed as a capability grant rather than treated as configuration",
"Letting each developer configure their own",
"Avoiding project scope entirely",
"Updating on a fixed schedule"],
c:0,
w:["Correct. Pinning plus deliberate promotion plus review is the standard risk management.",
"Per-developer configuration loses the shared definition that makes the team consistent.",
"Avoiding project scope loses zero-setup sharing.",
"A schedule does not itself reduce risk."],
e:"Project scope's strength &mdash; everyone gets it on clone &mdash; is also its risk, since a change reaches everyone at once. Pin versions, promote deliberately after testing, and review changes as capability grants: what does this server expose, what is the blast radius of a misrouted call, does it need a narrower wrapper. Because <code>.mcp.json</code> is version-controlled, that review happens naturally in a pull request.",
k:"Pin MCP versions, promote deliberately, and review changes as capability grants. Project scope reaches everyone at once."},

{i:1082,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to find all places a configuration key is read, and the key name is a common English word.",
ask:"What complicates the search?",
a:["The name will match prose, comments and unrelated identifiers, so the results need narrowing by context or file type rather than accepting the raw hit list",
"The search will be slow",
"Common words cannot be searched",
"The results will be truncated"],
c:0,
w:["Correct. Over-reporting from a common term needs narrowing by context, not filtering by reading.",
"Speed is not the issue.",
"They can be searched; the results are just noisy.",
"Truncation is not the primary problem."],
e:"Over-reporting is the mirror of the under-reporting problem with wrappers. Narrow at the search boundary rather than by reading: include surrounding syntax in the pattern (quotes, an access operator), restrict to relevant file types, and exclude vendored and generated directories. Refining the search costs nothing; filtering by reading costs the tokens of every irrelevant hit.",
k:"Narrow noisy searches at the search boundary with context and file-type filters &mdash; not by reading the hits."},

{i:1083,d:3,t:"3.1",s:"CGC",diff:2,
q:"You want new developers to understand the project quickly and Claude to follow its conventions.",
ask:"How should the content be organised?",
a:["A README for human onboarding and CLAUDE.md for the conventions that change Claude's decisions, with each linking to the other",
"One file serving both",
"CLAUDE.md only",
"README only"],
c:0,
w:["Correct. Different audiences want different content, and CLAUDE.md is a per-request context budget.",
"A combined file makes every human-oriented paragraph a recurring token cost.",
"CLAUDE.md alone omits the history, rationale and setup humans need.",
"A README alone is not in Claude's context."],
e:"The audiences overlap but their needs do not. Humans want history, rationale, org context and setup instructions. Claude needs the conventions that would otherwise be got wrong. Keep them separate, link between them, and where a document genuinely serves both, use <code>@path</code> to import only the decision-relevant section &mdash; remembering that imports load at launch and so still cost context.",
k:"README for humans, CLAUDE.md for decision-changing conventions. Import only the relevant section if they overlap."},

{i:1084,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill produces a side effect and you want it never to run without the developer intending it.",
ask:"Which field expresses this?",
a:["<code>disable-model-invocation: true</code>, so only manual invocation is possible",
"<code>user-invocable: false</code>",
"<code>context: fork</code>",
"<code>allowed-tools</code>"],
c:0,
w:["Correct. It prevents Claude loading the skill automatically, leaving deliberate invocation only.",
"That is the mirror &mdash; it hides the skill from you and leaves automatic invocation.",
"Forking isolates context without affecting who can invoke.",
"That pre-approves tools rather than controlling invocation."],
e:"Use <code>disable-model-invocation: true</code> for anything with side effects that should be triggered deliberately &mdash; a deploy, a commit, a destructive cleanup. Note that it controls <em>invocation</em>, not capability: if the skill's actions must also be constrained, that is <code>disallowed-tools</code>, and if they must be gated on approval, that is a <code>PreToolUse</code> hook returning <code>ask</code>.",
k:"<code>disable-model-invocation: true</code> = deliberate invocation only. Capability constraints are a separate concern."},

{i:1085,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want a rule to load for files in a directory but not for its subdirectories.",
ask:"Which glob shape does this?",
a:["<code>services/payments/*</code>, since a single <code>*</code> does not cross directory boundaries",
"<code>services/payments/**/*</code>",
"<code>services/payments/</code>",
"<code>**/payments/*</code>"],
c:0,
w:["Correct. A single <code>*</code> matches within one level; <code>**</code> is what crosses levels.",
"<code>**/</code> matches at any depth, which is the opposite of the requirement.",
"A bare directory path is not a file-matching pattern.",
"That matches payments directories anywhere, at one level each."],
e:"The depth distinction is the most consequential detail in glob patterns. A single <code>*</code> matches within one directory level; <code>**</code> crosses levels. Missing <code>**/</code> is the common reason a rule does not fire for nested files; including it when you wanted one level is the reason a rule fires more broadly than intended. Match the pattern to the convention's exact scope.",
k:"A single <code>*</code> stays within one level; <code>**</code> crosses levels. This is the most common glob mistake."},

{i:1086,d:3,t:"3.4",s:"CGC",diff:2,
q:"A task is well understood but the team disagrees about the approach.",
ask:"What does planning mode provide here?",
a:["An explicit written approach to disagree with concretely, moving the discussion from abstract preferences to a specific proposal",
"A resolution of the disagreement",
"Faster implementation",
"An objective answer"],
c:0,
w:["Correct. A written plan makes the disagreement concrete and reviewable.",
"Planning does not resolve human disagreement; it makes it specific.",
"Speed is not what planning provides.",
"There may be no objective answer between defensible approaches."],
e:"A written plan is a discussion artefact as much as an execution one. Abstract disagreement about approach is hard to resolve; a specific proposal with its phases, decisions and rationale is something people can review, amend and agree on. And recording the decision and its rationale in the plan file means the settled question is not re-litigated later, by a person or a resumed session.",
k:"Written plans make abstract disagreement concrete and reviewable, and record the settled decision so it stays settled."},

{i:1087,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to know whether your feedback is landing before investing another round.",
ask:"What is the cheap check?",
a:["Ask for a restatement of what will change before it changes anything, which surfaces a misunderstanding at almost no cost",
"Wait for the next iteration and inspect it",
"Give more detailed feedback",
"Give the feedback twice"],
c:0,
w:["Correct. A restatement is far cheaper than an iteration built on a misunderstanding.",
"Inspecting the result means the misunderstanding was already implemented.",
"More detail on a misunderstood point does not confirm understanding.",
"Repetition does not confirm comprehension."],
e:"This is the lightweight approach-check applied to feedback. Asking what will change before it changes surfaces a misreading in one cheap exchange, rather than after a full iteration that then has to be unwound. It is the same principle as catching a wrong approach at plan time rather than at file thirty &mdash; move the error detection as early and as cheaply as possible.",
k:"Ask for a restatement of intended changes before an iteration. Cheaper than discovering the misunderstanding afterwards."},

{i:1088,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review's findings are accurate but arrive after the developer has moved on.",
ask:"What matters here?",
a:["Latency to feedback, since a finding that arrives after context is lost costs far more to act on &mdash; parallelise passes and scope the input to reduce it",
"The number of findings",
"The severity distribution",
"The output format"],
c:0,
w:["Correct. Feedback value decays sharply with time since the developer held the context.",
"Volume is unrelated to timeliness.",
"Severity does not address arrival time.",
"Format matters for actionability but not for latency."],
e:"Feedback that arrives after the developer has switched context is far more expensive to act on, because they must reload everything they knew. The levers that reduce review latency without reducing coverage are parallelising the independent per-file passes, and scoping the input precisely &mdash; the latter improving cost, attention and latency simultaneously.",
k:"Review feedback decays in value with latency. Parallelise independent passes and scope the input tightly."},

{i:1089,d:4,t:"4.1",s:"SDE",diff:2,
q:"You want criteria that distinguish two categories that share most of their characteristics.",
ask:"What should the criteria emphasise?",
a:["The specific features that differ, since shared characteristics do no discriminating work",
"Everything about both categories",
"The more common category only",
"The consequences of each classification"],
c:0,
w:["Correct. Only the differences discriminate; shared features add length without helping.",
"Describing shared characteristics at length buries the distinguishing features.",
"Omitting one category leaves it undefined.",
"Consequences matter for prioritisation, not for classification."],
e:"When categories overlap heavily, the criteria should concentrate on the difference. Describing everything both share consumes attention and buries the one or two features that actually decide the classification. Pair the stated difference with contrastive near-miss examples &mdash; two cases identical except in the discriminating feature &mdash; which is where the classification errors concentrate.",
k:"For overlapping categories, state only the discriminating features and show contrastive near-miss pairs."},

{i:1090,d:4,t:"4.2",s:"SDE",diff:2,
q:"An example demonstrates the right output but takes a reasoning path you do not want repeated.",
ask:"What should you consider?",
a:["Whether to include the reasoning at all, since demonstrated reasoning is learned alongside the output",
"Nothing; only the output matters",
"Making the reasoning longer",
"Adding a note that the reasoning is illustrative"],
c:0,
w:["Correct. Reasoning shown in an example teaches the approach as well as the result.",
"Examples teach everything they demonstrate, including method.",
"Length compounds the problem.",
"A caveat is unlikely to prevent the demonstrated approach being learned."],
e:"Rationale in examples is valuable precisely because it generalises &mdash; which cuts both ways. If the reasoning shown is a route you do not want repeated, either show the reasoning you do want, or show only the input and output. The choice depends on whether you are teaching a classification principle, where rationale helps, or a format, where it may not be needed at all.",
k:"Examples teach the demonstrated reasoning as well as the output. Show the reasoning you want, or none."},

{i:1091,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want extracted output to be verifiable against the source without re-reading the whole document.",
ask:"What should the schema carry?",
a:["A source excerpt or location per extracted value, so a reviewer can check it locally",
"The document identifier only",
"A confidence score",
"The extraction timestamp"],
c:0,
w:["Correct. Local verification requires the supporting excerpt or location alongside the value.",
"An identifier locates the document, not the value within it.",
"Confidence is an assertion rather than evidence.",
"Timestamps are operational metadata."],
e:"This is the claim-to-source-to-quote pattern applied to extraction. A value with its supporting excerpt or page-and-line reference can be checked in seconds; a value alone requires re-reading the document. It speeds human review directly, and it makes systematic errors diagnosable &mdash; a field consistently read from the wrong region of the page is immediately visible once the excerpts are there.",
k:"Carry a source excerpt or location per extracted value. It makes review fast and systematic errors visible."},

{i:1092,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to prevent a retry from being triggered by a validation rule that is itself wrong.",
ask:"What practice helps?",
a:["Measuring each rule's precision against ground truth, so an over-strict rule is identified rather than silently generating retries",
"Reducing the number of rules",
"Increasing the retry budget",
"Loosening all rules"],
c:0,
w:["Correct. Per-rule precision measurement identifies rules that reject correct data.",
"Fewer rules reduces detection along with false rejections.",
"A larger budget spends more on retries a bad rule caused.",
"Blanket loosening loses genuine detection."],
e:"Validators need precision measurement just as review criteria do. A rule with poor precision rejects correct extractions, which wastes retries, floods human review with correct items, and looks like an extraction quality problem. Measuring per rule against ground truth &mdash; which requires recording which rule fired on each rejection &mdash; is what separates a bad rule from a genuine quality issue.",
k:"Measure per-rule precision against ground truth. Over-strict rules masquerade as extraction quality problems."},

{i:1093,d:4,t:"4.5",s:"SDE",diff:2,
q:"You want to run an expensive analysis on every document but only a subset needs it.",
ask:"What is the efficient structure?",
a:["A cheap classification pass to identify the subset, then the expensive analysis only on those &mdash; both batchable as separate stages",
"Run the expensive analysis on everything",
"Sample randomly and analyse the sample",
"Run the expensive analysis on the first N documents"],
c:0,
w:["Correct. Classify cheaply, then apply expensive work selectively &mdash; and both stages batch independently.",
"Running expensive analysis where it is not needed is the waste being avoided.",
"Random sampling misses documents that genuinely need the analysis.",
"Position is uncorrelated with need."],
e:"Two-stage filtering is a standard batch structure, and it fits Batch's constraints exactly: each stage is single-shot, so both can be batched, with the second stage's input built from the first stage's results. Validate the classifier's recall on a labelled set before relying on it &mdash; a document wrongly filtered out never receives the analysis it needed, and nothing downstream will notice.",
k:"Classify cheaply, then analyse selectively &mdash; two batchable single-shot stages. Validate the classifier's recall first."},

{i:1094,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want review findings to be comparable across runs so trends are visible.",
ask:"What does this require?",
a:["Stable finding categories and identifiers, so the same issue is recognisably the same across runs",
"A consistent number of findings per run",
"The same reviewer model each time",
"Findings sorted consistently"],
c:0,
w:["Correct. Stable identity is what makes trend analysis possible.",
"Count consistency is neither achievable nor meaningful.",
"Model consistency helps reproducibility but does not give findings stable identity.",
"Ordering is presentation."],
e:"Trend analysis needs findings to be identifiable across runs. Stable categories let you track precision per category over time; a stable finding identity &mdash; category plus location plus the <code>detected_pattern</code> that triggered it &mdash; lets you tell a persisting issue from a new one. That is also what makes re-runs able to report only new or still-unfixed issues rather than repeating everything.",
k:"Stable categories and finding identity enable trend analysis and let re-runs report only new or unfixed issues."},

{i:1095,d:5,t:"5.1",s:"CAP",diff:2,
q:"A conversation's facts block is correct but the agent still asks the customer to confirm a fact it holds.",
ask:"What might be happening?",
a:["The block may not be positioned where it is salient, or the agent may not have been told the block is authoritative and need not be re-confirmed",
"The block is too small",
"The customer's earlier statement was unclear",
"The temperature is too high"],
c:0,
w:["Correct. Presence is not enough &mdash; position and stated authority both matter.",
"Small is the intended property.",
"The premise is that the block holds the fact correctly.",
"Temperature does not cause redundant confirmation."],
e:"Two things beyond presence. Position: the block must sit where attention is strong, which for a re-injected block means a stable privileged position rather than buried mid-history. And framing: telling the agent that this block is verified case state, not customer claims to be confirmed, prevents defensive re-confirmation that wastes turns and irritates the customer.",
k:"A facts block needs presence, salient position, and stated authority &mdash; or the agent re-confirms what it already holds."},

{i:1096,d:5,t:"5.2",s:"CSA",diff:2,
q:"You want to reduce the time a customer waits after an escalation.",
ask:"What does the agent control?",
a:["The quality of the handoff, since a complete structured payload lets the human act immediately rather than re-establishing context",
"The human queue length",
"The human's working speed",
"The customer's patience"],
c:0,
w:["Correct. Handoff quality directly reduces the human's time-to-action.",
"Queue depth is an operational capacity matter.",
"Working speed is not something the handoff changes, but preparation is.",
"Patience is not a lever."],
e:"The agent cannot shorten the queue, but it can shorten the work once the human starts. A complete handoff &mdash; identifier, issue, verified facts, what was tried and the outcome, reason for escalation, recommended action &mdash; means the human acts rather than re-establishing context or re-asking the customer. That is the difference between a five-minute resolution and a twenty-minute one.",
k:"The agent controls handoff quality, which is the human's time-to-action. Queue depth is operational."},

{i:1097,d:5,t:"5.3",s:"MAR",diff:2,
q:"You want to know whether a subagent's failures are its own or its dependencies'.",
ask:"What should the error carry?",
a:["Which component actually failed, so failures can be attributed to the subagent's logic or to an upstream service",
"The subagent's identity only",
"The elapsed time",
"The retry count"],
c:0,
w:["Correct. Attribution to the failing component is what separates a subagent defect from a dependency outage.",
"Identity says which agent reported it, not what failed.",
"Elapsed time is operational.",
"Retry count describes handling, not cause."],
e:"Errors accrete context as they rise, and the failing component is part of that context. Without it, a subagent that reports failures frequently looks unreliable when it may simply depend on a flaky service. With it, you can aggregate by failing component, spot the shared dependency behind correlated failures, and direct the fix at the right place.",
k:"Errors should name the failing component, not just the reporting agent. It separates agent defects from dependency outages."},

{i:1098,d:5,t:"5.4",s:"DPT",diff:2,
q:"You want to prevent a long session from reaching context exhaustion unexpectedly.",
ask:"What practice gives you warning?",
a:["Monitoring context usage as the session progresses, so you act before degradation rather than after noticing vague answers",
"Setting a turn limit",
"Using a larger window",
"Compacting on a schedule"],
c:0,
w:["Correct. Visibility lets you externalise and reset before quality degrades.",
"Turn count is a weak proxy, since a single large read can consume more than twenty small turns.",
"A larger window delays the point without giving warning.",
"Scheduled compaction is lossy and may fire when unnecessary."],
e:"The degradation symptom &mdash; vague, pattern-level answers &mdash; appears after quality has already suffered, which makes it a poor trigger. Watching context consumption gives you warning, and <code>/context</code> shows what is loaded. Act on the warning by externalising findings and resetting, rather than compacting reactively once the specifics have already been summarised away.",
k:"Monitor context usage for early warning. The degradation symptom appears after quality has already suffered."},

{i:1099,d:5,t:"5.5",s:"SDE",diff:2,
q:"You want to justify the cost of maintaining a labelled validation set.",
ask:"What does it enable that nothing else does?",
a:["Controlled measurement of quality changes and segment-level accuracy, which every automation and threshold decision depends on",
"Faster processing",
"Lower token costs",
"Fewer retries"],
c:0,
w:["Correct. Every automation and calibration decision rests on measured accuracy against ground truth.",
"It does not affect processing speed.",
"It does not reduce token cost directly.",
"It informs improvements that reduce retries, but that is indirect."],
e:"A labelled set is the foundation for the decisions that matter: which segments can be automated, where thresholds should sit, whether a prompt or schema change improved or regressed quality, and whether calibration has drifted. Without it, those are guesses. Note that human review corrections are a low-cost source of high-quality labels generated as a by-product of work you already do.",
k:"A labelled set underpins every automation, threshold and change decision. Review corrections are a free source of labels."},

{i:1100,d:5,t:"5.6",s:"MAR",diff:2,
q:"You want a reader to be able to tell how recent a report's evidence is.",
ask:"What is required?",
a:["Dates on the sources, surfaced in the report rather than held only in the pipeline",
"A report generation timestamp",
"A note that the report is current",
"The number of sources"],
c:0,
w:["Correct. Source dates convey evidence currency; nothing else does.",
"A generation date says when the report was written, not how old its evidence is.",
"An assertion of currency is not evidence of it.",
"Count says nothing about recency."],
e:"A report written today can rest entirely on five-year-old sources, and a generation timestamp actively misleads about that. Source dates must reach the reader, which means they survive collection, aggregation and rendering &mdash; the same chain that carries claim-to-source attribution. Where only old sources exist, saying so is itself a useful finding about the evidence base.",
k:"Source dates must reach the reader. A generation timestamp misleads about evidence currency."},

{i:1101,d:1,t:"1.1",s:"AAT",diff:2,
q:"You are told an agent loop should always run to <code>end_turn</code>.",
ask:"What is the accurate position?",
a:["<code>end_turn</code> is the natural termination, but a loop also stops on a circuit breaker, an unrecoverable error, or an external cancellation &mdash; each with a distinct outcome",
"Yes, <code>end_turn</code> is the only valid termination",
"No, loops should terminate on a turn limit",
"No, loops should terminate on cost"],
c:0,
w:["Correct. Several termination paths exist and each should produce a distinct, reported outcome.",
"Circuit breakers and unrecoverable errors terminate legitimately.",
"A turn limit is a safety net rather than the intended termination.",
"Cost limits are a legitimate breaker, not the primary termination."],
e:"Enumerate the termination paths and give each a distinct outcome. <code>end_turn</code> is completion. A circuit breaker firing is a runaway that should be reported as an incident, not as success. An unrecoverable error terminates with the error propagated. An external cancellation stops cleanly with state checkpointed. The failure mode to avoid is treating any of the latter three as completion.",
k:"Enumerate termination paths: <code>end_turn</code>, breaker, unrecoverable error, cancellation. Never report the last three as success."},

{i:1102,d:1,t:"1.3",s:"MAR",diff:2,
q:"A coordinator passes a subagent everything it might conceivably need.",
ask:"What is the cost?",
a:["Tokens and attention dilution in the subagent's context, which can make it perform worse than a focused delegation would",
"Nothing; more context is always better",
"Slower spawning",
"Reduced parallelism"],
c:0,
w:["Correct. Over-scoped delegations cost tokens and dilute attention on the actual task.",
"Irrelevant context dilutes attention as it does anywhere else.",
"Spawn time is not the meaningful cost.",
"Parallelism is unaffected."],
e:"Match delegation scope to task scope in both directions. Under-scoping starves the task; over-scoping costs tokens and dilutes attention across material the subagent must filter out. The same lost-in-the-middle effect applies inside a subagent's context as anywhere else &mdash; a focused prompt with exactly the needed inputs outperforms a comprehensive one.",
k:"Over-scoped delegations dilute attention as much as under-scoped ones starve the task. Match scope to the task."},

{i:1103,d:2,t:"2.2",s:"AAT",diff:2,
q:"You want error handling to work the same whether a tool is called by an agent or by a scheduled job.",
ask:"What supports this?",
a:["One error contract with machine-readable fields, since both consumers branch on the same structured signals",
"Two contracts, one per caller",
"Prose messages tailored to each caller",
"Different error codes per caller"],
c:0,
w:["Correct. A single structured contract serves both, with the message serving explanation where needed.",
"Two contracts drift and double the surface.",
"Prose is brittle for a programmatic consumer.",
"Per-caller codes fragment what should be one taxonomy."],
e:"One contract, several consumers. The scheduled job branches on <code>errorCategory</code> and <code>isRetryable</code>; the agent branches on the same fields and additionally reads the message for explanation and corrected retries; your alerting aggregates on the category; an operator follows the correlation id into the logs. Divergent contracts per caller guarantee they drift apart.",
k:"One structured error contract serves agents, jobs, alerting and operators. Divergent per-caller contracts drift."},

{i:1104,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a skill to run against a different model from the session's.",
ask:"Which field, and what is the scope of the change?",
a:["<code>model</code>, which overrides the session model for that turn only &mdash; and with <code>context: fork</code>, sets the subagent's model instead",
"<code>effort</code>, permanently",
"<code>agent</code>, for the session",
"There is no such field"],
c:0,
w:["Correct, including the interaction with <code>context: fork</code>.",
"<code>effort</code> sets the reasoning level, and it is also turn-scoped rather than permanent.",
"<code>agent</code> selects the subagent type when forking, not the model.",
"The field exists."],
e:"Skills can override session settings for their own turn: <code>model</code> selects a different model or <code>inherit</code>, and <code>effort</code> selects a reasoning level from <code>low</code> to <code>max</code>. Both revert afterwards. The interaction worth knowing is that with <code>context: fork</code>, <code>model</code> applies to the subagent rather than the session &mdash; useful for running an expensive analysis on a stronger model in isolation.",
k:"Skill <code>model</code> and <code>effort</code> override the session for that turn. With <code>context: fork</code>, <code>model</code> applies to the subagent."},

{i:1105,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want a schema that will accommodate a new field next quarter without breaking current consumers.",
ask:"What supports this?",
a:["Adding the new field as optional when it arrives, since consumers written against the current schema ignore fields they do not expect",
"Reserving placeholder fields now",
"Versioning the schema from the start",
"Making all fields optional"],
c:0,
w:["Correct. Additive optional fields are backward-compatible for consumers that ignore unknown fields.",
"Placeholder fields cost tokens now and constrain the eventual design.",
"Versioning is worth doing but additive optionality is what avoids breaking consumers.",
"Making everything optional removes guarantees consumers rely on and invites omission."],
e:"Additive change is the compatible kind: a new optional field does not break a consumer that ignores unknown fields. What breaks consumers is removing a field, renaming one, changing a type, or making an optional field required. Version the schema so changes are traceable, and reserve the breaking kinds for a deliberate version bump with consumers migrated.",
k:"Additive optional fields are backward-compatible. Removals, renames, type changes and new requirements are breaking."},

{i:1106,d:5,t:"5.2",s:"CSA",diff:2,
q:"You want to know whether your escalation criteria are calibrated.",
ask:"What would you measure?",
a:["Errors in both directions &mdash; cases escalated that a human resolved routinely, and cases handled autonomously that should have escalated",
"The escalation rate alone",
"Customer satisfaction on escalated cases",
"The time to escalate"],
c:0,
w:["Correct. Calibration is a two-sided property and both error types must be measured.",
"A rate without a correctness judgement says nothing about calibration.",
"Satisfaction is confounded and does not identify miscalibration direction.",
"Speed is not calibration."],
e:"Calibration means the boundary is in the right place, which requires measuring both error types. Over-escalation shows up as cases a human resolved by applying documented policy the agent had. Under-escalation shows up as policy exceptions decided autonomously, or as repeat contacts after a bad autonomous outcome. Errors in <em>both</em> directions is the signature of an undefined boundary rather than a mis-set threshold.",
k:"Measure escalation errors in both directions. Errors both ways mean an undefined boundary, not a mis-set threshold."},

{i:1107,d:2,t:"2.4",s:"DPT",diff:2,
q:"You want to give an agent access to an internal system that has no MCP server.",
ask:"What are the options, in order of preference?",
a:["Check for a community server; otherwise build a narrow custom one exposing only the operations needed, with policy enforced at that boundary",
"Grant Bash access and let it use the CLI",
"Wait until someone builds a server",
"Expose the system's full REST API as tools"],
c:0,
w:["Correct. Community first, then a deliberately narrow custom server with policy at the boundary.",
"Bash grants arbitrary execution to solve an integration problem.",
"Waiting blocks the work.",
"Mirroring a full API produces dozens of tools and degrades selection for every agent."],
e:"The preference order is community server, then a narrow custom one. For a proprietary internal system there is usually no community option, which makes this one of the legitimate reasons to build custom &mdash; and the design principle is a deliberately narrower surface than the underlying API, exposing only the operations you need, with policy enforced at that boundary.",
k:"Community server first; otherwise a narrow custom one exposing only what you need, with policy at the boundary."},

{i:1108,d:3,t:"3.6",s:"CCI",diff:2,
q:"A CI review needs credentials to reach an internal service.",
ask:"What is the appropriate arrangement?",
a:["A dedicated service credential supplied from the CI secret store, scoped to only what the review needs",
"A team member's personal token",
"A shared token committed to the repository",
"A token in the CI job's script"],
c:0,
w:["Correct. Service credentials are attributable, scoped and independently revocable.",
"A personal token attributes CI activity to an individual and breaks when they leave.",
"Committing a credential is a security incident.",
"An inline token in a script is committed and therefore exposed."],
e:"Project <code>.mcp.json</code> with <code>${VAR}</code> substitution means the same configuration serves developers and CI &mdash; each resolving the variable from their own environment. In CI that should be a dedicated service credential from the secret store, scoped to the minimum the review needs, so access is attributable, independently revocable, and unaffected by staff changes.",
k:"CI uses a dedicated scoped service credential from the secret store, resolved through <code>${VAR}</code> substitution."},

{i:1109,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want to know whether independent review is worth its extra cost over self-review.",
ask:"What would you compare?",
a:["Defects found by each on the same changes, using changes with known defects so both can be scored",
"The number of comments each produces",
"The cost of each",
"Developer preference"],
c:0,
w:["Correct. Detection rate on known defects is the comparison that answers the question.",
"Comment count conflates detection with noise.",
"Cost is one side of the trade; you need the benefit side too.",
"Preference is not a detection measure."],
e:"The blueprint states that an independent instance finds subtler issues than the generating one, and you can verify it on your own workload. Run both against changes with known defects, compare detection rate and false positives, and weigh the difference against the extra cost. Expect independence to matter most on subtle defects, which is precisely where the generating instance's anchoring bites.",
k:"Compare self-review and independent review on known-defect changes. Independence matters most on subtle defects."},

{i:1110,d:5,t:"5.4",s:"CGC",diff:2,
q:"You want a findings file to remain useful after the investigation ends.",
ask:"What should it end with?",
a:["The conclusions, the evidence for each, and any open questions &mdash; so a later reader knows what was established and what was not",
"A list of every file read",
"The full command history",
"A note that the investigation is complete"],
c:0,
w:["Correct. Conclusions with evidence, plus what remains open, is what future readers need.",
"A file list records activity rather than findings.",
"Command history is process detail.",
"Completion status without content is not useful."],
e:"A findings file's lasting value is the conclusions with their evidence &mdash; paths, line references, quoted excerpts &mdash; so a later reader can verify rather than re-derive. Recording open questions matters equally: it prevents a future reader assuming everything was settled, and it is often where the next investigation starts. This is the same distinction as coverage annotation in a research report.",
k:"End findings files with conclusions, their evidence, and the open questions. Open questions prevent false confidence."}
);
