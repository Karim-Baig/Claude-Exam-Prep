QB.push(
{i:466,d:1,t:"1.1",s:"AAT",diff:2,
q:"An agent loop uses <code>max_tokens: 200</code> because responses should be short. Tool calls with large argument objects now fail intermittently.",
ask:"What is happening?",
a:["The output limit truncates generation mid-<code>tool_use</code>, producing incomplete arguments and <code>stop_reason: \"max_tokens\"</code>",
"The tool schema is too large for the request",
"The model is rate limited",
"Tool arguments are not counted against <code>max_tokens</code>"],
c:0,
w:["Correct. Tool arguments are generated output and count against the limit like any other tokens.",
"Input schema size is a separate concern from output limits.",
"Rate limiting produces a distinct error, not truncated arguments.",
"They absolutely count &mdash; that is the cause."],
e:"<code>max_tokens</code> caps everything the model generates, including the JSON arguments inside a <code>tool_use</code> block. Set it high enough for the largest plausible tool call, and always branch on <code>stop_reason: \"max_tokens\"</code> by discarding the partial turn rather than executing whatever parsed. Executing partially-generated arguments against a state-changing tool is the dangerous version of this bug.",
k:"Tool arguments count against <code>max_tokens</code>. On truncation, discard the turn &mdash; never execute partial arguments."},

{i:467,d:1,t:"1.2",s:"MAR",diff:3,
q:"Your coordinator dispatches five subagents, and one returns a result that contradicts three others on a key fact.",
ask:"What should the coordinator do?",
a:["Preserve the disagreement with attribution and route it for reconciliation rather than adopting the majority view silently",
"Adopt the majority position, since three sources agree",
"Discard the outlier as unreliable",
"Re-run the outlier subagent until it agrees"],
c:0,
w:["Correct. Majority is not truth, and a silent adoption hides a material disagreement from the reader.",
"Three agents citing derivative sources can all be wrong together; count is not evidence quality.",
"The outlier may have found the primary source the others missed.",
"Re-running until you get the answer you expect is not verification."],
e:"Two principles combine. Conflicts are preserved with attribution and dates rather than resolved silently at the wrong layer. And apparent corroboration must be traced to origins &mdash; three agents may be citing three articles derived from one study, while the outlier consulted the study itself. Surface the disagreement with its provenance so the reader or a human reviewer can judge.",
k:"Majority is not truth. Preserve disagreements with attribution and trace apparent corroboration to its origin."},

{i:468,d:1,t:"1.3",s:"AAT",diff:2,
q:"A subagent's output contract requires a <code>sources</code> array. It returns the array but every entry is a bare domain name.",
ask:"What should the contract have specified?",
a:["The required shape of each entry &mdash; full URL, title, date and the quote supporting the claim",
"That the array must contain at least five entries",
"That the array must be sorted",
"That the array must be returned as a string"],
c:0,
w:["Correct. Specifying a field's presence without specifying its element shape leaves the useful detail unstated.",
"Count without shape gives five equally useless bare domains.",
"Ordering does not make an unverifiable entry verifiable.",
"A string is strictly less structured."],
e:"Output contracts must specify nested shape, not just top-level fields. &ldquo;Return a <code>sources</code> array&rdquo; is satisfied by <code>[\"example.com\"]</code>. Specify each element: URL, title, publication date, and the quote that supports the claim it is cited for. Enforcing that with a JSON Schema makes the shape structural rather than a request.",
k:"Specify nested element shape in output contracts, not just field presence. Enforce with a schema where it matters."},

{i:469,d:1,t:"1.4",s:"CSA",diff:3,
q:"Your hook blocks refunds over $500. A customer has three separate orders, each with a $400 refund, totalling $1,200.",
ask:"What does this reveal?",
a:["The control is per-call, so an aggregate limit needs state tracking cumulative refunds per customer or per period",
"The threshold should be lowered to $300",
"Hooks cannot enforce financial controls",
"The agent should be instructed not to split refunds"],
c:0,
w:["Correct. Per-call thresholds are trivially circumvented by splitting; aggregate limits need aggregate state.",
"Lowering the per-call threshold does not stop splitting; it just requires more splits.",
"Hooks enforce this fine &mdash; the control was specified per call rather than per period.",
"A prompt instruction guarding a financial control, which is the pattern the exam consistently marks insufficient."],
e:"Threshold controls need a clearly defined scope: per call, per conversation, per customer, per day. A per-call limit is the easiest to implement and the easiest to circumvent, and the circumvention need not even be deliberate &mdash; three genuine orders produce the same effect. Track cumulative state in your store and evaluate the aggregate in the hook.",
k:"Define threshold scope explicitly: per call, conversation, customer or period. Per-call limits are trivially split."},

{i:470,d:1,t:"1.5",s:"AAT",diff:2,
q:"A <code>PostToolUse</code> hook throws an exception while normalising a result.",
ask:"What should happen?",
a:["Fail safely with a clear error rather than passing through unnormalised data that downstream logic assumes is normalised",
"Silently pass the raw result through",
"Retry the hook indefinitely",
"Terminate the session"],
c:0,
w:["Correct. Downstream code and prompts assume normalisation happened; violating that assumption silently is worse than failing.",
"Passing raw data through breaks the invariant the hook exists to maintain, and does so invisibly.",
"An unbounded retry converts a bug into a hang.",
"Killing the session over one normalisation failure is disproportionate."],
e:"Every hook needs a defined failure behaviour, and it follows from what the hook guarantees. A normalisation hook establishes an invariant that downstream reasoning depends on, so it should fail loudly rather than pass through data that violates it. Contrast with an observational logging hook, which should fail open because nothing depends on it.",
k:"Hook failure behaviour follows the guarantee: invariant-establishing hooks fail loudly, observational hooks fail open."},

{i:471,d:1,t:"1.6",s:"CGC",diff:2,
q:"A task has a discovery phase producing large output, then an implementation phase needing only a summary of it.",
ask:"What is the cleanest structure?",
a:["Run discovery in a subagent or forked context that returns a summary, then implement in the main session using that summary",
"Run both phases in one session and compact between them",
"Run implementation first and discover as needed",
"Run both phases in one session with a larger context window"],
c:0,
w:["Correct. Isolate the verbose phase so only the summary crosses into the phase that needs it.",
"Compaction is lossy about exactly the file paths and specifics implementation needs.",
"Implementing before understanding produces rework.",
"A larger window still fills with discovery output."],
e:"When phases have very different output profiles, isolate the verbose one. The subagent's greps and reads stay in its own window; the main session receives a structured summary with the paths and facts implementation requires. Write that summary to a file as well, so it survives a crash and can seed a fresh session if implementation runs long.",
k:"Isolate verbose phases in subagents or forks; carry forward a structured summary, and persist it to a file."},

{i:472,d:1,t:"1.7",s:"CGC",diff:2,
q:"You resume a session and immediately ask a question whose answer depends on a file you know has changed.",
ask:"What should you do first?",
a:["Tell the session which file changed and have it re-read the file before answering",
"Ask the question and trust the session to notice the change",
"Run <code>/compact</code> to clear stale content",
"Start over with a completely fresh session"],
c:0,
w:["Correct. Explicit invalidation plus a re-read replaces the stale content with current content.",
"Nothing prompts the session to re-read; it answers from what it holds.",
"Compaction summarises stale content into vaguer stale content.",
"Justified for widespread change, but heavy-handed when one known file changed."],
e:"Session context is a cache with no invalidation protocol, so you must invalidate it yourself. For a single known file, naming it and requesting a re-read is precise and cheap, and it puts the fresh content recently in context where salience is highest. Escalate to a fresh session with a summary when the change is large or structural &mdash; renames, moves and deletions are hard to invalidate piecemeal.",
k:"Invalidate explicitly for a few known files; restart fresh with a summary when the change is large or structural."},

{i:473,d:2,t:"2.1",s:"CSA",diff:2,
q:"Your <code>process_refund</code> description does not mention the $500 approval threshold.",
ask:"What is the consequence?",
a:["The agent attempts refunds it cannot complete, wasting turns and producing confusing conversations even though the hook blocks them",
"The hook will fail to block the refund",
"The API will reject the tool definition",
"Refunds above $500 will succeed"],
c:0,
w:["Correct. Enforcement holds, but an agent that does not know the rule behaves poorly around it.",
"The hook enforces regardless of what the description says.",
"Descriptions are not validated against business rules.",
"The hook blocks them; the problem is the wasted attempt and poor explanation."],
e:"This is the complementarity of prompts and code from the tool-description side. The hook guarantees the boundary; the description teaches the rule so the agent proactively routes large refunds to escalation, explains the situation to the customer, and does not burn turns on attempts that will be blocked. Stating constraints in descriptions improves behaviour without weakening enforcement.",
k:"State enforced constraints in the description too. Enforcement guarantees the outcome; description produces good behaviour around it."},

{i:474,d:2,t:"2.2",s:"AAT",diff:3,
q:"A tool can fail for six distinct reasons. You are deciding how granular the error categories should be.",
ask:"What should drive the granularity?",
a:["The number of genuinely distinct recovery actions the caller can take, not the number of internal failure modes",
"The number of internal exception types the implementation has",
"The number of fields in the response schema",
"A fixed standard of exactly four categories"],
c:0,
w:["Correct. Categories exist to drive recovery, so they should partition by recovery action.",
"Internal exception types are implementation detail with no bearing on what the caller should do.",
"Response shape is unrelated to error taxonomy.",
"Four is a useful starting taxonomy, not a rule &mdash; the recovery actions decide."],
e:"Error categories are an interface for the caller's decision, so partition by what the caller will do differently. Six internal failure modes that all imply &ldquo;retry shortly&rdquo; are one category. Two that imply &ldquo;escalate&rdquo; and &ldquo;fix the input&rdquo; are two. The common four &mdash; transient, validation, permission, business &mdash; work well because they map to four genuinely different recoveries; put the internal specifics in the message.",
k:"Partition error categories by distinct recovery actions, not by internal failure modes. Specifics go in the message."},

{i:475,d:2,t:"2.3",s:"AAT",diff:2,
q:"An agent needs one operation from a domain outside its specialisation, and it is a write operation.",
ask:"What is the most defensible design?",
a:["Route it through the coordinator to the agent that owns that domain, rather than granting a cross-domain write",
"Grant the agent the write tool directly as a cross-role utility",
"Grant it the full toolset of the owning agent",
"Have the agent instruct the user to perform the operation"],
c:0,
w:["Correct. Cross-role utilities should be read-only and narrow; writes belong to the domain owner.",
"Cross-role write access blurs ownership and expands the blast radius of a misrouted call.",
"Far broader than needed and degrades selection reliability.",
"Pushing an automatable operation onto the user defeats the purpose."],
e:"The blueprint's allowance for cross-role utilities is deliberately modest &mdash; a limited set, alongside role-relevant tools. Read-only lookups fit comfortably; writes do not, because they carry ownership, auditability and blast-radius implications. When a write is needed outside the domain, route it through the coordinator to the owning agent, which keeps the trace clean and the responsibility clear.",
k:"Cross-role utilities: read-only and narrow. Cross-domain writes route through the coordinator to the owning agent."},

{i:476,d:2,t:"2.4",s:"DPT",diff:2,
q:"A team member asks whether MCP servers configured in <code>.mcp.json</code> are available in CI.",
ask:"What is the correct answer?",
a:["Yes, provided the CI environment supplies the required environment variables and can reach the servers",
"No, MCP servers only work in interactive sessions",
"No, CI requires a separate MCP configuration file",
"Yes, and no credentials are needed in CI"],
c:0,
w:["Correct. The same project configuration applies; CI just needs the variables and network access.",
"MCP works in non-interactive runs.",
"The same <code>.mcp.json</code> serves both.",
"Credentials are still required &mdash; supplied from the CI secret store."],
e:"One of the strengths of project-scoped MCP configuration is that it serves developers and CI identically. Developers resolve <code>${TOKEN}</code> from their environment; CI resolves it from its secret store. Use a dedicated service credential in CI rather than a person's token, so access is attributable, scoped and independently revocable.",
k:"Project <code>.mcp.json</code> works in CI too. Supply variables from the CI secret store, using a service credential."},

{i:477,d:2,t:"2.5",s:"DPT",diff:3,
q:"An agent must determine whether a class is used anywhere outside its own module.",
ask:"What must the search account for?",
a:["Imports, re-exports and dynamic references, since a plain name search can miss indirect usage and over-report matches in comments",
"Only files with the same extension as the class definition",
"Only files modified in the last month",
"Only files in the same top-level directory"],
c:0,
w:["Correct. Both under- and over-reporting are risks, and indirect usage is the harder one.",
"Usage can occur in other file types &mdash; configuration, templates, tests in other languages.",
"Recency is unrelated to usage.",
"Usage from anywhere in the repository counts."],
e:"Symbol-usage searches have two failure modes. Under-reporting: usage flows through a re-export or a dynamic reference, so the plain name never appears at the call site. Over-reporting: the name appears in comments, strings or an unrelated symbol with the same name. Trace re-exports explicitly, inspect the matches rather than counting them, and state your coverage honestly rather than presenting a partial list as exhaustive.",
k:"Symbol searches under-report via re-exports and over-report via comments and collisions. Trace, inspect, and state coverage."},

{i:478,d:3,t:"3.1",s:"CGC",diff:3,
q:"A team wants CLAUDE.md to serve as onboarding documentation for new developers as well as context for Claude.",
ask:"What is the risk?",
a:["Content useful to humans but not decision-relevant to the model bloats the context on every request",
"CLAUDE.md cannot be read by humans",
"Human-oriented content causes parsing errors",
"Onboarding documentation must live in a different format"],
c:0,
w:["Correct. CLAUDE.md is a per-request context budget, so non-decision-relevant content has a recurring cost.",
"It is plain markdown and perfectly readable by humans.",
"There is no parsing constraint.",
"The format is not the issue; the cost of the content is."],
e:"The two audiences want different things. Humans want history, rationale, org context and how to get set up; the model needs the conventions that change its decisions. Keep CLAUDE.md focused on the latter and put the rest in a README, linking between them. Where a document genuinely serves both, <code>@path</code> imports let you include just the decision-relevant section.",
k:"CLAUDE.md is a per-request context budget, not onboarding documentation. Keep human context in the README."},

{i:479,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill needs to run a build command but nothing else.",
ask:"How should <code>allowed-tools</code> be configured?",
a:["Grant only the narrow tool that runs builds, or a tightly-scoped Bash permission &mdash; not general shell access",
"Grant <code>Bash</code> so the skill can run any command it needs",
"Grant all tools and rely on the skill's instructions",
"Leave <code>allowed-tools</code> unset for maximum flexibility"],
c:0,
w:["Correct. Grant the narrowest capability that accomplishes the task.",
"General <code>Bash</code> is arbitrary command execution, far beyond running a build.",
"Granting everything and restricting by instruction is the pattern the exam consistently marks insufficient.",
"Unset means unrestricted, which is the opposite of the requirement."],
e:"<code>allowed-tools</code> is where a skill's capability is decided, and the principle is the same as for agents: omit rather than grant-and-restrict. If a purpose-built build tool exists, use it &mdash; it also returns structured results the skill can act on. If shell access is unavoidable, scope it with permission rules that allow-list the specific commands.",
k:"Grant the narrowest capability in <code>allowed-tools</code>. Prefer purpose-built tools; scope Bash with allow-listed commands."},

{i:480,d:3,t:"3.3",s:"CGC",diff:2,
q:"Two rule files both match <code>src/api/**/*</code> and give different guidance about error responses.",
ask:"What should you do?",
a:["Consolidate them into one rule, or make the relationship explicit so one is clearly the exception to the other",
"Leave both and let the model decide which applies",
"Delete the older rule without review",
"Broaden both globs so they apply everywhere"],
c:0,
w:["Correct. Two contradictory rules loaded together are genuinely ambiguous and produce unpredictable behaviour.",
"Leaving the model to arbitrate between contradictory instructions is the failure, not a resolution.",
"Deleting without review may discard the more current guidance.",
"Broadening spreads the contradiction to more files."],
e:"Path scoping makes many rules manageable, but overlapping globs with contradictory content reintroduce ambiguity. Either consolidate into one rule that states the full picture, or phrase the narrower one as an explicit exception (&ldquo;in the public API layer, override the project default and return&hellip;&rdquo;). Reviewing overlapping globs periodically is worth building into your process.",
k:"Overlapping globs with contradictory content are ambiguous. Consolidate, or phrase one as an explicit exception."},

{i:481,d:3,t:"3.4",s:"CGC",diff:2,
q:"A change is small but touches a security-critical authorisation path.",
ask:"Which approach is appropriate?",
a:["Planning mode despite the small size, because the consequence of getting it wrong is high",
"Direct execution, since the change is small",
"Direct execution followed by a security review",
"A multi-agent workflow with three parallel implementations"],
c:0,
w:["Correct. Consequence, not size, is the deciding signal for planning.",
"Size alone understates the risk on an authorisation path.",
"Better than nothing, but reviewing after the fact is weaker than reasoning about the approach first.",
"Three parallel implementations of a small change is unjustified overhead."],
e:"The planning triggers are large scope, multiple viable approaches and architectural consequence &mdash; and security-critical paths fall under consequence regardless of diff size. A three-line change to an authorisation check deserves the approach being reasoned about explicitly before it is made, and it also deserves the independent review that follows.",
k:"Consequence outranks size in the planning decision. Security-critical paths get planned regardless of diff size."},

{i:482,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to convey a performance requirement: the function must handle 100,000 items in under two seconds.",
ask:"How should this be communicated?",
a:["As a test case with the stated input size and time bound, included in the test set before implementation",
"As a sentence in the prompt asking for efficient code",
"As a note in CLAUDE.md about performance mattering",
"By asking for the most optimised implementation possible"],
c:0,
w:["Correct. A performance test makes the requirement verifiable and keeps it verified.",
"&ldquo;Efficient&rdquo; is undefined and unverifiable.",
"A general note gives no specific target for this function.",
"&ldquo;Most optimised possible&rdquo; invites unnecessary complexity with no defined stopping point."],
e:"The blueprint prescribes building test sets covering expected behaviour, edge cases <em>and</em> performance requirements before implementation. Expressing the performance target as a test converts a vague aspiration into a checkable goal, gives the iteration loop a hard signal, and prevents both under-optimisation and gold-plating &mdash; once the test passes, you are done.",
k:"Express performance requirements as tests with concrete inputs and bounds. Vague efficiency requests are unverifiable."},

{i:483,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review needs to know which test fixtures are available so generated tests use them rather than inventing new ones.",
ask:"Where should this be documented?",
a:["In CLAUDE.md or a path-scoped rule covering test files, so it is in context whenever tests are generated",
"In a comment in each fixture file",
"In the CI job's environment variables",
"In the pull request template"],
c:0,
w:["Correct. The blueprint prescribes documenting testing standards and available fixtures in CLAUDE.md.",
"Comments in fixture files are only seen if those files are read, which requires already knowing about them.",
"Environment variables are not context for the model.",
"PR templates are read by humans, after the fact."],
e:"The blueprint names both remedies for poor test generation: document testing standards and available fixtures in CLAUDE.md, and include existing test files in context. The first gives durable knowledge of what exists; the second gives current style and coverage. A path-scoped rule on test files is the refined version, keeping the fixture inventory out of context on requests that touch no tests.",
k:"Document testing standards and fixtures in CLAUDE.md or a test-scoped rule; also include existing tests in context."},

{i:484,d:4,t:"4.1",s:"SDE",diff:2,
q:"Your prompt says: <em>extract the vendor name accurately.</em> Extractions sometimes capture the customer name instead.",
ask:"What is missing?",
a:["A statement of how to identify the vendor in these documents &mdash; where it appears, how it differs from the customer",
"An instruction to be more careful",
"A higher-capability model",
"A lower temperature"],
c:0,
w:["Correct. &ldquo;Accurately&rdquo; assumes the model knows how to tell vendor from customer; the prompt must say how.",
"Care without criteria does not resolve the confusion.",
"Capability helps at the margin but the ambiguity remains.",
"Temperature does not disambiguate two similarly-formatted names."],
e:"On an invoice, vendor and customer are both organisation names in similar positions, so &ldquo;accurately&rdquo; asks for a judgement the model has no basis to make. State the discriminator &mdash; the vendor issues the invoice and typically appears in the header with tax registration details; the customer appears under Bill To. Reinforce with few-shot examples, and consider extracting both fields so a swap is detectable.",
k:"Ambiguous fields need a stated discriminator, not an exhortation to accuracy. Extract both sides so swaps are detectable."},

{i:485,d:4,t:"4.2",s:"CSA",diff:2,
q:"Your few-shot examples show the agent escalating in three cases. Escalation rates rise sharply.",
ask:"What went wrong?",
a:["One-sided examples shifted behaviour toward escalation; examples of resolving autonomously are needed too",
"Three examples is too many",
"The examples were too detailed",
"Few-shot prompting does not work for escalation decisions"],
c:0,
w:["Correct. A boundary needs examples from both sides, or behaviour shifts toward whichever side was demonstrated.",
"Count is not the issue; one-sidedness is.",
"Detail helps; the imbalance is the problem.",
"It works well &mdash; when the examples are balanced."],
e:"Few-shot examples shift the behavioural distribution toward what they demonstrate. For a decision boundary that means showing both outcomes, ideally with near-miss pairs: a policy exception that escalates alongside a superficially similar case that is inside policy and resolves. Include the rationale for each so the model learns the discriminating feature rather than the surface pattern.",
k:"Boundary examples must be balanced. Show both outcomes, ideally as near-miss pairs with rationale."},

{i:486,d:4,t:"4.3",s:"SDE",diff:2,
q:"You need extracted line items as an array of objects, each with a description, quantity and unit price.",
ask:"How should the schema express this?",
a:["An array whose <code>items</code> is an object schema declaring the three fields with their types",
"An array of strings, each containing the three values delimited",
"Three parallel arrays, one per field",
"A single string containing the whole table"],
c:0,
w:["Correct. A typed array of objects makes the structure explicit and validates each element.",
"Delimited strings require downstream parsing and reintroduce the syntax errors you avoided.",
"Parallel arrays can desynchronise in length, permitting invalid states.",
"An unstructured string discards all the structure."],
e:"Schema design should make invalid states unrepresentable. An array of typed objects keeps each line item's fields bound together and validated. Parallel arrays permit a length mismatch that silently misaligns quantities with descriptions &mdash; the same class of error as a delimited string requiring you to parse it again. Declare numeric fields as numbers so arithmetic checks work without parsing.",
k:"Model repeated records as an array of typed objects. Parallel arrays and delimited strings permit invalid states."},

{i:487,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to distinguish extraction failures caused by poor source quality from those caused by prompt deficiencies.",
ask:"What should you capture?",
a:["The source characteristics alongside the failure &mdash; scan quality, document type, layout &mdash; so failures can be grouped by cause",
"The total time each extraction took",
"The model version only",
"The number of fields successfully extracted"],
c:0,
w:["Correct. Grouping failures by source characteristics is what separates a source problem from a prompt problem.",
"Latency does not indicate the cause of a quality failure.",
"Useful for change tracking but does not distinguish these two causes.",
"A count says how bad it was, not why."],
e:"Diagnosis requires attributes you can group by. If failures cluster on low-quality scans, the remedy is upstream &mdash; better capture, or routing scans to a different pipeline. If they cluster on one field across all source types, the remedy is the prompt or schema for that field. Without the source attributes recorded, both look like the same undifferentiated failure rate.",
k:"Record source characteristics with failures. Clustering by source vs by field distinguishes an input problem from a prompt problem."},

{i:488,d:4,t:"4.5",s:"SDE",diff:2,
q:"A batch job's results are needed by 09:00. Processing may take up to 24 hours.",
ask:"When should the batch be submitted at the latest?",
a:["By 09:00 the previous day, giving the full processing window before the deadline",
"By 21:00 the previous evening",
"By 06:00 on the day",
"At any time, since batches usually complete quickly"],
c:0,
w:["Correct. Plan against the stated maximum window, not the typical case.",
"Twelve hours is less than the stated maximum window, so it risks missing the deadline.",
"Three hours is far short of the possible processing time.",
"There is no latency SLA, so relying on typical completion is exactly what the guarantee does not cover."],
e:"Batch planning uses the worst case, because there is no latency SLA to fall back on. If the window is up to 24 hours and the deadline is 09:00, submit by 09:00 the previous day. Where documents arrive continuously, shorten the submission interval so queue-wait time is bounded too &mdash; total time is queue wait plus processing, not processing alone.",
k:"Plan batches against the maximum window, not the typical case. Total = queue wait + processing."},

{i:489,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want a second opinion on a design decision the agent already made and defended.",
ask:"How should you obtain it?",
a:["Ask a fresh instance with the problem statement and the proposed design, but not the reasoning that produced it",
"Ask the same instance to argue against its own decision",
"Ask the same instance to rate its confidence in the decision",
"Ask the same instance to list alternatives it considered"],
c:0,
w:["Correct. A fresh instance without the advocacy context evaluates the design on its merits.",
"Instructed self-criticism from within the same context produces token objections rather than genuine reassessment.",
"Self-rated confidence is uncalibrated and, from an advocacy context, systematically high.",
"Useful information, but it comes from the same reasoning that produced the decision."],
e:"The same context-isolation principle that makes independent code review effective applies to design decisions. Give the fresh instance the problem and the proposed solution &mdash; not the argument for it &mdash; and ask it to evaluate. If you want it to consider alternatives, ask for its own rather than showing you the first instance's list, which would anchor it.",
k:"Second opinions need a fresh context: give the problem and the proposal, withhold the advocacy."},

{i:490,d:5,t:"5.1",s:"CAP",diff:2,
q:"An agent must not lose the fact that a user is under 18, which affects what content it may discuss.",
ask:"Where should this live?",
a:["In the persistent facts block and, ideally, enforced by tool restrictions or a hook rather than relying on the model to remember",
"In the first user message only",
"In a summary the agent regenerates each turn",
"In the agent's own notes at the end of each response"],
c:0,
w:["Correct. A safety-relevant constraint needs both salience (facts block) and enforcement (code).",
"An early conversational statement loses salience across a long session.",
"A regenerated summary is subject to the same degradation as any summarisation.",
"Self-appended notes are unreliable and inflate every response."],
e:"Constraints with safety consequences get both layers. The facts block keeps the constraint salient so the agent behaves appropriately by default; a hook or tool restriction guarantees the boundary regardless. This is the same layering as the refund threshold &mdash; prompt for competence, code for the guarantee &mdash; applied to a context-management problem.",
k:"Safety-relevant facts get both salience (persistent block) and enforcement (hook or tool restriction)."},

{i:491,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent is asked to do something that is technically within its tool capability but outside what the business wants agents doing autonomously.",
ask:"What is the correct arrangement?",
a:["Remove or gate the capability, since a capability the business does not want exercised should not be freely available",
"Instruct the agent not to use the capability",
"Log every use for later review",
"Ask the agent to seek confirmation in conversation before using it"],
c:0,
w:["Correct. Capability should match intent; a tool you do not want used freely should be gated or removed.",
"An instruction guarding a capability the business explicitly does not want exercised is probabilistic where certainty is wanted.",
"Logging detects after the fact and does not prevent.",
"Conversational confirmation is a prompt-level behaviour with no enforcement."],
e:"Align capability with intent. If an operation should require human involvement, gate it behind an approval hook; if it should not happen at all, do not grant the tool. Granting a capability and asking the model not to use it is the recurring insufficient pattern, and it also degrades selection reliability by enlarging the toolset with something that should never be chosen.",
k:"Capability should match intent. Gate what needs approval, remove what should not happen. Do not grant-and-instruct."},

{i:492,d:5,t:"5.3",s:"MAR",diff:2,
q:"Three subagents each report a different error, all marked <code>transient</code> and retryable.",
ask:"What should the coordinator consider?",
a:["Whether a shared dependency is failing, since three simultaneous transient errors suggest a common cause rather than three coincidences",
"Retrying all three immediately in parallel",
"Escalating all three to a human",
"Marking all three as permanent failures"],
c:0,
w:["Correct. Correlated failures usually indicate a shared upstream problem, which changes the right response.",
"Immediate parallel retry against a struggling shared dependency can make the outage worse.",
"Premature; a shared transient outage often resolves without human involvement.",
"Overreacting in the other direction, and it discards genuinely recoverable work."],
e:"Individual error classification is necessary but not sufficient &mdash; patterns across errors carry information that no single error does. Three simultaneous transient failures point at a shared dependency, which argues for backing off rather than retrying immediately, and for annotating coverage if the outage persists. Correlated-failure detection belongs at the coordinator, which is the only component that sees all of them.",
k:"Look at error patterns, not just individual errors. Correlated transient failures suggest a shared dependency &mdash; back off."},

{i:493,d:5,t:"5.4",s:"DPT",diff:2,
q:"An investigation's scratchpad contains findings that later turned out to be wrong.",
ask:"What should happen to them?",
a:["Correct or explicitly mark them superseded, since a scratchpad read later cannot distinguish current findings from retracted ones",
"Leave them, since more information is always better",
"Delete the whole scratchpad and start over",
"Move them to the end of the file"],
c:0,
w:["Correct. An externalised record is only useful if it is trustworthy; stale findings actively mislead.",
"Wrong information is worse than no information when it is read as current.",
"Discards the findings that are still correct.",
"Position does not signal retraction to a later reader."],
e:"A scratchpad is a source of truth for future reads, including by a fresh session or a colleague, so its accuracy matters as much as its existence. Correct entries in place, or mark them explicitly as superseded with what replaced them &mdash; the correction history is often itself useful, showing which hypotheses were tested and ruled out.",
k:"Keep externalised findings accurate. Correct or explicitly mark superseded entries; a stale record misleads."},

{i:494,d:5,t:"5.5",s:"SDE",diff:2,
q:"Human reviewers approve 98% of the items routed to them for review.",
ask:"What does this suggest?",
a:["The routing threshold may be too conservative, sending items that did not need review and consuming reviewer capacity",
"The reviewers are not being careful enough",
"The extraction quality is too high to need review",
"The threshold should be lowered further to catch more errors"],
c:0,
w:["Correct. A 98% approval rate on the review queue suggests most routed items were already correct.",
"Possible, but the simpler explanation is that the queue contains mostly correct items.",
"Sampling of the auto-approved stream is still needed regardless.",
"Routing even more correct items to review worsens the inefficiency."],
e:"Review-queue approval rate is a calibration signal in the opposite direction from auto-approval error rate. A very high approval rate means the threshold is too conservative and reviewer capacity is being spent on correct items &mdash; which also causes review fatigue and reduces attention on the items that genuinely need it. Recalibrate against labelled data and monitor both directions.",
k:"Monitor both directions: error rate in the auto-approved stream, and approval rate in the review queue."},

{i:495,d:5,t:"5.6",s:"MAR",diff:2,
q:"A source is credible but its publication date is six years old, and the topic changes rapidly.",
ask:"How should this be represented?",
a:["Include the date prominently with the claim, so readers can judge its current relevance",
"Exclude the source as too old",
"Include it without the date to avoid undermining the finding",
"Adjust the figure to estimate a current value"],
c:0,
w:["Correct. The date is what lets a reader judge relevance; suppressing it removes their ability to do so.",
"Age alone does not invalidate a source, and it may be the only data available.",
"Omitting the date to protect a finding is misleading.",
"Extrapolating a figure invents data that no source supports."],
e:"Dates are part of the finding, not decoration. They let readers judge currency, and they prevent apparent contradictions that are really temporal differences. Where a topic changes rapidly and only old data exists, that itself is worth stating &mdash; &ldquo;the most recent available figure is from 2020&rdquo; is a useful finding about the evidence base, not a weakness to hide.",
k:"Dates travel with claims. They establish currency and prevent temporal differences reading as contradictions."},

{i:496,d:1,t:"1.1",s:"GEN",diff:2,
q:"Which <code>stop_reason</code> value indicates that a configured stop sequence was produced?",
ask:"Which is correct?",
a:["<code>stop_sequence</code>",
"<code>end_turn</code>",
"<code>max_tokens</code>",
"<code>tool_use</code>"],
c:0,
w:["Correct. <code>stop_sequence</code> means generation halted because a configured stop sequence appeared.",
"<code>end_turn</code> means the model finished naturally.",
"<code>max_tokens</code> means the output limit was reached.",
"<code>tool_use</code> means a tool call is being requested."],
e:"Know the values and their loop implications: <code>tool_use</code> &rarr; execute and continue; <code>end_turn</code> &rarr; stop; <code>max_tokens</code> &rarr; incomplete, discard the partial turn and retry with more headroom; <code>stop_sequence</code> &rarr; a configured sequence was hit, so handle according to why you configured it. Branching on all of them, rather than treating everything non-<code>end_turn</code> as &ldquo;continue&rdquo;, is what makes a loop robust.",
k:"Branch on every <code>stop_reason</code>. Treating all non-<code>end_turn</code> values as &ldquo;continue&rdquo; is how partial tool calls get executed."},

{i:497,d:2,t:"2.2",s:"GEN",diff:2,
q:"Which combination of metadata best supports agent recovery from a tool error?",
ask:"Which set?",
a:["An error category, a retryability flag, and a specific human-readable message",
"A numeric code and a timestamp",
"A stack trace and the tool version",
"A severity rating and an incident id"],
c:0,
w:["Correct. Category and retryability drive the recovery decision; the message supports explanation and correction.",
"A code requires a lookup table and a timestamp is operational metadata.",
"Stack traces are internal detail that leak implementation and give no recovery signal.",
"Severity and incident ids matter for operations, not for the agent's next action."],
e:"The recovery-supporting trio is category, retryability and a specific message. Category tells the agent what kind of problem this is; retryability answers the immediate question; the message enables a corrected retry or a clear explanation to the user. Add partial results where any were obtained, and keep internal diagnostics in your logs behind a correlation id.",
k:"Recovery metadata: <code>errorCategory</code> + <code>isRetryable</code> + specific message, plus partial results where available."},

{i:498,d:3,t:"3.2",s:"GEN",diff:2,
q:"Which statement about project versus user configuration in Claude Code is correct?",
ask:"Which is true?",
a:["Anything under the project's <code>.claude/</code> is version-controlled and shared; the <code>~/.claude/</code> equivalents are personal",
"User configuration always overrides project configuration for the whole team",
"Project configuration is only loaded when explicitly enabled",
"Only one of the two levels can be active at a time"],
c:0,
w:["Correct. Location determines sharing, and this rule holds for commands, skills, rules and CLAUDE.md alike.",
"User configuration affects only that user; it cannot affect the team.",
"Project configuration loads automatically when present.",
"Both levels apply together."],
e:"The location rule is the single most useful thing to know for Domain 3, because it answers a whole family of questions. <code>.claude/commands/</code>, <code>.claude/skills/</code>, <code>.claude/rules/</code> and project CLAUDE.md are shared through version control. The <code>~/.claude/</code> equivalents are personal. <code>.mcp.json</code> is project scope; <code>~/.claude.json</code> is user scope.",
k:"Project <code>.claude/</code> = shared via VCS. <code>~/.claude/</code> = personal. This rule answers most Domain 3 questions."},

{i:499,d:4,t:"4.5",s:"GEN",diff:2,
q:"Which is NOT a characteristic of the Message Batches API?",
ask:"Which statement is false?",
a:["It guarantees results within a fixed latency SLA",
"It offers approximately 50% cost savings",
"It has a processing window of up to 24 hours",
"It does not support multi-turn tool calling within a single request"],
c:0,
w:["Correct &mdash; this is the false statement. There is explicitly no latency SLA.",
"True: roughly 50% savings.",
"True: up to 24 hours.",
"True: batch requests are single-shot, not agent loops."],
e:"The absence of a latency guarantee is the defining constraint and the reason Batch is unsuitable for blocking work. Hold all four facts together: about 50% cheaper, up to a 24-hour window, no latency SLA, and no multi-turn tool calling per request. Together they define exactly which workloads fit.",
k:"Batch: ~50% cheaper, up to 24h, <strong>no latency SLA</strong>, single-shot only. The missing SLA is the deciding constraint."},

{i:500,d:5,t:"5.1",s:"GEN",diff:2,
q:"Which practice best preserves precise numeric values across a long conversation?",
ask:"Which one?",
a:["Maintaining them in a structured facts block outside the summarised history",
"Summarising more frequently so summaries stay short",
"Instructing the summariser to preserve numbers",
"Relying on the full history being re-sent each request"],
c:0,
w:["Correct. Keeping values out of the summarised stream entirely is the only reliable approach.",
"More frequent summarisation compounds loss with each pass.",
"An instruction to a lossy process reduces the rate without eliminating it.",
"Presence in history does not guarantee salience across 40 turns."],
e:"Progressive summarisation degrades numbers, percentages and dates first, turning them into qualitative paraphrase. A code-maintained facts block containing identifiers, amounts, dates and decisions never passes through the summariser, is small enough to re-inject every request, and sits in a stable position where salience is high.",
k:"Precise values belong in a code-maintained facts block, never in the summarised stream."}
);
