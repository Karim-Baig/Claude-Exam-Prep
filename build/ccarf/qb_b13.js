QB.push(
{i:501,d:1,t:"1.1",s:"CSA",diff:2,
q:"Your loop treats any response containing a <code>tool_use</code> block as a signal to continue, without reading <code>stop_reason</code>. A response arrives with <code>stop_reason: \"max_tokens\"</code> and a partially generated <code>tool_use</code> block.",
ask:"What will your loop do, and why is it dangerous?",
a:["It will execute a tool call with truncated arguments, which for a state-changing tool can cause a wrong or partial action",
"It will correctly skip the incomplete block and continue",
"It will raise a schema validation error before executing",
"It will terminate the loop safely"],
c:0,
w:["Correct. Inspecting content instead of <code>stop_reason</code> misses truncation entirely.",
"Nothing skips it &mdash; the loop was written to react to the block's presence.",
"Truncated JSON may still parse into a valid-looking partial object, so validation is not a guaranteed catch.",
"There is nothing in this loop that would terminate on truncation."],
e:"Content inspection is not a substitute for <code>stop_reason</code>. A truncated <code>tool_use</code> block can look superficially complete, and for <code>process_refund</code> or a write operation the consequence is real. Always branch on <code>stop_reason</code> first: <code>max_tokens</code> means discard the turn and retry with more headroom or a smaller request.",
k:"Branch on <code>stop_reason</code>, not on content. A truncated tool call can look valid and still be wrong."},

{i:502,d:1,t:"1.1",s:"AAT",diff:3,
q:"An agent must call a tool whose result determines whether two <em>other</em> independent tools should be called.",
ask:"What is the correct execution shape?",
a:["Call the first tool, then on the next turn emit both dependent calls in parallel if the result warrants them",
"Call all three in parallel in one turn to minimise latency",
"Call all three sequentially across three turns",
"Call the two dependent tools first and discard the results if the first tool says they were unnecessary"],
c:0,
w:["Correct. Respect the one genuine dependency, then parallelise the two calls that are independent of each other.",
"The two dependent calls cannot be emitted before their gating condition is known.",
"Serialising the independent pair wastes a round trip.",
"Speculative execution wastes calls and, for state-changing tools, causes real side effects that cannot be discarded."],
e:"Read the dependency graph rather than applying a blanket rule. One gating call, then a parallel pair, is two levels rather than three &mdash; the critical path is the longest dependency chain. Note why speculative execution is especially wrong for tools with side effects: you cannot un-send an email you decided afterwards was unnecessary.",
k:"Respect real dependencies, parallelise within each level. Never speculatively execute tools with side effects."},

{i:503,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator dispatches four subagents and one returns in 2 seconds while the others take 60. The fast one returned an error.",
ask:"What should the coordinator do?",
a:["Inspect the error's category and, if it is transient or the query was malformed, re-dispatch that subtask immediately in parallel with the others still running",
"Wait for all four to finish before considering the failure",
"Abort the workflow since one subagent failed",
"Ignore the failure and proceed with three results"],
c:0,
w:["Correct. An early failure is an opportunity to recover concurrently rather than serially after everything else finishes.",
"Waiting wastes 58 seconds during which the recovery could have run.",
"Aborting on one failure is the named anti-pattern.",
"Silently proceeding leaves an unannotated gap."],
e:"Fan-out failures do not have to be handled at the join. An error that arrives early can be diagnosed and re-dispatched while the other branches are still running, so the recovery costs no additional wall-clock time. This requires the error to carry category and context &mdash; without it the coordinator cannot decide whether re-dispatch is worthwhile.",
k:"Handle fan-out failures as they arrive, not at the join. Early failures can be recovered concurrently."},

{i:504,d:1,t:"1.3",s:"AAT",diff:2,
q:"You want a subagent to stop and report rather than proceed when it encounters something outside its assigned scope.",
ask:"Where should this be specified?",
a:["In the delegation prompt's quality criteria and output contract, stating the scope boundary and how to report an out-of-scope discovery",
"In the coordinator's system prompt",
"In the subagent's tool list",
"Nowhere &mdash; subagents naturally stay in scope"],
c:0,
w:["Correct. Scope boundaries and the reporting mechanism belong in the delegation, where the subagent will actually read them.",
"The coordinator's prompt is not visible to the subagent.",
"Tool restrictions limit capability but do not express a scope boundary within that capability.",
"Subagents will pursue interesting findings unless told where the boundary is."],
e:"A scope boundary is only useful if the subagent also has a way to report what it found beyond it &mdash; otherwise it either ignores the discovery or exceeds its scope. Add an <code>out_of_scope_observations</code> field to the output contract, so the finding reaches the coordinator, which can decide whether to dispatch a new subtask for it.",
k:"State scope boundaries in the delegation, and give the subagent a channel to report out-of-scope discoveries."},

{i:505,d:1,t:"1.4",s:"CSA",diff:2,
q:"An agent needs to take an action that is reversible but visible to the customer, such as sending a status email.",
ask:"How should this be governed?",
a:["Prompt guidance is usually sufficient, but consider a rate or frequency limit in code so a loop cannot spam the customer",
"Full human approval before every send",
"No governance &mdash; it is reversible",
"Remove the capability entirely"],
c:0,
w:["Correct. Match friction to consequence: guidance for the decision, a code-level bound against runaway repetition.",
"Approval on every routine email destroys throughput and causes approval fatigue.",
"An email is not truly reversible once read, and an agent loop can send many.",
"Removing a useful capability over a manageable risk."],
e:"Outward-facing actions sit between the clearly-prompt and clearly-code categories. The judgement (should we email now?) suits prompt guidance. The failure mode that actually hurts &mdash; a loop sending twelve emails &mdash; is bounded cheaply in code with a frequency limit. That is a good general pattern: guide the decision, bound the pathological case.",
k:"For outward-facing but reversible actions: guide the decision in the prompt, bound the pathological case in code."},

{i:506,d:1,t:"1.5",s:"AAT",diff:2,
q:"You need to know, for compliance, exactly which tool calls an agent attempted &mdash; including ones that were blocked.",
ask:"How should this be captured?",
a:["Log in the <code>PreToolUse</code> hook, which sees every attempt before the block decision is applied",
"Log in the <code>PostToolUse</code> hook, which sees results",
"Log the model's final response text",
"Log only successful tool executions"],
c:0,
w:["Correct. <code>PreToolUse</code> is the only position that observes attempts that never execute.",
"<code>PostToolUse</code> never fires for a blocked call, so blocked attempts would be invisible.",
"Final responses summarise; they do not record attempts.",
"Excludes exactly the blocked attempts the requirement asks for."],
e:"Position determines what a hook can observe as well as what it can do. <code>PreToolUse</code> sees every attempt, including those about to be denied &mdash; which is what makes it the right place for both enforcement and attempt-level audit. Log the decision alongside the attempt (allowed or blocked, and which rule fired) so the record is complete and provable.",
k:"<code>PreToolUse</code> observes every attempt including blocked ones. Log the attempt and the decision together."},

{i:507,d:1,t:"1.6",s:"MAR",diff:2,
q:"A research task has one sub-domain that is far larger than the other three.",
ask:"How should decomposition handle this?",
a:["Split the large sub-domain further so subtask sizes are roughly comparable and no single branch dominates latency",
"Assign the large sub-domain to a more capable model",
"Assign all four to one subagent since sizes differ",
"Reduce the scope of the large sub-domain to match the others"],
c:0,
w:["Correct. Balanced subtask sizes keep the critical path short and give each branch comparable depth.",
"A better model does not change the fact that one branch takes four times as long.",
"Collapsing to one agent forfeits parallelism entirely.",
"Truncating coverage to balance the partition sacrifices the thing you are measuring."],
e:"In a parallel fan-out, wall-clock time is set by the slowest branch, so wildly uneven partitions waste the parallelism. Sub-split the large branch so the sizes are comparable. This also improves depth &mdash; a subagent given four times as much ground covers each part more thinly, which is the shallow-coverage failure in a subtler form.",
k:"Balance subtask sizes in a fan-out. The slowest branch sets latency, and an oversized branch is covered thinly."},

{i:508,d:1,t:"1.7",s:"CGC",diff:2,
q:"A phased plan's phase 3 fails. Phases 1 and 2 succeeded and phase 4 depends only on phase 2.",
ask:"What should happen?",
a:["Retry or revise phase 3, and proceed with phase 4 in parallel since it does not depend on phase 3",
"Restart the whole plan from phase 1",
"Skip phase 3 and continue to phase 4",
"Abandon the plan"],
c:0,
w:["Correct. The dependency graph tells you exactly what is blocked and what is not.",
"Discards two successful phases and repeats their cost.",
"Skipping a failed phase leaves a gap that later work may silently depend on.",
"Abandoning over one recoverable phase failure is the abort anti-pattern."],
e:"A written plan pays off precisely here: it records the dependencies, so a failure's blast radius is computable rather than guessed. Phase 4 depends on phase 2, not phase 3, so it proceeds. Record in the plan that phase 3 is outstanding, so it is not forgotten when the run appears to complete.",
k:"Use the plan's dependency graph to scope a failure's impact. Only genuinely dependent phases are blocked."},

{i:509,d:2,t:"2.1",s:"AAT",diff:2,
q:"A tool's description is excellent but 900 words long, and there are twelve such tools.",
ask:"What is the trade-off?",
a:["Descriptions are sent on every request, so extremely long ones consume substantial context; aim for complete but concise",
"Long descriptions are rejected by the API",
"Long descriptions improve selection without limit",
"Description length has no effect on cost"],
c:0,
w:["Correct. Tool definitions are part of every request's payload, so twelve 900-word descriptions is a recurring cost.",
"There is no such rejection.",
"Returns diminish, and the context cost is real and recurring.",
"They are billed as input tokens on every request."],
e:"Descriptions must be complete enough to make selection reliable and no longer. The high-value content is compact: what it does, when to choose it, input format with a short example, what it returns, and the boundary against neighbours. Prose elaboration beyond that costs tokens on every call. Prompt caching on the stable prefix mitigates the cost but does not make verbosity free.",
k:"Tool definitions are sent every request. Complete but concise; cache the stable prefix to reduce the recurring cost."},

{i:510,d:2,t:"2.2",s:"CSA",diff:2,
q:"An error message says: <em>The refund could not be processed at this time.</em>",
ask:"What is wrong with it?",
a:["It gives neither the reason nor whether retrying could help, so the agent cannot choose between retry, alternative and escalation",
"It is too polite",
"It reveals too much internal detail",
"It is too short to be parsed"],
c:0,
w:["Correct. &ldquo;At this time&rdquo; hints at transience without committing, and no reason is given.",
"Tone is not the issue.",
"It reveals nothing at all &mdash; that is the problem.",
"Length is irrelevant; the missing information is."],
e:"This message is worse than it looks because &ldquo;at this time&rdquo; implies transience. If the real cause is a policy limit, the agent will retry a call that can never succeed. Every error needs an explicit category and retryability flag rather than an implication, plus a specific reason the agent can explain to the customer.",
k:"Never imply retryability with vague phrasing. State the category and the flag explicitly."},

{i:511,d:2,t:"2.3",s:"AAT",diff:3,
q:"You set <code>tool_choice: \"any\"</code> for a single-shot classification, but the model has three tools available and picks the wrong one about 8% of the time.",
ask:"What is the most direct fix?",
a:["Force the specific classification tool by name, removing the selection decision entirely",
"Improve the descriptions of all three tools",
"Switch to <code>auto</code> so the model has more freedom",
"Add few-shot examples of correct tool selection"],
c:0,
w:["Correct. If exactly one tool is correct for this call, force it and eliminate the choice.",
"Worth doing in general, but for a single-shot call where only one tool is ever correct, removing the decision is stronger.",
"<code>auto</code> would additionally permit a text response, making things worse.",
"Examples reduce the error rate without eliminating the decision that causes it."],
e:"When only one tool is ever correct for a given call, forcing it by name converts a probabilistic selection into a certainty. Reserve <code>any</code> for the case where several schemas are genuinely valid and the model must choose &mdash; then description quality does matter. Match the constraint to how much choice actually exists.",
k:"One correct tool &rarr; force it by name. Several valid &rarr; <code>any</code> plus good descriptions. Remove decisions that need not exist."},

{i:512,d:2,t:"2.4",s:"DPT",diff:2,
q:"Your team's <code>.mcp.json</code> is reviewed in pull requests. A developer proposes adding a server that grants write access to production systems.",
ask:"Why does the review matter here?",
a:["Project MCP configuration grants capability to every developer's agent on clone, so a write-capable production server is a significant expansion of blast radius",
"MCP servers must be reviewed for JSON syntax",
"Project configuration is loaded more slowly with more servers",
"Reviews are needed only for servers requiring credentials"],
c:0,
w:["Correct. Project scope means everyone gets it, so the capability decision is a team-wide security decision.",
"Syntax is the least of it; capability is the substance.",
"Load performance is not the concern.",
"Capability matters regardless of whether credentials are involved."],
e:"Project <code>.mcp.json</code> is a capability grant to every developer's agent. Treat additions as security review: what operations does the server expose, what is the blast radius of a misrouted call, is a narrower wrapper appropriate, and does write access to production belong behind a specialised agent with an approval gate rather than in everyone's default toolset.",
k:"Project <code>.mcp.json</code> grants capability team-wide. Review additions as a security decision, not a config change."},

{i:513,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to confirm whether a file exists before deciding what to do next.",
ask:"Which tool is most appropriate?",
a:["<code>Glob</code> with the specific path pattern, which answers the existence question without reading contents",
"<code>Read</code>, and treat a failure as non-existence",
"<code>Bash</code> with <code>test -f</code>",
"<code>Grep</code> for any character in the file"],
c:0,
w:["Correct. A filename query answered by the filename tool, with no content cost.",
"Using a failed read as an existence check is indirect and pulls in contents when the file does exist.",
"Would work but reaches for shell capability when a purpose-built tool answers directly.",
"An odd inversion that reads content to answer a metadata question."],
e:"Match the tool to the question being asked. Existence is a filename question, so <code>Glob</code> answers it directly and cheaply. Using <code>Read</code> as a probe works but costs the file's contents whenever the answer is yes, and reaching for <code>Bash</code> grants arbitrary execution to answer something a narrow tool handles.",
k:"Match tool to question: filename questions to Glob, content questions to Grep, contents to Read."},

{i:514,d:3,t:"3.1",s:"CGC",diff:2,
q:"A developer adds a personal preference to the project CLAUDE.md: <em>always explain your reasoning in detail before making changes.</em>",
ask:"What is the problem?",
a:["It imposes one developer's working style on the whole team and costs context and tokens for everyone",
"Detailed explanations are always undesirable",
"CLAUDE.md cannot contain instructions about response style",
"The instruction should be in a rules file instead"],
c:0,
w:["Correct. Personal working preferences belong at user level, not in shared project configuration.",
"Detailed explanations suit some developers; the issue is scope, not merit.",
"Style instructions are valid content &mdash; at the right level.",
"Moving it to a rules file keeps it shared, which is the actual problem."],
e:"The scope question is always &ldquo;who should this affect?&rdquo; A personal working preference goes in <code>~/.claude/CLAUDE.md</code>, where it follows that developer across every project without imposing on colleagues. Project CLAUDE.md is for conventions the team has agreed and that the model would otherwise get wrong.",
k:"Personal working style &rarr; user-level CLAUDE.md. Project level is for agreed team conventions only."},

{i:515,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill invoked with <code>context: fork</code> needs to return a decision the main session will act on.",
ask:"What must be true of its output?",
a:["It must return a compact, self-contained result, since the main session sees only what is returned and none of the forked context",
"It must return its full working transcript so the main session can verify it",
"It cannot return anything; forked skills are fire-and-forget",
"It must write its result to a file, since forked contexts cannot return values"],
c:0,
w:["Correct. Isolation means the return payload is the entire interface back to the main session.",
"Returning the transcript defeats the isolation that <code>context: fork</code> provides.",
"Forked skills return results; that is the point.",
"Files are a useful complement but not a requirement."],
e:"<code>context: fork</code> is the skill-level version of subagent delegation, and the same discipline applies: the return payload is the whole interface. Scope it deliberately &mdash; the conclusion, the minimum evidence needed to trust it, and an explicit uncertainty note. Everything else stays in the forked context and disappears with it.",
k:"With <code>context: fork</code>, the return payload is the entire interface. Scope it to conclusion + minimal evidence."},

{i:516,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want a security-review convention that applies to authentication code wherever it lives, and that code is spread across four services.",
ask:"What is the correct mechanism?",
a:["A path-scoped rule with globs matching the authentication paths in all four services",
"A directory CLAUDE.md in each of the four services",
"A single entry in the root CLAUDE.md",
"A skill that developers invoke when touching auth code"],
c:0,
w:["Correct. One rule with several globs covers a convention that follows a concern across directories.",
"Four copies that will drift, and they would apply to everything in those services rather than to auth code specifically.",
"Loads on every request regardless of what is being edited.",
"Skills are invoked manually; a security convention should not depend on remembering."],
e:"Path globs can list several patterns, which is what makes a single rule able to follow a concern across a codebase. That is the advantage over directory CLAUDE.md when the concern is cross-cutting: one definition, one place to update, and it activates precisely on the files it concerns.",
k:"A single path-scoped rule can carry several globs, following a concern across services. One definition, no drift."},

{i:517,d:3,t:"3.4",s:"CGC",diff:3,
q:"You are asked to choose between two libraries for a new subsystem. The choice affects several downstream design decisions.",
ask:"Which approach fits?",
a:["Planning mode, to explore both options' implications before committing &mdash; possibly with forked sessions to prototype each",
"Direct execution with the more popular library",
"Direct execution with both, then delete the loser",
"Ask the model to pick and proceed"],
c:0,
w:["Correct. Multiple viable approaches with downstream consequences is the planning trigger, and forking suits the comparison.",
"Popularity is a weak proxy for fit against your specific constraints.",
"Building both fully is expensive; a prototype comparison in forked sessions is the cheaper version of this instinct.",
"Delegating an architectural decision without exploring the implications is how avoidable rework happens."],
e:"This combines two mechanisms. Planning mode is right because there are multiple defensible approaches with architectural consequence. Forked sessions are the natural way to compare them &mdash; shared context about the requirements, independent exploration of each option, no cross-contamination. Record the rationale in the plan file so the decision survives the session.",
k:"Multiple viable approaches &rarr; plan, and fork to compare. Record the rationale in the plan file."},

{i:518,d:3,t:"3.5",s:"CGC",diff:2,
q:"An implementation is functionally correct but does not match your team's established patterns.",
ask:"What does this suggest about your configuration?",
a:["The patterns are not documented where the model can see them &mdash; CLAUDE.md or a path-scoped rule",
"The model is incapable of following patterns",
"The implementation should be accepted as an improvement",
"Patterns should be enforced by code review only"],
c:0,
w:["Correct. The model follows conventions it is given; a mismatch usually means they were never stated.",
"It follows documented patterns reliably.",
"Possibly true occasionally, but inconsistency across a codebase has real costs.",
"Review catches it after the work is done, repeatedly, rather than preventing it."],
e:"Recurring pattern mismatches are a configuration signal. Document the convention where it will be in context &mdash; CLAUDE.md for project-wide patterns, a path-scoped rule for patterns tied to particular file types. That converts a repeated review comment into a one-time configuration change, which is the leverage CLAUDE.md exists to provide.",
k:"Recurring review comments about conventions are a signal to document the convention, not to keep commenting."},

{i:519,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review job takes 12 minutes on large pull requests and developers complain it blocks merges.",
ask:"Which change is most appropriate?",
a:["Split the review into parallel per-file passes and a cross-file pass, so the work is concurrent rather than serial",
"Move the review to the Message Batches API for cost savings",
"Reduce the review criteria to make it faster",
"Run the review only on the main branch after merge"],
c:0,
w:["Correct. Parallelising the per-file passes reduces wall-clock time while preserving coverage.",
"Batch has no latency SLA, which is exactly wrong for a blocking pre-merge check.",
"Reducing coverage to reduce latency is the wrong trade.",
"Post-merge review means defects reach main."],
e:"A pre-merge check is blocking, which rules out Batch entirely. The right lever is concurrency: per-file passes are independent and can run in parallel, with the cross-file integration pass following. That also happens to be the architecture that produces better findings, so the latency fix and the quality fix are the same change.",
k:"Blocking checks stay synchronous. Reduce wall-clock time with parallel per-file passes, not by cutting coverage."},

{i:520,d:4,t:"4.1",s:"CCI",diff:2,
q:"Your review criteria say to flag &ldquo;potential security issues&rdquo;.",
ask:"Why is this likely to produce noise?",
a:["&ldquo;Potential&rdquo; has no threshold, so anything conceivably security-adjacent qualifies",
"Security issues are inherently hard to detect",
"The criterion is too narrow",
"Security review requires a specialised model"],
c:0,
w:["Correct. An unbounded qualifier invites flagging anything that touches security in any way.",
"Many are detectable; the problem is the criterion's breadth.",
"It is too broad, not too narrow.",
"Model choice does not supply a missing threshold."],
e:"Words like &ldquo;potential&rdquo;, &ldquo;possible&rdquo; and &ldquo;may&rdquo; remove the threshold from a criterion. Replace with categorical definitions: flag unvalidated input reaching a query or a shell command, credentials in source, authorisation checks that can be bypassed &mdash; each with a code example. That is checkable, and it is what keeps precision high enough for developers to keep trusting the bot.",
k:"&ldquo;Potential&rdquo; and &ldquo;possible&rdquo; remove the threshold. Define categories concretely with code examples."},

{i:521,d:4,t:"4.2",s:"SDE",diff:2,
q:"You add few-shot examples and accuracy improves on cases resembling the examples but degrades slightly elsewhere.",
ask:"What is happening?",
a:["The examples are steering behaviour toward their own characteristics; broaden them to span the input variation",
"Few-shot prompting is unsuitable for this task",
"The examples should be removed",
"The temperature should be raised to counteract the bias"],
c:0,
w:["Correct. Examples shift the distribution toward what they demonstrate; homogeneous examples produce a localised improvement.",
"It works well &mdash; the examples just need to be representative.",
"Removing them loses the improvement without addressing the cause.",
"Raising temperature adds variance rather than breadth."],
e:"Few-shot examples are a strong steer, which is why their composition matters. Keep the format consistent so the output shape is learned, and vary the content &mdash; different layouts, different difficulty levels, positive and negative cases &mdash; so the underlying rule is learned rather than a region of the input space.",
k:"Examples steer strongly. Consistent format, varied content, spanning the real input distribution."},

{i:522,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want the model to indicate which parts of a document it could not read clearly.",
ask:"How should the schema support this?",
a:["Add a field for per-field extraction quality or an explicit list of unreadable regions, so uncertainty is expressible rather than hidden",
"Rely on the model mentioning it in a free-text note",
"Assume any extracted value was readable",
"Reject any document with unreadable regions"],
c:0,
w:["Correct. If uncertainty has no place in the schema, it disappears and everything looks equally confident.",
"Free-text notes are unstructured and cannot drive routing decisions.",
"Assuming readability is how confidently-wrong values enter the warehouse.",
"Many documents have a smudged region and are otherwise perfectly extractable."],
e:"Schema design determines what the system can even represent. Without a field for uncertainty, a value guessed from a blurred region is indistinguishable from one read cleanly. Making it expressible turns a hidden risk into a routing signal &mdash; extractions touching unreadable regions go to human review, which is exactly the ambiguous-source criterion the blueprint prescribes.",
k:"Make uncertainty expressible in the schema. Unrepresentable uncertainty becomes invisible confidence."},

{i:523,d:4,t:"4.4",s:"SDE",diff:2,
q:"Your pipeline retries once and then routes to human review. Analysis shows a second retry would succeed 60% of the time.",
ask:"What should you consider?",
a:["Raising the retry budget, weighing the cost of an extra model call against the cost of human review time",
"Removing retries entirely to simplify the pipeline",
"Always retrying five times regardless of error category",
"Routing everything to human review"],
c:0,
w:["Correct. The retry budget is an economic decision between model cost and reviewer cost.",
"Removing retries would push far more work to humans.",
"Category-blind retry wastes attempts on deterministic failures.",
"Abandons automation entirely."],
e:"Retry budgets should be tuned with data, not chosen arbitrarily. If a second retry succeeds 60% of the time, and a model call costs far less than a reviewer's time, raising the budget is clearly correct. Keep the category discipline though &mdash; the extra retry applies to cases where retrying is meaningful, not to permission or business-rule failures.",
k:"Tune retry budgets economically: model cost versus reviewer time. Keep retries category-aware."},

{i:524,d:4,t:"4.5",s:"SDE",diff:2,
q:"A batch of 5,000 documents completes but 200 results are missing entirely from the output file.",
ask:"What should you check first?",
a:["Whether those 200 requests failed individually, identified by <code>custom_id</code>, rather than assuming a systemic output problem",
"Whether the output file was truncated during download",
"Whether the batch size limit was exceeded",
"Whether the model was unavailable"],
c:0,
w:["Correct. Per-item failure is the most common explanation and <code>custom_id</code> makes it directly checkable.",
"Possible but less likely, and easily ruled out after checking the per-item statuses.",
"A size limit would have been rejected at submission, not produced partial results.",
"Model unavailability would affect far more than 4% of items."],
e:"Batch processing is per-item, so missing results almost always mean per-item failures. Use <code>custom_id</code> to reconcile submitted against returned, inspect the failure reasons for the missing set, and look for what they share &mdash; a document type, a source, a size. Then re-submit only that subset, unless the cause is systematic and needs fixing first.",
k:"Reconcile batch results by <code>custom_id</code>. Missing results are usually per-item failures with a shared cause."},

{i:525,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want to measure whether your multi-pass review architecture is actually better than a single pass.",
ask:"How should you evaluate it?",
a:["Run both against a fixed set of pull requests with known defects, and compare detection rate and false-positive rate",
"Compare the number of comments each produces",
"Ask developers which they prefer",
"Compare the runtime of each approach"],
c:0,
w:["Correct. A labelled set lets you measure both recall and precision, which is what &ldquo;better&rdquo; means here.",
"Comment count conflates finding more defects with producing more noise.",
"Preference is useful signal but not a measurement of detection quality.",
"Runtime measures cost, not effectiveness."],
e:"Architecture decisions deserve measurement, and detection quality has two dimensions that must be measured together. A change that raises recall while raising false positives may be worse in practice, because false positives erode the trust that makes findings actionable. Use a fixed labelled set so the comparison is controlled, and report both rates.",
k:"Measure review architectures on recall <em>and</em> precision against a fixed labelled set. Comment count conflates the two."},

{i:526,d:5,t:"5.1",s:"CAP",diff:2,
q:"A conversation includes a long document the user pasted at turn 3. By turn 25 the context is mostly that document.",
ask:"What is the best remedy?",
a:["Extract the relevant facts from the document into the persistent facts block, and drop the full text once it is no longer needed",
"Keep the full document in context indefinitely",
"Summarise the whole conversation including the document",
"Ask the user to paste a shorter version"],
c:0,
w:["Correct. Extract what matters, then release the bulk &mdash; the same principle as trimming verbose tool output.",
"Carrying a large document through 25 turns crowds out the conversation itself.",
"Summarising the document loses the specifics that made it worth pasting.",
"Pushes the problem onto the user and may lose content that was needed."],
e:"Large pasted content is like a verbose tool result: valuable when first processed, then mostly dead weight. Extract the facts, figures and passages that matter into the structured facts block, note where the original can be retrieved if needed, and release the rest. That preserves what the conversation depends on at a fraction of the tokens.",
k:"Extract what matters from bulk content into the facts block, then release the bulk. Note where the original lives."},

{i:527,d:5,t:"5.2",s:"CSA",diff:2,
q:"A customer's request is within policy, but fulfilling it requires an action the agent's tools cannot perform.",
ask:"What should happen?",
a:["Escalate with a structured handoff noting that the request is in-policy and specifying the action required",
"Tell the customer the request is not possible",
"Attempt an approximate action with the tools available",
"Ask the customer to make the request differently"],
c:0,
w:["Correct. A capability gap is a legitimate escalation, and noting that it is in-policy speeds the human's work considerably.",
"The request is possible &mdash; just not by this agent.",
"An approximate action on the customer's account risks doing something they did not ask for.",
"Rephrasing does not create a capability that does not exist."],
e:"Capability gaps are a form of &ldquo;inability to progress&rdquo;, and the handoff quality matters. Stating that the request is in-policy and naming the specific action required means the human can act immediately rather than re-establishing eligibility. Recurring capability-gap escalations are also a product signal &mdash; they tell you which tool to build next.",
k:"Capability gaps escalate. State that the request is in-policy and name the required action; track them as a product signal."},

{i:528,d:5,t:"5.3",s:"MAR",diff:2,
q:"A subagent returns a result but flags that its data source was last updated 18 months ago.",
ask:"What should the coordinator do with this?",
a:["Carry the staleness note through to the output so readers can judge relevance, and consider whether a fresher source exists",
"Discard the result as too old",
"Use the result without the staleness note to keep the report clean",
"Retry the subagent hoping for fresher data"],
c:0,
w:["Correct. Staleness is metadata that belongs with the finding, and it may warrant seeking a fresher source.",
"Eighteen-month-old data is often the best available and may be perfectly relevant.",
"Suppressing the caveat misleads readers about the evidence's currency.",
"Retrying the same source returns the same data."],
e:"This is the dates-and-provenance principle applied to source freshness. The staleness note travels with the finding so readers can weigh it, and it prompts a reasonable follow-up &mdash; is there a more recent source? If not, saying &ldquo;the most recent available figure is from 2024&rdquo; is itself a useful finding about the evidence base.",
k:"Carry staleness and currency notes through to the output. They inform the reader and may prompt a better source."},

{i:529,d:5,t:"5.4",s:"DPT",diff:2,
q:"Your main session coordinates six subagent investigations. After the fourth, its own context is 70% full.",
ask:"What should you do before dispatching the fifth?",
a:["Write the accumulated findings to a file and reduce the live context to a compact summary plus the current working set",
"Dispatch the remaining two and hope the context holds",
"Increase the context window",
"Cancel the remaining investigations"],
c:0,
w:["Correct. Externalise before the pressure becomes degradation, keeping only what the next dispatch needs.",
"Running into degradation with two investigations remaining risks the quality of all six.",
"Postpones the problem without addressing the accumulation.",
"Abandons a third of the required work."],
e:"Act on context pressure before it becomes degradation, because the symptom &mdash; vague, pattern-level answers &mdash; appears after quality has already suffered. Write findings to a file, keep a compact summary in context, and dispatch the remaining subagents with the specific facts they need. The file also serves as the crash-recovery artefact for a long multi-phase run.",
k:"Act on context pressure before degradation appears. Externalise findings, keep a compact summary and the working set."},

{i:530,d:5,t:"5.5",s:"SDE",diff:2,
q:"You are asked to justify automating a pipeline segment to a risk committee.",
ask:"What evidence is most relevant?",
a:["Measured accuracy for that segment against labelled ground truth, with the error rate at the chosen threshold and the consequence of an error",
"The overall accuracy of the whole pipeline",
"The model's average self-reported confidence",
"The volume of documents the pipeline processes"],
c:0,
w:["Correct. Segment-level measured error rate plus consequence is what a risk decision actually needs.",
"Aggregate accuracy masks segment-level failure &mdash; the specific thing the committee should worry about.",
"Uncalibrated self-reported confidence is not evidence.",
"Volume describes scale, not risk."],
e:"A risk decision needs the error rate for the specific thing being automated and the consequence of an error at that rate. That means labelled ground truth, segmented by document type and field, at the threshold you propose &mdash; plus the sampling and monitoring plan that will detect drift after go-live. Aggregates and self-reported confidence do not support the decision.",
k:"Automation cases need segment-level measured error rate, the consequence of an error, and a drift-monitoring plan."},

{i:531,d:5,t:"5.6",s:"MAR",diff:2,
q:"A finding is supported only by a blog post with no author attribution.",
ask:"How should it be represented?",
a:["Include it with the source's nature stated, so readers can weigh it against better-sourced findings",
"Present it alongside peer-reviewed findings with equal weight",
"Exclude it entirely",
"Present it without naming the source to avoid undermining it"],
c:0,
w:["Correct. Source quality is part of the finding, and stating it lets readers weight appropriately.",
"Equal presentation of unequal evidence misleads.",
"Weak sources sometimes carry the only available signal on a topic.",
"Concealing the source's nature is the least defensible option."],
e:"This is the stable-versus-disputed structuring principle extended to source quality. Report the finding, state what supports it, and let the reader weigh it. Suppressing the source's nature to make a finding look stronger is a form of the attribution loss the whole provenance discipline exists to prevent.",
k:"Source quality travels with the finding. State it; do not equalise unequal evidence or conceal weak sourcing."},

{i:532,d:1,t:"1.2",s:"CSA",diff:2,
q:"Your router agent classifies a message as billing, but partway through the billing specialist discovers it is actually a shipping issue.",
ask:"What should happen?",
a:["The specialist returns to the coordinator with the correction and the context it gathered, and the coordinator re-routes",
"The billing specialist calls the shipping specialist directly",
"The billing specialist attempts to resolve the shipping issue itself",
"The conversation restarts with the customer"],
c:0,
w:["Correct. Re-routing is the coordinator's job, and returning the gathered context avoids repeating work.",
"Spoke-to-spoke calls break hub-and-spoke and remove the coordinator's routing control.",
"Handling an issue outside its specialisation with tools not suited to it is exactly the misuse pattern to avoid.",
"Restarting discards the context and frustrates the customer."],
e:"Misroutes happen, and the architecture should handle them cleanly rather than pretending they will not. The specialist recognises the mismatch, returns with a structured note including what it has already established, and the coordinator re-routes. That preserves the single trace, avoids duplicated work, and keeps the routing decision where it belongs.",
k:"Misroutes return to the coordinator with gathered context for re-routing. Never re-route spoke to spoke."},

{i:533,d:2,t:"2.2",s:"AAT",diff:2,
q:"Your tool returns <code>isError: true</code> with a good message, but no <code>errorCategory</code>.",
ask:"What does the agent lose?",
a:["The ability to apply a consistent recovery policy, since it must infer the category from prose on every call",
"The ability to display the message to the user",
"The ability to log the error",
"Nothing &mdash; a good message is sufficient"],
c:0,
w:["Correct. Prose inference is less reliable and less consistent than an explicit categorical field.",
"The message is present and can be displayed.",
"Logging works regardless.",
"A good message helps, but inferring category from it is exactly the unreliability the field removes."],
e:"A well-written message and an explicit category serve different consumers. The message supports explanation and corrected retries; the category and <code>isRetryable</code> flag give a machine-readable signal that the agent and your own code can branch on consistently. Relying on prose inference reintroduces variability into a decision that should be deterministic.",
k:"Message and category are complementary. Prose supports explanation; the category field drives consistent recovery."},

{i:534,d:3,t:"3.1",s:"CGC",diff:2,
q:"You want new team members to get the project's conventions automatically, with no setup steps.",
ask:"What is required?",
a:["The conventions must be in version-controlled project configuration &mdash; CLAUDE.md or <code>.claude/rules/</code> &mdash; so cloning is sufficient",
"Each developer must copy a template into their user configuration",
"The conventions should be in the team wiki with a link in the README",
"A setup script must install the configuration"],
c:0,
w:["Correct. Project configuration under version control is picked up on clone with no action required.",
"A copy step is exactly the setup the requirement excludes, and copies drift.",
"A wiki is not in the agent's context, and a link requires someone to follow it.",
"A setup script is a step, and it can be skipped or fail."],
e:"Zero-setup sharing is the defining property of project-scoped configuration, and it answers the new-joiner problem directly. Everything under the project's <code>.claude/</code> plus the root CLAUDE.md is version-controlled and active on clone. The one legitimate setup step is environment variables for MCP secrets, which is why those should be documented prominently.",
k:"Project configuration is active on clone with no setup. The only expected setup step is MCP environment variables."},

{i:535,d:4,t:"4.3",s:"SDE",diff:2,
q:"An extraction must return a value that is either a number or the string &ldquo;not stated&rdquo;.",
ask:"What is the better schema design?",
a:["A nullable number field, with absence represented as <code>null</code> rather than as a sentinel string",
"A string field that may contain either a number or the words &ldquo;not stated&rdquo;",
"Two fields: a number and a boolean indicating whether it was stated",
"A number field with -1 representing absence"],
c:0,
w:["Correct. A nullable typed field expresses absence without sacrificing the type.",
"A union-typed string forces downstream parsing and reintroduces the ambiguity you avoided.",
"Workable but more complex, and it permits invalid combinations such as a value present with the flag false.",
"Magic sentinel values are error-prone and can collide with legitimate data."],
e:"Express absence as <code>null</code> on a typed field rather than through sentinel strings or magic numbers. That keeps the type meaningful for arithmetic and validation, makes absence unambiguous, and avoids the classic sentinel collision where -1 is a legitimate value. It also aligns with making optional data explicitly nullable so the model is not pressured into fabrication.",
k:"Express absence as <code>null</code> on a typed field. Avoid sentinel strings and magic numbers."},

{i:536,d:5,t:"5.2",s:"CAP",diff:2,
q:"A user asks a question whose answer depends on which of two products they own, and the agent has no way to determine which.",
ask:"What should the agent do?",
a:["Ask which product they have, since this is a resolvable factual ambiguity",
"Answer for both products in one response",
"Escalate to a human",
"Answer for the more common product"],
c:0,
w:["Correct. A single clarifying question resolves it &mdash; the same pattern as multiple customer matches.",
"Answering for both produces a confusing response and doubles the chance of the user applying the wrong instructions.",
"Escalating something a question resolves depresses first-contact resolution.",
"Guessing risks giving instructions that will not work on their product."],
e:"Resolvable factual ambiguity is answered by asking, not by guessing or escalating. The parallel with multiple customer records is exact: ask for the discriminating detail and proceed. Reserve escalation for ambiguity about policy or authority, which no clarifying question can settle.",
k:"Resolvable factual ambiguity &rarr; ask one clarifying question. Escalate only ambiguity about policy or authority."},

{i:537,d:1,t:"1.5",s:"AAT",diff:2,
q:"A hook modifies a tool's arguments before execution. The agent's subsequent reasoning assumes the arguments it originally produced were used.",
ask:"What should you consider?",
a:["Making the modification visible in the result, so the agent's reasoning stays aligned with what actually happened",
"Never modifying arguments in hooks",
"Instructing the agent to assume arguments may change",
"Logging the modification and leaving the agent uninformed"],
c:0,
w:["Correct. Silent divergence between what the model requested and what executed causes misaligned reasoning.",
"Argument injection is a legitimate and recommended pattern for system-determined values.",
"A general instruction to distrust its own arguments is vague and unhelpful.",
"Logging serves operators, not the agent's reasoning."],
e:"Injecting system-determined values such as a tenant id is fine and usually invisible to the agent's reasoning. Modifying a value the agent chose deliberately is different &mdash; if a hook clamps a requested amount or rewrites a query, the result should say so, or the agent will report to the customer what it asked for rather than what happened.",
k:"Injecting system values is transparent; changing agent-chosen values should be surfaced in the result."},

{i:538,d:2,t:"2.1",s:"DPT",diff:2,
q:"You have a tool whose description says it is &ldquo;for advanced users&rdquo;.",
ask:"Why is this unhelpful?",
a:["It describes an audience rather than a trigger condition, so the model cannot tell when the tool applies",
"The API rejects descriptions containing audience references",
"It implies the tool is unreliable",
"It makes the description too short"],
c:0,
w:["Correct. Selection needs conditions on the request, not statements about who might use it.",
"No such rejection exists.",
"It implies nothing about reliability; it simply conveys nothing useful.",
"Length is not the issue."],
e:"Descriptions serve the model's selection decision, so every element should be a condition it can evaluate against the request. Audience notes, ownership and maturity labels convey nothing actionable. Replace with trigger conditions: when the request involves X, when the input is in format Y, and explicitly not when Z applies.",
k:"Descriptions state conditions the model can evaluate, not audiences, owners or maturity labels."},

{i:539,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review needs to skip files generated by a code generator.",
ask:"What is the cleanest way?",
a:["Exclude them when constructing the diff passed to the review, so they never enter the context",
"Instruct the model to ignore generated files",
"Review them and discard the findings afterwards",
"Add a comment header to every generated file"],
c:0,
w:["Correct. Excluding at input construction saves tokens and removes the possibility of misapplication.",
"An instruction still pays the token cost of the generated content and may not be followed reliably.",
"Reviewing then discarding wastes the entire cost of reviewing them.",
"Header comments depend on the model noticing and honouring them."],
e:"Control what enters the context rather than instructing the model to ignore what is already there. Filtering generated files, vendored dependencies and lockfiles out of the diff before it is sent saves tokens, sharpens attention on the code that matters, and makes the exclusion deterministic rather than a request.",
k:"Filter irrelevant content at input construction. Instructing the model to ignore it still costs the tokens."},

{i:540,d:4,t:"4.4",s:"SDE",diff:2,
q:"Your validation catches an error, the retry fixes it, and the record is persisted. You want to improve the first-attempt success rate.",
ask:"What should you analyse?",
a:["The distribution of first-attempt validation failures by rule and by document characteristics, to find the dominant correctable cause",
"The average number of retries per document",
"The total cost of retries",
"The time each retry takes"],
c:0,
w:["Correct. Grouping failures by rule and by source characteristics identifies what to fix.",
"An average tells you the scale of the problem, not its cause.",
"Cost quantifies the impact without indicating the remedy.",
"Latency is an operational metric, not a diagnostic one."],
e:"Improvement needs diagnosis, and diagnosis needs groupable attributes. If most first-attempt failures come from one field failing one rule, that field's description, examples or schema is the fix. If they cluster by document type instead, the remedy is a type-specific prompt or routing. Averages and costs describe the problem without locating it.",
k:"Group first-attempt failures by rule and by source characteristics. Averages describe; groupings diagnose."},

{i:541,d:5,t:"5.4",s:"CGC",diff:2,
q:"You are handing a long-running investigation to a colleague.",
ask:"What should you give them?",
a:["The findings file with current conclusions, exact paths, decisions taken and open questions",
"Your session id so they can resume it",
"A verbal summary in a meeting",
"The full session transcript"],
c:0,
w:["Correct. A structured findings file is the artefact designed to transfer.",
"Sessions are personal working contexts and are not a handover mechanism.",
"Verbal summaries lose the exact paths and values that make the work resumable.",
"A raw transcript is long, includes dead ends, and buries the conclusions."],
e:"Externalised findings serve three purposes at once: context management, crash recovery and handover. The file that keeps your own context small is the same artefact a colleague needs &mdash; conclusions, exact file paths and values, decisions with rationale, and what remains open. Keeping it accurate as you go is what makes all three uses work.",
k:"One findings file serves context management, crash recovery and handover. Keep it accurate and structured."},

{i:542,d:1,t:"1.6",s:"CGC",diff:2,
q:"A refactor can be done as one large change or as six smaller sequential changes, each independently verifiable.",
ask:"Which is generally preferable and why?",
a:["Six smaller changes, because each can be verified before the next begins, so a mistake surfaces early and is cheap to unwind",
"One large change, because it is faster overall",
"One large change, because it produces a single reviewable diff",
"Six smaller changes, because more commits look more thorough"],
c:0,
w:["Correct. Verification between steps limits how far a mistake propagates.",
"Speed is illusory if a defect at step two is only discovered after step six.",
"A large diff is harder to review, not easier.",
"Commit count is not a quality measure &mdash; the verification checkpoints are the point."],
e:"Phased execution with verification between phases is the recommended shape for substantial changes, and its value is entirely in the checkpoints. A defect caught after phase two costs one phase to unwind; the same defect caught at the end may invalidate everything after it. Where the phases are independent rather than sequential, they can also parallelise.",
k:"Prefer phased changes with verification between phases. The checkpoints, not the commit count, are the value."},

{i:543,d:2,t:"2.3",s:"AAT",diff:2,
q:"An agent has exactly the right tools for its role, but selection accuracy is still poor.",
ask:"What should you examine next?",
a:["The descriptions and names &mdash; whether each states its trigger conditions and boundary against the others",
"Whether the agent needs more tools",
"Whether the tool list order matters",
"Whether a larger model is required"],
c:0,
w:["Correct. With the right toolset, remaining selection errors point at description and naming quality.",
"Adding tools enlarges the choice space and makes selection worse.",
"Order is not a documented selection lever.",
"Capability helps at the margin but cannot resolve genuinely ambiguous descriptions."],
e:"Work through the causes in order. First, is the toolset the right size and scope for the role? Then, do the names and descriptions state trigger conditions and boundaries? Then, is there vocabulary in the system prompt creating an unintended pull toward a particular tool? Only after those does model capability become the limiting factor.",
k:"Selection debugging order: toolset size and scope &rarr; names and descriptions &rarr; system prompt vocabulary &rarr; model."},

{i:544,d:3,t:"3.2",s:"CGC",diff:2,
q:"A team has ten slash commands, and developers cannot remember what several of them do.",
ask:"What is the most useful improvement?",
a:["Give each a clear name and a concise description of when to use it, and prune commands nobody uses",
"Convert them all to skills",
"Move them to user scope so each developer keeps only their own",
"Consolidate all ten into one command with a mode parameter"],
c:0,
w:["Correct. Discoverability is a naming and description problem, and unused commands are noise.",
"Converting to skills adds machinery without addressing discoverability.",
"User scope loses the sharing that makes team commands worthwhile.",
"A mode parameter hides ten behaviours behind one name &mdash; the same problem multi-mode tools have."],
e:"Shared commands have the same discoverability requirements as tools: a name that conveys what it does and a short statement of when to reach for it. Pruning matters too &mdash; commands that nobody uses add cognitive load for everyone. Note why the mode-parameter option is wrong for the same reason it is wrong for tools: it buries the real distinction.",
k:"Shared commands need clear names, when-to-use descriptions, and pruning. Mode parameters bury the distinction."},

{i:545,d:4,t:"4.1",s:"SDE",diff:2,
q:"Your prompt lists twelve rules for extraction. Accuracy is good on the first few and worse on the later ones.",
ask:"What should you consider?",
a:["That a long undifferentiated rule list dilutes attention; group the rules by field or split into focused calls",
"That the later rules are inherently harder",
"That the rules should be listed alphabetically",
"That the prompt needs a stronger opening instruction"],
c:0,
w:["Correct. The positional pattern is the signature of attention dilution across a long instruction list.",
"Possible, but the positional correlation points at dilution rather than difficulty.",
"Alphabetical order is arbitrary with respect to importance.",
"A stronger opening does not help the rules at the end."],
e:"The same positional signature that appears in large diffs and 60-field schemas appears in long rule lists. Group rules with the fields they govern so each is read in context, or split the extraction into focused calls each carrying only its relevant rules. Position the most consequential rules early, and consider whether some are better expressed as schema constraints than as prose.",
k:"Long rule lists dilute attention, worst at the end. Group rules with their fields, split calls, or move rules into the schema."},

{i:546,d:5,t:"5.1",s:"AAT",diff:2,
q:"Your persistent facts block has grown to 40 entries over a long conversation.",
ask:"What should you do?",
a:["Prune entries that are no longer load-bearing, keeping the block small enough to stay salient",
"Keep adding, since more facts are always better",
"Move the block to the end of the request",
"Summarise the facts block"],
c:0,
w:["Correct. The block's value comes from being small and salient; an oversized block reintroduces the problem it solves.",
"An unbounded facts block becomes another source of dilution.",
"Position matters, but the size problem persists wherever you put it.",
"Summarising the facts block destroys the exact values that justified extracting them."],
e:"The facts block works because it is small, exact and salient. Let it grow unbounded and it becomes a second context to manage, with the same middle-position problem. Prune entries that are resolved or superseded &mdash; a disputed charge that has been refunded no longer needs to be live &mdash; and keep only what current and future turns actually depend on.",
k:"Keep the facts block small and current. Prune resolved entries; never summarise it, or you lose the exact values."},

{i:547,d:1,t:"1.3",s:"MAR",diff:2,
q:"Two subagents are given overlapping scopes by mistake and both research the same subtopic.",
ask:"What should the coordinator do with the duplicate results?",
a:["Use them as a consistency check where they agree, investigate where they disagree, and fix the partition before the next round",
"Discard one arbitrarily",
"Merge them without noting the duplication",
"Report both as independent corroboration"],
c:0,
w:["Correct. Extract what value the duplication offers, then fix the cause.",
"Discarding arbitrarily throws away a free cross-check.",
"Silent merging loses the signal that the partition was wrong.",
"They are not independent in the meaningful sense &mdash; they were given the same scope and may have found the same sources."],
e:"Accidental duplication has some salvage value as a consistency check: agreement is mildly reassuring, and disagreement is a strong signal worth investigating. But it is not independent corroboration in the way that matters, and the underlying partition defect should be fixed before the next dispatch round rather than tolerated.",
k:"Duplicate results are a consistency check, not corroboration. Investigate disagreements and fix the partition."},

{i:548,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to understand a build configuration spread across four files that reference each other.",
ask:"What is the efficient approach?",
a:["Read the entry configuration file, then follow only the references it actually makes, rather than reading all four up front",
"Read all four files immediately",
"Grep for configuration keys across the repository",
"Ask the developer to explain the configuration"],
c:0,
w:["Correct. Following actual references keeps reads justified and may show that one file is unused.",
"Reading all four assumes all are relevant; following references establishes which are.",
"Grep finds keys without revealing the reference structure that determines precedence.",
"Offloads to a human what the agent can determine directly."],
e:"Incremental investigation applies to configuration exactly as it does to code: start at the entry point and follow the references that actually exist. That establishes the precedence order &mdash; which matters a great deal in configuration &mdash; and may reveal that a file you assumed was relevant is not referenced at all.",
k:"Follow actual references from the entry point. It establishes precedence and reveals what is genuinely unused."},

{i:549,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want Claude to surface design considerations you may have missed for a new API.",
ask:"Which prompt approach fits?",
a:["Ask it to interview you &mdash; to ask the questions it would need answered before designing the API",
"Ask it to design the API and then critique the result",
"Ask it to list every possible design consideration for APIs generally",
"Ask it to produce three alternative designs"],
c:0,
w:["Correct. The interview pattern surfaces the unstated considerations specific to your case.",
"Designing first anchors the discussion on that design rather than on what is unspecified.",
"A generic checklist is not tailored to your API and buries the relevant items.",
"Three designs built on the same gaps share the same blind spots."],
e:"The interview pattern inverts the usual direction so the model's questions do the discovery. It is most valuable exactly when you suspect gaps but cannot name them, since a longer specification can only cover what you already thought of. The blueprint names cache invalidation and failure modes as typical of what it surfaces.",
k:"Interview pattern: have Claude ask what it needs to know. It surfaces gaps a longer specification cannot."},

{i:550,d:4,t:"4.6",s:"CCI",diff:2,
q:"Your independent review instance flags an issue the generating instance deliberately decided against, with a documented reason.",
ask:"What is the right handling?",
a:["Treat it as a legitimate finding to be resolved on its merits, since the reviewer's independence is exactly why it raised it",
"Suppress it, since the decision was already made",
"Give the reviewer the generation context so it stops raising the issue",
"Accept the reviewer's position automatically"],
c:0,
w:["Correct. An independent reviewer questioning a deliberate decision is the mechanism working as intended.",
"Suppressing findings that challenge decisions defeats the purpose of independent review.",
"Supplying the generation context reintroduces the anchoring that makes self-review weak.",
"The reviewer is not automatically right either &mdash; the decision may have been well-founded."],
e:"Independence produces friction by design. A reviewer that never questions the author's choices is not adding value, and a reviewer given the author's reasoning stops being independent. The resolution belongs to a human or to an explicit decision record &mdash; if the original decision was sound, document the rationale so the same finding can be dismissed quickly next time.",
k:"Independent reviewers should challenge deliberate decisions. Resolve on merit; document the rationale to settle recurrences."}
);
