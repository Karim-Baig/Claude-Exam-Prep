QB.push(
{i:346,d:2,t:"2.1",s:"CSA",diff:2,
q:"Your <code>escalate_to_human</code> tool description reads: &ldquo;Escalates the conversation.&rdquo; The agent calls it for cases it should handle and fails to call it for genuine policy exceptions.",
ask:"How should the description be rewritten?",
a:["State the specific trigger conditions &mdash; explicit request for a human, policy gap or exception, repeated failure to progress &mdash; and what should be resolved autonomously instead",
"Add the word IMPORTANT and repeat that escalation is serious",
"Explain the internal ticketing system the tool writes to",
"State the average time a human takes to respond after escalation"],
c:0,
w:["Correct. Naming the trigger conditions and the contrasting cases defines the decision boundary the model must apply.",
"Emphasis without criteria shifts the threshold arbitrarily; it does not clarify when to escalate.",
"Implementation detail the model cannot act on.",
"Operational metadata that does not tell the agent when escalation is appropriate."],
e:"Tool descriptions carry decision criteria, not just capability statements, and <code>escalate_to_human</code> is the clearest case. The three valid triggers &mdash; explicit request, policy gap or exception, inability to progress &mdash; belong in the description, along with the contrast: resolve autonomously when the case is inside documented policy. Pair with few-shot examples of both escalating and resolving to make the boundary learnable.",
k:"Descriptions of decision tools must carry the decision criteria: triggers, non-triggers, and the contrasting action."},

{i:347,d:2,t:"2.1",s:"AAT",diff:3,
q:"You maintain twelve tools. A new engineer adds a thirteenth with a description copied from an existing tool and lightly edited.",
ask:"What is the risk?",
a:["Near-identical descriptions create semantic overlap, so the model will misroute between the two unpredictably",
"Duplicate descriptions cause an API validation error",
"The newer tool will always be preferred over the older one",
"The older tool will be automatically deprecated"],
c:0,
w:["Correct. Overlapping descriptions are the primary cause of misrouting, and unpredictable routing is hard to debug.",
"The API does not compare descriptions for similarity.",
"There is no recency preference in selection.",
"Nothing is deprecated automatically."],
e:"Copy-and-edit is how tool catalogues drift into ambiguity. The safeguard is a review discipline: when adding a tool, compare its description against its nearest neighbours and require that each states an explicit boundary against the others. If you cannot articulate a clean boundary, the two tools probably should not both exist &mdash; merge them, or re-split along an axis that is genuinely unambiguous.",
k:"Review every new tool description against its neighbours. If you cannot state a clean boundary, the tools should not both exist."},

{i:348,d:2,t:"2.1",s:"SDE",diff:2,
q:"A tool returns different result shapes depending on input &mdash; sometimes an object, sometimes an array, sometimes a string.",
ask:"Why is this a problem for an agent?",
a:["Inconsistent output shapes make results hard to interpret reliably and force the agent to guess how to read them",
"The API rejects tools with variable return types",
"Variable shapes consume more tokens than fixed ones",
"The model cannot process arrays"],
c:0,
w:["Correct. A stable output contract is what lets the agent reason confidently about what it received.",
"Tool result content is not schema-validated by the API.",
"Token cost is incidental compared with the interpretation problem.",
"Models handle arrays fine."],
e:"Tool contracts run in both directions: the input schema constrains what the model sends, and a consistent output shape lets the model interpret what comes back. Variable shapes cause misreading, redundant re-queries and downstream errors. Return a stable envelope &mdash; always the same top-level fields, with an explicit empty representation &mdash; and normalise shape variation in a <code>PostToolUse</code> hook if the underlying service is inconsistent.",
k:"Keep tool output shape stable. Use a consistent envelope with an explicit empty case; normalise variation in a hook."},

{i:349,d:2,t:"2.1",s:"DPT",diff:2,
q:"An agent must choose between <code>search_docs</code> (semantic search over documentation) and <code>Grep</code> (literal text search over code). It uses <code>Grep</code> for conceptual questions and <code>search_docs</code> for finding exact symbol names.",
ask:"What is the fix?",
a:["Sharpen both descriptions to state the query type each expects &mdash; conceptual questions versus literal strings &mdash; with example queries for each",
"Remove <code>Grep</code> so there is only one search tool",
"Combine both into a single <code>search</code> tool",
"Add a system prompt rule about which to use"],
c:0,
w:["Correct. The distinction is about query type, so the descriptions must state it with examples.",
"<code>Grep</code> is essential for code work; removing it breaks legitimate use cases.",
"Merging hides two genuinely different retrieval mechanisms behind one interface and makes selection worse.",
"A system prompt rule duplicates what belongs in the descriptions and competes with everything else in the prompt."],
e:"Selection errors between complementary tools are usually about the <em>shape of the input</em> rather than the domain. Say so directly: &ldquo;use <code>search_docs</code> for conceptual questions phrased in natural language, for example <em>how does retry backoff work</em>; use <code>Grep</code> for literal strings you expect to appear in the source, for example a function name or an error message&rdquo;. Example queries do more work here than any abstract description.",
k:"When tools differ by query type, say so with example queries for each. Input shape is often the real distinction."},

{i:350,d:2,t:"2.2",s:"AAT",diff:2,
q:"A tool returns <code>{\"isError\": true, \"errorCategory\": \"transient\", \"isRetryable\": true, \"message\": \"Upstream service returned 503; retry after a short delay.\"}</code>",
ask:"What should the agent do?",
a:["Retry the same call after a brief delay, within a bounded retry budget",
"Escalate to a human immediately",
"Try a different tool that might accomplish the same result",
"Report the failure to the user and end the turn"],
c:0,
w:["Correct. A transient, retryable failure is the one case where retrying the identical call is appropriate.",
"Premature &mdash; a transient failure usually resolves on retry, and escalating wastes human time.",
"Routing around a temporary outage adds risk when waiting briefly would work.",
"Giving up on the first transient failure produces poor reliability."],
e:"This is the payoff of good error design: the flags make the recovery obvious. Transient plus retryable means retry, with backoff and a bounded budget. When the budget is exhausted, the situation changes category &mdash; the failure is now effectively persistent, so report it with structured context and escalate or degrade gracefully rather than retrying forever.",
k:"Transient + retryable = retry with backoff, bounded. When the budget is exhausted, treat it as persistent and change strategy."},

{i:351,d:2,t:"2.2",s:"SDE",diff:2,
q:"Your tool wraps a third-party API whose error messages are inconsistent and sometimes leak internal hostnames.",
ask:"What should your tool do?",
a:["Map upstream errors into your own structured error contract with clean, actionable messages",
"Pass the upstream error through unchanged for maximum fidelity",
"Suppress all upstream errors and return a generic failure",
"Log the upstream error and return success"],
c:0,
w:["Correct. Translating at your boundary gives callers a consistent contract and removes the disclosure risk.",
"Pass-through propagates inconsistency and leaks internal detail into the model's context and possibly to users.",
"A generic failure is the anti-pattern that prevents correct recovery decisions.",
"Reporting failure as success is the most dangerous option."],
e:"Your tool is an adapter, and part of its job is to present a consistent contract regardless of upstream messiness. Map each upstream condition to a category and a retryability flag, write a clean message suited to the model and the eventual user, and keep the raw upstream detail in your logs behind a correlation id. This also insulates your agents from upstream error-format changes.",
k:"Translate upstream errors into your own contract: category, retryability, clean message. Raw detail goes to logs, not context."},

{i:352,d:2,t:"2.2",s:"MAR",diff:3,
q:"A subagent hits a paywall on a source. It returns <code>{\"isError\": true, \"errorCategory\": \"permission\", \"isRetryable\": false, \"message\": \"Source requires a subscription; content not accessible.\", \"partial\": {\"title\": \"...\", \"abstract\": \"...\"}}</code>",
ask:"Why is this a strong response?",
a:["It classifies the failure correctly, marks it non-retryable, explains the cause, and preserves the partial content that was accessible",
"It is concise enough to save context",
"It hides the fact that a source was unavailable",
"It allows the coordinator to retry with different credentials automatically"],
c:0,
w:["Correct. Category, retryability, explanation and partial results together give the coordinator everything it needs.",
"Brevity is incidental; the structure and the preserved partial content are the value.",
"It does the opposite &mdash; it makes the limitation explicit so it can be annotated.",
"Nothing here implies automatic credential switching; the coordinator decides what to do next."],
e:"Note the inclusion of <code>partial</code>. A paywalled source often still yields a title and abstract, which may be enough for the coordinator to decide whether to seek an alternative route to the full text or to proceed and annotate the gap. Preserving what was salvaged is a recurring theme &mdash; failures should carry forward whatever value was obtained, not discard it.",
k:"Good errors preserve partial results. Category + retryability + explanation + what was salvaged."},

{i:353,d:2,t:"2.3",s:"AAT",diff:2,
q:"A single agent holds tools for reading files, sending emails, querying a database, deploying to production and posting to Slack.",
ask:"What is the primary concern?",
a:["The toolset spans unrelated domains and includes high-blast-radius operations, which harms both selection reliability and safety",
"Five tools is too few for a useful agent",
"Slack and email are redundant and one should be removed",
"Deployment tools require a different model"],
c:0,
w:["Correct. Both problems follow from the same over-broad grant: unrelated domains and an irreversible production capability.",
"Five is a reasonable count &mdash; the problem is which five.",
"They serve different audiences and purposes; redundancy is not the issue.",
"Model choice is unrelated to capability scoping."],
e:"Two independent objections converge. Reliability: unrelated domains mean the agent holds tools outside its specialisation, which the blueprint says leads to misuse. Safety: production deployment is irreversible and high-blast-radius, so it should sit behind a specialised agent with a human-approval gate rather than alongside routine read operations. Scope by role, and gate irreversible actions separately.",
k:"Scope tools by role and separate irreversible, high-blast-radius operations behind their own agent and approval gate."},

{i:354,d:2,t:"2.3",s:"SDE",diff:2,
q:"You want the model to produce structured output on the first call and then reason about it conversationally.",
ask:"How should <code>tool_choice</code> vary across the two turns?",
a:["Force the extraction tool on turn one, then switch to <code>auto</code> for the conversational turn",
"Use <code>any</code> on both turns",
"Use <code>auto</code> on both turns",
"Use <code>none</code> on turn one and <code>any</code> on turn two"],
c:0,
w:["Correct. Guarantee the structure where you need it, then release the constraint so the conversation can proceed and terminate.",
"<code>any</code> on turn two forces another tool call and prevents a natural conclusion.",
"<code>auto</code> on turn one permits a text response, losing the structural guarantee.",
"<code>none</code> on turn one makes structured tool output impossible."],
e:"<code>tool_choice</code> is per-request, so treat it as a workflow control rather than a global setting. The common shape is a constrained opening turn &mdash; forced tool for one known schema, or <code>any</code> when several are valid &mdash; followed by <code>auto</code> for the remainder. Leaving <code>any</code> set across a loop is the classic mistake, because the loop can then never emit a final text answer.",
k:"Vary <code>tool_choice</code> per turn: constrain where you need a guarantee, then <code>auto</code> so the loop can finish."},

{i:355,d:2,t:"2.4",s:"DPT",diff:2,
q:"Your <code>.mcp.json</code> defines a server with <code>\"env\": {\"API_TOKEN\": \"${INTERNAL_API_TOKEN}\"}</code>. A developer reports the server fails to start.",
ask:"What should you check first?",
a:["Whether <code>INTERNAL_API_TOKEN</code> is set in that developer's environment",
"Whether the JSON syntax supports variable substitution",
"Whether the token needs to be committed to the repository",
"Whether the server should be moved to user scope"],
c:0,
w:["Correct. Substitution resolves from the environment, so an unset variable is the obvious first cause.",
"Substitution is supported &mdash; that is why the syntax is there.",
"Committing secrets is never the fix and would be a security incident.",
"Scope is unrelated to whether the variable resolves."],
e:"Environment variable substitution keeps secrets out of version control, but it introduces a setup step: each developer must have the variable set. Document that requirement clearly &mdash; in the README or CLAUDE.md &mdash; and make the failure legible where you can. This is the intended trade: a one-time setup step in exchange for never having a credential in the repository.",
k:"<code>${ENV_VAR}</code> in <code>.mcp.json</code> resolves from the developer's environment. Document the required variables."},

{i:356,d:2,t:"2.4",s:"DPT",diff:3,
q:"You are evaluating whether to expose an internal service to agents via a general MCP server that mirrors its full REST API, or via a narrower custom server exposing six specific operations.",
ask:"Which consideration most favours the narrower server?",
a:["A smaller, purpose-built surface improves tool-selection reliability and lets you enforce policy at the tool boundary",
"Narrow servers are always faster",
"General servers cannot use environment variable substitution",
"A narrow server requires no maintenance"],
c:0,
w:["Correct. Fewer, better-described tools select more reliably, and a custom server is where you can enforce internal policy.",
"Latency is not the deciding factor and is largely equivalent.",
"Substitution works regardless of how many tools a server exposes.",
"Custom servers require more maintenance, not less &mdash; that is the cost you are trading against."],
e:"Prefer community servers for standard integrations, but a narrow custom server earns its maintenance cost when you need a deliberately smaller surface or policy enforcement at the boundary. Mirroring a full REST API typically produces dozens of tools, which degrades selection for every agent that can see them, and gives you no place to enforce rules like tenant scoping or write restrictions.",
k:"Custom MCP servers are justified by a deliberately narrower surface and boundary-level policy enforcement &mdash; not by preference."},

{i:357,d:2,t:"2.5",s:"DPT",diff:2,
q:"You need to read only lines 400&ndash;450 of a 12,000-line file.",
ask:"What is the appropriate approach?",
a:["Use <code>Read</code> with an offset and limit so only the needed range enters the context",
"Read the whole file and let the model find the relevant part",
"Use <code>Grep</code> to search for content near line 400",
"Use <code>Bash</code> with <code>sed</code> to extract the range"],
c:0,
w:["Correct. Targeted reads keep context spend proportional to what you actually need.",
"Reading 12,000 lines to use 50 wastes a large amount of context.",
"<code>Grep</code> searches by content, not by line number, and you already know the range.",
"Would work, but it reaches for shell capability when the purpose-built tool supports ranges directly."],
e:"Context discipline applies at the level of individual reads. When you know the range &mdash; from a stack trace, a grep hit, or a previous read &mdash; read only that range. This is the same principle as trimming verbose tool output: bring in what the task needs and leave the rest. Whole-file reads are appropriate when you genuinely need the whole file, not as a default.",
k:"Read targeted ranges with offset and limit. Whole-file reads are a choice, not a default."},

{i:358,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to run the project's test suite.",
ask:"Which approach is preferable if available?",
a:["A narrow purpose-built <code>run_tests</code> tool rather than granting general <code>Bash</code> access",
"<code>Bash</code>, since it is flexible and already available",
"<code>Read</code> the test files and reason about whether they would pass",
"<code>Write</code> a script and ask the developer to run it"],
c:0,
w:["Correct. A narrow tool grants exactly the needed capability without the arbitrary-execution surface of <code>Bash</code>.",
"<code>Bash</code> works but is the broadest possible grant &mdash; anything the shell can do.",
"Reasoning about test outcomes without executing them is unreliable and defeats the purpose of tests.",
"Pushing execution onto the developer defeats the automation."],
e:"Prefer the narrowest capability that accomplishes the task. A <code>run_tests</code> tool can also return structured results &mdash; pass and fail counts, failing test names, output excerpts &mdash; which is far easier for the agent to act on than raw shell output. Where <code>Bash</code> is genuinely necessary, constrain it with permission rules that allow-list specific commands, or intercept with a <code>PreToolUse</code> hook.",
k:"Prefer narrow purpose-built tools over <code>Bash</code>. They limit capability and return structured, actionable results."},

{i:359,d:2,t:"2.5",s:"DPT",diff:3,
q:"An agent needs to determine which of 300 configuration files contain a deprecated key.",
ask:"Which sequence is most efficient?",
a:["<code>Grep</code> for the key across the tree, then <code>Read</code> only the files that matched",
"<code>Glob</code> for all configuration files, then <code>Read</code> each one",
"<code>Read</code> every file in the configuration directory",
"<code>Bash</code> with a script that rewrites all 300 files at once"],
c:0,
w:["Correct. Content search narrows 300 candidates to the handful that matter before any expensive reads.",
"Finds the files but then reads all 300, which is the expensive part you were trying to avoid.",
"The same waste without even the benefit of a file list.",
"Modifying 300 files without first inspecting the matches is reckless and unreviewable."],
e:"The canonical pattern: cheap search narrows, expensive read confirms. <code>Grep</code> tells you which files contain the key and where; <code>Read</code> then confirms the context in just those files. Note the fourth option's separate problem &mdash; making a bulk modification before inspecting the matches produces an unreviewable change and risks altering occurrences that should have been left alone.",
k:"Search narrows, read confirms. Never bulk-modify before inspecting the matches."},

{i:360,d:2,t:"2.2",s:"CSA",diff:3,
q:"Your <code>process_refund</code> tool rejects a call because the session has no verified customer. Which error response best supports good agent behaviour?",
ask:"Which response is best?",
a:["<code>errorCategory: \"validation\"</code>, <code>isRetryable: false</code>, message stating that a verified customer is required and that <code>get_customer</code> must be called first",
"<code>errorCategory: \"transient\"</code>, <code>isRetryable: true</code>, message &ldquo;Please try again&rdquo;",
"<code>isError: true</code> with the message &ldquo;Precondition failed&rdquo;",
"Silent success with a flag in the response metadata"],
c:0,
w:["Correct. It names the missing precondition and the specific tool that satisfies it, so the agent can recover in one step.",
"Misclassifying a deterministic precondition failure as transient causes a retry loop that can never succeed.",
"Technically accurate but unactionable &mdash; the agent cannot tell which precondition or how to satisfy it.",
"Reporting a rejected refund as success is the most dangerous option."],
e:"A precondition failure is a validation error: not retryable unchanged, but recoverable once the agent takes the right action. Naming the required step (&ldquo;call <code>get_customer</code> and confirm the verified flag&rdquo;) turns a dead end into a one-step recovery. This is the error-design counterpart to enforcing the precondition in the first place &mdash; block deterministically, then tell the agent exactly how to proceed.",
k:"Precondition failures: validation category, non-retryable, and name the specific step that satisfies it."},

{i:361,d:3,t:"3.1",s:"CGC",diff:2,
q:"A team documents its API conventions in a wiki and expects Claude Code to follow them.",
ask:"What is the problem?",
a:["Claude Code reads CLAUDE.md and rules files, not the team wiki, so the conventions must be in the repository or reachable via a tool",
"Wikis cannot be version-controlled",
"The conventions need to be rewritten in a different style",
"Claude Code requires conventions to be in JSON format"],
c:0,
w:["Correct. Context has to be reachable &mdash; either in the repository configuration or through an MCP tool that can fetch it.",
"Many wikis are version-controlled; that is not the issue.",
"Style is not the barrier; accessibility is.",
"No format requirement of that kind exists."],
e:"Conventions only shape behaviour if they are in context. Two routes: put them in CLAUDE.md or <code>.claude/rules/</code> so they load automatically (ideally path-scoped so they load only when relevant), or expose the wiki through an MCP server so the agent can fetch them on demand. The first is better for durable conventions; the second suits large reference material that would bloat every request.",
k:"Conventions must be reachable: in CLAUDE.md / rules for durable ones, or via an MCP tool for large reference material."},

{i:362,d:3,t:"3.1",s:"CGC",diff:3,
q:"Your CLAUDE.md documents twelve formatting rules that your linter already enforces automatically.",
ask:"What should you do?",
a:["Remove them from CLAUDE.md and let the linter enforce them, freeing context for rules the linter cannot express",
"Keep them in both places for redundancy",
"Remove the linter and rely on CLAUDE.md",
"Move them to user-level CLAUDE.md"],
c:0,
w:["Correct. Deterministic tooling enforces mechanical rules better than instructions, and removing them reclaims context.",
"Redundancy costs context on every request and creates drift when the linter config changes.",
"Replacing deterministic enforcement with probabilistic guidance is a clear downgrade.",
"Moving them changes who pays the context cost without removing it."],
e:"Three tiers for conventions. <strong>Deterministic tooling</strong> (linters, formatters, type checkers) for mechanical rules &mdash; strongest and free at inference time. <strong>CLAUDE.md and rules</strong> for judgement-based conventions tooling cannot express, such as when to use a Result type or how to structure a service boundary. <strong>Hooks and permissions</strong> for hard constraints that must be guaranteed. Pushing rules down to the strongest applicable tier is how you keep CLAUDE.md small and effective.",
k:"Mechanical rules &rarr; linters. Judgement conventions &rarr; CLAUDE.md. Hard constraints &rarr; hooks. Keep CLAUDE.md for what only it can do."},

{i:363,d:3,t:"3.2",s:"CGC",diff:2,
q:"A slash command file in <code>.claude/commands/</code> contains a prompt with a placeholder for a file path, but developers keep invoking it without one.",
ask:"What would improve this?",
a:["Convert it to a skill and add an <code>argument-hint</code> so the required parameter is prompted for",
"Add a comment in the file explaining that a path is required",
"Rename the command to include the word PATH",
"Add a default path so it always runs"],
c:0,
w:["Correct. <code>argument-hint</code> exists specifically to prompt for required parameters.",
"A comment in the file is not shown at invocation time.",
"Naming conventions are a weak signal and do not prompt for anything.",
"A default path makes the command silently do the wrong thing rather than asking."],
e:"This is the practical difference between a slash command and a skill. A command is a reusable prompt; a skill adds declarative operational configuration &mdash; <code>argument-hint</code> for required parameters, <code>allowed-tools</code> for capability, <code>context: fork</code> for isolation. When invocation ergonomics or safety matter, escalate from a command to a skill.",
k:"Escalate a command to a skill when you need parameters prompted, tools restricted or context isolated."},

{i:364,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want a database-migration convention to apply only when files under <code>migrations/</code> are being edited.",
ask:"What is the correct configuration?",
a:["A <code>.claude/rules/migrations.md</code> file with <code>paths: [\"migrations/**/*\"]</code> in its YAML frontmatter",
"A section in the root CLAUDE.md titled &ldquo;Migrations&rdquo;",
"A skill named <code>/migrations</code>",
"A comment at the top of each migration file"],
c:0,
w:["Correct. Path frontmatter makes the rule load exactly when migration files are in play.",
"A titled section still loads on every request and relies on the model noticing it applies.",
"Skills are invoked manually; they do not activate automatically on file edits.",
"Per-file comments are not configuration and would need adding to every new migration."],
e:"Path-scoped rules give both correctness and efficiency: the convention cannot be misapplied to unrelated work, and it costs nothing on the many requests that touch no migrations. Compare with a directory-level CLAUDE.md, which would also work here since <code>migrations/</code> is a single subtree &mdash; the path rule is preferred when you also want the convention to follow migration files that might live elsewhere.",
k:"Path frontmatter scopes rules to file patterns. Prefer it when conventions follow file types rather than a single subtree."},

{i:365,d:3,t:"3.4",s:"CGC",diff:3,
q:"You are asked to add a feature that touches three files in a codebase you know well, with one obvious implementation.",
ask:"Which approach is appropriate?",
a:["Direct execution, with tests to verify",
"Planning mode, because three files counts as multi-file",
"An Explore subagent to map the three files first",
"A coordinator with three subagents, one per file"],
c:0,
w:["Correct. Familiar codebase, obvious implementation, small scope &mdash; direct execution.",
"File count alone is not the trigger; multiple viable approaches and architectural consequence are.",
"Exploration is unnecessary in code you already know.",
"Multi-agent orchestration for three coupled files is heavy over-engineering."],
e:"The planning-mode triggers are large scope, multiple defensible approaches, and architectural consequence &mdash; not a raw file count. Three coupled files with one obvious implementation in familiar code satisfies none of them. The exam includes several of these to test that you weigh the actual signals rather than reaching for the heavier process whenever more than one file is involved.",
k:"Planning triggers are uncertainty and consequence, not file count. Familiar code + obvious approach &rarr; direct execution."},

{i:366,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to communicate a non-obvious edge case: empty input should return an empty result, not an error.",
ask:"What is the most effective way?",
a:["Provide the input/output pair explicitly, and add it as a test case",
"Describe the behaviour in a sentence in the prompt",
"Add a note in CLAUDE.md about handling empty inputs generally",
"Ask the model to consider edge cases carefully"],
c:0,
w:["Correct. The example specifies the behaviour unambiguously and the test locks it in permanently.",
"A sentence is better than nothing but leaves room for interpretation and is not verified.",
"A general note does not specify what this particular function should do.",
"The canonical vague instruction; it does not say which edge case or what the correct behaviour is."],
e:"Concrete input/output examples are the most effective way to communicate expectations, and turning the example into a test converts the requirement into a verifiable goal that stays verified. This is the test-driven iteration pattern in miniature: state the expected behaviour as a case, make it pass, and keep it as a regression guard.",
k:"Edge cases: give the input/output pair and make it a test. Specification and verification in one artefact."},

{i:367,d:3,t:"3.6",s:"CCI",diff:2,
q:"A CI job must fail the build when Claude Code's review finds a critical issue.",
ask:"How should the pipeline determine this?",
a:["Request structured output with <code>--output-format json</code> and <code>--json-schema</code>, then branch on the severity field",
"Grep the output text for the word &ldquo;critical&rdquo;",
"Fail the build whenever the review produces any output",
"Ask Claude to exit with a non-zero status if it finds a critical issue"],
c:0,
w:["Correct. Structured output gives the pipeline a reliable field to branch on.",
"Text matching is brittle and produces false positives whenever the word appears incidentally.",
"Failing on any output makes the review useless as a gate.",
"Relying on the model to control the exit code is indirect and less reliable than reading a structured field."],
e:"This is the CLI analogue of using <code>tool_use</code> with a schema in the API: get a guaranteed shape rather than parsing prose. Define a schema with a findings array carrying severity, file and line, request it with <code>--json-schema</code>, and let the pipeline decide the build outcome from the data. That also gives you the fields needed to post inline PR comments.",
k:"Gate CI on structured fields, never on text matching. <code>--output-format json</code> + <code>--json-schema</code>, then branch on the data."},

{i:368,d:3,t:"3.6",s:"CCI",diff:3,
q:"Your CI review runs on a repository where different services have different review standards.",
ask:"How should the standards be supplied?",
a:["Path-scoped rules in <code>.claude/rules/</code> so each service's standards load when its files are in the diff",
"A single root CLAUDE.md containing every service's standards",
"A separate CI job per service, each with a different inline prompt",
"Ask reviewers to specify the standards in the PR description"],
c:0,
w:["Correct. Path scoping applies the right standards automatically based on which files changed.",
"All standards in every run dilutes attention and risks applying one service's rules to another.",
"Workable but duplicates configuration across jobs and drifts; the rules already express this cleanly.",
"Relies on humans remembering, and PR descriptions are not a configuration mechanism."],
e:"Path-scoped rules work identically in CI and locally, which is one of their strengths &mdash; the same configuration governs both, so a developer's local review and the CI review apply the same standards. Because the rules load based on the files in play, a diff touching two services automatically loads both sets without you having to orchestrate anything.",
k:"Path-scoped rules apply the right standards automatically in CI and locally, from one shared configuration."},

{i:369,d:3,t:"3.2",s:"CGC",diff:3,
q:"A skill produces 8,000 tokens of intermediate output but a 200-token conclusion. It is invoked several times per session.",
ask:"Which frontmatter setting matters most here?",
a:["<code>context: fork</code>, so each invocation's intermediate output stays in an isolated context",
"<code>allowed-tools</code>, to limit what the skill can call",
"<code>argument-hint</code>, to prompt for parameters",
"None &mdash; skills always run in isolated contexts by default"],
c:0,
w:["Correct. Repeated verbose output is precisely what <code>context: fork</code> is for.",
"Capability restriction is valuable but does not address the context accumulation described.",
"Parameter prompting is unrelated to the volume problem.",
"Isolation is opt-in via <code>context: fork</code>, not automatic."],
e:"Without forking, 8,000 tokens per invocation accumulate in the main session and crowd out the work the developer is actually doing. <code>context: fork</code> runs the skill in a subagent context so only the 200-token conclusion returns. This is the same context-isolation principle as delegating verbose discovery to a subagent, expressed declaratively so it applies every time the skill runs.",
k:"<code>context: fork</code> for any skill whose intermediate output dwarfs its conclusion. Isolation is opt-in, not automatic."},

{i:370,d:3,t:"3.1",s:"CGC",diff:2,
q:"Which content is most appropriate for a project CLAUDE.md?",
ask:"Which belongs there?",
a:["Team conventions the model would otherwise get wrong &mdash; error-handling style, testing standards, architectural boundaries",
"A full copy of the API reference documentation",
"Personal preferences about response verbosity",
"The commit history and release notes"],
c:0,
w:["Correct. CLAUDE.md carries decisions and conventions that shape how the agent works on this project.",
"Reference documentation is large and better fetched on demand via a tool or read from the repository when needed.",
"Personal preferences belong in user-level CLAUDE.md.",
"History is available from git and is not a convention."],
e:"CLAUDE.md is a context budget spent on every request, so it should hold what changes the model's decisions and nothing else. Conventions, boundaries, standards, and pointers to where things live. Large reference material belongs behind a tool or a file the agent reads when relevant. Personal preferences belong at user level. Mechanical rules belong in linters.",
k:"CLAUDE.md = decisions and conventions the model would otherwise get wrong. Not reference docs, not personal preferences, not history."},

{i:371,d:4,t:"4.1",s:"CSA",diff:2,
q:"Your agent's responses vary in structure &mdash; sometimes a summary first, sometimes the action taken first, sometimes just a confirmation.",
ask:"Which technique best standardises this?",
a:["Few-shot examples demonstrating the required response structure",
"An instruction to be consistent in response structure",
"A lower temperature setting",
"A longer system prompt describing the ideal response"],
c:0,
w:["Correct. Few-shot examples are the most effective method for consistently formatted output.",
"An instruction to be consistent supplies no template to be consistent against.",
"Temperature reduces phrasing variance but does not establish a structure.",
"Prose descriptions of structure leave interpretation room that examples close."],
e:"Format consistency is the archetypal few-shot use case. Two to three examples showing the exact structure &mdash; what comes first, how actions are reported, how the close is phrased &mdash; communicate more precisely than any description. Where the structure must be machine-parseable rather than merely consistent, escalate to <code>tool_use</code> with a JSON Schema for a structural guarantee.",
k:"Format consistency &rarr; few-shot examples. Machine-parseable structure &rarr; <code>tool_use</code> + JSON Schema."},

{i:372,d:4,t:"4.3",s:"SDE",diff:2,
q:"Your schema uses <code>\"type\": \"string\"</code> for an amount field, and downstream code performs arithmetic on it.",
ask:"What should change?",
a:["Declare it as a number in the schema so the type is enforced and no parsing ambiguity arises",
"Keep it as a string and parse defensively downstream",
"Add a description telling the model to format the string as a number",
"Add a separate boolean field indicating whether the string is numeric"],
c:0,
w:["Correct. Declaring the correct type is what the schema is for, and it eliminates a whole class of parsing errors.",
"Defensive parsing handles the symptom and still fails on currency symbols, thousands separators and locale differences.",
"A description is guidance; the type declaration is enforcement.",
"An extra flag adds complexity without fixing the underlying type mismatch."],
e:"Schema design is where you convert requirements into guarantees. Declaring a numeric field as a number means the model must produce a number, eliminating currency symbols, thousands separators and locale ambiguity at the source. Use the schema's expressive features fully &mdash; types, <code>enum</code>, <code>format</code>, ranges, <code>required</code> &mdash; because every constraint you express is one your code no longer has to defend against.",
k:"Declare types precisely and use <code>enum</code>, <code>format</code> and ranges. Every schema constraint is one less defensive check downstream."},

{i:373,d:4,t:"4.4",s:"SDE",diff:2,
q:"Your retry loop passes only the validation error message back, without the original document.",
ask:"What is the likely outcome?",
a:["The model cannot re-derive the correct value because it no longer has the source, so the retry is unlikely to succeed",
"The retry will succeed because the error message is sufficient",
"The retry will fail with a schema validation error",
"The model will ask for the document, adding one round trip"],
c:0,
w:["Correct. Correction requires the source; without it the model can only guess at a value that satisfies the error.",
"Knowing that a total does not reconcile does not tell you what the correct total is.",
"The output may well be schema-valid and still wrong.",
"It is more likely to fabricate a plausible correction than to ask &mdash; which is the dangerous outcome."],
e:"The retry payload has three parts for three reasons: the <strong>original document</strong> so the model can re-derive the correct values; the <strong>previous extraction</strong> so it can see what it produced; and the <strong>specific validation errors</strong> so it knows what to change. Drop any one and the retry degrades &mdash; dropping the source is the worst, because it invites a fabricated value that satisfies the validator.",
k:"Retry needs all three: source + previous attempt + specific errors. Without the source, corrections become fabrications."},

{i:374,d:4,t:"4.5",s:"SDE",diff:2,
q:"You need results for 200 documents within five minutes to display in a user-facing dashboard refresh.",
ask:"Which approach is correct?",
a:["The synchronous Messages API with controlled concurrency",
"The Message Batches API for the cost saving",
"The Batch API with a five-minute submission cadence",
"Streaming responses from the Batch API"],
c:0,
w:["Correct. A five-minute user-facing deadline requires bounded latency, which only the synchronous API provides.",
"Batch has no latency SLA; results may take hours.",
"A short submission cadence does not shorten the processing window.",
"Batch does not provide streaming of the kind that would meet this deadline."],
e:"The decision rule is whether anything is blocked on the result. A dashboard refresh a user is waiting for is blocked, so it is synchronous regardless of cost. Batch is for overnight reports, audits and backfills where the deadline is generous. The 50% saving is real but it buys latency uncertainty, and you cannot spend that on a five-minute user-facing deadline.",
k:"Anything user-facing or blocking &rarr; synchronous. Batch trades bounded latency for ~50% cost."},

{i:375,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want two independent perspectives on the same code change to reduce missed defects.",
ask:"Which setup provides genuine independence?",
a:["Two separate invocations, each seeing only the diff and the standards, with no shared context",
"One invocation asked to produce two independent reviews",
"Two sequential reviews in the same session",
"One review, run twice, with the second told to disagree with the first"],
c:0,
w:["Correct. Separate contexts on identical inputs is the only configuration with no shared reasoning to contaminate them.",
"A single context means the second review anchors on the first.",
"The second review sees the first in context, so the perspectives correlate.",
"Instructing disagreement produces contrarianism, not independent judgement."],
e:"Independence requires separate contexts, not merely separate instructions. This is the same principle as not reviewing in the generating session, applied to obtaining a second opinion. If you want genuinely different perspectives rather than merely uncorrelated ones, vary the review focus deliberately &mdash; one pass for security, one for correctness &mdash; rather than asking the same question twice.",
k:"Independence = separate contexts, same inputs. Instructing disagreement produces contrarianism, not judgement."},

{i:376,d:4,t:"4.2",s:"MAR",diff:2,
q:"Your extraction works well on one document layout but poorly on a second, structurally different one.",
ask:"Which few-shot strategy helps most?",
a:["Include examples of correct extraction from documents with different structures, so the model generalises across layouts",
"Add more examples from the layout that already works",
"Remove all examples so the model is not biased toward one layout",
"Add a longer description of the second layout"],
c:0,
w:["Correct. The blueprint prescribes examples of correct extraction from documents with different structures.",
"Reinforces the layout that is already handled and may deepen the bias against the other.",
"Removing examples loses the consistency benefit without addressing generalisation.",
"A description of the layout is weaker than an example of extracting from it."],
e:"Few-shot examples should span the variation you expect to encounter. Examples drawn from only one layout teach the model that layout's positional conventions, which actively hurts on a structurally different document. The blueprint also notes that few-shot helps the model generalise to new patterns rather than merely repeating defaults &mdash; which only holds if the examples themselves show variation.",
k:"Few-shot examples must span the input variation. Single-layout examples teach positional habits that break on other layouts."},

{i:377,d:4,t:"4.1",s:"CCI",diff:3,
q:"You want to reduce false positives without losing genuine findings.",
ask:"Which approach is most principled?",
a:["Define explicit categorical criteria for what qualifies, measure precision per category, and refine the categories that underperform",
"Instruct the model to report only findings it is highly confident about",
"Report only the single highest-severity finding per file",
"Raise the severity threshold for reporting"],
c:0,
w:["Correct. Define the criteria, measure per category, and improve the specific categories that are noisy.",
"Uncalibrated self-assessed confidence is not a reliable filter and suppresses genuine findings too.",
"Arbitrary and loses real defects that happen to rank second.",
"Threshold changes trade recall for precision uniformly rather than fixing the categories that are actually wrong."],
e:"Precision improves by sharpening definitions, not by suppressing output. Measure per category so you can see which ones are noisy &mdash; recording a <code>detected_pattern</code> on each finding is what makes that grouping possible &mdash; then rewrite those categories with explicit inclusion and exclusion criteria and contrastive examples. Suppression techniques reduce false positives and true positives together.",
k:"Improve precision by sharpening criteria per category, measured with <code>detected_pattern</code>. Suppression costs recall equally."},

{i:378,d:4,t:"4.3",s:"AAT",diff:2,
q:"You define a tool purely to obtain structured output; nothing actually executes when it is called.",
ask:"Is this valid?",
a:["Yes &mdash; the structured object arrives as <code>tool_use.input</code> and you simply read it without executing anything",
"No &mdash; every defined tool must have an implementation that returns a <code>tool_result</code>",
"No &mdash; the API rejects tools that are never executed",
"Yes, but only when <code>tool_choice</code> is set to <code>auto</code>"],
c:0,
w:["Correct. This is the standard structured-output pattern: the arguments the model constructs <em>are</em> the output.",
"For a single-shot extraction you never continue the conversation, so no <code>tool_result</code> is required.",
"The API does not track whether tools are executed.",
"It works with a forced tool or with <code>any</code>; <code>auto</code> would actually weaken the guarantee."],
e:"The structured-output pattern repurposes tool calling for a different job. Define a tool whose <code>input_schema</code> is your target schema, force it with <code>tool_choice</code>, read the object from <code>tool_use.input</code>, and stop. The only caveat is that if you <em>do</em> continue the conversation, every <code>tool_use</code> block must receive a matching <code>tool_result</code> in the next user message.",
k:"Structured output = a tool you never execute. Read <code>tool_use.input</code>. Only continue the conversation if you return a <code>tool_result</code>."},

{i:379,d:4,t:"4.4",s:"SDE",diff:3,
q:"An extraction returns a date of 2087-03-14 for an invoice issued last month.",
ask:"Which validation layer should catch this?",
a:["Domain validation in code, checking the date falls within a plausible range for the document type",
"The JSON Schema's <code>format: \"date\"</code> constraint",
"The model's own review of its output",
"A downstream analytics query that flags outliers"],
c:0,
w:["Correct. Plausibility is domain logic that no structural schema can express.",
"<code>format: \"date\"</code> validates that it is a well-formed date; 2087-03-14 is perfectly well formed.",
"Self-review is unreliable and produces no artefact your pipeline can act on.",
"Catches it after the bad data has been persisted and possibly consumed."],
e:"A clean illustration of the syntax/semantics boundary. The schema guarantees a well-formed date; only domain logic knows that an invoice date sixty years in the future is implausible. Build a validation layer that encodes those domain rules &mdash; plausible ranges, cross-field consistency, referential checks &mdash; and feed any failure into the retry loop with the specific problem stated, so the retry is guided rather than blind.",
k:"Schemas check form, domain validation checks plausibility. Range and cross-field rules belong in your code."},

{i:380,d:4,t:"4.5",s:"SDE",diff:2,
q:"You submit a batch and want to know which documents produced which results.",
ask:"What must you do at submission time?",
a:["Assign each request a meaningful <code>custom_id</code>, such as the document id",
"Record the submission order and rely on results returning in the same order",
"Include the document id in the prompt text and parse it from the response",
"Submit one document per batch so correlation is trivial"],
c:0,
w:["Correct. <code>custom_id</code> is the intended correlation mechanism and should carry an id meaningful in your system.",
"Result order is not guaranteed, so positional correlation is unsafe.",
"Round-tripping an id through the model's output is fragile and pointlessly indirect.",
"Single-document batches forfeit the efficiency of batching entirely."],
e:"Assign <code>custom_id</code> at submission using an identifier that is meaningful in your own system &mdash; the document id, not a sequence number &mdash; so correlation on the way back is trivial and so partial-failure recovery is straightforward. When some items fail, you re-submit exactly those <code>custom_id</code>s rather than reprocessing the whole batch.",
k:"Set <code>custom_id</code> to a meaningful system identifier at submission. It drives both correlation and partial re-submission."},

{i:381,d:5,t:"5.1",s:"CAP",diff:3,
q:"A conversational agent must remember, across a long session, that the user is a non-technical stakeholder and wants plain-language explanations.",
ask:"Where should this live?",
a:["In a persistent preferences block or the system prompt, re-asserted on every request",
"In the first user message, relying on the full history being sent",
"In a scratchpad file the agent reads when it remembers to",
"In a note the agent appends to each of its own responses"],
c:0,
w:["Correct. A durable preference needs a stable, privileged position that does not compete with intervening turns.",
"An instruction in turn one loses salience as the conversation grows &mdash; the lost-in-the-middle effect applied to instructions.",
"A file the agent reads only when it remembers is exactly as unreliable as remembering.",
"Self-appended notes inflate every response and are themselves subject to drift."],
e:"Distinguish preferences (durable, apply to the whole session) from requests (situational, apply to one turn). Durable preferences belong in the system prompt or in a persistent block your code maintains and re-injects &mdash; the same mechanism as the case-facts block for transactional details. Situational requests stay in the conversation where they belong.",
k:"Durable preferences &rarr; system prompt or persistent injected block. Situational requests stay in the conversation."},

{i:382,d:5,t:"5.2",s:"CAP",diff:3,
q:"A user says: &ldquo;Set my notification preference to daily &mdash; actually make it weekly &mdash; no, daily is fine.&rdquo;",
ask:"How should the agent handle this?",
a:["Apply the final stated intent (daily) and confirm it back to the user",
"Ask the user to clarify, since they changed their mind twice",
"Apply the first stated intent, since it was the original request",
"Apply weekly, as the compromise between the two"],
c:0,
w:["Correct. Self-corrections resolve to the latest stated intent, and confirming closes the loop safely.",
"The final intent is unambiguous; asking adds friction for no benefit.",
"Ignores two subsequent corrections.",
"Inventing a compromise the user did not ask for."],
e:"Distinguish genuine ambiguity from a self-correcting sequence. Multiple customer records matching a name is genuine ambiguity requiring a clarifying question. A user revising their own instruction within one message is not ambiguous &mdash; the final statement governs. Confirming the applied value back is cheap insurance and is good practice for any state change.",
k:"Self-corrections resolve to the last stated intent; confirm the result. Reserve clarifying questions for genuine ambiguity."},

{i:383,d:5,t:"5.3",s:"AAT",diff:2,
q:"A coordinator receives <code>{\"status\": \"error\"}</code> from a subagent with no further detail.",
ask:"What can it reasonably do?",
a:["Very little &mdash; it cannot distinguish transient from permanent failure, so any recovery choice is a guess",
"Retry three times, which is the correct default for any error",
"Escalate to a human, which is always safe",
"Proceed to synthesis, since one subagent's failure is not significant"],
c:0,
w:["Correct. This is precisely why generic errors are an anti-pattern &mdash; they leave the caller unable to choose.",
"A category-blind retry wastes attempts on deterministic failures.",
"Escalating every subagent hiccup to a human is not sustainable and is often unnecessary.",
"Proceeding silently produces an unannotated gap in the output."],
e:"The question tests whether you can see the consequence of a bad error contract from the caller's side. With no category, no retryability flag, no description of what was attempted and no partial results, every recovery option is a coin flip. The fix belongs upstream: the subagent must return structured failure context. Nothing the coordinator does can compensate for information that was never sent.",
k:"Generic errors leave the caller unable to choose a recovery. The fix is upstream, in the error contract."},

{i:384,d:5,t:"5.4",s:"DPT",diff:2,
q:"You want an investigation's findings to survive a crash and be usable by a colleague.",
ask:"Which mechanism is appropriate?",
a:["A scratchpad or findings file committed or stored outside the session",
"The session transcript, resumable later",
"A long final summary message at the end of the session",
"Relying on the agent to recall the findings in a future session"],
c:0,
w:["Correct. A file survives process death and is readable by anyone, which is exactly the requirement.",
"Transcripts are personal, lossy under compaction, and a colleague cannot resume your session.",
"A summary produced at the end does not exist if the crash happens before it.",
"There is no cross-session memory."],
e:"Two requirements are stated &mdash; crash survival and shareability &mdash; and only an externalised file satisfies both. Write findings as you go rather than at the end, since a crash does not wait for your summary. Include exact file paths, measured values, decisions taken and open questions, which makes the file useful both as a recovery artefact and as a handover document.",
k:"Externalised files survive crashes and transfer to colleagues. Write as you go; transcripts do neither."},

{i:385,d:5,t:"5.5",s:"SDE",diff:2,
q:"Your team proposes automating approval for all extractions once overall accuracy passes 95%.",
ask:"What should you push back on?",
a:["Automating on an aggregate figure, without validating accuracy by document type and by field first",
"The 95% threshold, which should be 99%",
"Automating at all, since human review is always safer",
"Measuring accuracy, since it is unreliable at scale"],
c:0,
w:["Correct. Aggregate metrics mask segment-level failure; validate by type and field before automating.",
"Moving an aggregate threshold does not address the masking problem.",
"Full manual review forfeits the automation entirely and is not the stated concern.",
"Accuracy is measurable; the issue is that the aggregate is the wrong granularity."],
e:"The blueprint's warning is specifically about granularity: a pipeline can hit 95% overall while being far worse on one document type or one field, and volume weighting hides it. Validate by type and by field, automate only the segments that meet the bar, route the rest to human review, and keep sampling the automated stream to catch drift.",
k:"Never automate on an aggregate. Validate by document type and field, automate the qualifying segments, sample the rest."},

{i:386,d:5,t:"5.6",s:"MAR",diff:2,
q:"A subagent returns a finding with a claim and a source URL but no supporting quote.",
ask:"Why does the quote matter?",
a:["It lets a reader or reviewer verify that the source actually supports the claim, rather than trusting that it does",
"It reduces the number of tokens needed to represent the finding",
"It makes the report longer and therefore more authoritative",
"It is required by the MCP specification"],
c:0,
w:["Correct. A URL proves a source was consulted; a quote proves what it actually said.",
"A quote adds tokens rather than saving them.",
"Length is not authority.",
"MCP has no such requirement; this is a design practice."],
e:"The claim-to-source mapping is only as good as its weakest link. A URL alone is unverifiable in practice &mdash; nobody follows forty links to check &mdash; and it does not protect against the source having been misread. The quote makes verification local and cheap. Combined with the date, it also lets a reader judge whether an apparent conflict between two sources is temporal rather than substantive.",
k:"Findings carry claim + source + supporting quote + date. A URL proves consultation; a quote proves what was said."},

{i:387,d:5,t:"5.1",s:"AAT",diff:2,
q:"An agent's context is filling with repeated near-identical tool results from polling a status endpoint.",
ask:"What is the cleanest remedy?",
a:["Return only the change since the last poll, or collapse repeated identical results in a hook before they reach the context",
"Increase the context window",
"Poll less frequently and accept slower detection",
"Summarise the conversation after every poll"],
c:0,
w:["Correct. Suppressing redundancy at the boundary keeps the signal and drops the noise.",
"Accommodates the waste; the same polling loop refills any window.",
"A real trade-off but it degrades responsiveness to fix a representation problem.",
"Summarising after every poll is expensive and compounds loss with each pass."],
e:"Redundant tool output is the same class of problem as over-verbose tool output, and it has the same remedy: fix it at the boundary in code. Return deltas rather than full snapshots, or collapse consecutive identical results in a <code>PostToolUse</code> hook so the context records &ldquo;status unchanged for 12 polls&rdquo; rather than twelve identical payloads.",
k:"Collapse redundant tool results at the boundary. Return deltas, or dedupe in a hook &mdash; do not pay for repetition."},

{i:388,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent cannot determine whether a customer's request falls inside policy because the policy language is genuinely ambiguous.",
ask:"What should it do?",
a:["Escalate, since ambiguous policy is outside the agent's authority to interpret",
"Choose the interpretation most favourable to the customer",
"Choose the interpretation most favourable to the business",
"Ask the customer which interpretation they prefer"],
c:0,
w:["Correct. The blueprint names ambiguous or silent policy as an escalation trigger.",
"Interpreting ambiguity in the customer's favour is still setting policy without authority.",
"The same problem in the other direction.",
"Asking the customer to interpret the company's own policy is not a resolution and invites disputes."],
e:"Note the parallel with the policy-gap case. The distinction the exam draws is between ambiguity the agent can resolve within its scope &mdash; multiple customer records, a conditional instruction &mdash; and ambiguity that concerns the boundaries of its own authority. The second always escalates, with a structured handoff describing the ambiguity so the human can rule on it and, ideally, close the gap in the policy.",
k:"Ambiguity about facts &rarr; ask and resolve. Ambiguity about policy or authority &rarr; escalate."},

{i:389,d:5,t:"5.3",s:"MAR",diff:2,
q:"Three of five subagents succeed, one returns partial results with a documented failure, and one times out with no context.",
ask:"What should the coordinator produce?",
a:["A result built from what succeeded, incorporating the partial results, with explicit coverage annotation naming both gaps",
"Nothing, since the workflow was not fully successful",
"A result from the three successes, silently omitting the other two",
"A retry of all five subagents from scratch"],
c:0,
w:["Correct. Use everything obtained, and make the two gaps visible with their reasons.",
"Aborting on partial failure is the named anti-pattern and discards three successful results.",
"Silent omission means the reader cannot tell the report is incomplete.",
"Re-running the three that succeeded wastes their work and may not fix the two that failed."],
e:"This exercises the whole error-handling stack at once: partial results are used rather than discarded, gaps are annotated rather than hidden, and neither anti-pattern (silent suppression, whole-workflow abort) is committed. Note the asymmetry between the two failures &mdash; the documented partial failure can be annotated with its reason, while the context-free timeout can only be reported as unexplained, which is itself an argument for better error contracts.",
k:"Use what succeeded, incorporate partials, annotate every gap with its reason. Never abort wholesale, never omit silently."},

{i:390,d:5,t:"5.4",s:"CGC",diff:2,
q:"A colleague suggests avoiding context problems by always starting a new session for each question.",
ask:"What is the trade-off?",
a:["Fresh sessions avoid degradation but discard accumulated understanding, so each question pays the full cost of rebuilding context",
"There is no trade-off; fresh sessions are always better",
"Fresh sessions are slower to start and that is the only cost",
"Fresh sessions cannot use CLAUDE.md or MCP servers"],
c:0,
w:["Correct. The trade is between context freshness and the cost of re-establishing understanding.",
"Rebuilding twenty minutes of codebase understanding for every question is expensive and error-prone.",
"Startup time is trivial; the real cost is the lost context.",
"Both load normally in any session."],
e:"The three session mechanisms sit at different points on this trade-off. <code>--resume</code> keeps everything, which is right when the context is still valid. <code>fork_session</code> keeps the shared prefix and diverges, which is right for comparing alternatives. A fresh session seeded with a structured summary keeps the distilled understanding without the accumulated noise &mdash; the middle path, and the right answer when context has gone stale.",
k:"Fresh sessions trade accumulated understanding for clean context. Seed them with a structured summary to keep both."},

{i:391,d:5,t:"5.5",s:"SDE",diff:3,
q:"Your validation set contains 500 documents, but 480 are typed invoices and 20 are handwritten forms.",
ask:"What is the problem with measuring accuracy on this set?",
a:["The set is unrepresentative of the segments you care about, so handwritten-form accuracy is measured on too few examples to be reliable",
"500 documents is too small a validation set overall",
"Validation sets should never contain more than one document type",
"Accuracy should be measured on production data only"],
c:0,
w:["Correct. Twenty examples gives a very wide confidence interval for the segment most likely to be problematic.",
"500 is reasonable in aggregate; the issue is the distribution within it.",
"Multiple types are exactly what you need in order to segment.",
"Production data is valuable, but the flaw described is the skewed composition."],
e:"Segmented measurement requires adequate samples in each segment. If handwritten forms are a segment you will make an automation decision about, you need enough of them to measure that decision reliably &mdash; which usually means deliberately over-sampling the rare and difficult segments relative to their production frequency, then weighting appropriately when you report an aggregate.",
k:"Segmented decisions need adequate samples per segment. Over-sample rare and difficult types in the validation set."},

{i:392,d:5,t:"5.6",s:"MAR",diff:3,
q:"Two sources report different values because they measure different populations &mdash; one national, one urban only.",
ask:"How should this be represented?",
a:["Preserve both values with their definitions and scopes stated, so the difference reads as a scope distinction rather than a conflict",
"Average the two values",
"Choose the national figure as more comprehensive",
"Report them as conflicting and flag for reconciliation"],
c:0,
w:["Correct. Capturing definitions and scope alongside the values resolves the apparent conflict correctly.",
"Averaging two figures measuring different populations produces a meaningless number.",
"Discards a genuinely useful urban figure and hides that two scopes exist.",
"They do not actually conflict &mdash; labelling them as conflicting misrepresents the evidence."],
e:"Not every difference is a disagreement. Alongside dates, definitions and population scope are the two most common explanations for apparent conflicts, which is why finding records should carry them. Capturing them turns what looks like a contradiction into two complementary data points, and prevents a reconciliation exercise that has nothing to reconcile.",
k:"Capture definitions and scope with every figure. Many apparent conflicts are differences in date, definition or population."},

{i:393,d:5,t:"5.1",s:"CSA",diff:2,
q:"Which items belong in a persistent case-facts block for a support conversation?",
ask:"Which set is most appropriate?",
a:["Verified customer id, order number, disputed amount, transaction date, and decisions already made",
"The full transcript of every turn so far",
"The agent's system prompt",
"A summary of the customer's emotional state"],
c:0,
w:["Correct. Compact, exact, load-bearing facts that must not degrade &mdash; the definition of a case-facts block.",
"That is the conversation history, which is the thing the block exists to protect against.",
"The system prompt is sent separately and is already stable.",
"Sentiment is an unreliable signal and is not a load-bearing fact."],
e:"The case-facts block is small by design: identifiers, amounts, dates and decisions &mdash; the things that must be exactly right and that summarisation destroys first. Your code maintains it, updating it as facts are established, and re-injects it in a stable position on every request. Everything else can live in the summarised history without harm.",
k:"Case-facts block: identifiers, amounts, dates, decisions. Small, exact, code-maintained, re-injected every request."},

{i:394,d:5,t:"5.3",s:"AAT",diff:3,
q:"You are designing what a subagent returns when it partially succeeds.",
ask:"Which structure is best?",
a:["An explicit partial status, the results obtained, and a list of what failed with reasons and retryability",
"A success status with the partial results, since some work completed",
"An error status with no results, since the task was not fully completed",
"A success status with placeholder values filling the gaps"],
c:0,
w:["Correct. An explicit partial status with both halves documented is what lets the coordinator decide correctly.",
"Reporting partial as full success hides the gap &mdash; the silent-suppression anti-pattern.",
"Discarding obtained results wastes work and gives the coordinator less to proceed with.",
"Placeholders are fabrication and corrupt everything downstream."],
e:"Partial success is a first-class outcome in agentic systems and deserves its own status. Report what succeeded, what failed, why each failure occurred, and whether retrying is worthwhile. That structure feeds directly into the coordinator's recovery decision and into the coverage annotation that appears in the final output, so a reader can see exactly where the evidence is thin.",
k:"Partial success is its own status: results obtained + failures with reasons and retryability. Never success, never bare error."},

{i:395,d:5,t:"5.4",s:"DPT",diff:3,
q:"You are about to spawn phase-two subagents after a long phase-one investigation.",
ask:"What should the delegation prompts contain?",
a:["A compact summary of phase-one findings preserving exact paths and values, plus each subagent's specific task and output contract",
"The complete raw transcript of phase one",
"A reference to phase one, since the subagents can look it up",
"Nothing about phase one, so the subagents form independent views"],
c:0,
w:["Correct. Preserve the load-bearing specifics, compress the narrative, and state each task and its contract.",
"Raw transcripts are enormous and bury the relevant findings in exploration noise.",
"Subagents inherit no context and cannot follow a reference to something they never received.",
"Discarding phase one means phase two repeats work and may contradict established findings."],
e:"This balances two pressures the exam raises repeatedly. Summarise to control context, but preserve exactly the file paths, measured values and source attributions that phase two depends on &mdash; compress the narrative, never the specifics. And because subagents inherit nothing, everything they need must be stated in the prompt, along with the task and the expected output shape.",
k:"Between phases: compress narrative, preserve specifics, state the task and the output contract. Subagents inherit nothing."}
);
