QB.push(
{i:791,d:2,t:"2.1",s:"CSA",diff:2,
q:"Your tool description says the tool &ldquo;handles customer requests&rdquo;.",
ask:"What is the core defect?",
a:["It describes a category so broad that it overlaps every other tool in the system, giving the model no discriminating signal",
"It is grammatically ambiguous",
"It is too short to be parsed",
"It uses the word customer, which is overloaded"],
c:0,
w:["Correct. A description that would fit any tool in a customer-support system discriminates nothing.",
"Grammar is not the issue.",
"Length is a symptom; breadth is the defect.",
"The specific vocabulary is not the problem."],
e:"A useful test: could this description also describe a different tool in the same system? If yes, it is not doing selection work. Every description should contain something that is true of this tool and false of its neighbours &mdash; the operation, the entity, the input format, the boundary. That discriminating content is what selection actually runs on.",
k:"Test every description: could it also describe a neighbouring tool? If yes, it discriminates nothing."},

{i:792,d:2,t:"2.1",s:"AAT",diff:2,
q:"You are adding a tool to a system that already has fifteen.",
ask:"What should you do as part of adding it?",
a:["Compare its description against its nearest neighbours and state boundaries on both sides, since selection is comparative",
"Add it and measure selection accuracy afterwards",
"Place it first in the tool list so it is noticed",
"Give it the shortest possible description to save tokens"],
c:0,
w:["Correct. Adding a tool perturbs its semantic neighbourhood, so boundaries must be stated on both sides.",
"Measuring afterwards is useful but reactive; the boundary work is cheap and prevents the regression.",
"List order is not a documented selection lever.",
"A short description is precisely what causes unreliable selection."],
e:"Selection is a comparison, so every addition changes the decision landscape for its neighbours &mdash; which is why accuracy can drop for existing tools when a new one arrives. Make boundary review part of adding a tool: identify the nearest one or two semantically, and ensure each description states when to prefer it and when to prefer the other.",
k:"Adding a tool perturbs its neighbours. Review boundaries on both sides as part of the addition."},

{i:793,d:2,t:"2.1",s:"SDE",diff:2,
q:"A tool's input schema is correct but its description does not mention a constraint the schema enforces.",
ask:"What is the consequence?",
a:["The model may attempt calls that fail validation, wasting turns on failures the description could have prevented",
"The schema will not be enforced",
"The API will reject the tool definition",
"There is no consequence; the schema is sufficient"],
c:0,
w:["Correct. The schema catches it, but only after a wasted turn each time.",
"Schema enforcement is unaffected by the description.",
"No validation of description completeness occurs.",
"It works, but inefficiently &mdash; and each failure costs a round trip."],
e:"Description and schema serve different moments. The description is read while the model decides <em>how to call</em>; the schema validates <em>after</em> it has decided. Stating constraints in both means the model gets them right the first time rather than discovering them through a rejection. This is the tool-level version of the layering principle: the schema guarantees, the description informs.",
k:"State constraints in the description as well as the schema. The schema catches errors; the description prevents them."},

{i:794,d:2,t:"2.1",s:"DPT",diff:2,
q:"A tool returns paginated results but the description does not mention pagination.",
ask:"What is likely to happen?",
a:["The agent treats the first page as the complete result set and draws conclusions from incomplete data",
"The agent will request all pages automatically",
"The tool call will fail",
"The agent will ask the user about pagination"],
c:0,
w:["Correct, and it fails silently &mdash; the answer looks complete and is not.",
"Nothing prompts it to paginate if it does not know pagination exists.",
"The call succeeds; it just returns partial data.",
"It has no reason to suspect there is more."],
e:"Silent incompleteness is among the more dangerous tool-design defects because nothing looks wrong. Document pagination in the description, and design the result to carry an explicit indicator &mdash; a <code>has_more</code> flag and a cursor &mdash; so the agent can see that more exists. This is the same principle as distinguishing an empty result from a failure: make the state explicit rather than inferable.",
k:"Document pagination and signal it in the result (<code>has_more</code>, cursor). Silent truncation looks like a complete answer."},

{i:795,d:2,t:"2.2",s:"AAT",diff:2,
q:"A tool wraps an operation that is slow but reliable. It times out at your client's default of 10 seconds.",
ask:"What should change, and what should accompany it?",
a:["Raise the timeout to match the operation's realistic duration, and classify a genuine timeout as transient with a retry budget",
"Leave the timeout and mark timeouts as non-retryable",
"Remove the timeout entirely",
"Retry immediately on every timeout without limit"],
c:0,
w:["Correct. Size the timeout to the operation, then classify and bound the genuine failures.",
"Marking a transient failure non-retryable forfeits a recovery that would usually work.",
"An unbounded wait converts a failure into a hang.",
"Unbounded immediate retry against a slow dependency makes things worse."],
e:"A timeout shorter than the operation manufactures failures that are not real, and those false timeouts then get classified as transient and retried &mdash; producing load on a service that was working fine. Size timeouts to the realistic duration first, so a timeout genuinely means something is wrong; then classify it as transient, retry with backoff, and bound the budget.",
k:"Size timeouts to the operation before classifying failures. A too-short timeout manufactures false transient errors."},

{i:796,d:2,t:"2.2",s:"SDE",diff:2,
q:"A tool can fail because the caller lacks permission, or because the requested record does not exist. It returns the same message for both.",
ask:"Why does this matter?",
a:["They demand different responses &mdash; escalate versus report a valid absence &mdash; and merging them also leaks whether a record exists",
"It matters only for logging",
"It does not matter; both are failures",
"It matters only if the record is sensitive"],
c:0,
w:["Correct. Different recoveries, and there is a genuine information-disclosure consideration too.",
"The agent's next action depends on the distinction, not just your logs.",
"Not-found is often a valid finding rather than a failure.",
"The recovery difference applies regardless of sensitivity."],
e:"Two considerations pull in opposite directions here. For recovery, the agent needs to distinguish &ldquo;you may not see this&rdquo; from &ldquo;this does not exist&rdquo;. For security, merging them is sometimes deliberate, to avoid revealing that a record exists to a caller who may not see it. Resolve it explicitly rather than by accident: decide whether the distinction is safe to expose, and if it is not, say so in the message so the agent escalates rather than reporting a false absence.",
k:"Permission-denied and not-found need different recoveries. If you merge them for security, say so, or the agent reports a false absence."},

{i:797,d:2,t:"2.2",s:"AAT",diff:3,
q:"You are designing what a tool returns when it partially succeeds and the caller is another agent rather than a human.",
ask:"What matters most about the format?",
a:["That the success and failure portions are machine-separable, so the calling agent can act on each without parsing prose",
"That it reads well in a transcript",
"That it is as short as possible",
"That it includes a stack trace"],
c:0,
w:["Correct. An agent consumer needs structure it can act on, not narrative.",
"Readability is secondary to actionability for an agent consumer.",
"Brevity is not the goal if it costs the structure.",
"Stack traces are internal detail with no recovery signal."],
e:"Design tool output for its actual consumer. When an agent is the caller, partial success needs explicit structure: which items succeeded, which failed, why each failed, whether each is retryable, and an aggregate status of <code>partial</code>. That lets the agent retry only the failures, proceed with what succeeded, and annotate the gap &mdash; the three behaviours partial results exist to enable.",
k:"For agent consumers, make partial success machine-separable: successes, failures with reasons, and a <code>partial</code> aggregate status."},

{i:798,d:2,t:"2.2",s:"MAR",diff:2,
q:"A subagent's tool fails and the subagent returns the raw tool error to the coordinator unchanged.",
ask:"Is this appropriate?",
a:["Only if the subagent genuinely could not recover; otherwise it should attempt local recovery first and add its own context about what it was trying to achieve",
"Yes, always &mdash; raw errors preserve maximum information",
"No, never &mdash; subagents should always translate errors",
"Yes, provided the error is well structured"],
c:0,
w:["Correct. Local recovery first, then propagate with the subagent's own context added.",
"Raw pass-through skips local recovery and omits the goal context the coordinator needs.",
"Too absolute &mdash; a well-structured error is worth preserving, with context added around it.",
"Structure helps, but the missing element is what the subagent was attempting."],
e:"The coordinator needs more than the tool's view of the failure. It needs to know what the subagent was trying to achieve, what it already attempted, what partial results it holds, and what alternatives exist. Preserving the structured tool error inside that context is ideal &mdash; but passing it through alone skips the local recovery step and strips the goal-level framing that makes the coordinator's decision possible.",
k:"Recover locally first, then propagate the structured error <em>wrapped in</em> the subagent's goal context and partial results."},

{i:799,d:2,t:"2.3",s:"AAT",diff:2,
q:"An agent's toolset is small and role-coherent, but two of its tools are almost never used.",
ask:"What should you consider?",
a:["Whether they belong to this role at all, since unused tools enlarge the choice space without contributing",
"Whether to force their use occasionally",
"Whether they need more capable implementations",
"Nothing &mdash; unused tools are harmless"],
c:0,
w:["Correct. Every option in the choice space affects selection for the others.",
"Forcing use of tools that are not needed is arbitrary.",
"Capability is irrelevant if the role never needs them.",
"They are not free; they enlarge the comparison the model makes on every turn."],
e:"Unused tools have a real cost: they consume input tokens on every request and they enlarge the comparison the model performs. Investigate before deleting, though &mdash; low usage can mean the role does not need them, or it can mean their descriptions fail to convey when they apply. Distinguishing those two is the same diagnostic as for rarely-chosen subagents.",
k:"Unused tools cost tokens and enlarge selection. Check whether the role needs them or the description fails."},

{i:800,d:2,t:"2.3",s:"CSA",diff:2,
q:"You are deciding whether a tool belongs to the triage agent or the billing specialist.",
ask:"What is the deciding question?",
a:["Which role's core responsibility the tool serves &mdash; and if it serves both, whether a narrow read-only version belongs to one and the full version to the other",
"Which agent is invoked more often",
"Which agent has fewer tools currently",
"Which agent was created first"],
c:0,
w:["Correct. Role coherence decides, with the narrow-utility split as the standard resolution for genuine overlap.",
"Invocation frequency is not a basis for capability assignment.",
"Balancing tool counts is arbitrary.",
"Creation order is irrelevant."],
e:"Assign by role coherence. Where a capability genuinely serves both, the standard resolution is asymmetric: the owning role gets the full tool including any write operations, and the other gets a narrow read-only utility. That preserves clear ownership and keeps blast radius where it belongs, while avoiding a mandatory round trip for a routine lookup.",
k:"Assign tools by role coherence. Genuine overlap resolves as full tool for the owner, narrow read-only utility for the other."},

{i:801,d:2,t:"2.4",s:"DPT",diff:2,
q:"A community MCP server is well-maintained but exposes operations your organisation's policy forbids.",
ask:"What is the appropriate response?",
a:["Withhold the forbidden tools per agent, or wrap the server to expose only the permitted surface, rather than relying on the agent avoiding them",
"Use it as-is and instruct agents not to call the forbidden operations",
"Avoid MCP servers entirely",
"Fork the server and delete the operations"],
c:0,
w:["Correct. Capability restriction in configuration, or a narrower wrapper, are both enforcement-level answers.",
"An instruction not to use a granted capability is the recurring insufficient pattern.",
"An overreaction that discards a maintained integration.",
"Forking works but takes on permanent maintenance that <code>disallowedTools</code> or a thin wrapper avoids."],
e:"Two enforcement-level options. Withhold the tools from agents that should not have them &mdash; <code>disallowedTools</code> with a <code>mcp__&lt;server&gt;</code> pattern for a whole server, or specific tool names for a subset. Or wrap the server with a thin internal one exposing only the permitted operations, which additionally gives you a place to enforce policy and normalise output. Forking is the heaviest option and takes on the maintenance you were avoiding.",
k:"Forbidden operations: withhold with <code>disallowedTools</code>, or wrap the server. Never grant and instruct against use."},

{i:802,d:2,t:"2.4",s:"DPT",diff:2,
q:"You add an MCP server to project scope and a colleague reports their agent behaves differently from yours.",
ask:"What should you check?",
a:["Whether they have a user-scoped or local-scoped server shadowing it, and whether their environment variables resolve",
"Whether they are using a different model",
"Whether their context window is smaller",
"Whether they have pulled the latest commit"],
c:0,
w:["Correct. Shadowing and unresolved variables are the two configuration causes of divergent behaviour.",
"Model choice would not produce a capability difference.",
"Window size does not change which tools exist.",
"Worth checking, but the configuration-specific causes are the substantive ones."],
e:"Two documented causes of per-developer divergence. A user-scoped or local-scoped server with the same name shadows the project definition, giving that developer different capability silently. And an unset environment variable produces a warning but still loads the server with the literal <code>${VAR}</code> text, so the server exists but fails oddly. Both are invisible in the repository, which is why &ldquo;works on my machine&rdquo; with agents is usually a scope question.",
k:"Divergent agent behaviour between developers: check for shadowing servers and unresolved <code>${VAR}</code> substitutions."},

{i:803,d:2,t:"2.4",s:"DPT",diff:3,
q:"You want an MCP server available to one subagent but not to the main agent.",
ask:"Which mechanism expresses this?",
a:["The subagent's <code>mcpServers</code> field, which scopes servers to that subagent",
"Project versus user scope in <code>.mcp.json</code>",
"A <code>paths</code> glob on the server definition",
"It cannot be expressed; MCP servers are session-wide"],
c:0,
w:["Correct. Subagent definitions can specify which MCP servers are available to them.",
"That scope determines who has the server, not which agent within a session.",
"Servers do not take path globs.",
"Per-subagent scoping is supported."],
e:"Per-subagent MCP scoping is what makes the &ldquo;connect only what the role needs&rdquo; principle practical. A subagent definition can specify <code>mcpServers</code> alongside <code>tools</code> and <code>disallowedTools</code>, so a large integration server can serve one specialist without enlarging every other agent's choice space. It pairs well with <code>mcp__&lt;server&gt;</code> patterns in <code>disallowedTools</code> for the inverse case.",
k:"Subagent definitions take an <code>mcpServers</code> field &mdash; scope large servers to the one role that needs them."},

{i:804,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to find where a configuration value is set, and it could be in code, YAML, or environment files.",
ask:"What is the efficient approach?",
a:["Grep for the key across all file types at once, since content search is not restricted by extension",
"Glob for each file type separately then read them",
"Read the most likely configuration file first",
"Ask the developer where it is set"],
c:0,
w:["Correct. A single content search across the tree covers every file type in one operation.",
"Globbing by type then reading is far more expensive than one content search.",
"Guessing may miss the actual location and costs a read.",
"The agent can determine this directly."],
e:"Configuration values are exactly the case where content search beats file discovery: you know the key, not the file. One grep covers code, YAML, environment files and templates simultaneously, and the results tell you both where it is set and how many places set it &mdash; which matters, since precedence between multiple definitions is usually the actual question.",
k:"Search by content when you know the key but not the file. One grep spans every file type at once."},

{i:805,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent must modify a file it has not read.",
ask:"What should happen first?",
a:["Read it, so the change is made against content the agent has actually seen rather than assumed",
"Write the new content directly",
"Grep for the relevant section only",
"Check the file's modification time"],
c:0,
w:["Correct. Modifying unread content risks destroying what you did not know was there.",
"Writing over unread content is how unrelated code gets silently deleted.",
"A grep hit shows a line, not the surrounding context a safe edit needs.",
"Modification time says nothing about content."],
e:"Read before write is a safety discipline, not a formality &mdash; and it is why <code>Edit</code>'s unique-match requirement exists, since a match is only meaningful against content you have seen. For a targeted change, read the relevant range and use <code>Edit</code>; reserve full-file <code>Write</code> for genuine replacement, and read first even then.",
k:"Read before modifying. <code>Edit</code> for targeted changes against seen content; <code>Write</code> only for genuine replacement."},

{i:806,d:2,t:"2.5",s:"DPT",diff:3,
q:"An agent's grep for a function name returns 200 matches, most of them in a vendored dependency directory.",
ask:"What is the efficient next step?",
a:["Narrow the search to exclude vendored paths, rather than reading through matches that cannot be the answer",
"Read all 200 matches",
"Read the first ten matches",
"Switch to a glob search"],
c:0,
w:["Correct. Refining the search is far cheaper than filtering results by reading them.",
"Reading 200 matches burns enormous context on mostly-irrelevant hits.",
"The first ten are arbitrary and likely all vendored.",
"Glob finds files by name, which does not help with a content question."],
e:"When a search returns too much, refine the search rather than filtering by reading. Excluding vendored directories, build output and generated files is usually the single highest-value narrowing. This is the same principle as scoping CI input: what you exclude at the search boundary costs nothing, while what you exclude after reading has already cost you the tokens.",
k:"Too many search hits? Refine the search, don't filter by reading. Exclude vendored and generated paths first."},

{i:807,d:2,t:"2.1",s:"AAT",diff:2,
q:"You are told a tool description should explain how the tool works internally.",
ask:"What is the accurate position?",
a:["No &mdash; the description exists to drive selection and correct invocation, so internals are noise unless they change when or how to call it",
"Yes &mdash; more information always helps selection",
"Yes &mdash; the model needs to understand the implementation to use it",
"No &mdash; descriptions should contain only the tool name"],
c:0,
w:["Correct. Include internals only when they change the calling decision, such as a rate limit or a cost characteristic.",
"Irrelevant information costs tokens on every request and dilutes the discriminating content.",
"The model uses the interface, not the implementation.",
"Minimal descriptions are the documented cause of unreliable selection."],
e:"Judge every element by whether it changes the calling decision. What it does, when to choose it, input format with an example, what it returns, and the boundary against neighbours all do. Internal architecture does not &mdash; unless an implementation fact leaks into the interface, such as &ldquo;this performs a full-corpus scan; prefer the index for keyword lookups&rdquo;, which is a preference rule dressed as an internal detail.",
k:"Descriptions serve selection and invocation. Include internals only when they change when or how to call."},

{i:808,d:2,t:"2.2",s:"CSA",diff:2,
q:"You want the agent to be able to tell the customer roughly how long a failed operation will be unavailable.",
ask:"What should the error carry?",
a:["An expected retry-after or resolution window where one is known, so the agent can set an accurate expectation",
"An apology",
"An internal incident identifier",
"The number of previous failures"],
c:0,
w:["Correct. A known window converts a vague failure into a manageable customer expectation.",
"Apologies carry no information.",
"An incident id is for your operators, not the customer.",
"Failure counts are operational metrics."],
e:"Error responses have two audiences, and the customer-facing half benefits from timing information when it exists. &ldquo;This service is unavailable; expected back within the hour&rdquo; lets the agent set an expectation and offer a follow-up rather than leaving the customer with an unqualified failure. Where no window is known, saying so honestly is better than implying transience with vague phrasing like &ldquo;at this time&rdquo;.",
k:"Include a retry-after or resolution window when known. Never imply transience vaguely when you cannot state one."},

{i:809,d:2,t:"2.3",s:"AAT",diff:2,
q:"You are told to give every agent the same toolset for consistency.",
ask:"What is the strongest objection?",
a:["It maximises every agent's choice space and gives each tools outside its specialisation &mdash; the two documented causes of unreliable selection",
"It is harder to configure",
"It uses more disk space",
"Agents cannot share tool definitions"],
c:0,
w:["Correct. Uniformity here maximises exactly the two conditions that degrade selection.",
"Configuration effort is trivial by comparison.",
"Disk usage is irrelevant.",
"Sharing definitions is fine; the problem is granting them all to everyone."],
e:"Uniformity is appealing operationally and wrong architecturally. The blueprint identifies two mechanisms behind selection failure &mdash; toolset size, and holding tools outside the agent's specialisation &mdash; and a universal toolset maximises both for every agent simultaneously. It also removes the security benefit of scoping and makes blast radius uniform across roles that carry very different risk.",
k:"A universal toolset maximises both causes of selection failure for every agent, and flattens blast radius across roles."},

{i:810,d:2,t:"2.4",s:"DPT",diff:2,
q:"Your team wants MCP server configuration reviewed like code.",
ask:"Which scope makes this possible?",
a:["Project scope in <code>.mcp.json</code>, which is version-controlled and therefore reviewable in pull requests",
"User scope in <code>~/.claude.json</code>",
"Local scope",
"Any scope; all are version-controlled"],
c:0,
w:["Correct. Only project scope lives in the repository and can go through review.",
"User scope is a personal file outside version control.",
"Local scope is also in <code>~/.claude.json</code> and not version-controlled.",
"Only project scope is version-controlled."],
e:"Reviewability is one of the strongest arguments for project scope. Because <code>.mcp.json</code> is committed, adding a server is a pull request &mdash; which matters because a project MCP server is a capability grant to every developer's agent. Treat additions as security review: what does the server expose, what is the blast radius, would a narrower wrapper be better.",
k:"Only project <code>.mcp.json</code> is version-controlled and reviewable. Treat server additions as capability grants."},

{i:811,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to understand a function's behaviour, and the function is 400 lines long.",
ask:"What is the reasonable approach?",
a:["Read the whole function, since it is the unit of behaviour being asked about and partial reads risk missing branches",
"Read the first 50 lines and infer the rest",
"Grep within the function for keywords",
"Read only the function signature"],
c:0,
w:["Correct. Trimming applies to irrelevant material, not to the primary subject of the question.",
"Behaviour lives in the branches, and the interesting ones are rarely first.",
"Keyword search within a function you need to understand is a poor substitute for reading it.",
"A signature gives the interface, not the behaviour."],
e:"Context discipline means cutting what is irrelevant, not cutting the subject of the work. A 400-line function is large but it is the unit of behaviour in question, and reading half of it risks missing exactly the branch that answers the question. Compare with reading 12 files on a guess &mdash; that is waste; this is the task. If the function is so large that reading it is genuinely costly, that is a signal about the code, not about your reading strategy.",
k:"Trim irrelevance, never the subject of the question. Read the whole unit of behaviour you are asked about."},

{i:812,d:2,t:"2.2",s:"AAT",diff:2,
q:"Two tools return errors with the same category but very different severity &mdash; one blocks the workflow, one is a minor degradation.",
ask:"How should this be conveyed?",
a:["With a separate field for impact, since category describes the failure type and impact describes what it costs the workflow",
"By using different categories",
"By making the message longer for the severe one",
"By setting <code>isRetryable</code> differently"],
c:0,
w:["Correct. Type and impact are independent dimensions and conflating them loses information.",
"Category should classify the failure type consistently; overloading it to encode severity breaks the taxonomy.",
"Message length is not a signal.",
"Retryability is a third independent dimension, not a severity proxy."],
e:"Keep the dimensions independent: <strong>category</strong> (transient / validation / permission / business) describes what kind of failure it is and drives recovery; <strong>retryability</strong> answers whether the identical call could succeed; <strong>impact</strong> describes what the failure costs the workflow and drives whether the coordinator degrades gracefully or stops. Overloading one field to carry two meanings makes both unreliable.",
k:"Category, retryability and impact are independent dimensions. Do not overload one field to carry another's meaning."},

{i:813,d:2,t:"2.1",s:"SDE",diff:2,
q:"A tool accepts either an id or a name for the same entity.",
ask:"What should the description make clear?",
a:["When to use each, and what happens when a name matches several entities",
"That both are accepted",
"That ids are preferred",
"That the tool is flexible"],
c:0,
w:["Correct. The interesting content is the decision rule and the ambiguous case, not the mere fact of flexibility.",
"Stating both are accepted leaves the choice and the ambiguity unaddressed.",
"A bare preference does not say when the alternative is appropriate.",
"&ldquo;Flexible&rdquo; is an adjective, not information."],
e:"Flexible inputs need a decision rule and an ambiguity contract. Say when each form applies &mdash; prefer the id when known, accept the name for a first lookup &mdash; and state what happens when a name is not unique. That connects directly to the escalation principle: multiple matches should return all candidates so the agent asks for another identifier, rather than the tool silently picking one.",
k:"For flexible inputs, state the decision rule and the ambiguity contract. Multiple matches must be returned, not resolved."},

{i:814,d:2,t:"2.3",s:"AAT",diff:2,
q:"You need an agent to be able to read from a system it must never write to.",
ask:"What is the cleanest expression?",
a:["Grant only the read tools; do not grant the write tools at all",
"Grant both and block writes in a hook",
"Grant both and instruct the agent not to write",
"Grant write access but revoke it after each turn"],
c:0,
w:["Correct. Omission is simpler, stronger, and improves selection by keeping the toolset small.",
"A hook blocking every write is a workaround for granting tools you should not have granted &mdash; and it enlarges the toolset.",
"Granting and instructing is the recurring insufficient pattern.",
"Per-turn revocation is complexity solving a problem omission removes."],
e:"When the answer is &ldquo;never&rdquo;, omission beats every alternative: nothing to block, nothing to bypass, and a smaller toolset that selects better. Reserve hooks for conditional rules &mdash; blocked above a threshold, blocked outside business hours, blocked without prior verification &mdash; where the capability is legitimately needed sometimes.",
k:"&ldquo;Never&rdquo; means omit the tool. Hooks are for <em>conditional</em> restrictions on capabilities you genuinely need."},

{i:815,d:2,t:"2.4",s:"DPT",diff:2,
q:"An MCP server exposes both tools and resources.",
ask:"How should the agent use each?",
a:["Consult resources for reference content such as schemas and catalogues; call tools to perform actions and fetch dynamic data",
"Use tools for everything; resources are for humans",
"Use resources for everything; tools are legacy",
"They are interchangeable"],
c:0,
w:["Correct. Resources are readable content; tools are actions and dynamic queries.",
"Resources are read by the model, not just by humans.",
"Both are current and serve different purposes.",
"They differ fundamentally in what they do."],
e:"The division matters for efficiency. Resources are content catalogues &mdash; database schemas, task summaries, reference tables &mdash; that the agent consults directly, removing the exploratory tool calls it would otherwise make to discover structure. Tools perform actions and fetch data that changes. Exposing stable reference material as a resource rather than behind a tool saves round trips and keeps the toolset smaller.",
k:"Resources = stable reference content, consulted directly. Tools = actions and dynamic data. Resources cut exploratory calls."},

{i:816,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to know which files changed in the last commit.",
ask:"Which tool is appropriate?",
a:["<code>Bash</code> with a git command, since this is version-control state rather than file content or names on disk",
"<code>Glob</code> with a recent-files pattern",
"<code>Grep</code> for commit metadata",
"<code>Read</code> on the git directory"],
c:0,
w:["Correct. Version-control state is not exposed by the file tools, so a git command is the right instrument.",
"Glob matches filename patterns and cannot express &ldquo;changed in the last commit&rdquo;.",
"Commit metadata is not searchable as file content in any useful way.",
"Reading git internals directly is fragile and unnecessary."],
e:"The preference for narrow tools over <code>Bash</code> is a default, not an absolute &mdash; and version-control state is a legitimate case where the file tools genuinely cannot answer the question. The discipline that remains: use the narrowest command that answers it, and where a repository does this often, consider a purpose-built tool that returns structured results rather than shell output the agent must parse.",
k:"<code>Bash</code> is right when file tools genuinely cannot answer &mdash; e.g. git state. Use the narrowest command; consider a structured tool if frequent."},

{i:817,d:2,t:"2.1",s:"CSA",diff:2,
q:"A tool's name and description are good, but its parameters are named <code>p1</code>, <code>p2</code> and <code>p3</code>.",
ask:"What is the consequence?",
a:["The model must infer each parameter's meaning from the description alone, increasing the chance of transposing values",
"The API will reject the schema",
"The tool cannot be called",
"There is no consequence if the description explains them"],
c:0,
w:["Correct. Parameter names are part of the interface the model reasons over, and opaque names invite transposition.",
"Parameter names are not validated for meaningfulness.",
"It can be called, just less reliably.",
"A description helps but the names still carry no signal at the point of construction."],
e:"Parameter names are read while the model constructs the call, so they carry selection-adjacent signal just as tool names do. <code>customer_id</code> and <code>order_id</code> are hard to transpose; <code>p1</code> and <code>p2</code> are easy to. Combine meaningful names with schema constraints &mdash; types, formats, enums &mdash; so that a transposition is likely to be caught structurally rather than silently accepted.",
k:"Parameter names are part of the interface. Meaningful names plus schema constraints prevent silent transposition."},

{i:818,d:2,t:"2.2",s:"SDE",diff:2,
q:"A tool validates its input and returns an error listing only the first invalid field.",
ask:"What should change?",
a:["Return every failing field in one response, so a single corrected retry can fix them all",
"Return only the most severe error",
"Return the errors one at a time across retries",
"Return a count of errors without naming them"],
c:0,
w:["Correct. Listing all failures makes one retry sufficient instead of several.",
"Severity ranking still hides the other failures.",
"Discovering errors one per retry burns a round trip each.",
"A count is unactionable."],
e:"Validation errors are the retryable-when-corrected category, so their message content directly determines how many round trips a correction costs. Returning all failing fields &mdash; each with the received value and the expected format &mdash; means one corrected retry resolves everything. Returning them one at a time turns a single fix into a sequence of round trips.",
k:"Return every failing field at once with received value and expected format. One corrected retry should fix everything."},

{i:819,d:2,t:"2.3",s:"AAT",diff:3,
q:"You want to guarantee an agent cannot reach a particular MCP server's tools, even if someone later adds them to its allowlist by mistake.",
ask:"What is the most robust arrangement?",
a:["Do not make the server available to that agent at all &mdash; scope it to the agents that need it, rather than relying on tool lists alone",
"Add every one of the server's tools to <code>disallowedTools</code>",
"Rely on the allowlist being reviewed",
"Add a hook that blocks calls to that server"],
c:0,
w:["Correct. Not making the server available to the agent is stronger than filtering its tools afterwards.",
"A <code>mcp__&lt;server&gt;</code> pattern is better than enumeration, but it still relies on the denylist staying correct.",
"Review is a process control, not a technical one.",
"A hook helps as a layer but does not prevent the tools appearing in the choice space."],
e:"Layer the controls by strength. Strongest: the server is not available to the agent, so its tools never enter the choice space &mdash; which also keeps the toolset small. Next: a <code>mcp__&lt;server&gt;</code> pattern in <code>disallowedTools</code>, remembering that a denylist applies before an allowlist resolves. Weakest but still useful: a hook denying calls, as defence in depth. Prefer removing availability over filtering it.",
k:"Strongest control is not making the server available to the agent. Denylists and hooks are layers on top, not substitutes."},

{i:820,d:2,t:"2.4",s:"DPT",diff:2,
q:"A tool from an MCP server returns data in a format your agents find awkward, and you do not control the server.",
ask:"What are your options?",
a:["Normalise in the tool layer you do control &mdash; a thin wrapper server exposing the shape you want &mdash; rather than asking the model to translate on every call",
"Instruct the model to translate the format each time",
"Accept the awkward format",
"Stop using the server"],
c:0,
w:["Correct. A wrapper is where a deterministic transformation belongs when you cannot change the source.",
"Per-call translation by the model is probabilistic work for a deterministic mapping.",
"Awkward formats cause repeated misinterpretation.",
"Dropping a useful integration over a format problem is disproportionate."],
e:"A thin wrapper server is the durable answer when you cannot change the upstream: it normalises the shape, trims fields you never use, translates the error contract into yours, and gives you a place to enforce policy. That is one of the legitimate reasons to build a custom MCP server despite the general preference for community ones &mdash; a deliberately narrower, normalised surface.",
k:"Wrap upstream servers you cannot change: normalise shape, trim fields, translate errors, enforce policy."},

{i:821,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to check whether a change it made compiles.",
ask:"What is the best tool arrangement?",
a:["A narrow build or typecheck tool returning structured results, rather than general shell access",
"<code>Bash</code>, since the build command varies",
"<code>Read</code> on the build configuration",
"<code>Grep</code> for syntax errors"],
c:0,
w:["Correct. A narrow tool limits capability and returns results the agent can act on directly.",
"Command variation is handled inside the tool, not by granting arbitrary execution.",
"Reading the configuration does not run the build.",
"Syntax errors are not findable by content search."],
e:"Purpose-built tools beat <code>Bash</code> twice over: they grant only the capability needed, and they can return structured results &mdash; error count, file, line, message &mdash; instead of console output the agent must parse. Where the command genuinely varies by project, that variation belongs inside the tool implementation or in project configuration, not in a grant of arbitrary shell execution.",
k:"Purpose-built tools limit capability <em>and</em> return structured results. Put command variation inside the tool."},

{i:822,d:2,t:"2.1",s:"AAT",diff:2,
q:"You have written a tool description and want to check whether it is good enough.",
ask:"What is a practical test?",
a:["Give it to someone unfamiliar with the system and ask when they would call this tool rather than its neighbours &mdash; if they hesitate, the model will too",
"Count the words",
"Check it mentions the tool name",
"Confirm it is longer than the others"],
c:0,
w:["Correct. Human ambiguity on the boundary predicts model ambiguity.",
"Length is not a quality measure.",
"Restating the name adds nothing.",
"Relative length is meaningless."],
e:"The boundary test is the practical one. If a colleague cannot say confidently when to use this tool rather than the nearest alternative, the description is not doing selection work &mdash; and no amount of additional prose about what the tool does will fix a boundary that was never stated. Add the trigger conditions and the explicit contrast, then re-test.",
k:"Test descriptions on a human: can they say when to use this rather than its neighbour? Hesitation predicts misrouting."},

{i:823,d:2,t:"2.2",s:"MAR",diff:2,
q:"You want a coordinator to be able to distinguish a subagent that failed from one that succeeded but found nothing useful.",
ask:"What must the contract provide?",
a:["Distinct representations for success-with-no-findings and failure, both carrying what was attempted",
"A single status field with two values",
"A confidence score",
"Elapsed time"],
c:0,
w:["Correct. Three outcomes &mdash; found, found nothing, could not look &mdash; need distinguishable representations.",
"Two values cannot express three outcomes.",
"Confidence does not express the distinction.",
"Elapsed time is an operational metric."],
e:"This is the empty-versus-failure distinction at the agent boundary rather than the tool boundary. &ldquo;I searched thoroughly and this sub-domain has little published material&rdquo; is a finding that belongs in the report's coverage annotation. &ldquo;I could not search&rdquo; is a failure needing recovery. Including what was attempted in both cases lets the coordinator judge whether an empty result reflects the topic or a poorly-formed query.",
k:"Three outcomes at every boundary: found, found nothing, could not look. Include what was attempted in all three."},

{i:824,d:2,t:"2.3",s:"CSA",diff:2,
q:"An agent is given a tool it needs for one rare edge case and never uses otherwise.",
ask:"What is the trade-off?",
a:["It costs tokens and choice-space on every request to serve a rare case; consider routing that case to a specialist instead",
"There is no cost to unused tools",
"It should be forced occasionally to keep it warm",
"Rare tools improve selection by contrast"],
c:0,
w:["Correct. Weigh the ongoing cost against the frequency of the case it serves.",
"Tool definitions are billed on every request and enlarge the comparison.",
"There is no warming concept.",
"Additional options degrade rather than sharpen selection."],
e:"Rare capabilities are exactly where routing beats granting. If a case arises once a week, paying its tool definition on every request and enlarging every selection decision is poor value &mdash; route it to a specialist agent that owns it, or escalate it. This mirrors the earlier judgement about adding a sixth agent for a rare case: the deciding factors are volume and what the alternative costs.",
k:"Rare capabilities are better routed than granted. Weigh per-request cost against case frequency."},

{i:825,d:2,t:"2.4",s:"DPT",diff:2,
q:"You want to know whether adding an MCP server has degraded your agents' tool selection.",
ask:"How would you find out?",
a:["Measure selection accuracy on a fixed set of representative requests before and after the change",
"Count the number of tools before and after",
"Ask developers whether it feels worse",
"Check whether the server is well-maintained"],
c:0,
w:["Correct. A controlled before-and-after on fixed inputs is what isolates the effect.",
"Tool count predicts risk without measuring the actual effect.",
"Subjective impressions are slow and confounded.",
"Server quality does not tell you about your agents' selection."],
e:"Configuration changes deserve the same measurement discipline as prompt changes. Keep a fixed set of representative requests with known correct tool selections, run it before and after any change to the tool surface, and compare. Without it, the gradual degradation from accumulated MCP connections is invisible until it is severe &mdash; and by then several changes are confounded.",
k:"Measure selection accuracy on fixed representative requests before and after tool-surface changes."},

{i:826,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent has read a file, made an edit, and now needs to confirm the edit landed correctly.",
ask:"What is the reasonable approach?",
a:["Rely on the edit tool's success signal, and verify behaviour with a test rather than re-reading to confirm the text changed",
"Re-read the whole file after every edit",
"Re-read only the edited range",
"Diff the file against its previous content"],
c:0,
w:["Correct. The tool reports success or failure; behavioural verification is what actually adds confidence.",
"Re-reading whole files after every edit is expensive and confirms only that text changed.",
"Better than a whole file, but still confirms text rather than behaviour.",
"A diff shows what changed textually, which the edit already told you."],
e:"Distinguish confirming the mechanical change from verifying the outcome. The edit tool already reports whether the change applied &mdash; re-reading to check adds context cost for information you have. What is genuinely worth doing is verifying <em>behaviour</em>: run the test. That is the goal-driven pattern applied to edits, and it catches the case where the text changed exactly as intended and the behaviour is still wrong.",
k:"Trust the edit's success signal; verify behaviour with tests. Re-reading confirms text, not correctness."},

{i:827,d:2,t:"2.2",s:"AAT",diff:2,
q:"You are designing an error contract and someone proposes including a suggested next tool call.",
ask:"Is this a good idea?",
a:["Often yes, where a clear correct next step exists &mdash; but it should suggest rather than dictate, since the tool cannot see the agent's broader goal",
"No &mdash; tools should never influence agent decisions",
"Yes &mdash; the agent should always follow the suggestion",
"No &mdash; suggestions add tokens without value"],
c:0,
w:["Correct. Suggest where the step is clear, and leave the decision with the agent that holds the goal context.",
"Errors already influence decisions; a useful suggestion is an extension of that.",
"Mandating a next step ignores context the tool cannot see.",
"A well-chosen suggestion is high-value, not noise."],
e:"Naming the next step is one of the most useful things an error can do &mdash; &ldquo;call <code>get_customer</code> first to establish a verified id&rdquo; converts a dead end into a one-step recovery. The nuance is framing: the tool knows its own precondition, not whether the agent should pursue this path at all. Suggest the step; leave the decision with the agent.",
k:"Errors should suggest the next step where one is clear, but not dictate it &mdash; the tool cannot see the agent's goal."},

{i:828,d:2,t:"2.1",s:"SDE",diff:2,
q:"A tool has an optional parameter that most callers should set but few do.",
ask:"What should you consider?",
a:["Whether the description makes clear when and why to set it, and whether the default is the right one for the common case",
"Making it required",
"Removing it",
"Renaming it"],
c:0,
w:["Correct. Low usage of a parameter that should usually be set is a description or default problem.",
"Making it required forces a decision on callers for whom the default is correct.",
"Removing a genuinely useful parameter is an overreaction.",
"Naming may contribute, but the substantive issues are guidance and default."],
e:"Two candidate causes with different fixes. If the default is wrong for the common case, change the default &mdash; that fixes it for every caller at once. If the default is right but the parameter matters in specific circumstances, say so in the description with the trigger condition. Making it required is the wrong lever, since it forces a decision even where the default is correct.",
k:"Under-set optional parameters mean a wrong default or unclear guidance. Fix the default first; then the description."},

{i:829,d:2,t:"2.3",s:"AAT",diff:2,
q:"You have scoped tools by role and selection accuracy has improved, but one agent now needs a capability it lacks for an occasional case.",
ask:"What is the ordered set of options?",
a:["Route to the owning agent; or grant a narrow read-only utility; or, only if neither fits, widen the toolset",
"Widen the toolset immediately",
"Escalate the case to a human every time",
"Merge the two agents"],
c:0,
w:["Correct. Try routing first, then a narrow utility, and widen only as a last resort.",
"Widening first undoes the scoping that produced the improvement.",
"Escalating routine work to humans is expensive and unnecessary.",
"Merging discards the specialisation."],
e:"Work down the options by cost to the architecture. Routing through the coordinator preserves scoping entirely and costs a round trip. A narrow read-only cross-role utility removes the round trip at minimal cost to the choice space &mdash; the <code>verify_fact</code> pattern. Widening the toolset with a full capability is the last resort, because it reintroduces exactly what scoping fixed.",
k:"Occasional cross-role needs: route first, then a narrow read-only utility, then widen only as a last resort."},

{i:830,d:2,t:"2.4",s:"DPT",diff:2,
q:"You are documenting an MCP server your team maintains, for other teams to adopt.",
ask:"What matters most in the documentation?",
a:["What each tool is for and when to prefer it over alternatives, since adopting teams will inherit its selection characteristics",
"The server's internal architecture",
"The server's deployment topology",
"The team that maintains it"],
c:0,
w:["Correct. Adopting teams inherit the tool descriptions and their effect on selection.",
"Internal architecture does not affect how agents use it.",
"Deployment matters operationally but not for tool selection.",
"Ownership belongs in a service catalogue."],
e:"When another team connects your server, its tools join every one of their agents' choice spaces &mdash; so your descriptions become their selection quality. Document what each tool is for, when to prefer it, its input formats with examples, and its error contract. Document the operational facts too (required environment variables, scopes), but the selection-relevant content is what determines whether adoption goes well.",
k:"A shared MCP server exports its selection characteristics. Document purpose, boundaries, input formats and error contract."}
);
