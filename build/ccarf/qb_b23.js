QB.push(
{i:871,d:1,t:"1.1",s:"AAT",diff:2,
q:"Your agent loop appends tool results correctly but re-sends the system prompt with a timestamp injected each turn.",
ask:"What does this cost you?",
a:["The stable prefix changes every turn, so prompt caching never hits and you pay full price for the system prompt and tools each iteration",
"Nothing; the timestamp is small",
"The model becomes confused about time",
"The loop cannot terminate"],
c:0,
w:["Correct. A single volatile element early in the request destroys the cacheable prefix.",
"The timestamp is small but its <em>position</em> invalidates everything cached after it.",
"Time confusion is not the issue; caching is.",
"Termination is unaffected."],
e:"Prompt caching operates on a prefix, so the cost is not the timestamp's size but its position. Anything volatile placed before the tool definitions invalidates the cache for the whole prefix. If the model genuinely needs the current time, put it in the volatile section after the stable content &mdash; or better, supply it through a tool call when actually needed.",
k:"One volatile element early destroys the whole cacheable prefix. Keep volatile content after stable content."},

{i:872,d:1,t:"1.1",s:"CSA",diff:2,
q:"An agent calls a tool, gets a result, and calls the identical tool again in the very next turn with the same arguments.",
ask:"What is the first thing to check?",
a:["Whether the tool result was actually appended to the history before the next request was sent",
"Whether the model is too capable",
"Whether the tool is too slow",
"Whether the iteration cap is too high"],
c:0,
w:["Correct. An immediate identical repeat most often means the result never reached the model.",
"Capability does not cause immediate repetition.",
"Tool latency does not cause a repeat call.",
"The cap bounds the loop; it does not cause repetition."],
e:"Distinguish immediate repetition from later re-querying. An <em>immediate</em> identical repeat usually means a loop bug &mdash; the result was not appended, or the assistant turn was dropped, so from the model's perspective it never asked. Re-querying <em>several turns later</em> is different: that points at unclear or unlabelled results, where the agent cannot tell it already has what it needs.",
k:"Immediate identical repeat = loop bug (result not appended). Later re-query = unclear tool output."},

{i:873,d:1,t:"1.1",s:"AAT",diff:3,
q:"You want your agent loop to be resilient to a single malformed tool result crashing the whole run.",
ask:"Where should the handling live?",
a:["In the executor, which catches the fault and returns a well-formed <code>tool_result</code> with <code>is_error: true</code> rather than letting the exception escape the loop",
"In the model, by instructing it to handle malformed results",
"In the API, which validates tool results",
"Nowhere; malformed results should crash the run"],
c:0,
w:["Correct. The executor converts faults into in-band error results the model can reason about.",
"The model cannot handle a result that never reaches it because the process crashed.",
"The API does not validate your tool result content for you.",
"Crashing loses the whole conversation over one tool fault."],
e:"Your executor is the boundary between arbitrary tool code and the conversation. Every failure it can encounter &mdash; exception, malformed response, timeout, unexpected type &mdash; should be converted into a well-formed <code>tool_result</code> with <code>is_error: true</code> and a specific message. That keeps the conversation valid, keeps the <code>tool_use</code> block answered as the API requires, and gives the model a chance to recover.",
k:"The executor converts every tool fault into a well-formed <code>tool_result</code> with <code>is_error: true</code>. Exceptions must not escape the loop."},

{i:874,d:1,t:"1.2",s:"MAR",diff:2,
q:"Your coordinator produces good results but each run takes a different path through the subagents.",
ask:"Is this a problem?",
a:["Only if the variation is unexplained &mdash; adaptive routing is the point, but identical inputs producing different paths for no reason makes behaviour undebuggable",
"Yes, coordinators must be deterministic",
"No, variation always indicates good adaptation",
"Yes, and the coordinator should be replaced with a fixed pipeline"],
c:0,
w:["Correct. Distinguish intended adaptation from unexplained non-determinism.",
"Adaptive routing is the reason to use a coordinator at all.",
"Variation on identical inputs is more often ambiguous descriptions than genuine adaptation.",
"A fixed pipeline forfeits adaptivity to solve a description problem."],
e:"Two different things look alike in the trace. Genuine adaptation &mdash; different inputs producing different paths, or a second dispatch round after a thin first result &mdash; is the value an agentic coordinator provides. Unexplained variation on identical inputs is usually overlapping agent descriptions, and it is corrosive because behaviour becomes irreproducible. Logging the routing rationale is what lets you tell them apart.",
k:"Adaptation on different inputs is the point. Variation on identical inputs means ambiguous descriptions."},

{i:875,d:1,t:"1.2",s:"MAR",diff:3,
q:"Your coordinator is given the ability to re-dispatch subtasks but never uses it, even when results are thin.",
ask:"What is likely missing?",
a:["Explicit quality criteria it can evaluate results against, since without a standard it has no basis to judge a result inadequate",
"Permission to re-dispatch",
"A larger context window",
"More subagents"],
c:0,
w:["Correct. Re-dispatch requires a standard against which a result can fail.",
"The capability is stated as present.",
"Window size does not supply a quality standard.",
"More agents does not help it judge the ones it has."],
e:"An iterative refinement loop needs three things, and criteria are the one most often missing: a concrete definition of done, the ability to detect a shortfall against it, and the mechanism to re-dispatch. Given the mechanism but no standard, the coordinator has no basis to conclude anything is inadequate, so it accepts whatever arrives. State the bar &mdash; sources per claim, sub-domains covered, dates present.",
k:"Re-dispatch needs a quality standard to fail against. Capability without criteria is never exercised."},

{i:876,d:1,t:"1.3",s:"AAT",diff:2,
q:"A subagent's prompt includes the phrase &ldquo;follow the usual process&rdquo;.",
ask:"What should replace it?",
a:["The process itself, stated in the prompt or encoded in the subagent's system prompt",
"A reference to the process document's filename",
"An instruction to ask the coordinator what the process is",
"Nothing; the subagent will infer it"],
c:0,
w:["Correct. Either state it in the delegation, or put it in the subagent's system prompt where it holds across invocations.",
"A filename resolves to nothing unless the subagent has a tool to read it and is told to.",
"Asking adds a round trip for information the coordinator already had.",
"There is no shared notion of &ldquo;usual&rdquo; for a fresh context."],
e:"Two placements, chosen by stability. A process that holds for every invocation of this subagent belongs in its <em>system prompt</em>, so it never has to be restated. A process specific to this delegation belongs in the <em>task prompt</em>. What never works is a reference &mdash; &ldquo;the usual process&rdquo;, &ldquo;as documented&rdquo;, &ldquo;same as last time&rdquo; &mdash; because a fresh context has nothing to resolve it against.",
k:"Stable process &rarr; subagent system prompt. This-delegation process &rarr; task prompt. Never a bare reference."},

{i:877,d:1,t:"1.3",s:"MAR",diff:2,
q:"A coordinator wants a subagent to be thorough but not to spend unbounded time.",
ask:"How should this be expressed?",
a:["As concrete bounds tied to the quality criteria &mdash; how many sources, what coverage &mdash; rather than as a time instruction the subagent cannot measure",
"As a maximum number of minutes",
"As an instruction to be efficient",
"As a maximum number of tool calls in the prompt"],
c:0,
w:["Correct. Bounds expressed in terms of the work are checkable by the subagent; wall-clock time is not.",
"A model cannot observe elapsed time, so a minutes instruction is unenforceable from inside.",
"&ldquo;Efficient&rdquo; is undefined.",
"A tool-call limit in the prompt is guidance; if you need a hard bound, enforce it in configuration."],
e:"Express bounds in terms the subagent can actually evaluate. &ldquo;Consult at least three independent sources; stop once every listed sub-domain has at least two&rdquo; is checkable from inside the task. Time is not observable to the model, so a wall-clock instruction is guidance at best &mdash; if you need a hard bound, enforce it in configuration (a turn limit, a timeout) rather than in the prompt.",
k:"Express bounds in work terms the subagent can evaluate. Wall-clock limits belong in configuration, not the prompt."},

{i:878,d:1,t:"1.4",s:"CSA",diff:2,
q:"A workflow requires two independent approvals before a high-value action.",
ask:"How should this be implemented?",
a:["As state your code owns and the tool checks &mdash; two recorded approvals from distinct approvers &mdash; not as something the agent tracks conversationally",
"By instructing the agent to obtain two approvals",
"By having the agent count approvals in the conversation",
"By escalating every high-value action"],
c:0,
w:["Correct. Approval state is authorisation state and must be authoritative, held server-side.",
"An instruction cannot guarantee a control with this consequence.",
"Counting conversationally lets the model be the source of truth for its own authorisation.",
"Escalating everything removes the workflow rather than implementing it."],
e:"Two-person approval is an authorisation invariant, so it follows the identity-verification pattern: the state lives in a store your code owns, the tool checks it before acting, and distinctness of approvers is enforced there. Anything the model tracks conversationally is untrusted input for this purpose &mdash; the same reason a model-supplied <code>verified: true</code> parameter proves nothing.",
k:"Approval state is authorisation state: server-side, checked by the tool. Never counted conversationally."},

{i:879,d:1,t:"1.4",s:"AAT",diff:3,
q:"You have a rule that must hold, and enforcing it requires information only available after the tool executes.",
ask:"What does this imply about the design?",
a:["The rule cannot be enforced by blocking the call, so either the tool must perform the check internally before committing, or the action must be made reversible",
"Use a <code>PostToolUse</code> hook to block it",
"Instruct the agent to check afterwards",
"Accept that the rule cannot be enforced"],
c:0,
w:["Correct. Enforcement must happen before the effect is committed, which means inside the tool or via reversibility.",
"<code>PostToolUse</code> runs after the tool has already executed, so it cannot prevent the effect.",
"A post-hoc agent check cannot undo a committed action.",
"It can be enforced, just not by pre-call blocking."],
e:"Enforcement requires acting before the effect is committed. When the deciding information only exists mid-operation, the check belongs <em>inside</em> the tool, before it commits &mdash; typically inside a transaction that can roll back. Where that is impossible, the alternative is to make the action reversible so a compensating step can undo it. What does not work is a hook after the fact, since the effect has already happened.",
k:"If the check needs post-execution information, put it inside the tool before commit, or make the action reversible."},

{i:880,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want a hook's decision to be explainable to an auditor months later.",
ask:"What should it record?",
a:["The inputs it evaluated, the rule version applied, the decision, and the reason &mdash; enough to reconstruct why it decided as it did",
"The decision only",
"The timestamp only",
"The agent's identity only"],
c:0,
w:["Correct. Reconstruction requires the inputs, the rule, the outcome and the reason together.",
"A bare decision cannot be explained without knowing what it was applied to.",
"A timestamp locates the event without explaining it.",
"Identity is one input among several."],
e:"Auditability is why code-level enforcement beats prompt-level rules, but only if the logging is complete. Record what was evaluated (the arguments, the relevant session state), which rule version applied, what was decided, and the stated reason &mdash; for allows as well as blocks. Rule versioning matters more than people expect: a decision that looks wrong today may have been correct under the policy in force at the time.",
k:"Log inputs, rule version, decision and reason &mdash; for allows and blocks alike. Version the rules."},

{i:881,d:1,t:"1.5",s:"CSA",diff:2,
q:"A hook must consult a slow external service to make its decision, and it sits on a customer-facing path.",
ask:"What is the reasonable design?",
a:["Cache the decision input where its staleness is acceptable, keep a tight timeout, and fail closed when it cannot be evaluated",
"Remove the check from the customer-facing path",
"Make the check asynchronous and allow the action meanwhile",
"Accept the latency without mitigation"],
c:0,
w:["Correct. Caching bounded by acceptable staleness, plus a tight timeout and fail-closed behaviour.",
"Removing an enforcement check to improve latency removes the control.",
"Allowing the action while the check runs is failing open on a control that must hold.",
"Unmitigated latency on a customer-facing path is a poor outcome when caching is available."],
e:"Enforcement hooks must fail closed, which makes their latency a real design constraint. Cache the inputs whose staleness you can tolerate &mdash; policy thresholds change rarely, account flags more often &mdash; and set the cache lifetime from that tolerance rather than from convenience. Keep a tight timeout, and on failure deny with a clear non-retryable message plus an escalation path, so the customer is routed rather than stranded.",
k:"Enforcement hooks on hot paths: cache by acceptable staleness, tight timeout, fail closed with an escalation path."},

{i:882,d:1,t:"1.6",s:"CGC",diff:2,
q:"A task decomposes cleanly but one subtask depends on a decision that has not been made yet.",
ask:"What should happen?",
a:["Sequence the decision first as its own step, since a subtask built on an unmade decision either blocks or assumes",
"Start the subtask and decide later",
"Remove the subtask",
"Make the decision arbitrarily to unblock"],
c:0,
w:["Correct. Make the decision an explicit step and sequence it before what depends on it.",
"Work built on an assumed decision is rework waiting to happen.",
"The subtask may be required.",
"Arbitrary decisions on things that matter is how avoidable rework starts."],
e:"Undecided questions are dependencies, and treating them as explicit steps makes them visible in the plan. Sequence the decision before everything that depends on it, and record the decision with its rationale in the plan file &mdash; that makes partial rollback possible if it later proves wrong, since you can see exactly which phases rested on it.",
k:"Undecided questions are dependencies. Sequence them as explicit steps and record the decision with rationale."},

{i:883,d:1,t:"1.6",s:"MAR",diff:2,
q:"A coordinator decomposes a task and one resulting subtask is far more uncertain than the others.",
ask:"How should this affect sequencing?",
a:["Run the uncertain subtask early, so a surprising result can reshape the plan before the certain work is done",
"Run it last, so the certain work is complete first",
"Run it in parallel with everything else",
"Remove it and handle it manually"],
c:0,
w:["Correct. Front-loading uncertainty means a surprise reshapes the plan before you have invested in work it might invalidate.",
"Running it last maximises the work potentially invalidated by its result.",
"Parallel is fine when nothing depends on it, but the sequencing insight still favours resolving uncertainty early.",
"Removing it does not resolve the uncertainty."],
e:"Front-load uncertainty. If a subtask might reveal something that changes the approach, discovering that after the other work is done wastes it. This is the same reasoning as sequencing a phase that depends on an unverified assumption early &mdash; you want the plan-invalidating information as cheaply and as soon as possible, which is also what makes partial rollback small.",
k:"Sequence uncertain work early. Plan-invalidating discoveries are cheapest before dependent work is done."},

{i:884,d:1,t:"1.7",s:"CGC",diff:2,
q:"You want to compare two approaches but they share only the first five minutes of context.",
ask:"Is forking worthwhile here?",
a:["Marginally &mdash; forking pays off in proportion to the shared context, so with little to share, independent sessions are similarly cheap and give stronger independence",
"Yes, forking is always preferable",
"No, forking is never appropriate for comparison",
"Yes, because forking is faster to set up"],
c:0,
w:["Correct. The value of forking scales with how expensive the shared prefix is.",
"With little shared context there is little to preserve.",
"Forking is the standard tool for comparison when context is expensive.",
"Setup speed is not the deciding factor."],
e:"Forking's value is proportional to the cost of the shared context. Twenty minutes of accumulated codebase understanding is expensive to rebuild twice, so forking is clearly right. Five minutes is cheap, and fully independent sessions give stronger independence &mdash; no shared prefix means no shared bias. Weigh what the shared context actually cost to build.",
k:"Forking's value scales with the shared context's cost. Cheap shared context favours fully independent sessions."},

{i:885,d:1,t:"1.7",s:"AAT",diff:2,
q:"A long-running agent's state file has become the bottleneck because it is rewritten in full after every step.",
ask:"What should change?",
a:["Write incremental updates or checkpoint at phase boundaries rather than after every step, matching write frequency to recovery granularity",
"Stop checkpointing",
"Keep the state in memory instead",
"Reduce the number of phases"],
c:0,
w:["Correct. Checkpoint frequency should match how much work you are willing to redo.",
"Removing checkpointing forfeits crash recovery.",
"In-memory state does not survive a crash, which is the point.",
"Fewer phases means coarser recovery, not a better write pattern."],
e:"Checkpoint frequency is a trade-off between write cost and how much work a crash loses. Phase boundaries are usually the right granularity: they are natural resume points, they are where outputs exist to record, and they bound the redo to one phase. Per-step checkpointing buys finer recovery at a cost that rarely pays &mdash; especially when steps are idempotent and cheap to repeat.",
k:"Checkpoint at phase boundaries. Frequency trades write cost against how much work a crash loses."},

{i:886,d:1,t:"1.1",s:"CAP",diff:2,
q:"A conversational agent must handle a user who asks a question, then interrupts with an unrelated urgent request before the first is answered.",
ask:"What is the correct behaviour?",
a:["Acknowledge both, handle the urgent one, and return to the first &mdash; tracking them as separate items rather than dropping one",
"Handle the urgent request and drop the first",
"Finish the first before acknowledging the second",
"Ask the user which to handle"],
c:0,
w:["Correct. Both are tracked; urgency orders them rather than eliminating one.",
"Dropping the first is the multi-aspect coverage failure.",
"Ignoring a stated urgent request to finish a question first is poor prioritisation.",
"The user already signalled the priority; asking adds friction."],
e:"This is multi-aspect tracking with an ordering signal. Both items go on the list; the stated urgency determines sequence, not membership. Confirming both back reassures the user that nothing was dropped and gives them a chance to correct the ordering. Verify both are resolved before ending the turn &mdash; the coverage check applies whether the items arrived together or sequentially.",
k:"Interruptions add items and reorder them; they do not remove items. Track both, confirm both, verify both."},

{i:887,d:1,t:"1.2",s:"AAT",diff:2,
q:"A coordinator's subagents each return quickly, but the coordinator spends a long time deciding what to do with the results.",
ask:"What is worth examining?",
a:["Whether the results arrive in a form the coordinator can act on directly, or whether it is doing interpretation work that structure would eliminate",
"Whether the coordinator needs a faster model",
"Whether there are too many subagents",
"Whether the coordinator's context is too small"],
c:0,
w:["Correct. Interpretation effort at the hub usually means unstructured returns.",
"A faster model would do the same unnecessary work faster.",
"Agent count is not indicated by this symptom.",
"Context size does not cause interpretation effort."],
e:"Time spent at the coordinator reconciling inconsistent shapes, extracting values from prose, or working out which agent produced what is work an output contract would eliminate. Specify the return shape &mdash; self-identifying, structured findings with values, sources and dates &mdash; and the coordinator's job reduces to routing and evaluation, which is what it should be doing.",
k:"Coordinator interpretation effort means unstructured returns. Fix with an output contract, not a faster model."},

{i:888,d:1,t:"1.3",s:"AAT",diff:2,
q:"You want a subagent to be reusable across several different coordinators.",
ask:"What determines whether it is reusable?",
a:["Whether its system prompt and tool list define a role rather than a task, so any coordinator can delegate to it with a task-specific prompt",
"Whether it uses a general-purpose model",
"Whether it has many tools",
"Whether it writes to a shared state store"],
c:0,
w:["Correct. Role-shaped definitions are reusable; task-shaped ones are not.",
"Model choice does not determine reusability.",
"A large toolset harms selection and does not aid reuse.",
"Shared state creates coupling rather than reusability."],
e:"Design subagent definitions around a role: what it is expert at, how it works, what standards it applies, what tools that requires. The task comes from the delegation prompt. A definition that hard-codes a particular task into its system prompt is single-use, and you end up with many near-duplicate agents &mdash; which is exactly the overlapping-description problem that degrades routing.",
k:"Define subagents by role, not task. The task comes from the delegation prompt; near-duplicate agents degrade routing."},

{i:889,d:1,t:"1.4",s:"CSA",diff:2,
q:"An agent is about to take an action that is reversible but whose reversal is expensive and visible to the customer.",
ask:"How should this be treated?",
a:["Closer to irreversible than to reversible &mdash; gate it, because reversibility that is costly and visible is not a safety net you want to rely on",
"As fully reversible, since it can be undone",
"As fully irreversible, requiring human approval always",
"It does not matter; reversibility is binary"],
c:0,
w:["Correct. Reversibility is a spectrum, and cost plus visibility push this toward the gated end.",
"Treating it as freely reversible ignores what the reversal actually costs.",
"Always requiring approval may be over-friction if the action is common.",
"Reversibility is not binary in any practical sense."],
e:"Classify actions by the <em>cost</em> of reversal rather than by whether reversal is theoretically possible. A refund that can be clawed back but requires contacting the customer is not meaningfully reversible in the way a cache write is. Gating this category &mdash; a confirmation, a threshold check, or an approval for larger cases &mdash; is proportionate, and it is why the reversibility question drives the human-in-the-loop targeting decision.",
k:"Classify by cost of reversal, not theoretical reversibility. Expensive, visible reversals are effectively irreversible."},

{i:890,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want to add an enforcement hook but the rule it enforces changes several times a year.",
ask:"How should this be designed?",
a:["Keep the rule's parameters in configuration or a policy service the hook reads, so a rule change does not require a code deployment",
"Hard-code the current rule and redeploy when it changes",
"Put the rule in the prompt instead, since it changes",
"Skip enforcement for rules that change"],
c:0,
w:["Correct. Separate the enforcement mechanism from the rule's parameters.",
"Redeploying for every threshold change is slow and discourages keeping the rule current.",
"Frequency of change is not a reason to abandon enforcement.",
"Changeability does not reduce the consequence of a violation."],
e:"Separate mechanism from policy. The hook &mdash; intercept, evaluate, block or allow, log &mdash; is stable code. The rule's parameters &mdash; the threshold, the permitted categories, the effective dates &mdash; belong in configuration or a policy service the hook reads. That lets policy change at business speed while enforcement stays constant, and it makes the rule version you log meaningful for auditing.",
k:"Separate the hook's mechanism from the rule's parameters. Policy in configuration, enforcement in code, version both."},

{i:891,d:1,t:"1.6",s:"CGC",diff:2,
q:"A plan has two phases that could run in parallel but share a file they both modify.",
ask:"What should happen?",
a:["Sequence them, since concurrent modification of the same file is a conflict regardless of the phases being logically independent",
"Run them in parallel and merge afterwards",
"Split the file",
"Run them in parallel on separate copies"],
c:0,
w:["Correct. Shared mutable state is a real dependency even when the logical tasks are independent.",
"Merging concurrent edits to the same file is exactly the conflict you can avoid by sequencing.",
"Splitting a file to enable parallelism is a large change for a small scheduling gain.",
"Separate copies recreate the merge problem at the end."],
e:"Logical independence is not sufficient for parallelism &mdash; shared mutable state creates a dependency the task graph may not show. Check for it explicitly when planning concurrency: two phases touching the same file, the same configuration, or the same database rows must be sequenced. This is the same caution as speculative tool execution: independence must hold for effects, not just for reasoning.",
k:"Parallelism requires independence of <em>effects</em>, not just of logic. Shared mutable state forces sequencing."},

{i:892,d:1,t:"1.7",s:"CGC",diff:2,
q:"You resume a session and it immediately produces work based on a plan phase you have already completed.",
ask:"What went wrong?",
a:["The plan file's progress markers were not updated when the phase completed, so the resumed session read stale state",
"The session should not have been resumed",
"The plan was too detailed",
"The model forgot what it did"],
c:0,
w:["Correct. Progress tracking must be updated as phases complete, or resumption reads a stale plan.",
"Resumption is reasonable; the state was wrong.",
"Detail is not the issue.",
"There is nothing to forget across a session boundary &mdash; the file is the memory."],
e:"A plan file is only useful for resumption if it stays current. Update progress markers at each phase boundary, alongside the outputs and any decisions taken &mdash; that is the same checkpoint discipline that makes crash recovery work. A stale plan is worse than none, because it actively directs the resumed session toward work already done.",
k:"Update plan progress at each phase boundary. A stale plan actively misdirects a resumed session."},

{i:893,d:1,t:"1.1",s:"AAT",diff:2,
q:"You are asked whether an agent loop should validate tool arguments before executing.",
ask:"What is the accurate position?",
a:["Yes &mdash; model-generated arguments are untrusted input, and schema validation plus domain checks belong before execution",
"No &mdash; the schema already guarantees them",
"No &mdash; validation duplicates the tool's own checks",
"Yes &mdash; but only for tools with side effects"],
c:0,
w:["Correct. Treat model-supplied arguments as you would any untrusted input.",
"Schemas constrain structure; they do not guarantee semantic correctness or authorisation.",
"Defence in depth is appropriate at a trust boundary.",
"Read tools can also leak or be abused with bad arguments, though side-effect tools carry more risk."],
e:"The trust boundary sits between the model's output and your execution, exactly as it does between a web form and your handler. Validate structure, validate domain constraints, and never accept authorisation facts from the model &mdash; inject those from authenticated state. The documentation notes the model may infer plausible values for parameters it was never given, which makes this concrete rather than theoretical.",
k:"Model-generated arguments are untrusted input. Validate structure and domain, and inject authorisation facts yourself."},

{i:894,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator must produce a result even when several subagents fail.",
ask:"What should be defined in advance?",
a:["The minimum viable set of results, so the coordinator knows whether it can proceed or must stop",
"The maximum number of retries",
"The order in which subagents are dispatched",
"The model each subagent uses"],
c:0,
w:["Correct. A defined minimum viable set is what makes degraded operation a decision rather than an accident.",
"Retry counts are part of the policy but do not define sufficiency.",
"Dispatch order does not determine what is sufficient.",
"Model choice is unrelated."],
e:"Graceful degradation needs a threshold. Define which results are on the critical path, which are optional enrichment, and what the minimum viable set is. Then a partial failure becomes a decision the coordinator can make &mdash; proceed with annotation, or stop and report &mdash; rather than either an unplanned abort or a silently thin output.",
k:"Define the minimum viable result set in advance. It turns partial failure into a decision rather than an accident."},

{i:895,d:1,t:"1.3",s:"MAR",diff:2,
q:"You want to prevent a subagent from spending its budget on material outside its assigned scope.",
ask:"What is most effective?",
a:["State the scope boundary and its exclusions explicitly, and give it a channel to report out-of-scope findings rather than pursuing them",
"Reduce its tool access",
"Reduce its turn limit",
"Instruct it to be efficient"],
c:0,
w:["Correct. Explicit exclusions plus a reporting channel means it neither pursues nor discards what it notices.",
"Tool restriction limits capability without defining scope.",
"A turn limit truncates without directing effort.",
"&ldquo;Efficient&rdquo; is undefined."],
e:"Scope drift happens because interesting adjacent material appears and the subagent has no instruction about it. Stating exclusions &mdash; and naming what siblings cover &mdash; tells it where to stop. Adding an <code>out_of_scope_observations</code> field to the output contract means the discovery still reaches the coordinator, which can dispatch a new subtask if it matters. Without that channel, you are choosing between drift and loss.",
k:"Prevent scope drift with explicit exclusions plus an out-of-scope reporting channel. Otherwise you choose drift or loss."},

{i:896,d:1,t:"1.4",s:"AAT",diff:2,
q:"You need to guarantee that an action is never taken outside business hours.",
ask:"Where does this belong?",
a:["In a hook or tool precondition evaluating the current time server-side, since the model cannot reliably observe time",
"In the system prompt, stating the business hours",
"In the tool description",
"In a scheduled job that reverses out-of-hours actions"],
c:0,
w:["Correct. Time is server-side state and the check must evaluate it authoritatively.",
"The model has no reliable clock, so it cannot apply this rule even if willing.",
"Descriptions inform selection; they cannot evaluate a condition.",
"Reversing afterwards is not prevention and may be impossible."],
e:"This rule fails at the prompt level for an unusual reason: the model has no reliable access to the current time, so it cannot apply the rule even in principle. That makes it a clean example of a check that must be evaluated server-side. State the rule in the prompt as well, so the agent understands the block and can explain it &mdash; but the enforcement is the hook.",
k:"Time-based rules must evaluate server-side; the model has no reliable clock. Prompt for understanding, hook for enforcement."},

{i:897,d:1,t:"1.5",s:"AAT",diff:2,
q:"A hook is added to normalise data, and a downstream prompt is written assuming the normalised shape.",
ask:"What coupling has been created?",
a:["The prompt now depends on the hook running, so disabling or reordering the hook silently breaks the prompt's assumption",
"None; they are independent",
"The hook depends on the prompt",
"They are coupled only in testing"],
c:0,
w:["Correct. An invariant established in one place and relied on in another is a real coupling.",
"The dependency is real even though it is invisible in either file.",
"The direction is the other way.",
"The coupling exists in production, which is where it matters."],
e:"Invariants create dependencies even when nothing references them explicitly. If the normalisation hook is disabled, reordered behind another hook that changes the shape, or fails, the prompt's assumption silently breaks &mdash; and the failure appears as a reasoning error rather than a configuration error. Document the invariant on both sides, and test the composition rather than only the individual pieces.",
k:"An invariant established in a hook and relied on in a prompt is a real coupling. Document both sides and test composition."},

{i:898,d:1,t:"1.6",s:"MAR",diff:2,
q:"You decompose a task and realise two subtasks would each produce half of one coherent answer.",
ask:"What does this suggest?",
a:["The split is in the wrong place &mdash; they should be one subtask, since neither half is meaningful alone",
"A third subtask should combine them",
"They should run in parallel and be concatenated",
"The subtasks are correctly sized"],
c:0,
w:["Correct. A slice that produces half an answer is a slice through the middle of a coherent unit.",
"Adding a combining subtask compensates for a bad partition rather than fixing it.",
"Concatenating halves does not produce a coherent whole.",
"Producing half an answer each is the definition of a bad boundary."],
e:"A good test for a decomposition boundary: does each part produce something meaningful on its own? If a subtask's output only makes sense once glued to its sibling's, you have cut through the middle of a coherent unit and created coupling the synthesis must repair. Slice at natural seams &mdash; sub-domains, components, phases &mdash; where each part stands alone.",
k:"Each subtask should produce something meaningful alone. Halves that only make sense together mean a bad boundary."},

{i:899,d:1,t:"1.7",s:"AAT",diff:2,
q:"You want to be able to reproduce a specific agent run for debugging.",
ask:"What must be captured?",
a:["The full inputs &mdash; system prompt, tools, conversation, configuration and model &mdash; not just the outputs",
"The final answer",
"The tool results",
"The elapsed time"],
c:0,
w:["Correct. Reproduction requires every input that determined the run.",
"An output cannot be reproduced without its inputs.",
"Tool results are one input among several.",
"Timing is operational metadata."],
e:"Reproducibility means recreating the inputs. That includes the system prompt and its version, the tool definitions, the full conversation, the relevant configuration (permission mode, hooks, connected servers), and the model. Note that exact reproduction is still not guaranteed &mdash; sampling introduces variation &mdash; but capturing the inputs is what makes an investigation possible at all, and it is why implicit context carryover is so corrosive to debugging.",
k:"Capture every input to reproduce a run: prompt, tools, conversation, configuration, model. Implicit context defeats this."},

{i:900,d:1,t:"1.1",s:"CSA",diff:2,
q:"An agent's first action on every conversation is to call the same lookup tool, regardless of what the customer asked.",
ask:"Is this a problem?",
a:["Only if the lookup is not always needed &mdash; an unconditional call wastes a round trip on conversations that do not require it",
"Yes, agents should never call tools on the first turn",
"No, consistent behaviour is always desirable",
"Yes, the first turn should always be a clarifying question"],
c:0,
w:["Correct. Judge it by whether the information is always needed.",
"First-turn tool calls are normal and often correct.",
"Consistency that is sometimes wasteful is not automatically desirable.",
"Clarifying questions are for genuine ambiguity, not a default opening."],
e:"An unconditional opening call is right when the information is always needed &mdash; verifying identity before anything else, for instance, which is often deliberately enforced. It is waste when many conversations do not need it. Measure how often the result is actually used; if it is low, make the call conditional on the request type rather than unconditional.",
k:"Unconditional opening calls are right when always needed, waste otherwise. Measure how often the result is used."},

{i:901,d:1,t:"1.2",s:"MAR",diff:2,
q:"A coordinator receives results and immediately dispatches synthesis without evaluating them.",
ask:"What has been skipped?",
a:["The evaluation step that would catch a thin or failed result before it propagates into the final output",
"Nothing; synthesis will catch problems",
"The aggregation step",
"The error-handling step"],
c:0,
w:["Correct. Evaluating intermediate results against criteria is what enables re-dispatch before synthesis.",
"Synthesis cannot manufacture evidence that was never gathered.",
"Dispatching to synthesis is the aggregation handoff.",
"Error handling applies to failures; this is about inadequate successes."],
e:"Successful-but-thin results are the case this catches. A subagent that reports success having found two sources where five were required has not failed, so error handling does not fire &mdash; only evaluation against explicit quality criteria catches it. Without that step, the coordinator is a router rather than a coordinator, and the thin result propagates into the report unremarked.",
k:"Evaluate intermediate results against criteria before synthesis. Thin-but-successful results bypass error handling."},

{i:902,d:1,t:"1.3",s:"AAT",diff:2,
q:"You want a subagent to work with a large input without that input entering the coordinator's context.",
ask:"How should the input reach it?",
a:["By reference the subagent can resolve with its own tools &mdash; a file path or an identifier &mdash; so the bulk loads in the subagent's isolated context",
"By being included in full in the delegation prompt after the coordinator loads it",
"By the coordinator summarising it first",
"It cannot be done; all input passes through the coordinator"],
c:0,
w:["Correct. A resolvable reference plus the tool to resolve it keeps the bulk out of the coordinator entirely.",
"That requires the coordinator to load it, which is what you are avoiding.",
"Summarising requires loading it, and loses fidelity.",
"References the subagent can resolve avoid this entirely."],
e:"An important refinement of &ldquo;subagents inherit nothing&rdquo;. A bare reference to a <em>conversation</em> resolves to nothing, because the subagent cannot see the conversation. A reference to something the subagent can independently resolve &mdash; a file path it can read, a document id it can fetch &mdash; works, provided you give it the tool and say what to do with it. That keeps large inputs entirely out of the coordinator's context.",
k:"Pass large inputs by resolvable reference (path, id) plus the tool to fetch it &mdash; never by loading it in the coordinator."},

{i:903,d:1,t:"1.4",s:"CSA",diff:2,
q:"A rule is enforced correctly but customers frequently hit it in situations the rule was not designed for.",
ask:"What should happen?",
a:["Revisit the rule's scope, since frequent legitimate collisions suggest it is drawn too broadly",
"Tighten the enforcement further",
"Instruct the agent to work around it",
"Escalate every collision"],
c:0,
w:["Correct. Frequent legitimate collisions are evidence about the rule, not about compliance.",
"Tightening a rule that already over-fires makes it worse.",
"Working around an enforced rule is not available and should not be.",
"Escalating a high-volume false collision floods human capacity."],
e:"Enforcement data is feedback about the rule. A control firing often on cases it was not designed for is drawn too broadly, and the remedy is to narrow its scope &mdash; not to add friction elsewhere. This is why logging allows as well as blocks matters: without the denominator you cannot tell whether a control fires rarely and correctly or often and wrongly.",
k:"Frequent legitimate collisions mean the rule is too broad. Log allows as well as blocks to see the rate."},

{i:904,d:1,t:"1.5",s:"AAT",diff:2,
q:"Two hooks both modify the same tool's arguments.",
ask:"What must be true for this to be safe?",
a:["The execution order must be defined and documented, and each hook's change must be compatible with the other's",
"Only one hook may modify arguments",
"They must run in parallel",
"They must modify different tools"],
c:0,
w:["Correct. Composition is safe when order is defined and the changes do not conflict.",
"Multiple hooks may legitimately modify arguments &mdash; injecting a tenant id and clamping a value, say.",
"Sequential composition is what argument modification requires.",
"Modifying the same tool is a normal case that must simply be handled carefully."],
e:"Argument-modifying hooks compose like middleware, and order-dependence is where the surprises live. Define and document the order, keep each hook's responsibility single, and prefer changes that commute where possible &mdash; injecting a field and clamping a different field are independent, while two hooks rewriting the same field are not. Test the composed pipeline, not just each hook alone.",
k:"Multiple argument-modifying hooks need a defined, documented order and non-conflicting changes. Test the composition."},

{i:905,d:1,t:"1.6",s:"CGC",diff:2,
q:"You are deciding how large each phase of a plan should be.",
ask:"What is the practical guide?",
a:["Each phase should end at a point where the result can be meaningfully verified",
"Each phase should take about the same amount of time",
"Each phase should touch about the same number of files",
"Each phase should be as small as possible"],
c:0,
w:["Correct. Verifiability at the boundary is what makes phasing valuable.",
"Equal duration is cosmetic and unrelated to the value of the checkpoint.",
"File count is a weak proxy for a meaningful boundary.",
"Minimal phases produce coordination overhead without better checkpoints."],
e:"The value of phasing comes entirely from the checkpoints, so the boundary should fall where you can actually check something &mdash; a passing test, a working endpoint, a compiling module. A phase that ends mid-way through a coherent change cannot be verified, so it provides no checkpoint and no safe rollback point. Verifiability, not size, is the criterion.",
k:"Phase boundaries fall where the result can be verified. Verifiability is the criterion, not size or duration."},

{i:906,d:1,t:"1.7",s:"CGC",diff:2,
q:"You want a resumed session to avoid repeating exploration it already did.",
ask:"What makes this work?",
a:["The findings from that exploration being written to a file the resumed session reads, rather than relying on them remaining in context",
"Resuming quickly after the previous session",
"A larger context window",
"Instructing it not to repeat work"],
c:0,
w:["Correct. Written findings are what survive reliably and can be read on demand.",
"Speed of resumption does not protect against compaction or staleness.",
"Window size does not help if the findings were summarised away.",
"An instruction cannot recover information that is no longer present."],
e:"Resumption restores the stored context, but that context may already have been compacted and, if files changed, may be stale. Externalised findings sidestep both: they are exact, they survive, and the resumed session can read just the parts it needs. That is why the findings file is the primary artefact and resumption is a convenience layered on top of it.",
k:"Externalised findings, not session resumption, are what reliably prevent repeated exploration."},

{i:907,d:1,t:"1.1",s:"AAT",diff:3,
q:"Your agent occasionally produces a tool call with an argument value that appears nowhere in the conversation.",
ask:"What is the most likely explanation and mitigation?",
a:["The model inferred a plausible value for a parameter it was not given; validate required parameters against what was actually supplied and reject inferred ones",
"The tool schema is malformed",
"The conversation history was truncated",
"The temperature is too high"],
c:0,
w:["Correct. The documentation notes models may infer reasonable values for missing required parameters.",
"A malformed schema would produce a validation error, not a plausible invented value.",
"Truncation is worth checking, but inference is the documented behaviour here.",
"Temperature affects variation, not whether a missing value is inferred."],
e:"This behaviour is documented: when a prompt lacks enough information to fill a required parameter, the model may ask &mdash; Opus more readily than Sonnet &mdash; but it may also infer a reasonable value, and the behaviour is not guaranteed. Defend by validating that supplied values trace to something the user or a tool actually provided, and by injecting system-determined values rather than requesting them.",
k:"Models may invent missing required parameters. Validate that values trace to real input; inject system-determined ones."},

{i:908,d:1,t:"1.2",s:"AAT",diff:2,
q:"A coordinator and its subagents all use the same model and the same system prompt.",
ask:"What has been lost?",
a:["Specialisation &mdash; the subagents have no distinct method, standards or focus, so delegation buys only context isolation",
"Nothing; uniformity simplifies maintenance",
"The ability to run in parallel",
"The ability to use different tools"],
c:0,
w:["Correct. Without distinct system prompts the agents differ only in context, not in expertise.",
"Uniformity is simpler and discards the main benefit of the pattern.",
"Parallelism is unaffected.",
"Tool lists can still differ, though a shared prompt undermines using them well."],
e:"Delegation buys two things: context isolation and specialisation. Identical system prompts keep the first and discard the second &mdash; which is sometimes an acceptable trade, if isolation is all you wanted. But a security reviewer and a test-coverage reviewer should look for different things, in different orders, with different severity thresholds, and that lives in the system prompt.",
k:"Delegation buys isolation and specialisation. Identical system prompts keep only the first."},

{i:909,d:1,t:"1.3",s:"MAR",diff:2,
q:"You want a subagent's output to be directly usable by the next subagent without the coordinator reformatting it.",
ask:"What makes this possible?",
a:["A shared output contract across the agents, so one agent's output is already a valid input shape for the next",
"Using the same model for both",
"Having them share a context",
"Running them in sequence"],
c:0,
w:["Correct. Compatible contracts at the boundaries are what let outputs flow without translation.",
"Model choice does not determine output shape.",
"Sharing context would break isolation and is not what makes shapes compatible.",
"Sequence is necessary for a handoff but does not make the shapes match."],
e:"Design the agent boundaries as an interface set rather than agent by agent. When the research agent's finding record is already the shape the synthesis agent consumes, the coordinator routes rather than translates &mdash; less code, fewer failure points, and no lossy reformatting step. It also makes the pipeline easier to extend, since a new agent slots in against a known contract.",
k:"Design agent boundaries as a shared contract set so outputs flow without coordinator translation."},

{i:910,d:1,t:"1.4",s:"AAT",diff:2,
q:"You are asked to enforce a rule that is genuinely ambiguous to state precisely.",
ask:"What does this suggest?",
a:["That it may not be enforceable in code &mdash; ambiguous rules are better expressed as prompt guidance with escalation for the unclear cases",
"That it should be enforced with a stricter interpretation",
"That it should be enforced with a looser interpretation",
"That it should be abandoned"],
c:0,
w:["Correct. Enforcement requires a decidable condition; an ambiguous rule is guidance plus an escalation path.",
"Picking a strict reading enforces something other than the rule.",
"The same problem in the other direction.",
"The rule may be valuable as guidance even if not enforceable."],
e:"Code enforcement requires a decidable predicate. When the rule cannot be stated as one, forcing it into a hook enforces some arbitrary interpretation rather than the rule &mdash; and does so invisibly. The right shape is prompt guidance for the judgement, with the genuinely unclear cases escalating: which is exactly the policy-gap trigger. Trying to enforce ambiguity produces false confidence.",
k:"Enforcement needs a decidable condition. Ambiguous rules become prompt guidance plus escalation for unclear cases."}
);
