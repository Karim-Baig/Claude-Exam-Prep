QB.push(
{i:431,d:1,t:"1.1",s:"CAP",diff:2,
q:"A multi-turn assistant must not lose track of a running total the user builds up over several turns (&ldquo;add 3 more&hellip; now remove 1&hellip; now double it&rdquo;).",
ask:"What is the most reliable design?",
a:["Maintain the total in application state updated by tool calls, and inject the current value into context each turn",
"Rely on the model to track the arithmetic across turns from the conversation history",
"Ask the user to restate the total periodically",
"Lower the temperature so arithmetic is more consistent"],
c:0,
w:["Correct. State that must be exact belongs in your code, with the current value surfaced to the model.",
"Mental arithmetic across a long history is exactly where small errors accumulate silently.",
"Pushes the system's bookkeeping onto the user.",
"Temperature does not make multi-step arithmetic reliable."],
e:"Anything that must be exact belongs in application state, not in the model's working memory. Have tool calls mutate the state, keep the authoritative value in your store, and inject the current value in the persistent facts block each turn. The model then reasons about intent (&ldquo;double it&rdquo;) while your code owns the arithmetic.",
k:"Exact state lives in your code; the model handles intent. Inject the current authoritative value each turn."},

{i:432,d:1,t:"1.1",s:"AAT",diff:3,
q:"You want to reduce the token cost of re-sending a large system prompt and 15 tool definitions on every loop iteration.",
ask:"What is the correct mechanism?",
a:["Prompt caching, marking the stable prefix with <code>cache_control</code> so repeat sends are billed at a large discount",
"Omitting the tool definitions after the first request",
"Truncating the system prompt on later iterations",
"Reducing the number of tools to shrink the payload"],
c:0,
w:["Correct. Caching is designed exactly for a large stable prefix re-sent many times.",
"Omitting definitions leaves the model with no tools &mdash; a correctness bug, not an optimisation.",
"Truncating the system prompt drops the durable instructions that hold the behaviour together.",
"Worth doing for selection reliability, but it is a design change rather than the mechanism for this cost problem."],
e:"The stateless API means the same prefix is sent on every iteration, which is expensive for large system prompts and tool schemas. Prompt caching addresses exactly this: mark the stable prefix with <code>cache_control</code>, and subsequent requests within the cache window are billed at a steep discount for that portion. Note the ordering constraint &mdash; the cached prefix must genuinely be a prefix, so put stable content first and volatile content last.",
k:"Large stable prefix re-sent often &rarr; prompt caching with <code>cache_control</code>. Stable content first, volatile content last."},

{i:433,d:1,t:"1.2",s:"MAR",diff:2,
q:"Your coordinator's prompt lists eight specialised subagents. Six are almost never used.",
ask:"What should you consider?",
a:["Whether those six should exist &mdash; unused agents add selection noise and maintenance cost for no benefit",
"Whether the coordinator needs a larger context window",
"Whether the six agents should be given more tools so they are chosen more often",
"Whether the coordinator should be instructed to use each agent equally"],
c:0,
w:["Correct. Rarely-used agents enlarge the choice space and complicate routing without contributing.",
"Window size is not the issue; the choice space is.",
"Adding capability to agents that are not needed makes routing worse, not better.",
"Forcing even distribution across agents is arbitrary and would produce bad routing by design."],
e:"The same logic that applies to tools applies to agents: every option in the choice space affects selection for the others. If six agents are almost never chosen, either their descriptions fail to convey when they apply &mdash; fixable &mdash; or they genuinely address cases that do not arise, in which case removing them sharpens routing for the two that matter. Investigate which before deleting.",
k:"Unused agents add selection noise. Either their descriptions fail, or they should not exist &mdash; find out which."},

{i:434,d:1,t:"1.3",s:"MAR",diff:2,
q:"You delegate to a subagent with a prompt containing the goal, the inputs and the output contract, but no quality criteria.",
ask:"What is the likely consequence?",
a:["The subagent produces output in the right shape but of unpredictable depth, since nothing defines what a good answer looks like",
"The subagent will refuse to start without quality criteria",
"The output will fail schema validation",
"The subagent will ask the coordinator for criteria"],
c:0,
w:["Correct. Format is specified but standard is not, so depth and thoroughness vary run to run.",
"It will not refuse; it will improvise a standard.",
"The shape is specified, so validation passes &mdash; the output is well-formed and possibly shallow.",
"More likely to proceed with an assumed standard than to ask."],
e:"The four elements of a delegation each prevent a different failure. Goal prevents doing the wrong task. Inputs prevent working from nothing. Output contract prevents shape mismatches. Quality criteria prevent shallow-but-well-formed answers &mdash; the failure mode that is hardest to detect, because the output looks correct. State the bar: how many sources, what depth, what must be covered, what constitutes done.",
k:"Delegations need quality criteria as well as a format contract. Shape-valid but shallow is the hardest failure to spot."},

{i:435,d:1,t:"1.4",s:"CSA",diff:2,
q:"Your escalation handoff includes the customer id, issue summary and recommended action, but human agents still ask the customer to repeat information.",
ask:"What is likely missing?",
a:["What the agent already verified and attempted, and with what result",
"The agent's confidence score for the case",
"The full conversation transcript",
"The time the escalation was created"],
c:0,
w:["Correct. Without the record of what was verified and tried, the human re-establishes it from scratch.",
"A confidence score gives the human nothing actionable about the case.",
"A raw transcript is the volume problem the structured handoff exists to solve.",
"A timestamp is operational metadata, not case content."],
e:"The complete handoff has six parts: identifier, issue summary, <strong>verified facts</strong>, <strong>what was attempted and the outcome</strong>, the reason for escalation, and a recommended action. The two emphasised parts are the ones most often omitted and the ones that save the human the most time &mdash; they prevent both re-asking the customer and repeating approaches that already failed.",
k:"Handoff: id &middot; issue &middot; verified facts &middot; what was tried and the result &middot; reason &middot; recommendation."},

{i:436,d:1,t:"1.5",s:"AAT",diff:3,
q:"You need a hook to run only for a specific tool, not for every tool call.",
ask:"What is the design consideration?",
a:["Scope the hook to the tool it concerns, so unrelated calls are not slowed or affected by logic that does not apply to them",
"Hooks always run for every tool and cannot be scoped",
"Run the hook for all tools and have it return early for irrelevant ones",
"Merge all tool-specific logic into one large hook"],
c:0,
w:["Correct. Scoping keeps each hook's responsibility clear and avoids taxing unrelated calls.",
"Scoping by tool is a normal and expected pattern.",
"An early return works but still runs the hook on every call and encourages logic to accumulate in one place.",
"A single large hook is exactly the unmanageable-composition problem to avoid."],
e:"Treat hooks as middleware with single responsibilities, scoped to what they concern. A refund-threshold check should evaluate on refund calls, not on every read. Scoping keeps each hook small and testable, keeps the execution order comprehensible, and avoids the fourteen-overlapping-hooks situation where nobody can predict what will happen to a given call.",
k:"Scope hooks to the tools they concern. One responsibility each, small and testable, with a documented order."},

{i:437,d:1,t:"1.6",s:"MAR",diff:2,
q:"A coordinator must research a topic with three obvious sub-domains and one that only becomes apparent after initial reading.",
ask:"Which decomposition strategy fits?",
a:["Adaptive &mdash; dispatch the three known sub-domains, then generate a fourth subtask once the initial results reveal it",
"Fixed &mdash; dispatch the three known sub-domains and stop",
"Fixed &mdash; guess a fourth sub-domain up front",
"Dispatch one agent for the whole topic and let it decide"],
c:0,
w:["Correct. Revising the plan from intermediate results is the definition of adaptive decomposition.",
"Stops at the three known ones and misses the fourth &mdash; the coverage failure the exam repeatedly illustrates.",
"A guessed sub-domain is as likely to be wrong as right and wastes a dispatch.",
"Forfeits parallelism and gives one agent an unbounded scope."],
e:"This is the hybrid case, and it is common: partly knowable up front, partly discoverable. Dispatch what you know, then treat the first round's results as input to a second decomposition. That combines the efficiency of parallel dispatch with the coverage of adaptive planning &mdash; and the coverage check at synthesis catches anything both rounds missed.",
k:"Hybrid decomposition: dispatch what is known, then re-decompose from what the first round reveals."},

{i:438,d:1,t:"1.7",s:"AAT",diff:2,
q:"An agent's workflow has 12 phases. You checkpoint after each one but store only a boolean &ldquo;complete&rdquo; flag.",
ask:"What is missing?",
a:["The phase outputs, or references to them, so a resumed run can use what earlier phases produced",
"A timestamp for each phase",
"The number of retries per phase",
"The model version used per phase"],
c:0,
w:["Correct. Knowing a phase completed is useless if its output is gone and phase eight needs it.",
"Useful operationally but not required for resumption.",
"Diagnostic information, not state needed to resume.",
"Useful for reproducibility but not for resuming."],
e:"Checkpoints must capture what downstream phases consume. Store the outputs, or durable references to them (file paths, object keys), alongside the completion markers. Otherwise a restart at phase eight has no inputs and must re-run phases one to seven anyway &mdash; which defeats the checkpointing entirely.",
k:"Checkpoint outputs, not just completion flags. Downstream phases need what earlier ones produced."},

{i:439,d:2,t:"2.1",s:"SDE",diff:2,
q:"A tool's description says it accepts &ldquo;a document&rdquo;. In practice it accepts a file path, not file contents.",
ask:"What is the consequence?",
a:["The agent will sometimes pass contents instead of a path, causing avoidable failures the description could have prevented",
"The API will reject the ambiguous description",
"The tool will silently succeed with either input",
"The model will always infer the correct form from the parameter name"],
c:0,
w:["Correct. Ambiguous input specification produces malformed calls that a precise description would eliminate.",
"Descriptions are not validated for precision.",
"It will fail on contents, since it expects a path.",
"Parameter names help but are not a substitute for stating the format explicitly."],
e:"Input format specification is one of the elements a good description must contain, and it belongs in both the description and the schema. Say &ldquo;accepts an absolute file path, for example <code>/data/invoices/2026-03/inv-8823.pdf</code>&rdquo; rather than &ldquo;a document&rdquo;, and constrain it in the schema where you can. An example in the description is worth several sentences of explanation.",
k:"State input formats precisely with an example, in both description and schema. &ldquo;A document&rdquo; is not a format."},

{i:440,d:2,t:"2.2",s:"SDE",diff:2,
q:"A tool returns success with an empty object when the requested record does not exist.",
ask:"What is the problem?",
a:["An empty object is ambiguous &mdash; it cannot be distinguished from a record that exists but has no populated fields",
"Empty objects are not valid JSON",
"The tool should have thrown an exception",
"The agent will always interpret an empty object as an error"],
c:0,
w:["Correct. Absence and emptiness are different states and need distinguishable representations.",
"Empty objects are perfectly valid JSON.",
"Not-found is often a legitimate outcome rather than an error condition.",
"It may interpret it either way, which is precisely the ambiguity problem."],
e:"Represent states explicitly rather than by convention. A not-found result should say so &mdash; <code>{\"found\": false}</code> or a null record with a clear status &mdash; so the agent can distinguish &ldquo;no such record&rdquo; from &ldquo;a record with no data&rdquo;. This is the same discipline as distinguishing an empty search result from an access failure: make the states nameable rather than inferable.",
k:"Make states explicit: found versus not-found versus error. Never encode meaning in emptiness."},

{i:441,d:2,t:"2.3",s:"CSA",diff:2,
q:"An agent has both <code>send_email</code> and <code>send_sms</code>. Business rules say SMS is only for urgent notifications.",
ask:"Where should that rule live?",
a:["In a <code>PreToolUse</code> hook that blocks non-urgent SMS, with the criteria for urgency also stated in the tool description so the agent understands the rule",
"In the tool description only",
"In the system prompt only",
"In a post-send audit that flags misuse"],
c:0,
w:["Correct. Enforcement in the hook, understanding in the description &mdash; the two layers doing their respective jobs.",
"A description guides selection but cannot block a call.",
"A prompt rule is guidance with the same limitation.",
"After-the-fact detection cannot unsend an SMS."],
e:"The layered answer is the recurring correct shape: a hook guarantees the boundary, and the description or prompt teaches the rule so the agent behaves well by default and can explain outcomes. Note that cost and customer-experience rules like this sit close to the line &mdash; the deciding factor is that an SMS is outward-facing and irreversible once sent.",
k:"Layer it: hook for the guarantee, description or prompt for understanding. Outward-facing sends are irreversible."},

{i:442,d:2,t:"2.4",s:"DPT",diff:2,
q:"A community MCP server exposes 30 tools; your team needs 4 of them.",
ask:"What should you consider?",
a:["Whether to wrap or restrict the surface, since 30 tools will degrade selection reliability for every agent that can see them",
"Whether the server supports environment variable substitution",
"Whether 30 tools exceeds a platform limit",
"Whether the server should be moved to user scope"],
c:0,
w:["Correct. All connected servers' tools are simultaneously available, so a 30-tool server taxes selection everywhere.",
"Substitution is about secrets, not surface area.",
"There is no such hard limit; the effect is a quality curve.",
"Scope determines who is affected, not how large the surface is."],
e:"Community servers are preferred for standard integrations, but a large one has a real cost: every one of its tools joins the choice space for any agent that can see it. Options include wrapping it in a narrow internal server exposing only the four operations you need, or scoping the connection to the specific agent that requires it rather than connecting it globally.",
k:"Large MCP servers tax selection for every agent. Wrap to narrow the surface, or scope the connection per agent."},

{i:443,d:2,t:"2.5",s:"DPT",diff:2,
q:"You need to know whether a specific configuration key exists anywhere in a repository, and you do not need its value.",
ask:"Which tool and mode is most efficient?",
a:["<code>Grep</code> in a files-with-matches mode, returning paths only",
"<code>Grep</code> returning full matching lines with context",
"<code>Glob</code> for configuration files, then <code>Read</code> each",
"<code>Read</code> the most likely configuration file and check"],
c:0,
w:["Correct. Asking only for the file list returns the minimum information that answers the question.",
"Content and context are more output than the question needs.",
"Reads every configuration file to answer an existence question.",
"Guessing which file to read may miss occurrences elsewhere."],
e:"Match the output mode to the question. Existence questions need paths; understanding questions need matching lines with context; detailed questions need targeted reads. Requesting more output than the question requires is the same context waste as returning 40 fields when 5 are needed &mdash; applied to your own tool usage rather than to a tool's design.",
k:"Match search output mode to the question: paths for existence, lines for understanding, reads for detail."},

{i:444,d:3,t:"3.1",s:"CGC",diff:2,
q:"A monorepo's root CLAUDE.md contains conventions for a package that was removed six months ago.",
ask:"Why does this matter?",
a:["Stale instructions consume context on every request and can cause the model to apply conventions that no longer exist anywhere",
"Stale instructions cause a validation error at load time",
"Stale instructions are ignored automatically",
"Stale instructions only matter if a developer reads them"],
c:0,
w:["Correct. They cost context and can actively mislead, since the model has no way to know the package is gone.",
"There is no validation of CLAUDE.md content against the repository.",
"Nothing detects staleness automatically.",
"CLAUDE.md is read by the agent, so it affects behaviour whether or not a human reads it."],
e:"CLAUDE.md needs the same maintenance discipline as code. Stale entries dilute the instructions that still matter and can cause misapplication &mdash; the model may look for a package that no longer exists or apply its conventions to something else. Prune as part of the change that removes the thing, and review the file periodically for entries that no longer earn their context.",
k:"Prune CLAUDE.md when the code changes. Stale entries dilute live ones and can actively mislead."},

{i:445,d:3,t:"3.2",s:"CGC",diff:2,
q:"Two developers create skills with the same name &mdash; one in the project's <code>.claude/skills/</code> and one in their own <code>~/.claude/skills/</code>.",
ask:"What is the practical problem?",
a:["It becomes ambiguous which one runs, so the personal variant should use a distinct name",
"The API will reject duplicate skill names",
"The project skill is always deleted",
"Skills cannot exist at both levels"],
c:0,
w:["Correct. The blueprint recommends personal variants under different names precisely to avoid this ambiguity.",
"There is no API-level validation of skill names.",
"Nothing is deleted.",
"Both levels are supported &mdash; that is why naming matters."],
e:"Distinct names for personal variants make invocation unambiguous and, just as importantly, make it obvious to the developer which version they are running. This matters when the personal variant has different defaults or looser tool restrictions &mdash; silently shadowing the team's version is how a developer ends up with behaviour nobody else can reproduce.",
k:"Name personal skill variants distinctly. Shadowing a team skill creates behaviour others cannot reproduce."},

{i:446,d:3,t:"3.3",s:"CGC",diff:2,
q:"A rule file has <code>paths: [\"**/*\"]</code> in its frontmatter.",
ask:"What is the effect?",
a:["It matches everything, so the rule always loads &mdash; equivalent to putting it in CLAUDE.md and losing the benefit of scoping",
"It disables the rule entirely",
"It causes a configuration error",
"It matches only files in the repository root"],
c:0,
w:["Correct. A universal glob is a no-op for scoping purposes.",
"It loads always, not never.",
"A valid glob does not error.",
"<code>**/*</code> matches at all depths, not just the root."],
e:"A universal glob signals that the rule is not actually path-specific and belongs in CLAUDE.md or a rules file without <code>paths</code>. Keeping it as a nominally-scoped rule is misleading &mdash; a reader assumes it is conditional when it is not. Either narrow the glob to the files it genuinely concerns, or move the content to where always-on conventions live.",
k:"<code>paths: [\"**/*\"]</code> is a no-op. If a rule always applies, put it where always-on conventions belong."},

{i:447,d:3,t:"3.4",s:"CGC",diff:2,
q:"During planning mode, Claude proposes an approach you know is wrong because of a constraint you did not mention.",
ask:"What is the right response?",
a:["State the constraint and have the plan revised before any execution begins",
"Let it execute and correct the result afterwards",
"Abandon planning mode and execute your own approach directly",
"Accept the plan and add the constraint as a later phase"],
c:0,
w:["Correct. Catching a wrong approach at the plan stage is precisely the value planning mode provides.",
"Executing a known-wrong approach guarantees rework.",
"Discards a plan that is largely useful over one correctable constraint.",
"Bolting the constraint on later leaves the plan built around a false premise."],
e:"Planning mode is a cheap place to be wrong. A constraint surfaced at plan time costs one revision; the same constraint surfaced at file 30 costs a partial rollback. This is also a reminder about the interview pattern &mdash; if you routinely find that plans miss constraints you knew about, have Claude interview you before planning so those constraints are elicited rather than discovered.",
k:"Correct plans, not implementations. If plans keep missing known constraints, interview before planning."},

{i:448,d:3,t:"3.5",s:"CGC",diff:2,
q:"You provide feedback: <em>the error handling needs work.</em> The next iteration changes error handling in ways you did not want.",
ask:"What was wrong with the feedback?",
a:["It named an area without specifying what was wrong or what correct behaviour looks like",
"It was too short and should have been a paragraph",
"It should have been given as a code comment",
"It should have been split across three separate messages"],
c:0,
w:["Correct. Naming an area is not the same as stating a defect and its correction.",
"Length is not the issue; specificity is. A long vague note is no better.",
"The channel is irrelevant to the missing content.",
"Splitting vague feedback produces three vague messages."],
e:"Effective iteration feedback names the specific defect and the expected behaviour, ideally as a concrete case: &ldquo;when the upstream call times out, this currently retries indefinitely; it should retry twice then return a non-retryable error with the attempted operation named&rdquo;. That is checkable, and it can become a test. &ldquo;Needs work&rdquo; invites a redesign you did not ask for.",
k:"Feedback must name the defect and the expected behaviour, ideally as a concrete case. Naming an area invites unwanted redesign."},

{i:449,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI review posts 40 comments on a 200-line pull request.",
ask:"What is the most likely problem?",
a:["The criteria are too broad, producing low-value findings that will train developers to ignore the bot",
"The pull request is genuinely of very poor quality",
"The model is too capable for this task",
"The review should have been split into more passes"],
c:0,
w:["Correct. A comment every five lines almost always indicates over-broad criteria rather than exceptionally bad code.",
"Possible, but the base rate strongly favours over-broad criteria.",
"Capability does not cause volume; criteria do.",
"More passes would produce more comments, not fewer."],
e:"Review volume is a criteria problem. Define what to report &mdash; bugs, security issues, incorrect behaviour &mdash; and what to ignore &mdash; minor style, subjective preferences, anything the linter already covers. Measure precision per category using the recorded <code>detected_pattern</code>, and disable categories with high false-positive rates while you sharpen them. Trust, once lost, is expensive to rebuild.",
k:"High comment volume = over-broad criteria. Define what to ignore as explicitly as what to report."},

{i:450,d:4,t:"4.1",s:"CCI",diff:2,
q:"You want the review bot to report incorrect behaviour but ignore subjective style preferences.",
ask:"How should this be expressed?",
a:["Explicitly list both what to report and what to ignore, with an example of each",
"Instruct the bot to use good judgement about what matters",
"Set a severity threshold and report only above it",
"Ask the bot to report everything and let developers filter"],
c:0,
w:["Correct. Stating the exclusions explicitly is as important as stating the inclusions.",
"&ldquo;Good judgement&rdquo; is the vague instruction the blueprint contrasts with explicit criteria.",
"A threshold assumes severity is already assigned consistently, which is the thing needing definition.",
"Pushing filtering onto developers is how trust erodes."],
e:"The blueprint's phrasing is to define review criteria as what to report (bugs, security) versus what to ignore (minor style). Exclusions do heavy lifting because they remove whole categories of noise, and an example of each makes the boundary concrete. This is the same contrastive structure that makes few-shot effective &mdash; show both sides of the line.",
k:"Define what to ignore as explicitly as what to report, with an example of each."},

{i:451,d:4,t:"4.2",s:"SDE",diff:2,
q:"You have limited context budget and must choose between four few-shot examples and a longer instruction section.",
ask:"Which generally gives more value for the tokens?",
a:["The few-shot examples, since they demonstrate format and boundary handling more precisely than prose",
"The longer instructions, since they cover more cases explicitly",
"Neither &mdash; both are equally effective per token",
"The longer instructions, since examples risk over-fitting"],
c:0,
w:["Correct. Few-shot is identified as the most effective method for consistent, actionable output.",
"Prose covers more cases nominally while specifying each less precisely.",
"They are not equivalent; examples are stronger for format and boundaries.",
"Over-fitting is a real risk, managed by varying the examples &mdash; not a reason to prefer prose."],
e:"For a fixed token budget, targeted examples usually beat additional prose, because they resolve ambiguities that prose leaves open. The complementarity still matters &mdash; use explicit criteria for general decision rules that would need too many examples to convey, and examples for formats and boundary cases. Vary the examples' content to avoid teaching a region rather than a rule.",
k:"Per token, examples beat prose for format and boundaries; criteria beat examples for general decision rules."},

{i:452,d:4,t:"4.3",s:"SDE",diff:3,
q:"Your extraction schema has 60 fields. Accuracy on the last 20 is noticeably worse than on the first 20.",
ask:"What should you consider?",
a:["Splitting the extraction into multiple focused calls, since a very large schema dilutes attention across fields",
"Reordering the fields alphabetically",
"Increasing <code>max_tokens</code> so all 60 fields fit",
"Adding more few-shot examples covering all 60 fields"],
c:0,
w:["Correct. The positional pattern points at attention dilution across a large output, which splitting addresses.",
"Alphabetical order is arbitrary and does not address dilution.",
"Output limits would truncate rather than degrade quality on later fields.",
"Examples covering 60 fields would be enormous and would dilute attention further."],
e:"The same attention-dilution effect that degrades a 3,000-line code review degrades a 60-field extraction, and the positional signature is the giveaway. Split into focused calls &mdash; header fields, line items, totals and tax &mdash; each with its own schema and examples. This also gives you per-group accuracy metrics, which supports the field-level calibration that automation decisions require.",
k:"Very large schemas dilute attention; later fields suffer. Split into focused calls and measure accuracy per group."},

{i:453,d:4,t:"4.4",s:"SDE",diff:2,
q:"Your retry succeeded, but the corrected extraction differs from the first attempt in fields that were not part of the validation error.",
ask:"What should you do?",
a:["Validate the whole corrected extraction again, since a retry can change fields that were previously correct",
"Accept the corrected extraction, since the reported error is now fixed",
"Merge the corrected field into the original extraction and keep the rest",
"Retry a third time to see which version is stable"],
c:0,
w:["Correct. A retry regenerates the whole output, so every field must be re-validated.",
"Fixing the reported error says nothing about fields that changed silently alongside it.",
"Merging assumes the original values were correct and the new ones are not &mdash; unjustified either way, and it can create an internally inconsistent record.",
"A third sample tells you about variance, not correctness."],
e:"Retries produce a new complete output, not a patch. Re-run the full validation suite on every attempt, including the checks that passed before. If fields drift between attempts, that itself is a signal worth investigating &mdash; typically an ambiguous source region, which is exactly the kind of case that should be routed to human review rather than automated.",
k:"Retries regenerate everything. Re-validate the whole output; unexplained drift between attempts is a review signal."},

{i:454,d:4,t:"4.5",s:"SDE",diff:2,
q:"Which workload is the clearest fit for the Message Batches API?",
ask:"Which one?",
a:["Re-classifying two years of archived support tickets for an analytics project due next week",
"Generating a response in a live chat interface",
"Validating a pull request before merge",
"Extracting fields from a document a user just uploaded and is waiting on"],
c:0,
w:["Correct. Large volume, no latency sensitivity, generous deadline &mdash; the canonical batch case.",
"A live interface requires immediate responses.",
"A pre-merge check is blocking on a developer.",
"A user waiting on an upload requires bounded latency."],
e:"The single question that decides this is whether anything is blocked on the result. Backfills, archive reprocessing, overnight reports and audits are not blocked, so they take the roughly 50% saving and the up-to-24-hour window. Anything a person or a pipeline is waiting on goes synchronous, regardless of cost.",
k:"Batch when nothing is blocked and the deadline is generous. Synchronous whenever someone or something is waiting."},

{i:455,d:4,t:"4.6",s:"CCI",diff:3,
q:"You run per-file review passes concurrently and a cross-file pass afterwards. The cross-file pass frequently reports issues the per-file passes already fixed context for.",
ask:"What should the cross-file pass receive?",
a:["The changed symbols, their consumers, and the per-file findings, so it can focus on interactions rather than re-deriving local issues",
"All the diffs concatenated with no additional structure",
"Only the file names that changed",
"The entire repository for maximum context"],
c:0,
w:["Correct. Give it the interaction graph plus what has already been found, so its remit is genuinely distinct.",
"Concatenated diffs recreate the attention dilution the per-file split was meant to solve.",
"Names without content give it nothing to analyse.",
"Whole-repository input is enormous and mostly irrelevant to this change."],
e:"Passes in a multi-pass architecture need distinct remits <em>and</em> distinct inputs. The cross-file pass should receive the changed definitions, the call sites that consume them, and the per-file findings so it does not repeat them. That makes its output genuinely additive &mdash; contract mismatches, broken dataflow, migrations inconsistent with models &mdash; rather than a noisier restatement of what is already known.",
k:"Give each pass a distinct remit and a distinct input. Cross-file gets the interaction graph plus prior findings."},

{i:456,d:5,t:"5.1",s:"CAP",diff:2,
q:"A conversation has run for 80 turns. You need to keep it going without losing the constraints established early.",
ask:"Which combination is most effective?",
a:["A persistent facts and preferences block re-injected each turn, plus summarisation of the conversational middle",
"Sending the full history unchanged indefinitely",
"Summarising everything including the established constraints",
"Truncating to the most recent 20 turns"],
c:0,
w:["Correct. Protect the load-bearing facts explicitly, and compress only the conversational material around them.",
"Eventually exceeds the window, and the middle degrades regardless.",
"Summarising the constraints is exactly how they get lost.",
"Position-based truncation discards early constraints by construction."],
e:"The durable pattern separates two kinds of content. Load-bearing facts and constraints go in a small structured block your code maintains and re-injects in a stable position &mdash; never summarised. The conversational flow around them can be compressed freely, because its details matter less. Truncation and blanket summarisation both fail because they treat all content as equivalent.",
k:"Protect facts and constraints in a persistent block; summarise only the conversational material around them."},

{i:457,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent is uncertain whether a request falls within policy, but the policy is actually clear &mdash; the agent simply has not retrieved it.",
ask:"What should it do?",
a:["Retrieve the policy and apply it, since the uncertainty is about information it can obtain rather than about authority",
"Escalate, since it is uncertain",
"Guess based on similar cases it has seen",
"Ask the customer what the policy says"],
c:0,
w:["Correct. Retrievable uncertainty is resolved by retrieving; it is not an authority problem.",
"Escalating what a lookup resolves depresses first-contact resolution for no reason.",
"Guessing when the authoritative answer is available is indefensible.",
"Customers do not know the company's internal policy."],
e:"Distinguish the kinds of uncertainty. <strong>Retrievable</strong> &mdash; the answer exists and the agent can fetch it &mdash; so fetch it. <strong>Resolvable by asking</strong> &mdash; multiple customer matches, an ambiguous fact &mdash; so ask. <strong>About authority</strong> &mdash; policy is silent or ambiguous &mdash; so escalate. Only the third is an escalation trigger.",
k:"Retrievable &rarr; retrieve. Resolvable by asking &rarr; ask. About authority &rarr; escalate."},

{i:458,d:5,t:"5.3",s:"AAT",diff:2,
q:"Your coordinator receives errors from subagents in five different formats because each subagent was written by a different team.",
ask:"What should you standardise?",
a:["A common error contract &mdash; category, retryability, message, partial results &mdash; that every subagent returns",
"The programming language each team uses",
"The number of retries each subagent performs",
"The model each subagent uses"],
c:0,
w:["Correct. A shared error contract is what lets the coordinator apply one recovery policy across all subagents.",
"Implementation language is irrelevant to the interface.",
"Retry counts can legitimately differ by operation; the contract is what must be shared.",
"Model choice is an independent decision per subagent."],
e:"Interfaces are where standardisation pays. A common error contract &mdash; <code>errorCategory</code>, <code>isRetryable</code>, a message, and any partial results &mdash; means the coordinator implements one recovery policy rather than five. Teams stay free to implement their subagents however they like behind that contract, which is exactly the right place to draw the line.",
k:"Standardise the error contract across subagents, not their implementations. One contract, one recovery policy."},

{i:459,d:5,t:"5.4",s:"DPT",diff:2,
q:"You must investigate a system spanning four repositories.",
ask:"How should context be managed?",
a:["Delegate a subagent per repository with a specific question each, and keep the cross-repository synthesis in the main session",
"Clone all four and read them in one session",
"Investigate one repository fully, then the next, in the same session",
"Ask the developer to summarise each repository first"],
c:0,
w:["Correct. Isolate the verbose per-repository exploration; keep the integration in the main session.",
"Four repositories' worth of exploration in one context is guaranteed degradation.",
"Sequential in one session still accumulates all four explorations.",
"Offloads the work to a human and may miss what the investigation is looking for."],
e:"Multi-repository investigation is the archetypal case for delegation: the work is independent per repository (so it parallelises) and verbose (so isolation protects the main context). Give each subagent a specific question and a tight return contract, and reserve the main session for the cross-repository synthesis that genuinely needs all four answers together.",
k:"Per-repository subagents with specific questions; cross-repository synthesis in the main session."},

{i:460,d:5,t:"5.5",s:"SDE",diff:2,
q:"You want to detect when extraction quality degrades after a model or prompt change.",
ask:"What is required?",
a:["A fixed labelled evaluation set run before and after the change, compared at field and document-type level",
"Monitoring the volume of extractions processed",
"Tracking the average confidence score reported by the model",
"Counting how many extractions pass schema validation"],
c:0,
w:["Correct. A fixed labelled set is the only way to compare quality across a change.",
"Volume says nothing about quality.",
"Self-reported confidence can stay high while accuracy falls &mdash; it is not ground truth.",
"Schema-pass rate measures syntax, not correctness."],
e:"Regression detection needs ground truth and a fixed comparison set, or you cannot separate a quality change from an input-mix change. Compare at field and document-type level, because an aggregate can stay flat while one segment degrades badly. This is the same segmentation discipline that governs automation decisions, applied to change management.",
k:"Detect regressions with a fixed labelled set, compared at field and type level. Confidence and pass rates are not ground truth."},

{i:461,d:5,t:"5.6",s:"MAR",diff:2,
q:"A finding is supported by three sources that all trace back to the same original study.",
ask:"How should this be represented?",
a:["Note that the three citations share one underlying source, so the claim is not independently corroborated",
"Present it as strongly supported by three independent sources",
"Cite only one of the three",
"Discard the claim as unreliable"],
c:0,
w:["Correct. Apparent corroboration that collapses to one source is a real and common trap; say so.",
"Treating three derivative citations as independent overstates the evidence substantially.",
"Citing one loses the useful information that the claim propagated widely.",
"A single well-conducted study is evidence; discarding it is an overreaction."],
e:"Provenance is about tracing to the origin, not counting citations. Three articles reporting the same study is one piece of evidence, and presenting it as three overstates confidence in a way that matters for decisions. Recording claim-to-source mappings with quotes makes this detectable &mdash; identical figures with identical phrasing across sources is the tell.",
k:"Trace citations to their origin. Three articles reporting one study is one piece of evidence, not three."},

{i:462,d:1,t:"1.2",s:"AAT",diff:2,
q:"Which responsibility belongs to the coordinator rather than to a subagent?",
ask:"Which one?",
a:["Deciding how to decompose the task and which specialists to dispatch",
"Performing the domain analysis within an assigned subtopic",
"Searching for sources on an assigned question",
"Writing the prose of an assigned report section"],
c:0,
w:["Correct. Decomposition and delegation are the coordinator's defining responsibilities.",
"Analysis within an assigned scope is specialist work.",
"Searching is specialist work.",
"Authoring is specialist work."],
e:"Coordinator responsibilities: task decomposition, delegation, result aggregation, error handling, routing and dynamic selection of subagents. Everything domain-specific belongs to a specialist. The recurring exam distractor is a coordinator doing specialist work &mdash; concatenating results instead of dispatching synthesis, or analysing instead of delegating.",
k:"Coordinator: decompose, delegate, aggregate, handle errors, route. Specialists do the domain work."},

{i:463,d:2,t:"2.3",s:"AAT",diff:2,
q:"When is <code>tool_choice: \"none\"</code> useful?",
ask:"Which case fits?",
a:["A turn where you want a purely textual summary or explanation with no side effects",
"A turn where you want to guarantee structured output",
"A turn where the model should pick among several tools",
"Every turn of a conversational agent loop"],
c:0,
w:["Correct. <code>none</code> forbids tool use, which suits an explanation turn with no actions.",
"That requires <code>any</code> or a forced tool.",
"That is <code>auto</code> or <code>any</code>.",
"A loop under <code>none</code> could never take any action."],
e:"<code>none</code> is the least-discussed of the four values but has a genuine use: a turn where you want reasoning or explanation and want to guarantee no side effects. It also keeps the tool definitions in context for later turns, unlike simply omitting the <code>tools</code> parameter. Vary <code>tool_choice</code> per request as the workflow requires.",
k:"<code>none</code> forbids tools for a turn &mdash; useful for side-effect-free explanation while keeping definitions in context."},

{i:464,d:3,t:"3.1",s:"CGC",diff:2,
q:"Which of these belongs in <code>.claude/rules/</code> rather than in the root CLAUDE.md?",
ask:"Which one?",
a:["A detailed convention that applies only to Terraform files",
"The project's overall architectural philosophy",
"The command to run the test suite",
"A one-line statement of the primary language used"],
c:0,
w:["Correct. A convention scoped to one file type is exactly what path-scoped rules are for.",
"Project-wide philosophy applies everywhere and belongs in CLAUDE.md.",
"A universally useful fact that belongs in CLAUDE.md.",
"A universally applicable one-liner; scoping it would add machinery for nothing."],
e:"The decision rule is applicability. Universal, short and always relevant &rarr; CLAUDE.md. Scoped to particular files or file types, and detailed enough that it would dilute other instructions &rarr; a path-scoped rule. Detailed but universally applicable content can live in a rules file without <code>paths</code>, purely for organisational clarity.",
k:"Universal and short &rarr; CLAUDE.md. Scoped and detailed &rarr; path-scoped rule. Detailed but universal &rarr; unscoped rule file."},

{i:465,d:4,t:"4.3",s:"SDE",diff:2,
q:"A field should contain one of three values, but occasionally the model emits a near-synonym instead.",
ask:"Which schema feature prevents this?",
a:["An <code>enum</code> constraining the field to exactly those three values",
"A <code>description</code> listing the three acceptable values",
"A <code>pattern</code> regular expression",
"Marking the field as <code>required</code>"],
c:0,
w:["Correct. An enum enforces membership structurally rather than requesting it.",
"A description is guidance; the model can still emit something else.",
"A pattern could work but is brittle and harder to read than an enum for a fixed value set.",
"<code>required</code> ensures presence, not value membership."],
e:"Use the schema feature that expresses the actual constraint. <code>enum</code> for a fixed value set, <code>type</code> for the data type, <code>format</code> for structured strings such as dates, ranges for numeric bounds, <code>required</code> for presence. Every constraint expressed structurally is one your downstream code no longer has to defend against &mdash; and remember to include <code>other</code> and <code>unclear</code> escape values so the enum does not force bad fits.",
k:"<code>enum</code> enforces value membership; <code>required</code> only enforces presence. Include escape values in every enum."}
);
