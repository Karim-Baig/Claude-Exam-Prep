QB.push(
{i:396,d:1,t:"1.1",s:"CSA",diff:2,
q:"Your agent loop executes a tool, appends the result, and re-sends. On the next response Claude produces text explaining what it would do rather than calling the next tool. Inspection shows your code appended the <code>tool_result</code> but forgot to append the assistant message containing the original <code>tool_use</code> block.",
ask:"Why does this break the loop?",
a:["A <code>tool_result</code> with no preceding <code>tool_use</code> in the history is orphaned, so the conversation shape is invalid and the model loses the thread of what it requested",
"The <code>tool_result</code> must always be the first message in the conversation",
"Assistant messages are optional in the history and this is unrelated",
"The model requires the tool definitions to be re-sent, which is the actual missing piece"],
c:0,
w:["Correct. The pairing between the assistant's <code>tool_use</code> block and the user's <code>tool_result</code> is what makes the exchange coherent.",
"There is no such ordering requirement.",
"Assistant messages are an essential part of the history &mdash; omitting them destroys the record of what the model decided.",
"Tool definitions do need re-sending, but the described defect is the missing assistant turn."],
e:"The loop's append contract has two halves and both are mandatory: the assistant message containing the <code>tool_use</code> block, then the user message containing the matching <code>tool_result</code>. Omitting the assistant turn leaves a result referencing a request that is not in the history, which is both structurally invalid and semantically confusing &mdash; the model cannot see that it asked for this.",
k:"Append both halves every iteration: the assistant <code>tool_use</code> turn <em>and</em> the user <code>tool_result</code> turn."},

{i:397,d:1,t:"1.1",s:"AAT",diff:3,
q:"Your agent handles a request that requires six sequential tool calls. Latency is 18 seconds. Analysis shows each model round trip costs about 2.5 seconds and each tool executes in under 100ms.",
ask:"Where should optimisation effort go?",
a:["Reducing the number of model round trips &mdash; by batching independent calls or restructuring the task &mdash; since they dominate the latency",
"Optimising the tools, since six of them run per request",
"Increasing the tool timeout so calls do not retry",
"Adding a caching layer in front of the tools"],
c:0,
w:["Correct. Six round trips at 2.5 seconds is 15 of the 18 seconds; the tools contribute under a second in total.",
"Optimising 600ms of tool time cannot meaningfully move an 18-second total.",
"Timeouts are not the bottleneck; nothing is timing out.",
"Caching saves under 100ms per call at best."],
e:"Latency in agentic systems is dominated by model round trips, not tool execution. Attack turn count: emit independent calls in parallel within a single turn, restructure so fewer sequential decisions are required, or move deterministic steps out of the loop into code. The arithmetic in this question is the point &mdash; always locate the dominant term before optimising.",
k:"Agent latency &asymp; round trips &times; per-turn latency. Optimise turn count first; tool execution is usually noise."},

{i:398,d:1,t:"1.2",s:"MAR",diff:2,
q:"Your coordinator gives each of four subagents the same broad research prompt and then deduplicates the results.",
ask:"What is the main inefficiency?",
a:["Four agents doing substantially the same work, so you pay four times for roughly one agent's coverage",
"Deduplication is computationally expensive",
"The subagents will produce contradictory results",
"Four is too many subagents for one coordinator"],
c:0,
w:["Correct. Identical prompts produce overlapping work, so the parallelism buys little additional coverage.",
"Deduplication cost is trivial compared with the wasted research.",
"Contradiction is possible but not the primary inefficiency described.",
"The count is not the problem; the identical scoping is."],
e:"Parallelism only pays when the work is partitioned. Identical prompts to isolated agents produce convergent results, because each independently gravitates to the most prominent sources. Partition explicitly with stated exclusions, and add a coverage check at synthesis. The one legitimate use of identical prompts is deliberate redundancy for reliability &mdash; but then you are buying agreement, not coverage, and should say so.",
k:"Parallelism requires partition. Identical prompts to isolated agents buy agreement, not coverage."},

{i:399,d:1,t:"1.2",s:"AAT",diff:3,
q:"A coordinator must decide between dispatching a specialist subagent and handling a simple lookup itself. The lookup takes one tool call.",
ask:"What should guide the decision?",
a:["Delegate when the work is verbose or genuinely specialised; handle it directly when it is a single cheap call the coordinator already has the tool for",
"Always delegate, to keep the coordinator's context clean",
"Always handle directly, to avoid coordination overhead",
"Delegate based on whether the specialist agent is currently idle"],
c:0,
w:["Correct. Delegation has real overhead, so it must be justified by verbosity or specialisation.",
"Delegating a single cheap call costs a spawn, a prompt and a return for no benefit.",
"Never delegating discards the isolation and specialisation benefits for the cases that need them.",
"Agent idleness is not a meaningful concept here; there is no scheduler to balance."],
e:"Delegation is not free: you pay for the spawn, for re-establishing context in the prompt, and for the return trip. It earns that cost when the work is verbose enough that isolation matters, or specialised enough that a focused prompt and scoped toolset produce materially better results. A single cheap lookup the coordinator can already perform meets neither test.",
k:"Delegate for verbosity or specialisation. A single cheap call the coordinator can already make should just be made."},

{i:400,d:1,t:"1.3",s:"AAT",diff:2,
q:"A subagent is asked to review a document and returns a review of a different document entirely.",
ask:"What is the most likely cause?",
a:["The delegation prompt referenced the document indirectly rather than including or precisely identifying it",
"The subagent has a bug in its document parser",
"Subagents cannot reliably handle document review",
"The coordinator's context window overflowed"],
c:0,
w:["Correct. Indirect references resolve to nothing, so the subagent works from whatever it can find or infer.",
"A parser bug would produce a parsing failure, not a review of the wrong document.",
"Document review is a standard subagent task.",
"Coordinator overflow would not manifest as a subagent reviewing a different document."],
e:"Every delegation must identify its inputs unambiguously &mdash; a full path, a URL, or the content inline. &ldquo;The document&rdquo;, &ldquo;the file we discussed&rdquo; and &ldquo;the latest report&rdquo; are all dangling references from the subagent's perspective, and the failure is silent: it produces a confident review of something else. Requiring the output to echo the identifier it reviewed makes the mismatch visible.",
k:"Identify subagent inputs unambiguously and have the output echo what it processed. Indirect references fail silently."},

{i:401,d:1,t:"1.4",s:"CSA",diff:3,
q:"A workflow must verify identity before a refund. You implement this as a <code>PreToolUse</code> hook that checks session state. A colleague argues the check should also exist inside <code>process_refund</code> itself.",
ask:"Is the colleague right?",
a:["Yes &mdash; the tool is the resource owner, so checking there protects against any caller, including future ones that bypass the hook",
"No &mdash; duplicating the check is redundant and adds maintenance cost",
"No &mdash; hooks always run, so the tool-level check can never fire",
"Yes, but only if the hook is removed to avoid duplication"],
c:0,
w:["Correct. Defence in depth: the hook catches it early with a good message, the tool guarantees it regardless of caller.",
"Duplication has a cost, but for a financial control the guarantee is worth it &mdash; and the two checks serve different scopes.",
"Hooks are client-side configuration and can be absent, misconfigured or bypassed by a different client.",
"Removing the hook loses the early, well-explained block that produces a good customer conversation."],
e:"The two layers do different jobs. The hook is close to the agent, so it can return an explanatory, non-retryable block that lets the agent escalate gracefully. The tool-level check is the authoritative guarantee, holding for any caller &mdash; a different agent, a script, a future integration. For financial and security controls, the enforcement belongs where the resource lives, with the hook as an ergonomic layer on top.",
k:"Enforce at the resource (guarantee) and in the hook (early, explainable). Client-side-only enforcement is bypassable."},

{i:402,d:1,t:"1.5",s:"AAT",diff:2,
q:"A <code>PostToolUse</code> hook trims tool results to five fields. A new workflow needs a sixth field that the hook is discarding.",
ask:"What does this reveal about trimming?",
a:["Trimming rules are coupled to consumers, so they must be reviewed when consumers change &mdash; and ideally scoped per tool or per workflow",
"Trimming is fundamentally unsafe and should be abandoned",
"The sixth field should be fetched with a second tool call",
"The workflow should be redesigned to need only five fields"],
c:0,
w:["Correct. Trimming is a projection over a consumer's needs, so it is a coupling that must be maintained.",
"Trimming is explicitly recommended; the lesson is about maintaining it, not abandoning it.",
"A second call to retrieve data the first call already returned is pure waste.",
"Constraining requirements to fit an internal optimisation is backwards."],
e:"Trimming is valuable and has a maintenance cost: the projection encodes what consumers currently need. Manage it by keeping the rule close to the tool definition, documenting which fields are dropped and why, and reviewing it when a workflow's needs change. Where different workflows need different projections, scope the trimming per workflow rather than applying one global rule.",
k:"Trimming is a projection over consumer needs. Document what is dropped, review it when consumers change, scope per workflow if needed."},

{i:403,d:1,t:"1.6",s:"CGC",diff:2,
q:"A task decomposes into four steps. Steps 1 and 2 are independent; step 3 needs both; step 4 needs step 3.",
ask:"What is the optimal execution shape?",
a:["Run steps 1 and 2 in parallel, then step 3, then step 4",
"Run all four sequentially in order",
"Run all four in parallel and reconcile afterwards",
"Run steps 1, 2 and 3 in parallel, then step 4"],
c:0,
w:["Correct. Parallelise the independent pair, then respect the two genuine dependencies.",
"Serialising steps 1 and 2 wastes an entire round trip for no reason.",
"Steps 3 and 4 have hard input dependencies and cannot run before their inputs exist.",
"Step 3 needs the outputs of 1 and 2, so it cannot run alongside them."],
e:"Read the dependency graph and parallelise every independent set. The general rule holds whether the units are tool calls in one turn or subagents spawned via <code>Task</code>: independence permits concurrency, dependency forces sequence. The critical path is the longest dependency chain, and that is the floor on your latency &mdash; here, three levels rather than four.",
k:"Parallelise independent sets, sequence real dependencies. Latency floor = the longest dependency chain."},

{i:404,d:1,t:"1.7",s:"CGC",diff:2,
q:"You want to keep a long investigation's context manageable while retaining the ability to answer follow-up questions about earlier findings.",
ask:"Which approach best supports both?",
a:["Maintain a findings file the agent writes to and can re-read selectively when a follow-up requires an earlier detail",
"Keep everything in the live context so nothing is ever unavailable",
"Compact aggressively and accept that earlier details are gone",
"Start a new session for every follow-up question"],
c:0,
w:["Correct. Externalised findings are both out of the live context and available on demand &mdash; the best of both.",
"Unbounded live context is what causes degradation in the first place.",
"Accepts permanent loss of details that follow-ups will need.",
"Each new session would have to rebuild understanding from scratch."],
e:"A findings file resolves the apparent tension between small context and full recall. Details live outside the window, so they do not degrade or crowd anything out, and a targeted read brings back exactly what a follow-up needs. Structure the file with clear headings so selective reading is possible &mdash; a single undifferentiated blob defeats the purpose by forcing you to read all of it.",
k:"Externalised, well-structured findings give small context <em>and</em> full recall. Use headings so reads can be selective."},

{i:405,d:2,t:"2.1",s:"AAT",diff:2,
q:"Two tools have clear, distinct descriptions, but one is named <code>process</code> and the other <code>handle</code>.",
ask:"What is the risk?",
a:["Both names are semantically empty, so the names contribute nothing to selection and the model must rely entirely on the descriptions",
"The API will reject generic tool names",
"Short names consume fewer tokens, which is always beneficial",
"Generic names cause the tools to be called more often"],
c:0,
w:["Correct. Names are part of the selection signal; empty names waste that signal and put all the load on the descriptions.",
"The API does not validate name quality.",
"Token savings are trivial and irrelevant compared with selection reliability.",
"There is no mechanism by which generic names increase call frequency."],
e:"Both the name and the description feed selection, and a good name does real work &mdash; it conveys the boundary before the description is even weighed. <code>process</code> and <code>handle</code> convey nothing. Prefer verb-plus-object names that state exactly what the tool does to what: <code>extract_web_results</code>, <code>load_document</code>, <code>lookup_order</code>. The blueprint's rename example makes precisely this point.",
k:"Names carry selection signal. Use verb-plus-object names that reveal the boundary; <code>process</code> and <code>handle</code> waste the signal."},

{i:406,d:2,t:"2.2",s:"AAT",diff:3,
q:"Your tool returns an error with a helpful message, but the agent relays the internal correlation id to the customer.",
ask:"How should the response be structured?",
a:["Separate a user-safe message from internal diagnostic fields, so the agent knows which part is safe to relay",
"Remove the correlation id entirely",
"Instruct the agent in the system prompt never to mention identifiers",
"Encrypt the correlation id in the response"],
c:0,
w:["Correct. Explicit field separation lets the agent relay the safe part and keeps diagnostics for logs.",
"The correlation id is genuinely useful for support; removing it loses that.",
"A prompt instruction is guidance and will occasionally fail.",
"Encryption makes it unreadable to your own support staff without solving the disclosure question."],
e:"Design error responses with audiences in mind and mark them structurally. A <code>userMessage</code> field carries plain-language text the agent may relay; <code>errorCategory</code> and <code>isRetryable</code> drive recovery; internal fields such as a correlation id or upstream detail are for logs and support. Making the separation explicit means the agent does not have to judge what is safe to repeat.",
k:"Structure errors by audience: user-safe message, model-facing recovery flags, internal diagnostics. Mark them explicitly."},

{i:407,d:2,t:"2.3",s:"AAT",diff:2,
q:"You are told to reduce an agent's toolset from 14 to 6 tools.",
ask:"Which selection principle should guide which six?",
a:["Keep the tools central to this agent's role, plus a small number of genuinely necessary cross-role utilities",
"Keep the six most frequently used tools regardless of role",
"Keep the six tools with the shortest descriptions",
"Keep the six most recently added tools"],
c:0,
w:["Correct. Scoped access means role-relevant tools plus a limited set of cross-role utilities.",
"Frequency is a reasonable input but can retain tools that belong to another agent's specialisation.",
"Description length is irrelevant to relevance.",
"Recency has no bearing on which tools this role needs."],
e:"The organising principle is role coherence, not usage statistics. Ask what this agent is <em>for</em>, keep the tools that serve that purpose, and add only the cross-role utilities it genuinely needs &mdash; preferably read-only and narrow. Tools that get cut should go to the agent whose specialisation they belong to, so capability is relocated rather than lost.",
k:"Scope by role coherence: core role tools + a few narrow cross-role utilities. Relocate the rest, do not delete them."},

{i:408,d:2,t:"2.4",s:"DPT",diff:2,
q:"An MCP server exposes a resource listing all database table schemas.",
ask:"How does this help the agent?",
a:["It can consult the schemas directly instead of running exploratory queries to discover structure",
"It allows the agent to modify the schemas",
"It caches query results for faster retrieval",
"It replaces the need for query tools entirely"],
c:0,
w:["Correct. Resources are content catalogues that eliminate exploratory tool calls.",
"Resources are readable content; modification would be a tool, not a resource.",
"Resources are not a query cache.",
"You still need tools to run actual queries; the resource informs how to write them."],
e:"MCP separates tools (actions the model invokes) from resources (content it can read). Exposing schemas, task summaries and reference data as resources means the agent starts informed rather than probing, which saves turns and reduces the risk of malformed queries. The advantage over putting the same content in a prompt is that the server keeps it current and the agent reads it on demand.",
k:"MCP resources are readable catalogues (schemas, reference data). They replace exploratory calls, not action tools."},

{i:409,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent must rename a symbol used in 40 files.",
ask:"What is the safest approach?",
a:["Grep to find every occurrence, review the matches for false positives, then apply targeted edits",
"Use Bash with a global find-and-replace across the repository",
"Edit the definition and let the compiler find the rest",
"Read all 40 files and rewrite each with Write"],
c:0,
w:["Correct. Find, review, then apply &mdash; the review step catches substring collisions and unrelated matches.",
"Global replace without review risks renaming unrelated occurrences, including in strings and comments.",
"Compiler-driven renaming works in some languages but misses dynamic references, strings and non-compiled files.",
"Whole-file rewrites of 40 files produce an enormous, hard-to-review diff and risk unintended changes."],
e:"The pattern is search, review, then apply targeted changes. The review step matters because symbol names collide with substrings, appear in comments and strings, and may exist in unrelated modules. <code>Edit</code>'s unique-match requirement is a safety feature here, forcing each change to be specific rather than blanket.",
k:"Rename safely: Grep to find &rarr; review matches &rarr; targeted Edits. Never blanket-replace without inspection."},

{i:410,d:3,t:"3.1",s:"CGC",diff:2,
q:"Which is the strongest CLAUDE.md entry?",
ask:"Which one is best?",
a:["Errors that a caller can reasonably recover from return a <code>Result</code>; reserve exceptions for programmer errors such as invalid arguments.",
"Handle errors properly and consistently throughout the codebase.",
"Error handling is very important in this project.",
"Follow standard error-handling best practices."],
c:0,
w:["Correct. It states a specific, checkable convention with the rationale implicit in the distinction.",
"&ldquo;Properly&rdquo; and &ldquo;consistently&rdquo; encode no decision.",
"An assertion of importance with no content.",
"&ldquo;Standard best practices&rdquo; differ by team and language; it says nothing."],
e:"Every CLAUDE.md line competes for context on every request, so it must change a decision the model would otherwise get wrong. The strong entry does: it tells the model exactly when to use a <code>Result</code> and when to throw. The three weak entries are interchangeable filler that could appear in any project's configuration.",
k:"CLAUDE.md entries must encode a specific decision. If the line could appear in any project, delete it."},

{i:411,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a skill that can read and analyse code but must never be able to modify it, and whose output is long enough to crowd the main session.",
ask:"Which frontmatter combination covers both requirements?",
a:["<code>context: fork</code> together with <code>disallowed-tools</code> removing Write, Edit and Bash",
"<code>context: fork</code> alone",
"<code>disallowed-tools</code> alone",
"<code>argument-hint</code> together with <code>context: fork</code>"],
c:0,
w:["Correct. Two requirements map to two fields: isolation via <code>context: fork</code>, capability removal via <code>disallowed-tools</code>.",
"Isolates the context but leaves the skill able to modify files.",
"Removes the write capability but lets the long output pollute the main session.",
"<code>argument-hint</code> addresses parameters, which is not one of the stated requirements."],
e:"Read each stated requirement and map it to the field that delivers it. Verbose output &rarr; <code>context: fork</code>. Must not modify &rarr; <code>disallowed-tools</code> (not <code>allowed-tools</code>, which only pre-approves). Requires a parameter &rarr; <code>argument-hint</code>. The exam frequently offers a partial answer satisfying one requirement and ignoring the other, so check every stated requirement is covered before choosing.",
k:"Map each requirement to its field: verbose &rarr; <code>context: fork</code>, capability removal &rarr; <code>disallowed-tools</code>, parameters &rarr; <code>argument-hint</code>."},

{i:412,d:3,t:"3.3",s:"CGC",diff:3,
q:"Your repository has 40 rule files, each path-scoped. A developer worries this is unmanageable.",
ask:"What is the most accurate assessment?",
a:["Many narrowly-scoped rules are fine, since only the matching ones load; the real risk is overlap and contradiction between them",
"Forty rule files will exceed the context window",
"Rules should always be consolidated into a single file",
"Path scoping stops working beyond ten rule files"],
c:0,
w:["Correct. Conditional loading means the count is not the constraint; coherence between overlapping rules is.",
"Only matching rules load, so the total count does not determine context usage.",
"Consolidation is exactly what path scoping exists to avoid.",
"No such limit exists."],
e:"Path scoping changes what &ldquo;too many&rdquo; means: forty rules of which two load for a given file is far better than one file containing all forty. The genuine risks are different &mdash; two rules matching the same file and giving contradictory guidance, or a rule whose glob is broader than intended. Manage those with a naming convention, periodic review, and explicit exception phrasing where rules interact.",
k:"With path scoping, rule count is not the constraint. Watch for overlapping globs and contradictory guidance instead."},

{i:413,d:3,t:"3.4",s:"CGC",diff:2,
q:"A task's requirements are unclear and the developer is not sure what they want.",
ask:"Which approach fits?",
a:["Use the interview pattern to have Claude surface the unstated requirements before any planning or implementation",
"Use planning mode to produce a plan from the unclear requirements",
"Start direct execution and refine as the requirements clarify",
"Ask the developer to write a full specification first"],
c:0,
w:["Correct. Unclear requirements are a specification problem, and the interview pattern is the named technique for surfacing them.",
"Planning from unclear requirements produces a confident plan for the wrong thing.",
"Building against unclear requirements guarantees rework.",
"If the developer could write a full specification, the requirements would not be unclear."],
e:"Distinguish the two kinds of uncertainty. Unclear <em>requirements</em> &mdash; you do not know what to build &mdash; call for the interview pattern, where Claude asks the questions that surface non-obvious considerations. Unclear <em>approach</em> &mdash; you know what to build but not how &mdash; calls for planning mode. Doing them in the wrong order produces a well-planned implementation of the wrong thing.",
k:"Unclear requirements &rarr; interview pattern. Unclear approach &rarr; planning mode. Requirements first."},

{i:414,d:3,t:"3.5",s:"CGC",diff:2,
q:"After three rounds of feedback, the implementation is still not right, and each round changes different things.",
ask:"What should you reconsider?",
a:["Whether the requirement itself is under-specified &mdash; oscillation usually means there is no clear target to converge on",
"Whether the model is capable of the task",
"Whether more rounds of the same feedback will eventually converge",
"Whether the temperature should be lowered"],
c:0,
w:["Correct. Oscillation across rounds is the signature of an unstated or shifting target.",
"Capability is rarely the issue when each round produces a different plausible attempt.",
"More rounds of unfocused feedback produce more oscillation.",
"Temperature does not supply a missing specification."],
e:"When iteration is not converging, stop iterating and fix the target. Write the requirement as concrete input/output examples or as failing tests, so both you and the model are working against the same definition of done. If you cannot write those examples, that is the finding &mdash; the requirement is not yet clear enough to build against, and the interview pattern is the next step.",
k:"Non-convergent iteration means an unclear target. Stop, write examples or tests, then resume."},

{i:415,d:3,t:"3.6",s:"CCI",diff:2,
q:"Your CI job passes the entire repository to Claude Code on every run, including unchanged files.",
ask:"What should change?",
a:["Pass only the diff plus the specific context needed to review it, such as the changed symbols' consumers",
"Pass the entire repository but with a shorter prompt",
"Pass only the changed files' names",
"Pass the entire repository less frequently"],
c:0,
w:["Correct. Scope the input to what the review needs &mdash; the change and its immediate context.",
"Prompt length is not the problem; input volume is.",
"Names without content give the reviewer nothing to review.",
"Reviewing less often means defects reach main."],
e:"Reviewing the whole repository on every push wastes tokens and dilutes attention across material that has not changed, concentrating the review on whatever appears early. Pass the diff, plus the targeted context that makes it reviewable &mdash; the definitions that changed and the call sites that consume them &mdash; which is also exactly the input a cross-file integration pass needs.",
k:"Review the diff plus its consuming context, not the whole repository. Volume dilutes attention."},

{i:416,d:4,t:"4.1",s:"SDE",diff:2,
q:"Your prompt says: <em>Extract the date.</em> Documents contain an invoice date, a due date and a delivery date.",
ask:"What is the problem?",
a:["The field is under-specified &mdash; the prompt must name which date, and the schema should have separate fields for each",
"The model cannot distinguish between date types",
"The temperature is too high",
"Dates should always be extracted as strings"],
c:0,
w:["Correct. Ambiguity in the requirement produces inconsistent selection among three valid candidates.",
"It can distinguish them when told which one is wanted.",
"Temperature does not resolve an ambiguous requirement.",
"The type is a separate question and does not address the ambiguity."],
e:"Two fixes work together. In the prompt, name exactly which date is wanted and how to identify it in the document. In the schema, define separate fields &mdash; <code>invoice_date</code>, <code>due_date</code>, <code>delivery_date</code> &mdash; each nullable, so all three are captured when present and absence is representable. Naming the fields precisely removes the ambiguity structurally.",
k:"Name fields precisely in both prompt and schema. &ldquo;The date&rdquo; is ambiguous when three dates exist."},

{i:417,d:4,t:"4.2",s:"CCI",diff:2,
q:"Your few-shot examples all show findings in the same file type and severity band.",
ask:"What is the risk?",
a:["The model over-fits to the demonstrated pattern and generalises poorly to other file types and severities",
"The examples consume too many tokens",
"The model will refuse to report findings outside the demonstrated pattern",
"There is no risk; consistent examples are always better"],
c:0,
w:["Correct. Examples that span only one region of the space teach that region's conventions rather than the general rule.",
"Token cost is secondary to the generalisation problem.",
"It will not refuse &mdash; it will report, but with the demonstrated pattern's biases.",
"Consistency in format is good; homogeneity in content is not."],
e:"Few-shot examples should be consistent in <em>format</em> and varied in <em>content</em>. Consistent format teaches the output shape; varied content &mdash; different file types, different severities, positive and negative cases &mdash; teaches the underlying rule. Homogeneous examples produce a model that handles the demonstrated region well and everything else poorly.",
k:"Few-shot: consistent format, varied content. Homogeneous examples teach a region, not a rule."},

{i:418,d:4,t:"4.3",s:"SDE",diff:2,
q:"You need the model to classify a document into one of four categories and provide a confidence value.",
ask:"How should the schema express the category?",
a:["An <code>enum</code> with the four categories plus explicit <code>other</code> and <code>unclear</code> values and a detail field",
"A free-text string field",
"An integer index into a category list documented in the prompt",
"Four boolean fields, one per category"],
c:0,
w:["Correct. An enum enforces the vocabulary while escape values prevent forced fits and reveal missing categories.",
"Free text destroys the controlled vocabulary that makes downstream analytics possible.",
"An index adds an indirection the model can get wrong and that a reader cannot interpret.",
"Independent booleans permit invalid states such as all-true or all-false."],
e:"Use the schema's expressive features to make invalid states impossible. An enum guarantees membership in the vocabulary. Escape values (<code>other</code>, <code>unclear</code>) plus a detail field give the model a truthful option instead of forcing a bad fit, and reviewing what lands in <code>other</code> tells you which fifth category you actually need. Four booleans, by contrast, allow states that make no sense.",
k:"Enums enforce vocabulary; escape values prevent forced fits and reveal gaps. Avoid schemas that permit invalid states."},

{i:419,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to know which validation rules fire most often so you can improve the prompt.",
ask:"What should you record?",
a:["Which specific rule failed on each retry, so failures can be aggregated by cause",
"The total number of retries per document",
"The time taken per retry",
"The model version used for each retry"],
c:0,
w:["Correct. Aggregating by failing rule is what tells you which prompt or schema change would help most.",
"A count says a problem exists without saying what it is.",
"Latency is operational data, not diagnostic of extraction quality.",
"Useful metadata but it does not identify the failing rule."],
e:"This mirrors the <code>detected_pattern</code> practice for review findings: record the cause, not just the occurrence. If 70% of retries are triggered by one validation rule, you know exactly where to focus &mdash; perhaps that field needs a clearer description, a few-shot example, or a schema change making it nullable. Without the breakdown, every retry is an anecdote.",
k:"Record which rule failed, not just that a retry happened. Aggregation by cause is what drives improvement."},

{i:420,d:4,t:"4.5",s:"SDE",diff:2,
q:"A nightly batch has been running for three weeks. Last night 12% of items failed, up from a typical 0.3%.",
ask:"What should you do first?",
a:["Inspect the failed items by <code>custom_id</code> to identify what they have in common before re-submitting anything",
"Immediately re-submit all failed items",
"Re-submit the entire batch",
"Increase the retry count for future batches"],
c:0,
w:["Correct. A 40-fold jump indicates a systematic change; diagnose before spending another run.",
"Blind re-submission of a systematic failure fails again and wastes a full cycle.",
"Reprocessing the successful 88% as well compounds the waste.",
"Raising retries treats a systematic failure as if it were transient."],
e:"A sudden jump in failure rate is a signal, not noise. Use <code>custom_id</code> to identify the failed set and look for what they share &mdash; a new document format from one supplier, an upstream schema change, a template revision. Systematic causes need a fix; only genuinely transient failures deserve a straight re-submission.",
k:"Investigate failure-rate jumps before re-submitting. Use <code>custom_id</code> to find what the failures share."},

{i:421,d:4,t:"4.6",s:"CCI",diff:2,
q:"Your multi-pass review runs per-file passes and a cross-file pass, but findings are duplicated across the two.",
ask:"How should this be handled?",
a:["Give each pass a distinct remit &mdash; local defects for per-file, cross-file interactions for the integration pass &mdash; and deduplicate on file and line when merging",
"Run only the cross-file pass",
"Run only the per-file passes",
"Accept the duplication as a sign of thoroughness"],
c:0,
w:["Correct. Distinct remits reduce overlap at the source, and merge-time deduplication handles the residue.",
"Loses local defect detection, which the per-file passes do best.",
"Loses cross-file detection entirely.",
"Duplicated findings are noise that erodes developer trust in the bot."],
e:"Multi-pass architectures need explicit remits, or the passes converge on the same easy findings. Tell the per-file pass to report local issues and explicitly not to speculate about other files; tell the integration pass to report only defects arising from interactions between changed files. Then deduplicate on file and line at merge time. Duplicate comments are a direct contributor to the trust erosion that makes developers ignore the bot.",
k:"Give each review pass a distinct remit and deduplicate at merge. Overlapping passes produce noise, not thoroughness."},

{i:422,d:5,t:"5.1",s:"AAT",diff:2,
q:"Which practice most directly addresses tool output accumulating in context disproportionately to its relevance?",
ask:"Which is it?",
a:["Trimming tool results to the fields the agent actually uses, in a hook, before they enter the context",
"Summarising the conversation once it grows long",
"Increasing the context window",
"Reducing the number of tool calls the agent makes"],
c:0,
w:["Correct. This is the blueprint's named remedy for the 40-fields-when-5-are-needed problem.",
"Summarisation is reactive and lossy about specifics; prevention is better.",
"Accommodates the accumulation rather than preventing it.",
"Fewer calls means less information; the problem is verbosity per call, not call count."],
e:"The stated problem is disproportion &mdash; volume that does not match relevance &mdash; so the remedy is a projection at the boundary. A <code>PostToolUse</code> hook that keeps only the needed fields applies deterministically to every call. Keep the ordering of remedies in mind: prevent at source first, mitigate with compaction only afterwards.",
k:"Disproportionate tool output &rarr; trim by field in a hook. Prevention first, compaction second."},

{i:423,d:5,t:"5.2",s:"CSA",diff:2,
q:"An agent has resolved a customer's issue but the customer then asks a question about a completely different product the agent has no tools or knowledge for.",
ask:"What should happen?",
a:["Escalate or route the new question appropriately, with a structured handoff, rather than attempting an ungrounded answer",
"Answer from general knowledge, since the customer is already engaged",
"Tell the customer the conversation is complete and close it",
"Ask the customer to open a new conversation"],
c:0,
w:["Correct. Out of scope with no tools means an ungrounded answer, so route it with the context already gathered.",
"Answering without tools or knowledge produces exactly the confident fabrication the design should prevent.",
"Closing on an unanswered question abandons the customer mid-need.",
"Forcing a new conversation loses the context and frustrates the customer."],
e:"Scope boundaries are capability boundaries. With no tools and no grounding for the new product, any answer would be confabulation. The right move is a structured handoff carrying what has already been established &mdash; verified identity, the resolved issue, and the new question &mdash; so the receiving party starts informed rather than from scratch.",
k:"Out of scope with no grounding &rarr; route with a structured handoff. Never answer ungrounded because the customer is already there."},

{i:424,d:5,t:"5.3",s:"MAR",diff:2,
q:"A subagent recovers from a transient failure on its second attempt and completes successfully.",
ask:"Should it report the failure to the coordinator?",
a:["It need not escalate, but noting the retry in its result is useful operational signal without requiring coordinator action",
"Yes &mdash; every failure must be escalated regardless of recovery",
"No &mdash; a recovered failure should be completely invisible",
"Yes &mdash; and the coordinator should re-run the subagent to confirm"],
c:0,
w:["Correct. Local recovery means no escalation is needed, but the retry is worth recording for observability.",
"Escalating recovered transient failures floods the coordinator with work it need not act on.",
"Complete invisibility hides a degrading dependency until it fails outright.",
"Re-running a successful subagent to confirm is pure waste."],
e:"Distinguish escalation from observability. Escalation is for things requiring a decision; a recovered transient failure needs none. But the retry is a signal &mdash; a dependency that increasingly needs two attempts is trending toward an outage. Note it in the result metadata or your logs, where it informs monitoring without demanding coordinator attention.",
k:"Recovered failures need observability, not escalation. Record retries as signal; escalate only what needs a decision."},

{i:425,d:5,t:"5.4",s:"CGC",diff:3,
q:"Your scratchpad file has grown to 3,000 lines over a long investigation.",
ask:"What should you do?",
a:["Structure it with clear headings and a summary section so it can be read selectively rather than in full",
"Delete it and start a new one",
"Read the whole file into context whenever any detail is needed",
"Stop writing to it to prevent further growth"],
c:0,
w:["Correct. Structure enables selective reading, which is what makes an externalised file useful at scale.",
"Deleting loses the findings the file exists to preserve.",
"Reading 3,000 lines for one detail reproduces the context problem the file was meant to solve.",
"Stopping writing means new findings are no longer preserved."],
e:"An externalised file solves the context problem only if you can read part of it. Organise by topic with clear headings, keep a short summary section at the top listing what is covered and where, and prune superseded material as the investigation progresses. Then a follow-up question costs a targeted read rather than a full one.",
k:"Structure scratchpads with headings and a summary index so reads can be selective. An unstructured blob recreates the problem."},

{i:426,d:5,t:"5.5",s:"SDE",diff:2,
q:"Your confidence calibration was performed six months ago on a document set that has since changed composition.",
ask:"What is the risk?",
a:["Calibration drifts as inputs change, so the thresholds may no longer correspond to the error rates they were set for",
"Calibration never expires once performed correctly",
"Calibration is only affected by model changes, not input changes",
"Old calibration is conservative and therefore safe"],
c:0,
w:["Correct. Calibration is a property of a model applied to a distribution; change either and it must be re-measured.",
"Calibration is not permanent.",
"Input distribution shifts affect calibration just as model changes do.",
"Drift is not directional &mdash; old thresholds can be optimistic as easily as conservative."],
e:"Calibration binds a confidence value to an observed error rate for a given model on a given input distribution. Shift the document mix &mdash; a new supplier's template, more scans, a different language &mdash; and the binding no longer holds. Re-calibrate periodically, and use stratified sampling of the auto-approved stream as the ongoing check that catches drift between full re-calibrations.",
k:"Calibration drifts with input mix and model changes. Re-measure periodically; sample the automated stream to detect drift early."},

{i:427,d:5,t:"5.6",s:"MAR",diff:2,
q:"A synthesis agent receives structured findings with claim-to-source mappings, then produces flowing prose with a bibliography at the end.",
ask:"What was lost?",
a:["The per-claim attribution &mdash; a reader can see which sources were consulted but not which one supports each claim",
"Nothing; a bibliography preserves attribution adequately",
"The publication dates, which bibliographies never include",
"The structural formatting, which is only cosmetic"],
c:0,
w:["Correct. A bibliography proves consultation; inline attribution proves support for a specific claim.",
"A bibliography answers a different question from the one a fact-checker asks.",
"Bibliographies commonly include dates; the loss is the claim-level mapping.",
"Attribution is not cosmetic &mdash; it is what makes claims checkable."],
e:"Attribution must survive the final rendering step, not just the aggregation steps. Having required structured claim-to-source mappings from subagents, the synthesis must preserve them inline &mdash; each claim carrying its source, and ideally its date &mdash; rather than flattening into prose with a trailing source list. Flattening at the last step discards everything the earlier discipline preserved.",
k:"Preserve attribution through rendering, inline per claim. A bibliography shows consultation, not support."},

{i:428,d:1,t:"1.4",s:"AAT",diff:2,
q:"Which of these should be enforced programmatically rather than by prompt?",
ask:"Which one?",
a:["A rule that customer data must never be sent to an external service that is not on the approved list",
"A preference for shorter variable names",
"A guideline to explain reasoning before giving an answer",
"A convention to prefer composition over inheritance"],
c:0,
w:["Correct. A data-egress control is a security requirement where a single violation is unacceptable.",
"A style preference with no consequence for occasional deviation.",
"A conversational quality guideline.",
"A design convention, appropriately guided by CLAUDE.md."],
e:"Apply the standard test: what does one violation in a hundred cost? For style and design conventions, nothing that matters. For data egress to an unapproved service, a potential breach. Enforce that in code &mdash; restrict the tools that can make outbound calls, allow-list destinations in a <code>PreToolUse</code> hook, and prefer an allow-list over a deny-list since the latter can never be complete.",
k:"Data egress, money, deletion and identity are code-enforced. Style and design conventions are prompt-guided."},

{i:429,d:2,t:"2.2",s:"AAT",diff:2,
q:"Which error category should a rate-limit response be assigned?",
ask:"Which is correct?",
a:["Transient, retryable after the indicated delay",
"Validation, since the request was rejected",
"Permission, since access was denied",
"Business, since a policy limit was exceeded"],
c:0,
w:["Correct. Rate limits are temporary and the identical request succeeds once the window resets.",
"The request was well-formed; nothing about it was invalid.",
"The caller is authorised; it is simply calling too often.",
"A rate limit is an infrastructure control, not a business rule about the request's content."],
e:"Apply the retryability test: would the identical call succeed shortly? For a rate limit, yes &mdash; which makes it transient and retryable, ideally honouring any retry-after hint. Contrast with a business-rule limit such as a refund cap, which is deterministic no matter how long you wait. The two can look superficially similar because both involve a &ldquo;limit&rdquo;, so classify by whether time alone resolves it.",
k:"Rate limit = transient, retry after the indicated delay. A business limit is deterministic and never retryable."},

{i:430,d:3,t:"3.6",s:"CCI",diff:3,
q:"You want your CI review to catch regressions in areas a previous incident affected, without reviewing everything at that depth.",
ask:"Which mechanism fits best?",
a:["A path-scoped rule carrying the incident's specific review criteria, applied to the affected files",
"A longer global review prompt covering the incident",
"A separate CI job that reviews only those files",
"A comment in the affected files reminding reviewers"],
c:0,
w:["Correct. Path scoping applies deeper criteria exactly where they matter, at no cost elsewhere.",
"A global prompt applies the criteria everywhere, diluting attention and raising false positives.",
"Workable but duplicates configuration and drifts from the main review job.",
"Comments are not review criteria and are easily missed."],
e:"Path-scoped rules are how you encode targeted, hard-won knowledge. After an incident, write the specific checks it taught you into a rule scoped to the files where that class of defect can occur. The criteria are deep where they need to be, absent everywhere else, and the same configuration governs local development and CI.",
k:"Encode incident-specific review criteria as path-scoped rules. Deep where it matters, absent where it does not."}
);
