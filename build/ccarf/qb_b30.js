QB.push(
{i:1151,d:1,t:"1.1",s:"AAT",diff:2,
q:"An agent loop is correct but a colleague wants to remove the assistant message from history to save tokens, keeping only tool results.",
ask:"What breaks?",
a:["The <code>tool_result</code> blocks become orphaned, since each must answer a <code>tool_use</code> block present in the history",
"Nothing; results carry enough information",
"Only the token count changes",
"The model loses its persona"],
c:0,
w:["Correct. The pairing is structural, and removing one half invalidates the conversation.",
"The result alone does not record what was requested or why.",
"The conversation becomes structurally invalid, not merely cheaper.",
"Persona lives in the system prompt."],
e:"The assistant turn carrying the <code>tool_use</code> blocks is not optional padding &mdash; it is what the <code>tool_result</code> blocks answer, matched by <code>tool_use_id</code>. Removing it orphans the results and breaks the required user/assistant alternation. If token cost is the concern, the levers are trimming verbose tool <em>results</em> and prompt-caching the stable prefix, not deleting structural elements.",
k:"The assistant <code>tool_use</code> turn is structural, not padding. Cut cost by trimming results and caching, never by deleting turns."},

{i:1152,d:1,t:"1.2",s:"MAR",diff:2,
q:"You want a coordinator to notice when its own decomposition missed something.",
ask:"What mechanism provides this?",
a:["A coverage check at synthesis that names what was <em>not</em> examined, since a gap is invisible when every dispatched subtask succeeded",
"Counting the subtasks",
"Checking each subagent succeeded",
"Measuring total output length"],
c:0,
w:["Correct. Only an explicit coverage check surfaces what was never assigned.",
"Count says nothing about what the partition omitted.",
"All subagents succeeding is exactly what a coverage failure looks like.",
"Length does not indicate coverage."],
e:"The visual-art failure is the canonical case: every subagent succeeded, and the report covered a third of the topic. Since success signals cannot detect an unassigned dimension, the check must be explicit &mdash; have synthesis enumerate the domain and state which parts were examined and which were not. That output is also what feeds the coverage annotation the reader sees.",
k:"Coverage gaps are invisible in success signals. Require synthesis to name what was <em>not</em> examined."},

{i:1153,d:1,t:"1.3",s:"AAT",diff:2,
q:"A subagent needs to know how its output will be used downstream.",
ask:"Why does this matter?",
a:["Downstream use determines the appropriate depth, format and audience &mdash; and the subagent cannot infer any of them",
"It does not; the task is the same regardless",
"It affects which model it should use",
"It affects how long it should take"],
c:0,
w:["Correct. Depth, format and audience all depend on downstream use, and none is inferable.",
"A finding feeding a customer-facing summary differs from one feeding an internal audit.",
"Model choice is the coordinator's decision.",
"Duration follows from the task, not from downstream use."],
e:"State the downstream use in the delegation. It determines depth &mdash; a headline figure versus a defensible analysis &mdash; format, and audience constraints such as excluding internal identifiers. A subagent given only the task will produce something reasonable for a default audience, which may be entirely wrong for where it is going.",
k:"State downstream use in delegations. Depth, format and audience all depend on it and none is inferable."},

{i:1154,d:1,t:"1.4",s:"CSA",diff:2,
q:"An enforced control is correct but the agent triggers it repeatedly within one conversation.",
ask:"What should you examine?",
a:["Whether the agent knows the rule &mdash; repeated attempts at a blocked action suggest it is not stated in the prompt",
"Whether the control should be relaxed",
"Whether the conversation should be terminated",
"Whether the agent needs a different model"],
c:0,
w:["Correct. Repeated attempts indicate the agent does not understand the boundary.",
"The control is stated as correct.",
"Terminating over a repeated block is disproportionate.",
"Capability is rarely the cause when the rule was never stated."],
e:"Repeated attempts at a blocked action have two causes worth checking. The rule may not be in the prompt, so the agent has no reason to avoid it. Or the block message may not be marked non-retryable and explanatory, so the agent cannot distinguish it from a transient failure. Both are the complementarity point: enforcement guarantees the outcome, the prompt and the message make the agent competent around it.",
k:"Repeated blocked attempts mean the rule is not in the prompt, or the block message is not explanatory and non-retryable."},

{i:1155,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want a hook to be safe to deploy to production on a Friday.",
ask:"What property matters most?",
a:["A fast revert path &mdash; versioned externalised policy &mdash; since recoverability matters more than confidence for anything touching enforcement",
"Comprehensive testing",
"A detailed rollout plan",
"Low expected traffic"],
c:0,
w:["Correct. Fast revert is what makes a change safe when you cannot supervise it closely.",
"Testing reduces the chance of a problem without speeding recovery.",
"A plan does not shorten revert time.",
"Low traffic reduces blast radius without improving recoverability."],
e:"Confidence and recoverability are different properties, and recoverability is what matters when supervision is thin. Externalised versioned policy makes a revert a configuration change measured in seconds. Combine it with shadow-mode rollout beforehand and logging of every evaluation, and an enforcement change becomes something you can safely make and quickly undo.",
k:"Recoverability beats confidence for enforcement changes. Versioned externalised policy makes revert a config change."},

{i:1156,d:1,t:"1.6",s:"MAR",diff:2,
q:"A coordinator's decomposition is good but synthesis still struggles.",
ask:"What is worth checking?",
a:["Whether the parts arrive in a form synthesis can integrate &mdash; consistent structure with attribution preserved &mdash; since a good partition can still produce unmergeable outputs",
"Whether there are too many parts",
"Whether the synthesis model is capable enough",
"Whether the parts are too long"],
c:0,
w:["Correct. Partition quality and output-shape compatibility are separate properties.",
"Count may contribute but the shape question comes first.",
"Capability cannot integrate inconsistently structured inputs.",
"Length is secondary to structure."],
e:"Decomposition and integration are two halves of one design, and both must work. A well-partitioned topic can still produce outputs synthesis cannot merge if each subagent returns a different shape, or if attribution was flattened into prose along the way. Define one return contract across the agents so outputs are already in a compatible form.",
k:"A good partition still needs compatible return shapes. Define one contract across the agents so outputs merge."},

{i:1157,d:1,t:"1.7",s:"CGC",diff:2,
q:"You want a resumed session to be as reliable as a fresh one.",
ask:"What is required?",
a:["Confidence that nothing the session cached has changed &mdash; which usually means checking file state, since resumption restores stale content as faithfully as valid content",
"A shorter gap since the last session",
"A larger context window",
"Fewer turns in the original session"],
c:0,
w:["Correct. Validity, not recency, is what determines whether resumption is safe.",
"Elapsed time is a weak proxy &mdash; a file can change in ten minutes.",
"Window size does not affect staleness.",
"Turn count does not determine validity."],
e:"Resumption restores everything, including what has since become false. The check is whether the world the session cached still matches: have the files it read changed, moved or been deleted? If a few known files changed, name them and have the session re-read. If the change is large or structural, a fresh session seeded with a structured summary is the more reliable option &mdash; which is what the blueprint notes explicitly.",
k:"Resumption safety depends on validity, not recency. Check whether cached file state still holds."},

{i:1158,d:2,t:"2.1",s:"AAT",diff:2,
q:"You want a tool to be selected correctly by an agent you did not configure.",
ask:"What does this depend on?",
a:["The description standing alone, since you cannot rely on that agent's prompt or its other tools to disambiguate",
"The tool's name being unique",
"The tool being widely used",
"The agent using a capable model"],
c:0,
w:["Correct. A tool shared beyond your own agents must be self-describing.",
"Uniqueness of name does not convey when to choose it.",
"Popularity does not aid a specific selection decision.",
"Capability cannot compensate for an under-specified description."],
e:"When a tool is shared &mdash; through an MCP server other teams connect &mdash; its description becomes their selection quality, and you control nothing else about their setup. State the trigger conditions, the input format with an example, what it returns, and the boundary in general terms. Boundary statements are harder here, since you do not know their other tools, which is a further reason to be specific about what this one is for.",
k:"Shared tools must be self-describing. You cannot rely on another team's prompt or toolset to disambiguate."},

{i:1159,d:2,t:"2.2",s:"AAT",diff:2,
q:"You want a tool's errors to be actionable without the caller knowing the tool's internals.",
ask:"What achieves this?",
a:["Expressing errors in terms of the caller's situation &mdash; what they asked for, why it could not be done, what would change it &mdash; rather than in terms of internal state",
"A detailed internal error description",
"An error code",
"A stack trace"],
c:0,
w:["Correct. Caller-oriented framing is what makes an error actionable without internal knowledge.",
"Internal state is not actionable to a caller who does not know the internals.",
"A code without a lookup is opaque.",
"Stack traces are internal detail and leak implementation."],
e:"Frame errors from the caller's side. &ldquo;The order cannot be refunded because it was delivered 47 days ago and the return window is 30 days&rdquo; is actionable without knowing anything about the service. &ldquo;<code>RefundEligibilityValidator</code> returned false&rdquo; is not. Keep the internal detail in your logs behind a correlation id, where an operator can reach it.",
k:"Frame errors in the caller's terms: what was asked, why it failed, what would change it. Internals go to logs."},

{i:1160,d:2,t:"2.3",s:"AAT",diff:2,
q:"You want to reduce an agent's toolset but every tool is used at least occasionally.",
ask:"What should you consider?",
a:["Whether the occasional uses represent a distinct role that should be a separate agent, so each agent's toolset becomes coherent",
"Removing the least-used tool anyway",
"Keeping them all",
"Merging tools to reduce the count"],
c:0,
w:["Correct. Occasional uses clustering into a distinct role is the signal that a split is available.",
"Removing a genuinely needed capability breaks those cases.",
"Accepting an oversized toolset accepts the degraded selection.",
"Merging hides distinctions and produces multi-mode tools with their own problems."],
e:"When every tool is genuinely used, the toolset is not oversized by accident &mdash; the <em>role</em> is too broad. Look at what the occasional uses have in common: they usually cluster into a distinct specialisation. Splitting gives each agent a coherent toolset, a focused system prompt, and appropriately scoped permissions, which is the structural fix rather than a cosmetic one.",
k:"If every tool is genuinely used, the role is too broad. Look for the specialisation the occasional uses cluster into."},

{i:1161,d:2,t:"2.4",s:"DPT",diff:2,
q:"You want to know which MCP servers a particular agent actually depends on.",
ask:"Why does this matter operationally?",
a:["It determines that agent's blast radius when a server is unavailable, and which agents need a degraded-mode plan",
"It determines the token cost only",
"It determines which model to use",
"It is not operationally relevant"],
c:0,
w:["Correct. Dependency mapping drives availability planning and degraded-mode design.",
"Token cost is one consequence among several.",
"Model choice is unrelated.",
"Availability planning depends directly on it."],
e:"Knowing which agents depend on which servers tells you what breaks when one is down. That drives the degraded-mode decisions: which agents can continue with reduced capability, which should stop, and what the coordinator should do when a specialist becomes unavailable. It also connects to correlated-failure detection &mdash; several agents failing at once often traces to one shared server.",
k:"Map agent-to-server dependencies. It determines blast radius, degraded-mode plans and correlated-failure diagnosis."},

{i:1162,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to understand a build failure whose output is thousands of lines.",
ask:"What is the efficient approach?",
a:["Extract the error lines and their immediate context rather than reading the whole output, since most of it is progress noise",
"Read the entire output",
"Read the last hundred lines",
"Read the first hundred lines"],
c:0,
w:["Correct. Search for the error signature and read around it.",
"Thousands of lines of mostly-progress output is a large context cost for a small signal.",
"The relevant error is often not at the end, particularly with parallel builds.",
"The start is progress output."],
e:"Build output is mostly noise around a small signal, which makes it the classic case for extracting rather than reading. Search for the error pattern and read the surrounding context. Better still, if this recurs, a purpose-built tool that returns structured results &mdash; error count, file, line, message &mdash; removes the parsing problem entirely and grants narrower capability than shell access.",
k:"Extract the error and its context from verbose output. A structured build tool removes the parsing problem entirely."},

{i:1163,d:3,t:"3.1",s:"CGC",diff:2,
q:"A convention applies to the whole project but only matters for one kind of task.",
ask:"Where does it belong?",
a:["A rules file, scoped if the task maps to file patterns &mdash; project-wide applicability does not mean it must always be loaded",
"The root CLAUDE.md",
"A skill",
"Nowhere"],
c:0,
w:["Correct. Applicability and always-loading are separate questions.",
"CLAUDE.md loads always, which pays context on every unrelated request.",
"Skills are invoked on demand; a convention should apply automatically.",
"It is a real convention worth recording."],
e:"Distinguish <em>where</em> a convention applies from <em>when</em> it needs to be in context. A rule that applies project-wide but only matters when editing migrations should be scoped to migration files &mdash; it still applies everywhere in principle and costs nothing on the many requests that touch none. Where the task does not map to file patterns, an unscoped rules file keeps CLAUDE.md focused even though it loads at launch.",
k:"Project-wide applicability does not require always-loading. Scope by the files the convention actually concerns."},

{i:1164,d:3,t:"3.2",s:"CGC",diff:2,
q:"A skill needs to run a sequence of steps that must not be interrupted partway.",
ask:"What should you consider?",
a:["Whether the steps are individually idempotent, since a skill can fail partway and there is no automatic rollback",
"Whether to fork its context",
"Whether to restrict its tools",
"Whether to set a longer effort level"],
c:0,
w:["Correct. Partial completion is the risk, and idempotency is what makes recovery safe.",
"Forking isolates context without addressing partial completion.",
"Tool restriction limits capability, not interruption.",
"Effort affects reasoning, not atomicity."],
e:"Skills are not transactional. A multi-step skill can fail partway, leaving some steps applied and others not &mdash; and note that a forked skill's edits fall outside session checkpoints, so <code>/rewind</code> will not undo them either. Design the steps to be individually idempotent so a re-run is safe, and record progress if the sequence is long enough that resuming matters.",
k:"Skills are not transactional and forked-skill edits escape <code>/rewind</code>. Make multi-step sequences idempotent."},

{i:1165,d:3,t:"3.3",s:"CGC",diff:2,
q:"You want a convention to load for a file type only in a repository where that file type is common.",
ask:"What determines whether this is worth scoping?",
a:["How often non-matching work occurs &mdash; if most work touches those files, the saving is small and an unscoped rule is simpler",
"The rule's length",
"The number of rules already present",
"Whether the file type is important"],
c:0,
w:["Correct. The saving from scoping is proportional to how often the rule would otherwise load unnecessarily.",
"Length affects the cost when it loads, not how often.",
"Rule count is not the criterion.",
"Importance does not determine loading frequency."],
e:"Path scoping saves context in proportion to how often the rule would otherwise load without being needed. In a repository that is 90% Python, scoping a Python convention saves little. In a polyglot monorepo it saves a great deal. Scope where the saving is real, and prefer the simpler unscoped rule where it is not &mdash; correctness is unaffected either way when the rule genuinely applies project-wide.",
k:"Path scoping saves in proportion to how often the rule would load unnecessarily. Little saving means prefer simplicity."},

{i:1166,d:3,t:"3.4",s:"CGC",diff:2,
q:"You want a record of why an approach was chosen, available to future sessions.",
ask:"Where should it live?",
a:["The plan file or a decision record in the repository, since a session's reasoning does not survive the session",
"The session transcript",
"A code comment",
"The pull request description"],
c:0,
w:["Correct. Durable artefacts in the repository survive and are readable by future sessions and colleagues.",
"Transcripts are personal, lossy under compaction, and not resumable by others.",
"A comment at one site cannot carry a decision spanning several modules or the alternatives rejected.",
"PR descriptions are historical and nothing consults them."],
e:"Recorded rationale does double duty. It stops a settled question being re-litigated, by a person or by a future session working from the plan file. And it makes partial rollback possible &mdash; when a decision proves wrong, the record shows which phases depended on it. Record what was decided, what alternatives were considered, and why the chosen one won.",
k:"Record decisions and rationale in the repository. It prevents re-litigation and makes partial rollback possible."},

{i:1167,d:3,t:"3.5",s:"CGC",diff:2,
q:"You want to reduce the number of iterations needed on a class of task.",
ask:"What is the durable improvement?",
a:["Turning the corrections you keep giving into documented conventions, so they apply automatically next time",
"Giving longer feedback",
"Using a more capable model",
"Accepting more iterations"],
c:0,
w:["Correct. A correction given twice belongs in configuration, where it applies without being restated.",
"Length does not prevent the same correction next time.",
"Capability does not supply project-specific conventions.",
"Accepting the cost forgoes the available improvement."],
e:"This is the documented CLAUDE.md trigger stated as a workflow improvement: when you type the same correction you typed last session, write it down. Each such correction converted into a convention removes one iteration from every future task of that class &mdash; and if it applies only to particular files, scope it so it costs nothing elsewhere.",
k:"Convert repeated corrections into conventions. Each one removes an iteration from every future task of that class."},

{i:1168,d:3,t:"3.6",s:"CCI",diff:2,
q:"You want CI review findings to be trusted after a period of poor precision.",
ask:"What rebuilds trust fastest?",
a:["Narrowing to the categories with demonstrated high precision, showing consistent value, then reintroducing categories once their criteria are sharpened",
"Increasing the number of findings",
"Adding a disclaimer about accuracy",
"Explaining the improvements in a message"],
c:0,
w:["Correct. Demonstrated precision on a narrow set rebuilds trust; announcements do not.",
"More findings after a precision problem reinforces the impression of noise.",
"A disclaimer signals unreliability.",
"Claims of improvement are not evidence of it."],
e:"Trust is rebuilt by observed behaviour. Disable the noisy categories, run only what has demonstrated precision, and let developers experience findings that are consistently worth reading. Then reintroduce categories one at a time, each with explicit criteria and measured precision. This is the documented remedy extended over time &mdash; and it matters because trust is global rather than per-category.",
k:"Rebuild trust by running only high-precision categories, then reintroducing others one at a time with measured precision."},

{i:1169,d:4,t:"4.1",s:"SDE",diff:2,
q:"You want criteria that a reviewer and the model would apply the same way.",
ask:"What is the test?",
a:["Give the same cases to both and compare classifications, since divergence identifies exactly where the criteria are ambiguous",
"Ask the reviewer whether the criteria seem clear",
"Count the criteria",
"Check the criteria are short"],
c:0,
w:["Correct. Comparing classifications on the same cases localises the ambiguity precisely.",
"Perceived clarity is a poor predictor of consistent application.",
"Count is unrelated.",
"Brevity does not confer clarity."],
e:"Divergence between a human reviewer and the model on the same cases is diagnostic: those specific cases are where the criteria are ambiguous. Sharpen them with the discriminating feature and add the case as a contrastive example. If two <em>humans</em> also diverge on those cases, the field is genuinely subjective &mdash; which is workable with borderline examples and an <code>unclear</code> option, but sets a realistic accuracy ceiling.",
k:"Compare human and model classifications on the same cases. Divergence localises the ambiguity; human divergence means subjectivity."},

{i:1170,d:4,t:"4.2",s:"SDE",diff:2,
q:"You want an example set that stays useful as the input distribution shifts.",
ask:"What practice supports this?",
a:["Reviewing whether the examples still represent the current distribution when monitoring shows it has shifted",
"Adding examples continuously",
"Never changing the examples",
"Choosing examples randomly"],
c:0,
w:["Correct. Examples represent a distribution, so they need review when the distribution moves.",
"Continuous addition grows the recurring cost without ensuring representativeness.",
"A frozen set drifts from the inputs it was meant to represent.",
"Random selection does not target the failure region."],
e:"Examples encode a distribution, and distributions shift &mdash; a new supplier template, a change in document mix. Your per-field null-rate and distribution monitoring is the signal that this has happened; when it fires, check whether the examples still represent what you actually process, and replace the ones that no longer do. Otherwise you are teaching yesterday's inputs.",
k:"Examples encode a distribution. Review them when monitoring shows the input mix has shifted."},

{i:1171,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want a schema that supports both a strict pipeline and a more permissive exploratory one.",
ask:"What is the reasonable arrangement?",
a:["One schema with the constraints the strict pipeline needs, and the permissive pipeline relaxing validation downstream rather than using a looser schema",
"Two schemas",
"One loose schema with strict validation downstream",
"One schema per consumer"],
c:0,
w:["Correct. One schema, differing validation strictness, keeps a single contract.",
"Two schemas drift apart and double the maintenance.",
"A loose schema forfeits the structural guarantees the strict pipeline needs at extraction time.",
"Per-consumer schemas is the same drift problem multiplied."],
e:"Keep one contract and vary the downstream strictness. The schema's job is to guarantee structure at extraction time, which benefits both pipelines. Domain validation is where strictness legitimately differs &mdash; the strict pipeline enforces every plausibility and cross-field rule, the exploratory one records violations without rejecting. One schema, two validation policies.",
k:"One schema for structure; vary domain-validation strictness per pipeline. Multiple schemas drift apart."},

{i:1172,d:4,t:"4.4",s:"SDE",diff:2,
q:"You want to know whether a validation rule is still needed.",
ask:"What would tell you?",
a:["How often it fires and whether those firings are genuine &mdash; a rule that never fires may be redundant, or may be preventing an error that no longer occurs",
"How long it takes to run",
"How many lines it is",
"When it was added"],
c:0,
w:["Correct, including the ambiguity that a never-firing rule may guard against a fixed problem.",
"Runtime is a cost input.",
"Implementation size is irrelevant.",
"Age does not determine necessity."],
e:"Interpreting a never-firing rule needs care. It may be redundant &mdash; superseded by a schema constraint that now catches the same thing earlier. Or it may guard against an error that stopped occurring because a prompt or schema change fixed it, in which case removing the rule removes the safety net. Check what changed before removing, and prefer moving the check into the schema where it can be expressed.",
k:"A never-firing rule may be redundant or may be guarding a fixed problem. Check what changed before removing."},

{i:1173,d:4,t:"4.5",s:"SDE",diff:2,
q:"You want batch results to be usable as soon as any are available rather than waiting for the whole batch.",
ask:"What supports this?",
a:["Smaller batches, since results become available per batch rather than per item within one",
"A longer processing window",
"More retries",
"A larger batch"],
c:0,
w:["Correct. Batch granularity determines how early partial results become usable.",
"A longer window delays availability.",
"Retries do not affect availability timing.",
"A larger batch delays the first available results."],
e:"Splitting a corpus into several batches buys earlier partial availability and better failure isolation &mdash; a systematic problem surfaces after 1,000 items rather than 10,000. What it does not buy is a completion guarantee, since there is no latency SLA regardless of how you slice it. Sample-testing the prompt first remains the real protection against a systematic problem.",
k:"Smaller batches give earlier partial results and failure isolation &mdash; not faster completion. Sample-test regardless."},

{i:1174,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want to know whether review criteria are drifting from what the team values.",
ask:"What signal would show this?",
a:["The rate at which findings are dismissed without action, segmented by category",
"The total number of findings",
"The review's runtime",
"The number of criteria"],
c:0,
w:["Correct. Dismissal rate by category is direct evidence of what developers do not value.",
"A total does not identify which categories are unvalued.",
"Runtime is a cost metric.",
"Criteria count is unrelated."],
e:"Dismissal rate is a useful complement to precision. A category dismissed consistently is either producing false positives &mdash; sharpen or disable it &mdash; or producing technically-correct findings the team does not act on, which means it is not worth its noise. Both warrant a change, and segmenting by category is what tells you which category to look at.",
k:"Segment dismissal rate by category. Consistent dismissal means false positives or findings the team does not value."},

{i:1175,d:5,t:"5.1",s:"CAP",diff:2,
q:"You want to know whether summarisation is losing something important.",
ask:"What check would show this?",
a:["Comparing specific facts before and after summarisation on representative conversations, since the loss is predictable and testable",
"Measuring the summary's length",
"Asking the model whether it lost anything",
"Counting the turns summarised"],
c:0,
w:["Correct. The loss is predictable &mdash; numbers, dates, specifics &mdash; so test exactly those.",
"Compression ratio does not indicate what was lost.",
"Self-assessment of loss is unreliable.",
"Turn count does not indicate what was preserved."],
e:"Summarisation degrades in a known direction: numeric values, percentages and dates condense into vague qualitative statements. That makes it testable &mdash; take representative conversations, note the specific facts, summarise, and check which survive. The result usually confirms why those facts belong in a code-maintained block that never passes through summarisation at all.",
k:"Test summarisation by checking whether specific values survive. The loss is predictable: numbers, dates, specifics."},

{i:1176,d:5,t:"5.2",s:"CSA",diff:2,
q:"You want escalation to feel like a continuation rather than a restart to the customer.",
ask:"What is required?",
a:["The human receiving enough context to continue the conversation without re-asking, which is exactly what the structured handoff provides",
"A faster human response",
"A warmer handoff message",
"A shorter queue"],
c:0,
w:["Correct. Continuity is a function of what the human receives.",
"Speed helps the wait without preventing the restart feeling.",
"Tone does not supply the facts that avoid re-asking.",
"Queue depth affects waiting, not continuity."],
e:"The restart feeling comes from being asked again for things already provided. The structured handoff prevents it &mdash; identifier, issue, verified facts, what was tried and the outcome &mdash; because the human can pick up rather than re-establish. Ask the receiving humans what they still end up looking up; that gap is what the handoff is missing.",
k:"Continuity comes from the handoff's completeness. Ask receiving humans what they still have to look up."},

{i:1177,d:5,t:"5.3",s:"MAR",diff:2,
q:"You want to know whether errors are being handled at the right level.",
ask:"What would indicate they are not?",
a:["A high proportion of escalated errors that the coordinator resolved by retrying &mdash; work the subagent could have done locally",
"A high total error count",
"Long error messages",
"Many error categories"],
c:0,
w:["Correct. Coordinator-level retries of transient failures indicate weak local recovery.",
"Volume does not indicate the handling level.",
"Message length is not a level signal.",
"Category count reflects taxonomy granularity."],
e:"Handle errors at the lowest level capable of resolving them. If the coordinator frequently resolves an escalated error by simply retrying, that transient failure should have been retried locally in the subagent &mdash; escalating it consumed coordinator attention for nothing. Measuring local recovery rate against escalation rate is what surfaces this skew.",
k:"Coordinator retries of escalated errors indicate weak local recovery. Measure local recovery against escalation rate."},

{i:1178,d:5,t:"5.4",s:"DPT",diff:2,
q:"You want a colleague to be able to continue an investigation without talking to you.",
ask:"What must the artefacts contain?",
a:["Conclusions with evidence, decisions with rationale, and the open questions &mdash; so they know what is established, why, and what remains",
"A list of files examined",
"The commands run",
"A summary of progress"],
c:0,
w:["Correct. Those three cover what is known, why it is trusted, and what is left.",
"Activity records do not convey findings.",
"Command history is process detail.",
"Progress without content does not enable continuation."],
e:"Handover needs three things. Conclusions with citable evidence &mdash; paths, lines, quotes &mdash; so they can verify rather than re-derive. Decisions with rationale so settled questions stay settled. Open questions so they know where to start and do not assume everything was resolved. All three live in durable files, since sessions do not transfer.",
k:"Handover artefacts: conclusions with evidence, decisions with rationale, open questions. All in durable files."},

{i:1179,d:5,t:"5.5",s:"SDE",diff:2,
q:"You want to know whether a confidence threshold is set correctly.",
ask:"What would you examine?",
a:["The measured error rate just above the threshold, and the proportion of correct items just below it &mdash; both sides of the boundary",
"The overall accuracy",
"The proportion of items above the threshold",
"The average confidence"],
c:0,
w:["Correct. A threshold is a boundary and both sides need examining.",
"Overall accuracy does not describe behaviour at the boundary.",
"Volume above the threshold is not a correctness measure.",
"An average says nothing about the boundary."],
e:"A threshold has two failure directions. Above it, the error rate is what you are accepting unreviewed &mdash; too high means it is set too low. Below it, correct items are being sent to review unnecessarily &mdash; a 98% approval rate in the review queue means it is set too conservatively. Examine both, and set the threshold from the measured error rate combined with what an error costs.",
k:"Examine both sides of a threshold: error rate above it, correct-item rate below it. Both directions are failures."},

{i:1180,d:5,t:"5.6",s:"MAR",diff:2,
q:"You want a report to distinguish what the research established from what it inferred.",
ask:"What supports this?",
a:["Marking derived claims as inferences with their supporting inputs, so readers can evaluate the reasoning separately from the evidence",
"A confidence score per claim",
"A longer methodology section",
"Separating the report into sections"],
c:0,
w:["Correct. Marking the epistemic status lets readers evaluate the inference itself.",
"An uncalibrated score does not distinguish evidence from inference.",
"Methodology describes the process rather than labelling individual claims.",
"Sectioning by topic does not separate established from inferred."],
e:"Synthesis legitimately produces claims no single source made, and that is much of its value &mdash; but presenting them as directly sourced misleads. Mark them as inferences, cite the inputs, and state the reasoning briefly. That lets a reader evaluate the inference on its merits, which they cannot do if it is indistinguishable from a sourced fact.",
k:"Mark inferences as inferences, cite the inputs, state the reasoning. Otherwise readers cannot evaluate them."},

{i:1181,d:1,t:"1.1",s:"AAT",diff:2,
q:"You want to reduce the cost of a loop where the system prompt and tools are large and the conversation is short.",
ask:"What is the highest-value lever?",
a:["Prompt caching on the stable prefix, since with a short conversation the fixed overhead dominates the total",
"Trimming the conversation",
"Reducing <code>max_tokens</code>",
"Reducing the iteration cap"],
c:0,
w:["Correct. When fixed overhead dominates, caching it is the lever with the most leverage.",
"A short conversation is not where the cost is.",
"Output limits do not address the large input prefix.",
"Fewer iterations reduces total requests but each still carries the full prefix."],
e:"Identify which term dominates before optimising. Short conversations with large prefixes are dominated by fixed overhead &mdash; system prompt and tool definitions re-sent every request &mdash; which prompt caching addresses directly. Long conversations with small prefixes are dominated by accumulated history and tool results, where trimming and externalising are the levers.",
k:"Short conversation, large prefix &rarr; cache the prefix. Long conversation &rarr; trim accumulation. Find the dominant term."},

{i:1182,d:1,t:"1.3",s:"AAT",diff:2,
q:"A subagent is given a task it can complete but which is not what the coordinator meant.",
ask:"What would have prevented this?",
a:["A goal statement specific enough that a competent stranger could not misread it, since the subagent has no other context to disambiguate from",
"A longer prompt",
"More tools",
"A more capable model"],
c:0,
w:["Correct. A goal that admits a plausible misreading will sometimes be misread.",
"Length without precision does not remove ambiguity.",
"Capability does not resolve an ambiguous instruction.",
"A capable model produces a well-formed answer to whichever reading it took."],
e:"The competent-stranger test catches this. If a capable person with no access to your conversation could plausibly read the goal two ways, the subagent will sometimes take the reading you did not mean &mdash; and produce a well-formed answer to the wrong question, which looks like success. State the goal so that only one reading is plausible.",
k:"Write goals so a competent stranger cannot misread them. A well-formed answer to the wrong question looks like success."},

{i:1183,d:2,t:"2.2",s:"AAT",diff:2,
q:"You want a tool's error handling to be consistent whether the failure originates locally or upstream.",
ask:"What achieves this?",
a:["Mapping every failure source into your own error contract at the tool boundary, so callers see one taxonomy",
"Passing upstream errors through unchanged",
"Handling only local failures",
"Using the upstream service's error format"],
c:0,
w:["Correct. Translate at your boundary so callers see one consistent contract.",
"Pass-through exposes callers to another service's taxonomy and inconsistency.",
"Ignoring upstream failures leaves them unclassified.",
"Adopting the upstream format couples your contract to theirs."],
e:"Your tool is an adapter, and part of its job is presenting one taxonomy regardless of where a failure originated. Map each upstream condition to your category and retryability flag, write a message in the caller's terms, and keep the raw upstream detail in your logs. That also insulates callers when the upstream service changes its error format.",
k:"Map every failure source into your own contract at the tool boundary. It insulates callers from upstream changes."},

{i:1184,d:3,t:"3.1",s:"CGC",diff:2,
q:"You want to prevent CLAUDE.md from growing without limit as the team adds to it.",
ask:"What practice helps most?",
a:["Requiring that each addition either replaces something or justifies its recurring context cost",
"Setting a line limit",
"Reviewing it annually",
"Assigning an owner"],
c:0,
w:["Correct. Making the cost visible at the moment of addition is what prevents accretion.",
"A limit prompts trimming without guiding what to trim.",
"Annual review lets a year of accretion accumulate first.",
"An owner becomes a bottleneck and lacks context on every change."],
e:"Accretion happens because additions are cheap at the moment they are made and their cost is paid later, on every request. Make the cost visible then: does this replace something, or does it justify loading on every request forever? Combined with pruning as part of the change that makes an entry obsolete, that keeps the file near the documented target of under 200 lines.",
k:"Make the recurring cost visible at the moment of addition. Accretion happens because the cost is paid later."},

{i:1185,d:4,t:"4.1",s:"SDE",diff:2,
q:"You want criteria that work for a field whose correct value depends on context elsewhere in the document.",
ask:"What must the criteria include?",
a:["How to find and use that context, since a criterion referring to it without saying where to look is not applicable",
"A stricter definition of the field",
"An example of the field alone",
"A note that context matters"],
c:0,
w:["Correct. Context-dependent criteria must state how to obtain the context.",
"Defining the field more tightly does not help if its value depends on elsewhere.",
"An isolated example omits the dependency.",
"Noting that context matters without saying which context is not actionable."],
e:"Context-dependent fields need the dependency made explicit: which other part of the document determines this value, how it is identified, and how the two combine. Consider extracting the context field too, so validation can check the relationship &mdash; the same reasoning as extracting both computed and stated totals so a discrepancy becomes visible to code.",
k:"State how to find context-dependent values, and extract the context field too so validation can check the relationship."},

{i:1186,d:5,t:"5.1",s:"CAP",diff:2,
q:"You want the agent to handle a customer who contradicts something in the facts block.",
ask:"What should happen?",
a:["Surface the discrepancy and resolve it &mdash; the block is authoritative for what was verified, but a customer correcting a verified fact needs investigating rather than either overwriting or ignoring",
"Update the block to the customer's version",
"Ignore the customer and use the block",
"Escalate immediately"],
c:0,
w:["Correct. A contradiction between verified state and a customer statement is a signal, not a silent overwrite.",
"Overwriting verified state on an unverified statement discards the verification.",
"Ignoring a customer correction is poor service and may miss a genuine error.",
"Escalation may follow but the first step is surfacing and investigating."],
e:"The block holds facts your system verified, so a customer contradicting one is informative in either direction: the verification may have been wrong, or the customer may be mistaken, or there may be two records. Surface it, re-verify where possible, and escalate if it cannot be resolved. What to avoid is silently choosing &mdash; either overwriting verified state or ignoring the customer.",
k:"A customer contradicting verified state is a signal to investigate, not to silently overwrite or ignore."},

{i:1187,d:1,t:"1.4",s:"CSA",diff:2,
q:"You want to know whether a control is placed at the right layer.",
ask:"What is the test?",
a:["Whether every path to the restricted outcome passes through it &mdash; a control not on every path is a control with a gap",
"Whether it is fast",
"Whether it is easy to change",
"Whether it is well tested"],
c:0,
w:["Correct. Coverage of every path is what determines whether the layer is right.",
"Speed matters operationally, not for placement.",
"Changeability is a separate property.",
"Testing a control with a gap tests the gap too."],
e:"Placement is determined by convergence. Enumerate the paths to the restricted outcome &mdash; other tools, split operations, other callers such as scheduled jobs &mdash; and place the control where all of them converge. That is usually the resource itself when you own it, or the boundary you control when you do not. A hook remains valuable as an earlier, more explainable layer on top.",
k:"Place controls where every path converges. Enumerate alternative tools, split operations and other callers first."},

{i:1188,d:2,t:"2.1",s:"AAT",diff:2,
q:"You want to know whether a tool's description is the reason it is under-used.",
ask:"What would distinguish that from the tool simply not being needed?",
a:["Whether the situations it is meant for actually arise and something else is being used instead &mdash; if they arise and it is not selected, the description is the problem",
"How long the description is",
"How recently it was added",
"How many other tools exist"],
c:0,
w:["Correct. Look at whether its situations arise and what gets selected when they do.",
"Length does not distinguish the two explanations.",
"Recency is unrelated.",
"Toolset size affects selection generally but does not distinguish these two cases."],
e:"Two explanations for an under-used tool, with different remedies. If its situations never arise, the role does not need it &mdash; remove it and reclaim the tokens and choice space. If they arise and something else is chosen instead, the description or name is failing, and the fix is to state the trigger conditions and the boundary against whatever is being chosen. Look at the trace to tell which.",
k:"Under-used tool: check whether its situations arise. Never arising means remove; arising means fix the description."},

{i:1189,d:3,t:"3.2",s:"CGC",diff:2,
q:"You want a skill to be safe for a new team member to invoke without understanding its internals.",
ask:"What supports this?",
a:["A clear description of what it does and what it changes, plus capability limited to what it needs &mdash; so an uninformed invocation cannot do damage",
"A longer skill body",
"A forked context",
"A more capable model"],
c:0,
w:["Correct. Clear expectations plus limited capability makes uninformed invocation safe.",
"Body length is internal detail the invoker does not read.",
"Forking isolates context without limiting what it can do.",
"Capability does not bound the blast radius."],
e:"Safety for an uninformed invoker has two parts. Expectations: the description says what it does and, crucially, what it changes &mdash; so nobody invokes a deploy skill thinking it is a dry run. Capability: <code>disallowed-tools</code> bounds what it can touch. For skills with genuinely irreversible effects, add <code>disable-model-invocation: true</code> so only deliberate invocation is possible.",
k:"Safe skills state what they change and limit capability. Irreversible ones also set <code>disable-model-invocation</code>."},

{i:1190,d:4,t:"4.3",s:"SDE",diff:2,
q:"You want to add a required field to an existing extraction schema.",
ask:"What should you check first?",
a:["Whether every document genuinely contains it, since making it required creates fabrication pressure on those that do not",
"Whether consumers can handle it",
"Whether it fits the naming convention",
"Whether it increases token cost"],
c:0,
w:["Correct. <code>required</code> on a field that is sometimes absent is what causes fabrication.",
"Consumer compatibility matters, but the fabrication risk is the immediate one.",
"Naming is secondary.",
"Token cost is negligible next to fabricated data."],
e:"Reserve <code>required</code> for fields present in every valid document. Where a field is usually but not always present, make it nullable with an explicit absence representation, and demonstrate the absent case in an example. Note the compatibility point too: adding a required field is a breaking change for consumers, whereas adding an optional one is not.",
k:"Only mark a field <code>required</code> if every valid document has it. Adding a required field is also a breaking change."},

{i:1191,d:5,t:"5.4",s:"DPT",diff:2,
q:"You want to know whether externalising findings is worth the effort on shorter investigations.",
ask:"What determines it?",
a:["Whether the investigation might be interrupted, resumed or handed over &mdash; the value is in surviving a boundary, so short uninterrupted work needs it less",
"The investigation's complexity",
"The number of files examined",
"The model being used"],
c:0,
w:["Correct. The value comes from surviving a boundary, so it scales with the chance of hitting one.",
"Complexity correlates loosely with duration but is not the criterion.",
"File count measures activity.",
"Model choice is unrelated."],
e:"Externalisation buys survival across a boundary &mdash; compaction, a crash, a session change, a handover. A short investigation completed in one sitting hits none of those, so the effort buys little. As duration grows the chance of hitting one rises sharply, which is why the practice matters for long investigations and multi-day work in particular.",
k:"Externalisation buys survival across boundaries. Its value scales with the chance of hitting one."},

{i:1192,d:1,t:"1.2",s:"MAR",diff:2,
q:"You want to know whether a multi-agent design is justified over a single agent.",
ask:"What would settle it?",
a:["Comparing result quality and total cost on the same tasks, since the design trades extra tokens and complexity for isolation and parallelism",
"Counting the agents",
"Measuring latency alone",
"Asking which feels more sophisticated"],
c:0,
w:["Correct. The trade is explicit, so measure both sides on comparable tasks.",
"Agent count is not a quality measure.",
"Latency is one dimension of the trade.",
"Sophistication is not a criterion."],
e:"The trade is stated: multi-agent buys context isolation, scoped toolsets and parallelism, and costs extra tokens because context is re-established per agent, plus coordination complexity. Whether it pays depends on the workload &mdash; measure quality and total cost on the same tasks rather than assuming. For small, tightly-coupled work, a single agent is often better as well as cheaper.",
k:"Multi-agent trades tokens and complexity for isolation and parallelism. Measure both sides on comparable tasks."},

{i:1193,d:2,t:"2.4",s:"DPT",diff:2,
q:"You want to reduce the risk that an MCP server change breaks a CI review.",
ask:"What practice helps?",
a:["Pinning the version used in CI and promoting changes there deliberately, so CI is not affected by an unreviewed update",
"Running CI more often",
"Using a different server in CI",
"Disabling MCP in CI"],
c:0,
w:["Correct. Pinning plus deliberate promotion keeps CI stable.",
"Frequency does not reduce the impact of a breaking change.",
"A different server means CI reviews against different capability than developers have.",
"Disabling MCP removes integrations the review may depend on."],
e:"CI benefits from the same version discipline as development, and it matters more there because a broken review blocks merges. Pin the version, promote deliberately after testing, and keep CI and local using the same pinned version so findings are comparable &mdash; divergent configuration is the usual cause of CI findings differing from local ones.",
k:"Pin MCP versions in CI and promote deliberately. Keep CI and local on the same version so findings are comparable."},

{i:1194,d:3,t:"3.4",s:"CGC",diff:2,
q:"You want to decide quickly whether a task needs planning.",
ask:"What is the fastest useful question?",
a:["Could a reasonable person choose a different approach here? If yes, plan; if there is one obvious way, execute",
"Will this take more than an hour?",
"Does this touch more than five files?",
"Is this in unfamiliar code?"],
c:0,
w:["Correct. Multiple defensible approaches is the core planning trigger and is fast to assess.",
"Duration correlates loosely and is often unknown up front.",
"File count misleads in both directions.",
"Unfamiliarity calls for exploration, which is a different remedy."],
e:"Approach uncertainty is the quickest useful test, and it captures the main trigger. Add consequence as the second check &mdash; a small change to a security-critical path warrants planning despite having one obvious approach. And keep exploration separate: unfamiliar code needs investigation, which is delegable, rather than a plan.",
k:"Ask whether a reasonable person could choose differently. Add consequence as the second check; exploration is separate."},

{i:1195,d:4,t:"4.2",s:"SDE",diff:2,
q:"You want to know whether an example is teaching the intended lesson.",
ask:"What would show this?",
a:["Testing on cases that share the intended principle but differ in surface detail &mdash; if they improve, the principle transferred; if only near-identical cases improve, the surface did",
"Checking the example is realistic",
"Counting how often its pattern appears in output",
"Asking the model what it learned"],
c:0,
w:["Correct. Transfer to structurally-similar but superficially-different cases is the test of what was learned.",
"Realism is necessary but does not show what transferred.",
"Pattern frequency conflates the example's influence with the input distribution.",
"Self-reported learning is unreliable."],
e:"Examples can teach the principle or the surface, and only a transfer test distinguishes them. Construct cases that share the underlying rule but differ in formatting, vocabulary and layout. Improvement there means the principle transferred; improvement only on near-identical cases means the example taught its surface, which is the over-fitting that varied examples and stated rationale are meant to prevent.",
k:"Test example transfer on structurally-similar, superficially-different cases. Surface-only improvement means over-fitting."},

{i:1196,d:5,t:"5.5",s:"SDE",diff:2,
q:"You want to expand automation to a segment currently routed to review.",
ask:"What is the responsible sequence?",
a:["Measure its accuracy from the review outcomes already collected, set a threshold from that and the cost of an error, then automate above it while sampling",
"Automate it and monitor",
"Automate it with a very high threshold",
"Wait until accuracy improves on its own"],
c:0,
w:["Correct. Review outcomes are already labelled data; use them to measure, then set the threshold deliberately.",
"Automating an unmeasured segment is the decision without the evidence.",
"A high threshold on uncalibrated confidence still has an unknown error rate.",
"Accuracy does not improve without a change."],
e:"The review queue has been generating labelled data all along &mdash; every reviewer correction is ground truth for that segment. Use it to measure accuracy per field, set a threshold combining that error rate with the cost of an error, automate above it, and keep sampling the newly-automated stream to catch what the confidence signal misses.",
k:"Review outcomes are labelled data. Measure the segment from them, set a threshold on cost, automate and keep sampling."},

{i:1197,d:1,t:"1.5",s:"AAT",diff:2,
q:"You want to know whether hooks are contributing to a latency problem.",
ask:"What should you measure?",
a:["Per-hook execution time as a proportion of the turn, since a hook consulting an external service can dominate an otherwise fast call",
"The number of hooks",
"The hooks' code size",
"How often each hook blocks"],
c:0,
w:["Correct. A single hook making a network call can dominate the turn.",
"Count does not indicate cost; nine cheap hooks may cost less than one slow one.",
"Code size is unrelated to execution time.",
"Block frequency measures decisions, not latency."],
e:"Hooks sit in the critical path of every tool call they match, so a single one consulting a slow external service can dominate. Measure per hook. The remedies for an expensive enforcement hook are ordering the checks cheap-first so only relevant cases pay the network call, and caching inputs whose staleness you can tolerate &mdash; without weakening the fail-closed guarantee.",
k:"Measure per-hook execution time. One hook making a network call can dominate the turn; order checks cheap-first."},

{i:1198,d:2,t:"2.5",s:"DPT",diff:2,
q:"An agent needs to check something in a file it read earlier in the same session.",
ask:"Should it re-read?",
a:["Not if the content is still in context and nothing has changed it &mdash; but it should re-read if the file may have been modified since",
"Always re-read to be safe",
"Never re-read within a session",
"Re-read only if the file is large"],
c:0,
w:["Correct. The deciding factor is whether the content might have changed since it was read.",
"Always re-reading wastes context on unchanged files.",
"Never re-reading means acting on stale content after a modification.",
"Size is not the criterion."],
e:"Context holds a snapshot from when the file was read. Within a session where nothing modified it, that snapshot is current and re-reading wastes context. After the agent itself edited it, or after an external change, the snapshot is stale &mdash; and this is the same staleness question as resuming a session, at a smaller scale. Track what has been modified since reading.",
k:"Re-read only if the file may have changed since. Context holds a snapshot, and edits make it stale."},

{i:1199,d:3,t:"3.6",s:"CCI",diff:2,
q:"You want a CI review to work identically for a developer running it locally.",
ask:"What must be true?",
a:["The same configuration, input scoping and criteria apply in both, so results are comparable and a developer can reproduce a CI finding",
"The same machine specification",
"The same time of day",
"The same model version only"],
c:0,
w:["Correct. Comparable results require the same configuration, input and criteria.",
"Machine specification does not affect findings.",
"Timing is irrelevant.",
"Version pinning helps but configuration and input scoping matter more."],
e:"Divergence between local and CI review is usually a configuration or input-scoping difference &mdash; setting sources excluding project rules, a different working directory changing which CLAUDE.md files load, or a different diff scope. Path-scoped rules help here because the same configuration governs both automatically. When findings differ, check what loaded before suspecting the model.",
k:"Local and CI review must share configuration, input scoping and criteria. Divergence is usually a loading difference."},

{i:1200,d:4,t:"4.6",s:"CCI",diff:2,
q:"You want to know whether a review architecture change is safe to keep.",
ask:"What evidence would justify keeping it?",
a:["Improved detection of genuine defects without a proportional rise in false positives, measured on a fixed labelled set",
"Fewer comments",
"Faster runtime",
"Positive developer feedback in the first week"],
c:0,
w:["Correct. Both dimensions on a controlled comparison is what justifies keeping a change.",
"Fewer comments could mean less noise or less detection.",
"Speed is a cost improvement rather than a quality one.",
"Early feedback is noisy and often reacts to novelty."],
e:"Architecture changes need controlled evaluation on both dimensions, because a change that raises recall while raising false positives may be a net loss &mdash; false positives erode the trust that makes every finding actionable. Use a fixed set of changes with known defects so the comparison is controlled, and report detection rate and false positives together.",
k:"Justify architecture changes with detection rate <em>and</em> false positives on a fixed labelled set. Either alone misleads."}
);
